{% set version = '2.2-0' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-spatstat.random
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/spatstat.random_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/spatstat.random/spatstat.random_{{ version }}.tar.gz
  sha256: 45f0bbdb9dbd53b6c4151c3cdd098451cf787729717ccbb063cd1f33910e604d

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: spatial, RandomFields (>= 3.1.24.1), RandomFieldsUtils(>= 0.3.3.1), spatstat.linnet (>= 2.0-0), spatstat.core (>= 2.4-0), spatstat (>= 2.3-3)
requirements:
  build:
    - {{ compiler('c') }}              # [not win]
    - {{ compiler('m2w64_c') }}        # [win]
    - {{ compiler('cxx') }}            # [not win]
    - {{ compiler('m2w64_cxx') }}      # [win]
    - {{ posix }}filesystem        # [win]
    - {{ posix }}make
    - {{ posix }}sed               # [win]
    - {{ posix }}coreutils         # [win]
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-spatstat.data >=2.1_0
    - r-spatstat.geom >=2.4_0
    - r-spatstat.utils >=2.2_0

  run:
    - r-base
    - {{native}}gcc-libs         # [win]
    - r-spatstat.data >=2.1_0
    - r-spatstat.geom >=2.4_0
    - r-spatstat.utils >=2.2_0

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('spatstat.random')"           # [not win]
    - "\"%R%\" -e \"library('spatstat.random')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: http://spatstat.org/
  license: GPL-2
  summary: Functionality for random generation of spatial data in the 'spatstat' family of packages.
    Generates random spatial patterns of points according to many simple rules (complete
    spatial randomness, Poisson, binomial, random grid, systematic, cell), randomised
    alteration of patterns (thinning, random shift, jittering),  simulated realisations
    of random point processes (simple sequential inhibition, Matern inhibition models,
    Matern cluster process, Neyman-Scott cluster processes, log-Gaussian Cox processes,
    product shot noise cluster processes) and simulation of Gibbs point processes (Metropolis-Hastings
    birth-death-shift algorithm, alternating Gibbs sampler). Also generates random spatial
    patterns of line segments, random tessellations, and random images (random noise,
    random mosaics). Excludes random generation on a linear network, which is covered
    by the separate package 'spatstat.linnet'.
  license_family: GPL2
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-2'

# The original CRAN metadata for this package was:

# Package: spatstat.random
# Version: 2.2-0
# Date: 2022-03-29
# Title: Random Generation Functionality for the 'spatstat' Family
# Authors@R: c(person("Adrian", "Baddeley", role = c("aut", "cre"), email = "Adrian.Baddeley@curtin.edu.au"), person("Rolf", "Turner", role = "aut", email="r.turner@auckland.ac.nz"), person("Ege",   "Rubak", role = "aut", email = "rubak@math.aau.dk"), person("Kasper", "Klitgaard Berthelsen", role = "ctb"), person("Tilman", "Davies", role = "ctb"), person("Ute", "Hahn", role = "ctb"), person("Abdollah", "Jalilian", role = "ctb"), person("Dominic", "Schuhmacher", role = "ctb"), person("Rasmus", "Plenge Waagepetersen", role = "ctb"))
# Maintainer: Adrian Baddeley <Adrian.Baddeley@curtin.edu.au>
# Depends: R (>= 3.5.0), spatstat.data (>= 2.1-0), spatstat.geom (>= 2.4-0), stats, utils, methods, grDevices
# Imports: spatstat.utils (>= 2.2-0)
# Suggests: spatial, RandomFields (>= 3.1.24.1), RandomFieldsUtils(>= 0.3.3.1), spatstat.linnet (>= 2.0-0), spatstat.core (>= 2.4-0), spatstat (>= 2.3-3)
# Description: Functionality for random generation of spatial data in the 'spatstat' family of packages. Generates random spatial patterns of points according to many simple rules (complete spatial randomness, Poisson, binomial, random grid, systematic, cell), randomised alteration of patterns (thinning, random shift, jittering),  simulated realisations of random point processes (simple sequential inhibition, Matern inhibition models, Matern cluster process, Neyman-Scott cluster processes, log-Gaussian Cox processes, product shot noise cluster processes) and simulation of Gibbs point processes (Metropolis-Hastings birth-death-shift algorithm, alternating Gibbs sampler). Also generates random spatial patterns of line segments, random tessellations, and random images (random noise, random mosaics). Excludes random generation on a linear network, which is covered by the separate package 'spatstat.linnet'.
# License: GPL (>= 2)
# URL: http://spatstat.org/
# NeedsCompilation: yes
# ByteCompile: true
# BugReports: https://github.com/spatstat/spatstat.random/issues
# Packaged: 2022-03-29 08:40:58 UTC; adrian
# Author: Adrian Baddeley [aut, cre], Rolf Turner [aut], Ege Rubak [aut], Kasper Klitgaard Berthelsen [ctb], Tilman Davies [ctb], Ute Hahn [ctb], Abdollah Jalilian [ctb], Dominic Schuhmacher [ctb], Rasmus Plenge Waagepetersen [ctb]
# Repository: CRAN
# Date/Publication: 2022-03-30 07:30:02 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
