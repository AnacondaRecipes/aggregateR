{% set version = '2.1.1' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-icesadvice
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/icesAdvice_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/icesAdvice/icesAdvice_{{ version }}.tar.gz
  sha256: bc6d942a938dee856953dd0354a3f0f7240ed546b8792087b0b6905ac89f87c4

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base

  run:
    - r-base

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('icesAdvice')"           # [not win]
    - "\"%R%\" -e \"library('icesAdvice')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://ices.dk/advice, https://github.com/ices-tools-prod/icesAdvice
  license: GPL-3
  summary: A collection of functions that facilitate computational steps related to advice for
    fisheries management, according to ICES guidelines. These include methods for calculating
    reference points and model diagnostics.
  license_family: GPL3
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-3'

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: icesAdvice
# Version: 2.1.1
# Title: Functions Related to ICES Advice
# Authors@R: c(person("Arni", "Magnusson", role="aut"), person("Colin", "Millar", role=c("aut","cre"), email="colin.millar@ices.dk"), person("Anne", "Cooper", role="aut"), person("Ruben", "Verkempynck", role="ctb"), person("Raphael", "Girardin", role="ctb"))
# Imports: graphics, stats, utils
# LazyData: yes
# Description: A collection of functions that facilitate computational steps related to advice for fisheries management, according to ICES guidelines. These include methods for calculating reference points and model diagnostics.
# License: GPL-3
# URL: https://ices.dk/advice, https://github.com/ices-tools-prod/icesAdvice
# BugReports: https://github.com/ices-tools-prod/icesAdvice/issues
# Encoding: UTF-8
# RoxygenNote: 7.1.2
# NeedsCompilation: no
# Packaged: 2022-02-17 18:58:12 UTC; colin
# Author: Arni Magnusson [aut], Colin Millar [aut, cre], Anne Cooper [aut], Ruben Verkempynck [ctb], Raphael Girardin [ctb]
# Maintainer: Colin Millar <colin.millar@ices.dk>
# Repository: CRAN
# Date/Publication: 2022-02-18 10:00:02 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
