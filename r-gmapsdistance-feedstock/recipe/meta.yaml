{% set version = '4.0.0' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-gmapsdistance
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/gmapsdistance_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/gmapsdistance/gmapsdistance_{{ version }}.tar.gz
  sha256: 4f1a9ea86338de1afdcacfcfa624f80f50489ea6a38ce768906ffe08aaee29e2

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: covr, knitr, rmarkdown, testthat (>= 3.0.0)
requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-rcurl
    - r-xml

  run:
    - r-base
    - r-rcurl
    - r-xml

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('gmapsdistance')"           # [not win]
    - "\"%R%\" -e \"library('gmapsdistance')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://github.com/jlacko/gmapsdistance
  license: GPL-3
  summary: Get distance and travel time between two points from Google Maps. Four possible modes
    of transportation (bicycling, walking, driving and public transportation).
  license_family: GPL3
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-3'

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: gmapsdistance
# Type: Package
# Title: Distance and Travel Time Between Two Points from Google Maps
# Version: 4.0.0
# Date: 2022-04-26
# Author: Rodrigo Azuero Melo & David Zarruk
# Maintainer: Jindra Lacko <jindra.lacko@gmail.com>
# URL: https://github.com/jlacko/gmapsdistance
# BugReports: https://github.com/jlacko/gmapsdistance/issues
# Description: Get distance and travel time between two points from Google Maps. Four possible modes of transportation (bicycling, walking, driving and public transportation).
# License: GPL (>= 3)
# Imports: RCurl, XML, methods
# RoxygenNote: 7.1.2
# Encoding: UTF-8
# Suggests: covr, knitr, rmarkdown, testthat (>= 3.0.0)
# Config/testthat/edition: 3
# VignetteBuilder: knitr
# NeedsCompilation: no
# Packaged: 2022-04-26 11:50:11 UTC; jindra
# Repository: CRAN
# Date/Publication: 2022-04-26 14:30:02 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
