{% set version = '3.33.1' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-swagger
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/swagger_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/swagger/swagger_{{ version }}.tar.gz
  sha256: 528369b04c6142112e05ee8b950ca1c01b3e390a4515eacb0fa17db98bafafd1

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: jsonlite, plumber, testthat
requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base

  run:
    - r-base

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('swagger')"           # [not win]
    - "\"%R%\" -e \"library('swagger')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://github.com/rstudio/swagger
  license: Apache License 2.0 | file LICENSE
  summary: 'A collection of ''HTML'', ''JavaScript'', and ''CSS'' assets that dynamically generate
    beautiful documentation from a ''Swagger'' compliant API: <https://swagger.io/specification/>.'
  license_family: APACHE
  license_file:
    - LICENSE

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: swagger
# Type: Package
# Title: Dynamically Generates Documentation from a 'Swagger' Compliant API
# Version: 3.33.1
# Authors@R: c( person("Barret", "Schloerke", email = "barret@rstudio.com", role = c("cre", "aut")), person("Javier", "Luraschi", email = "javier@rstudio.com", role = c("aut")), person(family = "RStudio", role = c("cph")), person(family = "SmartBear Software", role = c("aut", "cph")) )
# Suggests: jsonlite, plumber, testthat
# Description: A collection of 'HTML', 'JavaScript', and 'CSS' assets that dynamically generate beautiful documentation from a 'Swagger' compliant API: <https://swagger.io/specification/>.
# License: Apache License 2.0 | file LICENSE
# Encoding: UTF-8
# LazyData: true
# URL: https://github.com/rstudio/swagger
# BugReports: https://github.com/rstudio/swagger/issues
# RoxygenNote: 7.1.1
# NeedsCompilation: no
# Packaged: 2020-10-02 20:56:03 UTC; barret
# Author: Barret Schloerke [cre, aut], Javier Luraschi [aut], RStudio [cph], SmartBear Software [aut, cph]
# Maintainer: Barret Schloerke <barret@rstudio.com>
# Repository: CRAN
# Date/Publication: 2020-10-02 21:10:02 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
