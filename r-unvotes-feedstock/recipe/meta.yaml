{% set version = '0.3.0' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-unvotes
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/unvotes_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/unvotes/unvotes_{{ version }}.tar.gz
  sha256: 5c826e9ab6d6aa6bb52fe936050783e2ce5ba879166ba0444fe2f6e530dfdeed

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: knitr, dplyr, readxl, countrycode, tidyr, lubridate, devtools, rmarkdown, ggplot2
requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base

  run:
    - r-base

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('unvotes')"           # [not win]
    - "\"%R%\" -e \"library('unvotes')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://github.com/dgrtwo/unvotes
  license: MIT
  summary: Historical voting data of the United Nations General Assembly. This includes votes
    for each country in each roll call, as well as descriptions and topic classifications
    for each vote.
  license_family: MIT
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/MIT'
    - LICENSE

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: unvotes
# Type: Package
# Title: United Nations General Assembly Voting Data
# Version: 0.3.0
# Authors@R: c(person("David", "Robinson", email = "admiral.david@gmail.com", role = c("aut", "cre")), person("Nicholas", "Goguen-Compagnoni", email = "n.goguen-compagnoni@sms.ed.ac.uk", role = "ctb"))
# Description: Historical voting data of the United Nations General Assembly. This includes votes for each country in each roll call, as well as descriptions and topic classifications for each vote.
# License: MIT + file LICENSE
# LazyData: TRUE
# URL: https://github.com/dgrtwo/unvotes
# VignetteBuilder: knitr
# BugReports: https://github.com/dgrtwo/unvotes/issues
# Depends: R (>= 2.10)
# Suggests: knitr, dplyr, readxl, countrycode, tidyr, lubridate, devtools, rmarkdown, ggplot2
# RoxygenNote: 7.1.1
# NeedsCompilation: no
# Packaged: 2021-03-09 06:44:24 UTC; davidrobinson
# Author: David Robinson [aut, cre], Nicholas Goguen-Compagnoni [ctb]
# Maintainer: David Robinson <admiral.david@gmail.com>
# Repository: CRAN
# Date/Publication: 2021-03-09 07:50:02 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
