{% set version = '1.1.3' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-species
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/SPECIES_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/SPECIES/SPECIES_{{ version }}.tar.gz
  sha256: 988ebf818a100991d0d62b90b733f514f4a4a41b307c7083e9d153214adc0862

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

requirements:
  build:
    - {{ compiler('c') }}              # [not win]
    - {{ compiler('m2w64_c') }}        # [win]
    - {{ compiler('fortran') }}        # [not win]
    - {{ compiler('m2w64_fortran') }}  # [win]
    - {{ posix }}filesystem        # [win]
    - {{ posix }}make
    - {{ posix }}sed               # [win]
    - {{ posix }}coreutils         # [win]
    - {{ posix }}zip               # [win]

  host:
    - r-base

  run:
    - r-base
    - {{native}}gcc-libs         # [win]

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('SPECIES')"           # [not win]
    - "\"%R%\" -e \"library('SPECIES')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://CRAN.R-project.org/package=SPECIES
  license: GPL-2
  summary: Implementation of various methods in estimation of species richness or diversity in
    Wang (2011)<doi:10.18637/jss.v040.i09>.
  license_family: GPL2
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-2'

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: SPECIES
# Type: Package
# Title: Statistical Package for Species Richness Estimation
# Version: 1.1.3
# Date: 2022-04-25
# Author: Ji-Ping Wang, <jzwang@northwestern.edu>
# Maintainer: Ji-Ping Wang <jzwang@northwestern.edu>
# Description: Implementation of various methods in estimation of species richness or diversity in Wang (2011)<doi:10.18637/jss.v040.i09>.
# License: GPL-2
# Packaged: 2022-04-25 10:06:25 UTC; jon
# Repository: CRAN
# Date/Publication: 2022-04-25 13:40:06 UTC
# NeedsCompilation: yes

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
