{% set version = '2.0' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-rflptools
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/RFLPtools_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/RFLPtools/RFLPtools_{{ version }}.tar.gz
  sha256: 0d40763f67426bdc216665bf58ef96e58138d60381ac39150ec3e3ee3d041c1e

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: knitr, rmarkdown, lattice, MKomics
requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-rcolorbrewer

  run:
    - r-base
    - r-rcolorbrewer

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('RFLPtools')"           # [not win]
    - "\"%R%\" -e \"library('RFLPtools')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://CRAN.R-project.org/package=RFLPtools
  license: LGPL-3
  summary: Provides functions to analyse DNA fragment samples (i.e. derived from RFLP-analysis)
    and standalone BLAST report files (i.e. DNA sequence analysis).
  license_family: LGPL
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/LGPL-3'

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: RFLPtools
# Type: Package
# Title: Tools to Analyse RFLP Data
# Version: 2.0
# Date: 2022-02-07
# Authors@R: c(person("Fabienne", "Flessa", role = "aut", email = "Fabienne.Flessa@uni-bayreuth.de"), person("Alexandra", "Kehl", role = "aut", email = "Alexandra.Kehl@uni-tuebingen.de", comment = c(ORCID = "0000-0002-1547-0713")), person("Mohammed Aslam", "Imtiaz", role = "aut"), person("Matthias", "Kohl", role = c("aut", "cre"), email = "Matthias.Kohl@stamats.de", comment = c(ORCID = "0000-0001-9514-8910")))
# Author: Fabienne Flessa [aut], Alexandra Kehl [aut] (<https://orcid.org/0000-0002-1547-0713>), Mohammed Aslam Imtiaz [aut], Matthias Kohl [aut, cre] (<https://orcid.org/0000-0001-9514-8910>)
# Maintainer: Matthias Kohl <Matthias.Kohl@stamats.de>
# Description: Provides functions to analyse DNA fragment samples (i.e. derived from RFLP-analysis) and standalone BLAST report files (i.e. DNA sequence analysis).
# Depends: R(>= 4.0.0), RColorBrewer
# Imports: stats, utils, graphics, grDevices
# Suggests: knitr, rmarkdown, lattice, MKomics
# VignetteBuilder: knitr
# License: LGPL-3
# NeedsCompilation: no
# Packaged: 2022-02-08 09:24:24 UTC; kohlm
# Repository: CRAN
# Date/Publication: 2022-02-08 09:40:02 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
