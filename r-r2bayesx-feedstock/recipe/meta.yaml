{% set version = '1.1-4' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-r2bayesx
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/R2BayesX_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/R2BayesX/R2BayesX_{{ version }}.tar.gz
  sha256: 2a511cfd305cee49b91f9b490d13b114a15315003394fa73eef5f8c0490e0757

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: interp, coda, maps, MBA, parallel, sf, shapefiles, sp, spdep, splines, rgdal, spData, fields
requirements:
  build:
    - {{ compiler('c') }}              # [not win]
    - {{ compiler('m2w64_c') }}        # [win]
    - {{ posix }}filesystem        # [win]
    - {{ posix }}make
    - {{ posix }}sed               # [win]
    - {{ posix }}coreutils         # [win]
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-bayesxsrc
    - r-colorspace
    - r-mgcv

  run:
    - r-base
    - {{native}}gcc-libs         # [win]
    - r-bayesxsrc
    - r-colorspace
    - r-mgcv

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('R2BayesX')"           # [not win]
    - "\"%R%\" -e \"library('R2BayesX')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://CRAN.R-project.org/package=R2BayesX
  license: GPL-2 | GPL-3
  summary: An R interface to estimate structured additive regression (STAR) models with 'BayesX'.
  license_family: GPL3
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-2'
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-3'

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: R2BayesX
# Version: 1.1-4
# Date: 2023-06-02
# Title: Estimate Structured Additive Regression Models with 'BayesX'
# Authors@R: c(person(given = "Nikolaus", family = "Umlauf", role = c("aut", "cre"), email = "Nikolaus.Umlauf@uibk.ac.at", comment = c(ORCID = "0000-0003-2160-9803")), person(given = "Thomas", family = "Kneib", role = "aut", email = "tkneib@uni-goettingen.de"), person(given = "Stefan", family = "Lang", role = "aut", email = "Stefan.Lang@uibk.ac.at"), person(given = "Achim", family = "Zeileis", role = "aut", email = "Achim.Zeileis@R-project.org", comment = c(ORCID = "0000-0003-0918-3766")))
# Description: An R interface to estimate structured additive regression (STAR) models with 'BayesX'.
# Depends: R (>= 2.13.0), BayesXsrc, colorspace, mgcv
# Suggests: interp, coda, maps, MBA, parallel, sf, shapefiles, sp, spdep, splines, rgdal, spData, fields
# Imports: methods
# License: GPL-2 | GPL-3
# LazyLoad: yes
# NeedsCompilation: yes
# Packaged: 2023-06-02 07:26:46 UTC; nik
# Author: Nikolaus Umlauf [aut, cre] (<https://orcid.org/0000-0003-2160-9803>), Thomas Kneib [aut], Stefan Lang [aut], Achim Zeileis [aut] (<https://orcid.org/0000-0003-0918-3766>)
# Maintainer: Nikolaus Umlauf <Nikolaus.Umlauf@uibk.ac.at>
# Repository: CRAN
# Date/Publication: 2023-06-02 07:50:02 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
