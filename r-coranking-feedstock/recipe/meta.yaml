{% set version = '0.2.4' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-coranking
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/coRanking_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/coRanking/coRanking_{{ version }}.tar.gz
  sha256: 8b8a361f25ebf3c4bced4ca9233b723b1104fad06c9bca914124b7830b002bb0

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: testthat, knitr, rmarkdown, Rtsne, scatterplot3d
requirements:
  build:
    - {{ compiler('c') }}              # [not win]
    - {{ compiler('m2w64_c') }}        # [win]
    - {{ compiler('cxx') }}            # [not win]
    - {{ compiler('m2w64_cxx') }}      # [win]
    - {{ posix }}filesystem        # [win]
    - {{ posix }}make
    - {{ posix }}sed               # [win]
    - {{ posix }}coreutils         # [win]
    - {{ posix }}zip               # [win]

  host:
    - r-base

  run:
    - r-base
    - {{native}}gcc-libs         # [win]

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('coRanking')"           # [not win]
    - "\"%R%\" -e \"library('coRanking')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://www.guido-kraemer.com/software/coranking/
  license: GPL-3
  summary: Calculates the co-ranking matrix to assess the quality of a dimensionality reduction.
  license_family: GPL3
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-3'
    - LICENSE

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: coRanking
# Title: Co-Ranking Matrix
# Version: 0.2.4
# Authors@R: c( person("Guido", "Kraemer", email = "guido.kraemer@uni-leipzig.de", role = c("aut","cre"), comment = c(ORCID = "0000-0003-4865-5041") ) )
# Description: Calculates the co-ranking matrix to assess the quality of a dimensionality reduction.
# URL: https://www.guido-kraemer.com/software/coranking/
# BugReports: https://github.com/gdkrmr/coRanking/issues
# License: GPL-3 | file LICENSE
# Encoding: UTF-8
# Imports: methods, graphics, stats
# RoxygenNote: 7.2.3
# Collate: 'coranking-package.R' 'coranking.R' 'coranking_internals.R' 'lcmc.R' 'criteria.R' 'image.R' 'rankmatrix.R' 'submatrix.R' 'r_nx.R'
# Suggests: testthat, knitr, rmarkdown, Rtsne, scatterplot3d
# VignetteBuilder: knitr
# NeedsCompilation: yes
# Packaged: 2023-03-20 20:20:24 UTC; gkraemer
# Author: Guido Kraemer [aut, cre] (<https://orcid.org/0000-0003-4865-5041>)
# Maintainer: Guido Kraemer <guido.kraemer@uni-leipzig.de>
# Repository: CRAN
# Date/Publication: 2023-03-20 21:20:02 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
