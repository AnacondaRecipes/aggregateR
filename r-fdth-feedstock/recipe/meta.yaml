{% set version = '1.2-6' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-fdth
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/fdth_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/fdth/fdth_{{ version }}.tar.gz
  sha256: 3be014d7fa1359404f13cff70cc01ef64073323150a3d3332cdda2905b9d389d

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-xtable

  run:
    - r-base
    - r-xtable

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('fdth')"           # [not win]
    - "\"%R%\" -e \"library('fdth')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://CRAN.R-project.org/package=fdth
  license: GPL-2
  summary: Perform frequency distribution tables, associated histograms and polygons from vector,
    data.frame and matrix objects for numerical and categorical variables.
  license_family: GPL2
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-2'

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: fdth
# Type: Package
# Title: Frequency Distribution Tables, Histograms and Polygons
# Version: 1.2-6
# Date: 2021-10-11
# Author: Jos Cludio Faria, Enio G. Jelihovschi and Ivan B. Allaman
# Maintainer: Jos Cludio Faria <joseclaudio.faria@gmail.com>
# Depends: R (>= 2.6.0), stats, grDevices, graphics
# Imports: xtable
# Description: Perform frequency distribution tables, associated histograms and polygons from vector, data.frame and matrix objects for numerical and categorical variables.
# License: GPL (>= 2)
# Encoding: latin1
# LazyLoad: yes
# NeedsCompilation: no
# Packaged: 2021-10-15 22:06:59 UTC; jcfaria
# Repository: CRAN
# Date/Publication: 2021-10-16 04:10:07 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
