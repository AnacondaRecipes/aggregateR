{% set version = '1.6.1' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-mgsda
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/MGSDA_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/MGSDA/MGSDA_{{ version }}.tar.gz
  sha256: 2af1d522ca75bb1a39a8a349ab1c07eed62c4254db112cac6f02b6e64812f90e

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

requirements:
  build:
    - {{ compiler('c') }}              # [not win]
    - {{ compiler('m2w64_c') }}        # [win]
    - {{ posix }}filesystem        # [win]
    - {{ posix }}make
    - {{ posix }}sed               # [win]
    - {{ posix }}coreutils         # [win]
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-mass

  run:
    - r-base
    - {{native}}gcc-libs         # [win]
    - r-mass

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('MGSDA')"           # [not win]
    - "\"%R%\" -e \"library('MGSDA')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://CRAN.R-project.org/package=MGSDA
  license: GPL-2
  summary: Implements Multi-Group Sparse Discriminant Analysis proposal of I.Gaynanova, J.Booth
    and M.Wells (2016), Simultaneous sparse estimation of canonical vectors in the p>>N
    setting, JASA <doi:10.1080/01621459.2015.1034318>.
  license_family: GPL2
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-2'

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: MGSDA
# Type: Package
# Title: Multi-Group Sparse Discriminant Analysis
# Version: 1.6.1
# Date: 2023-09-03
# Author: Irina Gaynanova
# Maintainer: Irina Gaynanova <irinagn@umich.edu>
# Description: Implements Multi-Group Sparse Discriminant Analysis proposal of I.Gaynanova, J.Booth and M.Wells (2016), Simultaneous sparse estimation of canonical vectors in the p>>N setting, JASA <doi:10.1080/01621459.2015.1034318>.
# Imports: MASS, stats
# License: GPL (>= 2)
# RoxygenNote: 7.2.3
# NeedsCompilation: yes
# Packaged: 2023-09-03 20:15:39 UTC; irinag
# Repository: CRAN
# Date/Publication: 2023-09-03 21:00:05 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
