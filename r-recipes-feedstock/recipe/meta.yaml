{% set version = '0.2.0' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-recipes
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/recipes_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/recipes/recipes_{{ version }}.tar.gz
  sha256: 3d0073e3eb98ac089a94bf8430f3c50915ff1f495d8e967c37baa6a0f6cea0a4

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: covr, ddalpha, dials (>= 0.0.10.9001), ggplot2, igraph, kernlab, knitr, modeldata, parsnip (>= 0.1.7), RANN, RcppRoll, rmarkdown, rpart, rsample, RSpectra, testthat (>= 3.0.0), workflows, xml2
requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-matrix
    - r-dplyr
    - r-ellipsis
    - r-generics >=0.1.0.9000
    - r-glue
    - r-gower
    - r-hardhat >=0.1.6.9001
    - r-ipred >=0.9_12
    - r-lifecycle
    - r-lubridate
    - r-magrittr
    - r-purrr >=0.2.3
    - r-rlang >=0.4.0
    - r-tibble
    - r-tidyr >=1.0.0
    - r-tidyselect >=1.1.0
    - r-timedate
    - r-vctrs
    - r-withr

  run:
    - r-base
    - r-matrix
    - r-dplyr
    - r-ellipsis
    - r-generics >=0.1.0.9000
    - r-glue
    - r-gower
    - r-hardhat >=0.1.6.9001
    - r-ipred >=0.9_12
    - r-lifecycle
    - r-lubridate
    - r-magrittr
    - r-purrr >=0.2.3
    - r-rlang >=0.4.0
    - r-tibble
    - r-tidyr >=1.0.0
    - r-tidyselect >=1.1.0
    - r-timedate
    - r-vctrs
    - r-withr

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('recipes')"           # [not win]
    - "\"%R%\" -e \"library('recipes')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://github.com/tidymodels/recipes, https://recipes.tidymodels.org/
  license: MIT
  summary: A recipe prepares your data for modeling. We provide an extensible framework for pipeable
    sequences of feature engineering steps provides preprocessing tools to be applied
    to data. Statistical parameters for the steps can be estimated from an initial data
    set and then applied to other data sets. The resulting processed output can then
    be used as inputs for statistical or machine learning models.
  license_family: MIT
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/MIT'
    - LICENSE

extra:
  recipe-maintainers:
    - mingwandroid

# The original CRAN metadata for this package was:

# Package: recipes
# Title: Preprocessing and Feature Engineering Steps for Modeling
# Version: 0.2.0
# Authors@R: c( person("Max", "Kuhn", , "max@rstudio.com", role = c("aut", "cre")), person("Hadley", "Wickham", , "hadley@rstudio.com", role = "aut"), person("RStudio", role = "cph") )
# Description: A recipe prepares your data for modeling. We provide an extensible framework for pipeable sequences of feature engineering steps provides preprocessing tools to be applied to data. Statistical parameters for the steps can be estimated from an initial data set and then applied to other data sets. The resulting processed output can then be used as inputs for statistical or machine learning models.
# License: MIT + file LICENSE
# URL: https://github.com/tidymodels/recipes, https://recipes.tidymodels.org/
# BugReports: https://github.com/tidymodels/recipes/issues
# Depends: dplyr, R (>= 3.1)
# Imports: ellipsis, generics (>= 0.1.0.9000), glue, gower, hardhat (>= 0.1.6.9001), ipred (>= 0.9-12), lifecycle, lubridate, magrittr, Matrix, purrr (>= 0.2.3), rlang (>= 0.4.0), stats, tibble, tidyr (>= 1.0.0), tidyselect (>= 1.1.0), timeDate, utils, vctrs, withr
# Suggests: covr, ddalpha, dials (>= 0.0.10.9001), ggplot2, igraph, kernlab, knitr, modeldata, parsnip (>= 0.1.7), RANN, RcppRoll, rmarkdown, rpart, rsample, RSpectra, testthat (>= 3.0.0), workflows, xml2
# VignetteBuilder: knitr
# RdMacros: lifecycle
# Config/Needs/website: tidyverse/tidytemplate
# Config/testthat/edition: 3
# Encoding: UTF-8
# RoxygenNote: 7.1.2
# NeedsCompilation: no
# Packaged: 2022-02-18 22:37:19 UTC; max
# Author: Max Kuhn [aut, cre], Hadley Wickham [aut], RStudio [cph]
# Maintainer: Max Kuhn <max@rstudio.com>
# Repository: CRAN
# Date/Publication: 2022-02-18 23:20:02 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
