{% set version = '3.3-2' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-actuar
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/actuar_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/actuar/actuar_{{ version }}.tar.gz
  sha256: 6bf7f66ae4762eb4938d3b78395a32ad4d7ce44e7f106135ace2a5be309e477b

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: MASS
requirements:
  build:
    - {{ compiler('c') }}              # [not win]
    - {{ compiler('m2w64_c') }}        # [win]
    - {{ posix }}filesystem        # [win]
    - {{ posix }}make
    - {{ posix }}sed               # [win]
    - {{ posix }}coreutils         # [win]
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-expint

  run:
    - r-base
    - {{native}}gcc-libs         # [win]
    - r-expint

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('actuar')"           # [not win]
    - "\"%R%\" -e \"library('actuar')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://gitlab.com/vigou3/actuar
  license: GPL-2
  summary: 'Functions and data sets for actuarial science: modeling of loss distributions; risk
    theory and ruin theory; simulation of compound models, discrete mixtures and compound
    hierarchical models; credibility theory. Support for many additional probability
    distributions to model insurance loss size and frequency: 23 continuous heavy tailed
    distributions; the Poisson-inverse Gaussian discrete distribution; zero-truncated
    and zero-modified extensions of the standard discrete distributions. Support for
    phase-type distributions commonly used to compute ruin probabilities. Main reference:
    <doi:10.18637/jss.v025.i07>. Implementation of the Feller-Pareto family of distributions:
    <doi:10.18637/jss.v103.i06>.'
  license_family: GPL2
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-2'

# The original CRAN metadata for this package was:

# Package: actuar
# Type: Package
# Title: Actuarial Functions and Heavy Tailed Distributions
# Version: 3.3-2
# Date: 2023-02-06
# Authors@R: c(person("Vincent", "Goulet", role = c("cre", "aut"), email = "vincent.goulet@act.ulaval.ca"), person("Sebastien", "Auclair", role = "ctb"), person("Christophe", "Dutang", role = "aut", email = "dutang@ceremade.dauphine.fr"), person("Walter", "Garcia-Fontes", role = "ctb", email = "walter.garcia@upf.edu"), person("Nicholas", "Langevin", role = "ctb"), person("Xavier", "Milhaud", role = "ctb"), person("Tommy", "Ouellet", role = "ctb"), person("Alexandre", "Parent", role = "ctb"), person("Mathieu", "Pigeon", role = "aut", email = "pigeon.mathieu.2@uqam.ca"), person("Louis-Philippe", "Pouliot", role = "ctb"), person("Jeffrey A.", "Ryan", role = "aut", email = "jeff.a.ryan@gmail.com", comment = "Package API"), person("Robert", "Gentleman", role = "aut", comment = "Parts of the R to C interface"), person("Ross", "Ihaka", role = "aut", comment = "Parts of the R to C interface"), person(family = "R Core Team", role = "aut", comment = "Parts of the R to C interface"), person(family = "R Foundation", role = "aut", comment = "Parts of the R to C interface"))
# Description: Functions and data sets for actuarial science: modeling of loss distributions; risk theory and ruin theory; simulation of compound models, discrete mixtures and compound hierarchical models; credibility theory. Support for many additional probability distributions to model insurance loss size and frequency: 23 continuous heavy tailed distributions; the Poisson-inverse Gaussian discrete distribution; zero-truncated and zero-modified extensions of the standard discrete distributions. Support for phase-type distributions commonly used to compute ruin probabilities. Main reference: <doi:10.18637/jss.v025.i07>. Implementation of the Feller-Pareto family of distributions: <doi:10.18637/jss.v103.i06>.
# Depends: R (>= 4.1.0)
# Imports: stats, graphics, expint
# LinkingTo: expint
# Suggests: MASS
# License: GPL (>= 2)
# URL: https://gitlab.com/vigou3/actuar
# BugReports: https://gitlab.com/vigou3/actuar/-/issues
# Encoding: UTF-8
# LazyData: yes
# Classification/MSC-2010: 62P05, 91B30, 62G32
# NeedsCompilation: yes
# Packaged: 2023-02-07 03:21:40 UTC; vincent
# Author: Vincent Goulet [cre, aut], Sebastien Auclair [ctb], Christophe Dutang [aut], Walter Garcia-Fontes [ctb], Nicholas Langevin [ctb], Xavier Milhaud [ctb], Tommy Ouellet [ctb], Alexandre Parent [ctb], Mathieu Pigeon [aut], Louis-Philippe Pouliot [ctb], Jeffrey A. Ryan [aut] (Package API), Robert Gentleman [aut] (Parts of the R to C interface), Ross Ihaka [aut] (Parts of the R to C interface), R Core Team [aut] (Parts of the R to C interface), R Foundation [aut] (Parts of the R to C interface)
# Maintainer: Vincent Goulet <vincent.goulet@act.ulaval.ca>
# Repository: CRAN
# Date/Publication: 2023-02-07 04:12:31 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
