{% set version = '0.3.2' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-ghibli
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/ghibli_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/ghibli/ghibli_{{ version }}.tar.gz
  sha256: 1f945a60e30a327b5bf5c324c0f15a29377cd918908aa7b80e18f0d3876fc21c

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: ggplot2, dplyr, knitr, rmarkdown, testthat, crayon, cowplot, codemetar
requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-prismatic

  run:
    - r-base
    - r-prismatic

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('ghibli')"           # [not win]
    - "\"%R%\" -e \"library('ghibli')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://ewenme.github.io/ghibli/, https://github.com/ewenme/ghibli
  license: MIT
  summary: Colour palettes inspired by Studio Ghibli <https://en.wikipedia.org/wiki/Studio_Ghibli>
    films, ported to R for your enjoyment.
  license_family: MIT
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/MIT'
    - LICENSE

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: ghibli
# Title: Studio Ghibli Colour Palettes
# Description: Colour palettes inspired by Studio Ghibli <https://en.wikipedia.org/wiki/Studio_Ghibli> films, ported to R for your enjoyment.
# Authors@R: c( person("Ewen", "Henderson", email = "ewenhenderson@gmail.com", role = c("aut", "cre"), comment = c(ORCID = "0000-0002-4748-4693")), person("Danielle", "Desrosiers", role = "ctb"), person("Michael", "Chirico", role = "ctb") )
# Version: 0.3.2
# Depends: R (>= 2.10)
# License: MIT + file LICENSE
# Encoding: UTF-8
# LazyData: true
# Imports: grDevices, prismatic
# Suggests: ggplot2, dplyr, knitr, rmarkdown, testthat, crayon, cowplot, codemetar
# URL: https://ewenme.github.io/ghibli/, https://github.com/ewenme/ghibli
# BugReports: https://github.com/ewenme/ghibli/issues
# RoxygenNote: 7.0.2
# VignetteBuilder: knitr
# NeedsCompilation: no
# Packaged: 2020-04-16 07:24:36 UTC; Ewen
# Author: Ewen Henderson [aut, cre] (<https://orcid.org/0000-0002-4748-4693>), Danielle Desrosiers [ctb], Michael Chirico [ctb]
# Maintainer: Ewen Henderson <ewenhenderson@gmail.com>
# Repository: CRAN
# Date/Publication: 2020-04-16 07:40:02 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
