{% set version = '0.80' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-broman
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/broman_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/broman/broman_{{ version }}.tar.gz
  sha256: ba9ce2209c24ef779cf253c33111a3e9bd943763e5872a6cd76ade89765f680a

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: testthat, devtools, roxygen2
requirements:
  build:
    - {{ compiler('c') }}              # [not win]
    - {{ compiler('m2w64_c') }}        # [win]
    - {{ posix }}filesystem        # [win]
    - {{ posix }}make
    - {{ posix }}sed               # [win]
    - {{ posix }}coreutils         # [win]
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-ggplot2

  run:
    - r-base
    - {{native}}gcc-libs         # [win]
    - r-ggplot2

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('broman')"           # [not win]
    - "\"%R%\" -e \"library('broman')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://github.com/kbroman/broman
  license: GPL-3
  summary: Miscellaneous R functions, including functions related to graphics (mostly for base
    graphics), permutation tests, running mean/median, and general utilities.
  license_family: GPL3
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-3'

# The original CRAN metadata for this package was:

# Package: broman
# Version: 0.80
# Date: 2022-07-08
# Title: Karl Broman's R Code
# Description: Miscellaneous R functions, including functions related to graphics (mostly for base graphics), permutation tests, running mean/median, and general utilities.
# Authors@R: c(person("Karl W", "Broman", role=c("aut", "cre"), email="broman@wisc.edu", comment=c(ORCID = "0000-0002-4914-6671")), person("Aimee Teo", "Broman", role="ctb", comment=c(ORCID = "0000-0003-3783-2807")))
# Author: Karl W Broman [aut, cre] (<https://orcid.org/0000-0002-4914-6671>), Aimee Teo Broman [ctb] (<https://orcid.org/0000-0003-3783-2807>)
# Maintainer: Karl W Broman <broman@wisc.edu>
# Depends: R (>= 2.15.0)
# Imports: utils, graphics, grDevices, stats, ggplot2, grid
# Suggests: testthat, devtools, roxygen2
# License: GPL-3
# URL: https://github.com/kbroman/broman
# BugReports: https://github.com/kbroman/broman/issues
# Encoding: UTF-8
# ByteCompile: true
# LazyData: true
# RoxygenNote: 7.2.0
# NeedsCompilation: yes
# Packaged: 2022-07-08 14:53:09 UTC; kbroman
# Repository: CRAN
# Date/Publication: 2022-07-08 15:30:09 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
