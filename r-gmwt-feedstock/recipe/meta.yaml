{% set version = '1.4' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-gmwt
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/gMWT_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/gMWT/gMWT_{{ version }}.tar.gz
  sha256: 2a96272939fcdd094a14723ef63d72e26bf963d3ca33f8e036fd256ab66a67c7

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

requirements:
  build:
    - {{ compiler('c') }}              # [not win]
    - {{ compiler('m2w64_c') }}        # [win]
    - {{ compiler('cxx') }}            # [not win]
    - {{ compiler('m2w64_cxx') }}      # [win]
    - {{ posix }}filesystem        # [win]
    - {{ posix }}make
    - {{ posix }}sed               # [win]
    - {{ posix }}coreutils         # [win]
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-rcpp >=0.9.13
    - r-rcpparmadillo
    - r-clinfun

  run:
    - r-base
    - {{native}}gcc-libs         # [win]
    - r-rcpp >=0.9.13
    - r-rcpparmadillo
    - r-clinfun

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('gMWT')"           # [not win]
    - "\"%R%\" -e \"library('gMWT')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://CRAN.R-project.org/package=gMWT
  license: GPL-2
  summary: Generalized Mann-Whitney type tests based on probabilistic indices and new diagnostic
    plots, for the underlying manuscript see Fischer, Oja (2015) <doi:10.18637/jss.v065.i09>.
  license_family: GPL2
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-2'

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: gMWT
# Type: Package
# Title: Generalized Mann-Whitney Type Tests
# Version: 1.4
# Date: 2023-04-14
# Authors@R: c(person(given = "Daniel", family = "Fischer", role = c("aut","cre"), email = "daniel.fischer@luke.fi", comment=c(ORCID="0000-0002-7513-683X")), person(given = "Hannu", family = "Oja", role = c("aut", "ths")))
# Maintainer: Daniel Fischer <daniel.fischer@luke.fi>
# Depends: R (>= 3.2), clinfun, Rcpp (>= 0.9.13)
# LinkingTo: Rcpp, RcppArmadillo
# Description: Generalized Mann-Whitney type tests based on probabilistic indices and new diagnostic plots, for the underlying manuscript see Fischer, Oja (2015) <doi:10.18637/jss.v065.i09>.
# License: GPL (>= 2)
# LazyLoad: yes
# NeedsCompilation: yes
# Repository: CRAN
# Packaged: 2023-04-18 11:10:28 UTC; ejo138
# Author: Daniel Fischer [aut, cre] (<https://orcid.org/0000-0002-7513-683X>), Hannu Oja [aut, ths]
# Date/Publication: 2023-04-19 10:20:03 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
