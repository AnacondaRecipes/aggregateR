{% set version = '1.1' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-rmallow
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/RMallow_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/RMallow/RMallow_{{ version }}.tar.gz
  sha256: 92bde4b75ae7bb60aabe451607d15deca123d63847c09c1173398d73c818cae8

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-combinat

  run:
    - r-base
    - r-combinat

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('RMallow')"           # [not win]
    - "\"%R%\" -e \"library('RMallow')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://CRAN.R-project.org/package=RMallow
  license: GPL-2
  summary: An EM algorithm to fit Mallows' Models to full or partial rankings, with or without
    ties. Based on Adkins and Flinger (1998) <doi:10.1080/03610929808832223>.
  license_family: GPL2
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-2'

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: RMallow
# Type: Package
# Title: Fit Multi-Modal Mallows' Models to Ranking Data
# Version: 1.1
# Date: 2020-01-23
# Depends: combinat
# Authors@R: person("Erik", "Gregory", email = "egregory2011@gmail.com", role = c("aut", "cre"))
# Description: An EM algorithm to fit Mallows' Models to full or partial rankings, with or without ties. Based on Adkins and Flinger (1998) <doi:10.1080/03610929808832223>.
# License: GPL (>= 2)
# Packaged: 2020-02-06 17:51:43 UTC; adrien
# Repository: CRAN
# Date/Publication: 2020-02-07 01:10:16 UTC
# NeedsCompilation: no
# Author: Erik Gregory [aut, cre]
# Maintainer: Erik Gregory <egregory2011@gmail.com>

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
