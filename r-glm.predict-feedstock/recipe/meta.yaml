{% set version = '4.2-0' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-glm.predict
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/glm.predict_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/glm.predict/glm.predict_{{ version }}.tar.gz
  sha256: ba37b2b3ca0cded921d83bf467c80e280bdfb3b80289b7f6dfbbe36aacfc2699

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: knitr, rmarkdown, ggplot2, devtools
requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-aer
    - r-mass
    - r-dfidx
    - r-lme4
    - r-mlogit
    - r-nnet
    - r-survey
    - r-survival

  run:
    - r-base
    - r-aer
    - r-mass
    - r-dfidx
    - r-lme4
    - r-mlogit
    - r-nnet
    - r-survey
    - r-survival

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('glm.predict')"           # [not win]
    - "\"%R%\" -e \"library('glm.predict')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://benjaminschlegel.ch/r/glm-predict/
  license: GPL-2
  summary: 'Functions to calculate predicted values and the difference between the two cases
    with confidence interval for lm() [linear model], glm() [generalised linear model],
    glm.nb() [negative binomial model], polr() [ordinal logistic model], multinom()
    [multinomial model] and tobit() [tobit model], svyglm() [survey-weighted generalised
    linear models], lmer() [linear multilevel models] using Monte Carlo simulations
    or bootstrap. Reference: Bennet A. Zelner (2009) <doi:10.1002/smj.783>.'
  license_family: GPL2
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-2'

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: glm.predict
# Type: Package
# Title: Predicted Values and Discrete Changes for GLM
# Version: 4.2-0
# Date: 2022-12-02
# Author: Benjamin Schlegel [aut,cre]
# Maintainer: Benjamin Schlegel <kontakt@benjaminschlegel.ch>
# Description: Functions to calculate predicted values and the difference between the two cases with confidence interval for lm() [linear model], glm() [generalised linear model], glm.nb() [negative binomial model], polr() [ordinal logistic model], multinom() [multinomial model] and tobit() [tobit model], svyglm() [survey-weighted generalised linear models], lmer() [linear multilevel models] using Monte Carlo simulations or bootstrap. Reference: Bennet A. Zelner (2009) <doi:10.1002/smj.783>.
# License: GPL (>= 2)
# Depends: R (>= 3.5.0), stats
# Imports: nnet, AER, survival, MASS, parallel, mlogit, dfidx, survey, lme4
# Suggests: knitr, rmarkdown, ggplot2, devtools
# VignetteBuilder: knitr, rmarkdown
# LazyData: true
# URL: https://benjaminschlegel.ch/r/glm-predict/
# Language: en-US
# RoxygenNote: 7.1.1
# NeedsCompilation: no
# Packaged: 2022-12-02 14:14:10 UTC; benja
# Repository: CRAN
# Date/Publication: 2022-12-02 14:50:02 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
