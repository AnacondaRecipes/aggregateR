{% set version = '1.0.6' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-pade
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/Pade_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/Pade/Pade_{{ version }}.tar.gz
  sha256: f7efdf070f53135b6b141afe647a170cbf6ebfac2ac5efb2afbecf00fdc8532e

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: covr, tinytest
requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base

  run:
    - r-base

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('Pade')"           # [not win]
    - "\"%R%\" -e \"library('Pade')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://github.com/aadler/Pade
  license: GPL-2 | BSD_2_clause
  summary: "Given a vector of Taylor series coefficients of sufficient length as input, the function
    returns the numerator and denominator coefficients for the Pad\xE9 approximant of
    appropriate order (Baker, 1975) <ISBN:9780120748556>."
  license_family: GPL2
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-2'
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/BSD_2_clause'
    - LICENSE

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: Pade
# Type: Package
# Title: Pade Approximant Coefficients
# Version: 1.0.6
# Date: 2023-10-12
# Authors@R: c(person(given="Avraham", family="Adler", role=c("aut", "cph", "cre"), email="Avraham.Adler@gmail.com", comment = c(ORCID = "0000-0002-3039-0703")))
# Description: Given a vector of Taylor series coefficients of sufficient length as input, the function returns the numerator and denominator coefficients for the Pade approximant of appropriate order (Baker, 1975) <ISBN:9780120748556>.
# License: GPL (>= 2) | BSD_2_clause + file LICENSE
# Imports: utils
# Suggests: covr, tinytest
# URL: https://github.com/aadler/Pade
# BugReports: https://github.com/aadler/Pade/issues
# Encoding: UTF-8
# NeedsCompilation: no
# Packaged: 2023-10-12 00:44:50 UTC; Parents
# Author: Avraham Adler [aut, cph, cre] (<https://orcid.org/0000-0002-3039-0703>)
# Maintainer: Avraham Adler <Avraham.Adler@gmail.com>
# Repository: CRAN
# Date/Publication: 2023-10-12 02:20:02 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
