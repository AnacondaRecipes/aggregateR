{% set version = '0.5.5' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-searchtrees
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/SearchTrees_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/SearchTrees/SearchTrees_{{ version }}.tar.gz
  sha256: c980d92dc97cc130571e8caa6c6fd34cc8d604fa922d3a8379a7181a7c481ed8

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

requirements:
  build:
    - {{ compiler('c') }}              # [not win]
    - {{ compiler('m2w64_c') }}        # [win]
    - {{ posix }}filesystem        # [win]
    - {{ posix }}make
    - {{ posix }}sed               # [win]
    - {{ posix }}coreutils         # [win]
    - {{ posix }}zip               # [win]

  host:
    - r-base

  run:
    - r-base
    - {{native}}gcc-libs         # [win]

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('SearchTrees')"           # [not win]
    - "\"%R%\" -e \"library('SearchTrees')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://github.com/gmbecker/SearchTrees
  license: LGPL-3
  summary: The QuadTree data structure is useful for fast, neighborhood-restricted lookups. We
    use it to implement fast k-Nearest Neighbor and Rectangular range lookups in 2 dimenions.
    The primary target is high performance interactive graphics.
  license_family: LGPL
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/LGPL-3'

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: SearchTrees
# Type: Package
# Title: Spatial Search Trees
# Version: 0.5.5
# Date: 2022-10-03
# Author: Gabriel Becker
# Maintainer: Gabriel Becker <gabembecker@gmail.com>
# Description: The QuadTree data structure is useful for fast, neighborhood-restricted lookups. We use it to implement fast k-Nearest Neighbor and Rectangular range lookups in 2 dimenions. The primary target is high performance interactive graphics.
# Depends: methods
# License: LGPL
# LazyLoad: yes
# URL: https://github.com/gmbecker/SearchTrees
# BugReports: https://github.com/gmbecker/SearchTrees/issues
# NeedsCompilation: yes
# Packaged: 2022-10-03 17:57:30 UTC; gabrielbecker
# Repository: CRAN
# Date/Publication: 2022-10-03 22:20:06 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
