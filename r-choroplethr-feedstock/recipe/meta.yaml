{% set version = '3.7.2' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-choroplethr
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/choroplethr_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/choroplethr/choroplethr_{{ version }}.tar.gz
  sha256: 116a2b63b2e60787c5f04917e80bb9c6d1dd4f1c9503717d64ea7ad2d0e99cfa

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: testthat, choroplethrMaps, choroplethrAdmin1 (>= 1.1.0), knitr, rmarkdown, scales
requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-hmisc
    - r-r6
    - r-rgooglemaps
    - r-wdi
    - r-acs
    - r-dplyr
    - r-ggmap
    - r-ggplot2 >=2.0.0
    - r-gridextra
    - r-rvest
    - r-stringr
    - r-tidycensus
    - r-tidyr
    - r-tigris >=1.0
    - r-xml2

  run:
    - r-base
    - r-hmisc
    - r-r6
    - r-rgooglemaps
    - r-wdi
    - r-acs
    - r-dplyr
    - r-ggmap
    - r-ggplot2 >=2.0.0
    - r-gridextra
    - r-rvest
    - r-stringr
    - r-tidycensus
    - r-tidyr
    - r-tigris >=1.0
    - r-xml2

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('choroplethr')"           # [not win]
    - "\"%R%\" -e \"library('choroplethr')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: www.choroplethr.com
  license: BSD_3_clause
  summary: Choropleths are thematic maps where geographic regions, such as states, are colored
    according to some metric, such as the number of people who live in that state. This
    package simplifies this process by 1. Providing ready-made functions for creating
    choropleths of common maps. 2. Providing data and API connections to interesting
    data sources for making choropleths. 3. Providing a framework for creating choropleths
    from arbitrary shapefiles. 4. Overlaying those maps over reference maps from Google
    Maps.
  license_family: BSD
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/BSD_3_clause'
    - LICENSE

# The original CRAN metadata for this package was:

# Package: choroplethr
# Title: Simplify the Creation of Choropleth Maps in R
# Description: Choropleths are thematic maps where geographic regions, such as states, are colored according to some metric, such as the number of people who live in that state. This package simplifies this process by 1. Providing ready-made functions for creating choropleths of common maps. 2. Providing data and API connections to interesting data sources for making choropleths. 3. Providing a framework for creating choropleths from arbitrary shapefiles. 4. Overlaying those maps over reference maps from Google Maps.
# Version: 3.7.2
# Authors@R: c( person("Ari", "Lamstein", , "ari@lamsteinconsulting.com", c("aut", "cre")), person("Brian", "Johnson", , role = "ctb"), person("Trulia, Inc.", role = c("cph")) )
# Maintainer: Ari Lamstein <ari@lamsteinconsulting.com>
# URL: www.choroplethr.com
# Copyright: Trulia, Inc.
# License: BSD_3_clause + file LICENSE
# Imports: Hmisc, stringr, ggplot2 (>= 2.0.0), dplyr, R6, WDI, ggmap, RgoogleMaps, tigris (>= 1.0), gridExtra, xml2, rvest, tidyr, tidycensus
# Suggests: testthat, choroplethrMaps, choroplethrAdmin1 (>= 1.1.0), knitr, rmarkdown, scales
# Depends: R (>= 3.5.0), acs
# Collate: 'acs.R' 'choropleth.R' 'admin1.R' 'admin1_region.R' 'choroplethr_animate.R' 'choroplethr_wdi.R' 'country.R' 'usa.R' 'county.R' 'county_zoom.R' 'data.R' 'get_congress_116_party_data.R' 'get_congressional_demographics.R' 'get_county_demographics.R' 'get_state_demograhpics.R' 'get_tract_demographics.R' 'startup_messages.R' 'state.R' 'tract.R' 'utils.R'
# RoxygenNote: 7.2.3
# Encoding: UTF-8
# VignetteBuilder: knitr
# NeedsCompilation: no
# Packaged: 2023-07-06 13:24:46 UTC; arilamstein
# Author: Ari Lamstein [aut, cre], Brian Johnson [ctb], Trulia, Inc. [cph]
# Repository: CRAN
# Date/Publication: 2023-07-06 23:10:08 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
