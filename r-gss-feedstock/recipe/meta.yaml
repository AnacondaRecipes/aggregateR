{% set version = '2.1-7' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-gss
  version: {{ version|replace("-", "_") }}

source:
  fn: gss_2.1-7.tar.gz
  url:
    - {{ cran_mirror }}/src/contrib/gss_2.1-7.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/gss/gss_2.1-7.tar.gz
  sha256: 0405bb5e4c4d60b466335e5da07be4f9570045a24aed09e7bc0640e1a00f3adb







build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0


  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/


requirements:
  build:
    - {{ compiler('c') }}          # [not win]
    - {{ compiler('fortran') }}    # [not win]
    - {{native}}toolchain          # [win]
    - {{posix}}filesystem          # [win]
    - {{posix}}make

  host:
    - r-base

  run:
    - r-base
    - {{native}}gcc-libs           # [win]

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('gss')"           # [not win]
    - "\"%R%\" -e \"library('gss')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://CRAN.R-project.org/package=gss
  license: GPL (>= 2)
  summary: A comprehensive package for structural multivariate function estimation using smoothing
    splines.
  license_family: GPL3

extra:
  recipe-maintainers:
    - mingwandroid

# The original CRAN metadata for this package was:

# Package: gss
# Version: 2.1-7
# Date: 2017-02-23
# Title: General Smoothing Splines
# Author: Chong Gu <chong@purdue.edu>
# Maintainer: Chong Gu <chong@purdue.edu>
# Depends: R (>= 2.14.0), stats
# Description: A comprehensive package for structural multivariate function estimation using smoothing splines.
# License: GPL (>= 2)
# Packaged: 2017-02-24 07:32:31 UTC; chong
# NeedsCompilation: yes
# Repository: CRAN
# Date/Publication: 2017-02-24 16:46:53

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
