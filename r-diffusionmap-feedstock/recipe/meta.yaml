{% set version = '1.2.0' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-diffusionmap
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/diffusionMap_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/diffusionMap/diffusionMap_{{ version }}.tar.gz
  sha256: 523847592fbc3a29252bc92b5821e17564ce6b188c483c930e95e6950c3873e7

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-matrix
    - r-igraph
    - r-scatterplot3d

  run:
    - r-base
    - r-matrix
    - r-igraph
    - r-scatterplot3d

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('diffusionMap')"           # [not win]
    - "\"%R%\" -e \"library('diffusionMap')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://github.com/rcannood/diffusionMap
  license: GPL-3
  summary: Implements diffusion map method of data parametrization, including creation and visualization
    of diffusion map, clustering with diffusion K-means and regression using adaptive
    regression model. Richards (2009) <doi:10.1088/0004-637X/691/1/32>.
  license_family: GPL3
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-3'

# The original CRAN metadata for this package was:

# Package: diffusionMap
# Type: Package
# Title: Diffusion Map
# Version: 1.2.0
# Description: Implements diffusion map method of data parametrization, including creation and visualization of diffusion map, clustering with diffusion K-means and regression using adaptive regression model. Richards (2009) <doi:10.1088/0004-637X/691/1/32>.
# Authors@R: c( person( "Joseph", "Richards", email = "joeyrichar@gmail.com", role = c("aut"), comment = c(github = "joeyrichar") ), person( "Robrecht", "Cannoodt", email = "rcannood@gmail.com", role = c("aut", "cre"), comment = c(ORCID = "0000-0003-3641-729X", github = "rcannood") ) )
# Depends: R (>= 2.10)
# Imports: scatterplot3d, graphics, igraph, Matrix, stats
# License: GPL-3
# URL: https://github.com/rcannood/diffusionMap
# RoxygenNote: 6.1.1
# Encoding: UTF-8
# LazyData: true
# NeedsCompilation: no
# Packaged: 2019-09-10 11:41:29 UTC; rcannood
# Author: Joseph Richards [aut] (joeyrichar), Robrecht Cannoodt [aut, cre] (<https://orcid.org/0000-0003-3641-729X>, rcannood)
# Maintainer: Robrecht Cannoodt <rcannood@gmail.com>
# Repository: CRAN
# Date/Publication: 2019-09-10 22:50:18 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
