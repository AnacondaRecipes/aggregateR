{% set version = '1.2.1' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-lumberjack
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/lumberjack_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/lumberjack/lumberjack_{{ version }}.tar.gz
  sha256: 524515e1253095be99d0dead03111592e65f0261410dad77594987ae8e164637

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: tinytest
requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-r6

  run:
    - r-base
    - r-r6

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('lumberjack')"           # [not win]
    - "\"%R%\" -e \"library('lumberjack')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://github.com/markvanderloo/lumberjack
  license: EUPL
  summary: 'A framework that allows for easy logging of changes in data. Main features: start
    tracking changes by adding a single line of code to an existing script. Track changes
    in multiple datasets, using multiple loggers. Add custom-built loggers or use loggers
    offered by other packages. <doi:10.18637/jss.v098.i01>.'
  license_family: OTHER

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: lumberjack
# Maintainer: Mark van der Loo <mark.vanderloo@gmail.com>
# License: EUPL
# Title: Track Changes in Data
# Type: Package
# LazyLoad: yes
# Authors@R: person("Mark", "van der Loo", role=c("aut","cre"),email="mark.vanderloo@gmail.com" , comment=c(ORCID="0000-0002-9807-4686"))
# Description: A framework that allows for easy logging of changes in data. Main features: start tracking changes by adding a single line of code to an existing script. Track changes in multiple datasets, using multiple loggers. Add custom-built loggers or use loggers offered by other packages. <doi:10.18637/jss.v098.i01>.
# Version: 1.2.1
# URL: https://github.com/markvanderloo/lumberjack
# BugReports: https://github.com/markvanderloo/lumberjack/issues
# Imports: utils, R6
# Depends: R (>= 3.4.0)
# Suggests: tinytest
# RoxygenNote: 7.1.1
# Encoding: UTF-8
# NeedsCompilation: no
# Packaged: 2021-05-26 13:00:50 UTC; mark
# Author: Mark van der Loo [aut, cre] (<https://orcid.org/0000-0002-9807-4686>)
# Repository: CRAN
# Date/Publication: 2021-05-27 00:20:02 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
