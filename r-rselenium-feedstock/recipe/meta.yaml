{% set version = '1.7.9' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-rselenium
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/RSelenium_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/RSelenium/RSelenium_{{ version }}.tar.gz
  sha256: e249a675612891fe73e639179b9082bd250c0c63eccc98928ce9d175dd030da2

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: binman, XML, testthat, knitr, covr, rmarkdown
requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-catools
    - r-httr
    - r-wdman >=0.2.2

  run:
    - r-base
    - r-catools
    - r-httr
    - r-wdman >=0.2.2

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('RSelenium')"           # [not win]
    - "\"%R%\" -e \"library('RSelenium')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://docs.ropensci.org/RSelenium/
  license: AGPL-3
  summary: Provides a set of R bindings for the 'Selenium 2.0 WebDriver' (see <https://www.selenium.dev/documentation/>
    for more information) using the 'JsonWireProtocol' (see <https://github.com/SeleniumHQ/selenium/wiki/JsonWireProtocol>
    for more information). 'Selenium 2.0 WebDriver' allows driving a web browser natively
    as a user would either locally or on a remote machine using the Selenium server
    it marks a leap forward in terms of web browser automation. Selenium automates web
    browsers (commonly referred to as browsers). Using RSelenium you can automate browsers
    locally or remotely.
  license_family: AGPL
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/AGPL-3'

# The original CRAN metadata for this package was:

# Package: RSelenium
# Type: Package
# Title: R Bindings for 'Selenium WebDriver'
# Version: 1.7.9
# Authors@R: c( person("John", "Harrison", , "johndharrison0@gmail.com", role = "aut", comment = "original author"), person("Ju Yeong", "Kim", , "jkim2345@fredhutch.org", role = "cre", comment = "rOpenSci maintainer"))
# Description: Provides a set of R bindings for the 'Selenium 2.0 WebDriver' (see <https://www.selenium.dev/documentation/> for more information) using the 'JsonWireProtocol' (see <https://github.com/SeleniumHQ/selenium/wiki/JsonWireProtocol> for more information). 'Selenium 2.0 WebDriver' allows driving a web browser natively as a user would either locally or on a remote machine using the Selenium server it marks a leap forward in terms of web browser automation. Selenium automates web browsers (commonly referred to as browsers). Using RSelenium you can automate browsers locally or remotely.
# License: AGPL-3
# URL: https://docs.ropensci.org/RSelenium/
# BugReports: https://github.com/ropensci/RSelenium/issues
# Depends: R (>= 3.0.0)
# Imports: methods, caTools, utils, httr, wdman(>= 0.2.2)
# Suggests: binman, XML, testthat, knitr, covr, rmarkdown
# VignetteBuilder: knitr
# Collate: 'RSelenium.R' 'errorHandler.R' 'remoteDriver.R' 'rsDriver.R' 'selKeys-data.R' 'util.R' 'webElement.R'
# Encoding: UTF-8
# RoxygenNote: 7.2.1
# NeedsCompilation: no
# Packaged: 2022-09-01 20:00:13 UTC; juyeongkim
# Author: John Harrison [aut] (original author), Ju Yeong Kim [cre] (rOpenSci maintainer)
# Maintainer: Ju Yeong Kim <jkim2345@fredhutch.org>
# Repository: CRAN
# Date/Publication: 2022-09-02 08:10:11 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
