{% set version = '1.6.3' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-glmmlasso
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/glmmLasso_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/glmmLasso/glmmLasso_{{ version }}.tar.gz
  sha256: 2fb2bbdec58bf18dd1a7cde82c23346c29629c72a6d83801c5b3425b759d5cac

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

requirements:
  build:
    - {{ compiler('c') }}              # [not win]
    - {{ compiler('m2w64_c') }}        # [win]
    - {{ compiler('cxx') }}            # [not win]
    - {{ compiler('m2w64_cxx') }}      # [win]
    - {{ posix }}filesystem        # [win]
    - {{ posix }}make
    - {{ posix }}sed               # [win]
    - {{ posix }}coreutils         # [win]
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-matrix
    - r-rcpp >=0.12.12
    - r-rcppeigen
    - r-minqa

  run:
    - r-base
    - {{native}}gcc-libs         # [win]
    - r-matrix
    - r-rcpp >=0.12.12
    - r-rcppeigen
    - r-minqa

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('glmmLasso')"           # [not win]
    - "\"%R%\" -e \"library('glmmLasso')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://CRAN.R-project.org/package=glmmLasso
  license: GPL-2
  summary: A variable selection approach for generalized linear mixed models by L1-penalized
    estimation is provided, see Groll and Tutz (2014) <doi:10.1007/s11222-012-9359-z>.
    See also Groll and Tutz (2017) <doi:10.1007/s10985-016-9359-y> for discrete survival
    models including heterogeneity.
  license_family: GPL2
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-2'

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: glmmLasso
# Type: Package
# Title: Variable Selection for Generalized Linear Mixed Models by L1-Penalized Estimation
# Version: 1.6.3
# Date: 2023-08-19
# Author: Andreas Groll
# Maintainer: Andreas Groll <groll@statistik.tu-dortmund.de>
# Description: A variable selection approach for generalized linear mixed models by L1-penalized estimation is provided, see Groll and Tutz (2014) <doi:10.1007/s11222-012-9359-z>. See also Groll and Tutz (2017) <doi:10.1007/s10985-016-9359-y> for discrete survival models including heterogeneity.
# Imports: stats, minqa, Matrix, Rcpp (>= 0.12.12), methods
# LinkingTo: Rcpp, RcppEigen
# License: GPL-2
# NeedsCompilation: yes
# Packaged: 2023-08-23 07:24:37 UTC; user
# Repository: CRAN
# Date/Publication: 2023-08-23 14:30:13 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
