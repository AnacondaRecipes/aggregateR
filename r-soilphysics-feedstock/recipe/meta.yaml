{% set version = '4.1' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-soilphysics
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/soilphysics_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/soilphysics/soilphysics_{{ version }}.tar.gz
  sha256: e2f8008f8e1a884c116ed0ab0e2e142f89cb25bbf06e9fc853cece42f01609cf

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: knitr, rmarkdown, rpanel, tcltk
requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-mass
    - r-boot
    - r-fields
    - r-shiny
    - r-shinydashboard

  run:
    - r-base
    - r-mass
    - r-boot
    - r-fields
    - r-shiny
    - r-shinydashboard

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('soilphysics')"           # [not win]
    - "\"%R%\" -e \"library('soilphysics')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://arsilva87.github.io/soilphysics/
  license: GPL-2
  summary: Basic and model-based soil physical analyses.
  license_family: GPL2
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-2'

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: soilphysics
# Type: Package
# Title: Soil Physical Analysis
# Version: 4.1
# Date: 2021-11-21
# Description: Basic and model-based soil physical analyses.
# Authors@R: c(person(given = c("Anderson", "Rodrigo"), family = "da Silva", role = c("aut", "cre"), email = "anderson.agro@hotmail.com", comment = c(ORCID = "0000-0003-2518-542X")), person(given = c("Renato", "Paiva"), family = "de Lima", role = "aut", email = "renato_agro_@hotmail.com", comment = c(ORCID = "0000-0003-0524-439X")))
# Author: Anderson Rodrigo da Silva [aut, cre] (<https://orcid.org/0000-0003-2518-542X>), Renato Paiva de Lima [aut] (<https://orcid.org/0000-0003-0524-439X>)
# Maintainer: Anderson Rodrigo da Silva <anderson.agro@hotmail.com>
# URL: https://arsilva87.github.io/soilphysics/
# Imports: utils, stats, graphics, boot, grDevices, datasets, MASS, shiny, shinydashboard, fields
# Suggests: knitr, rmarkdown, rpanel, tcltk
# VignetteBuilder: knitr
# License: GPL (>= 2)
# NeedsCompilation: no
# Packaged: 2021-11-21 11:41:00 UTC; User
# Repository: CRAN
# Date/Publication: 2021-11-30 09:50:02 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
