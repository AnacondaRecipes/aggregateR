{% set version = '1.1-6' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-freegroup
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/freegroup_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/freegroup/freegroup_{{ version }}.tar.gz
  sha256: fd646ceb9d342887eebc9c7158f55cdd86e94d292980372b9129f937e863199e

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: knitr, rmarkdown, permutations, testthat
requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-freealg >=1.0_4
    - r-magic >=1.5_9
    - r-magrittr
    - r-plyr

  run:
    - r-base
    - r-freealg >=1.0_4
    - r-magic >=1.5_9
    - r-magrittr
    - r-plyr

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('freegroup')"           # [not win]
    - "\"%R%\" -e \"library('freegroup')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://github.com/RobinHankin/freegroup
  license: GPL-2
  summary: The free group in R; juxtaposition is represented by a plus.  Includes inversion,
    multiplication by a scalar, group-theoretic power operation, and Tietze forms.  To
    cite the package in publications please use Hankin (2022) <doi:10.48550/ARXIV.2212.05883>.
  license_family: GPL2
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-2'

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: freegroup
# Type: Package
# Title: The Free Group
# Version: 1.1-6
# Authors@R: c(person(c("Robin", "K. S. "), "Hankin", role=c("aut","cre"), email="hankin.robin@gmail.com", comment = c(ORCID = "0000-0001-5982-0415")))
# Maintainer: Robin K. S. Hankin <hankin.robin@gmail.com>
# Description: The free group in R; juxtaposition is represented by a plus.  Includes inversion, multiplication by a scalar, group-theoretic power operation, and Tietze forms.  To cite the package in publications please use Hankin (2022) <doi:10.48550/ARXIV.2212.05883>.
# Depends: magrittr, methods, magic (>= 1.5-9), plyr, R (>= 3.5.0)
# Suggests: knitr, rmarkdown, permutations, testthat
# LazyData: yes
# Imports: freealg (>= 1.0-4)
# VignetteBuilder: knitr
# License: GPL-2
# URL: https://github.com/RobinHankin/freegroup
# BugReports: https://github.com/RobinHankin/freegroup/issues
# NeedsCompilation: no
# Packaged: 2022-12-13 19:49:55 UTC; rhankin
# Author: Robin K. S.  Hankin [aut, cre] (<https://orcid.org/0000-0001-5982-0415>)
# Repository: CRAN
# Date/Publication: 2022-12-14 08:10:14 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
