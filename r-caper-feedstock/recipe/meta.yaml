{% set version = '1.0.3' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-caper
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/caper_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/caper/caper_{{ version }}.tar.gz
  sha256: 4f769748c5db33863d47adf10823cfef21d99c7bd07e9d33072487fd48e135b8

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: xtable, rmarkdown, knitr
requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-mass
    - r-ape >=3.0_6
    - r-mvtnorm

  run:
    - r-base
    - r-mass
    - r-ape >=3.0_6
    - r-mvtnorm

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('caper')"           # [not win]
    - "\"%R%\" -e \"library('caper')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://CRAN.R-project.org/package=caper
  license: GPL-2
  summary: Functions for performing phylogenetic comparative analyses.
  license_family: GPL2
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-2'

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: caper
# Type: Package
# Title: Comparative Analyses of Phylogenetics and Evolution in R
# Version: 1.0.3
# Date: 2023-09-26
# Authors@R: c( person('David', 'Orme', email='d.orme@imperial.ac.uk', role=c('aut','cre'), comment=c(ORCID='0000-0002-7005-1394')), person('Rob', 'Freckleton', role=c('aut')), person('Gavin', 'Thomas', role=c('aut')), person('Thomas', 'Petzoldt', role=c('aut')), person('Susanne', 'Fritz', role=c('aut')), person('Nick', 'Isaac', role=c('aut')), person('Will', 'Pearse', role=c('aut')))
# Maintainer: David Orme <d.orme@imperial.ac.uk>
# Imports: stats, graphics, utils, methods
# Depends: R (>= 2.10), ape (>= 3.0-6) , MASS, mvtnorm
# Suggests: xtable, rmarkdown, knitr
# Description: Functions for performing phylogenetic comparative analyses.
# License: GPL (>= 2)
# NeedsCompilation: no
# Packaged: 2023-09-26 13:28:35 UTC; dorme
# Author: David Orme [aut, cre] (<https://orcid.org/0000-0002-7005-1394>), Rob Freckleton [aut], Gavin Thomas [aut], Thomas Petzoldt [aut], Susanne Fritz [aut], Nick Isaac [aut], Will Pearse [aut]
# Repository: CRAN
# Date/Publication: 2023-09-26 14:50:05 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
