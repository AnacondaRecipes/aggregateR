{% set version = '0.6.2' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-dlookr
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/dlookr_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/dlookr/dlookr_{{ version }}.tar.gz
  sha256: ba8f08b3689ddf4e5e805c66a80f00ae11e8cce807086438dbcec077e1c2bcec

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: DBI, classInt, dbplyr, forecast (>= 8.3), Hmisc, ISLR, nycflights13, ppsr, randomForest, rpart, prettydoc, RSQLite, ranger, stringi
requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-dplyr >=0.7.6
    - r-ggplot2 >=3.0.0
    - r-gridextra
    - r-hrbrthemes >=0.8.0
    - r-htmltools
    - r-kableextra
    - r-knitr >=1.22
    - r-mice
    - r-pagedown >=0.15
    - r-partykit
    - r-purrr
    - r-reactable
    - r-rlang
    - r-rmarkdown
    - r-shiny
    - r-showtext >=0.9_4
    - r-sysfonts >=0.7.1
    - r-tibble
    - r-tidyr
    - r-tidyselect

  run:
    - r-base
    - r-dplyr >=0.7.6
    - r-ggplot2 >=3.0.0
    - r-gridextra
    - r-hrbrthemes >=0.8.0
    - r-htmltools
    - r-kableextra
    - r-knitr >=1.22
    - r-mice
    - r-pagedown >=0.15
    - r-partykit
    - r-purrr
    - r-reactable
    - r-rlang
    - r-rmarkdown
    - r-shiny
    - r-showtext >=0.9_4
    - r-sysfonts >=0.7.1
    - r-tibble
    - r-tidyr
    - r-tidyselect

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('dlookr')"           # [not win]
    - "\"%R%\" -e \"library('dlookr')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://github.com/choonghyunryu/dlookr/, https://choonghyunryu.github.io/dlookr/
  license: GPL-2
  summary: A collection of tools that support data diagnosis, exploration, and transformation.
    Data diagnostics provides information and visualization of missing values and outliers
    and unique and negative values to help you understand the distribution and quality
    of your data. Data exploration provides information and visualization of the descriptive
    statistics of univariate variables, normality tests and outliers, correlation of
    two variables, and relationship between target variable and predictor. Data transformation
    supports binning for categorizing continuous variables, imputates missing values
    and outliers, resolving skewness. And it creates automated reports that support
    these three tasks.
  license_family: GPL2
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-2'
    - LICENSE

# The original CRAN metadata for this package was:

# Package: dlookr
# Type: Package
# Title: Tools for Data Diagnosis, Exploration, Transformation
# Version: 0.6.2
# Date: 2023-06-30
# Authors@R: c( person("Choonghyun", "Ryu",, "choonghyun.ryu@gmail.com", role = c("aut", "cre")) )
# Description: A collection of tools that support data diagnosis, exploration, and transformation. Data diagnostics provides information and visualization of missing values and outliers and unique and negative values to help you understand the distribution and quality of your data. Data exploration provides information and visualization of the descriptive statistics of univariate variables, normality tests and outliers, correlation of two variables, and relationship between target variable and predictor. Data transformation supports binning for categorizing continuous variables, imputates missing values and outliers, resolving skewness. And it creates automated reports that support these three tasks.
# License: GPL-2 | file LICENSE
# URL: https://github.com/choonghyunryu/dlookr/, https://choonghyunryu.github.io/dlookr/
# Encoding: UTF-8
# LazyData: true
# Depends: R (>= 3.2.0)
# Imports: dplyr (>= 0.7.6), showtext (>= 0.9-4), sysfonts (>= 0.7.1), ggplot2 (>= 3.0.0), grid, gridExtra, hrbrthemes (>= 0.8.0), htmltools, kableExtra, knitr (>= 1.22), methods, mice, pagedown (>= 0.15), partykit, purrr, reactable, rlang, rmarkdown, shiny, tibble, tidyr, tidyselect, utils
# Suggests: DBI, classInt, dbplyr, forecast (>= 8.3), Hmisc, ISLR, nycflights13, ppsr, randomForest, rpart, prettydoc, RSQLite, ranger, stringi
# Author: Choonghyun Ryu [aut, cre]
# Maintainer: Choonghyun Ryu <choonghyun.ryu@gmail.com>
# BugReports: https://github.com/choonghyunryu/dlookr/issues
# VignetteBuilder: knitr
# RoxygenNote: 7.2.3
# NeedsCompilation: no
# Packaged: 2023-06-30 13:43:23 UTC; choonghyunryu
# Repository: CRAN
# Date/Publication: 2023-07-01 07:40:10 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
