{% set version = '0.2-1' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-phia
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/phia_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/phia/phia_{{ version }}.tar.gz
  sha256: 00e402f19d857e497df771bccaf62690d5624eb73c354dbada081155e8546267

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: nlme, lme4
requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-matrix
    - r-car

  run:
    - r-base
    - r-matrix
    - r-car

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('phia')"           # [not win]
    - "\"%R%\" -e \"library('phia')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://github.com/heliosdrm/phia
  license: GPL-3
  summary: Analysis of terms in linear, generalized and mixed linear models, on the basis of
    multiple comparisons of factor contrasts.  Specially suited for the analysis of
    interaction terms.
  license_family: GPL3
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-3'

# The original CRAN metadata for this package was:

# Package: phia
# Version: 0.2-1
# Date: 2015-11-07
# Title: Post-Hoc Interaction Analysis
# Authors@R: c(person("Helios", "De Rosario-Martinez", role=c("aut", "cre"), email="helios.derosario@gmail.com"), person("John", "Fox", role="ctb"), person("R Core Team", role="ctb"))
# Description: Analysis of terms in linear, generalized and mixed linear models, on the basis of multiple comparisons of factor contrasts.  Specially suited for the analysis of interaction terms.
# Depends: car, graphics, stats
# Suggests: nlme, lme4
# Imports: Matrix, grDevices, methods, utils
# License: GPL (>= 3)
# URL: https://github.com/heliosdrm/phia
# LazyData: yes
# NeedsCompilation: no
# Packaged: 2015-11-07 10:04:13 UTC; meliana
# Author: Helios De Rosario-Martinez [aut, cre], John Fox [ctb], R Core Team [ctb]
# Maintainer: Helios De Rosario-Martinez <helios.derosario@gmail.com>
# Repository: CRAN
# Date/Publication: 2015-11-07 14:53:33

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
