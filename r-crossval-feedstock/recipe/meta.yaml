{% set version = '1.0.4' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-crossval
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/crossval_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/crossval/crossval_{{ version }}.tar.gz
  sha256: d7132dc4e0901209af769523038e57cdb787e355d49b6207d06e126e98a7506f

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: MASS, sda, care, binda
requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base

  run:
    - r-base

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('crossval')"           # [not win]
    - "\"%R%\" -e \"library('crossval')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://cran.r-project.org/package=crossval
  license: GPL-3
  summary: Contains generic functions for performing cross validation and for computing diagnostic
    errors.
  license_family: GPL3
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-3'

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: crossval
# Version: 1.0.4
# Date: 2021-09-17
# Title: Generic Functions for Cross Validation
# Author: Korbinian Strimmer.
# Maintainer: Korbinian Strimmer <strimmerlab@gmail.com>
# Depends: R (>= 3.0.2)
# Imports: stats
# Suggests: MASS, sda, care, binda
# Description: Contains generic functions for performing cross validation and for computing diagnostic errors.
# License: GPL (>= 3)
# URL: https://cran.r-project.org/package=crossval
# NeedsCompilation: no
# Packaged: 2021-09-17 15:35:13 UTC; strimmer
# Repository: CRAN
# Date/Publication: 2021-09-17 16:10:02 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
