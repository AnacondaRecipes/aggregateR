{% set version = '2.6-2' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-rnetcdf
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/RNetCDF_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/RNetCDF/RNetCDF_{{ version }}.tar.gz
  sha256: b4de45798fc5f3021f23187bc52d5931a86a4f5c91fcdba7ca4f92c966ec022e

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: bit64
requirements:
  build:
    - {{ compiler('c') }}              # [not win]
    - {{ compiler('m2w64_c') }}        # [win]
    - {{ posix }}filesystem        # [win]
    - {{ posix }}sed               # [win]
    - {{ posix }}grep              # [win]
    - {{ posix }}autoconf
    - {{ posix }}automake          # [not win]
    - {{ posix }}automake-wrapper  # [win]
    - {{ posix }}pkg-config
    - {{ posix }}make
    - {{ posix }}coreutils         # [win]
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - netcdf4
  run:
    - r-base
    - {{native}}gcc-libs         # [win]
    - netcdf4

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('RNetCDF')"           # [not win]
    - "\"%R%\" -e \"library('RNetCDF')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://github.com/mjwoods/RNetCDF https://www.unidata.ucar.edu/software/netcdf/ https://www.unidata.ucar.edu/software/udunits/
  license: GPL-2
  summary: An interface to the 'NetCDF' file formats designed by Unidata for efficient storage
    of array-oriented scientific data and descriptions. Most capabilities of 'NetCDF'
    version 4 are supported. Optional conversions of time units are enabled by 'UDUNITS'
    version 2, also from Unidata.
  license_family: GPL2
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-2'
    - LICENSE

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: RNetCDF
# Version: 2.6-2
# Date: 2023-01-16
# Title: Interface to 'NetCDF' Datasets
# Authors@R: c(person("Pavel", "Michna", role = "aut", email = "rnetcdf-devel@bluewin.ch"), person("Milton", "Woods", role = c("aut", "cre"), email = "miltonjwoods@gmail.com"))
# Depends: R (>= 3.0.0)
# SystemRequirements: netcdf udunits-2
# Suggests: bit64
# Enhances: pbdMPI
# Description: An interface to the 'NetCDF' file formats designed by Unidata for efficient storage of array-oriented scientific data and descriptions. Most capabilities of 'NetCDF' version 4 are supported. Optional conversions of time units are enabled by 'UDUNITS' version 2, also from Unidata.
# License: GPL (>= 2) | file LICENSE
# URL: https://github.com/mjwoods/RNetCDF https://www.unidata.ucar.edu/software/netcdf/ https://www.unidata.ucar.edu/software/udunits/
# BugReports: https://github.com/mjwoods/RNetCDF/issues
# NeedsCompilation: yes
# Packaged: 2023-01-17 03:05:48 UTC; mwoods
# Author: Pavel Michna [aut], Milton Woods [aut, cre]
# Maintainer: Milton Woods <miltonjwoods@gmail.com>
# Repository: CRAN
# Date/Publication: 2023-01-17 04:40:06 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
