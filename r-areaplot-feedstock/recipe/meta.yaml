{% set version = '1.2.2' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-areaplot
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/areaplot_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/areaplot/areaplot_{{ version }}.tar.gz
  sha256: 8774529da1136976f2db88426a9af60e59d6d97d3d7fc11554ec5e61cc70d06f

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: MASS
requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base

  run:
    - r-base

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('areaplot')"           # [not win]
    - "\"%R%\" -e \"library('areaplot')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://CRAN.R-project.org/package=areaplot
  license: GPL-3
  summary: Plot stacked areas and confidence bands as filled polygons, or add polygons to existing
    plots. A variety of input formats are supported, including vectors, matrices, data
    frames, formulas, etc.
  license_family: GPL3
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-3'

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: areaplot
# Version: 1.2.2
# Date: 2020-11-23
# Title: Plot Stacked Areas and Confidence Bands as Filled Polygons
# Authors@R: person("Arni", "Magnusson", role=c("aut","cre"), email="thisisarni@gmail.com")
# Imports: graphics, grDevices, stats
# Suggests: MASS
# Description: Plot stacked areas and confidence bands as filled polygons, or add polygons to existing plots. A variety of input formats are supported, including vectors, matrices, data frames, formulas, etc.
# License: GPL-3
# Encoding: UTF-8
# RoxygenNote: 7.1.1
# NeedsCompilation: no
# Packaged: 2020-11-23 12:50:13 UTC; arnima
# Author: Arni Magnusson [aut, cre]
# Maintainer: Arni Magnusson <thisisarni@gmail.com>
# Repository: CRAN
# Date/Publication: 2020-11-23 15:40:06 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
