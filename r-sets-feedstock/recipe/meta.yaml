{% set version = '1.0-24' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-sets
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/sets_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/sets/sets_{{ version }}.tar.gz
  sha256: e75733f5c9418eb09fb950a4a94ccf84ddd88231c61ee80d02b7f0917debcac9

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: proxy
requirements:
  build:
    - {{ compiler('c') }}              # [not win]
    - {{ compiler('m2w64_c') }}        # [win]
    - {{ posix }}filesystem        # [win]
    - {{ posix }}make
    - {{ posix }}sed               # [win]
    - {{ posix }}coreutils         # [win]
    - {{ posix }}zip               # [win]

  host:
    - r-base

  run:
    - r-base
    - {{native}}gcc-libs         # [win]

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('sets')"           # [not win]
    - "\"%R%\" -e \"library('sets')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://CRAN.R-project.org/package=sets
  license: GPL-2
  summary: Data structures and basic operations for ordinary sets, generalizations such as fuzzy
    sets, multisets, and fuzzy multisets, customizable sets, and intervals.
  license_family: GPL2
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-2'

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: sets
# Version: 1.0-24
# Title: Sets, Generalized Sets, Customizable Sets and Intervals
# Description: Data structures and basic operations for ordinary sets, generalizations such as fuzzy sets, multisets, and fuzzy multisets, customizable sets, and intervals.
# Authors@R: c(person(given = "David", family = "Meyer", role = c("aut", "cre"), email = "David.Meyer@R-project.org", comment = c(ORCID = "0000-0002-5196-3048")), person(given = "Kurt", family = "Hornik", role = "aut", email = "Kurt.Hornik@R-project.org", comment = c(ORCID = "0000-0003-4198-9911")), person(given = "Christian", family = "Buchta", role = "ctb"))
# Depends: R (>= 2.7.0)
# Suggests: proxy
# Imports: graphics,grDevices,stats,utils
# License: GPL-2
# NeedsCompilation: yes
# Packaged: 2023-03-06 13:16:08 UTC; meyer
# Author: David Meyer [aut, cre] (<https://orcid.org/0000-0002-5196-3048>), Kurt Hornik [aut] (<https://orcid.org/0000-0003-4198-9911>), Christian Buchta [ctb]
# Maintainer: David Meyer <David.Meyer@R-project.org>
# Repository: CRAN
# Date/Publication: 2023-03-07 07:08:21 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
