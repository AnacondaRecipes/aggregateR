{% set version = '2.2.0' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-cir
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/cir_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/cir/cir_{{ version }}.tar.gz
  sha256: d5462ddd7ae97edf4ad3055d1edaf8e21b79687e2a65c922b8aca71f763cdb71

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: knitr, rmarkdown
requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base

  run:
    - r-base

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('cir')"           # [not win]
    - "\"%R%\" -e \"library('cir')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://CRAN.R-project.org/package=cir
  license: GPL-2
  summary: 'Isotonic regression (IR) and its improvement: centered isotonic regression (CIR).
    CIR is recommended in particular with small samples. Also, interval estimates for
    both, and additional utilities such as plotting dose-response data.'
  license_family: GPL2
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-2'

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: cir
# Type: Package
# Title: Centered Isotonic Regression and Dose-Response Utilities
# Version: 2.2.0
# Date: 2021-8-12
# Description: Isotonic regression (IR) and its improvement: centered isotonic regression (CIR). CIR is recommended in particular with small samples. Also, interval estimates for both, and additional utilities such as plotting dose-response data.
# License: GPL-2
# Authors@R: person('Assaf P.','Oron', role=c('cre','aut'), email='assaf.oron@gmail.com')
# RoxygenNote: 7.1.1
# Encoding: UTF-8
# VignetteBuilder: knitr
# Suggests: knitr, rmarkdown
# NeedsCompilation: no
# Packaged: 2021-08-23 08:00:39 UTC; Assaf
# Author: Assaf P. Oron [cre, aut]
# Maintainer: Assaf P. Oron <assaf.oron@gmail.com>
# Repository: CRAN
# Date/Publication: 2021-08-23 12:00:02 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
