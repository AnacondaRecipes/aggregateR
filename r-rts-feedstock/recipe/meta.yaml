{% set version = '1.1-14' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-rts
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/rts_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/rts/rts_{{ version }}.tar.gz
  sha256: 883a51a1afd96c744a3c1214f8afe30877f1b2f5abfeeabbb927a7bf90bd361f

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: digest, R.rsp
requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-rcurl
    - r-raster
    - r-terra
    - r-xts
    - r-zoo

  run:
    - r-base
    - r-rcurl
    - r-raster
    - r-terra
    - r-xts
    - r-zoo

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('rts')"           # [not win]
    - "\"%R%\" -e \"library('rts')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://r-gis.net/
  license: GPL-3
  summary: This framework aims to provide classes and methods for manipulating and processing
    of raster time series data (e.g. a time series of satellite images).
  license_family: GPL3
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-3'

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: rts
# Type: Package
# Title: Raster Time Series Analysis
# Version: 1.1-14
# Date: 2023-10-01
# Author: Babak Naimi
# Depends: R (>= 3.5.0), terra, xts
# Imports: methods, zoo, RCurl, raster
# Suggests: digest, R.rsp
# Maintainer: Babak Naimi <naimi.b@gmail.com>
# Description: This framework aims to provide classes and methods for manipulating and processing of raster time series data (e.g. a time series of satellite images).
# License: GPL (>= 3)
# URL: https://r-gis.net/
# VignetteBuilder: R.rsp
# NeedsCompilation: no
# Packaged: 2023-10-01 01:21:41 UTC; babak
# Repository: CRAN
# Date/Publication: 2023-10-01 02:20:02 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
