{% set version = '1.0.1' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-urlchecker
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/urlchecker_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/urlchecker/urlchecker_{{ version }}.tar.gz
  sha256: 62165ddbe1b748b58c71a50c8f07fdde6f3d19a7b39787b9fa2b4f9216250318

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: covr
requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-cli
    - r-curl
    - r-xml2

  run:
    - r-base
    - r-cli
    - r-curl
    - r-xml2

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('urlchecker')"           # [not win]
    - "\"%R%\" -e \"library('urlchecker')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://github.com/r-lib/urlchecker
  license: GPL-3
  summary: Provide the URL checking tools available in R 4.1+ as a package for earlier versions
    of R. Also uses concurrent requests so can be much faster than the serial versions.
  license_family: GPL3
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-3'

# The original CRAN metadata for this package was:

# Package: urlchecker
# Title: Run CRAN URL Checks from Older R Versions
# Version: 1.0.1
# Authors@R: c( person("R Core team", role = "aut", comment = "The code in urltools.R adapted from the tools package"), person("Jim", "Hester", role = "aut", comment = c(ORCID = "0000-0002-2739-7082")), person("Gabor", "Csardi", , "csardi.gabor@gmail.com", role = c("aut", "cre")), person("RStudio", role = c("cph", "fnd")) )
# Description: Provide the URL checking tools available in R 4.1+ as a package for earlier versions of R. Also uses concurrent requests so can be much faster than the serial versions.
# License: GPL-3
# URL: https://github.com/r-lib/urlchecker
# BugReports: https://github.com/r-lib/urlchecker/issues
# Depends: R (>= 3.3)
# Imports: cli, curl, tools, xml2
# Suggests: covr
# Encoding: UTF-8
# RoxygenNote: 7.1.2
# NeedsCompilation: no
# Packaged: 2021-11-30 00:26:56 UTC; jhester
# Author: R Core team [aut] (The code in urltools.R adapted from the tools package), Jim Hester [aut] (<https://orcid.org/0000-0002-2739-7082>), Gabor Csardi [aut, cre], RStudio [cph, fnd]
# Maintainer: Gabor Csardi <csardi.gabor@gmail.com>
# Repository: CRAN
# Date/Publication: 2021-11-30 13:40:02 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
