{% set version = '0.4.1' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-mittagleffler
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/MittagLeffleR_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/MittagLeffleR/MittagLeffleR_{{ version }}.tar.gz
  sha256: 4edc53fa2f7f0d1dde9d237a5663f1f3f4f607172a81d9073efb881a51209fe2

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: knitr, rmarkdown, testthat, animation, magrittr
requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-stabledist

  run:
    - r-base
    - r-stabledist

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('MittagLeffleR')"           # [not win]
    - "\"%R%\" -e \"library('MittagLeffleR')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://strakaps.github.io/MittagLeffleR/
  license: GPL-2
  summary: Implements the Mittag-Leffler function, distribution, random variate generation, and
    estimation. Based on the Laplace-Inversion algorithm by Garrappa, R. (2015) <doi:10.1137/140971191>.
  license_family: GPL2
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-2'

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: MittagLeffleR
# Type: Package
# Title: Mittag-Leffler Family of Distributions
# Version: 0.4.1
# Author: Katharina Hees [aut], Peter Straka [aut, cre], Gurtek Gill [aut], Roberto Garrappa [ctb]
# Maintainer: Peter Straka <straka.ps@gmail.com>
# Authors@R: c( person("Katharina", "Hees", email = "heeskatharina@gmail.com", role = "aut"), person("Peter", "Straka", email = "straka.ps@gmail.com", role = c("aut", "cre")), person("Gurtek", "Gill", role = "aut"), person("Roberto", "Garrappa", email = "roberto.garrappa@uniba.it", role = "ctb") )
# Description: Implements the Mittag-Leffler function, distribution, random variate generation, and estimation. Based on the Laplace-Inversion algorithm by Garrappa, R. (2015) <doi:10.1137/140971191>.
# URL: https://strakaps.github.io/MittagLeffleR/
# BugReports: https://github.com/strakaps/MittagLeffleR/issues
# License: GPL (>= 2)
# Encoding: UTF-8
# RoxygenNote: 7.1.1
# Imports: stats, stabledist
# Suggests: knitr, rmarkdown, testthat, animation, magrittr
# VignetteBuilder: knitr
# NeedsCompilation: no
# Packaged: 2021-09-05 10:40:46 UTC; strakaps
# Repository: CRAN
# Date/Publication: 2021-09-06 06:50:02 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
