{% set version = '3.5' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-irace
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/irace_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/irace/irace_{{ version }}.tar.gz
  sha256: d9928644a5a7e94838558d73afaaee8a914fd26fe68f691ad103331632060bf4

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: Rmpi (>= 0.6.0), parallel, knitr, testthat, withr, mlr (>= 2.15.0), ParamHelpers, devtools, covr
requirements:
  build:
    - {{ compiler('c') }}              # [not win]
    - {{ compiler('m2w64_c') }}        # [win]
    - {{ posix }}filesystem        # [win]
    - {{ posix }}make
    - {{ posix }}sed               # [win]
    - {{ posix }}coreutils         # [win]
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-r6

  run:
    - r-base
    - {{native}}gcc-libs         # [win]
    - r-r6

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('irace')"           # [not win]
    - "\"%R%\" -e \"library('irace')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://mlopez-ibanez.github.io/irace/, https://github.com/MLopez-Ibanez/irace
  license: GPL-2
  summary: "Iterated race is an extension of the Iterated F-race method for the automatic configuration
    of optimization algorithms, that is, (offline) tuning their parameters by finding
    the most appropriate settings given a set of instances of an optimization problem.
    M. L\xF3pez-Ib\xE1\xF1ez, J. Dubois-Lacoste, L. P\xE9rez C\xE1ceres, T. St\xFCtzle,
    and M. Birattari (2016) <doi:10.1016/j.orp.2016.09.002>."
  license_family: GPL2
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-2'

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: irace
# Type: Package
# Title: Iterated Racing for Automatic Algorithm Configuration
# Description: Iterated race is an extension of the Iterated F-race method for the automatic configuration of optimization algorithms, that is, (offline) tuning their parameters by finding the most appropriate settings given a set of instances of an optimization problem. M. Lopez-Ibanez, J. Dubois-Lacoste, L. Perez Caceres, T. Stutzle, and M. Birattari (2016) <doi:10.1016/j.orp.2016.09.002>.
# Version: 3.5
# Authors@R: c(person("Manuel", "Lopez-Ibanez", role = c("aut", "cre"), email = "manuel.lopez-ibanez@manchester.ac.uk", comment = c(ORCID = "0000-0001-9974-1295")), person("Jeremie", "Dubois-Lacoste", role = "aut"), person("Leslie", "Perez Caceres", role = "aut"), person("Thomas", "Stutzle", role = "aut"), person("Mauro", "Birattari", role = "aut"), person("Eric", "Yuan", role = "ctb"), person("Prasanna", "Balaprakash", role = "ctb"), person("Nguyen", "Dang", role = "ctb", email = "nttd@st-andrews.ac.uk"))
# Depends: R (>= 3.2.0)
# Imports: stats, utils, compiler, R6
# Suggests: Rmpi (>= 0.6.0), parallel, knitr, testthat, withr, mlr (>= 2.15.0), ParamHelpers, devtools, covr
# VignetteBuilder: knitr
# License: GPL (>= 2)
# URL: https://mlopez-ibanez.github.io/irace/, https://github.com/MLopez-Ibanez/irace
# BugReports: https://github.com/MLopez-Ibanez/irace/issues
# ByteCompile: yes
# Encoding: UTF-8
# RoxygenNote: 7.1.1
# SystemRequirements: GNU make
# NeedsCompilation: yes
# Packaged: 2022-10-23 13:44:18 UTC; manu
# Author: Manuel Lopez-Ibanez [aut, cre] (<https://orcid.org/0000-0001-9974-1295>), Jeremie Dubois-Lacoste [aut], Leslie Perez Caceres [aut], Thomas Stutzle [aut], Mauro Birattari [aut], Eric Yuan [ctb], Prasanna Balaprakash [ctb], Nguyen Dang [ctb]
# Maintainer: Manuel Lopez-Ibanez <manuel.lopez-ibanez@manchester.ac.uk>
# Repository: CRAN
# Date/Publication: 2022-10-23 14:55:06 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
