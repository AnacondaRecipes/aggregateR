{% set version = '0.8.5' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-adagio
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/adagio_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/adagio/adagio_{{ version }}.tar.gz
  sha256: 0bd046692bfee8b4f9b42796f53a90403b82d3f61cff9f9e97c7ccacd8c89c45

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-lpsolve >=5.6.15

  run:
    - r-base
    - r-lpsolve >=5.6.15

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('adagio')"           # [not win]
    - "\"%R%\" -e \"library('adagio')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://CRAN.R-project.org/package=adagio
  license: GPL-3
  summary: The R package 'adagio' will provide methods and algorithms for discrete optimization,
    e.g. knapsack and subset sum procedures, derivative-free Nelder-Mead and Hooke-Jeeves
    minimization, and some (evolutionary) global optimization functions.
  license_family: GPL3
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-3'

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: adagio
# Type: Package
# Title: Discrete and Global Optimization Routines
# Version: 0.8.5
# Date: 2022-09-30
# Authors@R: person("Hans W.", "Borchers", email = "hwborchers@googlemail.com", role = c("aut", "cre"))
# Maintainer: Hans W. Borchers <hwborchers@googlemail.com>
# Depends: R (>= 3.1.0)
# Imports: graphics, stats, lpSolve (>= 5.6.15)
# Description: The R package 'adagio' will provide methods and algorithms for discrete optimization, e.g. knapsack and subset sum procedures, derivative-free Nelder-Mead and Hooke-Jeeves minimization, and some (evolutionary) global optimization functions.
# License: GPL (>= 3)
# Author: Hans W. Borchers [aut, cre]
# Repository: CRAN
# Repository/R-Forge/Project: optimist
# Repository/R-Forge/Revision: 534
# Repository/R-Forge/DateTimeStamp: 2022-10-03 10:26:01
# Date/Publication: 2022-10-03 13:40:02 UTC
# NeedsCompilation: no
# Packaged: 2022-10-03 10:32:11 UTC; rforge

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
