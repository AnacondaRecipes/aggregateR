{% set version = '3.1-40' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-s20x
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/s20x_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/s20x/s20x_{{ version }}.tar.gz
  sha256: 65c12f5e07367c6ef1370d76f56110abbbc6f760073518dd228de43ad75c4bac

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: emmeans
requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base

  run:
    - r-base

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('s20x')"           # [not win]
    - "\"%R%\" -e \"library('s20x')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://github.com/STATS-UOA/s20x
  license: GPL-2
  summary: A set of functions used in teaching STATS 201/208 Data Analysis at the University
    of Auckland. The functions are designed to make parts of R more accessible to a
    large undergraduate population who are mostly not statistics majors.
  license_family: GPL2
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-2'
    - LICENSE

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: s20x
# Version: 3.1-40
# Date: 2023-08-21
# Title: Functions for University of Auckland Course STATS 201/208 Data Analysis
# Authors@R: c( person("Brant", "Deppa", role = "aut", comment = "Wrote the original R scripts this package is derived from"), person("James", "Curran", , "j.curran@auckland.ac.nz", role = c("aut", "cre"), comment = "Wrote the original R package. Current maintainer."), person("Rachel", "Fewster", , "r.fewster@auckland.ac.nz", role = "ctb"), person("Russell", "Millar", , "r.millar@auckland.ac.nz", role = "ctb"), person("Ben", "Stevenson", , "ben.stevenson@auckland.ac.nz", role = "ctb"), person("Andrew", "Balemi", , "a.balemi@auckland.ac.nz", role = "ctb"), person("Chris", "Wild", , "c.wild@auckland.ac.nz", role = "ctb"), person("Sophie", "Jones", role = "ctb"), person("Dineika", "Chandra", role = "ctr"), person("Brendan", "McArdle", role = "ctr") )
# Description: A set of functions used in teaching STATS 201/208 Data Analysis at the University of Auckland. The functions are designed to make parts of R more accessible to a large undergraduate population who are mostly not statistics majors.
# Depends: R (>= 4.0.0)
# Suggests: emmeans
# Encoding: UTF-8
# Imports: stats, graphics, methods
# License: GPL-2 | file LICENSE
# RoxygenNote: 7.2.3
# URL: https://github.com/STATS-UOA/s20x
# BugReports: https://github.com/STATS-UOA/s20x/issues
# NeedsCompilation: no
# Packaged: 2023-08-21 12:03:42 UTC; jcur002
# Author: Brant Deppa [aut] (Wrote the original R scripts this package is derived from), James Curran [aut, cre] (Wrote the original R package. Current maintainer.), Rachel Fewster [ctb], Russell Millar [ctb], Ben Stevenson [ctb], Andrew Balemi [ctb], Chris Wild [ctb], Sophie Jones [ctb], Dineika Chandra [ctr], Brendan McArdle [ctr]
# Maintainer: James Curran <j.curran@auckland.ac.nz>
# Repository: CRAN
# Date/Publication: 2023-08-21 14:00:02 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
