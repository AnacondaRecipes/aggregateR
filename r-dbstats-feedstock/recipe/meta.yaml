{% set version = '2.0.1' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-dbstats
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/dbstats_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/dbstats/dbstats_{{ version }}.tar.gz
  sha256: 96dd9706379baa04a4fa8b30b91ba48aa44e235c87139f45031f4888721c43e9

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: proxy
requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-cluster
    - r-pls

  run:
    - r-base
    - r-cluster
    - r-pls

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('dbstats')"           # [not win]
    - "\"%R%\" -e \"library('dbstats')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://CRAN.R-project.org/package=dbstats
  license: GPL-2
  summary: Prediction methods where explanatory information is coded as a matrix of distances
    between individuals. Distances can either be directly input as a distances matrix,
    a squared distances matrix, an inner-products matrix or computed from observed predictors.
  license_family: GPL2
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-2'

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: dbstats
# Type: Package
# Title: Distance-Based Statistics
# Version: 2.0.1
# Date: 2022-12-07
# Author: Boj, Eva <evaboj@ub.edu>, Caballe, Adria <adria.caballe@irb.barcelona.org>, Delicado, Pedro <pedro.delicado@upc.edu> and Fortiana, Josep <fortiana@ub.edu>.
# Maintainer: Eva Boj <evaboj@ub.edu>
# Description: Prediction methods where explanatory information is coded as a matrix of distances between individuals. Distances can either be directly input as a distances matrix, a squared distances matrix, an inner-products matrix or computed from observed predictors.
# License: GPL-2
# Repository: CRAN
# Depends: R (>= 4.0.0), cluster, pls
# Suggests: proxy
# NeedsCompilation: no
# Packaged: 2022-12-07 14:50:10 UTC; evaboj
# Date/Publication: 2022-12-07 15:40:06 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
