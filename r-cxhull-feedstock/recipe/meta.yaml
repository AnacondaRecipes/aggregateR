{% set version = '0.7.3' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-cxhull
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/cxhull_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/cxhull/cxhull_{{ version }}.tar.gz
  sha256: 8de662d083f788d34a82e61b93a2891ffb34bb02b58822d509760da5876ac84c

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: colorspace
requirements:
  build:
    - {{ compiler('c') }}              # [not win]
    - {{ compiler('m2w64_c') }}        # [win]
    - {{ posix }}filesystem        # [win]
    - {{ posix }}make
    - {{ posix }}sed               # [win]
    - {{ posix }}coreutils         # [win]
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-morpho
    - r-rvcg
    - r-data.table
    - r-rgl

  run:
    - r-base
    - {{native}}gcc-libs         # [win]
    - r-morpho
    - r-rvcg
    - r-data.table
    - r-rgl

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('cxhull')"           # [not win]
    - "\"%R%\" -e \"library('cxhull')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://github.com/stla/cxhull
  license: GPL-3
  summary: 'Computes the convex hull in arbitrary dimension, based on the Qhull library (<http://www.qhull.org>).
    The package provides a complete description of the convex hull: edges, ridges, facets,
    adjacencies. Triangulation is optional.'
  license_family: GPL3
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-3'

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Type: Package
# Package: cxhull
# Title: Convex Hull
# Version: 0.7.3
# Date: 2023-05-19
# Authors@R: c( person("C. B.", "Barber", role = "cph", comment = "author of the Qhull library"), person("The Geometry Center", role = "cph"), person("Stephane", "Laurent", , "laurent_step@outlook.fr", role = c("cph", "aut", "cre")) )
# Maintainer: Stephane Laurent <laurent_step@outlook.fr>
# Description: Computes the convex hull in arbitrary dimension, based on the Qhull library (<http://www.qhull.org>). The package provides a complete description of the convex hull: edges, ridges, facets, adjacencies. Triangulation is optional.
# License: GPL-3
# URL: https://github.com/stla/cxhull
# BugReports: https://github.com/stla/cxhull/issues
# Depends: R (>= 2.10)
# Imports: data.table, grDevices, Morpho, rgl, Rvcg
# Suggests: colorspace
# Encoding: UTF-8
# LazyData: true
# RoxygenNote: 7.2.3
# NeedsCompilation: yes
# Packaged: 2023-05-19 20:45:53 UTC; stla
# Author: C. B. Barber [cph] (author of the Qhull library), The Geometry Center [cph], Stephane Laurent [cph, aut, cre]
# Repository: CRAN
# Date/Publication: 2023-05-19 21:20:13 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
