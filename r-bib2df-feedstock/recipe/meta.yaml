{% set version = '1.1.1' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-bib2df
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/bib2df_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/bib2df/bib2df_{{ version }}.tar.gz
  sha256: c6d9a087805166e1501f8c3f5d981723b217dcf0b46a7d39c6e41cd70642a734

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: testthat, knitr, rmarkdown, ggplot2, tidyr
requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-dplyr
    - r-httr
    - r-humaniformat
    - r-stringr

  run:
    - r-base
    - r-dplyr
    - r-httr
    - r-humaniformat
    - r-stringr

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('bib2df')"           # [not win]
    - "\"%R%\" -e \"library('bib2df')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://github.com/ropensci/bib2df
  license: GPL-3
  summary: Parse a BibTeX file to a data.frame to make it accessible for further analysis and
    visualization.
  license_family: GPL3
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-3'

# The original CRAN metadata for this package was:

# Package: bib2df
# Type: Package
# Title: Parse a BibTeX File to a Data Frame
# Version: 1.1.1
# Authors@R: c(person("Philipp", "Ottolinger", email = "philipp@ottolinger.de", role = c("aut", "cre")), person("Thomas", "Leeper", email = "thosjleeper@gmail.com", role = "ctb"), person("Maelle", "Salmon", email = "maelle.salmon@yahoo.se", role = "ctb"), person("Paul", "Egeler", email = "paulegeler@gmail.com", role = "ctb"), person("Emilio Xavier", "Esposito", email = "emilio.esposito@gmail.com", role = "ctb"))
# Description: Parse a BibTeX file to a data.frame to make it accessible for further analysis and visualization.
# URL: https://github.com/ropensci/bib2df
# BugReports: http://github.com/ropensci/bib2df/issues
# License: GPL-3
# LazyData: TRUE
# Imports: dplyr, stringr, humaniformat, httr
# Suggests: testthat, knitr, rmarkdown, ggplot2, tidyr
# RoxygenNote: 6.1.1
# VignetteBuilder: knitr
# Encoding: UTF-8
# NeedsCompilation: no
# Packaged: 2019-05-22 20:54:58 UTC; philipp
# Author: Philipp Ottolinger [aut, cre], Thomas Leeper [ctb], Maelle Salmon [ctb], Paul Egeler [ctb], Emilio Xavier Esposito [ctb]
# Maintainer: Philipp Ottolinger <philipp@ottolinger.de>
# Repository: CRAN
# Date/Publication: 2019-05-22 22:15:15 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
