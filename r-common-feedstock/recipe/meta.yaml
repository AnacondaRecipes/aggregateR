{% set version = '1.1.0' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-common
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/common_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/common/common_{{ version }}.tar.gz
  sha256: 183e5f937823ff6b8ef1b22679177c3e25ffbb256784879ece0cefdba78e9978

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: knitr, rmarkdown, testthat (>= 3.0.0), glue, box, rstudioapi
requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base

  run:
    - r-base

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('common')"           # [not win]
    - "\"%R%\" -e \"library('common')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://common.r-sassy.org
  license: CC0
  summary: Contains functions for solving commonly encountered problems while programming in
    R. This package is intended to provide a lightweight supplement to Base R, and will
    be useful for almost any R user.
  license_family: CC

# The original CRAN metadata for this package was:

# Package: common
# Type: Package
# Title: Solutions for Common Problems in Base R
# Version: 1.1.0
# Authors@R: c( person(given = "David", family = "Bosak", role = c("aut", "cre"), email = "dbosak01@gmail.com"), person(given = "Duong", family = "Tran", role = c("ctb"), email = "trand000@aol.com") )
# Maintainer: David Bosak <dbosak01@gmail.com>
# Description: Contains functions for solving commonly encountered problems while programming in R. This package is intended to provide a lightweight supplement to Base R, and will be useful for almost any R user.
# License: CC0
# Encoding: UTF-8
# URL: https://common.r-sassy.org
# BugReports: https://github.com/dbosak01/common/issues
# Depends: R (>= 3.6.0)
# Suggests: knitr, rmarkdown, testthat (>= 3.0.0), glue, box, rstudioapi
# Enhances: base
# Imports: utils
# Config/testthat/edition: 3
# RoxygenNote: 7.2.3
# VignetteBuilder: knitr
# NeedsCompilation: no
# Packaged: 2023-10-25 17:53:19 UTC; dbosa
# Author: David Bosak [aut, cre], Duong Tran [ctb]
# Repository: CRAN
# Date/Publication: 2023-10-25 19:40:08 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
