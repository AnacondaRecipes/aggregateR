{% set version = '1.1.2' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-ekmcmc
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/EKMCMC_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/EKMCMC/EKMCMC_{{ version }}.tar.gz
  sha256: cf7d5d2bf2a46cb43f88a60a387c5a8334309a8b9872dd0f2c65af197be0343a

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: rmarkdown, knitr
requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-numderiv

  run:
    - r-base
    - r-numderiv

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('EKMCMC')"           # [not win]
    - "\"%R%\" -e \"library('EKMCMC')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://CRAN.R-project.org/package=EKMCMC
  license: GPL-3
  summary: Functions for estimating catalytic constant and Michaelis-Menten constant for enzyme
    kinetics model using Metropolis-Hasting algorithm within Gibbs sampler based on
    the Bayesian framework.
  license_family: GPL3
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-3'

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: EKMCMC
# Type: Package
# Title: MCMC Procedures for Estimating Enzyme Kinetics Constants
# Version: 1.1.2
# Author: Hyukpyo Hong, Boseung Choi, Jae Kyoung Kim
# Maintainer: Hyukpyo Hong <hphong@kaist.ac.kr>
# Description: Functions for estimating catalytic constant and Michaelis-Menten constant for enzyme kinetics model using Metropolis-Hasting algorithm within Gibbs sampler based on the Bayesian framework.
# License: GPL-3
# Encoding: UTF-8
# LazyData: true
# RoxygenNote: 7.1.1
# Imports: numDeriv
# Depends: R (>= 3.5.0)
# Suggests: rmarkdown, knitr
# NeedsCompilation: no
# Packaged: 2021-08-19 13:19:01 UTC; hyukpyohong
# Repository: CRAN
# Date/Publication: 2021-08-20 06:20:02 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
