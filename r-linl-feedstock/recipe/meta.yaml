{% set version = '0.0.5' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-linl
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/linl_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/linl/linl_{{ version }}.tar.gz
  sha256: 9dd8b035c91a003155899e0515bf66c1cba166247d3f26befc121e21b6cef202

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: testthat, yaml
requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-knitr
    - r-rmarkdown

  run:
    - r-base
    - r-knitr
    - r-rmarkdown

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('linl')"           # [not win]
    - "\"%R%\" -e \"library('linl')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://github.com/eddelbuettel/linl, https://dirk.eddelbuettel.com/code/linl.html
  license: GPL-3
  summary: A 'LaTeX' Letter class for 'rmarkdown', using the 'pandoc-letter' template adapted
    for use with 'markdown'.
  license_family: GPL3
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-3'

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: linl
# Type: Package
# Title: 'linl' is not 'Letter'
# Version: 0.0.5
# Date: 2023-01-11
# Author: Dirk Eddelbuettel and Aaron Wolen
# Maintainer: Dirk Eddelbuettel <edd@debian.org>
# Description: A 'LaTeX' Letter class for 'rmarkdown', using the 'pandoc-letter' template adapted for use with 'markdown'.
# URL: https://github.com/eddelbuettel/linl, https://dirk.eddelbuettel.com/code/linl.html
# BugReports: https://github.com/eddelbuettel/linl/issues
# Imports: rmarkdown, knitr
# Suggests: testthat, yaml
# VignetteBuilder: knitr
# License: GPL-3
# Encoding: UTF-8
# RoxygenNote: 7.2.3
# NeedsCompilation: no
# Packaged: 2023-01-12 01:33:47 UTC; edd
# Repository: CRAN
# Date/Publication: 2023-01-12 03:20:02 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
