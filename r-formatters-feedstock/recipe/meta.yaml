{% set version = '0.5.2' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-formatters
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/formatters_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/formatters/formatters_{{ version }}.tar.gz
  sha256: cb70a09d806876fec29371500eeaab24c9e8e2a1ce917f037ae3cbc2af6ad1e4

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: dplyr, gt (>= 0.7.0), huxtable, knitr, r2rtf, rmarkdown, testthat
requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-checkmate
    - r-htmltools

  run:
    - r-base
    - r-checkmate
    - r-htmltools

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('formatters')"           # [not win]
    - "\"%R%\" -e \"library('formatters')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://github.com/insightsengineering/formatters
  license: Apache License 2.0
  summary: We provide a framework for rendering complex tables to ASCII, and a set of formatters
    for transforming values or sets of values into ASCII-ready display strings.
  license_family: APACHE

# The original CRAN metadata for this package was:

# Package: formatters
# Title: ASCII Formatting for Values and Tables
# Version: 0.5.2
# Date: 2023-08-24
# Authors@R: c( person("Gabriel", "Becker", , "gabembecker@gmail.com", role = c("aut"), comment = "original creator of the package"), person("Adrian", "Waddell", , "adrian.waddell@gene.com", role = "aut"), person("Davide", "Garolini", , "davide.garolini@roche.com", role = "ctb"), person("Emily", "de la Rua", , "emily.de_la_rua@contractors.roche.com", role = "ctb"), person("Abinaya", "Yogasekaram", , "abinaya.yogasekaram@contractors.roche.com", role = "ctb"), person("Joe", "Zhu", , "joe.zhu@roche.com", role = c("ctb", "cre")), person("F. Hoffmann-La Roche AG", role = c("cph", "fnd")) )
# Description: We provide a framework for rendering complex tables to ASCII, and a set of formatters for transforming values or sets of values into ASCII-ready display strings.
# License: Apache License 2.0
# URL: https://github.com/insightsengineering/formatters
# BugReports: https://github.com/insightsengineering/formatters/issues
# Depends: methods, R (>= 2.10)
# Imports: checkmate, grid, htmltools
# Suggests: dplyr, gt (>= 0.7.0), huxtable, knitr, r2rtf, rmarkdown, testthat
# VignetteBuilder: knitr
# Config/Needs/website: insightsengineering/nesttemplate
# Encoding: UTF-8
# Language: en-US
# LazyData: true
# RoxygenNote: 7.2.3
# Collate: 'data.R' 'format_value.R' 'matrix_form.R' 'generics.R' 'labels.R' 'mpf_exporters.R' 'page_size.R' 'pagination.R' 'tostring.R' 'utils.R'
# NeedsCompilation: no
# Packaged: 2023-08-25 04:55:21 UTC; rstudio
# Author: Gabriel Becker [aut] (original creator of the package), Adrian Waddell [aut], Davide Garolini [ctb], Emily de la Rua [ctb], Abinaya Yogasekaram [ctb], Joe Zhu [ctb, cre], F. Hoffmann-La Roche AG [cph, fnd]
# Maintainer: Joe Zhu <joe.zhu@roche.com>
# Repository: CRAN
# Date/Publication: 2023-08-25 09:30:02 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
