{% set version = '1.1.2' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-cutpointr
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/cutpointr_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/cutpointr/cutpointr_{{ version }}.tar.gz
  sha256: 3280ecedb81e373629b20d2186285f86397a28e7ef684ac04e700d0d8180b6e3

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: KernSmooth (>= 2.23-15), fANCOVA (>= 0.5-1), testthat (>= 1.0.2), doRNG (>= 1.6), doParallel (>= 1.0.11), knitr, rmarkdown, mgcv (>= 1.8), crayon (>= 1.3.4), registry (>= 0.5-1), pkgmaker(>= 0.31.1), vctrs (>= 0.2.4)
requirements:
  build:
    - {{ compiler('c') }}              # [not win]
    - {{ compiler('m2w64_c') }}        # [win]
    - {{ compiler('cxx') }}            # [not win]
    - {{ compiler('m2w64_cxx') }}      # [win]
    - {{ posix }}filesystem        # [win]
    - {{ posix }}make
    - {{ posix }}sed               # [win]
    - {{ posix }}coreutils         # [win]
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-rcpp >=0.12.12
    - r-dplyr >=0.8.0
    - r-foreach >=1.4.3
    - r-ggplot2 >=3.0.0
    - r-gridextra >=2.2.1
    - r-purrr >=0.3.0
    - r-rlang >=0.4.0
    - r-tibble >=3.0.0
    - r-tidyr >=1.0.0
    - r-tidyselect >=1.1.0

  run:
    - r-base
    - {{native}}gcc-libs         # [win]
    - r-rcpp >=0.12.12
    - r-dplyr >=0.8.0
    - r-foreach >=1.4.3
    - r-ggplot2 >=3.0.0
    - r-gridextra >=2.2.1
    - r-purrr >=0.3.0
    - r-rlang >=0.4.0
    - r-tibble >=3.0.0
    - r-tidyr >=1.0.0
    - r-tidyselect >=1.1.0

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('cutpointr')"           # [not win]
    - "\"%R%\" -e \"library('cutpointr')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://github.com/thie1e/cutpointr
  license: GPL-3
  summary: Estimate cutpoints that optimize a specified metric in binary classification tasks
    and validate performance using bootstrapping. Some methods for more robust cutpoint
    estimation are supported, e.g. a parametric method assuming normal distributions,
    bootstrapped cutpoints, and smoothing of the metric values per cutpoint using Generalized
    Additive Models. Various plotting functions are included. For an overview of the
    package see Thiele and Hirschfeld (2021) <doi:10.18637/jss.v098.i11>.
  license_family: GPL3
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-3'

# The original CRAN metadata for this package was:

# Package: cutpointr
# Type: Package
# Title: Determine and Evaluate Optimal Cutpoints in Binary Classification Tasks
# Version: 1.1.2
# Date: 2022-04-13
# Authors@R: person(given = "Christian", family = "Thiele", role = c("cre", "aut"), email = "c.thiele@gmx-topmail.de", comment = c(ORCID = "0000-0002-1156-5117"))
# Description: Estimate cutpoints that optimize a specified metric in binary classification tasks and validate performance using bootstrapping. Some methods for more robust cutpoint estimation are supported, e.g. a parametric method assuming normal distributions, bootstrapped cutpoints, and smoothing of the metric values per cutpoint using Generalized Additive Models. Various plotting functions are included. For an overview of the package see Thiele and Hirschfeld (2021) <doi:10.18637/jss.v098.i11>.
# License: GPL-3
# URL: https://github.com/thie1e/cutpointr
# BugReports: https://github.com/thie1e/cutpointr/issues
# Encoding: UTF-8
# LazyData: true
# Depends: R (>= 3.5.0)
# LinkingTo: Rcpp
# Imports: gridExtra (>= 2.2.1), foreach (>= 1.4.3), dplyr (>= 0.8.0), tidyselect (>= 1.1.0), tidyr (>= 1.0.0), purrr (>= 0.3.0), tibble (>= 3.0.0), ggplot2 (>= 3.0.0), Rcpp (>= 0.12.12), stats, utils, rlang (>= 0.4.0)
# RoxygenNote: 7.1.2
# Suggests: KernSmooth (>= 2.23-15), fANCOVA (>= 0.5-1), testthat (>= 1.0.2), doRNG (>= 1.6), doParallel (>= 1.0.11), knitr, rmarkdown, mgcv (>= 1.8), crayon (>= 1.3.4), registry (>= 0.5-1), pkgmaker(>= 0.31.1), vctrs (>= 0.2.4)
# VignetteBuilder: knitr
# NeedsCompilation: yes
# Packaged: 2022-04-13 17:35:21 UTC; Khl4v
# Author: Christian Thiele [cre, aut] (<https://orcid.org/0000-0002-1156-5117>)
# Maintainer: Christian Thiele <c.thiele@gmx-topmail.de>
# Repository: CRAN
# Date/Publication: 2022-04-13 18:12:29 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
