{% set version = '1.0.9-1' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-sapp
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/SAPP_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/SAPP/SAPP_{{ version }}.tar.gz
  sha256: 46e2a389d86800f24f4792acbde9abde5f90c636fba84bd77f2c8fb0eaebfed9

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

requirements:
  build:
    - {{ compiler('c') }}              # [not win]
    - {{ compiler('m2w64_c') }}        # [win]
    - {{ compiler('fortran') }}        # [not win]
    - {{ compiler('m2w64_fortran') }}  # [win]
    - {{ posix }}filesystem        # [win]
    - {{ posix }}make
    - {{ posix }}sed               # [win]
    - {{ posix }}coreutils         # [win]
    - {{ posix }}zip               # [win]

  host:
    - r-base

  run:
    - r-base
    - {{native}}gcc-libs         # [win]

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('SAPP')"           # [not win]
    - "\"%R%\" -e \"library('SAPP')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://CRAN.R-project.org/package=SAPP
  license: GPL-2
  summary: Functions for statistical analysis of point processes.
  license_family: GPL2
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-2'

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: SAPP
# Version: 1.0.9-1
# Title: Statistical Analysis of Point Processes
# Author: The Institute of Statistical Mathematics
# Maintainer: Masami Saga <msaga@mtb.biglobe.ne.jp>
# Depends: R (>= 4.0.0)
# Imports: graphics, grDevices
# Description: Functions for statistical analysis of point processes.
# License: GPL (>= 2)
# MailingList: Please send bug reports to ismrp@grp.ism.ac.jp.
# NeedsCompilation: yes
# Packaged: 2023-06-02 02:09:07 UTC; msaga
# Repository: CRAN
# Date/Publication: 2023-06-02 03:50:02 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
