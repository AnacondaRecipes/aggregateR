{% set version = '0.2.3' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-badger
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/badger_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/badger/badger_{{ version }}.tar.gz
  sha256: 8ac7d8654dcf4dc72dd53bdd58354946cb830dc2f513d679f78c91bd4a36fd48

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: testthat (>= 2.0.0)
requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-desc
    - r-dlstats
    - r-rvcheck
    - r-usethis >=2.0.0

  run:
    - r-base
    - r-desc
    - r-dlstats
    - r-rvcheck
    - r-usethis >=2.0.0

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('badger')"           # [not win]
    - "\"%R%\" -e \"library('badger')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://github.com/GuangchuangYu/badger
  license: Artistic-2.0
  summary: Query information and generate badge for using in README and GitHub Pages.
  license_family: OTHER
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/Artistic-2.0'

# The original CRAN metadata for this package was:

# Package: badger
# Title: Badge for R Package
# Version: 0.2.3
# Authors@R: c( person("Guangchuang", "Yu",          email = "guangchuangyu@gmail.com",    role = c("aut", "cre"), comment = c(ORCID = "0000-0002-6485-8781")), person("Gregor", "de Cillia",        email = "de.cillia.gregor@gmail.com", role = "ctb"), person("Dirk", "Eddelbuettel",       email = "edd@debian.org",             role = "ctb"), person("Maelle", "Salmon",                                                 role = "ctb"), person("Robrecht", "Cannoodt",       email = "rcannood@gmail.com",         role = "ctb", comment = c(ORCID = "0000-0003-3641-729X", github = "rcannood")), person("Alexander", "Rossell Hayes", email = "alexander@rossellhayes.com", role = "ctb", comment = c(ORCID = "0000-0001-9412-0457", github = "rossellhayes")), person("Waldir", "Leoncio",          email = "w.l.netto@medisin.uio.no",   role = "ctb") )
# Description: Query information and generate badge for using in README and GitHub Pages.
# Depends: R (>= 3.3.0)
# Imports: dlstats, rvcheck, desc, usethis (>= 2.0.0)
# Suggests: testthat (>= 2.0.0)
# License: Artistic-2.0
# ByteCompile: true
# URL: https://github.com/GuangchuangYu/badger
# BugReports: https://github.com/GuangchuangYu/badger/issues
# Encoding: UTF-8
# RoxygenNote: 7.2.3
# NeedsCompilation: no
# Packaged: 2023-01-28 06:37:28 UTC; ygc
# Author: Guangchuang Yu [aut, cre] (<https://orcid.org/0000-0002-6485-8781>), Gregor de Cillia [ctb], Dirk Eddelbuettel [ctb], Maelle Salmon [ctb], Robrecht Cannoodt [ctb] (<https://orcid.org/0000-0003-3641-729X>, rcannood), Alexander Rossell Hayes [ctb] (<https://orcid.org/0000-0001-9412-0457>, rossellhayes), Waldir Leoncio [ctb]
# Maintainer: Guangchuang Yu <guangchuangyu@gmail.com>
# Repository: CRAN
# Date/Publication: 2023-01-28 13:40:10 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
