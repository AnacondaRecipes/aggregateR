{% set version = '1.2-7' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-brobdingnag
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/Brobdingnag_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/Brobdingnag/Brobdingnag_{{ version }}.tar.gz
  sha256: 73a734342736da5b29c2827d91f662101873503af7ad9cdf9e9e697bb32dd742

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: cubature
requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base

  run:
    - r-base

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('Brobdingnag')"           # [not win]
    - "\"%R%\" -e \"library('Brobdingnag')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://github.com/RobinHankin/Brobdingnag
  license: GPL-3
  summary: Very large numbers in R.  Real numbers are held using their natural logarithms, plus
    a logical flag indicating sign.  Functionality for complex numbers is also provided.  The
    package includes a vignette that gives a step-by-step introduction to using S4 methods.
  license_family: GPL3
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-3'

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: Brobdingnag
# Type: Package
# Title: Very Large Numbers in R
# Version: 1.2-7
# Authors@R: person(given=c("Robin", "K. S."), family="Hankin", role = c("aut","cre"), email="hankin.robin@gmail.com", comment = c(ORCID = "0000-0001-5982-0415"))
# Maintainer: Robin K. S. Hankin <hankin.robin@gmail.com>
# Depends: R (>= 2.13.0), methods
# Description: Very large numbers in R.  Real numbers are held using their natural logarithms, plus a logical flag indicating sign.  Functionality for complex numbers is also provided.  The package includes a vignette that gives a step-by-step introduction to using S4 methods.
# Suggests: cubature
# License: GPL
# Repository: CRAN
# URL: https://github.com/RobinHankin/Brobdingnag
# NeedsCompilation: no
# Packaged: 2022-02-02 21:08:17 UTC; rhankin
# Author: Robin K. S. Hankin [aut, cre] (<https://orcid.org/0000-0001-5982-0415>)
# Date/Publication: 2022-02-03 07:50:18 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
