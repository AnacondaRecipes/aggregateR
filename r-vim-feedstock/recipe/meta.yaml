{% set version = '6.2.2' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-vim
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/VIM_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/VIM/VIM_{{ version }}.tar.gz
  sha256: afa7492c54508c46eff39ac66fa4b05627e0044253ebe4a61b2a78d459f715e4

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: dplyr, tinytest, knitr, rmarkdown, reactable, covr, withr
requirements:
  build:
    - {{ compiler('c') }}              # [not win]
    - {{ compiler('m2w64_c') }}        # [win]
    - {{ compiler('cxx') }}            # [not win]
    - {{ compiler('m2w64_cxx') }}      # [win]
    - {{ posix }}filesystem        # [win]
    - {{ posix }}make
    - {{ posix }}sed               # [win]
    - {{ posix }}coreutils         # [win]
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-mass
    - r-rcpp
    - r-car
    - r-colorspace
    - r-data.table >=1.9.4
    - r-e1071
    - r-laeken
    - r-magrittr
    - r-nnet
    - r-ranger
    - r-robustbase
    - r-sp
    - r-vcd

  run:
    - r-base
    - {{native}}gcc-libs         # [win]
    - r-mass
    - r-rcpp
    - r-car
    - r-colorspace
    - r-data.table >=1.9.4
    - r-e1071
    - r-laeken
    - r-magrittr
    - r-nnet
    - r-ranger
    - r-robustbase
    - r-sp
    - r-vcd

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('VIM')"           # [not win]
    - "\"%R%\" -e \"library('VIM')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://github.com/statistikat/VIM
  license: GPL-2
  summary: New tools for the visualization of missing and/or imputed values are introduced, which
    can be used for exploring the data and the structure of the missing and/or imputed
    values. Depending on this structure of the missing values, the corresponding methods
    may help to identify the mechanism generating the missing values and allows to explore
    the data including missing values. In addition, the quality of imputation can be
    visually explored using various univariate, bivariate, multiple and multivariate
    plot methods. A graphical user interface available in the separate package VIMGUI
    allows an easy handling of the implemented plot methods.
  license_family: GPL2
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-2'

# The original CRAN metadata for this package was:

# Package: VIM
# Version: 6.2.2
# Title: Visualization and Imputation of Missing Values
# Authors@R: c( person("Matthias", "Templ", email = "matthias.templ@gmail.com", role = c("aut","cre")), person("Alexander", "Kowarik", email = "alexander.kowarik@statistik.gv.at", role = c("aut"), comment=c(ORCID="0000-0001-8598-4130")), person("Andreas", "Alfons", role = c("aut")), person("Gregor", "de Cillia", email = "gregor.decillia@statistik.gv.at", role = c("aut")), person("Bernd", "Prantner", role = c("ctb")), person("Wolfgang", "Rannetbauer", role = c("aut")))
# Depends: R (>= 3.5.0),colorspace,grid
# Imports: car, grDevices, magrittr, robustbase, stats, sp, vcd,MASS,nnet,e1071,methods,Rcpp,utils,graphics,laeken,ranger, data.table(>= 1.9.4)
# Suggests: dplyr, tinytest, knitr, rmarkdown, reactable, covr, withr
# Description: New tools for the visualization of missing and/or imputed values are introduced, which can be used for exploring the data and the structure of the missing and/or imputed values. Depending on this structure of the missing values, the corresponding methods may help to identify the mechanism generating the missing values and allows to explore the data including missing values. In addition, the quality of imputation can be visually explored using various univariate, bivariate, multiple and multivariate plot methods. A graphical user interface available in the separate package VIMGUI allows an easy handling of the implemented plot methods.
# LazyData: TRUE
# ByteCompile: TRUE
# License: GPL (>= 2)
# URL: https://github.com/statistikat/VIM
# Repository: CRAN
# LinkingTo: Rcpp
# RoxygenNote: 7.1.2
# Encoding: UTF-8
# VignetteBuilder: knitr
# NeedsCompilation: yes
# Packaged: 2022-08-23 14:44:03 UTC; alex
# Author: Matthias Templ [aut, cre], Alexander Kowarik [aut] (<https://orcid.org/0000-0001-8598-4130>), Andreas Alfons [aut], Gregor de Cillia [aut], Bernd Prantner [ctb], Wolfgang Rannetbauer [aut]
# Maintainer: Matthias Templ <matthias.templ@gmail.com>
# Date/Publication: 2022-08-25 12:12:42 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
