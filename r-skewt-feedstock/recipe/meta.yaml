{% set version = '1.0' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-skewt
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/skewt_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/skewt/skewt_{{ version }}.tar.gz
  sha256: 878e4e26e1b5fe0bbd98669f93a848ba28cddbbf7f8324f2167e7ef9b06d54c9

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base

  run:
    - r-base

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('skewt')"           # [not win]
    - "\"%R%\" -e \"library('skewt')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://CRAN.R-project.org/package=skewt
  license: GPL-3
  summary: Density, distribution function, quantile function and random generation for the skewed
    t distribution of Fernandez and Steel.
  license_family: GPL3
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-3'

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: skewt
# Version: 1.0
# Date: 2021-12-10
# Title: The Skewed Student-t Distribution
# Authors@R: c(person(given="Robert",family="King", role=c("aut","cre"), email="Robert.King.newcastle@gmail.com", comment=c(ORCID="0000-0001-7495-6599")), person(given="Emily",family="Anderson",role="aut") )
# Description: Density, distribution function, quantile function and random generation for the skewed t distribution of Fernandez and Steel.
# License: GPL
# NeedsCompilation: no
# Packaged: 2021-12-09 23:57:40 UTC; rober
# Author: Robert King [aut, cre] (<https://orcid.org/0000-0001-7495-6599>), Emily Anderson [aut]
# Maintainer: Robert King <Robert.King.newcastle@gmail.com>
# Repository: CRAN
# Date/Publication: 2021-12-10 12:00:02 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
