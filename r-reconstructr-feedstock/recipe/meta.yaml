{% set version = '2.0.4' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-reconstructr
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/reconstructr_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/reconstructr/reconstructr_{{ version }}.tar.gz
  sha256: 262c8298aa40634482c27635b9fbcf16b9208af81297799816d9d7e952b8db16

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: testthat, knitr
requirements:
  build:
    - {{ compiler('c') }}              # [not win]
    - {{ compiler('m2w64_c') }}        # [win]
    - {{ compiler('cxx') }}            # [not win]
    - {{ compiler('m2w64_cxx') }}      # [win]
    - {{ posix }}filesystem        # [win]
    - {{ posix }}make
    - {{ posix }}sed               # [win]
    - {{ posix }}coreutils         # [win]
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-rcpp
    - r-openssl

  run:
    - r-base
    - {{native}}gcc-libs         # [win]
    - r-rcpp
    - r-openssl

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('reconstructr')"           # [not win]
    - "\"%R%\" -e \"library('reconstructr')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://github.com/Ironholds/reconstructr
  license: MIT
  summary: Functions to reconstruct sessions from web log or other user trace data and calculate
    various metrics around them, producing tabular, output that is compatible with 'dplyr'
    or 'data.table' centered processes.
  license_family: MIT
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/MIT'
    - LICENSE

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: reconstructr
# Type: Package
# Title: Session Reconstruction and Analysis
# Version: 2.0.4
# Date: 2022-02-17
# Author: Os Keyes
# Maintainer: Os Keyes <ironholds@gmail.com>
# Description: Functions to reconstruct sessions from web log or other user trace data and calculate various metrics around them, producing tabular, output that is compatible with 'dplyr' or 'data.table' centered processes.
# License: MIT + file LICENSE
# LinkingTo: Rcpp
# Encoding: UTF-8
# LazyData: TRUE
# Imports: Rcpp, openssl
# Suggests: testthat, knitr
# VignetteBuilder: knitr
# URL: https://github.com/Ironholds/reconstructr
# BugReports: https://github.com/Ironholds/reconstructr/issues
# RoxygenNote: 7.1.2
# Depends: R (>= 3.3.0)
# NeedsCompilation: yes
# Packaged: 2022-02-17 18:25:35 UTC; ironholds
# Repository: CRAN
# Date/Publication: 2022-02-18 08:30:02 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
