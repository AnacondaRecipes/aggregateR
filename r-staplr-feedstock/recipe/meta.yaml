{% set version = '3.1.1' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-staplr
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/staplr_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/staplr/staplr_{{ version }}.tar.gz
  sha256: ea71ff5bf89c51fc5ef545ae9760dc3a6db4c47aa0ae8479199cc466f1086a3c

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: lattice, testthat, pdftools
requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-xml
    - r-assertthat
    - r-glue
    - r-rjava
    - r-stringr
    - openjdk  # [linux]

  run:
    - r-base
    - r-xml
    - r-assertthat
    - r-glue
    - r-rjava
    - r-stringr

test:
  requires:
    - openjdk  # [linux]
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('staplr')"           # [not win]
    - "\"%R%\" -e \"library('staplr')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://CRAN.R-project.org/package=staplr
  license: GPL-3
  summary: 'Provides function to manipulate PDF files: fill out PDF forms; merge multiple PDF
    files into one; remove selected pages from a file; rename multiple files in a directory;
    rotate entire pdf document; rotate selected pages of a pdf file; Select pages from
    a file; splits single input PDF document into individual pages; splits single input
    PDF document into parts from given points. ''staplr'' requires Java 8 installed
    on your system.'
  license_family: GPL3
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-3'

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: staplr
# Type: Package
# Title: A Toolkit for PDF Files
# Version: 3.1.1
# Depends: R (>= 3.4.0)
# Authors@R: c( person("Priyanga Dilini", "Talagala", email="pritalagala@gmail.com", role= c("aut","cre")), person("Ogan", "Mancarci", email="ogan.mancarci@gmail.com", role='aut'), person("Daniel", "Padfield", email= "d.padfield@exeter.ac.uk", role ='aut'), person("Granville", "Matheson", email= "mathesong@gmail.com", role ='aut') )
# Description: Provides function to manipulate PDF files: fill out PDF forms; merge multiple PDF files into one; remove selected pages from a file; rename multiple files in a directory; rotate entire pdf document; rotate selected pages of a pdf file; Select pages from a file; splits single input PDF document into individual pages; splits single input PDF document into parts from given points. 'staplr' requires Java 8 installed on your system.
# SystemRequirements: Java 8 or higher
# License: GPL-3
# LazyData: true
# RoxygenNote: 7.1.1
# Imports: tcltk, stringr, assertthat, glue, XML, rJava
# Suggests: lattice, testthat, pdftools
# Encoding: UTF-8
# BugReports: https://github.com/pridiltal/staplr/issues
# NeedsCompilation: no
# Packaged: 2021-01-11 08:41:04 UTC; priyangatalagala
# Author: Priyanga Dilini Talagala [aut, cre], Ogan Mancarci [aut], Daniel Padfield [aut], Granville Matheson [aut]
# Maintainer: Priyanga Dilini Talagala <pritalagala@gmail.com>
# Repository: CRAN
# Date/Publication: 2021-01-11 09:40:02 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
