{% set version = '0.2-16' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-skmeans
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/skmeans_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/skmeans/skmeans_{{ version }}.tar.gz
  sha256: 3aab34ab867cf53deafc3f16164bf8fed73ac4543897f1a1903b0682e5742f66

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-clue >=0.3_39
    - r-cluster
    - r-slam >=0.1_31

  run:
    - r-base
    - r-clue >=0.3_39
    - r-cluster
    - r-slam >=0.1_31

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('skmeans')"           # [not win]
    - "\"%R%\" -e \"library('skmeans')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://CRAN.R-project.org/package=skmeans
  license: GPL-2
  summary: Algorithms to compute spherical k-means partitions. Features several methods, including
    a genetic and a fixed-point algorithm and an interface to the CLUTO vcluster program.
  license_family: GPL2
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-2'

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: skmeans
# Version: 0.2-16
# Title: Spherical k-Means Clustering
# Authors@R: c(person("Kurt", "Hornik", role = c("aut", "cre"), email = "Kurt.Hornik@R-project.org", comment = c(ORCID = "0000-0003-4198-9911")), person("Ingo", "Feinerer", role = "aut", comment = c(ORCID = "0000-0001-7656-8338")), person("Martin", "Kober", role = "aut"))
# Description: Algorithms to compute spherical k-means partitions. Features several methods, including a genetic and a fixed-point algorithm and an interface to the CLUTO vcluster program.
# Imports: slam (>= 0.1-31), clue (>= 0.3-39), cluster, stats, utils
# Enhances: Matrix, kmndirs
# Additional_repositories: https://R-Forge.R-project.org/
# License: GPL-2
# NeedsCompilation: no
# Packaged: 2023-09-23 11:02:54 UTC; hornik
# Author: Kurt Hornik [aut, cre] (<https://orcid.org/0000-0003-4198-9911>), Ingo Feinerer [aut] (<https://orcid.org/0000-0001-7656-8338>), Martin Kober [aut]
# Maintainer: Kurt Hornik <Kurt.Hornik@R-project.org>
# Repository: CRAN
# Date/Publication: 2023-09-23 11:26:02 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
