{% set version = '1.1' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-correctedfdr
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/CorrectedFDR_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/CorrectedFDR/CorrectedFDR_{{ version }}.tar.gz
  sha256: 5a3022404b278ee3012f5122e62c403f90a454e7725e9227f0d49a7deab90fdc

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: LFDR.MLE, LFDREmpiricalBayes, ProData
requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base

  run:
    - r-base

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('CorrectedFDR')"           # [not win]
    - "\"%R%\" -e \"library('CorrectedFDR')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://CRAN.R-project.org/package=CorrectedFDR
  license: LGPL-3
  summary: 'There are many estimators of false discovery rate. In this package we compute the
    Nonlocal False Discovery Rate (NFDR) and the estimators of local false discovery
    rate: Corrected False discovery Rate (CFDR), Re-ranked False Discovery rate (RFDR)
    and the blended estimator. Bickel, D.R., Rahal, A. (2019) <https://tinyurl.com/kkdc9rk8>.'
  license_family: LGPL
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/LGPL-3'

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: CorrectedFDR
# Type: Package
# Title: Correcting False Discovery Rates
# Version: 1.1
# Date: 2021-10-06
# Author: Abbas Rahal, Anna Akpawu, Justin Chitpin and David R. Bickel
# Maintainer: Abbas Rahal <Abbas.Rahal13@gmail.com>
# Description: There are many estimators of false discovery rate. In this package we compute the Nonlocal False Discovery Rate (NFDR) and the estimators of local false discovery rate: Corrected False discovery Rate (CFDR), Re-ranked False Discovery rate (RFDR) and the blended estimator. Bickel, D.R., Rahal, A. (2019) <https://tinyurl.com/kkdc9rk8>.
# Depends: R(>= 2.14.2)
# Suggests: LFDR.MLE, LFDREmpiricalBayes, ProData
# biocViews: Bayesian Statistics, MathematicalBiology
# License: LGPL-3
# NeedsCompilation: no
# Packaged: 2021-10-06 17:58:35 UTC; Rahal
# Repository: CRAN
# Date/Publication: 2021-10-07 13:50:06 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
