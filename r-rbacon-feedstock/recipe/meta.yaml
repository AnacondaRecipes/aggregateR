{% set version = '3.1.1' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-rbacon
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/rbacon_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/rbacon/rbacon_{{ version }}.tar.gz
  sha256: 57c2bb67199b99855fb9c9c4626acb34c84d38e9e86a4ee72ca8c041ae69810d

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: knitr, rmarkdown, utf8
requirements:
  build:
    - {{ compiler('c') }}              # [not win]
    - {{ compiler('m2w64_c') }}        # [win]
    - {{ compiler('cxx') }}            # [not win]
    - {{ compiler('m2w64_cxx') }}      # [win]
    - {{ posix }}filesystem        # [win]
    - {{ posix }}make
    - {{ posix }}sed               # [win]
    - {{ posix }}coreutils         # [win]
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-rcpp >=0.12.12
    - r-coda >=0.19_1
    - r-data.table
    - r-rintcal >=0.6.1

  run:
    - r-base
    - {{native}}gcc-libs         # [win]
    - r-rcpp >=0.12.12
    - r-coda >=0.19_1
    - r-data.table
    - r-rintcal >=0.6.1

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('rbacon')"           # [not win]
    - "\"%R%\" -e \"library('rbacon')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://CRAN.R-project.org/package=rbacon
  license: GPL-2
  summary: An approach to age-depth modelling that uses Bayesian statistics to reconstruct accumulation
    histories for deposits, through combining radiocarbon and other dates with prior
    information on accumulation rates and their variability. See Blaauw & Christen (2011).
  license_family: GPL2
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-2'

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: rbacon
# Type: Package
# Title: Age-Depth Modelling using Bayesian Statistics
# Version: 3.1.1
# Author: Maarten Blaauw [aut, cre] (<https://orcid.org/0000-0002-5680-1515>), J. Andres Christen [aut, ctb, cph] (<https://orcid.org/0000-0002-5795-4345>), Marco A. Aquino Lopez [aut] (<https://orcid.org/0000-0002-5076-7205>), Judith Esquivel Vazquez [ctb], Oscar M. Gonzalez V. [ctb], Ted Belding [cph], James Theiler [cph], Brian Gough [cph], Charles Karney [cph]
# Authors@R: c(person(given = "Maarten", family = "Blaauw", role = c("aut", "cre"), email = "maarten.blaauw@qub.ac.uk", comment = c(ORCID = "0000-0002-5680-1515")), person(given = "J. Andres", family = "Christen", role = c("aut", "ctb", "cph"), email = "jac@cimat.mx", comment = c(ORCID = "0000-0002-5795-4345")), person(given = "Marco A.", family = "Aquino Lopez", role = "aut", email = "aquino@cimat.mx", comment = c(ORCID = "0000-0002-5076-7205")), person(given = "Judith", family = "Esquivel Vazquez", role = "ctb"), person(given = "Oscar M.", family = "Gonzalez V.", role = "ctb"), person(given = "Ted", family = "Belding", role = "cph"), person(given = "James", family = "Theiler", role = "cph"), person(given = "Brian", family = "Gough", role = "cph"), person(given = "Charles", family = "Karney", role = "cph"))
# Description: An approach to age-depth modelling that uses Bayesian statistics to reconstruct accumulation histories for deposits, through combining radiocarbon and other dates with prior information on accumulation rates and their variability. See Blaauw & Christen (2011).
# Encoding: UTF-8
# Repository: CRAN
# License: GPL (>= 2)
# NeedsCompilation: yes
# Imports: coda (>= 0.19-1), graphics, grDevices, Rcpp (>= 0.12.12), stats, utils, data.table
# Depends: rintcal (>= 0.6.1), R (>= 3.5.0)
# LinkingTo: Rcpp
# RoxygenNote: 7.2.3
# Suggests: knitr, rmarkdown, utf8
# VignetteBuilder: knitr
# Maintainer: Maarten Blaauw <maarten.blaauw@qub.ac.uk>
# Packaged: 2023-07-15 09:50:53 UTC; maarten
# Date/Publication: 2023-07-15 10:30:06 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
