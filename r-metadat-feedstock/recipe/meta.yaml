{% set version = '1.2-0' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-metadat
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/metadat_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/metadat/metadat_{{ version }}.tar.gz
  sha256: f0cce5e30c3d256eaf5a41e4f52ffc7108e195016a4b99409e0ab4c2ef58f5b8

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: metafor, numDeriv, BiasedUrn, dfoptim, igraph, ape, testthat, digest, lme4, clubSandwich, meta, netmeta, mvtnorm, gridExtra, rms
requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-mathjaxr

  run:
    - r-base
    - r-mathjaxr

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('metadat')"           # [not win]
    - "\"%R%\" -e \"library('metadat')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://github.com/wviechtb/metadat
  license: GPL-2
  summary: A collection of meta-analysis datasets for teaching purposes, illustrating/testing
    meta-analytic methods, and validating published analyses.
  license_family: GPL2
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-2'

# The original CRAN metadata for this package was:

# Package: metadat
# Version: 1.2-0
# Date: 2022-04-05
# Title: Meta-Analysis Datasets
# Authors@R: c( person(given = "Thomas", family="White", role="aut", email = "thomas.white@sydney.edu.au", comment = c(ORCID = "0000-0002-3976-1734")), person(given = "Daniel", family="Noble", role="aut", email = "daniel.noble@anu.edu.au", comment = c(ORCID = "0000-0001-9460-8743")), person(given = "Alistair", family="Senior", role="aut", email = "alistair.senior@sydney.edu.au", comment = c(ORCID = "0000-0001-9805-7280")), person(given = "W. Kyle", family="Hamilton", role="aut", email = "whamilton@ucmerced.edu", comment = c(ORCID = "0000-0002-8642-7990")), person(given = "Wolfgang", family = "Viechtbauer", role = c("aut","cre"), email = "wvb@metafor-project.org", comment = c(ORCID = "0000-0003-3463-4063")), person(given = "Guido", family = "Schwarzer", role = "dtc", email = "sc@imbi.uni-freiburg.de", comment = c(ORCID = "0000-0001-6214-9087")))
# Depends: R (>= 4.0.0)
# Imports: utils, tools, mathjaxr
# Suggests: metafor, numDeriv, BiasedUrn, dfoptim, igraph, ape, testthat, digest, lme4, clubSandwich, meta, netmeta, mvtnorm, gridExtra, rms
# Description: A collection of meta-analysis datasets for teaching purposes, illustrating/testing meta-analytic methods, and validating published analyses.
# License: GPL (>= 2)
# ByteCompile: TRUE
# LazyData: TRUE
# Encoding: UTF-8
# RdMacros: mathjaxr
# URL: https://github.com/wviechtb/metadat
# BugReports: https://github.com/wviechtb/metadat/issues
# NeedsCompilation: no
# Packaged: 2022-04-05 18:20:44 UTC; wviechtb
# Author: Thomas White [aut] (<https://orcid.org/0000-0002-3976-1734>), Daniel Noble [aut] (<https://orcid.org/0000-0001-9460-8743>), Alistair Senior [aut] (<https://orcid.org/0000-0001-9805-7280>), W. Kyle Hamilton [aut] (<https://orcid.org/0000-0002-8642-7990>), Wolfgang Viechtbauer [aut, cre] (<https://orcid.org/0000-0003-3463-4063>), Guido Schwarzer [dtc] (<https://orcid.org/0000-0001-6214-9087>)
# Maintainer: Wolfgang Viechtbauer <wvb@metafor-project.org>
# Repository: CRAN
# Date/Publication: 2022-04-06 11:52:37 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
