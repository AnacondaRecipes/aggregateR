{% set version = '2.8.0' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-nomclust
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/nomclust_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/nomclust/nomclust_{{ version }}.tar.gz
  sha256: fb4a6e550ec987937b2a6b6737e298ef742a02da4076d911a92d46e2d41789ae

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

requirements:
  build:
    - {{ compiler('c') }}              # [not win]
    - {{ compiler('m2w64_c') }}        # [win]
    - {{ compiler('cxx') }}            # [not win]
    - {{ compiler('m2w64_cxx') }}      # [win]
    - {{ posix }}filesystem        # [win]
    - {{ posix }}make
    - {{ posix }}sed               # [win]
    - {{ posix }}coreutils         # [win]
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-rcpp >=0.11.0
    - r-clvalid
    - r-cluster

  run:
    - r-base
    - {{native}}gcc-libs         # [win]
    - r-rcpp >=0.11.0
    - r-clvalid
    - r-cluster

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('nomclust')"           # [not win]
    - "\"%R%\" -e \"library('nomclust')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://CRAN.R-project.org/package=nomclust
  license: GPL-2
  summary: Similarity measures for hierarchical clustering of objects characterized by nominal
    (categorical) variables. Evaluation criteria for nominal data clustering.
  license_family: GPL2
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-2'

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: nomclust
# Title: Hierarchical Cluster Analysis of Nominal Data
# Authors@R: c(person("Zdenek", "Sulc", role = c("aut", "cre"), email = "zdenek.sulc@vse.cz"), person("Jana", "Cibulkova", role = "aut", email = "jana.cibulkova@vse.cz"), person("Hana", "Rezankova", role = "aut", email = "hana.rezankova@vse.cz"), person("Jaroslav", "Hornicek", role = "aut", email = "jaroslav.hornicek@vse.cz"))
# Author: Zdenek Sulc [aut, cre], Jana Cibulkova [aut], Hana Rezankova [aut], Jaroslav Hornicek [aut]
# Maintainer: Zdenek Sulc <zdenek.sulc@vse.cz>
# Version: 2.8.0
# Date: 2023-8-18
# Description: Similarity measures for hierarchical clustering of objects characterized by nominal (categorical) variables. Evaluation criteria for nominal data clustering.
# Depends: cluster, methods, clValid
# License: GPL (>= 2)
# RoxygenNote: 7.2.3
# NeedsCompilation: yes
# Packaged: 2023-08-18 09:34:22 UTC; zdenek
# Encoding: UTF-8
# Imports: Rcpp (>= 0.11.0)
# LinkingTo: Rcpp
# Repository: CRAN
# Date/Publication: 2023-08-18 10:12:38 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
