{% set version = '1.2-3' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-multiplencc
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/multipleNCC_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/multipleNCC/multipleNCC_{{ version }}.tar.gz
  sha256: d50c0a6946d613b5f1579920a40f3c0b6dc107bf1a5b3914c2dad72f1823efbb

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-mgcv
    - r-survival

  run:
    - r-base
    - r-mgcv
    - r-survival

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('multipleNCC')"           # [not win]
    - "\"%R%\" -e \"library('multipleNCC')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://CRAN.R-project.org/package=multipleNCC
  license: GPL-2
  summary: Fit Cox proportional hazard models with a weighted partial likelihood. It handles
    one or multiple endpoints, additional matching and makes it possible to reuse controls
    for other endpoints Stoer NC and Samuelsen SO (2016) <doi:10.32614/rj-2016-030>.
  license_family: GPL2
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-2'

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: multipleNCC
# Type: Package
# Title: Weighted Cox-Regression for Nested Case-Control Data
# Version: 1.2-3
# Date: 2023-07-08
# Author: Nathalie C. Stoer, Sven Ove Samuelsen
# Maintainer: Nathalie C. Stoer <nathalie.c.stoer@kreftregisteret.no>
# Description: Fit Cox proportional hazard models with a weighted partial likelihood. It handles one or multiple endpoints, additional matching and makes it possible to reuse controls for other endpoints Stoer NC and Samuelsen SO (2016) <doi:10.32614/rj-2016-030>.
# Depends: survival, mgcv
# License: GPL-2
# NeedsCompilation: no
# Packaged: 2023-07-08 14:30:47 UTC; nacst
# Repository: CRAN
# Date/Publication: 2023-07-09 09:10:09 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
