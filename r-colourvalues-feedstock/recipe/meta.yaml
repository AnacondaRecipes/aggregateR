{% set version = '0.3.7' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-colourvalues
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/colourvalues_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/colourvalues/colourvalues_{{ version }}.tar.gz
  sha256: f9365b9925f1743d3dccf2f7b1aad347517f13cef81dedabcc8622313aeeedf6

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: covr, microbenchmark, scales, testthat, viridisLite
requirements:
  build:
    - {{ compiler('c') }}              # [not win]
    - {{ compiler('m2w64_c') }}        # [win]
    - {{ compiler('cxx') }}            # [not win]
    - {{ compiler('m2w64_cxx') }}      # [win]
    - {{ posix }}filesystem        # [win]
    - {{ posix }}make
    - {{ posix }}sed               # [win]
    - {{ posix }}coreutils         # [win]
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-bh >=1.72.0_3
    - r-rcpp

  run:
    - r-base
    - {{native}}gcc-libs         # [win]
    - r-bh >=1.72.0_3
    - r-rcpp

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('colourvalues')"           # [not win]
    - "\"%R%\" -e \"library('colourvalues')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://symbolixau.github.io/colourvalues/
  license: GPL-3
  summary: "Maps one of the viridis colour palettes, or a user-specified palette to values. Viridis
    colour maps are created by St\xE9fan van der Walt and Nathaniel Smith, and were
    set as the default palette for the 'Python' 'Matplotlib' library <https://matplotlib.org/>.
    Other palettes available in this library have been derived from 'RColorBrewer' <https://CRAN.R-project.org/package=RColorBrewer>
    and 'colorspace' <https://CRAN.R-project.org/package=colorspace> packages."
  license_family: GPL3
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-3'

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: colourvalues
# Type: Package
# Title: Assigns Colours to Values
# Version: 0.3.7
# Date: 2020-12-02
# Authors@R: c( person("David", "Cooley", ,"dcooley@symbolix.com.au", role = c("aut", "cre")) )
# Description: Maps one of the viridis colour palettes, or a user-specified palette to values. Viridis colour maps are created by Stefan van der Walt and Nathaniel Smith, and were set as the default palette for the 'Python' 'Matplotlib' library <https://matplotlib.org/>. Other palettes available in this library have been derived from 'RColorBrewer' <https://CRAN.R-project.org/package=RColorBrewer> and 'colorspace' <https://CRAN.R-project.org/package=colorspace> packages.
# License: GPL-3
# URL: https://symbolixau.github.io/colourvalues/
# BugReports: https://github.com/SymbolixAU/colourvalues/issues
# Encoding: UTF-8
# LazyData: true
# Depends: R (>= 3.3.0)
# SystemRequirements: C++11
# LinkingTo: BH (>= 1.72.0-3), Rcpp
# Imports: graphics, Rcpp
# RoxygenNote: 7.1.1
# Suggests: covr, microbenchmark, scales, testthat, viridisLite
# NeedsCompilation: yes
# Packaged: 2020-12-03 04:30:52 UTC; dave
# Author: David Cooley [aut, cre]
# Maintainer: David Cooley <dcooley@symbolix.com.au>
# Repository: CRAN
# Date/Publication: 2020-12-07 23:50:02 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
