{% set version = '2.1.0' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-mwshiny
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/mwshiny_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/mwshiny/mwshiny_{{ version }}.tar.gz
  sha256: f4d13c540b7891c7db50672e63f39027e2de6a18b35af5a1735f2b8b53032a30

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: knitr, rmarkdown, ggplot2 (>= 3.1.0), visNetwork (>= 2.0.5), htmlwidgets (>= 1.3), datasets
requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-htmltools >=0.3.6
    - r-shiny >=1.2.0

  run:
    - r-base
    - r-htmltools >=0.3.6
    - r-shiny >=1.2.0

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('mwshiny')"           # [not win]
    - "\"%R%\" -e \"library('mwshiny')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://CRAN.R-project.org/package=mwshiny
  license: MIT
  summary: A simple function, mwsApp(), that runs a 'shiny' app spanning multiple, connected
    windows. This uses all standard 'shiny' conventions, and depends only on the 'shiny'
    package.
  license_family: MIT
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/MIT'
    - LICENSE

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: mwshiny
# Type: Package
# Title: 'Shiny' for Multiple Windows
# Version: 2.1.0
# Date: 2020-06-05
# Authors@R: c( person("Hannah", "De los Santos", email = "hdelossantos653@gmail.com", role = c("aut","cre")), person("John", "Erickson", role = c("aut")), person("Joe","Cheng",role = c("ctb")), person("Nicholas", "Thomson", role = c("ctb")), person("Kristin", "Bennett", role = c("aut")) )
# Maintainer: Hannah De los Santos <hdelossantos653@gmail.com>
# Description: A simple function, mwsApp(), that runs a 'shiny' app spanning multiple, connected windows. This uses all standard 'shiny' conventions, and depends only on the 'shiny' package.
# License: MIT + file LICENSE
# Encoding: UTF-8
# LazyData: true
# Depends: shiny (>= 1.2.0)
# Imports: htmltools (>= 0.3.6)
# Suggests: knitr, rmarkdown, ggplot2 (>= 3.1.0), visNetwork (>= 2.0.5), htmlwidgets (>= 1.3), datasets
# VignetteBuilder: knitr
# RoxygenNote: 6.1.1
# NeedsCompilation: no
# Packaged: 2020-06-05 18:43:46 UTC; delosh
# Author: Hannah De los Santos [aut, cre], John Erickson [aut], Joe Cheng [ctb], Nicholas Thomson [ctb], Kristin Bennett [aut]
# Repository: CRAN
# Date/Publication: 2020-06-05 22:00:02 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
