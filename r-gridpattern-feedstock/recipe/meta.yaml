{% set version = '1.1.1' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-gridpattern
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/gridpattern_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/gridpattern/gridpattern_{{ version }}.tar.gz
  sha256: 5128054a3e9fb5a493cda0e984563fc472af8631a0da93b31a9be46cd773fdec

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: ambient, knitr, magick (>= 2.7.4), ragg (>= 1.2.0), rmarkdown, svglite (>= 2.1.0), testthat, vdiffr (>= 1.0.6)
requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-glue
    - r-memoise
    - r-png
    - r-rlang
    - r-sf

  run:
    - r-base
    - r-glue
    - r-memoise
    - r-png
    - r-rlang
    - r-sf

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('gridpattern')"           # [not win]
    - "\"%R%\" -e \"library('gridpattern')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://trevorldavis.com/R/gridpattern/, https://github.com/trevorld/gridpattern
  license: MIT
  summary: Provides 'grid' grobs that fill in a user-defined area with various patterns.  Includes
    enhanced versions of the geometric and image-based patterns originally contained
    in the 'ggpattern' package as well as original 'pch', 'polygon_tiling', 'regular_polygon',
    'rose', 'text', 'wave', and 'weave' patterns plus support for custom user-defined
    patterns.
  license_family: MIT
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/MIT'
    - LICENSE

# The original CRAN metadata for this package was:

# Package: gridpattern
# Type: Package
# Title: 'grid' Pattern Grobs
# Version: 1.1.1
# Authors@R: c(person("Mike", "FC", role = "aut", comment = "Code/docs adapted from ggpattern"), person("Trevor L", "Davis", role=c("aut", "cre"), email="trevor.l.davis@gmail.com", comment = c(ORCID = "0000-0001-6341-4639")), person("Thomas Lin", "Pedersen", role = "ctb", comment = "new_data_frame() copied from ggplot2"))
# Description: Provides 'grid' grobs that fill in a user-defined area with various patterns.  Includes enhanced versions of the geometric and image-based patterns originally contained in the 'ggpattern' package as well as original 'pch', 'polygon_tiling', 'regular_polygon', 'rose', 'text', 'wave', and 'weave' patterns plus support for custom user-defined patterns.
# URL: https://trevorldavis.com/R/gridpattern/, https://github.com/trevorld/gridpattern
# BugReports: https://github.com/trevorld/gridpattern/issues
# License: MIT + file LICENSE
# Encoding: UTF-8
# RoxygenNote: 7.2.3
# Depends: R (>= 3.4.0)
# Imports: glue, grDevices, grid, memoise, png, rlang, sf, utils
# Suggests: ambient, knitr, magick (>= 2.7.4), ragg (>= 1.2.0), rmarkdown, svglite (>= 2.1.0), testthat, vdiffr (>= 1.0.6)
# VignetteBuilder: knitr, rmarkdown
# NeedsCompilation: no
# Packaged: 2023-10-25 17:26:22 UTC; trevorld
# Author: Mike FC [aut] (Code/docs adapted from ggpattern), Trevor L Davis [aut, cre] (<https://orcid.org/0000-0001-6341-4639>), Thomas Lin Pedersen [ctb] (new_data_frame() copied from ggplot2)
# Maintainer: Trevor L Davis <trevor.l.davis@gmail.com>
# Repository: CRAN
# Date/Publication: 2023-10-25 19:00:02 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
