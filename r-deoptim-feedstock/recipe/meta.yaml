{% set version = '2.2-8' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-deoptim
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/DEoptim_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/DEoptim/DEoptim_{{ version }}.tar.gz
  sha256: 631eabdcf26ec25a759651f699db1971beca3ae193c7fbd1c63a78248fdbf54c

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: foreach, iterators, colorspace, lattice, parallelly
requirements:
  build:
    - {{ compiler('c') }}              # [not win]
    - {{ compiler('m2w64_c') }}        # [win]
    - {{ posix }}filesystem        # [win]
    - {{ posix }}make
    - {{ posix }}sed               # [win]
    - {{ posix }}coreutils         # [win]
    - {{ posix }}zip               # [win]

  host:
    - r-base

  run:
    - r-base
    - {{native}}gcc-libs         # [win]

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('DEoptim')"           # [not win]
    - "\"%R%\" -e \"library('DEoptim')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://github.com/ArdiaD/DEoptim
  license: GPL-2
  summary: Implements the Differential Evolution algorithm for global optimization of a real-valued
    function of a real-valued parameter vector as described in Mullen et al. (2011)
    <doi:10.18637/jss.v040.i06>.
  license_family: GPL2
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-2'

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: DEoptim
# Version: 2.2-8
# Title: Global Optimization by Differential Evolution
# Authors@R: c(person("David", "Ardia", role = c("aut"), email = "david.ardia.ch@gmail.com", comment = c(ORCID = "0000-0003-2823-782X")), person("Katharine", "Mullen", role = c("aut", "cre"), email="mullenkate@gmail.com"), person("Brian", "Peterson", role = "aut"), person("Joshua", "Ulrich", role = "aut"), person("Kris", "Boudt", role = "ctb"))
# Description: Implements the Differential Evolution algorithm for global optimization of a real-valued function of a real-valued parameter vector as described in Mullen et al. (2011) <doi:10.18637/jss.v040.i06>.
# Suggests: foreach, iterators, colorspace, lattice, parallelly
# Depends: parallel
# Imports: methods
# BugReports: https://github.com/ArdiaD/DEoptim/issues
# URL: https://github.com/ArdiaD/DEoptim
# License: GPL (>= 2)
# Repository: CRAN
# Maintainer: Katharine Mullen <mullenkate@gmail.com>
# NeedsCompilation: yes
# Packaged: 2022-11-09 22:45:39 UTC; kmm-windog
# Author: David Ardia [aut] (<https://orcid.org/0000-0003-2823-782X>), Katharine Mullen [aut, cre], Brian Peterson [aut], Joshua Ulrich [aut], Kris Boudt [ctb]
# Date/Publication: 2022-11-11 18:10:09 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
