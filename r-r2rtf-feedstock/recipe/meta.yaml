{% set version = '1.1.1' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-r2rtf
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/r2rtf_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/r2rtf/r2rtf_{{ version }}.tar.gz
  sha256: d64b88187b5eb7bc69eae37f493fdae8417d5127d0a44f1586a00827bc321f52

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: covr, dplyr, emmeans, ggplot2, knitr, magrittr, officer, rmarkdown, stringi, testthat, tidyr, xml2
requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base

  run:
    - r-base

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('r2rtf')"           # [not win]
    - "\"%R%\" -e \"library('r2rtf')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://merck.github.io/r2rtf/, https://github.com/Merck/r2rtf
  license: GPL-3
  summary: Create production-ready Rich Text Format (RTF) table and figure with flexible format.
  license_family: GPL3
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-3'

# The original CRAN metadata for this package was:

# Package: r2rtf
# Title: Easily Create Production-Ready Rich Text Format (RTF) Table and Figure
# Version: 1.1.1
# Authors@R: c( person("Yilong", "Zhang", role = "aut"), person("Siruo", "Wang", role = "aut"), person("Simiao", "Ye", role = "aut"), person("Fansen", "Kong", role = "aut"), person("Brian", "Lang", role = "aut"), person("Benjamin", "Wang", , email = "benjamin.wang@merck.com", role = c("aut", "cre")), person("Nan", "Xiao", role = "ctb"), person("Madhusudhan", "Ginnaram", role = "ctb"), person("Ruchitbhai", "Patel", role = "ctb"), person("Huei-Ling", "Chen", role = "ctb"), person("Peikun", "Wu", role = "ctb"), person("Uday Preetham", "Palukuru", role = "ctb"), person("Daniel", "Woodie", role = "ctb"), person("Sarad", "Nepal", role = "ctb"), person("Jane", "Liao", role = "ctb"), person("Jeff", "Cheng", role = "ctb"), person("Yirong", "Cao", role = "ctb"), person("Amin", "Shirazi", role = "ctb"), person("Merck Sharp & Dohme Corp", role = "cph") )
# Description: Create production-ready Rich Text Format (RTF) table and figure with flexible format.
# License: GPL-3
# URL: https://merck.github.io/r2rtf/, https://github.com/Merck/r2rtf
# BugReports: https://github.com/Merck/r2rtf/issues
# Encoding: UTF-8
# VignetteBuilder: knitr
# LazyData: true
# Depends: R (>= 3.5.0)
# Imports: grDevices, tools
# Suggests: covr, dplyr, emmeans, ggplot2, knitr, magrittr, officer, rmarkdown, stringi, testthat, tidyr, xml2
# Config/testthat/edition: 3
# RoxygenNote: 7.2.3
# NeedsCompilation: no
# Packaged: 2023-10-24 22:44:25 UTC; wangben
# Author: Yilong Zhang [aut], Siruo Wang [aut], Simiao Ye [aut], Fansen Kong [aut], Brian Lang [aut], Benjamin Wang [aut, cre], Nan Xiao [ctb], Madhusudhan Ginnaram [ctb], Ruchitbhai Patel [ctb], Huei-Ling Chen [ctb], Peikun Wu [ctb], Uday Preetham Palukuru [ctb], Daniel Woodie [ctb], Sarad Nepal [ctb], Jane Liao [ctb], Jeff Cheng [ctb], Yirong Cao [ctb], Amin Shirazi [ctb], Merck Sharp & Dohme Corp [cph]
# Maintainer: Benjamin Wang <benjamin.wang@merck.com>
# Repository: CRAN
# Date/Publication: 2023-10-25 08:00:02 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
