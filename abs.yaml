aggregate_check: false

upload_channels: [r]

use_prefect_platforms:
    - linux-64

build_parameters:
  - "--suppress-variables"
  - "--skip-existing"
  - "--error-overlinking"
  - "--variant-config-files ../conda_build_config.yaml"


channels:
  - r
