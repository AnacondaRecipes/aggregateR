{% set version = '1.2.7' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-tpmsm
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/TPmsm_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/TPmsm/TPmsm_{{ version }}.tar.gz
  sha256: adfce0218eebb1987258cd1a5fa5d1f5bd77be6b2e687b80694f1dcb6d650176

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: knitr
requirements:
  build:
    - {{ compiler('c') }}              # [not win]
    - {{ compiler('m2w64_c') }}        # [win]
    - {{ posix }}filesystem        # [win]
    - {{ posix }}make
    - {{ posix }}sed               # [win]
    - {{ posix }}coreutils         # [win]
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-kernsmooth

  run:
    - r-base
    - {{native}}gcc-libs         # [win]
    - r-kernsmooth

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('TPmsm')"           # [not win]
    - "\"%R%\" -e \"library('TPmsm')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://github.com/arturstat/TPmsm
  license: GPL-3
  summary: Estimation of transition probabilities for the illness-death model and or the three-state
    progressive model.
  license_family: GPL3
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-3'

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Encoding: UTF-8
# Package: TPmsm
# Type: Package
# Version: 1.2.7
# Date: 2023-01-14
# Title: Estimation of Transition Probabilities in Multistate Models
# Authors@R: c( person( given = "Artur", family = "Araujo", role = c("aut", "cre"), email = "artur.stat@gmail.com", comment = c(ORCID = "0000-0003-1419-4208") ), person( given = "Javier", family = "Roca-Pardinas", role = "aut", email = "roca@uvigo.es", comment = c(ORCID = "0000-0003-3107-4515") ), person( given = "Luis", family = "Meira-Machado", role = "aut", email = "lmachado@math.uminho.pt", comment = c(ORCID = "0000-0002-8577-7665") ) )
# Depends: R (>= 3.1.0), graphics, grDevices, KernSmooth
# Enhances: etm
# Suggests: knitr
# VignetteBuilder: knitr
# Description: Estimation of transition probabilities for the illness-death model and or the three-state progressive model.
# License: GPL-3
# URL: https://github.com/arturstat/TPmsm
# BugReports: https://github.com/arturstat/TPmsm/issues
# LazyLoad: yes
# LazyData: yes
# NeedsCompilation: yes
# Packaged: 2023-01-13 20:06:48 UTC; User
# Author: Artur Araujo [aut, cre] (<https://orcid.org/0000-0003-1419-4208>), Javier Roca-Pardinas [aut] (<https://orcid.org/0000-0003-3107-4515>), Luis Meira-Machado [aut] (<https://orcid.org/0000-0002-8577-7665>)
# Maintainer: Artur Araujo <artur.stat@gmail.com>
# Repository: CRAN
# Date/Publication: 2023-01-13 20:30:02 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
