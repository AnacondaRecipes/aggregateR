{% set version = '0.3.1' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-shiny.router
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/shiny.router_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/shiny.router/shiny.router_{{ version }}.tar.gz
  sha256: 7d444d38fad2d9ddd3e9d44428ca8e873f644e832fcbb3638840185e7b63e8fd

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: covr, lintr, rcmdcheck, spelling, testthat
requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-glue
    - r-htmltools
    - r-rlang
    - r-shiny

  run:
    - r-base
    - r-glue
    - r-htmltools
    - r-rlang
    - r-shiny

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('shiny.router')"           # [not win]
    - "\"%R%\" -e \"library('shiny.router')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://appsilon.github.io/shiny.router/, https://github.com/Appsilon/shiny.router
  license: MIT
  summary: It is a simple router for your Shiny apps. The router allows you to create dynamic
    web applications with real-time User Interface and easily share url to pages within
    your Shiny apps.
  license_family: MIT
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/MIT'
    - LICENSE

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: shiny.router
# Type: Package
# Title: Basic Routing for Shiny Web Applications
# Version: 0.3.1
# Authors@R: c( person("Ryszard", "Szymanski", email = "opensource+ryszard@appsilon.com", role = c("cre", "aut")), person("Jakub", "Nowicki", email = "kuba@appsilon.com", role = c("aut")), person("Filip", "Stachura", email = "filip@appsilon.com", role = c("aut")), person("Dominik", "Krzeminski", email = "raymon92@gmail.com", role = c("aut")), person("Krystian", "Igras", email = "krystian8207@gmail.com", role = c("aut")), person("Servet Ahmet", "Cizmeli", email = "ahmet.cizmeli@contractors.appsilon.com", role = c("ctb")), person("Appsilon Sp. z o.o.", role = "cph", email = "opensource@appsilon.com") )
# Description: It is a simple router for your Shiny apps. The router allows you to create dynamic web applications with real-time User Interface and easily share url to pages within your Shiny apps.
# URL: https://appsilon.github.io/shiny.router/, https://github.com/Appsilon/shiny.router
# BugReports: https://github.com/Appsilon/shiny.router/issues
# Encoding: UTF-8
# License: MIT + file LICENSE
# Imports: htmltools, glue, rlang, shiny
# RoxygenNote: 7.2.3
# Suggests: covr, lintr, rcmdcheck, spelling, testthat
# NeedsCompilation: no
# Packaged: 2023-04-18 07:43:57 UTC; kuba
# Author: Ryszard Szymanski [cre, aut], Jakub Nowicki [aut], Filip Stachura [aut], Dominik Krzeminski [aut], Krystian Igras [aut], Servet Ahmet Cizmeli [ctb], Appsilon Sp. z o.o. [cph]
# Maintainer: Ryszard Szymanski <opensource+ryszard@appsilon.com>
# Repository: CRAN
# Date/Publication: 2023-04-18 08:00:02 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
