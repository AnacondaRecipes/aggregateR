{% set version = '1.3' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-ggmridge
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/GGMridge_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/GGMridge/GGMridge_{{ version }}.tar.gz
  sha256: 9e2cf6d54528dbce0fe52e4e9b6858c741563d6efccdc83753025b620f804a29

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-mass
    - r-mvtnorm

  run:
    - r-base
    - r-mass
    - r-mvtnorm

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('GGMridge')"           # [not win]
    - "\"%R%\" -e \"library('GGMridge')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://CRAN.R-project.org/package=GGMridge
  license: GPL-2
  summary: Estimation of partial correlation matrix using ridge penalty followed by thresholding
    and reestimation. Under multivariate Gaussian assumption, the matrix constitutes
    an Gaussian graphical model (GGM).
  license_family: GPL2
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-2'

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: GGMridge
# Type: Package
# Title: Gaussian Graphical Models Using Ridge Penalty Followed by Thresholding and Reestimation
# Version: 1.3
# Date: 2023-10-01
# Author: Min Jin Ha [aut, cre], Shannon T. Holloway [ctb]
# Maintainer: Shannon T. Holloway <shannon.t.holloway@gmail.com>
# Depends: mvtnorm, MASS, stats, graphics
# Description: Estimation of partial correlation matrix using ridge penalty followed by thresholding and reestimation. Under multivariate Gaussian assumption, the matrix constitutes an Gaussian graphical model (GGM).
# License: GPL-2
# LazyLoad: yes
# NeedsCompilation: no
# Encoding: UTF-8
# RoxygenNote: 7.2.1
# Collate: 'EM.mixture.R' 'scaledMat.R' 'svdFunc.R' 'splitSets.R' 'ne.lambda.cv.R' 'R.separate.ridge.R' 'StructuredEstimate.R' 'ksStat.R' 'getEfronp.R' 'lambda.TargetD.R' 'lambda.cv.R' 'transFisher.R' 'lambda.pcut.cv1.R' 'lambda.pcut.cv.R' 'simulateData.R'
# Packaged: 2023-10-01 20:07:57 UTC; 19194
# Repository: CRAN
# Date/Publication: 2023-10-01 21:00:02 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
