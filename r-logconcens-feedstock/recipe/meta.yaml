{% set version = '0.17-0' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-logconcens
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/logconcens_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/logconcens/logconcens_{{ version }}.tar.gz
  sha256: 796fcaf9f9e384a1a492dc9e3b030538ec1b6d10b78f35506523fb58c4f2367f

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: logcondens
requirements:
  build:
    - {{ compiler('c') }}              # [not win]
    - {{ compiler('m2w64_c') }}        # [win]
    - {{ posix }}filesystem        # [win]
    - {{ posix }}make
    - {{ posix }}sed               # [win]
    - {{ posix }}coreutils         # [win]
    - {{ posix }}zip               # [win]

  host:
    - r-base

  run:
    - r-base
    - {{native}}gcc-libs         # [win]

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('logconcens')"           # [not win]
    - "\"%R%\" -e \"library('logconcens')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: http://www.dominic.schuhmacher.name, http://www.kasparrufibach.ch, https://www.imsv.unibe.ch/ueber_uns/personen/prof_dr_duembgen_lutz
  license: GPL-2
  summary: Based on right or interval censored data, compute the maximum likelihood estimator
    of a (sub)probability density under the assumption that it is log-concave. For further
    information see Duembgen, Rufibach and Schuhmacher (2014) <doi:10.1214/14-EJS930>.
  license_family: GPL2
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-2'

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: logconcens
# Type: Package
# Title: Maximum Likelihood Estimation of a log-Concave Density Based on Censored Data
# Version: 0.17-0
# Date: 2019-11-26
# Author: Dominic Schuhmacher <dominic.schuhmacher@mathematik.uni-goettingen.de>, Kaspar Rufibach <kaspar.rufibach@gmail.com>, Lutz Duembgen <duembgen@stat.unibe.ch>
# Maintainer: Dominic Schuhmacher <dominic.schuhmacher@mathematik.uni-goettingen.de>
# Depends: R (>= 2.15.0)
# Imports: grDevices, graphics, stats, utils
# Suggests: logcondens
# Description: Based on right or interval censored data, compute the maximum likelihood estimator of a (sub)probability density under the assumption that it is log-concave. For further information see Duembgen, Rufibach and Schuhmacher (2014) <doi:10.1214/14-EJS930>.
# License: GPL (>= 2)
# URL: http://www.dominic.schuhmacher.name, http://www.kasparrufibach.ch, https://www.imsv.unibe.ch/ueber_uns/personen/prof_dr_duembgen_lutz
# LazyLoad: yes
# NeedsCompilation: yes
# Packaged: 2019-11-26 23:19:34 UTC; schumi
# Repository: CRAN
# Date/Publication: 2019-11-27 07:00:03 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
