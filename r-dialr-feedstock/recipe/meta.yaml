{% set version = '0.4.1' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-dialr
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/dialr_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/dialr/dialr_{{ version }}.tar.gz
  sha256: 6a89d8a42c76dd8a87daf9ad21e8905fbd38d7fffa807be7af5a27c6d3f07907

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: covr, dplyr, knitr, pillar, rmarkdown, testthat (>= 2.1.0)
requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-dialrjars >=8.10.11.1
    - r-rjava
    - openjdk  # [linux]

  run:
    - r-base
    - r-dialrjars >=8.10.11.1
    - r-rjava

test:
  requires:
    - openjdk  # [linux]
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('dialr')"           # [not win]
    - "\"%R%\" -e \"library('dialr')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://socialresearchcentre.github.io/dialr/, https://github.com/socialresearchcentre/dialr,
    https://github.com/socialresearchcentre/dialrjars, https://github.com/google/libphonenumber
  license: GPL-3
  summary: Parse, format, and validate international phone numbers using Google's 'libphonenumber'
    java library, <https://github.com/google/libphonenumber>.
  license_family: GPL3
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-3'

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: dialr
# Title: Parse, Format, and Validate International Phone Numbers
# Version: 0.4.1
# Authors@R: c(person(given = "Danny", family = "Smith", role = c("aut", "cre", "cph"), email = "danny@gorcha.org"), person(given = "The Social Research Centre", role = "cph"))
# Description: Parse, format, and validate international phone numbers using Google's 'libphonenumber' java library, <https://github.com/google/libphonenumber>.
# License: GPL (>= 3)
# URL: https://socialresearchcentre.github.io/dialr/, https://github.com/socialresearchcentre/dialr, https://github.com/socialresearchcentre/dialrjars, https://github.com/google/libphonenumber
# BugReports: https://github.com/socialresearchcentre/dialr/issues
# Depends: R (>= 3.6.0)
# Imports: dialrjars (>= 8.10.11.1), rJava
# Suggests: covr, dplyr, knitr, pillar, rmarkdown, testthat (>= 2.1.0)
# VignetteBuilder: knitr
# Encoding: UTF-8
# NeedsCompilation: no
# RoxygenNote: 7.2.1
# SystemRequirements: java (>= 1.6)
# Packaged: 2022-08-25 02:05:34 UTC; danny
# Author: Danny Smith [aut, cre, cph], The Social Research Centre [cph]
# Maintainer: Danny Smith <danny@gorcha.org>
# Repository: CRAN
# Date/Publication: 2022-08-25 02:32:33 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
