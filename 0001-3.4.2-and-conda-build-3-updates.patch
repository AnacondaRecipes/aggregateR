Submodule r-acepack-feedstock contains modified content
diff --git a/r-acepack-feedstock/recipe/meta.yaml b/r-acepack-feedstock/recipe/meta.yaml
index 68c81a8..abd8384 100644
--- a/r-acepack-feedstock/recipe/meta.yaml
+++ b/r-acepack-feedstock/recipe/meta.yaml
@@ -12,38 +12,49 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/acepack_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/acepack/acepack_{{ version }}.tar.gz
-
   sha256: 82750507926f02a696f6cc03693e8d4a5ee7e92500c8c15a16a9c12addcd28b9
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
-  skip: true  # [win32]
+  # If this is a new build for the same version, increment the build number.
   number: 0
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: testthat
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{ compiler('fortran') }}  # [not win]
+    - {{native}}toolchain        # [win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('acepack')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('acepack')"           # [not win]
     - "\"%R%\" -e \"library('acepack')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=acepack
-  license: MIT
-  license_file: LICENSE
-  summary: |
-    Two nonparametric methods for multiple regression transform selection are provided.
+  license: MIT + file LICENSE
+  summary: Two nonparametric methods for multiple regression transform selection are provided.
     The first, Alternative Conditional Expectations (ACE),  is an algorithm to find
     the fixed point of maximal correlation, i.e. it finds a set of transformed response
     variables that maximizes R^2 using smoothing functions [see Breiman, L., and J.H.
@@ -61,3 +72,23 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: acepack
+# Maintainer: Shawn Garbett <shawn.garbett@vanderbilt.edu>
+# Version: 1.4.1
+# Author: Phil Spector, Jerome Friedman, Robert Tibshirani, Thomas Lumley, Shawn Garbett, Jonathan Baron
+# Description: Two nonparametric methods for multiple regression transform selection are provided. The first, Alternative Conditional Expectations (ACE),  is an algorithm to find the fixed point of maximal correlation, i.e. it finds a set of transformed response variables that maximizes R^2 using smoothing functions [see Breiman, L., and J.H. Friedman. 1985. "Estimating Optimal Transformations for Multiple Regression and Correlation". Journal of the American Statistical Association. 80:580-598. <doi:10.1080/01621459.1985.10478157>]. Also included is the Additivity Variance Stabilization (AVAS) method which works better than ACE when correlation is low [see Tibshirani, R.. 1986. "Estimating Transformations for Regression via Additivity and Variance Stabilization". Journal of the American Statistical Association. 83:394-405.  <doi:10.1080/01621459.1988.10478610>]. A good introduction to these two methods is in chapter 16 of Frank Harrel's "Regression Modeling Strategies" in the Springer Series in Statistics.
+# Title: ACE and AVAS for Selecting Multiple Regression Transformations
+# License: MIT + file LICENSE
+# Suggests: testthat
+# Packaged: 2016-10-28 15:38:59 UTC; garbetsp
+# Repository: CRAN
+# Date/Publication: 2016-10-29 00:11:52
+# NeedsCompilation: yes
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-adgoftest-feedstock contains modified content
diff --git a/r-adgoftest-feedstock/recipe/meta.yaml b/r-adgoftest-feedstock/recipe/meta.yaml
index 53b2625..23df0e2 100644
--- a/r-adgoftest-feedstock/recipe/meta.yaml
+++ b/r-adgoftest-feedstock/recipe/meta.yaml
@@ -12,17 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/ADGofTest_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/ADGofTest/ADGofTest_{{ version }}.tar.gz
-
   sha256: 9cd9313954f6ecd82480d373f6c5371ca84ab33e3f5c39d972d35cfcf1096846
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
-  skip: true  # [win32]
+  # If this is a new build for the same version, increment the build number.
   number: 0
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
@@ -32,14 +38,20 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('ADGofTest')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('ADGofTest')"           # [not win]
     - "\"%R%\" -e \"library('ADGofTest')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=ADGofTest
   license: GPL
-  summary: |
-    Anderson-Darling GoF test with p-value calculation based on Marsaglia's 2004 paper
+  summary: Anderson-Darling GoF test with p-value calculation based on Marsaglia's 2004 paper
     "Evaluating the Anderson-Darling Distribution"
   license_family: GPL
 
@@ -47,3 +59,25 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: ADGofTest
+# Type: Package
+# Title: Anderson-Darling GoF test
+# Version: 0.3
+# Date: 2011-12-28
+# Author: Carlos J. Gil Bellosta
+# Maintainer: Carlos J. Gil Bellosta <cgb@datanalytics.com>
+# Description: Anderson-Darling GoF test with p-value calculation based on Marsaglia's 2004 paper "Evaluating the Anderson-Darling Distribution"
+# License: GPL
+# LazyLoad: yes
+# Packaged: 2011-12-28 01:41:10 UTC; carlos
+# Repository: CRAN
+# Date/Publication: 2011-12-28 13:50:19
+# NeedsCompilation: no
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-aer-feedstock contains modified content
diff --git a/r-aer-feedstock/recipe/meta.yaml b/r-aer-feedstock/recipe/meta.yaml
index 88ead42..81261ed 100644
--- a/r-aer-feedstock/recipe/meta.yaml
+++ b/r-aer-feedstock/recipe/meta.yaml
@@ -14,13 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/AER/AER_{{ version }}.tar.gz
   sha256: ef0cf14ff9d3de2b97e5855243426cc918808eb1011f0e2253b3b00043927a62
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: boot, dynlm, effects, fGarch, forecast, foreign, ineq, KernSmooth, lattice, longmemo, MASS, mlogit, nlme, nnet, np, plm, pscl, quantreg, rgl, ROCR, rugarch, sampleSelection, scatterplot3d, strucchange, systemfit, truncreg, tseries, urca, vars
 requirements:
   build:
     - r-base
@@ -30,6 +38,7 @@ requirements:
     - r-sandwich
     - r-survival >=2.37_5
     - r-zoo
+
   run:
     - r-base
     - r-formula >=0.2_0
@@ -41,14 +50,20 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('AER')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('AER')"           # [not win]
     - "\"%R%\" -e \"library('AER')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=AER
-  license: GPL (>= 2)
-  summary: |
-    Functions, data sets, examples, demos, and vignettes for the book Christian Kleiber
+  license: GPL-2 | GPL-3
+  summary: Functions, data sets, examples, demos, and vignettes for the book Christian Kleiber
     and Achim Zeileis (2008), Applied Econometrics with R, Springer-Verlag, New York.
     ISBN 978-0-387-77316-2. (See the vignette "AER" for a package overview.)
   license_family: GPL3
@@ -57,3 +72,28 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: AER
+# Version: 1.2-5
+# Date: 2017-01-06
+# Title: Applied Econometrics with R
+# Authors@R: c(person(given = "Christian", family = "Kleiber", role = "aut", email = "Christian.Kleiber@unibas.ch"), person(given = "Achim", family = "Zeileis", role = c("aut", "cre"), email = "Achim.Zeileis@R-project.org"))
+# Description: Functions, data sets, examples, demos, and vignettes for the book Christian Kleiber and Achim Zeileis (2008), Applied Econometrics with R, Springer-Verlag, New York. ISBN 978-0-387-77316-2. (See the vignette "AER" for a package overview.)
+# LazyLoad: yes
+# Depends: R (>= 2.13.0), car (>= 2.0-19), lmtest, sandwich, survival (>= 2.37-5), zoo
+# Suggests: boot, dynlm, effects, fGarch, forecast, foreign, ineq, KernSmooth, lattice, longmemo, MASS, mlogit, nlme, nnet, np, plm, pscl, quantreg, rgl, ROCR, rugarch, sampleSelection, scatterplot3d, strucchange, systemfit, truncreg, tseries, urca, vars
+# Imports: stats, Formula (>= 0.2-0)
+# License: GPL-2 | GPL-3
+# NeedsCompilation: no
+# Packaged: 2017-01-05 23:26:19 UTC; zeileis
+# Author: Christian Kleiber [aut], Achim Zeileis [aut, cre]
+# Maintainer: Achim Zeileis <Achim.Zeileis@R-project.org>
+# Repository: CRAN
+# Date/Publication: 2017-01-07 00:11:57
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-afex-feedstock contains modified content
diff --git a/r-afex-feedstock/recipe/meta.yaml b/r-afex-feedstock/recipe/meta.yaml
index 489f9ef..6cfb95b 100644
--- a/r-afex-feedstock/recipe/meta.yaml
+++ b/r-afex-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '0.17-8' %}
+{% set version = '0.18-0' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,15 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/afex_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/afex/afex_{{ version }}.tar.gz
-  sha256: 83d84943ec78ecdaae83539dd094e5f129f64d61e722d8d66d425302ae35eafa
+  sha256: 65bf137d1cffa97ac0bd79923612d29738d8dea3d1efa572875c0b570eab4453
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: xtable, parallel, plyr, optimx, nloptr, knitr, rmarkdown, lattice, latticeExtra, multcomp, testthat, mlmRev, dplyr, tidyr, dfoptim, Matrix, psych
 requirements:
   build:
     - r-base
@@ -32,6 +40,7 @@ requirements:
     - r-pbkrtest >=0.4_1
     - r-reshape2
     - r-stringr
+
   run:
     - r-base
     - r-car
@@ -45,14 +54,20 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('afex')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('afex')"           # [not win]
     - "\"%R%\" -e \"library('afex')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://afex.singmann.science/, https://github.com/singmann/afex
   license: GPL (>= 2)
-  summary: |
-    Convenience functions for analyzing factorial experiments using ANOVA or mixed models.
+  summary: Convenience functions for analyzing factorial experiments using ANOVA or mixed models.
     aov_ez(), aov_car(), and aov_4() allow specification of between, within (i.e., repeated-measures),
     or mixed between-within (i.e., split-plot) ANOVAs for data in long format (i.e.,
     one observation per row), aggregating multiple observations per individual and cell
@@ -67,3 +82,33 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: afex
+# Type: Package
+# Title: Analysis of Factorial Experiments
+# Depends: R (>= 3.1.0), lme4 (>= 1.1-8), lsmeans (>= 2.17)
+# Suggests: xtable, parallel, plyr, optimx, nloptr, knitr, rmarkdown, lattice, latticeExtra, multcomp, testthat, mlmRev, dplyr, tidyr, dfoptim, Matrix, psych
+# Imports: stringr, coin, pbkrtest (>= 0.4-1), lmerTest, car, reshape2, stats, methods
+# Description: Convenience functions for analyzing factorial experiments using ANOVA or mixed models. aov_ez(), aov_car(), and aov_4() allow specification of between, within (i.e., repeated-measures), or mixed between-within (i.e., split-plot) ANOVAs for data in long format (i.e., one observation per row), aggregating multiple observations per individual and cell of the design. mixed() fits mixed models using lme4::lmer() and computes p-values for all fixed effects using either Kenward-Roger or Satterthwaite approximation for degrees of freedom (LMM only), parametric bootstrap (LMMs and GLMMs), or likelihood ratio tests (LMMs and GLMMs). afex uses type 3 sums of squares as default (imitating commercial statistical software).
+# URL: http://afex.singmann.science/, https://github.com/singmann/afex
+# BugReports: https://github.com/singmann/afex/issues
+# License: GPL (>= 2)
+# Encoding: UTF-8
+# VignetteBuilder: knitr
+# Authors@R: c(person(given="Henrik", family="Singmann", role=c("aut", "cre"), email="singmann+afex@gmail.com"), person(given="Ben", family="Bolker", role=c("aut")), person(given="Jake", family="Westfall", role=c("aut")), person(given="Frederik", family="Aust", role=c("aut")), person(given="Sren", family="Hjsgaard", role=c("ctb")), person(given="John", family="Fox", role=c("ctb")), person(given="Michael A.", family="Lawrence", role=c("ctb")), person(given="Ulf", family="Mertens", role=c("ctb")), person(given="Jonathan", family="Love", role=c("ctb")) )
+# Version: 0.18-0
+# Date: 2017-05-25
+# RoxygenNote: 6.0.1
+# NeedsCompilation: no
+# Packaged: 2017-05-25 12:25:31 UTC; henrik
+# Author: Henrik Singmann [aut, cre], Ben Bolker [aut], Jake Westfall [aut], Frederik Aust [aut], Sren Hjsgaard [ctb], John Fox [ctb], Michael A. Lawrence [ctb], Ulf Mertens [ctb], Jonathan Love [ctb]
+# Maintainer: Henrik Singmann <singmann+afex@gmail.com>
+# Repository: CRAN
+# Date/Publication: 2017-05-25 22:17:48 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-anomalydetection-feedstock contains modified content
diff --git a/r-anomalydetection-feedstock/recipe/meta.yaml b/r-anomalydetection-feedstock/recipe/meta.yaml
index 63814da..3cb2672 100644
--- a/r-anomalydetection-feedstock/recipe/meta.yaml
+++ b/r-anomalydetection-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '0.1.1' %}
+{% set version = '0.2.4' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,63 +12,114 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/anomalyDetection_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/anomalyDetection/anomalyDetection_{{ version }}.tar.gz
-  sha256: 8e900e25a032952818ec35476ecb013ed07d202e9166a18f13802e8560c38610
+  sha256: 5f0248d2e8fb03f8be54bc2ad76c11fe9f7bc481d1234c53e83dedf4fb4a3529
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  # r-tidyverse is not available under windows
-  skip: true  # [win]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: gplots, knitr, RColorBrewer, rmarkdown, testthat,
 requirements:
   build:
     - r-base
+    - {{ compiler('cxx') }}      # [not win]
+    - {{ compiler('fortran') }}  # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
     - r-mass
-    - r-rcolorbrewer
+    - r-rcpp >=0.12.11
+    - r-rcpparmadillo
     - r-caret
     - r-dplyr
+    - r-ggplot2
     - r-gmp
-    - r-gplots
     - r-magrittr
-    - r-matrixstats
     - r-plyr
     - r-purrr
-    - r-qdaptools
     - r-tibble
-    - r-tidyverse
+    - r-tidyr
+
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-mass
-    - r-rcolorbrewer
+    - r-rcpp >=0.12.11
+    - r-rcpparmadillo
     - r-caret
     - r-dplyr
+    - r-ggplot2
     - r-gmp
-    - r-gplots
     - r-magrittr
-    - r-matrixstats
     - r-plyr
     - r-purrr
-    - r-qdaptools
     - r-tibble
-    - r-tidyverse
+    - r-tidyr
 
 test:
   commands:
-    - $R -e "library('anomalyDetection')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('anomalyDetection')"           # [not win]
     - "\"%R%\" -e \"library('anomalyDetection')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
-  home: https://github.com/bradleyboehmke/anomalyDetection
+  home: https://github.com/AFIT-R/anomalyDetection
   license: GPL (>= 2)
-  summary: |
-    Implements procedures to aid in detecting network log anomalies. By combining various
-    multivariate analytic approaches relevant to network anomaly detection, it provides
-    cyber analysts efficient means to detect suspected anomalies requiring further evaluation.
+  summary: Implements procedures to aid in detecting network log anomalies. By  combining various
+    multivariate analytic approaches relevant to network anomaly  detection, it provides
+    cyber analysts efficient means to detect suspected  anomalies requiring further
+    evaluation.
   license_family: GPL3
 
 extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: anomalyDetection
+# Type: Package
+# Title: Implementation of Augmented Network Log Anomaly Detection Procedures
+# Version: 0.2.4
+# Authors@R: c(  person("Andrew", "McCarthy", email = "andrew.mccarthy@theperducogroup.com", role = c("aut", "cre")), person("Brandon", "Greenwell", email = "greenwell.brandon@gmail.com", role = c("aut")), person("Bradley", "Boehmke",  email = "bradleyboehmke@gmail.com",  role = c("aut")),  person("Robert", "Gutierrez",  email = "rjgutierrez2015@gmail.com",  role = c("aut")), person("Kenneth", "Bauer",  email = "kenneth.bauer@afit.edu",  role = c("ctb")), person("Trevor", "Bihl",  email = "trevor.bihl.2@us.af.mil",  role = c("ctb")), person("Cade", "Saie",  email = "cade.saie@gmail.com",  role = c("ctb")) )
+# Maintainer: Andrew McCarthy <andrew.mccarthy@theperducogroup.com>
+# Date: 2017-09-22
+# Description: Implements procedures to aid in detecting network log anomalies. By  combining various multivariate analytic approaches relevant to network anomaly  detection, it provides cyber analysts efficient means to detect suspected  anomalies requiring further evaluation.
+# URL: https://github.com/AFIT-R/anomalyDetection
+# BugReports: https://github.com/AFIT-R/anomalyDetection/issues
+# License: GPL (>= 2)
+# Encoding: UTF-8
+# LazyData: true
+# Depends: R (>= 2.10)
+# Imports: caret, dplyr, ggplot2, gmp, magrittr, MASS, plyr, purrr, Rcpp (>= 0.12.11), stats, tibble, tidyr,
+# RoxygenNote: 6.0.1
+# Suggests: gplots, knitr, RColorBrewer, rmarkdown, testthat,
+# LinkingTo: Rcpp, RcppArmadillo
+# VignetteBuilder: knitr
+# NeedsCompilation: yes
+# Packaged: 2017-09-22 12:58:14 UTC; Andy
+# Author: Andrew McCarthy [aut, cre], Brandon Greenwell [aut], Bradley Boehmke [aut], Robert Gutierrez [aut], Kenneth Bauer [ctb], Trevor Bihl [ctb], Cade Saie [ctb]
+# Repository: CRAN
+# Date/Publication: 2017-09-22 17:21:02 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-ape-feedstock contains modified content
diff --git a/r-ape-feedstock/recipe/meta.yaml b/r-ape-feedstock/recipe/meta.yaml
index f2b6bb9..11d94b2 100644
--- a/r-ape-feedstock/recipe/meta.yaml
+++ b/r-ape-feedstock/recipe/meta.yaml
@@ -14,34 +14,50 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/ape/ape_{{ version }}.tar.gz
   sha256: 935af5ddadcba832d3f9cc032a80fc1a2e627a7ed54ef5f3773f87e06374a924
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: gee, expm
 requirements:
   build:
     - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('fortran') }}  # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
     - r-lattice
     - r-nlme
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-lattice
     - r-nlme
-    - libgcc  # [not win]
 
 test:
   commands:
-    - $R -e "library('ape')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('ape')"           # [not win]
     - "\"%R%\" -e \"library('ape')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://ape-package.ird.fr/
   license: GPL (>= 2)
@@ -64,3 +80,29 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: ape
+# Version: 4.1
+# Date: 2017-02-14
+# Title: Analyses of Phylogenetics and Evolution
+# Authors@R: c(person("Emmanuel", "Paradis", role = c("aut", "cre", "cph"), email = "Emmanuel.Paradis@ird.fr"), person("Simon", "Blomberg", role = c("aut", "cph")), person("Ben", "Bolker", role = c("aut", "cph")), person("Julien", "Claude", role = c("aut", "cph")), person("Hoa Sien", "Cuong", role = c("aut", "cph")), person("Richard", "Desper", role = c("aut", "cph")), person("Gilles", "Didier", role = c("aut", "cph")), person("Benoit", "Durand", role = c("aut", "cph")), person("Julien", "Dutheil", role = c("aut", "cph")), person("RJ", "Ewing", role = c("aut", "cph")), person("Olivier", "Gascuel", role = c("aut", "cph")), person("Christoph", "Heibl", role = c("aut", "cph")), person("Anthony", "Ives", role = c("aut", "cph")), person("Bradley", "Jones", role = c("aut", "cph")), person("Daniel", "Lawson", role = c("aut", "cph")), person("Vincent", "Lefort", role = c("aut", "cph")), person("Pierre", "Legendre", role = c("aut", "cph")), person("Jim", "Lemon", role = c("aut", "cph")), person("Rosemary", "McCloskey", role = c("aut", "cph")), person("Johan", "Nylander", role = c("aut", "cph")), person("Rainer", "Opgen-Rhein", role = c("aut", "cph")), person("Andrei-Alin", "Popescu", role = c("aut", "cph")), person("Manuela", "Royer-Carenzi", role = c("aut", "cph")), person("Klaus", "Schliep", role = c("aut", "cph")), person("Korbinian", "Strimmer", role = c("aut", "cph")), person("Damien", "de Vienne", role = c("aut", "cph")))
+# Depends: R (>= 3.2.0)
+# Suggests: gee, expm
+# Imports: nlme, lattice, graphics, methods, stats, tools, utils, parallel
+# ZipData: no
+# Description: Functions for reading, writing, plotting, and manipulating phylogenetic trees, analyses of comparative data in a phylogenetic framework, ancestral character analyses, analyses of diversification and macroevolution, computing distances from DNA sequences, reading and writing nucleotide sequences as well as importing from BioConductor, and several tools such as Mantel's test, generalized skyline plots, graphical exploration of phylogenetic data (alex, trex, kronoviz), estimation of absolute evolutionary rates and clock-like trees using mean path lengths and penalized likelihood, dating trees with non-contemporaneous sequences, translating DNA into AA sequences, and assessing sequence alignments. Phylogeny estimation can be done with the NJ, BIONJ, ME, MVR, SDM, and triangle methods, and several methods handling incomplete distance matrices (NJ*, BIONJ*, MVR*, and the corresponding triangle method). Some functions call external applications (PhyML, Clustal, T-Coffee, Muscle) whose results are returned into R.
+# License: GPL (>= 2)
+# URL: http://ape-package.ird.fr/
+# NeedsCompilation: yes
+# Packaged: 2017-02-14 16:20:43 UTC; paradis
+# Author: Emmanuel Paradis [aut, cre, cph], Simon Blomberg [aut, cph], Ben Bolker [aut, cph], Julien Claude [aut, cph], Hoa Sien Cuong [aut, cph], Richard Desper [aut, cph], Gilles Didier [aut, cph], Benoit Durand [aut, cph], Julien Dutheil [aut, cph], RJ Ewing [aut, cph], Olivier Gascuel [aut, cph], Christoph Heibl [aut, cph], Anthony Ives [aut, cph], Bradley Jones [aut, cph], Daniel Lawson [aut, cph], Vincent Lefort [aut, cph], Pierre Legendre [aut, cph], Jim Lemon [aut, cph], Rosemary McCloskey [aut, cph], Johan Nylander [aut, cph], Rainer Opgen-Rhein [aut, cph], Andrei-Alin Popescu [aut, cph], Manuela Royer-Carenzi [aut, cph], Klaus Schliep [aut, cph], Korbinian Strimmer [aut, cph], Damien de Vienne [aut, cph]
+# Maintainer: Emmanuel Paradis <Emmanuel.Paradis@ird.fr>
+# Repository: CRAN
+# Date/Publication: 2017-02-14 19:15:21
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-argparse-feedstock contains modified content
diff --git a/r-argparse-feedstock/recipe/meta.yaml b/r-argparse-feedstock/recipe/meta.yaml
index 0740d9e..034e1d5 100644
--- a/r-argparse-feedstock/recipe/meta.yaml
+++ b/r-argparse-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '1.0.4' %}
+{% set version = '1.0.7' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,15 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/argparse_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/argparse/argparse_{{ version }}.tar.gz
-  sha256: 0fbd2346e3539089233dc772ebd9b0dbb037dec7eb3c44361a43cf5da750cce3
+  sha256: 1a2b2d1721260eadc6833c382f739bf50858cc9130ea2308db2195fd911d01de
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: knitr (>= 1.15.19), testthat
 requirements:
   build:
     - r-base
@@ -35,13 +43,19 @@ requirements:
     - r-getopt >=1.19
     - r-proto
     - r-rjson
-    - python
 
 test:
   commands:
-    - $R -e "library('argparse')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('argparse')"           # [not win]
     - "\"%R%\" -e \"library('argparse')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/trevorld/argparse
   license: GPL (>= 2)
@@ -53,3 +67,33 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: argparse
+# Type: Package
+# Title: Command Line Optional and Positional Argument Parser
+# Version: 1.0.7
+# Author: Trevor L Davis. Ports examples from the argparse Python module by the Python Software Foundation. Ports examples from the getopt package by Allen Day.
+# Maintainer: Trevor L Davis <trevor.l.davis@gmail.com>
+# Description: A command line parser to be used with Rscript to write "#!" shebang scripts that gracefully accept positional and optional arguments and automatically generate usage.
+# License: GPL (>= 2)
+# Copyright: See file (inst/)COPYRIGHTS.
+# URL: https://github.com/trevorld/argparse
+# BugReports: https://github.com/trevorld/argparse/issues
+# LazyLoad: yes
+# Depends: proto
+# Imports: findpython, getopt (>= 1.19), rjson
+# SystemRequirements: python (>= 2.7)
+# Suggests: knitr (>= 1.15.19), testthat
+# VignetteBuilder: knitr
+# RoxygenNote: 6.0.1
+# NeedsCompilation: no
+# Packaged: 2017-08-22 22:46:37 UTC; trevorld
+# Repository: CRAN
+# Date/Publication: 2017-08-23 03:05:28 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-assertthat-feedstock contains modified content
diff --git a/r-assertthat-feedstock/recipe/meta.yaml b/r-assertthat-feedstock/recipe/meta.yaml
index c6fa10e..d3a992f 100644
--- a/r-assertthat-feedstock/recipe/meta.yaml
+++ b/r-assertthat-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '0.1' %}
+{% set version = '0.2.0' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,15 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/assertthat_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/assertthat/assertthat_{{ version }}.tar.gz
-  sha256: 1363645a9a128f615aa0641dc5f5c5abd960b1c38320492366dad1e7a5c29a37
+  sha256: d73ef79b1e75293ed889a99571b237a95829c099f7da094d4763f83ea6fde5f2
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: testthat
 requirements:
   build:
     - r-base
@@ -30,14 +38,20 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('assertthat')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('assertthat')"           # [not win]
     - "\"%R%\" -e \"library('assertthat')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=assertthat
   license: GPL-3
-  summary: |
-    assertthat is an extension to stopifnot() that makes it easy to declare the pre and
+  summary: assertthat is an extension to stopifnot() that makes it easy to declare the pre and
     post conditions that you code should satisfy, while also producing friendly error
     messages so that your users know what they've done wrong.
   license_family: GPL3
@@ -46,3 +60,27 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: assertthat
+# Title: Easy Pre and Post Assertions
+# Version: 0.2.0
+# Authors@R:  person("Hadley", "Wickham", , "hadley@rstudio.com", c("aut", "cre"))
+# Description: assertthat is an extension to stopifnot() that makes it easy to declare the pre and post conditions that you code should satisfy, while also producing friendly error messages so that your users know what they've done wrong.
+# License: GPL-3
+# Imports: tools
+# Suggests: testthat
+# Collate: 'assert-that.r' 'on-failure.r' 'assertions-file.r' 'assertions-scalar.R' 'assertions.r' 'base.r' 'base-comparison.r' 'base-is.r' 'base-logical.r' 'base-misc.r' 'utils.r' 'validate-that.R'
+# RoxygenNote: 6.0.1
+# NeedsCompilation: no
+# Packaged: 2017-04-10 19:00:43 UTC; hadley
+# Author: Hadley Wickham [aut, cre]
+# Maintainer: Hadley Wickham <hadley@rstudio.com>
+# Repository: CRAN
+# Date/Publication: 2017-04-11 21:28:45 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-backports-feedstock contains modified content
diff --git a/r-backports-feedstock/recipe/meta.yaml b/r-backports-feedstock/recipe/meta.yaml
index d97de8f..397f624 100644
--- a/r-backports-feedstock/recipe/meta.yaml
+++ b/r-backports-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '1.0.5' %}
+{% set version = '1.1.1' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,34 +12,49 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/backports_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/backports/backports_{{ version }}.tar.gz
+  sha256: 494e81a4829339c8f1cc3e015daa807e9138b8e21b929965fc7c00b1abbe8897
 
-  sha256: c0e6d87eab50f8f90f02088d31ae4b76bee4f8d27f8ad23766fdcdb5518cc1de
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
-  skip: true  # [win32]
+  # If this is a new build for the same version, increment the build number.
   number: 0
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('backports')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('backports')"           # [not win]
     - "\"%R%\" -e \"library('backports')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/mllg/backports
   license: GPL-2
-  summary: |
-    Implementations of functions which have been introduced in R since version 3.0.0.
+  summary: Implementations of functions which have been introduced in R since version 3.0.0.
     The backports are conditionally exported which results in R resolving the function
     names to the version shipped with R (if available) and uses the implemented backports
     as fallback. This way package developers can make use of the new functions without
@@ -50,3 +65,29 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: backports
+# Type: Package
+# Title: Reimplementations of Functions Introduced Since R-3.0.0
+# Version: 1.1.1
+# Author: Michel Lang <michellang@gmail.com>
+# Maintainer: Michel Lang <michellang@gmail.com>
+# Description: Implementations of functions which have been introduced in R since version 3.0.0. The backports are conditionally exported which results in R resolving the function names to the version shipped with R (if available) and uses the implemented backports as fallback. This way package developers can make use of the new functions without worrying about the minimum required R version.
+# URL: https://github.com/mllg/backports
+# BugReports: https://github.com/mllg/backports/issues
+# License: GPL-2
+# NeedsCompilation: yes
+# ByteCompile: yes
+# Depends: R (>= 3.0.0)
+# Imports: utils
+# RoxygenNote: 6.0.1
+# Packaged: 2017-09-25 20:20:29 UTC; michel
+# Repository: CRAN
+# Date/Publication: 2017-09-25 22:09:46 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-base-feedstock contains modified content
Submodule r-base-feedstock 1c65d15..217d204:
diff --git a/r-base-feedstock/recipe/build.sh b/r-base-feedstock/recipe/build.sh
index a86733e..b8bee01 100644
--- a/r-base-feedstock/recipe/build.sh
+++ b/r-base-feedstock/recipe/build.sh
@@ -64,6 +64,8 @@ Linux() {
     # make check-all -j1 V=1 > $(uname)-make-check.log 2>&1 || make check-all -j1 V=1 > $(uname)-make-check.2.log 2>&1
 
     make install
+    # Prevent C and C++ extensions from linking to libgfortran.
+    sed -i -r 's|(^LDFLAGS = .*)-lgfortran|\1|g' ${PREFIX}/lib/R/etc/Makeconf
 }
 
 # This was an attempt to see how far we could get with using Autotools as things
diff --git a/r-base-feedstock/recipe/meta.yaml b/r-base-feedstock/recipe/meta.yaml
index 2203534..d4f6f8e 100644
--- a/r-base-feedstock/recipe/meta.yaml
+++ b/r-base-feedstock/recipe/meta.yaml
@@ -1,6 +1,6 @@
 {% set native = 'm2w64-' if win else '' %}
 {% set posix= 'm2-' if win else '' %}
-{% set version= '3.4.1' %}
+{% set version= '3.4.2' %}
 
 package:
   name: r-base
@@ -8,7 +8,7 @@ package:
 
 source:
   url: https://cran.r-project.org/src/base/R-3/R-{{ version }}.tar.gz
-  sha256: 02b1135d15ea969a3582caeb95594a05e830a6debcdb5b85ed2d5836a6a3fc78
+  sha256: 971e30c2436cf645f58552905105d75788bd9733bddbcb7c4fbff4c1a6d80c64
   patches:
     - mac-unicode.patch  # [osx]
     - 0002-Win32-Fix-trio-config-h-include-depth-issue.patch
@@ -23,7 +23,7 @@ source:
     - 0013-javareconf-do-not-fail-on-compile-fail.patch
 
 build:
-  number: 3
+  number: 0
   rpaths:
     - lib/R/lib/
     - lib/
@@ -54,6 +54,7 @@ requirements:
     - {{ cdt('libxt-devel') }}           # [linux]
     - {{ cdt('libxcb') }}                # [linux]
     - {{ cdt('libxau') }}                # [linux]
+    - {{ cdt('java-1.7.0-openjdk') }}    # [linux]
     - {{posix}}pkg-config
     - posix                              # [win]
     - m2-texinfo-tex                     # [win]
@@ -65,7 +66,7 @@ requirements:
     - curl                               # [not win]
     - xz                                 # [not win]
     - libpng                             # [not win]
-    - texlive-core                       # [osx]
+    - texlive-core                       # [unix]
     - {{posix}}autoconf
     - {{posix}}automake-wrapper          # [win]
     - {{posix}}automake                  # [not win]
@@ -145,7 +146,8 @@ about:
 
 extra:
   recipe-maintainers:
+    - bgruening
     - johanneskoester
+    - mingwandroid
     - ocefpaf
     - sodre
-    - bgruening
Submodule r-base64enc-feedstock contains modified content
diff --git a/r-base64enc-feedstock/recipe/meta.yaml b/r-base64enc-feedstock/recipe/meta.yaml
index 9912c45..9923b79 100644
--- a/r-base64enc-feedstock/recipe/meta.yaml
+++ b/r-base64enc-feedstock/recipe/meta.yaml
@@ -12,37 +12,49 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/base64enc_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/base64enc/base64enc_{{ version }}.tar.gz
-
   sha256: 6d856d8a364bcdc499a0bf38bfd283b7c743d08f0b288174fba7dbf0a04b688d
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
-  skip: true  # [win32]
+  # If this is a new build for the same version, increment the build number.
   number: 0
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('base64enc')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('base64enc')"           # [not win]
     - "\"%R%\" -e \"library('base64enc')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://www.rforge.net/base64enc
   license: GPL-2 | GPL-3
-  summary: |
-    This package provides tools for handling base64 encoding. It is more flexible than
+  summary: This package provides tools for handling base64 encoding. It is more flexible than
     the orphaned base64 package.
   license_family: GPL3
 
@@ -50,3 +62,25 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: base64enc
+# Version: 0.1-3
+# Title: Tools for base64 encoding
+# Author: Simon Urbanek <Simon.Urbanek@r-project.org>
+# Maintainer: Simon Urbanek <Simon.Urbanek@r-project.org>
+# Depends: R (>= 2.9.0)
+# Enhances: png
+# Description: This package provides tools for handling base64 encoding. It is more flexible than the orphaned base64 package.
+# License: GPL-2 | GPL-3
+# URL: http://www.rforge.net/base64enc
+# NeedsCompilation: yes
+# Packaged: 2015-02-04 20:31:00 UTC; svnuser
+# Repository: CRAN
+# Date/Publication: 2015-07-28 08:03:37
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
diff --git a/r-bcp-feedstock/recipe/meta.yaml b/r-bcp-feedstock/recipe/meta.yaml
index b641b5a..1f4953b 100644
--- a/r-bcp-feedstock/recipe/meta.yaml
+++ b/r-bcp-feedstock/recipe/meta.yaml
@@ -12,9 +12,9 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/bcp_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/bcp/bcp_{{ version }}.tar.gz
+  sha256: f5b54d26fb09d087cf187dec0bc105a52476bf162b1c5a4a9a9e2929023a6dae
 
 
-  sha256: f5b54d26fb09d087cf187dec0bc105a52476bf162b1c5a4a9a9e2929023a6dae
   # patches:
    # List any patch files here
    # - fix.patch
@@ -32,23 +32,24 @@ build:
 requirements:
   build:
     - r-base
+    - {{ compiler('cxx') }}      # [not win]
+    - {{ compiler('fortran') }}  # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
     - r-rcpp >=0.9.2
     - r-rcpparmadillo
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-rcpp >=0.9.2
     - r-rcpparmadillo
-    - {{native}}gcc-libs   # [win]
-    - libgcc               # [not win]
 
 test:
   commands:
     # You can put additional test commands to be run here.
-    - $R -e "library('bcp')"  # [not win]
+    - $R -e "library('bcp')"           # [not win]
     - "\"%R%\" -e \"library('bcp')\""  # [win]
 
   # You can also put a file called run_test.py, run_test.sh, or run_test.bat
@@ -67,6 +68,10 @@ about:
     Parallel MCMC, previously available in bcp v.3.0.0, is currently not implemented.
   license_family: GPL3
 
+extra:
+  recipe-maintainers:
+    - mingwandroid
+
 # The original CRAN metadata for this package was:
 
 # Package: bcp
Submodule r-bdsmatrix-feedstock contains modified content
diff --git a/r-bdsmatrix-feedstock/recipe/meta.yaml b/r-bdsmatrix-feedstock/recipe/meta.yaml
index 017d5a2..453f5ba 100644
--- a/r-bdsmatrix-feedstock/recipe/meta.yaml
+++ b/r-bdsmatrix-feedstock/recipe/meta.yaml
@@ -12,35 +12,49 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/bdsmatrix_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/bdsmatrix/bdsmatrix_{{ version }}.tar.gz
-
   sha256: 9e5bf1f73302883a4495708853c560fcce1fe1f91c8bf160aa56870c2677109b
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
-  skip: true  # [win32]
+  # If this is a new build for the same version, increment the build number.
   number: 0
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
+
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('bdsmatrix')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('bdsmatrix')"           # [not win]
     - "\"%R%\" -e \"library('bdsmatrix')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://r-forge.r-project.org
   license: LGPL-2
-  summary: This is a special case of sparse matrices, used by coxme.
+  summary: 'This is a special case of sparse matrices, used by coxme '
 
   license_family: LGPL
 
@@ -48,3 +62,28 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: bdsmatrix
+# Title: Routines for Block Diagonal Symmetric matrices
+# Maintainer: Terry Therneau <therneau@mayo.edu>
+# Version: 1.3-2
+# Date: 2014-08-22
+# Depends: methods, R (>= 2.0.0)
+# LazyData: Yes
+# LazyLoad: Yes
+# Author: Terry Therneau
+# Description: This is a special case of sparse matrices, used by coxme
+# License: LGPL-2
+# URL: http://r-forge.r-project.org
+# Collate: bdsmatrix.R gchol.R gchol.bdsmatrix.R as.matrix.bdsmatrix.R bdsBlock.R bdsI.R bdsmatrix.ibd.R bdsmatrix.reconcile.R diag.bdsmatrix.R listbdsmatrix.R multiply.bdsmatrix.R solve.bdsmatrix.R solve.gchol.R solve.gchol.bdsmatrix.R backsolve.R
+# Packaged: 2014-08-22 14:44:45 UTC; therneau
+# NeedsCompilation: yes
+# Repository: CRAN
+# Date/Publication: 2014-08-22 21:20:31
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-bestglm-feedstock contains modified content
diff --git a/r-bestglm-feedstock/recipe/meta.yaml b/r-bestglm-feedstock/recipe/meta.yaml
index 17820b2..6455191 100644
--- a/r-bestglm-feedstock/recipe/meta.yaml
+++ b/r-bestglm-feedstock/recipe/meta.yaml
@@ -14,13 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/bestglm/bestglm_{{ version }}.tar.gz
   sha256: 90f9b1e0a718cdc1ea273ae5a6717fa849cc01e0d0d12b333484bfcf2c0e5ab5
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: xtable, MASS
 requirements:
   build:
     - r-base
@@ -28,9 +36,6 @@ requirements:
     - r-grpreg
     - r-lattice
     - r-leaps
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
@@ -38,13 +43,19 @@ requirements:
     - r-grpreg
     - r-lattice
     - r-leaps
-    - libgcc  # [not win]
 
 test:
   commands:
-    - $R -e "library('bestglm')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('bestglm')"           # [not win]
     - "\"%R%\" -e \"library('bestglm')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://www.stats.uwo.ca/faculty/aim
   license: GPL (>= 2)
@@ -55,3 +66,32 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: bestglm
+# Type: Package
+# Title: Best Subset GLM
+# Version: 0.36
+# Date: 2017-02-23
+# Author: A.I. McLeod  and Changjiang Xu
+# Maintainer: A.I. McLeod <aimcleod@uwo.ca>
+# Depends: R (>= 2.0.0), leaps
+# Suggests: xtable, MASS
+# Imports: lattice, glmnet, grpreg
+# Description: Best subset glm using information criteria or cross-validation.
+# LazyLoad: yes
+# LazyData: yes
+# Classification/ACM: G.3, G.4, I.5.1
+# Classification/MSC: 62M10, 91B84
+# License: GPL (>= 2)
+# URL: http://www.stats.uwo.ca/faculty/aim
+# NeedsCompilation: yes
+# Packaged: 2017-02-23 17:54:29 UTC; IanMcLeod
+# Repository: CRAN
+# Date/Publication: 2017-02-23 23:44:05
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-bh-feedstock contains modified content
diff --git a/r-bh-feedstock/recipe/meta.yaml b/r-bh-feedstock/recipe/meta.yaml
index 268cd3c..04cfb8c 100644
--- a/r-bh-feedstock/recipe/meta.yaml
+++ b/r-bh-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '1.62.0-1' %}
+{% set version = '1.65.0-1' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,48 +12,85 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/BH_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/BH/BH_{{ version }}.tar.gz
+  sha256: 82baa78afe8f1edc3c7e84e1c9924321047e14c1e990df9b848407baf3f7cb58
 
-  sha256: 5718a14084e30e35288a9e021487df5518913b1ae28f4131b69e34fbdd6d6e07
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
+
   run:
     - r-base
 
 test:
   commands:
-    - $R -e "library('BH')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('BH')"           # [not win]
     - "\"%R%\" -e \"library('BH')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=BH
   license: BSL-1.0
-  summary: |
-    Boost provides free peer-reviewed portable C++ source  libraries.  A large part of
+  summary: 'Boost provides free peer-reviewed portable C++ source  libraries.  A large part of
     Boost is provided as C++ template code which is resolved entirely at compile-time
     without linking.  This  package aims to provide the most useful subset of Boost
     libraries  for template use among CRAN package. By placing these libraries in  this
     package, we offer a more efficient distribution system for CRAN  as replication
-    of this code in the sources of other packages is  avoided. As of release 1.62.0-1,
-    the following Boost libraries are included: ''algorithm'' ''any'' ''atomic'' ''bimap''
-    ''bind'' ''circular_buffer'' ''concept'' ''config'' ''container'' ''date''_''time''
+    of this code in the sources of other packages is  avoided. As of release 1.65.0-1,
+    the following Boost libraries are included: ''algorithm'' ''align'' ''any'' ''atomic''
+    ''bimap'' ''bind'' ''circular_buffer'' ''concept'' ''config'' ''container'' ''date''_''time''
     ''detail'' ''dynamic_bitset'' ''exception'' ''filesystem'' ''flyweight'' ''foreach''
     ''functional'' ''fusion'' ''geometry'' ''graph'' ''heap'' ''icl'' ''integer'' ''interprocess''
     ''intrusive'' ''io'' ''iostreams'' ''iterator'' ''math'' ''move'' ''mpl'' ''multiprcecision''
     ''numeric'' ''pending'' ''phoenix'' ''preprocessor'' ''propery_tree'' ''random''
-    ''range'' ''scope_exit'' ''smart_ptr'' ''spirit'' ''tuple'' ''type_traits'' ''typeof''
-    ''unordered'' ''utility'' ''uuid''.
+    ''range'' ''scope_exit'' ''smart_ptr'' ''sort'' ''spirit'' ''tuple'' ''type_traits''
+    ''typeof'' ''unordered'' ''utility'' ''uuid''.'
+
   license_family: OTHER
 
 extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: BH
+# Type: Package
+# Title: Boost C++ Header Files
+# Version: 1.65.0-1
+# Date: 2017-08-24
+# Author: Dirk Eddelbuettel, John W. Emerson and Michael J. Kane
+# Maintainer: Dirk Eddelbuettel <edd@debian.org>
+# Description: Boost provides free peer-reviewed portable C++ source  libraries.  A large part of Boost is provided as C++ template code which is resolved entirely at compile-time without linking.  This  package aims to provide the most useful subset of Boost libraries  for template use among CRAN package. By placing these libraries in  this package, we offer a more efficient distribution system for CRAN  as replication of this code in the sources of other packages is  avoided. As of release 1.65.0-1, the following Boost libraries are included: 'algorithm' 'align' 'any' 'atomic' 'bimap' 'bind' 'circular_buffer' 'concept' 'config' 'container' 'date'_'time' 'detail' 'dynamic_bitset' 'exception' 'filesystem' 'flyweight' 'foreach' 'functional' 'fusion' 'geometry' 'graph' 'heap' 'icl' 'integer' 'interprocess' 'intrusive' 'io' 'iostreams' 'iterator' 'math' 'move' 'mpl' 'multiprcecision' 'numeric' 'pending' 'phoenix' 'preprocessor' 'propery_tree' 'random' 'range' 'scope_exit' 'smart_ptr' 'sort' 'spirit' 'tuple' 'type_traits' 'typeof' 'unordered' 'utility' 'uuid'.
+# License: BSL-1.0
+# BugReports: https://github.com/eddelbuettel/bh/issues
+# NeedsCompilation: no
+# Packaged: 2017-08-24 11:41:27 UTC; edd
+# Repository: CRAN
+# Date/Publication: 2017-08-24 14:39:52 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-bindr-feedstock contains modified content
diff --git a/r-bindr-feedstock/recipe/meta.yaml b/r-bindr-feedstock/recipe/meta.yaml
index b9e449e..0aa3bbe 100644
--- a/r-bindr-feedstock/recipe/meta.yaml
+++ b/r-bindr-feedstock/recipe/meta.yaml
@@ -14,14 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/bindr/bindr_{{ version }}.tar.gz
   sha256: cca166612eeafd6e1c961b34aaf177f9b47f8b4bc37520e277b9920eaa8b2535
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: testthat
 requirements:
   build:
     - r-base
@@ -31,12 +38,19 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('bindr')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('bindr')"           # [not win]
     - "\"%R%\" -e \"library('bindr')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/krlmlr/bindr, https://krlmlr.github.io/bindr
-  license: MIT
+  license: MIT + file LICENSE
   summary: Provides a simple interface for creating active bindings where the bound function
     accepts additional arguments.
   license_family: MIT
@@ -47,3 +61,30 @@ extra:
     - bgruening
     - daler
     - jdblischak
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: bindr
+# Title: Parametrized Active Bindings
+# Version: 0.1
+# Authors@R: c( person("Kirill", "Muller", role = c("aut", "cre"), email = "krlmlr+r@mailbox.org"), person("RStudio", role = "cph") )
+# Description: Provides a simple interface for creating active bindings where the bound function accepts additional arguments.
+# Suggests: testthat
+# LazyData: true
+# Date: 2016-11-12
+# BugReports: https://github.com/krlmlr/bindr/issues
+# URL: https://github.com/krlmlr/bindr, https://krlmlr.github.io/bindr
+# RoxygenNote: 5.0.1.9000
+# License: MIT + file LICENSE
+# Encoding: UTF-8
+# NeedsCompilation: no
+# Packaged: 2016-11-12 22:17:21 UTC; muelleki
+# Author: Kirill Muller [aut, cre], RStudio [cph]
+# Maintainer: Kirill Muller <krlmlr+r@mailbox.org>
+# Repository: CRAN
+# Date/Publication: 2016-11-13 01:48:48
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-bindrcpp-feedstock contains modified content
diff --git a/r-bindrcpp-feedstock/recipe/meta.yaml b/r-bindrcpp-feedstock/recipe/meta.yaml
index fa99558..8e3ac0c 100644
--- a/r-bindrcpp-feedstock/recipe/meta.yaml
+++ b/r-bindrcpp-feedstock/recipe/meta.yaml
@@ -14,39 +14,54 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/bindrcpp/bindrcpp_{{ version }}.tar.gz
   sha256: d0efa1313cb8148880f7902a4267de1dcedae916f28d9a0ef5911f44bf103450
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: testthat
 requirements:
   build:
-    - {{ compiler('cxx') }}  # [not win]
-    - {{ compiler('c') }}    # [not win]
     - r-base
+    - {{ compiler('cxx') }}      # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
     - r-rcpp
     - r-bindr
     - r-plogr
-    - posix                  # [win]
-    - {{native}}toolchain    # [win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-rcpp
     - r-bindr
     - r-plogr
-    - {{native}}gcc-libs     # [win]
 
 test:
   commands:
-    - $R -e "library('bindrcpp')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('bindrcpp')"           # [not win]
     - "\"%R%\" -e \"library('bindrcpp')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/krlmlr/bindrcpp, https://krlmlr.github.io/bindrcpp
-  license: MIT
+  license: MIT + file LICENSE
   summary: Provides an easy way to fill an environment with active bindings that call a C++ function.
   license_family: MIT
 
@@ -56,3 +71,32 @@ extra:
     - bgruening
     - daler
     - jdblischak
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: bindrcpp
+# Title: An 'Rcpp' Interface to Active Bindings
+# Description: Provides an easy way to fill an environment with active bindings that call a C++ function.
+# Authors@R: c( person("Kirill", "Muller", role = c("aut", "cre"), email = "krlmlr+r@mailbox.org"), person("RStudio", role = "cph") )
+# Version: 0.2
+# Date: 2017-06-15
+# URL: https://github.com/krlmlr/bindrcpp, https://krlmlr.github.io/bindrcpp
+# BugReports: https://github.com/krlmlr/bindrcpp/issues
+# Imports: Rcpp, bindr
+# Suggests: testthat
+# LinkingTo: Rcpp, plogr
+# RoxygenNote: 6.0.1
+# LazyData: true
+# License: MIT + file LICENSE
+# Encoding: UTF-8
+# NeedsCompilation: yes
+# Packaged: 2017-06-15 20:53:22 UTC; muelleki
+# Author: Kirill Muller [aut, cre], RStudio [cph]
+# Maintainer: Kirill Muller <krlmlr+r@mailbox.org>
+# Repository: CRAN
+# Date/Publication: 2017-06-17 23:52:40 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-bit-feedstock contains modified content
diff --git a/r-bit-feedstock/recipe/meta.yaml b/r-bit-feedstock/recipe/meta.yaml
index 00f095d..16748d2 100644
--- a/r-bit-feedstock/recipe/meta.yaml
+++ b/r-bit-feedstock/recipe/meta.yaml
@@ -12,37 +12,51 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/bit_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/bit/bit_{{ version }}.tar.gz
-
   sha256: ce281c87fb7602bf1a599e72f3e25f9ff7a13e390c124a4506087f69ad79d128
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
-  skip: true  # [win32]
+  # If this is a new build for the same version, increment the build number.
   number: 0
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('bit')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('bit')"           # [not win]
     - "\"%R%\" -e \"library('bit')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://ff.r-forge.r-project.org/
   license: GPL-2
-  summary: |
-    bitmapped vectors of booleans (no NAs),  coercion from and to logicals, integers
+  summary: 'bitmapped vectors of booleans (no NAs),  coercion from and to logicals, integers
     and integer subscripts;  fast boolean operators and fast summary statistics.  With
     ''bit'' vectors you can store true binary booleans {FALSE,TRUE} at the  expense
     of 1 bit only, on a 32 bit architecture this means factor 32 less  RAM and ~ factor
@@ -58,10 +72,37 @@ about:
     to bit comes at a  performance penalty of 150%. the package now contains further
     classes for  representing logical selections: ''bitwhich'' for very skewed selections
     and  ''ri'' for selecting ranges of values for chunked processing. All three index  classes
-    can be used for subsetting ''ff'' objects (ff-2.1-0 and higher).
+    can be used for subsetting ''ff'' objects (ff-2.1-0 and higher).'
+
   license_family: GPL2
 
 extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: bit
+# Type: Package
+# Title: A class for vectors of 1-bit booleans
+# Version: 1.1-12
+# Date: 2012-01-17
+# Author: Jens Oehlschlagel <Jens.Oehlschlaegel@truecluster.com>
+# Maintainer: Jens Oehlschlagel <Jens.Oehlschlaegel@truecluster.com>
+# Depends: R (>= 2.9.2)
+# Description: bitmapped vectors of booleans (no NAs),  coercion from and to logicals, integers and integer subscripts;  fast boolean operators and fast summary statistics.  With 'bit' vectors you can store true binary booleans {FALSE,TRUE} at the  expense of 1 bit only, on a 32 bit architecture this means factor 32 less  RAM and ~ factor 32 more speed on boolean operations. Due to overhead of  R calls, actual speed gain depends on the size of the vector: expect gains  for vectors of size > 10000 elements. Even for one-time boolean operations  it can pay-off to convert to bit, the pay-off is obvious, when such  components are used more than once.  Reading from and writing to bit is approximately as fast as accessing  standard logicals - mostly due to R's time for memory allocation. The package  allows to work with pre-allocated memory for return values by calling .Call()  directly: when evaluating the speed of C-access with pre-allocated vector  memory, coping from bit to logical requires only 70% of the time for copying  from logical to logical; and copying from logical to bit comes at a  performance penalty of 150%. the package now contains further classes for  representing logical selections: 'bitwhich' for very skewed selections and  'ri' for selecting ranges of values for chunked processing. All three index  classes can be used for subsetting 'ff' objects (ff-2.1-0 and higher).
+# License: GPL-2
+# LazyLoad: yes
+# ByteCompile: yes
+# Encoding: latin1
+# URL: http://ff.r-forge.r-project.org/
+# Packaged: 2014-04-08 21:51:43 UTC; jens
+# NeedsCompilation: yes
+# Repository: CRAN
+# Date/Publication: 2014-04-09 09:54:10
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-bit64-feedstock contains modified content
diff --git a/r-bit64-feedstock/recipe/meta.yaml b/r-bit64-feedstock/recipe/meta.yaml
index 95de8b3..7aaf64a 100644
--- a/r-bit64-feedstock/recipe/meta.yaml
+++ b/r-bit64-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '0.9-5' %}
+{% set version = '0.9-7' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,40 +12,61 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/bit64_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/bit64/bit64_{{ version }}.tar.gz
-  sha256: 8f529f9e92caa133ced9c63f327a2682b70fac3f4fa96a0e95fcf50ddda8e53b
+  sha256: 7b9aaa7f971198728c3629f9ba1a1b24d53db5c7e459498b0fdf86bbd3dff61f
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
+
+
 requirements:
   build:
     - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
     - r-bit >=1.1_12
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-bit >=1.1_12
 
 test:
   commands:
-    - $R -e "library('bit64')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('bit64')"           # [not win]
     - "\"%R%\" -e \"library('bit64')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://ff.r-forge.r-project.org/
   license: GPL-2
-  summary: ' Package ''bit64'' provides serializable S3 atomic 64bit (signed) integers  that
-    can be used in vectors, matrices, arrays and data.frames. Methods are  available
-    for coercion from and to logicals, integers, doubles, characters  and factors as
-    well as many elementwise and summary functions. Many fast  algorithmic operations
-    such as ''match'' and ''order'' support interactive data exploration and manipulation
-    and optionally leverage caching.'
+  summary: ' Package ''bit64'' provides serializable S3 atomic 64bit (signed) integers.  These
+    are useful for handling database keys and exact counting in +-2^63. WARNING: do
+    not use them as replacement for 32bit integers, integer64 are not supported for
+    subscripting by R-core and they have different semantics when  combined with double,
+    e.g. integer64 + double => integer64.  Class integer64 can be used in vectors, matrices,
+    arrays and data.frames.  Methods are available for coercion from and to logicals,
+    integers, doubles,  characters and factors as well as many elementwise and summary
+    functions.  Many fast algorithmic operations such as ''match'' and ''order'' support
+    inter- active data exploration and manipulation and optionally leverage caching.'
 
   license_family: GPL2
 
@@ -53,3 +74,29 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: bit64
+# Type: Package
+# Title: A S3 Class for Vectors of 64bit Integers
+# Version: 0.9-7
+# Date: 2017-05-07
+# Author: Jens Oehlschlagel <Jens.Oehlschlaegel@truecluster.com>
+# Maintainer: Jens Oehlschlagel <Jens.Oehlschlaegel@truecluster.com>
+# Depends: R (>= 3.0.1), bit (>= 1.1-12), utils, methods, stats
+# Description:  Package 'bit64' provides serializable S3 atomic 64bit (signed) integers.  These are useful for handling database keys and exact counting in +-2^63. WARNING: do not use them as replacement for 32bit integers, integer64 are not supported for subscripting by R-core and they have different semantics when  combined with double, e.g. integer64 + double => integer64.  Class integer64 can be used in vectors, matrices, arrays and data.frames.  Methods are available for coercion from and to logicals, integers, doubles,  characters and factors as well as many elementwise and summary functions.  Many fast algorithmic operations such as 'match' and 'order' support inter- active data exploration and manipulation and optionally leverage caching.
+# License: GPL-2
+# LazyLoad: yes
+# ByteCompile: yes
+# URL: http://ff.r-forge.r-project.org/
+# Encoding: UTF-8
+# NeedsCompilation: yes
+# Packaged: 2017-05-07 19:07:42 UTC; jo
+# Repository: CRAN
+# Date/Publication: 2017-05-08 13:21:40 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-bitops-feedstock contains modified content
diff --git a/r-bitops-feedstock/recipe/meta.yaml b/r-bitops-feedstock/recipe/meta.yaml
index 3682010..e7b16a3 100644
--- a/r-bitops-feedstock/recipe/meta.yaml
+++ b/r-bitops-feedstock/recipe/meta.yaml
@@ -12,32 +12,45 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/bitops_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/bitops/bitops_{{ version }}.tar.gz
-
   sha256: 9b731397b7166dd54941fb0d2eac6df60c7a483b2e790f7eb15b4d7b79c9d69c
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
-  skip: true  # [win32]
+  # If this is a new build for the same version, increment the build number.
   number: 0
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('bitops')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('bitops')"           # [not win]
     - "\"%R%\" -e \"library('bitops')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=bitops
   license: GPL (>= 2)
@@ -48,3 +61,23 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: bitops
+# Version: 1.0-6
+# Date: 2013-08-17
+# Author: S original by Steve Dutky <sdutky@terpalum.umd.edu> initial R port and extensions by Martin Maechler; revised and modified by Steve Dutky
+# Maintainer: Martin Maechler <maechler@stat.math.ethz.ch>
+# Title: Bitwise Operations
+# Description: Functions for bitwise operations on integer vectors.
+# License: GPL (>= 2)
+# Packaged: 2013-08-17 15:58:57 UTC; maechler
+# NeedsCompilation: yes
+# Repository: CRAN
+# Date/Publication: 2013-08-17 21:10:34
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
diff --git a/r-blob-feedstock/recipe/bld.bat b/r-blob-feedstock/recipe/bld.bat
index e69de29..8b10c94 100644
--- a/r-blob-feedstock/recipe/bld.bat
+++ b/r-blob-feedstock/recipe/bld.bat
@@ -0,0 +1,8 @@
+"%R%" CMD INSTALL --build .
+if errorlevel 1 exit 1
+
+@rem Add more build steps here, if they are necessary.
+
+@rem See
+@rem http://docs.continuum.io/conda/build.html
+@rem for a list of environment variables that are set during the build process.
diff --git a/r-blob-feedstock/recipe/build.sh b/r-blob-feedstock/recipe/build.sh
index e69de29..0c37df6 100644
--- a/r-blob-feedstock/recipe/build.sh
+++ b/r-blob-feedstock/recipe/build.sh
@@ -0,0 +1,13 @@
+#!/bin/bash
+
+# R refuses to build packages that mark themselves as Priority: Recommended
+mv DESCRIPTION DESCRIPTION.old
+grep -v '^Priority: ' DESCRIPTION.old > DESCRIPTION
+
+$R CMD INSTALL --build .
+
+# Add more build steps here, if they are necessary.
+
+# See
+# http://docs.continuum.io/conda/build.html
+# for a list of environment variables that are set during the build process.
diff --git a/r-blob-feedstock/recipe/meta.yaml b/r-blob-feedstock/recipe/meta.yaml
index e69de29..ba3df0d 100644
--- a/r-blob-feedstock/recipe/meta.yaml
+++ b/r-blob-feedstock/recipe/meta.yaml
@@ -0,0 +1,89 @@
+{% set version = '1.1.0' %}
+
+{% set posix = 'm2-' if win else '' %}
+{% set native = 'm2w64-' if win else '' %}
+
+package:
+  name: r-blob
+  version: {{ version|replace("-", "_") }}
+
+source:
+  fn: blob_{{ version }}.tar.gz
+  url:
+    - https://cran.r-project.org/src/contrib/blob_{{ version }}.tar.gz
+    - https://cran.r-project.org/src/contrib/Archive/blob/blob_{{ version }}.tar.gz
+  sha256: 16d6603df3ddba177f0ac4d9469c938f89131c4bf8834345db838defd9ffea16
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
+build:
+  # If this is a new build for the same version, increment the build number.
+  number: 0
+
+  # This is required to make R link correctly on Linux.
+  rpaths:
+    - lib/R/lib/
+    - lib/
+
+# Suggests: testthat, covr
+requirements:
+  build:
+    - r-base
+    - r-tibble
+
+  run:
+    - r-base
+    - r-tibble
+
+test:
+  commands:
+    # You can put additional test commands to be run here.
+    - $R -e "library('blob')"           # [not win]
+    - "\"%R%\" -e \"library('blob')\""  # [win]
+
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
+about:
+  home: https://github.com/hadley/blob
+  license: GPL-3
+  summary: R's raw vector is useful for storing a single binary object. What if you want to put
+    a vector of them in a data frame? The blob package provides the blob object, a list
+    of raw vectors, suitable for use as a column in data frame.
+  license_family: GPL3
+
+extra:
+  recipe-maintainers:
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: blob
+# Title: A Simple S3 Class for Representing Vectors of Binary Data ('BLOBS')
+# Version: 1.1.0
+# Authors@R: c( person("Hadley", "Wickham", role = c("aut")), person("Kirill", "Muller", , "krlmlr+r@mailbox.org", role = c("cre")), person("RStudio", role = "cph") )
+# Description: R's raw vector is useful for storing a single binary object. What if you want to put a vector of them in a data frame? The blob package provides the blob object, a list of raw vectors, suitable for use as a column in data frame.
+# Imports: tibble
+# License: GPL-3
+# Encoding: UTF-8
+# LazyData: true
+# URL: https://github.com/hadley/blob
+# BugReports: https://github.com/hadley/blob/issues
+# Suggests: testthat, covr
+# RoxygenNote: 6.0.1
+# NeedsCompilation: no
+# Packaged: 2017-06-17 04:01:59 UTC; muelleki
+# Author: Hadley Wickham [aut], Kirill Muller [cre], RStudio [cph]
+# Maintainer: Kirill Muller <krlmlr+r@mailbox.org>
+# Repository: CRAN
+# Date/Publication: 2017-06-17 14:57:46 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-boot-feedstock contains modified content
diff --git a/r-boot-feedstock/recipe/meta.yaml b/r-boot-feedstock/recipe/meta.yaml
index 3d5939f..7bee6eb 100644
--- a/r-boot-feedstock/recipe/meta.yaml
+++ b/r-boot-feedstock/recipe/meta.yaml
@@ -1,49 +1,89 @@
-{% set name = 'boot' %}
-{% set version = '1.3-18' %}
-{% set sha256 = '12fd237f810a69cc8d0a51a67c57eaf9506bf0341c764f8ab7c1feb73722235e' %}
+{% set version = '1.3-20' %}
+
+{% set posix = 'm2-' if win else '' %}
+{% set native = 'm2w64-' if win else '' %}
 
 package:
-  name: r-{{ name|lower }}
+  name: r-boot
   version: {{ version|replace("-", "_") }}
 
 source:
-  fn: {{ name }}_{{ version }}.tar.gz
+  fn: boot_{{ version }}.tar.gz
   url:
-    - https://cran.r-project.org/src/contrib/{{ name }}_{{ version }}.tar.gz
-    - https://cran.r-project.org/src/contrib/Archive/{{ name }}/{{ name }}_{{ version }}.tar.gz
+    - https://cran.r-project.org/src/contrib/boot_{{ version }}.tar.gz
+    - https://cran.r-project.org/src/contrib/Archive/boot/boot_{{ version }}.tar.gz
+  sha256: adcb90b72409705e3f9c69ea6c15673dcb649b464fed06723fe0930beac5212a
+
 
-  sha256: {{ sha256 }}
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  # skip on windows because of this error:
-  # https://ci.appveyor.com/project/conda-forge/staged-recipes/build/1.0.10696/job/hma4wc1ms5ssj2qk#L415
-  skip: true  # [win or linux32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: MASS, survival
+requirements:
+  build:
+    - r-base
 
-requirements:  # [not win32]
-  build:       # [not win32]
-    - r-base   # [not win32]
-  run:         # [not win32]
-    - r-base   # [not win32]
+  run:
+    - r-base
 
 test:
   commands:
-    - $R -e "library('boot')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('boot')"           # [not win]
     - "\"%R%\" -e \"library('boot')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=boot
   license: Unlimited
-  summary: 'Functions and datasets for bootstrapping from the book "Bootstrap Methods and Their
+  summary: Functions and datasets for bootstrapping from the book "Bootstrap Methods and Their
     Application" by A. C. Davison and  D. V. Hinkley (1997, CUP), originally written
-    by Angelo Canty for S.'
+    by Angelo Canty for S.
   license_family: MIT
 
 extra:
   recipe-maintainers:
     - johanneskoester
     - ocefpaf
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: boot
+# Priority: recommended
+# Version: 1.3-20
+# Date: 2017-07-30
+# Authors@R: c(person("Angelo", "Canty", role = "aut", email = "cantya@mcmaster.ca"), person("Brian", "Ripley", role = c("aut", "trl", "cre"), email = "ripley@stats.ox.ac.uk", comment = "author of parallel support"))
+# Maintainer: Brian Ripley <ripley@stats.ox.ac.uk>
+# Note: Maintainers are not available to give advice on using a package they did not author.
+# Description: Functions and datasets for bootstrapping from the book "Bootstrap Methods and Their Application" by A. C. Davison and  D. V. Hinkley (1997, CUP), originally written by Angelo Canty for S.
+# Title: Bootstrap Functions (Originally by Angelo Canty for S)
+# Depends: R (>= 3.0.0), graphics, stats
+# Suggests: MASS, survival
+# LazyData: yes
+# ByteCompile: yes
+# License: Unlimited
+# NeedsCompilation: no
+# Packaged: 2017-07-30 08:28:30 UTC; ripley
+# Author: Angelo Canty [aut], Brian Ripley [aut, trl, cre] (author of parallel support)
+# Repository: CRAN
+# Date/Publication: 2017-07-30 10:08:23 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-bradleyterry2-feedstock contains modified content
diff --git a/r-bradleyterry2-feedstock/recipe/meta.yaml b/r-bradleyterry2-feedstock/recipe/meta.yaml
index 07ec5ec..d3656fe 100644
--- a/r-bradleyterry2-feedstock/recipe/meta.yaml
+++ b/r-bradleyterry2-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '1.0-6' %}
+{% set version = '1.0-8' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,37 +12,54 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/BradleyTerry2_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/BradleyTerry2/BradleyTerry2_{{ version }}.tar.gz
-  sha256: 2d8cf29094552f01d28880e0ad2084be4441c6ef820f288117d479c2ddc10081
+  sha256: 1f95e885cd68d72a06c1990e6dd6a5f9ae5e26a1cf9b5af63fbdcce27efb8625
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: prefmod
 requirements:
   build:
     - r-base
     - r-brglm
     - r-gtools
     - r-lme4 >=1.0
+    - r-qvcalc
+
   run:
     - r-base
     - r-brglm
     - r-gtools
     - r-lme4 >=1.0
+    - r-qvcalc
 
 test:
   commands:
-    - $R -e "library('BradleyTerry2')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('BradleyTerry2')"           # [not win]
     - "\"%R%\" -e \"library('BradleyTerry2')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
-  home: http://bradleyterry2.r-forge.r-project.org
+  home: https://github.com/hturner/BradleyTerry2
   license: GPL (>= 2)
-  summary: |
-    Specify and fit the Bradley-Terry model, including structured versions in which the
+  summary: Specify and fit the Bradley-Terry model, including structured versions in which the
     parameters are related to explanatory variables through a linear predictor and versions
     with contest-specific effects, such as a home advantage.
   license_family: GPL3
@@ -51,3 +68,32 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: BradleyTerry2
+# Version: 1.0-8
+# Title: Bradley-Terry Models
+# Authors@R: c(person("Heather", "Turner", role = c("aut", "cre"), email = "ht@heatherturner.net"), person("David", "Firth", role = "aut"))
+# URL: https://github.com/hturner/BradleyTerry2
+# BugReports: https://github.com/hturner/BradleyTerry2/issues
+# Description: Specify and fit the Bradley-Terry model, including structured versions in which the parameters are related to explanatory variables through a linear predictor and versions with contest-specific effects, such as a home advantage.
+# Depends: R (>= 2.10)
+# Imports: brglm, gtools, lme4 (>= 1.0), qvcalc, stats
+# Suggests: prefmod
+# Enhances: gnm
+# License: GPL (>= 2)
+# LazyData: yes
+# Encoding: UTF-8
+# RoxygenNote: 6.0.1
+# NeedsCompilation: no
+# Packaged: 2017-09-20 08:33:05 UTC; hturner
+# Author: Heather Turner [aut, cre], David Firth [aut]
+# Maintainer: Heather Turner <ht@heatherturner.net>
+# Repository: CRAN
+# Date/Publication: 2017-09-20 09:26:09 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-brew-feedstock contains modified content
diff --git a/r-brew-feedstock/recipe/meta.yaml b/r-brew-feedstock/recipe/meta.yaml
index 7aed5a8..2c9f029 100644
--- a/r-brew-feedstock/recipe/meta.yaml
+++ b/r-brew-feedstock/recipe/meta.yaml
@@ -14,14 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/brew/brew_{{ version }}.tar.gz
   sha256: d70d1a9a01cf4a923b4f11e4374ffd887ad3ff964f35c6f9dc0f29c8d657f0ed
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
@@ -31,9 +38,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('brew')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('brew')"           # [not win]
     - "\"%R%\" -e \"library('brew')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=brew
   license: GPL-2
@@ -46,3 +60,24 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: brew
+# Type: Package
+# Title: Templating Framework for Report Generation
+# Version: 1.0-6
+# Date: 2010-09-30
+# Author: Jeffrey Horner
+# Maintainer: Jeffrey Horner <jeffrey.horner@gmail.com>
+# Description: brew implements a templating framework for mixing text and R code for report generation. brew template syntax is similar to PHP, Ruby's erb module, Java Server Pages, and Python's psp module.
+# License: GPL-2
+# Packaged: 2011-04-13 14:36:39 UTC; hornerj
+# Repository: CRAN
+# Date/Publication: 2011-04-13 15:16:08
+# NeedsCompilation: no
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-brglm-feedstock contains modified content
diff --git a/r-brglm-feedstock/recipe/meta.yaml b/r-brglm-feedstock/recipe/meta.yaml
index cb109cc..37bb28a 100644
--- a/r-brglm-feedstock/recipe/meta.yaml
+++ b/r-brglm-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '0.5-9' %}
+{% set version = '0.6.1' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,32 +12,47 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/brglm_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/brglm/brglm_{{ version }}.tar.gz
-  sha256: 5b70bc8216d1af37304e5e7c6e902b92327cc2aad5798ea6ba1634dfab921d92
+  sha256: b786db50edc703b1b6a986b1ce13d31aab76fd3c672a6540161b25f5cd57239f
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: MASS
 requirements:
   build:
     - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
     - r-profilemodel
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-profilemodel
 
 test:
   commands:
-    - $R -e "library('brglm')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('brglm')"           # [not win]
     - "\"%R%\" -e \"library('brglm')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://www.ucl.ac.uk/~ucakiko/index.html
   license: GPL (>= 2)
@@ -57,3 +72,26 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: brglm
+# Title: Bias Reduction in Binomial-Response Generalized Linear Models
+# Version: 0.6.1
+# Date: 2017-06-28
+# Author: Ioannis Kosmidis <i.kosmidis@ucl.ac.uk>
+# Maintainer: Ioannis Kosmidis <i.kosmidis@ucl.ac.uk>
+# URL: http://www.ucl.ac.uk/~ucakiko/index.html
+# Description: Fit generalized linear models with binomial responses using either an adjusted-score approach to bias reduction or maximum penalized likelihood where penalization is by Jeffreys invariant prior. These procedures return estimates with improved frequentist properties (bias, mean squared error) that are always finite even in cases where the maximum likelihood estimates are infinite (data separation). Fitting takes place by fitting generalized linear models on iteratively updated pseudo-data. The interface is essentially the same as 'glm'.  More flexibility is provided by the fact that custom pseudo-data representations can be specified and used for model fitting. Functions are provided for the construction of confidence intervals for the reduced-bias estimates.
+# License: GPL (>= 2)
+# Depends: R (>= 2.6.0), profileModel
+# Suggests: MASS
+# NeedsCompilation: yes
+# Packaged: 2017-06-28 21:46:05 UTC; yiannis
+# Repository: CRAN
+# Date/Publication: 2017-06-29 05:56:44 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-broom-feedstock contains modified content
diff --git a/r-broom-feedstock/recipe/meta.yaml b/r-broom-feedstock/recipe/meta.yaml
index 455a9a7..ca50cf7 100644
--- a/r-broom-feedstock/recipe/meta.yaml
+++ b/r-broom-feedstock/recipe/meta.yaml
@@ -14,13 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/broom/broom_{{ version }}.tar.gz
   sha256: 9f409413623cf25e7110452e6215353af5114f7044d73af182bd6c10971c5a44
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: knitr, boot, survival, gam, glmnet, lfe, Lahman, MASS, sp, maps, maptools, multcomp, testthat, lme4, zoo, lmtest, plm, biglm, ggplot2, nnet, geepack, AUC, ergm, network, statnet.common, xergm, btergm, binGroup, Hmisc, bbmle, gamlss, rstan, rstanarm, brms, coda, gmm, Matrix, ks, purrr, orcutt, mgcv, lmodel2, poLCA, mclust, covr, lsmeans, betareg, robust, akima
 requirements:
   build:
     - r-base
@@ -44,22 +52,57 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('broom')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('broom')"           # [not win]
     - "\"%R%\" -e \"library('broom')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://github.com/tidyverse/broom
-  license: MIT
-  summary: |
-    Convert statistical analysis objects from R into tidy data frames, so that they can
+  license: MIT + file LICENSE
+  summary: 'Convert statistical analysis objects from R into tidy data frames, so that they can
     more easily be combined, reshaped and otherwise processed with tools like ''dplyr'',
     ''tidyr'' and ''ggplot2''. The package provides three S3 generics: tidy, which summarizes
     a model''s statistical findings such as coefficients of a regression; augment, which
     adds columns to the original data such as predictions, residuals and cluster assignments;
-    and glance, which provides a one-row summary of model-level statistics.
+    and glance, which provides a one-row summary of model-level statistics.'
+
   license_family: MIT
 
 extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: broom
+# Type: Package
+# Title: Convert Statistical Analysis Objects into Tidy Data Frames
+# Version: 0.4.2
+# Date: 2016-02-13
+# Authors@R: c( person("David", "Robinson", email = "admiral.david@gmail.com", role = c("aut", "cre")), person("Matthieu", "Gomez", email = "mattg@princeton.edu", role = "ctb"), person("Boris", "Demeshev", email = "boris.demeshev@gmail.com", role = "ctb"), person("Dieter", "Menne", email = "dieter.menne@menne-biomed.de", role = "ctb"), person("Benjamin", "Nutter", email = "nutter@battelle.org", role = "ctb"), person("Luke", "Johnston", email = "luke.johnston@mail.utoronto.ca", role = "ctb"), person("Ben", "Bolker", email = "bolker@mcmaster.ca", role = "ctb"), person("Francois", "Briatte", email = "f.briatte@gmail.com", role = "ctb"), person("Jeffrey", "Arnold", email = "jeffrey.arnold@gmail.com", role = "ctb"), person("Jonah", "Gabry", email = "jsg2201@columbia.edu", role = "ctb"), person("Luciano", "Selzer", email = "luciano.selzer@gmail.com", role = "ctb"), person("Gavin", "Simpson", email = "ucfagls@gmail.com", role = "ctb"), person("Jens", "Preussner", email = " jens.preussner@mpi-bn.mpg.de", role = "ctb"), person("Jay", "Hesselberth", email = "jay.hesselberth@gmail.com", role = "ctb"), person("Hadley", "Wickham", email = "hadley@rstudio.com", role = "ctb") )
+# Maintainer: David Robinson <admiral.david@gmail.com>
+# Description: Convert statistical analysis objects from R into tidy data frames, so that they can more easily be combined, reshaped and otherwise processed with tools like 'dplyr', 'tidyr' and 'ggplot2'. The package provides three S3 generics: tidy, which summarizes a model's statistical findings such as coefficients of a regression; augment, which adds columns to the original data such as predictions, residuals and cluster assignments; and glance, which provides a one-row summary of model-level statistics.
+# Imports: plyr, dplyr, tidyr, psych, stringr, reshape2, nlme, methods
+# Suggests: knitr, boot, survival, gam, glmnet, lfe, Lahman, MASS, sp, maps, maptools, multcomp, testthat, lme4, zoo, lmtest, plm, biglm, ggplot2, nnet, geepack, AUC, ergm, network, statnet.common, xergm, btergm, binGroup, Hmisc, bbmle, gamlss, rstan, rstanarm, brms, coda, gmm, Matrix, ks, purrr, orcutt, mgcv, lmodel2, poLCA, mclust, covr, lsmeans, betareg, robust, akima
+# URL: http://github.com/tidyverse/broom
+# BugReports: http://github.com/tidyverse/broom/issues
+# VignetteBuilder: knitr
+# License: MIT + file LICENSE
+# RoxygenNote: 5.0.1
+# NeedsCompilation: no
+# Packaged: 2017-02-13 21:57:25 UTC; drobinson
+# Author: David Robinson [aut, cre], Matthieu Gomez [ctb], Boris Demeshev [ctb], Dieter Menne [ctb], Benjamin Nutter [ctb], Luke Johnston [ctb], Ben Bolker [ctb], Francois Briatte [ctb], Jeffrey Arnold [ctb], Jonah Gabry [ctb], Luciano Selzer [ctb], Gavin Simpson [ctb], Jens Preussner [ctb], Jay Hesselberth [ctb], Hadley Wickham [ctb]
+# Repository: CRAN
+# Date/Publication: 2017-02-13 23:34:38
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-cairo-feedstock contains modified content
diff --git a/r-cairo-feedstock/recipe/meta.yaml b/r-cairo-feedstock/recipe/meta.yaml
index 34a713e..245de4f 100644
--- a/r-cairo-feedstock/recipe/meta.yaml
+++ b/r-cairo-feedstock/recipe/meta.yaml
@@ -12,39 +12,55 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/Cairo_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/Cairo/Cairo_{{ version }}.tar.gz
+  sha256: 2a867b6cae96671d6bc3acf9334d6615dc01f6ecf1953a27cde8a43c724a38f4
 
 
-  sha256: 2a867b6cae96671d6bc3acf9334d6615dc01f6ecf1953a27cde8a43c724a38f4
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: True  # [win32]
-
-
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: png
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
-    - pkg-config           # [unix]
-    - cairo 1.14.*
+    - {{ compiler('c') }}                # [not win]
+    - {{native}}toolchain                # [win]
+    - {{ cdt('libxrender-devel') }}      # [linux]
+    - {{ cdt('libxext-devel') }}         # [linux]
+    - {{ cdt('libx11-devel') }}          # [linux]
+    - {{ cdt('xorg-x11-proto-devel') }}  # [linux]
+    - {{ cdt('libxau-devel') }}          # [linux]
+    - {{ cdt('libxt-devel') }}           # [linux]
+    - {{posix}}filesystem                # [win]
+    - {{posix}}autoconf
+    - {{posix}}automake
+    - {{posix}}pkg-config
+    - {{posix}}make
 
   run:
     - r-base
-    - cairo 1.14.*
+    - {{native}}gcc-libs                 # [win]
 
 test:
   commands:
-    - $R -e "library('Cairo')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('Cairo')"           # [not win]
     - "\"%R%\" -e \"library('Cairo')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
 
+  # requires:
+    # Put any additional test requirements here.
 
 about:
   home: http://www.rforge.net/Cairo/
@@ -60,8 +76,31 @@ about:
     are modular such that any subset of backends is supported.
   license_family: GPL2
 
-
-
 extra:
   recipe-maintainers:
     - croth1
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: Cairo
+# Version: 1.5-9
+# Title: R graphics device using cairo graphics library for creating high-quality bitmap (PNG, JPEG, TIFF), vector (PDF, SVG, PostScript) and display (X11 and Win32) output
+# Author: Simon Urbanek <Simon.Urbanek@r-project.org>, Jeffrey Horner <jeff.horner@vanderbilt.edu>
+# Maintainer: Simon Urbanek <Simon.Urbanek@r-project.org>
+# Depends: R (>= 2.4.0)
+# Imports: grDevices, graphics
+# Suggests: png
+# Enhances: FastRWeb
+# Description: Cairo graphics device that can be use to create high-quality vector (PDF, PostScript and SVG) and bitmap output (PNG,JPEG,TIFF), and high-quality rendering in displays (X11 and Win32). Since it uses the same back-end for all output, copying across formats is WYSIWYG. Files are created without the dependence on X11 or other external programs. This device supports alpha channel (semi-transparent drawing) and resulting images can contain transparent and semi-transparent regions. It is ideal for use in server environments (file output) and as a replacement for other devices that don't have Cairo's capabilities such as alpha support or anti-aliasing. Backends are modular such that any subset of backends is supported.
+# License: GPL-2
+# SystemRequirements: cairo (>= 1.2 http://www.cairographics.org/)
+# URL: http://www.rforge.net/Cairo/
+# NeedsCompilation: yes
+# Packaged: 2015-09-25 17:49:11 UTC; svnuser
+# Repository: CRAN
+# Date/Publication: 2015-09-26 09:05:40
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-car-feedstock contains modified content
diff --git a/r-car-feedstock/recipe/meta.yaml b/r-car-feedstock/recipe/meta.yaml
index b1e5530..13f9294 100644
--- a/r-car-feedstock/recipe/meta.yaml
+++ b/r-car-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '2.1-4' %}
+{% set version = '2.1-5' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,15 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/car_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/car/car_{{ version }}.tar.gz
-  sha256: fd39cf1750cb560a66623fea3fa9e6a94fc24e3dc36367aff24df7d0743edb28
+  sha256: a60eb64a59cbc238ede7a1018182f364a79ce39ea401fc7e1c1f18f5929ea0ae
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: alr4, boot, coxme, leaps, lme4, lmtest, Matrix, MatrixModels, nlme, rgl (>= 0.93.960), sandwich, SparseM, survival, survey, nloptr
 requirements:
   build:
     - r-base
@@ -29,6 +37,7 @@ requirements:
     - r-nnet
     - r-pbkrtest >=0.4_4
     - r-quantreg
+
   run:
     - r-base
     - r-mass
@@ -39,18 +48,57 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('car')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('car')"           # [not win]
     - "\"%R%\" -e \"library('car')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
-  home: https://r-forge.r-project.org/projects/car/
+  home: https://r-forge.r-project.org/projects/car/, https://CRAN.R-project.org/package=car,
+    http://socserv.socsci.mcmaster.ca/jfox/Books/Companion/index.html
   license: GPL (>= 2)
-  summary: |
-    Functions and Datasets to Accompany J. Fox and S. Weisberg, An R Companion to Applied
-    Regression, Second Edition, Sage, 2011
+  summary: ' Functions and Datasets to Accompany J. Fox and S. Weisberg,  An R Companion to Applied
+    Regression, Second Edition, Sage, 2011.'
+
   license_family: GPL3
 
 extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: car
+# Version: 2.1-5
+# Date: 2017-06-25
+# Title: Companion to Applied Regression
+# Authors@R: c(person("John", "Fox", role = c("aut", "cre"), email = "jfox@mcmaster.ca"), person("Sanford", "Weisberg", role = "aut", email = "sandy@umn.edu"), person("Daniel", "Adler", role="ctb"), person("Douglas", "Bates", role = "ctb"), person("Gabriel", "Baud-Bovy", role = "ctb"), person("Steve", "Ellison", role="ctb"), person("David", "Firth", role = "ctb"), person("Michael", "Friendly", role = "ctb"), person("Gregor", "Gorjanc", role = "ctb"), person("Spencer", "Graves", role = "ctb"), person("Richard", "Heiberger", role = "ctb"), person("Rafael", "Laboissiere", role = "ctb"), person("Georges", "Monette", role = "ctb"), person("Duncan", "Murdoch", role="ctb"), person("Henric", "Nilsson", role = "ctb"), person("Derek", "Ogle", role = "ctb"), person("Brian", "Ripley", role = "ctb"), person("William", "Venables", role = "ctb"), person("David", "Winsemius", role="ctb"), person("Achim", "Zeileis", role = "ctb"), person("R-Core", role="ctb"))
+# Depends: R (>= 3.2.0)
+# Imports: MASS, mgcv, nnet, pbkrtest (>= 0.4-4), quantreg, grDevices, utils, stats, graphics
+# Suggests: alr4, boot, coxme, leaps, lme4, lmtest, Matrix, MatrixModels, nlme, rgl (>= 0.93.960), sandwich, SparseM, survival, survey, nloptr
+# ByteCompile: yes
+# LazyLoad: yes
+# LazyData: yes
+# Description:  Functions and Datasets to Accompany J. Fox and S. Weisberg,  An R Companion to Applied Regression, Second Edition, Sage, 2011.
+# License: GPL (>= 2)
+# URL: https://r-forge.r-project.org/projects/car/, https://CRAN.R-project.org/package=car, http://socserv.socsci.mcmaster.ca/jfox/Books/Companion/index.html
+# Author: John Fox [aut, cre], Sanford Weisberg [aut], Daniel Adler [ctb], Douglas Bates [ctb], Gabriel Baud-Bovy [ctb], Steve Ellison [ctb], David Firth [ctb], Michael Friendly [ctb], Gregor Gorjanc [ctb], Spencer Graves [ctb], Richard Heiberger [ctb], Rafael Laboissiere [ctb], Georges Monette [ctb], Duncan Murdoch [ctb], Henric Nilsson [ctb], Derek Ogle [ctb], Brian Ripley [ctb], William Venables [ctb], David Winsemius [ctb], Achim Zeileis [ctb], R-Core [ctb]
+# Maintainer: John Fox <jfox@mcmaster.ca>
+# Repository: CRAN
+# Repository/R-Forge/Project: car
+# Repository/R-Forge/Revision: 481
+# Repository/R-Forge/DateTimeStamp: 2017-07-03 14:10:10
+# Date/Publication: 2017-07-04 08:00:39 UTC
+# NeedsCompilation: no
+# Packaged: 2017-07-03 14:25:35 UTC; rforge
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-caret-feedstock contains modified content
diff --git a/r-caret-feedstock/recipe/meta.yaml b/r-caret-feedstock/recipe/meta.yaml
index 1f31b11..78e8b0b 100644
--- a/r-caret-feedstock/recipe/meta.yaml
+++ b/r-caret-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '6.0-76' %}
+{% set version = '6.0-77' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,46 +12,63 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/caret_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/caret/caret_{{ version }}.tar.gz
-  sha256: 8c210893d9be02b5ce09153302d72205dba8bab47c077db4c9e62259efef61f0
+  sha256: beec200e18a0bbcefa6ba0380b50bea2faf535ac66c4e7febd56e8630a018515
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: BradleyTerry2, e1071, earth (>= 2.2-3), fastICA, gam, ipred, kernlab, klaR, MASS, ellipse, mda, mgcv, mlbench, MLmetrics, nnet, party (>= 0.9-99992), pls, pROC, proxy, randomForest, RANN, spls, subselect, pamr, superpc, Cubist, testthat (>= 0.9.1)
 requirements:
   build:
     - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
     - r-modelmetrics >=1.1.0
-    - r-car
     - r-foreach
     - r-ggplot2
     - r-lattice >=0.20
     - r-nlme
     - r-plyr
+    - r-recipes >=0.0.1
     - r-reshape2
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - r-withr >=2.0.0
+
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-modelmetrics >=1.1.0
-    - r-car
     - r-foreach
     - r-ggplot2
     - r-lattice >=0.20
     - r-nlme
     - r-plyr
+    - r-recipes >=0.0.1
     - r-reshape2
-    - libgcc  # [not win]
+    - r-withr >=2.0.0
 
 test:
   commands:
-    - $R -e "library('caret')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('caret')"           # [not win]
     - "\"%R%\" -e \"library('caret')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/topepo/caret/
   license: GPL (>= 2)
@@ -62,3 +79,28 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: caret
+# Version: 6.0-77
+# Title: Classification and Regression Training
+# Author: Max Kuhn. Contributions from Jed Wing, Steve Weston, Andre Williams, Chris Keefer, Allan Engelhardt, Tony Cooper, Zachary Mayer, Brenton Kenkel, the R Core Team, Michael Benesty, Reynald Lescarbeau, Andrew Ziem, Luca Scrucca, Yuan Tang, Can Candan, and Tyler Hunt.
+# Description: Misc functions for training and plotting classification and regression models.
+# Maintainer: Max Kuhn <mxkuhn@gmail.com>
+# Depends: R (>= 2.10), lattice (>= 0.20), ggplot2
+# URL: https://github.com/topepo/caret/
+# BugReports: https://github.com/topepo/caret/issues
+# Imports: foreach, methods, plyr, ModelMetrics (>= 1.1.0), nlme, reshape2, stats, stats4, utils, grDevices, recipes (>= 0.0.1), withr (>= 2.0.0)
+# Suggests: BradleyTerry2, e1071, earth (>= 2.2-3), fastICA, gam, ipred, kernlab, klaR, MASS, ellipse, mda, mgcv, mlbench, MLmetrics, nnet, party (>= 0.9-99992), pls, pROC, proxy, randomForest, RANN, spls, subselect, pamr, superpc, Cubist, testthat (>= 0.9.1)
+# License: GPL (>= 2)
+# RoxygenNote: 6.0.1
+# NeedsCompilation: yes
+# Packaged: 2017-09-01 14:30:16 UTC; max
+# Repository: CRAN
+# Date/Publication: 2017-09-07 21:15:33 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-catools-feedstock contains modified content
diff --git a/r-catools-feedstock/recipe/meta.yaml b/r-catools-feedstock/recipe/meta.yaml
index 16a4e57..2c0df6c 100644
--- a/r-catools-feedstock/recipe/meta.yaml
+++ b/r-catools-feedstock/recipe/meta.yaml
@@ -14,30 +14,46 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/caTools/caTools_{{ version }}.tar.gz
   sha256: d32a73febf00930355cc00f3e4e71357412e0f163faae6a4bf7f552cacfe9af4
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: MASS, rpart
 requirements:
   build:
     - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('cxx') }}      # [not win]
+    - {{native}}toolchain        # [win]
     - r-bitops
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-bitops
 
 test:
   commands:
-    - $R -e "library('caTools')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('caTools')"           # [not win]
     - "\"%R%\" -e \"library('caTools')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=caTools
   license: GPL-3
@@ -52,3 +68,27 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: caTools
+# Type: Package
+# Title: Tools: moving window statistics, GIF, Base64, ROC AUC, etc.
+# Version: 1.17.1
+# Date: 2014-04-21
+# Author: Jarek Tuszynski <jaroslaw.w.tuszynski@saic.com>
+# Maintainer: Harutyun Khachatryan <kh_harut@yahoo.com>
+# Depends: R (>= 2.2.0)
+# Imports: bitops
+# Suggests: MASS, rpart
+# Description: Contains several basic utility functions including: moving (rolling, running) window statistic functions, read/write for GIF and ENVI binary files, fast calculation of AUC, LogitBoost classifier, base64 encoder/decoder, round-off-error-free sum and cumsum, etc.
+# License: GPL-3
+# Packaged: 2014-09-09 21:05:06 UTC; ripley
+# NeedsCompilation: yes
+# Repository: CRAN
+# Date/Publication: 2014-09-10 16:22:53
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-cellranger-feedstock contains modified content
diff --git a/r-cellranger-feedstock/recipe/meta.yaml b/r-cellranger-feedstock/recipe/meta.yaml
index abb2d38..3be2cdc 100644
--- a/r-cellranger-feedstock/recipe/meta.yaml
+++ b/r-cellranger-feedstock/recipe/meta.yaml
@@ -14,14 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/cellranger/cellranger_{{ version }}.tar.gz
   sha256: 5d38f288c752bbb9cea6ff830b8388bdd65a8571fd82d8d96064586bd588cf99
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: covr, testthat (>= 1.0.0), knitr, rmarkdown
 requirements:
   build:
     - r-base
@@ -35,12 +42,19 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('cellranger')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('cellranger')"           # [not win]
     - "\"%R%\" -e \"library('cellranger')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/rsheets/cellranger
-  license: MIT
+  license: MIT + file LICENSE
   summary: Helper functions to work with spreadsheets and the "A1:D10" style of cell range specification.
   license_family: MIT
 
@@ -48,3 +62,31 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: cellranger
+# Title: Translate Spreadsheet Cell Ranges to Rows and Columns
+# Version: 1.1.0
+# Authors@R: c( person("Jennifer", "Bryan", , "jenny@stat.ubc.ca", c("cre", "aut")), person("Hadley", "Wickham", , "hadley@rstudio.com", "ctb") )
+# Description: Helper functions to work with spreadsheets and the "A1:D10" style of cell range specification.
+# Depends: R (>= 3.0.0)
+# License: MIT + file LICENSE
+# LazyData: true
+# URL: https://github.com/rsheets/cellranger
+# BugReports: https://github.com/rsheets/cellranger/issues
+# Suggests: covr, testthat (>= 1.0.0), knitr, rmarkdown
+# RoxygenNote: 5.0.1.9000
+# VignetteBuilder: knitr
+# Imports: rematch, tibble
+# NeedsCompilation: no
+# Packaged: 2016-07-26 06:50:00 UTC; jenny
+# Author: Jennifer Bryan [cre, aut], Hadley Wickham [ctb]
+# Maintainer: Jennifer Bryan <jenny@stat.ubc.ca>
+# Repository: CRAN
+# Date/Publication: 2016-07-27 03:17:48
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-checkmate-feedstock contains modified content
diff --git a/r-checkmate-feedstock/recipe/meta.yaml b/r-checkmate-feedstock/recipe/meta.yaml
index 2f30f0e..ac1ffb2 100644
--- a/r-checkmate-feedstock/recipe/meta.yaml
+++ b/r-checkmate-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '1.8.2' %}
+{% set version = '1.8.4' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,37 +12,51 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/checkmate_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/checkmate/checkmate_{{ version }}.tar.gz
-  sha256: 8f182f7894bce100fb2ed4af4487fbf2aceb9bd6d07f8301db549f9ae87b0cff
+  sha256: 6f948883e5a885a1c409d997f0c782e754a549227ec3c8eb18318deceb38f8f6
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: R6, bit, data.table (>= 1.9.8), devtools, ggplot2, knitr, magrittr, microbenchmark, rmarkdown, testthat (>= 0.11.0), tibble
 requirements:
   build:
     - r-base
-    - r-backports
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
+    - r-backports >=1.1.0
+
   run:
     - r-base
-    - r-backports
-    - libgcc  # [not win]
+    - {{native}}gcc-libs         # [win]
+    - r-backports >=1.1.0
 
 test:
   commands:
-    - $R -e "library('checkmate')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('checkmate')"           # [not win]
     - "\"%R%\" -e \"library('checkmate')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/mllg/checkmate
-  license: BSD_3_clause
-  summary: |
-    Tests and assertions to perform frequent argument checks. A substantial part of the
+  license: BSD_3_clause + file LICENSE
+  summary: Tests and assertions to perform frequent argument checks. A substantial part of the
     package was written in C to minimize any worries about execution time overhead.
   license_family: BSD
 
@@ -50,3 +64,35 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: checkmate
+# Type: Package
+# Title: Fast and Versatile Argument Checks
+# Description: Tests and assertions to perform frequent argument checks. A substantial part of the package was written in C to minimize any worries about execution time overhead.
+# Version: 1.8.4
+# Authors@R: c( person("Michel", "Lang", NULL, "michellang@gmail.com", role = c("cre", "aut")), person("Bernd", "Bischl", NULL, "bernd_bischl@gmx.de", role = "ctb") )
+# URL: https://github.com/mllg/checkmate
+# URLNote: https://github.com/mllg/checkmate
+# BugReports: https://github.com/mllg/checkmate/issues
+# NeedsCompilation: yes
+# ByteCompile: yes
+# Encoding: UTF-8
+# Depends: R (>= 3.0.0)
+# Imports: backports (>= 1.1.0), utils
+# Suggests: R6, bit, data.table (>= 1.9.8), devtools, ggplot2, knitr, magrittr, microbenchmark, rmarkdown, testthat (>= 0.11.0), tibble
+# License: BSD_3_clause + file LICENSE
+# VignetteBuilder: knitr
+# RoxygenNote: 6.0.1
+# Collate: 'AssertCollection.R' 'allMissing.R' 'anyInfinite.R' 'anyMissing.R' 'anyNaN.R' 'asInteger.R' 'assert.R' 'helper.R' 'makeExpectation.R' 'makeTest.R' 'makeAssertion.R' 'checkAccess.R' 'checkArray.R' 'checkAtomic.R' 'checkAtomicVector.R' 'checkBit.R' 'checkCharacter.R' 'checkChoice.R' 'checkClass.R' 'checkComplex.R' 'checkCount.R' 'checkDataFrame.R' 'checkDataTable.R' 'checkDate.R' 'checkDirectoryExists.R' 'checkEnvironment.R' 'checkFALSE.R' 'checkFactor.R' 'checkFileExists.R' 'checkFlag.R' 'checkFunction.R' 'checkInt.R' 'checkInteger.R' 'checkIntegerish.R' 'checkList.R' 'checkLogical.R' 'checkMatrix.R' 'checkNamed.R' 'checkNames.R' 'checkNull.R' 'checkNumber.R' 'checkNumeric.R' 'checkOS.R' 'checkPathForOutput.R' 'checkR6.R' 'checkScalar.R' 'checkScalarNA.R' 'checkSetEqual.R' 'checkString.R' 'checkSubset.R' 'checkTRUE.R' 'checkTibble.R' 'checkVector.R' 'coalesce.R' 'isIntegerish.R' 'matchArg.R' 'qassert.R' 'qassertr.R' 'vname.R' 'wfwl.R' 'zzz.R'
+# Packaged: 2017-09-25 19:52:36 UTC; michel
+# Author: Michel Lang [cre, aut], Bernd Bischl [ctb]
+# Maintainer: Michel Lang <michellang@gmail.com>
+# Repository: CRAN
+# Date/Publication: 2017-09-25 22:36:45 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-chron-feedstock contains modified content
diff --git a/r-chron-feedstock/recipe/meta.yaml b/r-chron-feedstock/recipe/meta.yaml
index b9508f3..3e737df 100644
--- a/r-chron-feedstock/recipe/meta.yaml
+++ b/r-chron-feedstock/recipe/meta.yaml
@@ -14,29 +14,43 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/chron/chron_{{ version }}.tar.gz
   sha256: a9efd0bb306bf2fd1969175e42d85e7fe1a85839ea8e45358e3647fd80a17af0
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: scales, ggplot2
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('chron')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('chron')"           # [not win]
     - "\"%R%\" -e \"library('chron')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=chron
   license: GPL-2
@@ -47,3 +61,27 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: chron
+# Version: 2.3-50
+# Title: Chronological Objects which can Handle Dates and Times
+# Description: Provides chronological objects which can handle dates and times.
+# Authors@R: c(person("David", "James", role = "aut", comment = "S original"), person("Kurt", "Hornik", role = c("aut", "trl", "cre"), email = "Kurt.Hornik@R-project.org"), person("Gabor", "Grothendieck", role = "ctb", email = "ggrothendieck@gmail.com"))
+# Depends: R (>= 2.12.0)
+# Imports: graphics, stats
+# Suggests: scales, ggplot2
+# Enhances: zoo
+# License: GPL-2
+# NeedsCompilation: yes
+# Packaged: 2017-02-20 18:48:49 UTC; hornik
+# Author: David James [aut] (S original), Kurt Hornik [aut, trl, cre], Gabor Grothendieck [ctb]
+# Maintainer: Kurt Hornik <Kurt.Hornik@R-project.org>
+# Repository: CRAN
+# Date/Publication: 2017-02-20 20:21:23
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-class-feedstock contains modified content
diff --git a/r-class-feedstock/recipe/meta.yaml b/r-class-feedstock/recipe/meta.yaml
index cdd844b..20f6844 100644
--- a/r-class-feedstock/recipe/meta.yaml
+++ b/r-class-feedstock/recipe/meta.yaml
@@ -14,31 +14,45 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/class/class_{{ version }}.tar.gz
   sha256: 18b876dbc18bebe6a00890eab7d04ef72b903ba0049d5ce50731406a82426b9c
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
     - r-mass
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-mass
 
 test:
   commands:
-    - $R -e "library('class')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('class')"           # [not win]
     - "\"%R%\" -e \"library('class')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://www.stats.ox.ac.uk/pub/MASS4/
   license: GPL-2 | GPL-3
@@ -50,3 +64,29 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: class
+# Priority: recommended
+# Version: 7.3-14
+# Date: 2015-08-29
+# Depends: R (>= 3.0.0), stats, utils
+# Imports: MASS
+# Authors@R: c(person("Brian", "Ripley", role = c("aut", "cre", "cph"), email = "ripley@stats.ox.ac.uk"), person("William", "Venables", role = "cph"))
+# Description: Various functions for classification, including k-nearest neighbour, Learning Vector Quantization and Self-Organizing Maps.
+# Title: Functions for Classification
+# ByteCompile: yes
+# License: GPL-2 | GPL-3
+# URL: http://www.stats.ox.ac.uk/pub/MASS4/
+# NeedsCompilation: yes
+# Packaged: 2015-08-28 15:25:36 UTC; ripley
+# Author: Brian Ripley [aut, cre, cph], William Venables [cph]
+# Maintainer: Brian Ripley <ripley@stats.ox.ac.uk>
+# Repository: CRAN
+# Date/Publication: 2015-08-30 08:59:38
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-cluster-feedstock contains modified content
diff --git a/r-cluster-feedstock/recipe/meta.yaml b/r-cluster-feedstock/recipe/meta.yaml
index 0289128..4bc0bd5 100644
--- a/r-cluster-feedstock/recipe/meta.yaml
+++ b/r-cluster-feedstock/recipe/meta.yaml
@@ -14,29 +14,44 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/cluster/cluster_{{ version }}.tar.gz
   sha256: f424b87f10595ed358b1ac80471b520ab9c767a4d203663c25b177c8ce5f8ffc
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: MASS
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('fortran') }}  # [not win]
+    - {{native}}toolchain        # [win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('cluster')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('cluster')"           # [not win]
     - "\"%R%\" -e \"library('cluster')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=cluster
   license: GPL (>= 2)
@@ -49,3 +64,33 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: cluster
+# Version: 2.0.6
+# Date: 2017-03-10
+# Priority: recommended
+# Title: "Finding Groups in Data": Cluster Analysis Extended Rousseeuw et al.
+# Description: Methods for Cluster analysis.  Much extended the original from Peter Rousseeuw, Anja Struyf and Mia Hubert, based on Kaufman and Rousseeuw (1990) "Finding Groups in Data".
+# Maintainer: Martin Maechler <maechler@stat.math.ethz.ch>
+# Authors@R: c(person("Martin","Maechler", role=c("aut","cre"), email="maechler@stat.math.ethz.ch") ,person("Peter", "Rousseeuw", role="aut", email="rousse@uia.ua.ac.be", comment = "Fortran original") ,person("Anja", "Struyf", role="aut", comment= "S original") ,person("Mia", "Hubert", role="aut", email= "Mia.Hubert@uia.ua.ac.be", comment= "S original") ,person("Kurt", "Hornik", role=c("trl", "ctb"), email="Kurt.Hornik@R-project.org", comment="port to R; maintenance(1999-2000)") ,person("Matthias", "Studer", role="ctb") ,person("Pierre",  "Roudier", role="ctb") ,person("Juan",   "Gonzalez", role="ctb") )
+# Depends: R (>= 3.0.1)
+# Imports: graphics, grDevices, stats, utils
+# Suggests: MASS
+# SuggestsNote: two small examples using MASS' cov.rob() and mvrnorm()
+# LazyLoad: yes
+# LazyData: yes
+# ByteCompile: yes
+# BuildResaveData: no
+# License: GPL (>= 2)
+# NeedsCompilation: yes
+# Packaged: 2017-03-10 09:25:12 UTC; maechler
+# Author: Martin Maechler [aut, cre], Peter Rousseeuw [aut] (Fortran original), Anja Struyf [aut] (S original), Mia Hubert [aut] (S original), Kurt Hornik [trl, ctb] (port to R; maintenance(1999-2000)), Matthias Studer [ctb], Pierre Roudier [ctb], Juan Gonzalez [ctb]
+# Repository: CRAN
+# Date/Publication: 2017-03-16 13:24:03 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-coda-feedstock contains modified content
diff --git a/r-coda-feedstock/recipe/meta.yaml b/r-coda-feedstock/recipe/meta.yaml
index ec7ba3e..557fcde 100644
--- a/r-coda-feedstock/recipe/meta.yaml
+++ b/r-coda-feedstock/recipe/meta.yaml
@@ -14,12 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/coda/coda_{{ version }}.tar.gz
   sha256: d41ff5731da6805170769dba75dd011ab33f916d15b2336001f279e21a524491
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
+
+
 requirements:
   build:
     - r-base
@@ -31,9 +40,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('coda')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('coda')"           # [not win]
     - "\"%R%\" -e \"library('coda')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=coda
   license: GPL (>= 2)
@@ -46,3 +62,26 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: coda
+# Version: 0.19-1
+# Date: 2016-12-07
+# Title: Output Analysis and Diagnostics for MCMC
+# Authors@R: c(person("Martyn", "Plummer", role=c("aut","cre","trl"), email="plummerm@iarc.fr"), person("Nicky", "Best", role="aut"), person("Kate", "Cowles", role="aut"), person("Karen", "Vines", role="aut"), person("Deepayan", "Sarkar", role="aut"), person("Douglas", "Bates", role="aut"), person("Russell", "Almond", role="aut"), person("Arni", "Magnusson", role="aut"))
+# Depends: R (>= 2.14.0)
+# Imports: lattice
+# Description: Provides functions for summarizing and plotting the output from Markov Chain Monte Carlo (MCMC) simulations, as well as diagnostic tests of convergence to the equilibrium distribution of the Markov chain.
+# License: GPL (>= 2)
+# NeedsCompilation: no
+# Packaged: 2016-12-07 15:34:56 UTC; martyn
+# Author: Martyn Plummer [aut, cre, trl], Nicky Best [aut], Kate Cowles [aut], Karen Vines [aut], Deepayan Sarkar [aut], Douglas Bates [aut], Russell Almond [aut], Arni Magnusson [aut]
+# Maintainer: Martyn Plummer <plummerm@iarc.fr>
+# Repository: CRAN
+# Date/Publication: 2016-12-08 10:25:11
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-codetools-feedstock contains modified content
diff --git a/r-codetools-feedstock/recipe/meta.yaml b/r-codetools-feedstock/recipe/meta.yaml
index a3ac601..86878ba 100644
--- a/r-codetools-feedstock/recipe/meta.yaml
+++ b/r-codetools-feedstock/recipe/meta.yaml
@@ -14,14 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/codetools/codetools_{{ version }}.tar.gz
   sha256: 4e0798ed79281a614f8cdd199e25f2c1bd8f35ecec902b03016544bd7795fa40
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
@@ -31,9 +38,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('codetools')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('codetools')"           # [not win]
     - "\"%R%\" -e \"library('codetools')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=codetools
   license: GPL
@@ -44,3 +58,24 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: codetools
+# Version: 0.2-15
+# Priority: recommended
+# Author: Luke Tierney <luke-tierney@uiowa.edu>
+# Description: Code analysis tools for R.
+# Title: Code Analysis Tools for R
+# Depends: R (>= 2.1)
+# Maintainer: Luke Tierney <luke-tierney@uiowa.edu>
+# License: GPL
+# NeedsCompilation: no
+# Packaged: 2016-10-05 12:32:32 UTC; luke
+# Repository: CRAN
+# Date/Publication: 2016-10-05 16:36:47
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-coin-feedstock contains modified content
diff --git a/r-coin-feedstock/recipe/meta.yaml b/r-coin-feedstock/recipe/meta.yaml
index 862c092..0da5e16 100644
--- a/r-coin-feedstock/recipe/meta.yaml
+++ b/r-coin-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '1.1-3' %}
+{% set version = '1.2-1' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,40 +12,56 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/coin_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/coin/coin_{{ version }}.tar.gz
-  sha256: 8b88ecc25903c83539dfc73cdc31a160e2aa4a7bea1773b22c79133d2f006035
+  sha256: 612103149a006cdadfc003925d4498bed2dc8831a32f064d415c4cab9dfaddd2
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: parallel, xtable, e1071, vcd, TH.data (>= 1.0-7), libcoin (>= 0.9-0)
 requirements:
   build:
     - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('fortran') }}  # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
     - r-modeltools >=0.2_9
     - r-multcomp
     - r-mvtnorm >=1.0_5
     - r-survival
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-modeltools >=0.2_9
     - r-multcomp
     - r-mvtnorm >=1.0_5
     - r-survival
-    - libgcc  # [not win]
 
 test:
   commands:
-    - $R -e "library('coin')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('coin')"           # [not win]
     - "\"%R%\" -e \"library('coin')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://coin.r-forge.r-project.org
   license: GPL-2
@@ -58,3 +74,30 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: coin
+# Version: 1.2-1
+# Date: 2017-07-16
+# Title: Conditional Inference Procedures in a Permutation Test Framework
+# Authors@R: c(person("Torsten", "Hothorn", role = c("aut", "cre"), email = "Torsten.Hothorn@R-project.org"), person("Kurt", "Hornik", role = "aut"), person("Mark A.", "van de Wiel", role = "aut"), person("Henric", "Winell", role = "aut"), person("Achim", "Zeileis", role = "aut"))
+# Description: Conditional inference procedures for the general independence problem including two-sample, K-sample (non-parametric ANOVA), correlation, censored, ordered and multivariate problems.
+# Depends: R (>= 2.14.0), methods, survival
+# Imports: stats, modeltools (>= 0.2-9), mvtnorm (>= 1.0-5), multcomp
+# Suggests: parallel, xtable, e1071, vcd, TH.data (>= 1.0-7), libcoin (>= 0.9-0)
+# LazyData: yes
+# NeedsCompilation: yes
+# ByteCompile: yes
+# License: GPL-2
+# URL: http://coin.r-forge.r-project.org
+# Packaged: 2017-07-17 09:10:20 UTC; hothorn
+# Author: Torsten Hothorn [aut, cre], Kurt Hornik [aut], Mark A. van de Wiel [aut], Henric Winell [aut], Achim Zeileis [aut]
+# Maintainer: Torsten Hothorn <Torsten.Hothorn@R-project.org>
+# Repository: CRAN
+# Date/Publication: 2017-07-17 22:32:53 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-colorspace-feedstock contains modified content
diff --git a/r-colorspace-feedstock/recipe/meta.yaml b/r-colorspace-feedstock/recipe/meta.yaml
index 3c65a24..794aa74 100644
--- a/r-colorspace-feedstock/recipe/meta.yaml
+++ b/r-colorspace-feedstock/recipe/meta.yaml
@@ -14,33 +14,46 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/colorspace/colorspace_{{ version }}.tar.gz
   sha256: dd9fd2342b650456901d014e7ff6d2e201f8bec0b555be63b1a878d2e1513e34
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: datasets, stats, utils, KernSmooth, MASS, kernlab, mvtnorm, vcd, dichromat, tcltk, shiny, shinyjs
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('colorspace')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('colorspace')"           # [not win]
     - "\"%R%\" -e \"library('colorspace')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://hclwizard.org/
-  license: BSD_3_clause
-  license_file: LICENSE
+  license: BSD_3_clause + file LICENSE
   summary: Carries out mapping between assorted color spaces including RGB, HSV, HLS, CIEXYZ,
     CIELUV, HCL (polar CIELUV), CIELAB and polar CIELAB. Qualitative, sequential, and
     diverging color palettes based on HCL colors are provided along with an interactive
@@ -51,3 +64,29 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: colorspace
+# Version: 1.3-2
+# Date: 2016-12-14
+# Title: Color Space Manipulation
+# Authors@R: c(person(given = "Ross", family = "Ihaka", role = "aut", email = "ihaka@stat.auckland.ac.nz"), person(given = "Paul", family = "Murrell", role = "aut", email = "paul@stat.auckland.ac.nz"), person(given = "Kurt", family = "Hornik", role = "aut", email = "Kurt.Hornik@R-project.org"), person(given = c("Jason", "C."), family = "Fisher", role = "aut", email = "jfisher@usgs.gov"), person(given = "Reto", family = "Stauffer", role = "aut", email = "Reto.Stauffer@uibk.ac.at"), person(given = "Achim", family = "Zeileis", role = c("aut", "cre"), email = "Achim.Zeileis@R-project.org"))
+# Description: Carries out mapping between assorted color spaces including RGB, HSV, HLS, CIEXYZ, CIELUV, HCL (polar CIELUV), CIELAB and polar CIELAB. Qualitative, sequential, and diverging color palettes based on HCL colors are provided along with an interactive palette picker (with either a Tcl/Tk or a shiny GUI).
+# Depends: R (>= 2.13.0), methods
+# Imports: graphics, grDevices
+# Suggests: datasets, stats, utils, KernSmooth, MASS, kernlab, mvtnorm, vcd, dichromat, tcltk, shiny, shinyjs
+# License: BSD_3_clause + file LICENSE
+# URL: https://hclwizard.org/
+# LazyData: yes
+# NeedsCompilation: yes
+# Packaged: 2016-12-14 22:11:46 UTC; zeileis
+# Author: Ross Ihaka [aut], Paul Murrell [aut], Kurt Hornik [aut], Jason C. Fisher [aut], Reto Stauffer [aut], Achim Zeileis [aut, cre]
+# Maintainer: Achim Zeileis <Achim.Zeileis@R-project.org>
+# Repository: CRAN
+# Date/Publication: 2016-12-14 23:28:25
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-commonmark-feedstock contains modified content
diff --git a/r-commonmark-feedstock/recipe/meta.yaml b/r-commonmark-feedstock/recipe/meta.yaml
index 75e1bde..17a21d6 100644
--- a/r-commonmark-feedstock/recipe/meta.yaml
+++ b/r-commonmark-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '1.2' %}
+{% set version = '1.4' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,35 +12,50 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/commonmark_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/commonmark/commonmark_{{ version }}.tar.gz
-  sha256: 13b23c8b9826daac40cca697ef614a3028d45b2cdee85806eb8dcfdd99ad058b
+  sha256: 865ecf9432763393ca9de52d0106a58c4be9e8d5196da60e068eed0b67ca68ed
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: curl
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
 
   run:
     - r-base
-    - libgcc               # [not win]
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('commonmark')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('commonmark')"           # [not win]
     - "\"%R%\" -e \"library('commonmark')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
-  home: https://github.com/jeroenooms/commonmark http://commonmark.org/help/
-  license: BSD_2_clause
+  home: http://github.com/jeroen/commonmark (devel) https://github.github.com/gfm/ (spec)
+  license: BSD_2_clause + file LICENSE
   summary: The CommonMark specification defines a rationalized version of markdown syntax. This
     package uses the 'cmark' reference implementation for converting markdown text into
     various formats including html, latex and groff man. In addition it exposes the
@@ -52,3 +67,28 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: commonmark
+# Type: Package
+# Title: High Performance CommonMark and Github Markdown Rendering in R
+# Version: 1.4
+# Authors@R: c( person("Jeroen", "Ooms", ,"jeroen@berkeley.edu", role = c("aut", "cre")), person("John MacFarlane", role = "cph", comment = "Author of cmark") )
+# URL: http://github.com/jeroen/commonmark (devel) https://github.github.com/gfm/ (spec)
+# BugReports: http://github.com/jeroen/commonmark/issues
+# Description: The CommonMark specification defines a rationalized version of markdown syntax. This package uses the 'cmark' reference implementation for converting markdown text into various formats including html, latex and groff man. In addition it exposes the markdown parse tree in xml format. The latest version of this package also adds support for Github extensions including tables, autolinks and strikethrough text.
+# License: BSD_2_clause + file LICENSE
+# Suggests: curl
+# RoxygenNote: 6.0.1
+# NeedsCompilation: yes
+# Packaged: 2017-08-31 21:37:58 UTC; jeroen
+# Author: Jeroen Ooms [aut, cre], John MacFarlane [cph] (Author of cmark)
+# Maintainer: Jeroen Ooms <jeroen@berkeley.edu>
+# Repository: CRAN
+# Date/Publication: 2017-09-01 09:47:38 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-config-feedstock contains modified content
diff --git a/r-config-feedstock/recipe/meta.yaml b/r-config-feedstock/recipe/meta.yaml
index ae0525d..918201f 100644
--- a/r-config-feedstock/recipe/meta.yaml
+++ b/r-config-feedstock/recipe/meta.yaml
@@ -14,13 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/config/config_{{ version }}.tar.gz
   sha256: 40cc2969cc655214817edc053c9649b9226f4b24368fccca9d2d338833560fdc
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: testthat, knitr
 requirements:
   build:
     - r-base
@@ -32,9 +40,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('config')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('config')"           # [not win]
     - "\"%R%\" -e \"library('config')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/rstudio/config
   license: GPL-3
@@ -47,3 +62,30 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: config
+# Type: Package
+# Title: Manage Environment Specific Configuration Values
+# Version: 0.2
+# Authors@R: c( person("JJ", "Allaire", role = c("aut", "cre"), email = "jj@rstudio.com"), person(family = "RStudio, Inc.", role = "cph"))
+# Imports: yaml (>= 2.1.13)
+# Suggests: testthat, knitr
+# Description: Manage configuration values across multiple environments (e.g. development, test, production). Read values using a function that determines the current environment and returns the appropriate value.
+# License: GPL-3
+# URL: https://github.com/rstudio/config
+# BugReports: https://github.com/rstudio/config/issues
+# RoxygenNote: 5.0.1
+# VignetteBuilder: knitr
+# NeedsCompilation: no
+# Packaged: 2016-08-02 13:24:09 UTC; jjallaire
+# Author: JJ Allaire [aut, cre], RStudio, Inc. [cph]
+# Maintainer: JJ Allaire <jj@rstudio.com>
+# Repository: CRAN
+# Date/Publication: 2016-08-02 16:59:45
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-copula-feedstock contains modified content
diff --git a/r-copula-feedstock/recipe/meta.yaml b/r-copula-feedstock/recipe/meta.yaml
index b552822..53e8d1d 100644
--- a/r-copula-feedstock/recipe/meta.yaml
+++ b/r-copula-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '0.999-16' %}
+{% set version = '0.999-18' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,18 +12,31 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/copula_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/copula/copula_{{ version }}.tar.gz
-  sha256: 8a2b6c704701cba68fbdc837ca73db1f3d3c4bfbbdb2e4dab4d6af0c65b9d056
+  sha256: 067db37a3123dd8e324c4912866b661244b9deb64494eb7b8442d1f7d1b5b2f5
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: MASS, KernSmooth, sfsmisc, scatterplot3d, Rmpfr, bbmle, knitr, parallel, gridExtra, lcopula, mvnormtest, partitions, polynom, qrng, randtoolbox, rugarch, Runuran, tseries, VGAM, VineCopula, zoo
 requirements:
   build:
     - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('fortran') }}  # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
     - r-adgoftest
     - r-matrix
     - r-colorspace
@@ -34,13 +47,10 @@ requirements:
     - r-pcapp
     - r-pspline
     - r-stabledist >=0.6_4
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
-    - openblas 0.2.19|0.2.19.*
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-adgoftest
     - r-matrix
     - r-colorspace
@@ -51,24 +61,66 @@ requirements:
     - r-pcapp
     - r-pspline
     - r-stabledist >=0.6_4
-    - openblas 0.2.19|0.2.19.*
-    - libgcc  # [not win]
 
 test:
   commands:
-    - $R -e "library('copula')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('copula')"           # [not win]
     - "\"%R%\" -e \"library('copula')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://copula.r-forge.r-project.org/
   license: GPL (>= 3) | file LICENCE
-  summary: Classes (S4) of commonly used elliptical, Archimedean, extreme value and some more
-    copula families.  Methods for density, distribution, random number generation, bivariate
-    dependence measures, perspective and contour plots.  Fitting copula models including
-    variance estimates.
+  summary: Classes (S4) of commonly used elliptical, Archimedean, extreme-value and other copula
+    families, as well as their rotations, mixtures and asymmetrizations. Nested Archimedean
+    copulas, related tools and special functions. Methods for density, distribution,
+    random number generation, bivariate dependence measures, Rosenblatt transform, Kendall
+    distribution function, perspective and contour plots. Fitting of copula models with
+    potentially partly fixed parameters, including standard errors. Serial independence
+    tests, copula specification tests (independence, exchangeability, radial symmetry,
+    extreme-value dependence, goodness-of-fit) and model selection based on cross-validation.
+    Empirical copula, smoothed versions, and non-parametric estimators of the Pickands
+    dependence function.
   license_family: GPL3
 
 extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: copula
+# Version: 0.999-18
+# VersionNote: Last CRAN: 0.999-17 on 2017-06-17
+# Date: 2017-08-31
+# Title: Multivariate Dependence with Copulas
+# Author: Marius Hofert <marius.hofert@uwaterloo.ca>, Ivan Kojadinovic <ivan.kojadinovic@univ-pau.fr>, Martin Maechler <maechler@stat.math.ethz.ch>, and Jun Yan <jun.yan@uconn.edu>
+# Maintainer: Martin Maechler <maechler@stat.math.ethz.ch>
+# Depends: R (>= 3.1.0)
+# Imports: stats, graphics, methods, stats4, Matrix, lattice, colorspace, gsl, ADGofTest, stabledist (>= 0.6-4), mvtnorm, pcaPP, pspline, numDeriv
+# Suggests: MASS, KernSmooth, sfsmisc, scatterplot3d, Rmpfr, bbmle, knitr, parallel, gridExtra, lcopula, mvnormtest, partitions, polynom, qrng, randtoolbox, rugarch, Runuran, tseries, VGAM, VineCopula, zoo
+# SuggestsNote: the last lines' packages {parallel, ..., zoo} are only used in vignettes, demos and few tests.
+# Enhances: nor1mix
+# Description: Classes (S4) of commonly used elliptical, Archimedean, extreme-value and other copula families, as well as their rotations, mixtures and asymmetrizations. Nested Archimedean copulas, related tools and special functions. Methods for density, distribution, random number generation, bivariate dependence measures, Rosenblatt transform, Kendall distribution function, perspective and contour plots. Fitting of copula models with potentially partly fixed parameters, including standard errors. Serial independence tests, copula specification tests (independence, exchangeability, radial symmetry, extreme-value dependence, goodness-of-fit) and model selection based on cross-validation. Empirical copula, smoothed versions, and non-parametric estimators of the Pickands dependence function.
+# License: GPL (>= 3) | file LICENCE
+# ByteCompile: yes
+# VignetteBuilder: knitr
+# Collate: AllClass.R Classes.R AllGeneric.R Auxiliaries.R aux-acopula.R asymCopula.R mixCopula.R rotCopula.R Copula.R special-func.R amhCopula.R claytonCopula.R frankCopula.R cop_objects.R nacopula.R dC-dc.R amhExpr.R An.R archmCopula.R cCopula.R claytonExpr.R ellipCopula.R empcop.R empPsi.R acR.R estimation.R evCopula.R evTests.R exchTests.R fgmCopula.R fitCopula.R fitLambda.R fitMvdc.R fixedPar.R frankExpr.R galambosCopula.R galambosExpr-math.R galambosExpr.R ggraph-tools.R pairsRosenblatt.R prob.R gofTrafos.R gofEVTests.R gofCopula.R graphics.R gumbelCopula.R gumbelExpr.R huslerReissCopula.R huslerReissExpr.R indepCopula.R indepTests.R joeCopula.R K.R logseries.R mvdc.R margCopula.R matrix_tools.R normalCopula.R opower.R plackettCopula.R plackettExpr.R rstable1.R safeUroot.R schlatherCopula.R stable.R timing.R tCopula.R tawnCopula.R tawnExpr.R tevCopula.R varianceReduction.R wrapper.R xvCopula.R zzz.R
+# Encoding: UTF-8
+# URL: http://copula.r-forge.r-project.org/
+# NeedsCompilation: yes
+# Packaged: 2017-08-31 15:24:16 UTC; maechler
+# Repository: CRAN
+# Date/Publication: 2017-09-01 11:33:14 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-crayon-feedstock contains modified content
diff --git a/r-crayon-feedstock/recipe/bld.bat b/r-crayon-feedstock/recipe/bld.bat
new file mode 100644
index 0000000..8b10c94
--- /dev/null
+++ b/r-crayon-feedstock/recipe/bld.bat
@@ -0,0 +1,8 @@
+"%R%" CMD INSTALL --build .
+if errorlevel 1 exit 1
+
+@rem Add more build steps here, if they are necessary.
+
+@rem See
+@rem http://docs.continuum.io/conda/build.html
+@rem for a list of environment variables that are set during the build process.
diff --git a/r-crayon-feedstock/recipe/build.sh b/r-crayon-feedstock/recipe/build.sh
new file mode 100644
index 0000000..0c37df6
--- /dev/null
+++ b/r-crayon-feedstock/recipe/build.sh
@@ -0,0 +1,13 @@
+#!/bin/bash
+
+# R refuses to build packages that mark themselves as Priority: Recommended
+mv DESCRIPTION DESCRIPTION.old
+grep -v '^Priority: ' DESCRIPTION.old > DESCRIPTION
+
+$R CMD INSTALL --build .
+
+# Add more build steps here, if they are necessary.
+
+# See
+# http://docs.continuum.io/conda/build.html
+# for a list of environment variables that are set during the build process.
diff --git a/r-crayon-feedstock/recipe/meta.yaml b/r-crayon-feedstock/recipe/meta.yaml
index cc773a9..28bda45 100644
--- a/r-crayon-feedstock/recipe/meta.yaml
+++ b/r-crayon-feedstock/recipe/meta.yaml
@@ -1,44 +1,90 @@
-{% set version = "1.3.2" %}
+{% set version = '1.3.4' %}
+
+{% set posix = 'm2-' if win else '' %}
+{% set native = 'm2w64-' if win else '' %}
 
 package:
   name: r-crayon
-  version: {{ version }}
+  version: {{ version|replace("-", "_") }}
 
 source:
   fn: crayon_{{ version }}.tar.gz
   url:
-    - http://cran.r-project.org/src/contrib/crayon_{{ version }}.tar.gz
-    - http://cran.r-project.org/src/contrib/Archive/crayon/crayon_{{ version }}.tar.gz
-  sha256: 9a6b75d63c05fe64baf222f1921330ceb727924bcc5fc2753ff0528d42555e68
+    - https://cran.r-project.org/src/contrib/crayon_{{ version }}.tar.gz
+    - https://cran.r-project.org/src/contrib/Archive/crayon/crayon_{{ version }}.tar.gz
+  sha256: fc6e9bf990e9532c4fcf1a3d2ce22d8cf12d25a95e4779adfa17713ed836fa68
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: True  # [win32]
-  script: R CMD INSTALL --build .
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
-requirements:  # [not win32]
-  build:  # [not win32]
-    - r-base  # [not win32]
-  run:  # [not win32]
-    - r-base  # [not win32]
+# Suggests: mockery, rstudioapi, testthat, withr
+requirements:
+  build:
+    - r-base
+
+  run:
+    - r-base
 
 test:
   commands:
-    - R -e "library('crayon')"  # [not win]
-    - R -e \"library('crayon')\"  # [win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('crayon')"           # [not win]
+    - "\"%R%\" -e \"library('crayon')\""  # [win]
+
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
 
 about:
-  home: https://github.com/gaborcsardi/crayon
-  license: MIT
-  license_file: LICENSE
-  summary: "Colored terminal output on terminals that support 'ANSI' color and highlight codes.
+  home: https://github.com/r-lib/crayon#readme
+  license: MIT + file LICENSE
+  summary: Colored terminal output on terminals that support 'ANSI' color and highlight codes.
     It also works in 'Emacs' 'ESS'. 'ANSI' color support is automatically detected.
     Colors and highlighting can be combined and nested. New styles can also be created
-    easily. This package was inspired by the 'chalk' 'JavaScript' project."
+    easily. This package was inspired by the 'chalk' 'JavaScript' project.
+  license_family: MIT
 
 extra:
   recipe-maintainers:
     - ocefpaf
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: crayon
+# Title: Colored Terminal Output
+# Version: 1.3.4
+# Authors@R: c( person("Gabor", "Csardi", , "csardi.gabor@gmail.com", role = c("aut", "cre")), person( "Brodie", "Gaslam", email="brodie.gaslam@yahoo.com", role=c("ctb")) )
+# Description: Colored terminal output on terminals that support 'ANSI' color and highlight codes. It also works in 'Emacs' 'ESS'. 'ANSI' color support is automatically detected. Colors and highlighting can be combined and nested. New styles can also be created easily. This package was inspired by the 'chalk' 'JavaScript' project.
+# License: MIT + file LICENSE
+# LazyData: true
+# URL: https://github.com/r-lib/crayon#readme
+# BugReports: https://github.com/r-lib/crayon/issues
+# Collate: 'ansi-256.r' 'combine.r' 'string.r' 'utils.r' 'crayon-package.r' 'disposable.r' 'has_ansi.r' 'has_color.r' 'styles.r' 'machinery.r' 'parts.r' 'print.r' 'style-var.r' 'show.r' 'string_operations.r'
+# Imports: grDevices, methods, utils
+# Suggests: mockery, rstudioapi, testthat, withr
+# RoxygenNote: 6.0.1.9000
+# Encoding: UTF-8
+# NeedsCompilation: no
+# Packaged: 2017-09-15 18:14:04 UTC; gaborcsardi
+# Author: Gabor Csardi [aut, cre], Brodie Gaslam [ctb]
+# Maintainer: Gabor Csardi <csardi.gabor@gmail.com>
+# Repository: CRAN
+# Date/Publication: 2017-09-16 19:49:46 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-crosstalk-feedstock contains modified content
diff --git a/r-crosstalk-feedstock/recipe/meta.yaml b/r-crosstalk-feedstock/recipe/meta.yaml
index 01d01bb..d80e965 100644
--- a/r-crosstalk-feedstock/recipe/meta.yaml
+++ b/r-crosstalk-feedstock/recipe/meta.yaml
@@ -14,13 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/crosstalk/crosstalk_{{ version }}.tar.gz
   sha256: b31eada24cac26f24c9763d9a8cbe0adfd87b264cf57f8725027fe0c7742ca51
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
@@ -30,6 +38,7 @@ requirements:
     - r-jsonlite
     - r-lazyeval
     - r-shiny >=0.11
+
   run:
     - r-base
     - r-r6
@@ -41,14 +50,20 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('crosstalk')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('crosstalk')"           # [not win]
     - "\"%R%\" -e \"library('crosstalk')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://rstudio.github.io/crosstalk/
-  license: MIT
-  summary: |
-    Provides building blocks for allowing HTML widgets to communicate with each other,
+  license: MIT + file LICENSE
+  summary: Provides building blocks for allowing HTML widgets to communicate with each other,
     with Shiny or without (i.e. static .html files). Currently supports linked brushing
     and filtering.
   license_family: MIT
@@ -57,3 +72,28 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: crosstalk
+# Type: Package
+# Title: Inter-Widget Interactivity for HTML Widgets
+# Version: 1.0.0
+# Authors@R: c( person("Joe", "Cheng", role = c("aut", "cre"), email = "joe@rstudio.com"), person(family = "RStudio", role = "cph"), person(family = "jQuery Foundation", role = "cph", comment = "jQuery library and jQuery UI library"), person(family = "jQuery contributors", role = c("ctb", "cph"), comment = "jQuery library; authors listed in inst/www/shared/jquery-AUTHORS.txt"), person("Mark", "Otto", role = "ctb", comment = "Bootstrap library"), person("Jacob", "Thornton", role = "ctb", comment = "Bootstrap library"), person(family = "Bootstrap contributors", role = "ctb", comment = "Bootstrap library"), person(family = "Twitter, Inc", role = "cph", comment = "Bootstrap library"), person("Brian", "Reavis", role = c("ctb", "cph"), comment = "selectize.js library"), person("Kristopher Michael", "Kowal", role = c("ctb", "cph"), comment = "es5-shim library"), person(family = "es5-shim contributors", role = c("ctb", "cph"), comment = "es5-shim library"), person("Denis", "Ineshin", role = c("ctb", "cph"), comment = "ion.rangeSlider library"), person("Sami", "Samhuri", role = c("ctb", "cph"), comment = "Javascript strftime library") )
+# Description: Provides building blocks for allowing HTML widgets to communicate with each other, with Shiny or without (i.e. static .html files). Currently supports linked brushing and filtering.
+# License: MIT + file LICENSE
+# Imports: htmltools (>= 0.3.5), jsonlite, lazyeval, R6, shiny (>= 0.11), ggplot2
+# URL: https://rstudio.github.io/crosstalk/
+# BugReports: https://github.com/rstudio/crosstalk/issues
+# RoxygenNote: 5.0.1
+# NeedsCompilation: no
+# Packaged: 2016-12-20 20:01:51 UTC; jcheng
+# Author: Joe Cheng [aut, cre], RStudio [cph], jQuery Foundation [cph] (jQuery library and jQuery UI library), jQuery contributors [ctb, cph] (jQuery library; authors listed in inst/www/shared/jquery-AUTHORS.txt), Mark Otto [ctb] (Bootstrap library), Jacob Thornton [ctb] (Bootstrap library), Bootstrap contributors [ctb] (Bootstrap library), Twitter, Inc [cph] (Bootstrap library), Brian Reavis [ctb, cph] (selectize.js library), Kristopher Michael Kowal [ctb, cph] (es5-shim library), es5-shim contributors [ctb, cph] (es5-shim library), Denis Ineshin [ctb, cph] (ion.rangeSlider library), Sami Samhuri [ctb, cph] (Javascript strftime library)
+# Maintainer: Joe Cheng <joe@rstudio.com>
+# Repository: CRAN
+# Date/Publication: 2016-12-21 08:30:32
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-ctv-feedstock contains modified content
diff --git a/r-ctv-feedstock/recipe/meta.yaml b/r-ctv-feedstock/recipe/meta.yaml
index 6f482a6..5c4071f 100644
--- a/r-ctv-feedstock/recipe/meta.yaml
+++ b/r-ctv-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '0.8-2' %}
+{% set version = '0.8-3' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,16 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/ctv_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/ctv/ctv_{{ version }}.tar.gz
-  sha256: 3b2f7708eb1fdfd8124ab1d0499e17b824eb9d427642d166abdb886eececfd76
+  sha256: 1b1629bdca5094a74517b9b939c2cca4ea47fa8d5eca2cc88ef7cb0b417d5075
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: XML
 requirements:
   build:
     - r-base
@@ -31,9 +38,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('ctv')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('ctv')"           # [not win]
     - "\"%R%\" -e \"library('ctv')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=ctv
   license: GPL-2 | GPL-3
@@ -47,3 +61,26 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: ctv
+# Version: 0.8-3
+# Date: 2017-10-07
+# Title: CRAN Task Views
+# Authors@R: c(person(given = "Achim", family = "Zeileis", role = c("aut", "cre"), email = "Achim.Zeileis@R-project.org", comment = c(ORCID = "0000-0003-0918-3766")), person(given = "Kurt", family = "Hornik", role = "aut", email = "Kurt.Hornik@R-project.org"))
+# Description: Infrastructure for task views to CRAN-style repositories: Querying task views and installing the associated packages (client-side tools), generating HTML pages and storing task view information in the repository (server-side tools).
+# Depends: R (>= 2.13.0), utils
+# Suggests: XML
+# License: GPL-2 | GPL-3
+# NeedsCompilation: no
+# Packaged: 2017-10-07 08:36:23 UTC; zeileis
+# Author: Achim Zeileis [aut, cre] (0000-0003-0918-3766), Kurt Hornik [aut]
+# Maintainer: Achim Zeileis <Achim.Zeileis@R-project.org>
+# Repository: CRAN
+# Date/Publication: 2017-10-07 08:56:09 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-curl-feedstock contains modified content
diff --git a/r-curl-feedstock/recipe/meta.yaml b/r-curl-feedstock/recipe/meta.yaml
index f3691b1..5c2f9f5 100644
--- a/r-curl-feedstock/recipe/meta.yaml
+++ b/r-curl-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '2.4' %}
+{% set version = '3.0' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,35 +12,53 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/curl_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/curl/curl_{{ version }}.tar.gz
-  sha256: f5121a8cc9634acdcc6018919d84adafe6142cc03f07a3d9dd90d89b23113148
+  sha256: 7bf8e3ae7cc77802ae300277e85d925d4c0611a9b7dad5c5601e0d2cbe14a506
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: testthat (>= 1.0.0), knitr, jsonlite, rmarkdown, magrittr, httpuv, webutils
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}autoconf
+    - {{posix}}automake
+    - {{posix}}pkg-config
+    - {{posix}}make
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('curl')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('curl')"           # [not win]
     - "\"%R%\" -e \"library('curl')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
-  home: https://github.com/jeroen/curl#readme
-  license: MIT
-  license_file: LICENSE
+  home: https://github.com/jeroen/curl#readme (devel) https://curl.haxx.se/libcurl/ (upstream)
+  license: MIT + file LICENSE
   summary: The curl() and curl_download() functions provide highly configurable drop-in replacements
     for base url() and download.file() with better performance, support for encryption
     (https, ftps), gzip compression, authentication, and other 'libcurl' goodies. The
@@ -55,3 +73,32 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: curl
+# Type: Package
+# Title: A Modern and Flexible Web Client for R
+# Version: 3.0
+# Authors@R: c( person("Jeroen", "Ooms", , "jeroen@berkeley.edu", role = c("cre", "aut")), person("Hadley", "Wickham", , "hadley@rstudio.com", role = "ctb"), person("RStudio", role = "cph") )
+# Description: The curl() and curl_download() functions provide highly configurable drop-in replacements for base url() and download.file() with better performance, support for encryption (https, ftps), gzip compression, authentication, and other 'libcurl' goodies. The core of the package implements a framework for performing fully customized requests where data can be processed either in memory, on disk, or streaming via the callback or connection interfaces. Some knowledge of 'libcurl' is recommended; for a more-user-friendly web client see the 'httr' package which builds on this package with http specific tools and logic.
+# License: MIT + file LICENSE
+# SystemRequirements: libcurl: libcurl-devel (rpm) or libcurl4-openssl-dev (deb).
+# URL: https://github.com/jeroen/curl#readme (devel) https://curl.haxx.se/libcurl/ (upstream)
+# BugReports: https://github.com/jeroen/curl/issues
+# Suggests: testthat (>= 1.0.0), knitr, jsonlite, rmarkdown, magrittr, httpuv, webutils
+# VignetteBuilder: knitr
+# Depends: R (>= 3.0.0)
+# LazyData: true
+# RoxygenNote: 6.0.1
+# NeedsCompilation: yes
+# Packaged: 2017-10-05 11:21:57 UTC; jeroen
+# Author: Jeroen Ooms [cre, aut], Hadley Wickham [ctb], RStudio [cph]
+# Maintainer: Jeroen Ooms <jeroen@berkeley.edu>
+# Repository: CRAN
+# Date/Publication: 2017-10-06 10:18:36 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
diff --git a/r-cvst-feedstock/recipe/bld.bat b/r-cvst-feedstock/recipe/bld.bat
index e69de29..8b10c94 100644
--- a/r-cvst-feedstock/recipe/bld.bat
+++ b/r-cvst-feedstock/recipe/bld.bat
@@ -0,0 +1,8 @@
+"%R%" CMD INSTALL --build .
+if errorlevel 1 exit 1
+
+@rem Add more build steps here, if they are necessary.
+
+@rem See
+@rem http://docs.continuum.io/conda/build.html
+@rem for a list of environment variables that are set during the build process.
diff --git a/r-cvst-feedstock/recipe/build.sh b/r-cvst-feedstock/recipe/build.sh
index e69de29..0c37df6 100644
--- a/r-cvst-feedstock/recipe/build.sh
+++ b/r-cvst-feedstock/recipe/build.sh
@@ -0,0 +1,13 @@
+#!/bin/bash
+
+# R refuses to build packages that mark themselves as Priority: Recommended
+mv DESCRIPTION DESCRIPTION.old
+grep -v '^Priority: ' DESCRIPTION.old > DESCRIPTION
+
+$R CMD INSTALL --build .
+
+# Add more build steps here, if they are necessary.
+
+# See
+# http://docs.continuum.io/conda/build.html
+# for a list of environment variables that are set during the build process.
diff --git a/r-cvst-feedstock/recipe/meta.yaml b/r-cvst-feedstock/recipe/meta.yaml
index e69de29..a03b030 100644
--- a/r-cvst-feedstock/recipe/meta.yaml
+++ b/r-cvst-feedstock/recipe/meta.yaml
@@ -0,0 +1,94 @@
+{% set version = '0.2-1' %}
+
+{% set posix = 'm2-' if win else '' %}
+{% set native = 'm2w64-' if win else '' %}
+
+package:
+  name: r-cvst
+  version: {{ version|replace("-", "_") }}
+
+source:
+  fn: CVST_{{ version }}.tar.gz
+  url:
+    - https://cran.r-project.org/src/contrib/CVST_{{ version }}.tar.gz
+    - https://cran.r-project.org/src/contrib/Archive/CVST/CVST_{{ version }}.tar.gz
+  sha256: a27fd2bfa778fce9b9a68d2b9206c66af27b3c36a973dd45ce673886a267aa9f
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
+build:
+  # If this is a new build for the same version, increment the build number.
+  number: 0
+
+  # This is required to make R link correctly on Linux.
+  rpaths:
+    - lib/R/lib/
+    - lib/
+
+
+requirements:
+  build:
+    - r-base
+    - r-matrix
+    - r-kernlab
+
+  run:
+    - r-base
+    - r-matrix
+    - r-kernlab
+
+test:
+  commands:
+    # You can put additional test commands to be run here.
+    - $R -e "library('CVST')"           # [not win]
+    - "\"%R%\" -e \"library('CVST')\""  # [win]
+
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
+about:
+  home: https://CRAN.R-project.org/package=CVST
+  license: GPL (>= 2.0)
+  summary: This package implements the fast cross-validation via sequential testing (CVST) procedure.
+    CVST is an improved cross-validation procedure which uses non-parametric testing
+    coupled with sequential analysis to determine the best parameter set on linearly
+    increasing subsets of the data. By eliminating underperforming candidates quickly
+    and keeping promising candidates as long as possible, the method speeds up the computation
+    while preserving the capability of a full cross-validation. Additionally to the
+    CVST the package contains an implementation of the ordinary k-fold cross-validation
+    with a flexible and powerful set of helper objects and methods to handle the overall
+    model selection process. The implementations of the Cochran's Q test with permutations
+    and the sequential testing framework of Wald are generic and can therefore also
+    be used in other contexts.
+  license_family: GPL3
+
+extra:
+  recipe-maintainers:
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: CVST
+# Type: Package
+# Title: Fast Cross-Validation via Sequential Testing
+# Version: 0.2-1
+# Date: 2013-12-10
+# Depends: kernlab,Matrix
+# Author: Tammo Krueger, Mikio Braun
+# Maintainer: Tammo Krueger <tammokrueger@googlemail.com>
+# Description: This package implements the fast cross-validation via sequential testing (CVST) procedure. CVST is an improved cross-validation procedure which uses non-parametric testing coupled with sequential analysis to determine the best parameter set on linearly increasing subsets of the data. By eliminating underperforming candidates quickly and keeping promising candidates as long as possible, the method speeds up the computation while preserving the capability of a full cross-validation. Additionally to the CVST the package contains an implementation of the ordinary k-fold cross-validation with a flexible and powerful set of helper objects and methods to handle the overall model selection process. The implementations of the Cochran's Q test with permutations and the sequential testing framework of Wald are generic and can therefore also be used in other contexts.
+# License: GPL (>= 2.0)
+# Packaged: 2013-12-10 13:06:36 UTC; tammok
+# NeedsCompilation: no
+# Repository: CRAN
+# Date/Publication: 2013-12-10 14:50:04
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-cvtools-feedstock contains modified content
diff --git a/r-cvtools-feedstock/recipe/meta.yaml b/r-cvtools-feedstock/recipe/meta.yaml
index cc699a0..293c7f7 100644
--- a/r-cvtools-feedstock/recipe/meta.yaml
+++ b/r-cvtools-feedstock/recipe/meta.yaml
@@ -14,13 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/cvTools/cvTools_{{ version }}.tar.gz
   sha256: 520f0b194444d918440e971b16ad4e8579f953dd6d39ffc5f87b61589b59fd2c
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
+
+
 requirements:
   build:
     - r-base
@@ -34,9 +42,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('cvTools')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('cvTools')"           # [not win]
     - "\"%R%\" -e \"library('cvTools')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=cvTools
   license: GPL (>= 2)
@@ -48,3 +63,28 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: cvTools
+# Type: Package
+# Title: Cross-validation tools for regression models
+# Version: 0.3.2
+# Date: 2012-05-11
+# Author: Andreas Alfons
+# Maintainer: Andreas Alfons <andreas.alfons@econ.kuleuven.be>
+# Depends: R (>= 2.11.0), lattice, robustbase
+# Imports: lattice, robustbase, stats
+# Description: Tools that allow developers to write functions for cross-validation with minimal programming effort and assist users with model selection.
+# License: GPL (>= 2)
+# LazyLoad: yes
+# Collate: 'accessors.R' 'aggregate.R' 'bwplot.R' 'cost.R' 'cvFit.R' 'cvFolds.R' 'cvReshape.R' 'cvSelect.R' 'cvTool.R' 'cvTuning.R' 'densityplot.R' 'dotplot.R' 'plot.R' 'print.R' 'repCV.R' 'selectBest.R' 'subset.R' 'summary.R' 'utils.R' 'xyplot.R'
+# Packaged: 2012-05-11 16:02:12 UTC; andi
+# Repository: CRAN
+# Date/Publication: 2012-05-14 15:13:43
+# NeedsCompilation: no
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-data.table-feedstock contains modified content
diff --git a/r-data.table-feedstock/recipe/meta.yaml b/r-data.table-feedstock/recipe/meta.yaml
index da8c0af..926b135 100644
--- a/r-data.table-feedstock/recipe/meta.yaml
+++ b/r-data.table-feedstock/recipe/meta.yaml
@@ -14,33 +14,48 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/data.table/data.table_{{ version }}.tar.gz
   sha256: 865fdf6aad389071ad063ec1c75a78ffc86eeb88bba011f3ea5281d243966b7a
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: bit64, knitr, nanotime, chron, ggplot2 (>= 0.9.0), plyr, reshape, reshape2, testthat (>= 0.4), hexbin, fastmatch, nlme, xts, gdata, GenomicRanges, caret, curl, zoo, plm, rmarkdown, parallel
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('data.table')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('data.table')"           # [not win]
     - "\"%R%\" -e \"library('data.table')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://r-datatable.com
-  license: GPL-3
-  license_file: LICENSE
+  license: GPL-3 | file LICENSE
   summary: Fast aggregation of large data (e.g. 100GB in RAM), fast ordered joins, fast add/modify/delete
     of columns by group using no copies at all, list columns, a fast friendly file reader
     and parallel file writer. Offers a natural and flexible syntax, for faster development.
@@ -50,3 +65,31 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: data.table
+# Version: 1.10.4
+# Title: Extension of `data.frame`
+# Authors@R: c( person("Matt","Dowle",      role=c("aut","cre"), email="mattjdowle@gmail.com"), person("Arun","Srinivasan", role="aut", email="arunkumar.sriniv@gmail.com"), person("Jan","Gorecki",     role="ctb"), person("Tom","Short",       role="ctb"), person("Steve","Lianoglou", role="ctb"), person("Eduard","Antonyan", role="ctb") )
+# Depends: R (>= 3.0.0)
+# Imports: methods
+# Suggests: bit64, knitr, nanotime, chron, ggplot2 (>= 0.9.0), plyr, reshape, reshape2, testthat (>= 0.4), hexbin, fastmatch, nlme, xts, gdata, GenomicRanges, caret, curl, zoo, plm, rmarkdown, parallel
+# Description: Fast aggregation of large data (e.g. 100GB in RAM), fast ordered joins, fast add/modify/delete of columns by group using no copies at all, list columns, a fast friendly file reader and parallel file writer. Offers a natural and flexible syntax, for faster development.
+# License: GPL-3 | file LICENSE
+# URL: http://r-datatable.com
+# BugReports: https://github.com/Rdatatable/data.table/issues
+# MailingList: datatable-help@lists.r-forge.r-project.org
+# VignetteBuilder: knitr
+# ByteCompile: TRUE
+# NeedsCompilation: yes
+# Packaged: 2017-02-01 00:15:31.074 UTC; mdowle
+# Author: Matt Dowle [aut, cre], Arun Srinivasan [aut], Jan Gorecki [ctb], Tom Short [ctb], Steve Lianoglou [ctb], Eduard Antonyan [ctb]
+# Maintainer: Matt Dowle <mattjdowle@gmail.com>
+# Repository: CRAN
+# Date/Publication: 2017-02-01 15:52:19
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-dbi-feedstock contains modified content
diff --git a/r-dbi-feedstock/recipe/meta.yaml b/r-dbi-feedstock/recipe/meta.yaml
index e6eab24..174266f 100644
--- a/r-dbi-feedstock/recipe/meta.yaml
+++ b/r-dbi-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '0.6-1' %}
+{% set version = '0.7' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,16 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/DBI_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/DBI/DBI_{{ version }}.tar.gz
-  sha256: f94d878ce61f8dc073f95dfc83c3aeebf2c92db990679bfacf831a4b262ae1b9
+  sha256: 2557d5d59a45620ec9de340c2c25eec4cc478d3fc3f8b87979cf337c5bcfde11
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: blob, covr, hms, knitr, magrittr, rprojroot, rmarkdown, RSQLite (>= 1.1-2), testthat, xml2
 requirements:
   build:
     - r-base
@@ -31,9 +38,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('DBI')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('DBI')"           # [not win]
     - "\"%R%\" -e \"library('DBI')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://rstats-db.github.io/DBI
   license: LGPL (>= 2)
@@ -46,3 +60,33 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: DBI
+# Version: 0.7
+# Date: 2017-06-17
+# Title: R Database Interface
+# Description: A database interface definition for communication between R and relational database management systems.  All classes in this package are virtual and need to be extended by the various R/DBMS implementations.
+# Authors@R: c( person("R Special Interest Group on Databases (R-SIG-DB)", role = "aut"), person("Hadley", "Wickham", role = "aut"), person("Kirill", "Muller", email = "krlmlr+r@mailbox.org", role = c("aut", "cre")) )
+# Depends: R (>= 3.0.0), methods
+# Suggests: blob, covr, hms, knitr, magrittr, rprojroot, rmarkdown, RSQLite (>= 1.1-2), testthat, xml2
+# Encoding: UTF-8
+# License: LGPL (>= 2)
+# URL: http://rstats-db.github.io/DBI
+# URLNote: https://github.com/rstats-db/DBI
+# BugReports: https://github.com/rstats-db/DBI/issues
+# Collate: 'DBObject.R' 'DBDriver.R' 'DBConnection.R' 'ANSI.R' 'DBI-package.R' 'DBResult.R' 'data-types.R' 'data.R' 'deprecated.R' 'hidden.R' 'interpolate.R' 'quote.R' 'rownames.R' 'table-create.R' 'table-insert.R' 'table.R' 'transactions.R' 'util.R'
+# VignetteBuilder: knitr
+# RoxygenNote: 6.0.1
+# NeedsCompilation: no
+# Packaged: 2017-06-18 08:02:52 UTC; muelleki
+# Author: R Special Interest Group on Databases (R-SIG-DB) [aut], Hadley Wickham [aut], Kirill Muller [aut, cre]
+# Maintainer: Kirill Muller <krlmlr+r@mailbox.org>
+# Repository: CRAN
+# Date/Publication: 2017-06-18 15:23:10 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-dbplyr-feedstock contains modified content
diff --git a/r-dbplyr-feedstock/recipe/meta.yaml b/r-dbplyr-feedstock/recipe/meta.yaml
index 0bd2dbb..bf3b791 100644
--- a/r-dbplyr-feedstock/recipe/meta.yaml
+++ b/r-dbplyr-feedstock/recipe/meta.yaml
@@ -14,14 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/dbplyr/dbplyr_{{ version }}.tar.gz
   sha256: 7b1e456a2d1056fa6284582cd82d2df66d06b3eea92e9995f5a91a45f246f69d
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: covr, knitr, Lahman (>= 3.0-1), nycflights13, rmarkdown, RSQLite (>= 1.0.0), RMySQL, RPostgreSQL, testthat
 requirements:
   build:
     - r-base
@@ -47,12 +54,19 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('dbplyr')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('dbplyr')"           # [not win]
     - "\"%R%\" -e \"library('dbplyr')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/tidyverse/dbplyr
-  license: MIT
+  license: MIT + file LICENSE
   summary: A 'dplyr' back end for databases that allows you to work with  remote database tables
     as if they are in-memory data frames. Basic features works with any database that
     has a 'DBI' back end; more advanced features  require 'SQL' translation to be provided
@@ -65,3 +79,33 @@ extra:
     - bgruening
     - daler
     - jdblischak
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: dbplyr
+# Type: Package
+# Version: 1.1.0
+# Title: A 'dplyr' Back End for Databases
+# Description: A 'dplyr' back end for databases that allows you to work with  remote database tables as if they are in-memory data frames. Basic features works with any database that has a 'DBI' back end; more advanced features  require 'SQL' translation to be provided by the package author.
+# Authors@R: c( person("Hadley", "Wickham", , "hadley@rstudio.com", c("aut", "cre")), person("RStudio", role = c("cph", "fnd")) )
+# URL: https://github.com/tidyverse/dbplyr
+# BugReports: https://github.com/tidyverse/dplyr/issues
+# Depends: R (>= 3.1.2)
+# Imports: assertthat, DBI (>= 0.5), dplyr (>= 0.5.0.9004), glue, methods, purrr, rlang (>= 0.1.0), tibble (>= 1.3.0.9007), R6, utils
+# Suggests: covr, knitr, Lahman (>= 3.0-1), nycflights13, rmarkdown, RSQLite (>= 1.0.0), RMySQL, RPostgreSQL, testthat
+# VignetteBuilder: knitr
+# LazyData: yes
+# License: MIT + file LICENSE
+# Collate: 'cache.r' 'compat-purrr.R' 'data-lahman.r' 'data-nycflights13.r' 'db-compute.R' 'db-mysql.r' 'db-odbc-hive.R' 'db-odbc-impala.R' 'db-odbc-mssql.R' 'db-odbc-oracle.R' 'db-odbc-postgres.R' 'db-postgres.r' 'db-sqlite.r' 'dbi-s3.r' 'dbplyr.R' 'do.r' 'explain.r' 'ident.R' 'lazy-ops.R' 'memdb.R' 'partial-eval.r' 'query.r' 'schema.R' 'simulate.r' 'sql-build.R' 'sql-escape.r' 'sql-generic.R' 'sql-optimise.R' 'sql-query.R' 'sql-render.R' 'sql.R' 'src-sql.r' 'src_dbi.R' 'tbl-lazy.R' 'tbl-sql.r' 'test-frame.R' 'testthat.r' 'translate-sql-helpers.r' 'translate-sql-window.r' 'translate-sql-base.r' 'translate-sql-clause.r' 'translate-sql-odbc.R' 'translate-sql.r' 'utils-format.r' 'utils.r' 'window.R' 'zzz.R'
+# RoxygenNote: 6.0.1
+# NeedsCompilation: no
+# Packaged: 2017-06-26 19:19:44 UTC; hadley
+# Author: Hadley Wickham [aut, cre], RStudio [cph, fnd]
+# Maintainer: Hadley Wickham <hadley@rstudio.com>
+# Repository: CRAN
+# Date/Publication: 2017-06-27 06:55:24 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
diff --git a/r-ddalpha-feedstock/recipe/bld.bat b/r-ddalpha-feedstock/recipe/bld.bat
index e69de29..8b10c94 100644
--- a/r-ddalpha-feedstock/recipe/bld.bat
+++ b/r-ddalpha-feedstock/recipe/bld.bat
@@ -0,0 +1,8 @@
+"%R%" CMD INSTALL --build .
+if errorlevel 1 exit 1
+
+@rem Add more build steps here, if they are necessary.
+
+@rem See
+@rem http://docs.continuum.io/conda/build.html
+@rem for a list of environment variables that are set during the build process.
diff --git a/r-ddalpha-feedstock/recipe/build.sh b/r-ddalpha-feedstock/recipe/build.sh
index e69de29..0c37df6 100644
--- a/r-ddalpha-feedstock/recipe/build.sh
+++ b/r-ddalpha-feedstock/recipe/build.sh
@@ -0,0 +1,13 @@
+#!/bin/bash
+
+# R refuses to build packages that mark themselves as Priority: Recommended
+mv DESCRIPTION DESCRIPTION.old
+grep -v '^Priority: ' DESCRIPTION.old > DESCRIPTION
+
+$R CMD INSTALL --build .
+
+# Add more build steps here, if they are necessary.
+
+# See
+# http://docs.continuum.io/conda/build.html
+# for a list of environment variables that are set during the build process.
diff --git a/r-ddalpha-feedstock/recipe/meta.yaml b/r-ddalpha-feedstock/recipe/meta.yaml
index e69de29..0770e20 100644
--- a/r-ddalpha-feedstock/recipe/meta.yaml
+++ b/r-ddalpha-feedstock/recipe/meta.yaml
@@ -0,0 +1,106 @@
+{% set version = '1.3.1' %}
+
+{% set posix = 'm2-' if win else '' %}
+{% set native = 'm2w64-' if win else '' %}
+
+package:
+  name: r-ddalpha
+  version: {{ version|replace("-", "_") }}
+
+source:
+  fn: ddalpha_{{ version }}.tar.gz
+  url:
+    - https://cran.r-project.org/src/contrib/ddalpha_{{ version }}.tar.gz
+    - https://cran.r-project.org/src/contrib/Archive/ddalpha/ddalpha_{{ version }}.tar.gz
+  sha256: 707d72464c37726f4a067417d6a8927a232b59cf67fa77aa48eef8414ae29f5d
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
+build:
+  # If this is a new build for the same version, increment the build number.
+  number: 0
+
+  # This is required to make R link correctly on Linux.
+  rpaths:
+    - lib/R/lib/
+    - lib/
+
+
+requirements:
+  build:
+    - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('cxx') }}      # [not win]
+    - {{ compiler('fortran') }}  # [not win]
+    - {{native}}toolchain        # [win]
+    - r-bh
+    - r-mass
+    - r-rcpp >=0.11.0
+    - r-class
+    - r-robustbase
+    - r-sfsmisc
+
+  run:
+    - r-base
+    - {{native}}gcc-libs         # [win]
+    - r-bh
+    - r-mass
+    - r-rcpp >=0.11.0
+    - r-class
+    - r-robustbase
+    - r-sfsmisc
+
+test:
+  commands:
+    # You can put additional test commands to be run here.
+    - $R -e "library('ddalpha')"           # [not win]
+    - "\"%R%\" -e \"library('ddalpha')\""  # [win]
+
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
+about:
+  home: https://CRAN.R-project.org/package=ddalpha
+  license: GPL-2
+  summary: Contains procedures for depth-based supervised learning, which are entirely non-parametric,
+    in particular the DDalpha-procedure (Lange, Mosler and Mozharovskyi, 2014). The
+    training data sample is transformed by a statistical depth function to a compact
+    low-dimensional space, where the final classification is done. It also offers an
+    extension to functional data and routines for calculating certain notions of statistical
+    depth functions. 50 multivariate and 5 functional classification problems are included.
+  license_family: GPL2
+
+extra:
+  recipe-maintainers:
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: ddalpha
+# Type: Package
+# Title: Depth-Based Classification and Calculation of Data Depth
+# Version: 1.3.1
+# Date: 2017-09-27
+# Authors@R: c(person("Oleksii", "Pokotylo", role=c("aut", "cre"), email = "alexey.pokotylo@gmail.com"), person("Pavlo", "Mozharovskyi", role=c("aut"), email = "pavlo.mozharovskyi@ensai.fr"), person("Rainer", "Dyckerhoff", role=c("aut"), email = "rainer.dyckerhoff@statistik.uni-koeln.de"), person("Stanislav", "Nagy", role=c("aut"), email = "nagy@karlin.mff.cuni.cz"))
+# SystemRequirements: C++11
+# Depends: stats, utils, graphics, grDevices, MASS, class, robustbase, sfsmisc
+# Imports: Rcpp (>= 0.11.0)
+# LinkingTo: BH, Rcpp
+# Description: Contains procedures for depth-based supervised learning, which are entirely non-parametric, in particular the DDalpha-procedure (Lange, Mosler and Mozharovskyi, 2014). The training data sample is transformed by a statistical depth function to a compact low-dimensional space, where the final classification is done. It also offers an extension to functional data and routines for calculating certain notions of statistical depth functions. 50 multivariate and 5 functional classification problems are included.
+# License: GPL-2
+# NeedsCompilation: yes
+# Packaged: 2017-09-27 18:51:34 UTC; User
+# Author: Oleksii Pokotylo [aut, cre], Pavlo Mozharovskyi [aut], Rainer Dyckerhoff [aut], Stanislav Nagy [aut]
+# Maintainer: Oleksii Pokotylo <alexey.pokotylo@gmail.com>
+# Repository: CRAN
+# Date/Publication: 2017-09-27 20:30:35 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
diff --git a/r-debugme-feedstock/recipe/bld.bat b/r-debugme-feedstock/recipe/bld.bat
index e69de29..8b10c94 100644
--- a/r-debugme-feedstock/recipe/bld.bat
+++ b/r-debugme-feedstock/recipe/bld.bat
@@ -0,0 +1,8 @@
+"%R%" CMD INSTALL --build .
+if errorlevel 1 exit 1
+
+@rem Add more build steps here, if they are necessary.
+
+@rem See
+@rem http://docs.continuum.io/conda/build.html
+@rem for a list of environment variables that are set during the build process.
diff --git a/r-debugme-feedstock/recipe/build.sh b/r-debugme-feedstock/recipe/build.sh
index e69de29..0c37df6 100644
--- a/r-debugme-feedstock/recipe/build.sh
+++ b/r-debugme-feedstock/recipe/build.sh
@@ -0,0 +1,13 @@
+#!/bin/bash
+
+# R refuses to build packages that mark themselves as Priority: Recommended
+mv DESCRIPTION DESCRIPTION.old
+grep -v '^Priority: ' DESCRIPTION.old > DESCRIPTION
+
+$R CMD INSTALL --build .
+
+# Add more build steps here, if they are necessary.
+
+# See
+# http://docs.continuum.io/conda/build.html
+# for a list of environment variables that are set during the build process.
diff --git a/r-debugme-feedstock/recipe/meta.yaml b/r-debugme-feedstock/recipe/meta.yaml
index e69de29..2f5baf6 100644
--- a/r-debugme-feedstock/recipe/meta.yaml
+++ b/r-debugme-feedstock/recipe/meta.yaml
@@ -0,0 +1,87 @@
+{% set version = '1.0.2' %}
+
+{% set posix = 'm2-' if win else '' %}
+{% set native = 'm2w64-' if win else '' %}
+
+package:
+  name: r-debugme
+  version: {{ version|replace("-", "_") }}
+
+source:
+  fn: debugme_{{ version }}.tar.gz
+  url:
+    - https://cran.r-project.org/src/contrib/debugme_{{ version }}.tar.gz
+    - https://cran.r-project.org/src/contrib/Archive/debugme/debugme_{{ version }}.tar.gz
+  sha256: 1f8d7887aa67c427a9bc840ee50e9470ce4bbf37925db161ad68ffa24c54d82b
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
+build:
+  # If this is a new build for the same version, increment the build number.
+  number: 0
+
+  # This is required to make R link correctly on Linux.
+  rpaths:
+    - lib/R/lib/
+    - lib/
+
+# Suggests: covr, mockery, testthat, withr
+requirements:
+  build:
+    - r-base
+    - r-crayon
+
+  run:
+    - r-base
+    - r-crayon
+
+test:
+  commands:
+    # You can put additional test commands to be run here.
+    - $R -e "library('debugme')"           # [not win]
+    - "\"%R%\" -e \"library('debugme')\""  # [win]
+
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
+about:
+  home: https://github.com/gaborcsardi/debugme#readme
+  license: MIT + file LICENSE
+  summary: Specify debug messages as special string constants, and control debugging of packages
+    via environment variables.
+  license_family: MIT
+
+extra:
+  recipe-maintainers:
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: debugme
+# Title: Debug R Packages
+# Version: 1.0.2
+# Author: Gabor Csardi
+# Maintainer: Gabor Csardi <csardi.gabor@gmail.com>
+# Description: Specify debug messages as special string constants, and control debugging of packages via environment variables.
+# License: MIT + file LICENSE
+# LazyData: true
+# URL: https://github.com/gaborcsardi/debugme#readme
+# BugReports: https://github.com/gaborcsardi/debugme/issues
+# RoxygenNote: 5.0.1.9000
+# Imports: crayon, grDevices
+# Suggests: covr, mockery, testthat, withr
+# Encoding: UTF-8
+# NeedsCompilation: no
+# Packaged: 2017-03-01 14:12:04 UTC; gaborcsardi
+# Repository: CRAN
+# Date/Publication: 2017-03-01 19:05:46
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-deoptimr-feedstock contains modified content
diff --git a/r-deoptimr-feedstock/recipe/meta.yaml b/r-deoptimr-feedstock/recipe/meta.yaml
index 25cb0eb..23bee5e 100644
--- a/r-deoptimr-feedstock/recipe/meta.yaml
+++ b/r-deoptimr-feedstock/recipe/meta.yaml
@@ -14,14 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/DEoptimR/DEoptimR_{{ version }}.tar.gz
   sha256: 846911c1b2561a9fae73a8c60a21a5680963ebb0050af3c1f1147ae9a121e5ef
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
@@ -31,9 +38,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('DEoptimR')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('DEoptimR')"           # [not win]
     - "\"%R%\" -e \"library('DEoptimR')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=DEoptimR
   license: GPL (>= 2)
@@ -48,3 +62,30 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: DEoptimR
+# Version: 1.0-8
+# Date: 2016-11-19
+# Title: Differential Evolution Optimization in Pure R
+# Authors@R: c(person("Eduardo L. T.", "Conceicao", role = c("aut", "cre"), email = "mail@eduardoconceicao.org"), person("Martin", "Maechler", role = "ctb", email = "maechler@stat.math.ethz.ch") )
+# Maintainer: Eduardo L. T. Conceicao <mail@eduardoconceicao.org>
+# Description: Differential Evolution (DE) stochastic algorithms for global optimization of problems with and without constraints. The aim is to curate a collection of its state-of-the-art variants that (1) do not sacrifice simplicity of design, (2) are essentially tuning-free, and (3) can be efficiently implemented directly in the R language. Currently, it only provides an implementation of the 'jDE' algorithm by Brest et al. (2006) <doi:10.1109/TEVC.2006.872133>.
+# Imports: stats
+# Enhances: robustbase
+# License: GPL (>= 2)
+# ByteCompile: yes
+# Author: Eduardo L. T. Conceicao [aut, cre], Martin Maechler [ctb]
+# Repository: CRAN
+# Repository/R-Forge/Project: robustbase
+# Repository/R-Forge/Revision: 746
+# Repository/R-Forge/DateTimeStamp: 2016-11-19 14:54:56
+# Date/Publication: 2016-11-19 18:16:34
+# NeedsCompilation: no
+# Packaged: 2016-11-19 15:06:00 UTC; rforge
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-desc-feedstock contains modified content
diff --git a/r-desc-feedstock/recipe/meta.yaml b/r-desc-feedstock/recipe/meta.yaml
index 7a2ae3b..f1c766b 100644
--- a/r-desc-feedstock/recipe/meta.yaml
+++ b/r-desc-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '1.1.0' %}
+{% set version = '1.1.1' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,16 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/desc_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/desc/desc_{{ version }}.tar.gz
-  sha256: f94ac3c0a03504ec845f945e77f3b0ad5ef9abf2cefcfe9fd1477acc63958155
+  sha256: 32ff99b658db3bf0172cf30d15b7f34021144ffc08a5896afd3d30055fc4074c
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: covr, testthat, whoami, withr
 requirements:
   build:
     - r-base
@@ -39,12 +46,19 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('desc')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('desc')"           # [not win]
     - "\"%R%\" -e \"library('desc')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
-  home: https://github.com/r-pkgs/desc
-  license: MIT
+  home: https://github.com/r-lib/desc#readme
+  license: MIT + file LICENSE
   summary: Tools to read, write, create, and manipulate DESCRIPTION files. It is intended for
     packages that create or manipulate other packages.
   license_family: MIT
@@ -53,3 +67,32 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: desc
+# Title: Manipulate DESCRIPTION Files
+# Version: 1.1.1
+# Authors@R: c( person("Gabor", "Csardi",, "csardi.gabor@gmail.com", role = c("aut", "cre")), person("Kirill", "Muller", role = c("aut")))
+# Maintainer: Gabor Csardi <csardi.gabor@gmail.com>
+# Description: Tools to read, write, create, and manipulate DESCRIPTION files. It is intended for packages that create or manipulate other packages.
+# License: MIT + file LICENSE
+# LazyData: true
+# URL: https://github.com/r-lib/desc#readme
+# BugReports: https://github.com/r-lib/desc/issues
+# Depends: R (>= 3.1.0)
+# Suggests: covr, testthat, whoami, withr
+# Imports: assertthat, utils, R6, crayon, rprojroot
+# Encoding: UTF-8
+# RoxygenNote: 5.0.1.9000
+# Collate: 'assertions.R' 'authors-at-r.R' 'classes.R' 'collate.R' 'constants.R' 'deps.R' 'description.R' 'encoding.R' 'latex.R' 'non-oo-api.R' 'package-archives.R' 'read.R' 'remotes.R' 'str.R' 'syntax_checks.R' 'urls.R' 'utils.R' 'validate.R' 'version.R'
+# NeedsCompilation: no
+# Packaged: 2017-08-03 09:45:28 UTC; gaborcsardi
+# Author: Gabor Csardi [aut, cre], Kirill Muller [aut]
+# Repository: CRAN
+# Date/Publication: 2017-08-03 15:22:33 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-devtools-feedstock contains modified content
diff --git a/r-devtools-feedstock/recipe/meta.yaml b/r-devtools-feedstock/recipe/meta.yaml
index ebd413c..a47aad0 100644
--- a/r-devtools-feedstock/recipe/meta.yaml
+++ b/r-devtools-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '1.13.2' %}
+{% set version = '1.13.3' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,16 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/devtools_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/devtools/devtools_{{ version }}.tar.gz
-  sha256: fba85be24e854ea3f2b78ca67c5d616d1840704db46c39baa661342e42d65221
+  sha256: e6fabcd68c6c3dc0a2d838493b56a39f80d07fb542df60e10601baca1258dc90
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: curl (>= 0.9), crayon, testthat (>= 1.0.2), BiocInstaller, Rcpp (>= 0.10.0), MASS, rmarkdown, knitr, hunspell (>= 2.0), lintr (>= 0.2.1), bitops, roxygen2 (>= 5.0.0), evaluate, rversions, covr, gmailr (> 0.7.0)
 requirements:
   build:
     - r-base
@@ -47,9 +54,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('devtools')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('devtools')"           # [not win]
     - "\"%R%\" -e \"library('devtools')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/hadley/devtools
   license: GPL (>= 2)
@@ -61,3 +75,31 @@ extra:
     - jdblischak
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: devtools
+# Title: Tools to Make Developing R Packages Easier
+# Version: 1.13.3
+# Authors@R: c( person("Hadley", "Wickham", , "hadley@rstudio.com", role = c("aut", "cre")), person("Winston", "Chang", role = "aut"), person("RStudio", role = "cph"), person("R Core team", role = "ctb", comment = "Some namespace and vignette code extracted from base R") )
+# Encoding: UTF-8
+# Description: Collection of package development tools.
+# URL: https://github.com/hadley/devtools
+# BugReports: https://github.com/hadley/devtools/issues
+# Depends: R (>= 3.0.2)
+# Imports: httr (>= 0.4), utils, tools, methods, memoise (>= 1.0.0), whisker, digest, rstudioapi (>= 0.2.0), jsonlite, stats, git2r (>= 0.11.0), withr
+# Suggests: curl (>= 0.9), crayon, testthat (>= 1.0.2), BiocInstaller, Rcpp (>= 0.10.0), MASS, rmarkdown, knitr, hunspell (>= 2.0), lintr (>= 0.2.1), bitops, roxygen2 (>= 5.0.0), evaluate, rversions, covr, gmailr (> 0.7.0)
+# License: GPL (>= 2)
+# VignetteBuilder: knitr
+# RoxygenNote: 6.0.1
+# NeedsCompilation: no
+# Packaged: 2017-08-01 13:17:50 UTC; jhester
+# Author: Hadley Wickham [aut, cre], Winston Chang [aut], RStudio [cph], R Core team [ctb] (Some namespace and vignette code extracted from base R)
+# Maintainer: Hadley Wickham <hadley@rstudio.com>
+# Repository: CRAN
+# Date/Publication: 2017-08-02 07:05:15 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-diagrammer-feedstock contains modified content
diff --git a/r-diagrammer-feedstock/recipe/meta.yaml b/r-diagrammer-feedstock/recipe/meta.yaml
index 7514a83..b785940 100644
--- a/r-diagrammer-feedstock/recipe/meta.yaml
+++ b/r-diagrammer-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '0.9.0' %}
+{% set version = '0.9.2' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,60 +12,114 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/DiagrammeR_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/DiagrammeR/DiagrammeR_{{ version }}.tar.gz
-  sha256: 8f42964f6a52bfee601297cf03bef99d1568b672426d44d14c0b543ec6814626
+  sha256: dc9d24c5129b29688d1dd74438fe1fc5f02f3537a9a94cd26fd54f496468e63b
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  # r-igraph is not available for Windows
-  skip: true  # [win]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: covr, DiagrammeRsvg, rsvg, knitr, testthat
 requirements:
   build:
     - r-base
     - r-rcolorbrewer >=1.1_2
-    - r-dplyr >=0.5.0
-    - r-htmlwidgets >=0.8
-    - r-igraph >=1.0.1
+    - r-downloader >=0.4
+    - r-dplyr >=0.7.2
+    - r-htmltools >=0.3.6
+    - r-htmlwidgets >=0.9
+    - r-igraph >=1.1.2
     - r-influencer >=0.1.0
     - r-magrittr >=1.5
+    - r-purrr >=0.2.3
+    - r-readr >=1.1.1
     - r-rgexf >=0.15.3
+    - r-rlang >=0.1.1
     - r-rstudioapi >=0.6
-    - r-scales >=0.4.1
-    - r-stringr >=1.1.0
-    - r-tibble >=1.2
-    - r-viridis >=0.3.4
-    - r-visnetwork >=1.0.2
+    - r-scales >=0.5.0
+    - r-stringr >=1.2.0
+    - r-tibble >=1.3.3
+    - r-tidyr >=0.6.3
+    - r-viridis >=0.4.0
+    - r-visnetwork >=2.0.1
+
   run:
     - r-base
     - r-rcolorbrewer >=1.1_2
-    - r-dplyr >=0.5.0
-    - r-htmlwidgets >=0.8
-    - r-igraph >=1.0.1
+    - r-downloader >=0.4
+    - r-dplyr >=0.7.2
+    - r-htmltools >=0.3.6
+    - r-htmlwidgets >=0.9
+    - r-igraph >=1.1.2
     - r-influencer >=0.1.0
     - r-magrittr >=1.5
+    - r-purrr >=0.2.3
+    - r-readr >=1.1.1
     - r-rgexf >=0.15.3
+    - r-rlang >=0.1.1
     - r-rstudioapi >=0.6
-    - r-scales >=0.4.1
-    - r-stringr >=1.1.0
-    - r-tibble >=1.2
-    - r-viridis >=0.3.4
-    - r-visnetwork >=1.0.2
+    - r-scales >=0.5.0
+    - r-stringr >=1.2.0
+    - r-tibble >=1.3.3
+    - r-tidyr >=0.6.3
+    - r-viridis >=0.4.0
+    - r-visnetwork >=2.0.1
 
 test:
   commands:
-    - $R -e "library('DiagrammeR')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('DiagrammeR')"           # [not win]
     - "\"%R%\" -e \"library('DiagrammeR')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/rich-iannone/DiagrammeR
-  license: MIT
-  summary: Create graph diagrams and flowcharts using R.
+  license: MIT + file LICENSE
+  summary: Graph and network visualization using tabular data.
   license_family: MIT
 
 extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: DiagrammeR
+# Type: Package
+# Title: Graph/Network Visualization
+# Version: 0.9.2
+# Authors@R: c( person( "Knut", "Sveidqvist" , role = c("aut", "cph") , comment = "mermaid.js library in htmlwidgets/lib, http://github.com/knsv/mermaid/" ), person( "Mike", "Bostock" , role = c("aut", "cph") , comment = "d3.js library in htmlwidgets/lib, http://d3js.org" ), person( "Chris", "Pettitt" , role = c("aut", "cph") , comment = "dagre-d3.js library in htmlwidgets/lib, http://github.com/cpettitt/dagre-d3" ), person( "Mike", "Daines" , role = c("aut", "cph") , comment = "viz.js library in htmlwidgets/lib, http://github.com/mdaines/viz.js/" ), person( "Richard", "Iannone" , role = c("aut", "cre") , comment = "R interface" , email = "riannone@me.com" ) )
+# Maintainer: Richard Iannone <riannone@me.com>
+# Description: Graph and network visualization using tabular data.
+# Depends: R (>= 3.2.0)
+# License: MIT + file LICENSE
+# Imports: dplyr (>= 0.7.2), downloader (>= 0.4), htmltools (>= 0.3.6), htmlwidgets (>= 0.9), igraph (>= 1.1.2), influenceR (>= 0.1.0), magrittr (>= 1.5), purrr (>= 0.2.3), RColorBrewer (>= 1.1-2), readr (>= 1.1.1), rlang (>= 0.1.1), rstudioapi (>= 0.6), rgexf (>= 0.15.3), scales (>= 0.5.0), stringr (>= 1.2.0), tibble (>= 1.3.3), tidyr (>= 0.6.3), viridis (>= 0.4.0), visNetwork (>= 2.0.1)
+# URL: https://github.com/rich-iannone/DiagrammeR
+# BugReports: https://github.com/rich-iannone/DiagrammeR/issues
+# Suggests: covr, DiagrammeRsvg, rsvg, knitr, testthat
+# RoxygenNote: 6.0.1
+# NeedsCompilation: no
+# Packaged: 2017-09-06 04:28:01 UTC; riannone
+# Author: Knut Sveidqvist [aut, cph] (mermaid.js library in htmlwidgets/lib, http://github.com/knsv/mermaid/), Mike Bostock [aut, cph] (d3.js library in htmlwidgets/lib, http://d3js.org), Chris Pettitt [aut, cph] (dagre-d3.js library in htmlwidgets/lib, http://github.com/cpettitt/dagre-d3), Mike Daines [aut, cph] (viz.js library in htmlwidgets/lib, http://github.com/mdaines/viz.js/), Richard Iannone [aut, cre] (R interface)
+# Repository: CRAN
+# Date/Publication: 2017-09-06 20:37:43 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-dichromat-feedstock contains modified content
diff --git a/r-dichromat-feedstock/recipe/meta.yaml b/r-dichromat-feedstock/recipe/meta.yaml
index 74b535a..0f4500d 100644
--- a/r-dichromat-feedstock/recipe/meta.yaml
+++ b/r-dichromat-feedstock/recipe/meta.yaml
@@ -14,14 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/dichromat/dichromat_{{ version }}.tar.gz
   sha256: 31151eaf36f70bdc1172da5ff5088ee51cc0a3db4ead59c7c38c25316d580dd1
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
@@ -31,9 +38,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('dichromat')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('dichromat')"           # [not win]
     - "\"%R%\" -e \"library('dichromat')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=dichromat
   license: GPL-2
@@ -45,3 +59,26 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: dichromat
+# Version: 2.0-0
+# Date: 2013-01-23
+# Title: Color Schemes for Dichromats
+# Authors@R: c(person(given = "Thomas", family = "Lumley", role = c("aut", "cre"), email = "tlumley@u.washington.edu"), person(given = "Ken", family = "Knoblauch", role = "ctb", email = "ken.knoblauch@inserm.fr"), person(given = "Scott", family = "Waichler", role = "ctb", email = "scott.waichler@pnl.gov"), person(given = "Achim", family = "Zeileis", role = "ctb", email = "Achim.Zeileis@R-project.org"))
+# Description: Collapse red-green or green-blue distinctions to simulate the effects of different types of color-blindness.
+# Depends: R (>= 2.10), stats
+# License: GPL-2
+# LazyLoad: Yes
+# Packaged: 2013-01-23 06:25:04 UTC; zeileis
+# Author: Thomas Lumley [aut, cre], Ken Knoblauch [ctb], Scott Waichler [ctb], Achim Zeileis [ctb]
+# Maintainer: Thomas Lumley <tlumley@u.washington.edu>
+# Repository: CRAN
+# Date/Publication: 2013-01-24 08:18:09
+# NeedsCompilation: no
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-digest-feedstock contains modified content
diff --git a/r-digest-feedstock/recipe/bld.bat b/r-digest-feedstock/recipe/bld.bat
new file mode 100644
index 0000000..8b10c94
--- /dev/null
+++ b/r-digest-feedstock/recipe/bld.bat
@@ -0,0 +1,8 @@
+"%R%" CMD INSTALL --build .
+if errorlevel 1 exit 1
+
+@rem Add more build steps here, if they are necessary.
+
+@rem See
+@rem http://docs.continuum.io/conda/build.html
+@rem for a list of environment variables that are set during the build process.
diff --git a/r-digest-feedstock/recipe/build.sh b/r-digest-feedstock/recipe/build.sh
new file mode 100644
index 0000000..0c37df6
--- /dev/null
+++ b/r-digest-feedstock/recipe/build.sh
@@ -0,0 +1,13 @@
+#!/bin/bash
+
+# R refuses to build packages that mark themselves as Priority: Recommended
+mv DESCRIPTION DESCRIPTION.old
+grep -v '^Priority: ' DESCRIPTION.old > DESCRIPTION
+
+$R CMD INSTALL --build .
+
+# Add more build steps here, if they are necessary.
+
+# See
+# http://docs.continuum.io/conda/build.html
+# for a list of environment variables that are set during the build process.
diff --git a/r-digest-feedstock/recipe/meta.yaml b/r-digest-feedstock/recipe/meta.yaml
index ecf86ee..c27cb58 100644
--- a/r-digest-feedstock/recipe/meta.yaml
+++ b/r-digest-feedstock/recipe/meta.yaml
@@ -1,48 +1,66 @@
-{% set version = "0.6.12" %}
+{% set version = '0.6.12' %}
+
+{% set posix = 'm2-' if win else '' %}
+{% set native = 'm2w64-' if win else '' %}
 
 package:
   name: r-digest
-  version: {{ version }}
+  version: {{ version|replace("-", "_") }}
 
 source:
   fn: digest_{{ version }}.tar.gz
   url:
-    - http://cran.r-project.org/src/contrib/digest_{{ version }}.tar.gz
-    - http://cran.r-project.org/src/contrib/Archive/digest/digest_{{ version }}.tar.gz
+    - https://cran.r-project.org/src/contrib/digest_{{ version }}.tar.gz
+    - https://cran.r-project.org/src/contrib/Archive/digest/digest_{{ version }}.tar.gz
   sha256: a479463f120037ad8e88bb1387170842e635a1f07ce7e3575316efd6e14d9eab
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: True  # [win32]
-  script: R CMD INSTALL --build .
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: knitr, rmarkdown
 requirements:
   build:
     - r-base
-    - posix  # [win64]
-    - m2w64-toolchain  # [win64]
-    - gcc  # [not win]
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
+
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - R -e "library('digest')"  # [not win]
-    - R -e \"library('digest')\"  # [win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('digest')"           # [not win]
+    - "\"%R%\" -e \"library('digest')\""  # [win]
+
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
 
 about:
   home: http://dirk.eddelbuettel.com/code/digest.html
-  license: GPL-2.0
-  summary: |
-    Implementation of a function 'digest()' for the creation  of hash digests of arbitrary
+  license: GPL (>= 2)
+  summary: Implementation of a function 'digest()' for the creation  of hash digests of arbitrary
     R objects (using the 'md5', 'sha-1', 'sha-256',  'crc32', 'xxhash' and 'murmurhash'
     algorithms) permitting easy comparison of R language objects, as well as a function
     'hmac()' to create hash-based message authentication code. Please note that this
     package is not meant to be deployed for cryptographic purposes for which more comprehensive
     (and widely tested) libraries such as 'OpenSSL' should be used.
+  license_family: GPL3
 
 extra:
   recipe-maintainers:
@@ -50,3 +68,28 @@ extra:
     - johanneskoester
     - bgruening
     - daler
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: digest
+# Author: Dirk Eddelbuettel <edd@debian.org> with contributions  by Antoine Lucas, Jarek Tuszynski, Henrik Bengtsson, Simon Urbanek, Mario Frasca, Bryan Lewis, Murray Stokely, Hannes Muehleisen, Duncan Murdoch, Jim Hester, Wush Wu, Qiang Kou, Thierry Onkelinx,  Michel Lang and Viliam Simko.
+# Version: 0.6.12
+# Date: 2017-01-26
+# Maintainer: Dirk Eddelbuettel <edd@debian.org>
+# Title: Create Compact Hash Digests of R Objects
+# Description: Implementation of a function 'digest()' for the creation  of hash digests of arbitrary R objects (using the 'md5', 'sha-1', 'sha-256',  'crc32', 'xxhash' and 'murmurhash' algorithms) permitting easy comparison of R language objects, as well as a function 'hmac()' to create hash-based message authentication code. Please note that this package is not meant to be deployed for cryptographic purposes for which more comprehensive (and widely tested) libraries such as 'OpenSSL' should be used.
+# URL: http://dirk.eddelbuettel.com/code/digest.html
+# BugReports: https://github.com/eddelbuettel/digest/issues
+# Depends: R (>= 2.4.1)
+# License: GPL (>= 2)
+# Suggests: knitr, rmarkdown
+# VignetteBuilder: knitr
+# NeedsCompilation: yes
+# Packaged: 2017-01-26 13:52:49.564188 UTC; edd
+# Repository: CRAN
+# Date/Publication: 2017-01-27 10:18:01
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
diff --git a/r-dimred-feedstock/recipe/bld.bat b/r-dimred-feedstock/recipe/bld.bat
index e69de29..8b10c94 100644
--- a/r-dimred-feedstock/recipe/bld.bat
+++ b/r-dimred-feedstock/recipe/bld.bat
@@ -0,0 +1,8 @@
+"%R%" CMD INSTALL --build .
+if errorlevel 1 exit 1
+
+@rem Add more build steps here, if they are necessary.
+
+@rem See
+@rem http://docs.continuum.io/conda/build.html
+@rem for a list of environment variables that are set during the build process.
diff --git a/r-dimred-feedstock/recipe/build.sh b/r-dimred-feedstock/recipe/build.sh
index e69de29..0c37df6 100644
--- a/r-dimred-feedstock/recipe/build.sh
+++ b/r-dimred-feedstock/recipe/build.sh
@@ -0,0 +1,13 @@
+#!/bin/bash
+
+# R refuses to build packages that mark themselves as Priority: Recommended
+mv DESCRIPTION DESCRIPTION.old
+grep -v '^Priority: ' DESCRIPTION.old > DESCRIPTION
+
+$R CMD INSTALL --build .
+
+# Add more build steps here, if they are necessary.
+
+# See
+# http://docs.continuum.io/conda/build.html
+# for a list of environment variables that are set during the build process.
diff --git a/r-dimred-feedstock/recipe/meta.yaml b/r-dimred-feedstock/recipe/meta.yaml
index e69de29..84825f9 100644
--- a/r-dimred-feedstock/recipe/meta.yaml
+++ b/r-dimred-feedstock/recipe/meta.yaml
@@ -0,0 +1,87 @@
+{% set version = '0.1.0' %}
+
+{% set posix = 'm2-' if win else '' %}
+{% set native = 'm2w64-' if win else '' %}
+
+package:
+  name: r-dimred
+  version: {{ version|replace("-", "_") }}
+
+source:
+  fn: dimRed_{{ version }}.tar.gz
+  url:
+    - https://cran.r-project.org/src/contrib/dimRed_{{ version }}.tar.gz
+    - https://cran.r-project.org/src/contrib/Archive/dimRed/dimRed_{{ version }}.tar.gz
+  sha256: fb0cef7a21b8a5219c74e5227a282168599dd63e904130366f3d2fed8a625a39
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
+build:
+  # If this is a new build for the same version, increment the build number.
+  number: 0
+
+  # This is required to make R link correctly on Linux.
+  rpaths:
+    - lib/R/lib/
+    - lib/
+
+# Suggests: MASS, Matrix, RANN, RSpectra, Rtsne, coRanking, diffusionMap, energy, fastICA, ggplot2, graphics, igraph, kernlab, lle, loe, optimx, pcaPP, rgl, scales, scatterplot3d, stats, testthat, tidyr, vegan
+requirements:
+  build:
+    - r-base
+    - r-drr
+
+  run:
+    - r-base
+    - r-drr
+
+test:
+  commands:
+    # You can put additional test commands to be run here.
+    - $R -e "library('dimRed')"           # [not win]
+    - "\"%R%\" -e \"library('dimRed')\""  # [win]
+
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
+about:
+  home: https://github.com/gdkrmr/dimRed
+  license: GPL-3 | file LICENSE
+  summary: A collection of dimensionality reduction techniques from R packages and provides a
+    common interface for calling the methods.
+  license_family: GPL3
+
+extra:
+  recipe-maintainers:
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: dimRed
+# Title: A Framework for Dimensionality Reduction
+# Version: 0.1.0
+# Authors@R: person("Guido", "Kraemer", email = "gkraemer@bgc-jena.mpg.de", role = c("aut", "cre"))
+# Description: A collection of dimensionality reduction techniques from R packages and provides a common interface for calling the methods.
+# Depends: R (>= 3.0.0), methods, DRR
+# Suggests: MASS, Matrix, RANN, RSpectra, Rtsne, coRanking, diffusionMap, energy, fastICA, ggplot2, graphics, igraph, kernlab, lle, loe, optimx, pcaPP, rgl, scales, scatterplot3d, stats, testthat, tidyr, vegan
+# License: GPL-3 | file LICENSE
+# URL: https://github.com/gdkrmr/dimRed
+# LazyData: true
+# Collate: 'misc.R' 'dimRedData-class.R' 'dataSets.R' 'dimRedMethod-class.R' 'dimRedResult-class.R' 'diffmap.R' 'dimRed.R' 'drr.R' 'embed.R' 'fastica.R' 'get_info.R' 'graph_embed.R' 'hlle.R' 'isomap.R' 'kpca.R' 'leim.R' 'lle.R' 'loe.R' 'mds.R' 'mixColorSpaces.R' 'nmds.R' 'pca.R' 'plot.R' 'quality.R' 'rotate.R' 'soe.R' 'tsne.R'
+# RoxygenNote: 6.0.1
+# NeedsCompilation: yes
+# Packaged: 2017-05-04 14:56:00 UTC; gkraemer
+# Author: Guido Kraemer [aut, cre]
+# Maintainer: Guido Kraemer <gkraemer@bgc-jena.mpg.de>
+# Repository: CRAN
+# Date/Publication: 2017-05-04 15:37:41 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-domc-feedstock contains modified content
diff --git a/r-domc-feedstock/recipe/meta.yaml b/r-domc-feedstock/recipe/meta.yaml
index 608f73a..3ec99b5 100644
--- a/r-domc-feedstock/recipe/meta.yaml
+++ b/r-domc-feedstock/recipe/meta.yaml
@@ -14,14 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/doMC/doMC_{{ version }}.tar.gz
   sha256: 76a1092fca59b441e6f89eb7e2fb3a12de807f736a217949c80339f20c958778
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  # Unix only package
-  skip: true  # [win]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
+
+
 requirements:
   build:
     - r-base
@@ -35,9 +42,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('doMC')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('doMC')"           # [not win]
     - "\"%R%\" -e \"library('doMC')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=doMC
   license: GPL-2
@@ -49,3 +63,31 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: doMC
+# Type: Package
+# Title: Foreach Parallel Adaptor for 'parallel'
+# Version: 1.3.4
+# Authors@R: c(person("Rich", "Calaway", role="cre", email="richcala@microsoft.com"), person("Revolution", "Analytics", role=c("aut", "cph")), person("Steve", "Weston", role="aut"))
+# Description: Provides a parallel backend for the %dopar% function using the multicore functionality of the parallel package.
+# Depends: R (>= 2.14.0), foreach(>= 1.2.0), iterators(>= 1.0.0), parallel
+# Imports: utils
+# Enhances: compiler, RUnit
+# License: GPL-2
+# Author: Rich Calaway [cre], Revolution Analytics [aut, cph], Steve Weston [aut]
+# Maintainer: Rich Calaway <richcala@microsoft.com>
+# Repository: CRAN
+# Repository/R-Forge/Project: domc
+# Repository/R-Forge/Revision: 17
+# Repository/R-Forge/DateTimeStamp: 2015-10-10 00:31:58
+# Date/Publication: 2015-10-13 09:12:51
+# NeedsCompilation: no
+# Packaged: 2015-10-10 00:46:21 UTC; rforge
+# OS_type: unix
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-doparallel-feedstock contains modified content
diff --git a/r-doparallel-feedstock/recipe/meta.yaml b/r-doparallel-feedstock/recipe/meta.yaml
index 323b239..2d1c912 100644
--- a/r-doparallel-feedstock/recipe/meta.yaml
+++ b/r-doparallel-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '1.0.10' %}
+{% set version = '1.0.11' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,16 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/doParallel_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/doParallel/doParallel_{{ version }}.tar.gz
-  sha256: 70024b6950025cc027022ee409f382e5ad3680c0a25bcd404bfc16418be8add5
+  sha256: 4ccbd2eb46d3e4f5251b0c3de4d93d9168b02bb0be493656d6aea236667ff76a
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: caret, mlbench, rpart
 requirements:
   build:
     - r-base
@@ -35,9 +42,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('doParallel')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('doParallel')"           # [not win]
     - "\"%R%\" -e \"library('doParallel')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=doParallel
   license: GPL-2
@@ -48,3 +62,30 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: doParallel
+# Type: Package
+# Title: Foreach Parallel Adaptor for the 'parallel' Package
+# Version: 1.0.11
+# Authors@R: c(person("Rich", "Calaway", role="cre", email="richcala@microsoft.com"), person("Microsoft", "Corporation", role=c("aut", "cph")), person("Steve", "Weston", role="aut"), person("Dan", "Tenenbaum", role="ctb"))
+# Description: Provides a parallel backend for the %dopar% function using the parallel package.
+# Depends: R (>= 2.14.0), foreach(>= 1.2.0), iterators(>= 1.0.0), parallel, utils
+# Suggests: caret, mlbench, rpart
+# Enhances: compiler, RUnit
+# License: GPL-2
+# Author: Rich Calaway [cre], Microsoft Corporation [aut, cph], Steve Weston [aut], Dan Tenenbaum [ctb]
+# Maintainer: Rich Calaway <richcala@microsoft.com>
+# Repository: CRAN
+# Repository/R-Forge/Project: doparallel
+# Repository/R-Forge/Revision: 16
+# Repository/R-Forge/DateTimeStamp: 2017-09-18 23:27:23
+# Date/Publication: 2017-09-28 16:24:39 UTC
+# NeedsCompilation: no
+# Packaged: 2017-09-18 23:46:05 UTC; rforge
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
diff --git a/r-downloader-feedstock/recipe/bld.bat b/r-downloader-feedstock/recipe/bld.bat
index e69de29..8b10c94 100644
--- a/r-downloader-feedstock/recipe/bld.bat
+++ b/r-downloader-feedstock/recipe/bld.bat
@@ -0,0 +1,8 @@
+"%R%" CMD INSTALL --build .
+if errorlevel 1 exit 1
+
+@rem Add more build steps here, if they are necessary.
+
+@rem See
+@rem http://docs.continuum.io/conda/build.html
+@rem for a list of environment variables that are set during the build process.
diff --git a/r-downloader-feedstock/recipe/build.sh b/r-downloader-feedstock/recipe/build.sh
index e69de29..0c37df6 100644
--- a/r-downloader-feedstock/recipe/build.sh
+++ b/r-downloader-feedstock/recipe/build.sh
@@ -0,0 +1,13 @@
+#!/bin/bash
+
+# R refuses to build packages that mark themselves as Priority: Recommended
+mv DESCRIPTION DESCRIPTION.old
+grep -v '^Priority: ' DESCRIPTION.old > DESCRIPTION
+
+$R CMD INSTALL --build .
+
+# Add more build steps here, if they are necessary.
+
+# See
+# http://docs.continuum.io/conda/build.html
+# for a list of environment variables that are set during the build process.
diff --git a/r-downloader-feedstock/recipe/meta.yaml b/r-downloader-feedstock/recipe/meta.yaml
index e69de29..c3603b0 100644
--- a/r-downloader-feedstock/recipe/meta.yaml
+++ b/r-downloader-feedstock/recipe/meta.yaml
@@ -0,0 +1,87 @@
+{% set version = '0.4' %}
+
+{% set posix = 'm2-' if win else '' %}
+{% set native = 'm2w64-' if win else '' %}
+
+package:
+  name: r-downloader
+  version: {{ version|replace("-", "_") }}
+
+source:
+  fn: downloader_{{ version }}.tar.gz
+  url:
+    - https://cran.r-project.org/src/contrib/downloader_{{ version }}.tar.gz
+    - https://cran.r-project.org/src/contrib/Archive/downloader/downloader_{{ version }}.tar.gz
+  sha256: 1890e75b028775154023f2135cafb3e3eed0fe908138ab4f7eff1fc1b47dafab
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
+build:
+  # If this is a new build for the same version, increment the build number.
+  number: 0
+
+  # This is required to make R link correctly on Linux.
+  rpaths:
+    - lib/R/lib/
+    - lib/
+
+# Suggests: testthat
+requirements:
+  build:
+    - r-base
+    - r-digest
+
+  run:
+    - r-base
+    - r-digest
+
+test:
+  commands:
+    # You can put additional test commands to be run here.
+    - $R -e "library('downloader')"           # [not win]
+    - "\"%R%\" -e \"library('downloader')\""  # [win]
+
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
+about:
+  home: https://github.com/wch/downloader
+  license: GPL-2
+  summary: Provides a wrapper for the download.file function, making it possible to download
+    files over HTTPS on Windows, Mac OS X, and other Unix-like platforms. The 'RCurl'
+    package provides this functionality (and much more) but can be difficult to install
+    because it must be compiled with external dependencies. This package has no external
+    dependencies, so it is much easier to install.
+  license_family: GPL2
+
+extra:
+  recipe-maintainers:
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: downloader
+# Maintainer: Winston Chang <winston@stdout.org>
+# Author: Winston Chang <winston@stdout.org>
+# Version: 0.4
+# License: GPL-2
+# Title: Download Files over HTTP and HTTPS
+# Description: Provides a wrapper for the download.file function, making it possible to download files over HTTPS on Windows, Mac OS X, and other Unix-like platforms. The 'RCurl' package provides this functionality (and much more) but can be difficult to install because it must be compiled with external dependencies. This package has no external dependencies, so it is much easier to install.
+# URL: https://github.com/wch/downloader
+# Imports: utils, digest
+# Suggests: testthat
+# BugReports: https://github.com/wch/downloader/issues
+# NeedsCompilation: no
+# Packaged: 2015-07-09 01:22:22 UTC; winston
+# Repository: CRAN
+# Date/Publication: 2015-07-09 14:47:41
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-dplyr-feedstock contains modified content
diff --git a/r-dplyr-feedstock/recipe/meta.yaml b/r-dplyr-feedstock/recipe/meta.yaml
index 97d2949..17c394d 100644
--- a/r-dplyr-feedstock/recipe/meta.yaml
+++ b/r-dplyr-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '0.7.0' %}
+{% set version = '0.7.4' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,56 +12,73 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/dplyr_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/dplyr/dplyr_{{ version }}.tar.gz
-  sha256: 27b3593c09da5e99c0c4fb19ea826edd2cab619f8aaefd0cfd2a4140a0bd9410
+  sha256: 7b1fc90750fbb46483423da6721832c545d37b157f4f3355784a65e50fada8c2
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: bit64, covr, dbplyr, dtplyr, DBI, ggplot2, hms, knitr, Lahman (>= 3.0-1), mgcv, microbenchmark, nycflights13, rmarkdown, RMySQL, RPostgreSQL, RSQLite, testthat, withr
 requirements:
   build:
-    - {{ compiler('c') }}  # [not win]
-    - r-assertthat
     - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('cxx') }}      # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
     - r-bh >=1.58.0_1
     - r-r6
-    - r-rcpp >=0.12.6
-    - r-bindrcpp
-    - r-glue
+    - r-rcpp >=0.12.7
+    - r-assertthat
+    - r-bindrcpp >=0.2
+    - r-glue >=1.1.1
     - r-magrittr
     - r-pkgconfig
     - r-plogr
-    - r-rlang >=0.1
+    - r-rlang >=0.1.2
     - r-tibble >=1.3.1
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
 
   run:
     - r-base
-    - r-assertthat
+    - {{native}}gcc-libs         # [win]
     - r-bh >=1.58.0_1
     - r-r6
-    - r-rcpp >=0.12.6
-    - r-bindrcpp
-    - r-glue
+    - r-rcpp >=0.12.7
+    - r-assertthat
+    - r-bindrcpp >=0.2
+    - r-glue >=1.1.1
     - r-magrittr
     - r-pkgconfig
     - r-plogr
-    - r-rlang >=0.1
+    - r-rlang >=0.1.2
     - r-tibble >=1.3.1
-    - {{native}}gcc-libs   # [win]
 
 test:
   commands:
-    - $R -e "library('dplyr')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('dplyr')"           # [not win]
     - "\"%R%\" -e \"library('dplyr')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
-  home: https://github.com/hadley/dplyr
-  license: MIT
+  home: http://dplyr.tidyverse.org, https://github.com/tidyverse/dplyr
+  license: MIT + file LICENSE
   summary: A fast, consistent tool for working with data frame like objects, both in memory and
     out of memory.
   license_family: MIT
@@ -70,3 +87,34 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Type: Package
+# Package: dplyr
+# Version: 0.7.4
+# Title: A Grammar of Data Manipulation
+# Description: A fast, consistent tool for working with data frame like objects, both in memory and out of memory.
+# Authors@R: c( person("Hadley", "Wickham", , "hadley@rstudio.com", c("aut", "cre")), person("Romain", "Francois", , "romain@r-enthusiasts.com", role = "aut"), person("Lionel", "Henry", role = "aut"), person("Kirill", "Muller", role = "aut"), person("RStudio", role = c("cph", "fnd")) )
+# URL: http://dplyr.tidyverse.org, https://github.com/tidyverse/dplyr
+# BugReports: https://github.com/tidyverse/dplyr/issues
+# Encoding: UTF-8
+# Depends: R (>= 3.1.2)
+# Imports: assertthat, bindrcpp (>= 0.2), glue (>= 1.1.1), magrittr, methods, pkgconfig, rlang (>= 0.1.2), R6, Rcpp (>= 0.12.7), tibble (>= 1.3.1), utils
+# Suggests: bit64, covr, dbplyr, dtplyr, DBI, ggplot2, hms, knitr, Lahman (>= 3.0-1), mgcv, microbenchmark, nycflights13, rmarkdown, RMySQL, RPostgreSQL, RSQLite, testthat, withr
+# VignetteBuilder: knitr
+# LinkingTo: Rcpp (>= 0.12.0), BH (>= 1.58.0-1), bindrcpp, plogr
+# LazyData: yes
+# License: MIT + file LICENSE
+# RoxygenNote: 6.0.1
+# NeedsCompilation: yes
+# Packaged: 2017-09-16 15:25:52 UTC; muelleki
+# Author: Hadley Wickham [aut, cre], Romain Francois [aut], Lionel Henry [aut], Kirill Muller [aut], RStudio [cph, fnd]
+# Maintainer: Hadley Wickham <hadley@rstudio.com>
+# Repository: CRAN
+# Date/Publication: 2017-09-28 20:43:29 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
diff --git a/r-drr-feedstock/recipe/bld.bat b/r-drr-feedstock/recipe/bld.bat
index e69de29..8b10c94 100644
--- a/r-drr-feedstock/recipe/bld.bat
+++ b/r-drr-feedstock/recipe/bld.bat
@@ -0,0 +1,8 @@
+"%R%" CMD INSTALL --build .
+if errorlevel 1 exit 1
+
+@rem Add more build steps here, if they are necessary.
+
+@rem See
+@rem http://docs.continuum.io/conda/build.html
+@rem for a list of environment variables that are set during the build process.
diff --git a/r-drr-feedstock/recipe/build.sh b/r-drr-feedstock/recipe/build.sh
index e69de29..0c37df6 100644
--- a/r-drr-feedstock/recipe/build.sh
+++ b/r-drr-feedstock/recipe/build.sh
@@ -0,0 +1,13 @@
+#!/bin/bash
+
+# R refuses to build packages that mark themselves as Priority: Recommended
+mv DESCRIPTION DESCRIPTION.old
+grep -v '^Priority: ' DESCRIPTION.old > DESCRIPTION
+
+$R CMD INSTALL --build .
+
+# Add more build steps here, if they are necessary.
+
+# See
+# http://docs.continuum.io/conda/build.html
+# for a list of environment variables that are set during the build process.
diff --git a/r-drr-feedstock/recipe/meta.yaml b/r-drr-feedstock/recipe/meta.yaml
index e69de29..82a3db0 100644
--- a/r-drr-feedstock/recipe/meta.yaml
+++ b/r-drr-feedstock/recipe/meta.yaml
@@ -0,0 +1,90 @@
+{% set version = '0.0.2' %}
+
+{% set posix = 'm2-' if win else '' %}
+{% set native = 'm2w64-' if win else '' %}
+
+package:
+  name: r-drr
+  version: {{ version|replace("-", "_") }}
+
+source:
+  fn: DRR_{{ version }}.tar.gz
+  url:
+    - https://cran.r-project.org/src/contrib/DRR_{{ version }}.tar.gz
+    - https://cran.r-project.org/src/contrib/Archive/DRR/DRR_{{ version }}.tar.gz
+  sha256: 02860f05f97a6020012fb6ebfe0d7738dd9e9db0555326f1ef57199fcde58ee9
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
+build:
+  # If this is a new build for the same version, increment the build number.
+  number: 0
+
+  # This is required to make R link correctly on Linux.
+  rpaths:
+    - lib/R/lib/
+    - lib/
+
+# Suggests: knitr
+requirements:
+  build:
+    - r-base
+    - r-cvst
+    - r-matrix
+    - r-kernlab
+
+  run:
+    - r-base
+    - r-cvst
+    - r-matrix
+    - r-kernlab
+
+test:
+  commands:
+    # You can put additional test commands to be run here.
+    - $R -e "library('DRR')"           # [not win]
+    - "\"%R%\" -e \"library('DRR')\""  # [win]
+
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
+about:
+  home: https://CRAN.R-project.org/package=DRR
+  license: GPL-3
+  summary: An Implementation of Dimensionality Reduction via Regression using Kernel Ridge Regression.
+  license_family: GPL3
+
+extra:
+  recipe-maintainers:
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: DRR
+# Title: Dimensionality Reduction via Regression
+# Version: 0.0.2
+# Authors@R: person("Guido", "Kraemer", email = "gkraemer@bgc-jena.mpg.de", role = c("aut","cre"))
+# Description: An Implementation of Dimensionality Reduction via Regression using Kernel Ridge Regression.
+# License: GPL-3
+# Imports: stats, methods
+# Suggests: knitr
+# VignetteBuilder: knitr
+# LazyData: true
+# Depends: kernlab, CVST, Matrix
+# RoxygenNote: 5.0.1
+# NeedsCompilation: no
+# Packaged: 2016-09-15 09:54:12 UTC; gkraemer
+# Author: Guido Kraemer [aut, cre]
+# Maintainer: Guido Kraemer <gkraemer@bgc-jena.mpg.de>
+# Repository: CRAN
+# Date/Publication: 2016-09-15 15:46:27
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-dt-feedstock contains modified content
diff --git a/r-dt-feedstock/recipe/meta.yaml b/r-dt-feedstock/recipe/meta.yaml
index b4fbd6b..333a4e7 100644
--- a/r-dt-feedstock/recipe/meta.yaml
+++ b/r-dt-feedstock/recipe/meta.yaml
@@ -14,13 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/DT/DT_{{ version }}.tar.gz
   sha256: a1b7f9e5c31a241fdf78ac582499f346e915ff948554980bbc2262c924b806bd
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: jsonlite (>= 0.9.16), knitr (>= 1.8), rmarkdown, shiny (>= 0.12.1)
 requirements:
   build:
     - r-base
@@ -36,12 +44,19 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('DT')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('DT')"           # [not win]
     - "\"%R%\" -e \"library('DT')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://rstudio.github.io/DT
-  license: GPL-3
+  license: GPL-3 | file LICENSE
   summary: Data objects in R can be rendered as HTML tables using the JavaScript library 'DataTables'
     (typically via R Markdown or Shiny). The 'DataTables' library has been included
     in this R package. The package name 'DT' is an abbreviation of 'DataTables'.
@@ -51,3 +66,31 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: DT
+# Type: Package
+# Title: A Wrapper of the JavaScript Library 'DataTables'
+# Version: 0.2
+# Date: 2016-08-08
+# Authors@R: c( person("Yihui", "Xie", email = "xie@yihui.name", role = c("aut", "cre")), person("Joe", "Cheng", role = "ctb"), person("JJ", "Allaire", role = "ctb"), person("jQuery contributors", role = c("ctb", "cph"), comment = "jQuery in htmlwidgets/lib"), person("SpryMedia Limited", role = c("ctb", "cph"), comment = "DataTables in htmlwidgets/lib"), person("Brian", "Reavis", role = c("ctb", "cph"), comment = "selectize.js in htmlwidgets/lib"), person("Leon", "Gersen", role = c("ctb", "cph"), comment = "noUiSlider in htmlwidgets/lib"), person("Bartek", "Szopka", role = c("ctb", "cph"), comment = "jquery.highlight.js in htmlwidgets/lib"), person("RStudio Inc", role = "cph") )
+# Maintainer: Yihui Xie <xie@yihui.name>
+# Description: Data objects in R can be rendered as HTML tables using the JavaScript library 'DataTables' (typically via R Markdown or Shiny). The 'DataTables' library has been included in this R package. The package name 'DT' is an abbreviation of 'DataTables'.
+# URL: http://rstudio.github.io/DT
+# BugReports: https://github.com/rstudio/DT/issues
+# License: GPL-3 | file LICENSE
+# Imports: htmltools (>= 0.3.5), htmlwidgets (>= 0.6), magrittr
+# Suggests: jsonlite (>= 0.9.16), knitr (>= 1.8), rmarkdown, shiny (>= 0.12.1)
+# VignetteBuilder: knitr
+# RoxygenNote: 5.0.1
+# NeedsCompilation: no
+# Packaged: 2016-08-09 04:06:45 UTC; yihui
+# Author: Yihui Xie [aut, cre], Joe Cheng [ctb], JJ Allaire [ctb], jQuery contributors [ctb, cph] (jQuery in htmlwidgets/lib), SpryMedia Limited [ctb, cph] (DataTables in htmlwidgets/lib), Brian Reavis [ctb, cph] (selectize.js in htmlwidgets/lib), Leon Gersen [ctb, cph] (noUiSlider in htmlwidgets/lib), Bartek Szopka [ctb, cph] (jquery.highlight.js in htmlwidgets/lib), RStudio Inc [cph]
+# Repository: CRAN
+# Date/Publication: 2016-08-09 11:41:46
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-dygraphs-feedstock contains modified content
diff --git a/r-dygraphs-feedstock/recipe/meta.yaml b/r-dygraphs-feedstock/recipe/meta.yaml
index 5a43db7..ae1cf86 100644
--- a/r-dygraphs-feedstock/recipe/meta.yaml
+++ b/r-dygraphs-feedstock/recipe/meta.yaml
@@ -14,13 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/dygraphs/dygraphs_{{ version }}.tar.gz
   sha256: 69d1c97573b650cfbf24d11948e5904b103fdb4b3396d244aab3f3dc0f4e8f6f
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: testthat
 requirements:
   build:
     - r-base
@@ -40,12 +48,19 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('dygraphs')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('dygraphs')"           # [not win]
     - "\"%R%\" -e \"library('dygraphs')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/rstudio/dygraphs
-  license: MIT
+  license: MIT + file LICENSE
   summary: An R interface to the 'dygraphs' JavaScript charting library (a copy of which is included
     in the package). Provides rich facilities for charting time-series data in R, including
     highly configurable series- and axis-display and interactive features like zoom/pan
@@ -56,3 +71,32 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: dygraphs
+# Type: Package
+# Title: Interface to 'Dygraphs' Interactive Time Series Charting Library
+# Version: 1.1.1.4
+# Date: 2017-01-04
+# Authors@R: c( person("Dan", "Vanderkam", role = c("aut", "cph"), comment = "dygraphs library in htmlwidgets/lib, http://dygraphs.com/"), person("JJ", "Allaire", role = c("aut", "cre"), comment = "R interface", email = "jj@rstudio.com"), person(family = "RStudio", role = "cph"), person("Jonathan", "Owen", role = c("aut", "cph")), person("Daniel", "Gromer", role = c("aut", "cph")), person("Petr", "Shevtsov", role = c("aut", "cph")), person("Benoit", "Thieurmel", role = c("aut", "cph")), person(family = "jQuery Foundation", role = "cph", comment = "jQuery library"), person(family = "jQuery contributors", role = c("ctb", "cph"), comment = "jQuery library; authors listed in inst/htmlwidgets/lib/jquery/AUTHORS.txt") )
+# Maintainer: JJ Allaire <jj@rstudio.com>
+# URL: https://github.com/rstudio/dygraphs
+# BugReports: https://github.com/rstudio/dygraphs/issues
+# Description: An R interface to the 'dygraphs' JavaScript charting library (a copy of which is included in the package). Provides rich facilities for charting time-series data in R, including highly configurable series- and axis-display and interactive features like zoom/pan and series/point highlighting.
+# License: MIT + file LICENSE
+# Depends: R (>= 3.0)
+# Imports: magrittr, htmlwidgets (>= 0.6), htmltools (>= 0.3.5), zoo (>= 1.7-10), xts (>= 0.9-7)
+# Suggests: testthat
+# Enhances: rmarkdown (>= 0.3.3), shiny (>= 0.10.2.1)
+# RoxygenNote: 5.0.1
+# NeedsCompilation: no
+# Packaged: 2017-01-04 13:43:02 UTC; jjallaire
+# Author: Dan Vanderkam [aut, cph] (dygraphs library in htmlwidgets/lib, http://dygraphs.com/), JJ Allaire [aut, cre] (R interface), RStudio [cph], Jonathan Owen [aut, cph], Daniel Gromer [aut, cph], Petr Shevtsov [aut, cph], Benoit Thieurmel [aut, cph], jQuery Foundation [cph] (jQuery library), jQuery contributors [ctb, cph] (jQuery library; authors listed in inst/htmlwidgets/lib/jquery/AUTHORS.txt)
+# Repository: CRAN
+# Date/Publication: 2017-01-04 16:34:56
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-e1071-feedstock contains modified content
diff --git a/r-e1071-feedstock/recipe/meta.yaml b/r-e1071-feedstock/recipe/meta.yaml
index ec101c4..e87f5ce 100644
--- a/r-e1071-feedstock/recipe/meta.yaml
+++ b/r-e1071-feedstock/recipe/meta.yaml
@@ -14,37 +14,55 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/e1071/e1071_{{ version }}.tar.gz
   sha256: f68601743b9b49e1d1f8b9ec9963d6500d66158417c53f65bf7232678d88c622
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: cluster, mlbench, nnet, randomForest, rpart, SparseM, xtable, Matrix, MASS
 requirements:
   build:
     - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('cxx') }}      # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}autoconf
+    - {{posix}}automake
+    - {{posix}}pkg-config
+    - {{posix}}make
     - r-class
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-class
-    - libgcc  # [not win]
 
 test:
   commands:
-    - $R -e "library('e1071')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('e1071')"           # [not win]
     - "\"%R%\" -e \"library('e1071')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=e1071
   license: GPL-2
-  summary: |
-    Functions for latent class analysis, short time Fourier transform, fuzzy clustering,
+  summary: Functions for latent class analysis, short time Fourier transform, fuzzy clustering,
     support vector machines, shortest path computation, bagged clustering, naive Bayes
     classifier, ...
   license_family: GPL2
@@ -53,3 +71,26 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: e1071
+# Version: 1.6-8
+# Title: Misc Functions of the Department of Statistics, Probability Theory Group (Formerly: E1071), TU Wien
+# Imports: graphics, grDevices, class, stats, methods, utils
+# Suggests: cluster, mlbench, nnet, randomForest, rpart, SparseM, xtable, Matrix, MASS
+# Authors@R: c(person(given = "David", family = "Meyer", role = c("aut", "cre"), email = "David.Meyer@R-project.org"), person(given = "Evgenia", family = "Dimitriadou", role = c("aut","cph")), person(given = "Kurt", family = "Hornik", role = "aut"), person(given = "Andreas", family = "Weingessel", role = "aut"), person(given = "Friedrich", family = "Leisch", role = "aut"), person(given = "Chih-Chung", family = "Chang", role = c("ctb","cph"), comment = "libsvm C++-code"), person(given = "Chih-Chen", family = "Lin", role = c("ctb","cph"), comment = "libsvm C++-code"))
+# Description: Functions for latent class analysis, short time Fourier transform, fuzzy clustering, support vector machines, shortest path computation, bagged clustering, naive Bayes classifier, ...
+# License: GPL-2
+# LazyLoad: yes
+# NeedsCompilation: yes
+# Packaged: 2017-02-01 16:13:21 UTC; meyer
+# Author: David Meyer [aut, cre], Evgenia Dimitriadou [aut, cph], Kurt Hornik [aut], Andreas Weingessel [aut], Friedrich Leisch [aut], Chih-Chung Chang [ctb, cph] (libsvm C++-code), Chih-Chen Lin [ctb, cph] (libsvm C++-code)
+# Maintainer: David Meyer <David.Meyer@R-project.org>
+# Repository: CRAN
+# Date/Publication: 2017-02-02 12:37:10
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-essentials-feedstock 9b95251..2f3c984:
diff --git a/r-essentials-feedstock/recipe/meta.yaml b/r-essentials-feedstock/recipe/meta.yaml
index ba9d2eb..5c1d479 100644
--- a/r-essentials-feedstock/recipe/meta.yaml
+++ b/r-essentials-feedstock/recipe/meta.yaml
@@ -1,71 +1,134 @@
 {% set name = "r-essentials" %}
-{% set version = "1.5.2" %}
+{% set version = "1.7.0" %}
+
+{% set r_base_ver = '3.4.2' %}
+{% set notebook_ver = '5.0.0' %}
+{% set r_broom_ver = '0.4.2' %}
+{% set r_caret_ver = '6.0_77' %}
+{% set r_data_table_ver = '1.10.4' %}
+{% set r_dbi_ver = '0.7' %}
+{% set r_dplyr_ver = '0.7.4' %}
+{% set r_forcats_ver = '0.2.0' %}
+{% set r_formatr_ver = '1.5' %}
+{% set r_ggplot2_ver = '2.2.1' %}
+{% set r_glmnet_ver = '2.0_13' %}
+{% set r_haven_ver = '1.1.0' %}
+{% set r_hms_ver = '0.3' %}
+{% set r_httr_ver = '1.3.1' %}
+{% set r_irkernel_ver = '0.8.9' %}
+{% set r_jsonlite_ver = '1.5' %}
+{% set r_lubridate_ver = '1.6.0' %}
+{% set r_magrittr_ver = '1.5' %}
+{% set r_modelr_ver = '0.1.1' %}
+{% set r_plyr_ver = '1.8.4' %}
+{% set r_purrr_ver = '0.2.3' %}
+{% set r_quantmod_ver = '0.4_11' %}
+{% set r_randomforest_ver = '4.6_12' %}
+{% set r_rbokeh_ver = '0.5.0' %}
+{% set r_readr_ver = '1.1.1' %}
+{% set r_readxl_ver = '1.0.0' %}
+{% set r_recommended_ver = '3.4.2' %}
+{% set r_reshape2_ver = '1.4.2' %}
+{% set r_rmarkdown_ver = '1.6' %}
+{% set r_rvest_ver = '0.3.2' %}
+{% set r_shiny_ver = '1.0.5' %}
+{% set r_stringr_ver = '1.2.0' %}
+{% set r_tibble_ver = '1.3.4' %}
+{% set r_tidyr_ver = '0.7.1' %}
+{% set r_tidyverse_ver = '1.1.1' %}
+{% set r_xml2_ver = '1.1.1' %}
+{% set r_zoo_ver = '1.8_0' %}
 
 package:
   name: {{ name }}
   version: {{ version }}
 
-build:
-  # r-rbokeh is not available under Windows
-  skip: true  # [win]
-  number: 0
-
 requirements:
-    run:
-        - r-base
-        - r-recommended >=3.3.2
-        - r-ggplot2 >=2.2.0
-        - r-plyr >=1.8.4
-        - r-reshape2 >=1.4.2
-        - r-dplyr >=0.5.0
-        - r-tidyr >=0.6.0
-        - r-caret >=6.0_73
-        - r-randomforest >=4.6_12
-        - r-data.table >=1.10.0
-        - r-quantmod >=0.4_7
-        - r-shiny >=0.14.2
-        - r-rmarkdown >=1.3
-        - r-glmnet >=2.0_5
-        - r-jsonlite >=1.1
-        - r-zoo >=1.7_13
-        - r-irkernel >=0.7.1
-        - r-rbokeh >=0.5.0
-        - r-formatr >=1.4
-        - notebook >=4.2.3
-        - r-tidyverse >=1.0.0
-        - r-dbi >=0.5_1
-        - r-broom >=0.4.1
-        - r-forcats >=0.1.1
-        - r-haven >=1.0.0
-        - r-hms >=0.3
-        - r-httr >=1.2.1
-        - r-jsonlite >=1.1
-        - r-lubridate >=1.6.0
-        - r-magrittr >=1.5
-        - r-modelr >=0.1.0
-        - r-purrr >=0.2.2
-        - r-readr >=1.0.0
-        - r-readxl >=0.1.1
-        - r-rvest >=0.3.2
-        - r-stringr >=1.1.0
-        - r-tibble >=1.2
-        - r-tidyr >=0.6.0
-        - r-xml2 >=1.0.0
+  build:
+    - r-base >={{r_base_ver}}
+    - notebook >={{notebook_ver}}
+    - r-broom >={{r_broom_ver}}
+    - r-caret >={{r_caret_ver}}
+    - r-data.table >={{r_data_table_ver}}
+    - r-dbi >={{r_dbi_ver}}
+    - r-dplyr >={{r_dplyr_ver}}
+    - r-forcats >={{r_forcats_ver}}
+    - r-formatr >={{r_formatr_ver}}
+    - r-ggplot2 >={{r_ggplot2_ver}}
+    - r-glmnet >={{r_glmnet_ver}}
+    - r-haven >={{r_haven_ver}}
+    - r-hms >={{r_hms_ver}}
+    - r-httr >={{r_httr_ver}}
+    - r-irkernel >={{r_irkernel_ver}}
+    - r-jsonlite >={{r_jsonlite_ver}}
+    - r-lubridate >={{r_lubridate_ver}}
+    - r-magrittr >={{r_magrittr_ver}}
+    - r-modelr >={{r_modelr_ver}}
+    - r-plyr >={{r_plyr_ver}}
+    - r-purrr >={{r_purrr_ver}}
+    - r-quantmod >={{r_quantmod_ver}}
+    - r-randomforest >={{r_randomforest_ver}}
+    - r-rbokeh >={{r_rbokeh_ver}}
+    - r-readr >={{r_readr_ver}}
+    - r-readxl >={{r_readxl_ver}}
+    - r-recommended >={{r_recommended_ver}}
+    - r-reshape2 >={{r_reshape2_ver}}
+    - r-rmarkdown >={{r_rmarkdown_ver}}
+    - r-rvest >={{r_rvest_ver}}
+    - r-shiny >={{r_shiny_ver}}
+    - r-stringr >={{r_stringr_ver}}
+    - r-tibble >={{r_tibble_ver}}
+    - r-tidyr >={{r_tidyr_ver}}
+    - r-tidyverse >={{r_tidyverse_ver}}
+    - r-xml2 >={{r_xml2_ver}}
+    - r-zoo >={{r_zoo_ver}}
 
-test:
-  commands:
-    - $R -e "library('ggplot2')"  # [not win]
-    - $R -e "library('tibble')"  # [not win]
-    - "\"%R%\" -e \"library('ggplot2')\""  # [win]
-    - "\"%R%\" -e \"library('tibble')\""  # [win]
+  run:
+    - r-base >={{r_base_ver}}
+    - notebook >={{notebook_ver}}
+    - r-broom >={{r_broom_ver}}
+    - r-caret >={{r_caret_ver}}
+    - r-data.table >={{r_data_table_ver}}
+    - r-dbi >={{r_dbi_ver}}
+    - r-dplyr >={{r_dplyr_ver}}
+    - r-forcats >={{r_forcats_ver}}
+    - r-formatr >={{r_formatr_ver}}
+    - r-ggplot2 >={{r_ggplot2_ver}}
+    - r-glmnet >={{r_glmnet_ver}}
+    - r-haven >={{r_haven_ver}}
+    - r-hms >={{r_hms_ver}}
+    - r-httr >={{r_httr_ver}}
+    - r-irkernel >={{r_irkernel_ver}}
+    - r-jsonlite >={{r_jsonlite_ver}}
+    - r-lubridate >={{r_lubridate_ver}}
+    - r-magrittr >={{r_magrittr_ver}}
+    - r-modelr >={{r_modelr_ver}}
+    - r-plyr >={{r_plyr_ver}}
+    - r-purrr >={{r_purrr_ver}}
+    - r-quantmod >={{r_quantmod_ver}}
+    - r-randomforest >={{r_randomforest_ver}}
+    - r-rbokeh >={{r_rbokeh_ver}}
+    - r-readr >={{r_readr_ver}}
+    - r-readxl >={{r_readxl_ver}}
+    - r-recommended >={{r_recommended_ver}}
+    - r-reshape2 >={{r_reshape2_ver}}
+    - r-rmarkdown >={{r_rmarkdown_ver}}
+    - r-rvest >={{r_rvest_ver}}
+    - r-shiny >={{r_shiny_ver}}
+    - r-stringr >={{r_stringr_ver}}
+    - r-tibble >={{r_tibble_ver}}
+    - r-tidyr >={{r_tidyr_ver}}
+    - r-tidyverse >={{r_tidyverse_ver}}
+    - r-xml2 >={{r_xml2_ver}}
+    - r-zoo >={{r_zoo_ver}}
 
 about:
-    home: https://github.com/conda-forge/r-essentials-feedstock
-    license: Various
-    license_family: Other
-    summary: Some essential packages for working with R. This was migrated from the 'r' channel.
+  summary: "Some essential packages for working with R"
+  license: Various
+  license_family: Other
 
 extra:
   recipe-maintainers:
-    - johanneskoester
     - bgruening
+    - johanneskoester
+    - mingwandroid
Submodule r-estimability-feedstock contains modified content
diff --git a/r-estimability-feedstock/recipe/meta.yaml b/r-estimability-feedstock/recipe/meta.yaml
index 14b914b..61eae97 100644
--- a/r-estimability-feedstock/recipe/meta.yaml
+++ b/r-estimability-feedstock/recipe/meta.yaml
@@ -14,14 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/estimability/estimability_{{ version }}.tar.gz
   sha256: 30a5b0abae9525209531fe8dea44750f6b296948d35ebbe55759e565ef02688d
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
@@ -31,9 +38,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('estimability')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('estimability')"           # [not win]
     - "\"%R%\" -e \"library('estimability')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=estimability
   license: GPL (>= 3)
@@ -45,3 +59,28 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: estimability
+# Type: Package
+# Title: Tools for Assessing Estimability of Linear Predictions
+# Version: 1.2
+# Date: 2016-11-16
+# Authors@R: c(person("Russell", "Lenth", role = c("aut", "cre", "cph"),  email = "russell-lenth@uiowa.edu"))
+# Depends: stats
+# Description: Provides tools for determining estimability of linear functions of regression coefficients,  and 'epredict' methods that handle non-estimable cases correctly.
+# LazyData: yes
+# ByteCompile: yes
+# License: GPL (>= 3)
+# NeedsCompilation: no
+# Packaged: 2016-11-19 01:48:37 UTC; rlenth
+# Author: Russell Lenth [aut, cre, cph]
+# Maintainer: Russell Lenth <russell-lenth@uiowa.edu>
+# Repository: CRAN
+# Date/Publication: 2016-11-19 10:13:47
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-evaluate-feedstock contains modified content
diff --git a/r-evaluate-feedstock/recipe/bld.bat b/r-evaluate-feedstock/recipe/bld.bat
new file mode 100644
index 0000000..8b10c94
--- /dev/null
+++ b/r-evaluate-feedstock/recipe/bld.bat
@@ -0,0 +1,8 @@
+"%R%" CMD INSTALL --build .
+if errorlevel 1 exit 1
+
+@rem Add more build steps here, if they are necessary.
+
+@rem See
+@rem http://docs.continuum.io/conda/build.html
+@rem for a list of environment variables that are set during the build process.
diff --git a/r-evaluate-feedstock/recipe/build.sh b/r-evaluate-feedstock/recipe/build.sh
new file mode 100644
index 0000000..0c37df6
--- /dev/null
+++ b/r-evaluate-feedstock/recipe/build.sh
@@ -0,0 +1,13 @@
+#!/bin/bash
+
+# R refuses to build packages that mark themselves as Priority: Recommended
+mv DESCRIPTION DESCRIPTION.old
+grep -v '^Priority: ' DESCRIPTION.old > DESCRIPTION
+
+$R CMD INSTALL --build .
+
+# Add more build steps here, if they are necessary.
+
+# See
+# http://docs.continuum.io/conda/build.html
+# for a list of environment variables that are set during the build process.
diff --git a/r-evaluate-feedstock/recipe/meta.yaml b/r-evaluate-feedstock/recipe/meta.yaml
index 0cb6cc2..1de3266 100644
--- a/r-evaluate-feedstock/recipe/meta.yaml
+++ b/r-evaluate-feedstock/recipe/meta.yaml
@@ -1,43 +1,90 @@
-{% set version = "0.10" %}
+{% set version = '0.10.1' %}
+
+{% set posix = 'm2-' if win else '' %}
+{% set native = 'm2w64-' if win else '' %}
 
 package:
   name: r-evaluate
-  version: {{ version }}
+  version: {{ version|replace("-", "_") }}
 
 source:
   fn: evaluate_{{ version }}.tar.gz
   url:
-    - http://cran.r-project.org/src/contrib/evaluate_{{ version }}.tar.gz
-    - http://cran.r-project.org/src/contrib/Archive/evaluate/evaluate_{{ version }}.tar.gz
-  sha256: 6163baeb382c2c1e87d4e36a2e986ef74673d8a92ea8508c39ac662ff3519657
+    - https://cran.r-project.org/src/contrib/evaluate_{{ version }}.tar.gz
+    - https://cran.r-project.org/src/contrib/Archive/evaluate/evaluate_{{ version }}.tar.gz
+  sha256: c9a763895d3f460dbf87c43a6469e4b41a251a74477df8c5d7e7d2b66cdd1b1c
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: True  # [win32]
-  script: R CMD INSTALL --build .
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
-requirements:  # [not win32]
-  build:  # [not win32]
-    - r-base  # [not win32]
-    - r-stringr >=0.6.2  # [not win32]
-  run:  # [not win32]
-    - r-base  # [not win32]
-    - r-stringr >=0.6.2  # [not win32]
+# Suggests: testthat, lattice, ggplot2
+requirements:
+  build:
+    - r-base
+    - r-stringr >=0.6.2
+
+  run:
+    - r-base
+    - r-stringr >=0.6.2
 
 test:
   commands:
-    - R -e "library('evaluate')"  # [not win]
-    - R -e \"library('evaluate')\"  # [win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('evaluate')"           # [not win]
+    - "\"%R%\" -e \"library('evaluate')\""  # [win]
+
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
 
 about:
   home: https://github.com/hadley/evaluate
-  license: MIT
-  license_file: LICENSE
-  summary: 'Parsing and evaluation tools that make it easy to recreate the command line behaviour of R.'
+  license: MIT + file LICENSE
+  summary: Parsing and evaluation tools that make it easy to recreate the command line behaviour
+    of R.
+  license_family: MIT
 
 extra:
   recipe-maintainers:
     - ocefpaf
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: evaluate
+# Type: Package
+# Title: Parsing and Evaluation Tools that Provide More Details than the Default
+# Version: 0.10.1
+# Date: 2017-06-24
+# Authors@R: c( person("Hadley", "Wickham", role = "aut"), person("Yihui", "Xie", role = c("cre", "ctb"), email = "xie@yihui.name"), person("Michael", "Lawrence", role = "ctb"), person("Thomas", "Kluyver", role = "ctb"), person("Barret", "Schloerke", role = "ctb"), person("Adam", "Ryczkowski", role = "ctb") )
+# Description: Parsing and evaluation tools that make it easy to recreate the command line behaviour of R.
+# License: MIT + file LICENSE
+# URL: https://github.com/hadley/evaluate
+# BugReports: https://github.com/hadley/evaluate/issues
+# Depends: R (>= 3.0.2)
+# Imports: methods, stringr (>= 0.6.2)
+# Suggests: testthat, lattice, ggplot2
+# RoxygenNote: 6.0.1
+# NeedsCompilation: no
+# Packaged: 2017-06-23 22:15:15 UTC; yihui
+# Author: Hadley Wickham [aut], Yihui Xie [cre, ctb], Michael Lawrence [ctb], Thomas Kluyver [ctb], Barret Schloerke [ctb], Adam Ryczkowski [ctb]
+# Maintainer: Yihui Xie <xie@yihui.name>
+# Repository: CRAN
+# Date/Publication: 2017-06-24 07:25:41 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-feather-feedstock contains modified content
diff --git a/r-feather-feedstock/recipe/meta.yaml b/r-feather-feedstock/recipe/meta.yaml
index da9c25a..f7df550 100644
--- a/r-feather-feedstock/recipe/meta.yaml
+++ b/r-feather-feedstock/recipe/meta.yaml
@@ -14,39 +14,57 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/feather/feather_{{ version }}.tar.gz
   sha256: f1069bf920e9bc3da6bf43542a3a7ccc3142544d759945115ecade69dd5ccde0
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: testthat, dplyr
 requirements:
   build:
     - r-base
+    - {{ compiler('cxx') }}      # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}autoconf
+    - {{posix}}automake
+    - {{posix}}pkg-config
+    - {{posix}}make
     - r-rcpp
     - r-hms
     - r-tibble
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-rcpp
     - r-hms
     - r-tibble
-    - libgcc  # [not win]
 
 test:
   commands:
-    - $R -e "library('feather')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('feather')"           # [not win]
     - "\"%R%\" -e \"library('feather')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/wesm/feather
-  license: Apache 2.0
+  license: Apache License 2.0
   summary: Read and write feather files, a lightweight binary columnar data store designed for
     maximum speed.
   license_family: APACHE
@@ -55,3 +73,32 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: feather
+# Title: R Bindings to the Feather 'API'
+# Version: 0.3.1
+# Authors@R: c( person("Hadley", "Wickham", , "hadley@rstudio.com", role = c("aut", "cre")), person("RStudio", role = "cph"), person("Feather developers", role = "ctb", comment = "Bundled feather library"), person("Google", role = "ctb", comment = "Bundled flatbuffers code"), person("LevelDB Authors", role = "ctb") )
+# Description: Read and write feather files, a lightweight binary columnar data store designed for maximum speed.
+# Encoding: UTF-8
+# License: Apache License 2.0
+# LazyData: true
+# URL: https://github.com/wesm/feather
+# BugReports: https://github.com/wesm/feather/issues
+# LinkingTo: Rcpp
+# Imports: Rcpp, tibble, hms
+# Suggests: testthat, dplyr
+# SystemRequirements: little-endian platform
+# RoxygenNote: 5.0.1
+# NeedsCompilation: yes
+# Packaged: 2016-11-08 22:45:11 UTC; kevin
+# Author: Hadley Wickham [aut, cre], RStudio [cph], Feather developers [ctb] (Bundled feather library), Google [ctb] (Bundled flatbuffers code), LevelDB Authors [ctb]
+# Maintainer: Hadley Wickham <hadley@rstudio.com>
+# Repository: CRAN
+# Date/Publication: 2016-11-09 00:35:01
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-findpython-feedstock contains modified content
diff --git a/r-findpython-feedstock/recipe/meta.yaml b/r-findpython-feedstock/recipe/meta.yaml
index 058ddf3..c902722 100644
--- a/r-findpython-feedstock/recipe/meta.yaml
+++ b/r-findpython-feedstock/recipe/meta.yaml
@@ -12,19 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/findpython_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/findpython/findpython_{{ version }}.tar.gz
+  sha256: b38b148521927b27a8fd480df797a4c6bc618407606f27bef07d7b93fb6a68f4
 
 
-  sha256: b38b148521927b27a8fd480df797a4c6bc618407606f27bef07d7b93fb6a68f4
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: True  # [win32]
-
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: testthat
 requirements:
   build:
     - r-base
@@ -34,20 +38,47 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('findpython')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('findpython')"           # [not win]
     - "\"%R%\" -e \"library('findpython')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
 
+  # requires:
+    # Put any additional test requirements here.
 
 about:
   home: https://github.com/trevorld/findpython
-  license: MIT
+  license: MIT + file LICENSE
   summary: Package designed to find an acceptable python binary.
   license_family: MIT
-  license_file: LICENSE
-
-
 
 extra:
   recipe-maintainers:
     - croth1
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: findpython
+# Type: Package
+# Title: Python Tools to Find an Acceptable Python Binary
+# Version: 1.0.2
+# Author: Trevor L Davis and Paul Gilbert.
+# Maintainer: Trevor L Davis <trevor.l.davis@gmail.com>
+# URL: https://github.com/trevorld/findpython
+# BugReports: https://github.com/trevorld/findpython/issues
+# Description: Package designed to find an acceptable python binary.
+# Suggests: testthat
+# License: MIT + file LICENSE
+# Collate: 'find_python_cmd.r'
+# RoxygenNote: 5.0.1
+# NeedsCompilation: no
+# Packaged: 2017-03-14 21:28:13 UTC; trevorld
+# Repository: CRAN
+# Date/Publication: 2017-03-15 05:25:12
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-forcats-feedstock contains modified content
diff --git a/r-forcats-feedstock/recipe/meta.yaml b/r-forcats-feedstock/recipe/meta.yaml
index 668a31a..bd261f2 100644
--- a/r-forcats-feedstock/recipe/meta.yaml
+++ b/r-forcats-feedstock/recipe/meta.yaml
@@ -14,14 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/forcats/forcats_{{ version }}.tar.gz
   sha256: b5bce370422d4c0ec9509249ae645373949bfbe9217cdf50dce2bfbdad9f7cd7
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: ggplot2, testthat, covr
 requirements:
   build:
     - r-base
@@ -35,14 +42,20 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('forcats')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('forcats')"           # [not win]
     - "\"%R%\" -e \"library('forcats')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://forcats.tidyverse.org, https://github.com/tidyverse/forcats
   license: GPL-3
-  summary: |
-    Helpers for reordering factor levels (including moving specified levels to front,
+  summary: Helpers for reordering factor levels (including moving specified levels to front,
     ordering by first appearance, reversing, and randomly shuffling), and tools for
     modifying factor levels (including collapsing rare levels into other, 'anonymising',
     and manually 'recoding').
@@ -52,3 +65,31 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: forcats
+# Title: Tools for Working with Categorical Variables (Factors)
+# Version: 0.2.0
+# Authors@R: c( person("Hadley", "Wickham", , "hadley@rstudio.com", role = c("aut", "cre")), person("RStudio", role = c("cph", "fnd")) )
+# Description: Helpers for reordering factor levels (including moving specified levels to front, ordering by first appearance, reversing, and randomly shuffling), and tools for modifying factor levels (including collapsing rare levels into other, 'anonymising', and manually 'recoding').
+# License: GPL-3
+# Encoding: UTF-8
+# LazyData: true
+# URL: http://forcats.tidyverse.org, https://github.com/tidyverse/forcats
+# BugReports: https://github.com/tidyverse/forcats/issues
+# Depends: R (>= 2.10)
+# Imports: tibble, magrittr
+# RoxygenNote: 5.0.1.9000
+# Suggests: ggplot2, testthat, covr
+# NeedsCompilation: no
+# Packaged: 2017-01-22 19:24:31 UTC; hornik
+# Author: Hadley Wickham [aut, cre], RStudio [cph, fnd]
+# Maintainer: Hadley Wickham <hadley@rstudio.com>
+# Repository: CRAN
+# Date/Publication: 2017-01-23 16:39:48
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-foreach-feedstock contains modified content
diff --git a/r-foreach-feedstock/recipe/meta.yaml b/r-foreach-feedstock/recipe/meta.yaml
index 38f6538..55731f7 100644
--- a/r-foreach-feedstock/recipe/meta.yaml
+++ b/r-foreach-feedstock/recipe/meta.yaml
@@ -12,16 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/foreach_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/foreach/foreach_{{ version }}.tar.gz
-
   sha256: 1ef03f770f726a62e3753f2402eb26b226245958fa99d570d003fc9e47d35881
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: randomForest
 requirements:
   build:
     - r-base
@@ -35,21 +42,56 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('foreach')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('foreach')"           # [not win]
     - "\"%R%\" -e \"library('foreach')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=foreach
-  license: Apache 2.0
+  license: Apache License (== 2.0)
   summary: Support for the foreach looping construct.  Foreach is an idiom that allows for iterating
     over elements in a collection, without the use of an explicit loop counter.  This
     package in particular is intended to be used for its return value, rather than for
     its side effects.  In that sense, it is similar to the standard lapply function,
     but doesn't require the evaluation of a function.  Using foreach without side effects
     also facilitates executing the loop in parallel.
-  license_family: Apache
+  license_family: APACHE
 
 extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: foreach
+# Type: Package
+# Title: Provides Foreach Looping Construct for R
+# Version: 1.4.3
+# Authors@R: c(person("Rich", "Calaway", role="cre", email="richcala@microsoft.com"), person("Revolution", "Analytics", role=c("aut", "cph")), person("Steve", "Weston", role="aut"))
+# Description: Support for the foreach looping construct.  Foreach is an idiom that allows for iterating over elements in a collection, without the use of an explicit loop counter.  This package in particular is intended to be used for its return value, rather than for its side effects.  In that sense, it is similar to the standard lapply function, but doesn't require the evaluation of a function.  Using foreach without side effects also facilitates executing the loop in parallel.
+# Depends: R (>= 2.5.0)
+# Imports: codetools, utils, iterators
+# Suggests: randomForest
+# Enhances: compiler, doMC, RUnit, doParallel
+# License: Apache License (== 2.0)
+# Author: Rich Calaway [cre], Revolution Analytics [aut, cph], Steve Weston [aut]
+# Maintainer: Rich Calaway <richcala@microsoft.com>
+# Repository: CRAN
+# Repository/R-Forge/Project: foreach
+# Repository/R-Forge/Revision: 27
+# Repository/R-Forge/DateTimeStamp: 2015-10-12 01:37:31
+# Date/Publication: 2015-10-13 09:12:53
+# NeedsCompilation: no
+# Packaged: 2015-10-12 02:26:29 UTC; rforge
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-forecast-feedstock contains modified content
diff --git a/r-forecast-feedstock/recipe/meta.yaml b/r-forecast-feedstock/recipe/meta.yaml
index 6ac736c..854f728 100644
--- a/r-forecast-feedstock/recipe/meta.yaml
+++ b/r-forecast-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '8.0' %}
+{% set version = '8.2' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,18 +12,32 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/forecast_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/forecast/forecast_{{ version }}.tar.gz
-  sha256: 75456318b8a202bfb3780ccfa24ab0d7264e96c6788fd14e01622fbf8556ad42
+  sha256: eb3fab64ed139d068e7d026cd3880f1b623f4153a832fb71845488fa75e8b812
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: testthat, knitr, rmarkdown, expsmooth, rticles
 requirements:
   build:
     - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('cxx') }}      # [not win]
+    - {{ compiler('fortran') }}  # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
     - r-rcpp >=0.11.0
     - r-rcpparmadillo >=0.2.35
     - r-colorspace
@@ -35,12 +49,10 @@ requirements:
     - r-timedate
     - r-tseries
     - r-zoo
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-rcpp >=0.11.0
     - r-rcpparmadillo >=0.2.35
     - r-colorspace
@@ -52,16 +64,22 @@ requirements:
     - r-timedate
     - r-tseries
     - r-zoo
-    - libgcc  # [not win]
 
 test:
   commands:
-    - $R -e "library('forecast')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('forecast')"           # [not win]
     - "\"%R%\" -e \"library('forecast')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
-  home: http://github.com/robjhyndman/forecast
-  license: GPL (>= 2)
+  home: http://pkg.robjhyndman.com/forecast, https://github.com/robjhyndman/forecast
+  license: GPL-3
   summary: Methods and tools for displaying and analysing univariate time series forecasts including
     exponential smoothing via state space models and automatic ARIMA modelling.
   license_family: GPL3
@@ -70,3 +88,33 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: forecast
+# Version: 8.2
+# Title: Forecasting Functions for Time Series and Linear Models
+# Description: Methods and tools for displaying and analysing univariate time series forecasts including exponential smoothing via state space models and automatic ARIMA modelling.
+# Depends: R (>= 3.0.2),
+# Imports: stats, graphics, tseries, fracdiff, Rcpp (>= 0.11.0), nnet, colorspace, parallel, ggplot2 (>= 2.0.0), magrittr, lmtest, zoo, timeDate
+# Suggests: testthat, knitr, rmarkdown, expsmooth, rticles
+# LinkingTo: Rcpp (>= 0.11.0), RcppArmadillo (>= 0.2.35)
+# LazyData: yes
+# ByteCompile: TRUE
+# Authors@R: c( person("Rob", "Hyndman", email="Rob.Hyndman@monash.edu", role=c("aut", "cre", "cph")), person("Mitchell", "O'Hara-Wild", role="aut"), person("Christoph", "Bergmeir", role="aut"), person("Slava", "Razbash", role="aut"), person("Earo", "Wang", role="aut") )
+# BugReports: https://github.com/robjhyndman/forecast/issues
+# License: GPL-3
+# URL: http://pkg.robjhyndman.com/forecast, https://github.com/robjhyndman/forecast
+# VignetteBuilder: knitr
+# RoxygenNote: 6.0.1.9000
+# NeedsCompilation: yes
+# Packaged: 2017-09-25 01:25:04 UTC; hyndman
+# Author: Rob Hyndman [aut, cre, cph], Mitchell O'Hara-Wild [aut], Christoph Bergmeir [aut], Slava Razbash [aut], Earo Wang [aut]
+# Maintainer: Rob Hyndman <Rob.Hyndman@monash.edu>
+# Repository: CRAN
+# Date/Publication: 2017-09-25 18:12:34 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-foreign-feedstock contains modified content
diff --git a/r-foreign-feedstock/recipe/meta.yaml b/r-foreign-feedstock/recipe/meta.yaml
index d6d215c..95a8623 100644
--- a/r-foreign-feedstock/recipe/meta.yaml
+++ b/r-foreign-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '0.8-67' %}
+{% set version = '0.8-69' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,39 +12,81 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/foreign_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/foreign/foreign_{{ version }}.tar.gz
-  sha256: ea797e171bdd5e80e31e40a7a1e93662a11c1a32934233f349165dff4b401f48
+  sha256: 13689f5ec1ab09e8973be81c7f1799b7de4313176072887a9fa0b5825aed3468
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('foreign')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('foreign')"           # [not win]
     - "\"%R%\" -e \"library('foreign')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=foreign
   license: GPL (>= 2)
-  summary: Functions for reading and writing data stored by some versions of Epi Info, Minitab,
-    S, SAS, SPSS, Stata, Systat and Weka and for reading and writing some dBase files.
+  summary: Reading and writing data stored by some versions of 'Epi Info', 'Minitab', 'S', 'SAS',
+    'SPSS', 'Stata', 'Systat', 'Weka', and for reading and writing some 'dBase' files.
   license_family: GPL3
 
 extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: foreign
+# Priority: recommended
+# Version: 0.8-69
+# Date: 2017-06-21
+# Title: Read Data Stored by 'Minitab', 'S', 'SAS', 'SPSS', 'Stata', 'Systat', 'Weka', 'dBase', ...
+# Depends: R (>= 3.0.0)
+# Imports: methods, utils, stats
+# Authors@R: c( person("R Core Team", email = "R-core@R-project.org", role = c("aut", "cph", "cre")), person("Roger", "Bivand", role = c("ctb", "cph")), person(c("Vincent", "J."), "Carey", role = c("ctb", "cph")), person("Saikat", "DebRoy", role = c("ctb", "cph")), person("Stephen", "Eglen", role = c("ctb", "cph")), person("Rajarshi", "Guha", role = c("ctb", "cph")), person("Swetlana", "Herbrandt", role = "ctb"), person("Nicholas", "Lewin-Koh", role = c("ctb", "cph")), person("Mark", "Myatt", role = c("ctb", "cph")), person("Ben", "Pfaff", role = "ctb"), person("Brian", "Quistorff", role = "ctb"), person("Frank", "Warmerdam", role = c("ctb", "cph")), person("Stephen", "Weigand", role = c("ctb", "cph")), person("Free Software Foundation, Inc.", role = "cph"))
+# Copyright: see file COPYRIGHTS
+# Description: Reading and writing data stored by some versions of 'Epi Info', 'Minitab', 'S', 'SAS', 'SPSS', 'Stata', 'Systat', 'Weka', and for reading and writing some 'dBase' files.
+# ByteCompile: yes
+# Biarch: yes
+# License: GPL (>= 2)
+# BugReports: https://bugs.r-project.org
+# NeedsCompilation: yes
+# Packaged: 2017-06-21 15:59:59 UTC; ligges
+# Author: R Core Team [aut, cph, cre], Roger Bivand [ctb, cph], Vincent J. Carey [ctb, cph], Saikat DebRoy [ctb, cph], Stephen Eglen [ctb, cph], Rajarshi Guha [ctb, cph], Swetlana Herbrandt [ctb], Nicholas Lewin-Koh [ctb, cph], Mark Myatt [ctb, cph], Ben Pfaff [ctb], Brian Quistorff [ctb], Frank Warmerdam [ctb, cph], Stephen Weigand [ctb, cph], Free Software Foundation, Inc. [cph]
+# Maintainer: R Core Team <R-core@R-project.org>
+# Repository: CRAN
+# Date/Publication: 2017-06-21 18:03:34 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-formatr-feedstock contains modified content
diff --git a/r-formatr-feedstock/recipe/meta.yaml b/r-formatr-feedstock/recipe/meta.yaml
index 405930a..f353143 100644
--- a/r-formatr-feedstock/recipe/meta.yaml
+++ b/r-formatr-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '1.4' %}
+{% set version = '1.5' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,16 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/formatR_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/formatR/formatR_{{ version }}.tar.gz
-  sha256: 6ec47a7b1f18efb5fd7559b81427363b66415d81cded9d5e7e2907e900b67ebb
+  sha256: 874c197ae3720ec11b44984a055655b99a698e1912104eb9034c11fdf6104da7
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: codetools, shiny, testit, rmarkdown, knitr
 requirements:
   build:
     - r-base
@@ -31,11 +38,18 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('formatR')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('formatR')"           # [not win]
     - "\"%R%\" -e \"library('formatR')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
-  home: http://yihui.name/formatR
+  home: https://yihui.name/formatR
   license: GPL
   summary: Provides a function tidy_source() to format R source code. Spaces and indent will
     be added to the code automatically, and comments will be preserved under certain
@@ -47,3 +61,31 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: formatR
+# Type: Package
+# Title: Format R Code Automatically
+# Version: 1.5
+# Date: 2017-04-26
+# Authors@R: c( person("Yihui", "Xie", email = "xie@yihui.name", role = c("aut", "cre")), person("Eugene", "Ha", role = "ctb"), person("Kohske", "Takahashi", role = "ctb"), person("Ed", "Lee", role = "ctb") )
+# Maintainer: Yihui Xie <xie@yihui.name>
+# Description: Provides a function tidy_source() to format R source code. Spaces and indent will be added to the code automatically, and comments will be preserved under certain conditions, so that R code will be more human-readable and tidy. There is also a Shiny app as a user interface in this package (see tidy_app()).
+# Depends: R (>= 3.0.2)
+# Suggests: codetools, shiny, testit, rmarkdown, knitr
+# License: GPL
+# URL: https://yihui.name/formatR
+# BugReports: https://github.com/yihui/formatR/issues
+# VignetteBuilder: knitr
+# RoxygenNote: 6.0.1
+# NeedsCompilation: no
+# Packaged: 2017-04-25 16:06:05 UTC; yihui
+# Author: Yihui Xie [aut, cre], Eugene Ha [ctb], Kohske Takahashi [ctb], Ed Lee [ctb]
+# Repository: CRAN
+# Date/Publication: 2017-04-25 23:31:46 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-formattable-feedstock contains modified content
diff --git a/r-formattable-feedstock/recipe/meta.yaml b/r-formattable-feedstock/recipe/meta.yaml
index 62d0896..a183808 100644
--- a/r-formattable-feedstock/recipe/meta.yaml
+++ b/r-formattable-feedstock/recipe/meta.yaml
@@ -14,13 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/formattable/formattable_{{ version }}.tar.gz
   sha256: 2e935699fb8c68f55cad824ee2ff76acdb6ee0f363a4342a6f9b0809ea93f2e8
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: testthat, DT, shiny, covr
 requirements:
   build:
     - r-base
@@ -28,6 +36,7 @@ requirements:
     - r-htmlwidgets
     - r-knitr
     - r-rmarkdown
+
   run:
     - r-base
     - r-htmltools
@@ -37,12 +46,19 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('formattable')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('formattable')"           # [not win]
     - "\"%R%\" -e \"library('formattable')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://renkun.me/formattable, https://github.com/renkun-ken/formattable
-  license: MIT
+  license: MIT + file LICENSE
   summary: Provides functions to create formattable vectors and data frames. 'Formattable' vectors
     are printed with text formatting, and formattable data frames are printed with multiple
     types of formatting in HTML to improve the readability of data presented in tabular
@@ -53,3 +69,33 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: formattable
+# Title: Create 'Formattable' Data Structures
+# Version: 0.2.0.1
+# Authors@R: c( person("Kun", "Ren", role = c("aut", "cre"), email = "ken@renkun.me"), person("Kenton", "Russell", role = "aut", email = "kent.russell@timelyportfolio.com") )
+# Description: Provides functions to create formattable vectors and data frames. 'Formattable' vectors are printed with text formatting, and formattable data frames are printed with multiple types of formatting in HTML to improve the readability of data presented in tabular form rendered in web pages.
+# Depends: R (> 3.0.2)
+# Date: 2016-08-05
+# Imports: methods, htmltools, htmlwidgets, knitr, rmarkdown
+# License: MIT + file LICENSE
+# LazyData: true
+# URL: https://renkun.me/formattable, https://github.com/renkun-ken/formattable
+# BugReports: https://github.com/renkun-ken/formattable/issues
+# ByteCompile: TRUE
+# Suggests: testthat, DT, shiny, covr
+# RoxygenNote: 5.0.1
+# VignetteBuilder: knitr
+# NeedsCompilation: no
+# Packaged: 2016-08-05 01:44:15 UTC; renkun
+# Author: Kun Ren [aut, cre], Kenton Russell [aut]
+# Maintainer: Kun Ren <ken@renkun.me>
+# Repository: CRAN
+# Date/Publication: 2016-08-05 11:53:18
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-formula-feedstock contains modified content
diff --git a/r-formula-feedstock/recipe/meta.yaml b/r-formula-feedstock/recipe/meta.yaml
index 1456717..a034d05 100644
--- a/r-formula-feedstock/recipe/meta.yaml
+++ b/r-formula-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '1.2-1' %}
+{% set version = '1.2-2' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,16 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/Formula_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/Formula/Formula_{{ version }}.tar.gz
-  sha256: 5db1ef55119b299c9d291e1c5c08e2d51b696303daf4e7295c38ff5fc428360a
+  sha256: 8def4600fb7457d38db8083733477501b54528974aa216e4adf8871bff4aa429
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
@@ -31,17 +38,46 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('Formula')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('Formula')"           # [not win]
     - "\"%R%\" -e \"library('Formula')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=Formula
   license: GPL-2 | GPL-3
   summary: Infrastructure for extended formulas with multiple parts on the right-hand side and/or
-    multiple responses on the left-hand side.
+    multiple responses on the left-hand side (see <DOI:10.18637/jss.v034.i01>).
   license_family: GPL3
 
 extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: Formula
+# Version: 1.2-2
+# Date: 2017-07-11
+# Title: Extended Model Formulas
+# Description: Infrastructure for extended formulas with multiple parts on the right-hand side and/or multiple responses on the left-hand side (see <DOI:10.18637/jss.v034.i01>).
+# Authors@R: c(person(given = "Achim", family = "Zeileis", role = c("aut", "cre"), email = "Achim.Zeileis@R-project.org"), person(given = "Yves", family = "Croissant", role = "aut", email = "Yves.Croissant@univ-reunion.fr"))
+# Depends: R (>= 2.0.0), stats
+# License: GPL-2 | GPL-3
+# NeedsCompilation: no
+# Packaged: 2017-07-10 23:09:07 UTC; zeileis
+# Author: Achim Zeileis [aut, cre], Yves Croissant [aut]
+# Maintainer: Achim Zeileis <Achim.Zeileis@R-project.org>
+# Repository: CRAN
+# Date/Publication: 2017-07-10 23:28:09 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-fracdiff-feedstock contains modified content
diff --git a/r-fracdiff-feedstock/recipe/meta.yaml b/r-fracdiff-feedstock/recipe/meta.yaml
index 944a169..2c10e77 100644
--- a/r-fracdiff-feedstock/recipe/meta.yaml
+++ b/r-fracdiff-feedstock/recipe/meta.yaml
@@ -14,29 +14,46 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/fracdiff/fracdiff_{{ version }}.tar.gz
   sha256: 983781cedc2b4e3ba9fa020213957d5133ae9cd6710bc61d6225728e2f6e850e
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: longmemo, urca
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('fortran') }}  # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('fracdiff')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('fracdiff')"           # [not win]
     - "\"%R%\" -e \"library('fracdiff')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=fracdiff
   license: GPL (>= 2)
@@ -48,3 +65,24 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: fracdiff
+# Version: 1.4-2
+# Date: 2012-12-01
+# Title: Fractionally differenced ARIMA aka ARFIMA(p,d,q) models
+# Author: S original by Chris Fraley, U.Washington, Seattle.  R port by Fritz Leisch at TU Wien; since 2003-12: Martin Maechler; fdGPH(), fdSperio(), etc by Valderio Reisen and Artur Lemonte.
+# Maintainer: Martin Maechler <maechler@stat.math.ethz.ch>
+# Description: Maximum likelihood estimation of the parameters of a fractionally differenced ARIMA(p,d,q) model (Haslett and Raftery, Appl.Statistics, 1989).
+# Suggests: longmemo, urca
+# License: GPL (>= 2)
+# Packaged: 2012-12-01 23:24:14 UTC; maechler
+# Repository: CRAN
+# Date/Publication: 2012-12-02 07:08:12
+# NeedsCompilation: yes
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-functional-feedstock contains modified content
diff --git a/r-functional-feedstock/recipe/meta.yaml b/r-functional-feedstock/recipe/meta.yaml
index 7ad4da1..0227972 100644
--- a/r-functional-feedstock/recipe/meta.yaml
+++ b/r-functional-feedstock/recipe/meta.yaml
@@ -14,14 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/functional/functional_{{ version }}.tar.gz
   sha256: 19b78e27c27b1081245222c42da4dd1cb65c5643e6da9d6c1a6e997755c21888
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
@@ -31,9 +38,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('functional')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('functional')"           # [not win]
     - "\"%R%\" -e \"library('functional')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=functional
   license: GPL (>= 2)
@@ -44,3 +58,25 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: functional
+# Type: Package
+# Title: Curry, Compose, and other higher-order functions
+# Version: 0.6
+# Date: 2014-07-15
+# Author: Peter Danenberg <pcd@roxygen.org>
+# Maintainer: Peter Danenberg <pcd@roxygen.org>
+# Description: Curry, Compose, and other higher-order functions
+# License: GPL (>= 2)
+# LazyLoad: yes
+# Packaged: 2014-07-15 23:41:35 UTC; danenberg
+# NeedsCompilation: no
+# Repository: CRAN
+# Date/Publication: 2014-07-16 07:38:04
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-fwdselect-feedstock contains modified content
diff --git a/r-fwdselect-feedstock/recipe/meta.yaml b/r-fwdselect-feedstock/recipe/meta.yaml
index 2e3fe88..b0cce4e 100644
--- a/r-fwdselect-feedstock/recipe/meta.yaml
+++ b/r-fwdselect-feedstock/recipe/meta.yaml
@@ -14,13 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/FWDselect/FWDselect_{{ version }}.tar.gz
   sha256: e77313b3066806333edec3904d05e4fd51b596737edc337ba4019ecf609d1070
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
@@ -34,12 +42,19 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('FWDselect')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('FWDselect')"           # [not win]
     - "\"%R%\" -e \"library('FWDselect')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://cran.r-project.org/package=FWDselect
-  license: MIT
+  license: MIT + file LICENSE
   summary: A simple method to select the best model or best subset of variables using different
     types of data (binary, Gaussian or Poisson) and applying it in different contexts
     (parametric or non-parametric).
@@ -49,3 +64,30 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: FWDselect
+# Title: Selecting Variables in Regression Models
+# Version: 2.1.0
+# Date: 2015-12-18
+# Author: Marta Sestelo [aut, cre], Nora M. Villanueva [aut], Javier Roca-Pardinas [aut]
+# Maintainer: Marta Sestelo <sestelo@uvigo.es>
+# Authors@R: c( person("Marta", "Sestelo",email= "sestelo@uvigo.es", role = c("aut", "cre")), person("Nora", "M. Villanueva",email= "nmvillanueva@uvigo.es", role = "aut"), person("Javier", "Roca-Pardinas",email= "roca@uvigo.es", role = "aut"))
+# Description: A simple method to select the best model or best subset of variables using different types of data (binary, Gaussian or Poisson) and applying it in different contexts (parametric or non-parametric).
+# URL: http://cran.r-project.org/package=FWDselect
+# BugReports: http://github.com/sestelo/fwdselect/issues
+# Depends: R (>= 3.1.0)
+# License: MIT + file LICENSE
+# LazyData: true
+# Imports: cvTools, mgcv, parallel, graphics, stats
+# RoxygenNote: 5.0.0
+# NeedsCompilation: no
+# Packaged: 2015-12-18 18:21:53 UTC; Sestelo
+# Repository: CRAN
+# Date/Publication: 2015-12-19 09:34:15
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-gdata-feedstock contains modified content
diff --git a/r-gdata-feedstock/recipe/meta.yaml b/r-gdata-feedstock/recipe/meta.yaml
index 6bb4129..0f95162 100644
--- a/r-gdata-feedstock/recipe/meta.yaml
+++ b/r-gdata-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '2.17.0' %}
+{% set version = '2.18.0' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,15 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/gdata_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/gdata/gdata_{{ version }}.tar.gz
-  sha256: 8097ec0e4868f6bf746f821cff7842f696e874bb3a84f1b2aa977ecd961c3e4e
+  sha256: 4b287f59f5bbf5fcbf18db16477852faac4a605b10c5284c46b93fa6e9918d7f
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: RUnit
 requirements:
   build:
     - r-base
@@ -32,16 +40,62 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('gdata')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('gdata')"           # [not win]
     - "\"%R%\" -e \"library('gdata')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=gdata
   license: GPL-2
-  summary: 'Various R programming tools for data manipulation, including'
+  summary: 'Various R programming tools for data manipulation, including: - medical unit conversions
+    (''ConvertMedUnits'', ''MedUnits''), - combining objects (''bindData'', ''cbindX'',
+    ''combine'', ''interleave''), - character vector operations (''centerText'', ''startsWith'',
+    ''trim''), - factor manipulation (''levels'', ''reorder.factor'', ''mapLevels''),
+    - obtaining information about R objects (''object.size'', ''elem'', ''env'', ''humanReadable'',
+    ''is.what'', ''ll'', ''keep'', ''ls.funs'', ''Args'',''nPairs'', ''nobs''), - manipulating
+    MS-Excel formatted files (''read.xls'', ''installXLSXsupport'', ''sheetCount'',
+    ''xlsFormats''), - generating fixed-width format files (''write.fwf''), - extricating
+    components of date & time objects (''getYear'', ''getMonth'', ''getDay'', ''getHour'',
+    ''getMin'', ''getSec''), - operations on columns of data frames  (''matchcols'',
+    ''rename.vars''), - matrix operations (''unmatrix'', ''upperTriangle'', ''lowerTriangle''),
+    - operations on vectors (''case'', ''unknownToNA'', ''duplicated2'', ''trimSum''),
+    - operations on data frames (''frameApply'', ''wideByFactor''), - value of last
+    evaluated expression (''ans''), and - wrapper for ''sample'' that ensures consistent
+    behavior for both scalar and vector arguments (''resample'').'
+
   license_family: GPL2
 
 extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: gdata
+# Title: Various R Programming Tools for Data Manipulation
+# Description: Various R programming tools for data manipulation, including: - medical unit conversions ('ConvertMedUnits', 'MedUnits'), - combining objects ('bindData', 'cbindX', 'combine', 'interleave'), - character vector operations ('centerText', 'startsWith', 'trim'), - factor manipulation ('levels', 'reorder.factor', 'mapLevels'), - obtaining information about R objects ('object.size', 'elem', 'env', 'humanReadable', 'is.what', 'll', 'keep', 'ls.funs', 'Args','nPairs', 'nobs'), - manipulating MS-Excel formatted files ('read.xls', 'installXLSXsupport', 'sheetCount', 'xlsFormats'), - generating fixed-width format files ('write.fwf'), - extricating components of date & time objects ('getYear', 'getMonth', 'getDay', 'getHour', 'getMin', 'getSec'), - operations on columns of data frames  ('matchcols', 'rename.vars'), - matrix operations ('unmatrix', 'upperTriangle', 'lowerTriangle'), - operations on vectors ('case', 'unknownToNA', 'duplicated2', 'trimSum'), - operations on data frames ('frameApply', 'wideByFactor'), - value of last evaluated expression ('ans'), and - wrapper for 'sample' that ensures consistent behavior for both scalar and vector arguments ('resample').
+# Depends: R (>= 2.3.0)
+# SystemRequirements: perl (>= 5.10.0)
+# Imports: gtools, stats, methods, utils
+# Version: 2.18.0
+# Date: 2017-06-05
+# Author: Gregory R. Warnes, Ben Bolker, Gregor Gorjanc, Gabor Grothendieck, Ales Korosec, Thomas Lumley, Don MacQueen, Arni Magnusson, Jim Rogers, and others
+# Maintainer: Gregory R. Warnes <greg@warnes.net>
+# License: GPL-2
+# NeedsCompilation: no
+# Suggests: RUnit
+# Packaged: 2017-06-05 21:27:42 UTC; gwarnes
+# Repository: CRAN
+# Date/Publication: 2017-06-06 15:34:19 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-getopt-feedstock contains modified content
diff --git a/r-getopt-feedstock/recipe/meta.yaml b/r-getopt-feedstock/recipe/meta.yaml
index 543dd45..8a45cd3 100644
--- a/r-getopt-feedstock/recipe/meta.yaml
+++ b/r-getopt-feedstock/recipe/meta.yaml
@@ -12,19 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/getopt_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/getopt/getopt_{{ version }}.tar.gz
+  sha256: f920baa2a0ef7082155c8b666851af0c77534af8b2ca0cd067e7d56fdf3ec501
 
 
-  sha256: f920baa2a0ef7082155c8b666851af0c77534af8b2ca0cd067e7d56fdf3ec501
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: True  # [win32]
-
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: testthat
 requirements:
   build:
     - r-base
@@ -34,10 +38,15 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('getopt')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('getopt')"           # [not win]
     - "\"%R%\" -e \"library('getopt')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
 
+  # requires:
+    # Put any additional test requirements here.
 
 about:
   home: https://github.com/trevorld/getopt
@@ -48,8 +57,30 @@ about:
     option and usage, support for default values, positional argument support, etc.
   license_family: GPL3
 
-
-
 extra:
   recipe-maintainers:
     - croth1
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: getopt
+# Type: Package
+# Title: C-like getopt behavior.
+# Version: 1.20.0
+# Author: Allen Day. Contributions from Trevor L Davis.
+# Maintainer: Trevor L Davis <trevor.l.davis@stanford.edu>
+# URL: https://github.com/trevorld/getopt
+# BugReports: https://github.com/trevorld/getopt/issues
+# Description: Package designed to be used with Rscript to write ``#!'' shebang scripts that accept short and long flags/options. Many users will prefer using instead the packages optparse or argparse which add extra features like automatically generated help option and usage, support for default values, positional argument support, etc.
+# License: GPL (>= 2)
+# Suggests: testthat
+# Collate: 'getopt.R' 'utils.R'
+# Packaged: 2013-08-30 07:02:26 UTC; trevorld
+# NeedsCompilation: no
+# Repository: CRAN
+# Date/Publication: 2013-08-30 10:36:29
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-ggplot2-feedstock contains modified content
diff --git a/r-ggplot2-feedstock/recipe/meta.yaml b/r-ggplot2-feedstock/recipe/meta.yaml
index 2fb1c11..d04105d 100644
--- a/r-ggplot2-feedstock/recipe/meta.yaml
+++ b/r-ggplot2-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '2.2.0' %}
+{% set version = '2.2.1' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,16 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/ggplot2_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/ggplot2/ggplot2_{{ version }}.tar.gz
-  sha256: 64d7d9085573b439295bcb58ea4d39ba5d71911dbb501f1e7a30d13740ea2cda
+  sha256: 5fbc89fec3160ad14ba90bd545b151c7a2e7baad021c0ab4b950ecd6043a8314
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: covr, ggplot2movies, hexbin, Hmisc, lattice, mapproj, maps, maptools, mgcv, multcomp, nlme, testthat (>= 0.11.0), quantreg, knitr, rpart, rmarkdown, svglite
 requirements:
   build:
     - r-base
@@ -47,13 +54,19 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('ggplot2')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('ggplot2')"           # [not win]
     - "\"%R%\" -e \"library('ggplot2')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://ggplot2.tidyverse.org, https://github.com/tidyverse/ggplot2
-  license: GPL-2
-  license_file: LICENSE
+  license: GPL-2 | file LICENSE
   summary: A system for 'declaratively' creating graphics, based on "The Grammar of Graphics".
     You provide the data, tell 'ggplot2' how to map variables to aesthetics, what graphical
     primitives to use, and it takes care of the details.
@@ -64,3 +77,33 @@ extra:
     - johanneskoester
     - bgruening
     - ocefpaf
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: ggplot2
+# Version: 2.2.1
+# Title: Create Elegant Data Visualisations Using the Grammar of Graphics
+# Description: A system for 'declaratively' creating graphics, based on "The Grammar of Graphics". You provide the data, tell 'ggplot2' how to map variables to aesthetics, what graphical primitives to use, and it takes care of the details.
+# Authors@R: c( person("Hadley", "Wickham", , "hadley@rstudio.com", c("aut", "cre")), person("Winston", "Chang", , "winston@rstudio.com", "aut"), person("RStudio", role = c("cph")) )
+# Depends: R (>= 3.1)
+# Imports: digest, grid, gtable (>= 0.1.1), MASS, plyr (>= 1.7.1), reshape2, scales (>= 0.4.1), stats, tibble, lazyeval
+# Suggests: covr, ggplot2movies, hexbin, Hmisc, lattice, mapproj, maps, maptools, mgcv, multcomp, nlme, testthat (>= 0.11.0), quantreg, knitr, rpart, rmarkdown, svglite
+# Enhances: sp
+# License: GPL-2 | file LICENSE
+# URL: http://ggplot2.tidyverse.org, https://github.com/tidyverse/ggplot2
+# BugReports: https://github.com/tidyverse/ggplot2/issues
+# LazyData: true
+# Collate: 'ggproto.r' 'aaa-.r' 'aes-calculated.r' 'aes-colour-fill-alpha.r' 'aes-group-order.r' 'aes-linetype-size-shape.r' 'aes-position.r' 'utilities.r' 'aes.r' 'legend-draw.r' 'geom-.r' 'annotation-custom.r' 'annotation-logticks.r' 'geom-polygon.r' 'geom-map.r' 'annotation-map.r' 'geom-raster.r' 'annotation-raster.r' 'annotation.r' 'autoplot.r' 'axis-secondary.R' 'bench.r' 'bin.R' 'coord-.r' 'coord-cartesian-.r' 'coord-fixed.r' 'coord-flip.r' 'coord-map.r' 'coord-munch.r' 'coord-polar.r' 'coord-quickmap.R' 'coord-transform.r' 'data.R' 'facet-.r' 'facet-grid-.r' 'facet-null.r' 'facet-wrap.r' 'fortify-lm.r' 'fortify-map.r' 'fortify-multcomp.r' 'fortify-spatial.r' 'fortify.r' 'stat-.r' 'geom-abline.r' 'geom-rect.r' 'geom-bar.r' 'geom-bin2d.r' 'geom-blank.r' 'geom-boxplot.r' 'geom-col.r' 'geom-path.r' 'geom-contour.r' 'geom-count.r' 'geom-crossbar.r' 'geom-segment.r' 'geom-curve.r' 'geom-defaults.r' 'geom-ribbon.r' 'geom-density.r' 'geom-density2d.r' 'geom-dotplot.r' 'geom-errorbar.r' 'geom-errorbarh.r' 'geom-freqpoly.r' 'geom-hex.r' 'geom-histogram.r' 'geom-hline.r' 'geom-jitter.r' 'geom-label.R' 'geom-linerange.r' 'geom-point.r' 'geom-pointrange.r' 'geom-quantile.r' 'geom-rug.r' 'geom-smooth.r' 'geom-spoke.r' 'geom-text.r' 'geom-tile.r' 'geom-violin.r' 'geom-vline.r' 'ggplot2.r' 'grob-absolute.r' 'grob-dotstack.r' 'grob-null.r' 'grouping.r' 'guide-colorbar.r' 'guide-legend.r' 'guides-.r' 'guides-axis.r' 'guides-grid.r' 'hexbin.R' 'labeller.r' 'labels.r' 'layer.r' 'layout.R' 'limits.r' 'margins.R' 'plot-build.r' 'plot-construction.r' 'plot-last.r' 'plot.r' 'position-.r' 'position-collide.r' 'position-dodge.r' 'position-identity.r' 'position-jitter.r' 'position-jitterdodge.R' 'position-nudge.R' 'position-stack.r' 'quick-plot.r' 'range.r' 'save.r' 'scale-.r' 'scale-alpha.r' 'scale-brewer.r' 'scale-continuous.r' 'scale-date.r' 'scale-discrete-.r' 'scale-gradient.r' 'scale-grey.r' 'scale-hue.r' 'scale-identity.r' 'scale-linetype.r' 'scale-manual.r' 'scale-shape.r' 'scale-size.r' 'scale-type.R' 'scales-.r' 'stat-bin.r' 'stat-bin2d.r' 'stat-bindot.r' 'stat-binhex.r' 'stat-boxplot.r' 'stat-contour.r' 'stat-count.r' 'stat-density-2d.r' 'stat-density.r' 'stat-ecdf.r' 'stat-ellipse.R' 'stat-function.r' 'stat-identity.r' 'stat-qq.r' 'stat-quantile.r' 'stat-smooth-methods.r' 'stat-smooth.r' 'stat-sum.r' 'stat-summary-2d.r' 'stat-summary-bin.R' 'stat-summary-hex.r' 'stat-summary.r' 'stat-unique.r' 'stat-ydensity.r' 'summary.r' 'theme-elements.r' 'theme.r' 'theme-defaults.r' 'theme-current.R' 'translate-qplot-ggplot.r' 'translate-qplot-lattice.r' 'utilities-break.r' 'utilities-grid.r' 'utilities-help.r' 'utilities-matrix.r' 'utilities-resolution.r' 'utilities-table.r' 'zxx.r' 'zzz.r'
+# VignetteBuilder: knitr
+# RoxygenNote: 5.0.1.9000
+# NeedsCompilation: no
+# Packaged: 2016-12-30 17:10:52 UTC; hadley
+# Author: Hadley Wickham [aut, cre], Winston Chang [aut], RStudio [cph]
+# Maintainer: Hadley Wickham <hadley@rstudio.com>
+# Repository: CRAN
+# Date/Publication: 2016-12-30 22:45:17
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-ggvis-feedstock contains modified content
diff --git a/r-ggvis-feedstock/recipe/meta.yaml b/r-ggvis-feedstock/recipe/meta.yaml
index e8251da..f27a3c9 100644
--- a/r-ggvis-feedstock/recipe/meta.yaml
+++ b/r-ggvis-feedstock/recipe/meta.yaml
@@ -14,14 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/ggvis/ggvis_{{ version }}.tar.gz
   sha256: 34d517783016aaa1c4bef8972f4c06df5cd9ca0568035b647e60a8369043ecdc
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: MASS, mgcv, lubridate, testthat (>= 0.8.1), knitr (>= 1.6), rmarkdown
 requirements:
   build:
     - r-base
@@ -45,12 +52,19 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('ggvis')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('ggvis')"           # [not win]
     - "\"%R%\" -e \"library('ggvis')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://ggvis.rstudio.com/
-  license: GPL-2
+  license: GPL-2 | file LICENSE
   summary: An implementation of an interactive grammar of graphics, taking the best parts of
     'ggplot2', combining them with the reactive framework of 'shiny' and drawing web
     graphics using 'vega'.
@@ -60,3 +74,30 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: ggvis
+# Title: Interactive Grammar of Graphics
+# Description: An implementation of an interactive grammar of graphics, taking the best parts of 'ggplot2', combining them with the reactive framework of 'shiny' and drawing web graphics using 'vega'.
+# Version: 0.4.3
+# Authors@R: c( person("Winston", "Chang", role = c("aut", "cre"), email = "winston@rstudio.com"), person("Hadley", "Wickham", role = "aut", email = "hadley@rstudio.com"), person(family = "RStudio", role = "cph"), person(family = "jQuery Foundation", role = "cph", comment = "jQuery library and jQuery UI library"), person(family = "jQuery contributors", role = c("ctb", "cph"), comment = "jQuery library; authors listed in inst/www/lib/jquery/AUTHORS.txt"), person(family = "jQuery UI contributors", role = c("ctb", "cph"), comment = "jQuery UI library; authors listed in inst/www/lib/jquery-ui/AUTHORS.txt"), person("Mike", "Bostock", role = c("ctb", "cph"), comment = "D3 library"), person(family = "D3 contributors", role = "ctb", comment = "D3 library; authors listed at https://github.com/mbostock/d3/graphs/contributors"), person(family = "Trifacta Inc.", role = "cph", comment = "Vega library"), person(family = "Vega contributors", role = "ctb", comment = "Vega library; authors listed at https://github.com/trifacta/vega/graphs/contributors"), person("Sebastian", "Decima", role = c("ctb", "cph"), comment = "javascript-detect-element-resize library") )
+# URL: http://ggvis.rstudio.com/
+# Depends: R (>= 3.0)
+# Imports: assertthat, jsonlite (>= 0.9.11), shiny (>= 0.11.1), magrittr, dplyr (>= 0.4.0), lazyeval, htmltools (>= 0.2.4), methods
+# Suggests: MASS, mgcv, lubridate, testthat (>= 0.8.1), knitr (>= 1.6), rmarkdown
+# Encoding: UTF-8
+# License: GPL-2 | file LICENSE
+# LazyData: true
+# RoxygenNote: 5.0.1
+# NeedsCompilation: no
+# Packaged: 2016-07-22 16:15:28 UTC; winston
+# Author: Winston Chang [aut, cre], Hadley Wickham [aut], RStudio [cph], jQuery Foundation [cph] (jQuery library and jQuery UI library), jQuery contributors [ctb, cph] (jQuery library; authors listed in inst/www/lib/jquery/AUTHORS.txt), jQuery UI contributors [ctb, cph] (jQuery UI library; authors listed in inst/www/lib/jquery-ui/AUTHORS.txt), Mike Bostock [ctb, cph] (D3 library), D3 contributors [ctb] (D3 library; authors listed at https://github.com/mbostock/d3/graphs/contributors), Trifacta Inc. [cph] (Vega library), Vega contributors [ctb] (Vega library; authors listed at https://github.com/trifacta/vega/graphs/contributors), Sebastian Decima [ctb, cph] (javascript-detect-element-resize library)
+# Maintainer: Winston Chang <winston@rstudio.com>
+# Repository: CRAN
+# Date/Publication: 2016-07-22 18:45:42
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-gistr-feedstock contains modified content
diff --git a/r-gistr-feedstock/recipe/meta.yaml b/r-gistr-feedstock/recipe/meta.yaml
index 7de07e0..36c4638 100644
--- a/r-gistr-feedstock/recipe/meta.yaml
+++ b/r-gistr-feedstock/recipe/meta.yaml
@@ -14,13 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/gistr/gistr_{{ version }}.tar.gz
   sha256: 51771a257379a17552d0c88ada72ca6263954bbe896997f8a66cde3bf0b83ce0
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: roxygen2 (>= 6.0.1), git2r, testthat
 requirements:
   build:
     - r-base
@@ -31,6 +39,7 @@ requirements:
     - r-knitr
     - r-magrittr
     - r-rmarkdown
+
   run:
     - r-base
     - r-assertthat
@@ -43,20 +52,26 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('gistr')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('gistr')"           # [not win]
     - "\"%R%\" -e \"library('gistr')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/ropensci/gistr
-  license: MIT
-  summary: |
-    Work with ''GitHub'' ''gists'' from ''R'' (e.g.,  <http://en.wikipedia.org/wiki/GitHub#Gist>,  <https://help.github.com/articles/about-gists/>).
+  license: MIT + file LICENSE
+  summary: 'Work with ''GitHub'' ''gists'' from ''R'' (e.g.,  <http://en.wikipedia.org/wiki/GitHub#Gist>,  <https://help.github.com/articles/about-gists/>).
     A ''gist'' is simply one or more files with code/text/images/etc. This package allows
     the user to create new ''gists'', update ''gists'' with new files, rename files,
     delete files, get and delete ''gists'', star and ''un-star'' ''gists'', fork ''gists'',
     open a ''gist'' in your default browser, get embed code for a ''gist'', list ''gist''
     ''commits'', and get rate limit information when ''authenticated''. Some requests
-    require authentication and some do not.
+    require authentication and some do not. ''Gists'' website:  <https://gist.github.com/>.'
 
   license_family: MIT
 
@@ -64,3 +79,29 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: gistr
+# Title: Work with 'GitHub' 'Gists'
+# Description: Work with 'GitHub' 'gists' from 'R' (e.g.,  <http://en.wikipedia.org/wiki/GitHub#Gist>,  <https://help.github.com/articles/about-gists/>). A 'gist' is simply one or more files with code/text/images/etc. This package allows the user to create new 'gists', update 'gists' with new files, rename files, delete files, get and delete 'gists', star and 'un-star' 'gists', fork 'gists', open a 'gist' in your default browser, get embed code for a 'gist', list 'gist' 'commits', and get rate limit information when 'authenticated'. Some requests require authentication and some do not. 'Gists' website:  <https://gist.github.com/>.
+# Version: 0.4.0
+# Authors@R: c( person("Scott", "Chamberlain", role = c("aut", "cre"), email = "myrmecocystus@gmail.com"), person("Ramnath", "Vaidyanathan", role = "aut"), person("Karthik", "Ram", role = "aut") )
+# License: MIT + file LICENSE
+# URL: https://github.com/ropensci/gistr
+# BugReports: https://github.com/ropensci/gistr/issues
+# VignetteBuilder: knitr
+# Imports: jsonlite (>= 1.4), httr (>= 1.2.0), magrittr, assertthat, knitr, rmarkdown, dplyr
+# Suggests: roxygen2 (>= 6.0.1), git2r, testthat
+# RoxygenNote: 6.0.1
+# NeedsCompilation: no
+# Packaged: 2017-04-20 19:42:16 UTC; sacmac
+# Author: Scott Chamberlain [aut, cre], Ramnath Vaidyanathan [aut], Karthik Ram [aut]
+# Maintainer: Scott Chamberlain <myrmecocystus@gmail.com>
+# Repository: CRAN
+# Date/Publication: 2017-04-20 23:40:22 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-git2r-feedstock contains modified content
diff --git a/r-git2r-feedstock/recipe/build.sh b/r-git2r-feedstock/recipe/build.sh
index 4bc8e18..655ff63 100644
--- a/r-git2r-feedstock/recipe/build.sh
+++ b/r-git2r-feedstock/recipe/build.sh
@@ -1,3 +1,4 @@
 #!/bin/bash
 
-$R CMD INSTALL --build .
+$R CMD INSTALL --build --configure-args='--with-zlib-include=${PREFIX}/include --with-zlib-lib=${PREFIX}/lib' .
+
diff --git a/r-git2r-feedstock/recipe/meta.yaml b/r-git2r-feedstock/recipe/meta.yaml
index b180032..a462c47 100644
--- a/r-git2r-feedstock/recipe/meta.yaml
+++ b/r-git2r-feedstock/recipe/meta.yaml
@@ -14,31 +14,52 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/git2r/git2r_{{ version }}.tar.gz
   sha256: 380121c31dfb53f3417f9014e841a3b0ff8889f896375295984ea9daec724673
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: getPass
 requirements:
   build:
     - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}autoconf
+    - {{posix}}automake
+    - {{posix}}pkg-config
+    - {{posix}}make
     - {{native}}openssl
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{native}}zlib
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - {{native}}openssl
+    - {{native}}zlib
 
 test:
   commands:
-    - $R -e "library('git2r')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('git2r')"           # [not win]
     - "\"%R%\" -e \"library('git2r')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/ropensci/git2r
   license: GPL-2
@@ -52,3 +73,34 @@ extra:
     - jdblischak
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: git2r
+# Title: Provides Access to Git Repositories
+# Description: Interface to the 'libgit2' library, which is a pure C implementation of the 'Git' core methods. Provides access to 'Git' repositories to extract data and running some basic 'Git' commands.
+# Version: 0.19.0
+# License: GPL-2
+# Copyright: See COPYRIGHTS file.
+# URL: https://github.com/ropensci/git2r
+# BugReports: https://github.com/ropensci/git2r/issues
+# Maintainer: Stefan Widgren <stefan.widgren@gmail.com>
+# Author: See AUTHORS file.
+# Imports: graphics, utils
+# Depends: R (>= 3.0.2), methods
+# Suggests: getPass
+# Type: Package
+# LazyData: true
+# Biarch: true
+# NeedsCompilation: yes
+# SystemRequirements: zlib headers and library. OpenSSL headers and library. LibSSH2 (optional on non-Windows) to enable the SSH transport.
+# Collate: 'S4_classes.r' 'blame.r' 'blob.r' 'branch.r' 'bundle_r_package.r' 'checkout.r' 'commit.r' 'config.r' 'contributions.r' 'credential.r' 'diff.r' 'fetch.r' 'git2r.r' 'index.r' 'libgit2.r' 'merge.r' 'note.r' 'odb.r' 'plot.r' 'pull.r' 'punch_card.r' 'refspec.r' 'push.r' 'reference.r' 'reflog.r' 'remote.r' 'repository.r' 'reset.r' 'revparse.r' 'signature.r' 'stash.r' 'status.r' 'tag.r' 'time.r' 'tree.r' 'when.r'
+# RoxygenNote: 6.0.1
+# Packaged: 2017-07-19 09:49:20 UTC; stefan
+# Repository: CRAN
+# Date/Publication: 2017-07-19 21:48:49 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-glmnet-feedstock contains modified content
diff --git a/r-glmnet-feedstock/recipe/meta.yaml b/r-glmnet-feedstock/recipe/meta.yaml
index 0e38867..840b777 100644
--- a/r-glmnet-feedstock/recipe/meta.yaml
+++ b/r-glmnet-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '2.0-5' %}
+{% set version = '2.0-13' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,47 +12,92 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/glmnet_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/glmnet/glmnet_{{ version }}.tar.gz
-  sha256: 2ca95352c8fbd93aa7800f3d972ee6c1a5fcfeabc6be8c10deee0cb457fd77b1
+  sha256: f3288dcaddb2f7014d42b755bede6563f73c17bc87f8292c2ef7776cb9b9b8fd
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: survival, knitr, lars
 requirements:
   build:
     - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('fortran') }}  # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}autoconf
+    - {{posix}}automake
+    - {{posix}}pkg-config
+    - {{posix}}make
     - r-matrix >=1.0_6
     - r-foreach
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-matrix >=1.0_6
     - r-foreach
-    - libgcc  # [not win]
 
 test:
   commands:
-    - $R -e "library('glmnet')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('glmnet')"           # [not win]
     - "\"%R%\" -e \"library('glmnet')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://www.jstatsoft.org/v33/i01/.
   license: GPL-2
-  summary: 'Extremely efficient procedures for fitting the entire lasso or elastic-net regularization
+  summary: Extremely efficient procedures for fitting the entire lasso or elastic-net regularization
     path for linear regression, logistic and multinomial regression models, Poisson
     regression and the Cox model. Two recent additions are the multiple-response Gaussian,
-    and the grouped multinomial. The algorithm uses cyclical coordinate descent in a
-    path-wise fashion, as described in the paper linked to via the URL below. '
-
+    and the grouped multinomial regression. The algorithm uses cyclical coordinate descent
+    in a path-wise fashion, as described in the paper linked to via the URL below.
   license_family: GPL2
 
 extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: glmnet
+# Type: Package
+# Title: Lasso and Elastic-Net Regularized Generalized Linear Models
+# Version: 2.0-13
+# Date: 2017-09-21
+# Author: Jerome Friedman [aut, cre], Trevor Hastie [aut, cre], Noah Simon [aut, ctb], Junyang Qian [ctb], Rob Tibshirani [aut, cre]
+# Maintainer: Trevor Hastie <hastie@stanford.edu>
+# Depends: Matrix (>= 1.0-6), utils, foreach
+# Imports: methods
+# Suggests: survival, knitr, lars
+# Description: Extremely efficient procedures for fitting the entire lasso or elastic-net regularization path for linear regression, logistic and multinomial regression models, Poisson regression and the Cox model. Two recent additions are the multiple-response Gaussian, and the grouped multinomial regression. The algorithm uses cyclical coordinate descent in a path-wise fashion, as described in the paper linked to via the URL below.
+# License: GPL-2
+# VignetteBuilder: knitr
+# URL: http://www.jstatsoft.org/v33/i01/.
+# NeedsCompilation: yes
+# Packaged: 2017-09-21 22:42:34 UTC; hastie
+# Repository: CRAN
+# Date/Publication: 2017-09-22 05:43:14 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-glue-feedstock contains modified content
diff --git a/r-glue-feedstock/recipe/meta.yaml b/r-glue-feedstock/recipe/meta.yaml
index 005fb9e..2b59744 100644
--- a/r-glue-feedstock/recipe/meta.yaml
+++ b/r-glue-feedstock/recipe/meta.yaml
@@ -14,32 +14,46 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/glue/glue_{{ version }}.tar.gz
   sha256: 309eedfd84018f3fa24339aa6ee87ba7af88f941edd450f2879fe7e317ae5c05
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: testthat, covr, magrittr
 requirements:
   build:
-    - {{ compiler('c') }}  # [not win]
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
 
   run:
     - r-base
-    - {{native}}gcc-libs   # [win]
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('glue')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('glue')"           # [not win]
     - "\"%R%\" -e \"library('glue')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/tidyverse/glue
-  license: MIT
+  license: MIT + file LICENSE
   summary: An implementation of interpreted string literals, inspired by Python's Literal String
     Interpolation <https://www.python.org/dev/peps/pep-0498/> and Docstrings <https://www.python.org/dev/peps/pep-0257/>
     and Julia's Triple-Quoted String Literals <https://docs.julialang.org/en/stable/manual/strings/#triple-quoted-string-literals>.
@@ -51,3 +65,31 @@ extra:
     - bgruening
     - daler
     - jdblischak
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: glue
+# Title: Interpreted String Literals
+# Version: 1.1.1
+# Authors@R: person("Jim", "Hester", email = "james.f.hester@gmail.com", role = c("aut", "cre"))
+# Description: An implementation of interpreted string literals, inspired by Python's Literal String Interpolation <https://www.python.org/dev/peps/pep-0498/> and Docstrings <https://www.python.org/dev/peps/pep-0257/> and Julia's Triple-Quoted String Literals <https://docs.julialang.org/en/stable/manual/strings/#triple-quoted-string-literals>.
+# Depends: R (>= 3.0.0)
+# Suggests: testthat, covr, magrittr
+# Imports: methods
+# License: MIT + file LICENSE
+# Encoding: UTF-8
+# LazyData: true
+# RoxygenNote: 6.0.1
+# URL: https://github.com/tidyverse/glue
+# BugReports: https://github.com/tidyverse/glue/issues
+# NeedsCompilation: yes
+# Packaged: 2017-06-16 20:43:57 UTC; jhester
+# Author: Jim Hester [aut, cre]
+# Maintainer: Jim Hester <james.f.hester@gmail.com>
+# Repository: CRAN
+# Date/Publication: 2017-06-21 16:58:04 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
diff --git a/r-gmp-feedstock/recipe/bld.bat b/r-gmp-feedstock/recipe/bld.bat
index e69de29..8b10c94 100644
--- a/r-gmp-feedstock/recipe/bld.bat
+++ b/r-gmp-feedstock/recipe/bld.bat
@@ -0,0 +1,8 @@
+"%R%" CMD INSTALL --build .
+if errorlevel 1 exit 1
+
+@rem Add more build steps here, if they are necessary.
+
+@rem See
+@rem http://docs.continuum.io/conda/build.html
+@rem for a list of environment variables that are set during the build process.
diff --git a/r-gmp-feedstock/recipe/build.sh b/r-gmp-feedstock/recipe/build.sh
index e69de29..aead2e5 100644
--- a/r-gmp-feedstock/recipe/build.sh
+++ b/r-gmp-feedstock/recipe/build.sh
@@ -0,0 +1,15 @@
+#!/bin/bash
+
+# R refuses to build packages that mark themselves as Priority: Recommended
+mv DESCRIPTION DESCRIPTION.old
+grep -v '^Priority: ' DESCRIPTION.old > DESCRIPTION
+
+export CFLAGS="${CFLAGS} -I${PREFIX}/include"
+export LDFLAGS="${LDFLAGS} -L${PREFIX}/lib"
+$R CMD INSTALL --build .
+
+# Add more build steps here, if they are necessary.
+
+# See
+# http://docs.continuum.io/conda/build.html
+# for a list of environment variables that are set during the build process.
diff --git a/r-gmp-feedstock/recipe/meta.yaml b/r-gmp-feedstock/recipe/meta.yaml
index e69de29..c932187 100644
--- a/r-gmp-feedstock/recipe/meta.yaml
+++ b/r-gmp-feedstock/recipe/meta.yaml
@@ -0,0 +1,97 @@
+{% set version = '0.5-13.1' %}
+
+{% set posix = 'm2-' if win else '' %}
+{% set native = 'm2w64-' if win else '' %}
+
+package:
+  name: r-gmp
+  version: {{ version|replace("-", "_") }}
+
+source:
+  fn: gmp_{{ version }}.tar.gz
+  url:
+    - https://cran.r-project.org/src/contrib/gmp_{{ version }}.tar.gz
+    - https://cran.r-project.org/src/contrib/Archive/gmp/gmp_{{ version }}.tar.gz
+  sha256: 2f805374a26742cd43f6b2054130d8670eda1940070aabb9971e9e48226d0976
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
+build:
+  # If this is a new build for the same version, increment the build number.
+  number: 0
+
+  # This is required to make R link correctly on Linux.
+  rpaths:
+    - lib/R/lib/
+    - lib/
+
+# Suggests: Rmpfr
+requirements:
+  build:
+    - r-base
+    - {{ compiler('cxx') }}      # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}autoconf
+    - {{posix}}automake
+    - {{posix}}pkg-config
+    - {{posix}}make
+    - {{native}}gmp
+
+  run:
+    - r-base
+    - {{native}}gcc-libs         # [win]
+    - {{native}}gmp
+
+test:
+  commands:
+    # You can put additional test commands to be run here.
+    - $R -e "library('gmp')"           # [not win]
+    - "\"%R%\" -e \"library('gmp')\""  # [win]
+
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
+about:
+  home: http://mulcyber.toulouse.inra.fr/projects/gmp
+  license: GPL
+  summary: Multiple Precision Arithmetic (big integers and rationals, prime number tests, matrix
+    computation), "arithmetic without limitations" using the C library GMP (GNU Multiple
+    Precision Arithmetic).
+  license_family: GPL
+
+extra:
+  recipe-maintainers:
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: gmp
+# Version: 0.5-13.1
+# Date: 2017-01-02
+# Title: Multiple Precision Arithmetic
+# Author: Antoine Lucas, Immanuel Scholz, Rainer Boehme <rb-gmp@reflex-studio.de>, Sylvain Jasson <jasson@toulouse.inra.fr>, Martin Maechler <maechler@stat.math.ethz.ch>
+# Maintainer: Antoine Lucas <antoinelucas@gmail.com>
+# Description: Multiple Precision Arithmetic (big integers and rationals, prime number tests, matrix computation), "arithmetic without limitations" using the C library GMP (GNU Multiple Precision Arithmetic).
+# Depends: R (>= 3.0.0)
+# Imports: methods
+# Suggests: Rmpfr
+# SystemRequirements: gmp (>= 4.2.3)
+# License: GPL
+# BuildResaveData: no
+# LazyDataNote: not available, as we use data/*.R *and* our classes
+# URL: http://mulcyber.toulouse.inra.fr/projects/gmp
+# NeedsCompilation: yes
+# Packaged: 2017-03-10 06:58:47 UTC; ripley
+# Repository: CRAN
+# Date/Publication: 2017-03-10 08:05:38
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
diff --git a/r-gower-feedstock/recipe/bld.bat b/r-gower-feedstock/recipe/bld.bat
index e69de29..8b10c94 100644
--- a/r-gower-feedstock/recipe/bld.bat
+++ b/r-gower-feedstock/recipe/bld.bat
@@ -0,0 +1,8 @@
+"%R%" CMD INSTALL --build .
+if errorlevel 1 exit 1
+
+@rem Add more build steps here, if they are necessary.
+
+@rem See
+@rem http://docs.continuum.io/conda/build.html
+@rem for a list of environment variables that are set during the build process.
diff --git a/r-gower-feedstock/recipe/build.sh b/r-gower-feedstock/recipe/build.sh
index e69de29..0c37df6 100644
--- a/r-gower-feedstock/recipe/build.sh
+++ b/r-gower-feedstock/recipe/build.sh
@@ -0,0 +1,13 @@
+#!/bin/bash
+
+# R refuses to build packages that mark themselves as Priority: Recommended
+mv DESCRIPTION DESCRIPTION.old
+grep -v '^Priority: ' DESCRIPTION.old > DESCRIPTION
+
+$R CMD INSTALL --build .
+
+# Add more build steps here, if they are necessary.
+
+# See
+# http://docs.continuum.io/conda/build.html
+# for a list of environment variables that are set during the build process.
diff --git a/r-gower-feedstock/recipe/meta.yaml b/r-gower-feedstock/recipe/meta.yaml
index e69de29..716a57e 100644
--- a/r-gower-feedstock/recipe/meta.yaml
+++ b/r-gower-feedstock/recipe/meta.yaml
@@ -0,0 +1,94 @@
+{% set version = '0.1.2' %}
+
+{% set posix = 'm2-' if win else '' %}
+{% set native = 'm2w64-' if win else '' %}
+
+package:
+  name: r-gower
+  version: {{ version|replace("-", "_") }}
+
+source:
+  fn: gower_{{ version }}.tar.gz
+  url:
+    - https://cran.r-project.org/src/contrib/gower_{{ version }}.tar.gz
+    - https://cran.r-project.org/src/contrib/Archive/gower/gower_{{ version }}.tar.gz
+  sha256: eb91b2d2784d237c7055abcf3cfa4fc0b2226b855a0c29fc5a4e8eaa689079d5
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
+build:
+  # If this is a new build for the same version, increment the build number.
+  number: 0
+
+  # This is required to make R link correctly on Linux.
+  rpaths:
+    - lib/R/lib/
+    - lib/
+
+# Suggests: testthat, knitr, rmarkdown
+requirements:
+  build:
+    - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
+
+  run:
+    - r-base
+    - {{native}}gcc-libs         # [win]
+
+test:
+  commands:
+    # You can put additional test commands to be run here.
+    - $R -e "library('gower')"           # [not win]
+    - "\"%R%\" -e \"library('gower')\""  # [win]
+
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
+about:
+  home: https://github.com/markvanderloo/gower
+  license: GPL-3
+  summary: Compute Gower's distance (or similarity) coefficient between records. Compute  the
+    top-n matches between records. Core algorithms are executed in parallel on systems
+    supporting OpenMP.
+  license_family: GPL3
+
+extra:
+  recipe-maintainers:
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: gower
+# Maintainer: Mark van der Loo <mark.vanderloo@gmail.com>
+# License: GPL-3
+# Title: Gower's Distance
+# LazyData: no
+# Type: Package
+# LazyLoad: yes
+# Authors@R: c( person("Mark", "van der Loo", role=c("aut","cre"),email="mark.vanderloo@gmail.com"))
+# Description: Compute Gower's distance (or similarity) coefficient between records. Compute  the top-n matches between records. Core algorithms are executed in parallel on systems supporting OpenMP.
+# Version: 0.1.2
+# URL: https://github.com/markvanderloo/gower
+# BugReports: https://github.com/markvanderloo/gower/issues
+# Date: 2017-02-23
+# Suggests: testthat, knitr, rmarkdown
+# RoxygenNote: 6.0.1
+# VignetteBuilder: knitr
+# NeedsCompilation: yes
+# Packaged: 2017-02-23 20:13:17 UTC; mark
+# Author: Mark van der Loo [aut, cre]
+# Repository: CRAN
+# Date/Publication: 2017-02-23 23:35:17
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-gplots-feedstock contains modified content
diff --git a/r-gplots-feedstock/recipe/meta.yaml b/r-gplots-feedstock/recipe/meta.yaml
index 425368f..fd795ab 100644
--- a/r-gplots-feedstock/recipe/meta.yaml
+++ b/r-gplots-feedstock/recipe/meta.yaml
@@ -14,14 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/gplots/gplots_{{ version }}.tar.gz
   sha256: 343df84327ac3d03494992e79ee3afc78ba3bdc08af9a305ee3fb0a38745cb0a
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: grid, MASS
 requirements:
   build:
     - r-base
@@ -39,16 +46,59 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('gplots')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('gplots')"           # [not win]
     - "\"%R%\" -e \"library('gplots')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=gplots
   license: GPL-2
-  summary: Various R programming tools for plotting data.
+  summary: 'Various R programming tools for plotting data, including: - calculating and plotting
+    locally smoothed summary function as (''bandplot'', ''wapply''), - enhanced versions
+    of standard plots (''barplot2'', ''boxplot2'', ''heatmap.2'', ''smartlegend''),
+    - manipulating colors (''col2hex'', ''colorpanel'', ''redgreen'', ''greenred'',
+    ''bluered'', ''redblue'', ''rich.colors''), - calculating and plotting two-dimensional
+    data summaries (''ci2d'', ''hist2d''), - enhanced regression diagnostic plots (''lmplot2'',
+    ''residplot''), - formula-enabled interface to ''stats::lowess'' function (''lowess''),
+    - displaying textual data in plots (''textplot'', ''sinkplot''), - plotting a matrix
+    where each cell contains a dot whose size reflects the relative magnitude of the
+    elements (''balloonplot''), - plotting "Venn" diagrams (''venn''), - displaying
+    Open-Office style plots (''ooplot''), - plotting multiple data on same region, with
+    separate axes (''overplot''), - plotting means and confidence intervals (''plotCI'',
+    ''plotmeans''), - spacing points in an x-y plot so they don''t overlap (''space'').'
+
   license_family: GPL2
 
 extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: gplots
+# Title: Various R Programming Tools for Plotting Data
+# Description: Various R programming tools for plotting data, including: - calculating and plotting locally smoothed summary function as ('bandplot', 'wapply'), - enhanced versions of standard plots ('barplot2', 'boxplot2', 'heatmap.2', 'smartlegend'), - manipulating colors ('col2hex', 'colorpanel', 'redgreen', 'greenred', 'bluered', 'redblue', 'rich.colors'), - calculating and plotting two-dimensional data summaries ('ci2d', 'hist2d'), - enhanced regression diagnostic plots ('lmplot2', 'residplot'), - formula-enabled interface to 'stats::lowess' function ('lowess'), - displaying textual data in plots ('textplot', 'sinkplot'), - plotting a matrix where each cell contains a dot whose size reflects the relative magnitude of the elements ('balloonplot'), - plotting "Venn" diagrams ('venn'), - displaying Open-Office style plots ('ooplot'), - plotting multiple data on same region, with separate axes ('overplot'), - plotting means and confidence intervals ('plotCI', 'plotmeans'), - spacing points in an x-y plot so they don't overlap ('space').
+# Depends: R (>= 3.0)
+# Imports: gtools, gdata, stats, caTools, KernSmooth
+# Suggests: grid, MASS
+# Version: 3.0.1
+# Date: 2016-03-30
+# Author: Gregory R. Warnes, Ben Bolker, Lodewijk Bonebakker, Robert Gentleman, Wolfgang Huber Andy Liaw, Thomas Lumley, Martin Maechler, Arni Magnusson, Steffen Moeller, Marc Schwartz, Bill Venables
+# Maintainer: Gregory R. Warnes <greg@warnes.net>
+# License: GPL-2
+# NeedsCompilation: no
+# Packaged: 2016-03-30 17:32:42 UTC; gwarnes1
+# Repository: CRAN
+# Date/Publication: 2016-03-30 21:04:18
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-gridbase-feedstock contains modified content
diff --git a/r-gridbase-feedstock/recipe/meta.yaml b/r-gridbase-feedstock/recipe/meta.yaml
index df40f5c..68959d2 100644
--- a/r-gridbase-feedstock/recipe/meta.yaml
+++ b/r-gridbase-feedstock/recipe/meta.yaml
@@ -14,14 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/gridBase/gridBase_{{ version }}.tar.gz
   sha256: be8718d24cd10f6e323dce91b15fc40ed88bccaa26acf3192d5e38fe33e15f26
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: lattice
 requirements:
   build:
     - r-base
@@ -31,9 +38,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('gridBase')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('gridBase')"           # [not win]
     - "\"%R%\" -e \"library('gridBase')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=gridBase
   license: GPL
@@ -44,3 +58,25 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: gridBase
+# Title: Integration of base and grid graphics
+# Version: 0.4-7
+# Author: Paul Murrell
+# Depends: R (>= 2.3.0)
+# Imports: graphics, grid
+# Suggests: lattice
+# Description: Integration of base and grid graphics
+# Maintainer: Paul Murrell <paul@stat.auckland.ac.nz>
+# License: GPL
+# Packaged: 2014-02-24 01:35:23 UTC; EC\pmur002
+# NeedsCompilation: no
+# Repository: CRAN
+# Date/Publication: 2014-02-24 07:20:37
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-gridextra-feedstock contains modified content
diff --git a/r-gridextra-feedstock/recipe/meta.yaml b/r-gridextra-feedstock/recipe/meta.yaml
index e8afc3d..31d1853 100644
--- a/r-gridextra-feedstock/recipe/meta.yaml
+++ b/r-gridextra-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '2.2.1' %}
+{% set version = '2.3' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,15 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/gridExtra_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/gridExtra/gridExtra_{{ version }}.tar.gz
-  sha256: 44fe455a5bcdf48a4ece7a542f83e7749cf251dc1df6ae7634470240398c6818
+  sha256: 81b60ce6f237ec308555471ae0119158b115463df696d2eca9b177ded8988e3b
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: ggplot2, egg, lattice, knitr, testthat
 requirements:
   build:
     - r-base
@@ -32,11 +40,18 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('gridExtra')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('gridExtra')"           # [not win]
     - "\"%R%\" -e \"library('gridExtra')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
-  home: https://github.com/baptiste/gridextra
+  home: https://CRAN.R-project.org/package=gridExtra
   license: GPL (>= 2)
   summary: Provides a number of user-level functions to work with "grid" graphics, notably to
     arrange multiple grid-based plots on a page, and draw tables.
@@ -46,3 +61,28 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: gridExtra
+# Authors@R: c(person("Baptiste", "Auguie", email = "baptiste.auguie@gmail.com", role = c("aut", "cre")), person("Anton", "Antonov", email = "tonytonov@gmail.com", role = c("ctb")))
+# License: GPL (>= 2)
+# Title: Miscellaneous Functions for "Grid" Graphics
+# Type: Package
+# Description: Provides a number of user-level functions to work with "grid" graphics, notably to arrange multiple grid-based plots on a page, and draw tables.
+# Version: 2.3
+# VignetteBuilder: knitr
+# Imports: gtable, grid, grDevices, graphics, utils
+# Suggests: ggplot2, egg, lattice, knitr, testthat
+# RoxygenNote: 6.0.1
+# NeedsCompilation: no
+# Packaged: 2017-09-08 22:52:09 UTC; baptiste
+# Author: Baptiste Auguie [aut, cre], Anton Antonov [ctb]
+# Maintainer: Baptiste Auguie <baptiste.auguie@gmail.com>
+# Repository: CRAN
+# Date/Publication: 2017-09-09 14:12:08 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-grpreg-feedstock contains modified content
diff --git a/r-grpreg-feedstock/recipe/meta.yaml b/r-grpreg-feedstock/recipe/meta.yaml
index 392225d..8d2289e 100644
--- a/r-grpreg-feedstock/recipe/meta.yaml
+++ b/r-grpreg-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '3.0-2' %}
+{% set version = '3.1-2' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,33 +12,47 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/grpreg_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/grpreg/grpreg_{{ version }}.tar.gz
-  sha256: 12111d8ecb45974070a22ac6998155cc67935b49d88b358f5eaaa0f2833dd8f0
+  sha256: 38d5f277b8fbbdc3a760277c2372ae993f7bda4f0198338609ccc91110575c10
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: grpregOverlap, knitr, survival
 requirements:
   build:
     - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
     - r-matrix
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-matrix
-    - libgcc  # [not win]
 
 test:
   commands:
-    - $R -e "library('grpreg')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('grpreg')"           # [not win]
     - "\"%R%\" -e \"library('grpreg')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=grpreg
   license: GPL-3
@@ -52,3 +66,27 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: grpreg
+# Title: Regularization Paths for Regression Models with Grouped Covariates
+# Version: 3.1-2
+# Date: 2017-07-05
+# Author: Patrick Breheny [aut, cre], Yaohui Zeng [ctb]
+# Maintainer: Patrick Breheny <patrick-breheny@uiowa.edu>
+# Depends: R (>= 3.1.0), Matrix
+# Suggests: grpregOverlap, knitr, survival
+# VignetteBuilder: knitr
+# Description: Efficient algorithms for fitting the regularization path of linear or logistic regression models with grouped penalties.  This includes group selection methods such as group lasso, group MCP, and group SCAD as well as bi-level selection methods such as the group exponential lasso, the composite MCP, and the group bridge.
+# BugReports: http://github.com/pbreheny/grpreg/issues
+# License: GPL-3
+# NeedsCompilation: yes
+# Packaged: 2017-07-05 21:31:51 UTC; pbreheny
+# Repository: CRAN
+# Date/Publication: 2017-07-06 10:19:31 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-gsl-feedstock contains modified content
diff --git a/r-gsl-feedstock/recipe/meta.yaml b/r-gsl-feedstock/recipe/meta.yaml
index 652cdcf..84a39f3 100644
--- a/r-gsl-feedstock/recipe/meta.yaml
+++ b/r-gsl-feedstock/recipe/meta.yaml
@@ -14,31 +14,48 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/gsl/gsl_{{ version }}.tar.gz
   sha256: 4f9fc4dc8170ba93c9b45940448da089ce9ad4c45ba39c8f264e1505a3e03a02
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
-    - {{native}}gsl
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}autoconf
+    - {{posix}}automake
+    - {{posix}}pkg-config
+    - {{posix}}make
 
   run:
     - r-base
-    - {{native}}gsl
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('gsl')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('gsl')"           # [not win]
     - "\"%R%\" -e \"library('gsl')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=gsl
   license: GPL (>= 2)
@@ -53,3 +70,25 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: gsl
+# Version: 1.9-10.3
+# Depends: R (>= 2.10.0)
+# Date: 2016-12-14
+# Title: Wrapper for the Gnu Scientific Library
+# Author: Robin K. S. Hankin; qrng functions by Duncan Murdoch and multimin by Andrew Clausen
+# SystemRequirements: Gnu Scientific Library version >= 1.12
+# Description:  An R wrapper for the special functions and quasi random number generators of the Gnu Scientific Library (http://www.gnu.org/software/gsl/).  See gsl-package.Rd for details of  overall package organization, and Misc.Rd for some functions that are widely used in the package, and some tips on installation.
+# Maintainer: Robin K. S. Hankin <hankin.robin@gmail.com>
+# License: GPL (>= 2)
+# NeedsCompilation: yes
+# Repository: CRAN
+# Date/Publication: 2017-01-05 11:13:10
+# Packaged: 2017-01-05 07:48:21 UTC; rksh
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-gsw-feedstock contains modified content
diff --git a/r-gsw-feedstock/recipe/bld.bat b/r-gsw-feedstock/recipe/bld.bat
new file mode 100644
index 0000000..8b10c94
--- /dev/null
+++ b/r-gsw-feedstock/recipe/bld.bat
@@ -0,0 +1,8 @@
+"%R%" CMD INSTALL --build .
+if errorlevel 1 exit 1
+
+@rem Add more build steps here, if they are necessary.
+
+@rem See
+@rem http://docs.continuum.io/conda/build.html
+@rem for a list of environment variables that are set during the build process.
diff --git a/r-gsw-feedstock/recipe/build.sh b/r-gsw-feedstock/recipe/build.sh
new file mode 100644
index 0000000..0c37df6
--- /dev/null
+++ b/r-gsw-feedstock/recipe/build.sh
@@ -0,0 +1,13 @@
+#!/bin/bash
+
+# R refuses to build packages that mark themselves as Priority: Recommended
+mv DESCRIPTION DESCRIPTION.old
+grep -v '^Priority: ' DESCRIPTION.old > DESCRIPTION
+
+$R CMD INSTALL --build .
+
+# Add more build steps here, if they are necessary.
+
+# See
+# http://docs.continuum.io/conda/build.html
+# for a list of environment variables that are set during the build process.
diff --git a/r-gsw-feedstock/recipe/meta.yaml b/r-gsw-feedstock/recipe/meta.yaml
index 5f5592a..620075b 100644
--- a/r-gsw-feedstock/recipe/meta.yaml
+++ b/r-gsw-feedstock/recipe/meta.yaml
@@ -1,47 +1,100 @@
-{% set version = "1.0-3" %}
+{% set version = '1.0-5' %}
+
+{% set posix = 'm2-' if win else '' %}
+{% set native = 'm2w64-' if win else '' %}
 
 package:
   name: r-gsw
-  version: {{ version.replace("-", "_") }}
+  version: {{ version|replace("-", "_") }}
 
 source:
   fn: gsw_{{ version }}.tar.gz
   url:
-    - http://cran.r-project.org/src/contrib/gsw_{{ version }}.tar.gz
-    - http://cran.r-project.org/src/contrib/Archive/gsw/gsw_{{ version }}.tar.gz
-  sha256: e24c6802467546d616531499c2a98610fdb5c35c8a1e161b6d6b887c4c814331
+    - https://cran.r-project.org/src/contrib/gsw_{{ version }}.tar.gz
+    - https://cran.r-project.org/src/contrib/Archive/gsw/gsw_{{ version }}.tar.gz
+  sha256: eb468918ee91e429b47fbcac43269eca627b7f64b61520de5bbe8fa223e96453
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  script: R CMD INSTALL --build .
-  skip: True  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
-requirements:  # [not win32]
-  build:  # [not win32]
-    - r-base  # [not win32]
-    - posix  # [win64]
-    - m2w64-toolchain  # [win64]
-    - gcc  # [not win]
-  run:  # [not win32]
-    - r-base  # [not win32]
+# Suggests: knitr,
+requirements:
+  build:
+    - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
+    - r-testthat
+
+  run:
+    - r-base
+    - {{native}}gcc-libs         # [win]
+    - r-testthat
 
 test:
   commands:
-    - R -e "library('gsw')"  # [not win]
-    - R -e \"library('gsw')\"  # [win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('gsw')"           # [not win]
+    - "\"%R%\" -e \"library('gsw')\""  # [win]
+
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
 
 about:
   home: http://teos-10.github.io/GSW-R/index.html
-  license: GPL-3.0
-  license_file: LICENSE
-  summary: 'Provides an interface to the Gibbs SeaWater (TEOS-10) C library, which
-    derives from Matlab and other code written by WG127 (Working Group 127) of
-    SCOR/IAPSO (Scientific Committee on Oceanic Research / International Association
-    for the Physical Sciences of the Oceans).'
+  license: GPL (>= 2) | file LICENSE
+  summary: Provides an interface to the Gibbs 'SeaWater' ('TEOS-10') C library, version 3.05-4
+    (commit '5b4d959e54031f9e972f3e863f63e67fa4f5bfec', dated 2017-08-07, available
+    at <https://github.com/TEOS-10/GSW-C>, which stems from 'Matlab' and other code
+    written by members of Working Group 127 of 'SCOR'/'IAPSO' (Scientific Committee
+    on Oceanic Research / International Association for the Physical Sciences of the
+    Oceans).
+  license_family: GPL3
 
 extra:
   recipe-maintainers:
     - ocefpaf
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: gsw
+# Version: 1.0-5
+# Date: 2017-08-08
+# Title: Gibbs Sea Water Functions
+# Authors@R: c(person("Dan", "Kelley", role=c("aut","cre","cph"), email="dan.kelley@dal.ca", comment="C wrapper plus R code, tests, and documentation"), person("Clark", "Richards", role=c("aut","cph"), email="clark.richards@gmail.com", comment="C wrapper plus R code, tests, and documentation"), person("WG127", "SCOR/IAPSO", role=c("aut","cph"), comment="Original 'Matlab' and derived code"))
+# Copyright: Original algorithms and 'Matlab'/C library (c) 2015-2017 WG127 SCOR/IAPSO (Scientific Committee on Oceanic Research / International Association for the Physical Sciences of the Oceans, Working Group 127); C wrapper code and R code (c) 2015-2017 Dan Kelley and Clark Richards
+# Maintainer: Dan Kelley <dan.kelley@dal.ca>
+# Depends: R (>= 2.15), testthat
+# Suggests: knitr,
+# BugReports: https://github.com/TEOS-10/GSW-R/issues
+# Description: Provides an interface to the Gibbs 'SeaWater' ('TEOS-10') C library, version 3.05-4 (commit '5b4d959e54031f9e972f3e863f63e67fa4f5bfec', dated 2017-08-07, available at <https://github.com/TEOS-10/GSW-C>, which stems from 'Matlab' and other code written by members of Working Group 127 of 'SCOR'/'IAPSO' (Scientific Committee on Oceanic Research / International Association for the Physical Sciences of the Oceans).
+# URL: http://teos-10.github.io/GSW-R/index.html
+# License: GPL (>= 2) | file LICENSE
+# LazyLoad: yes
+# LazyData: no
+# Packaged: 2017-08-09 14:47:12 UTC; kelley
+# Encoding: UTF-8
+# RoxygenNote: 6.0.1
+# VignetteBuilder: knitr
+# NeedsCompilation: yes
+# Author: Dan Kelley [aut, cre, cph] (C wrapper plus R code, tests, and documentation), Clark Richards [aut, cph] (C wrapper plus R code, tests, and documentation), WG127 SCOR/IAPSO [aut, cph] (Original 'Matlab' and derived code)
+# Repository: CRAN
+# Date/Publication: 2017-08-09 21:05:24 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-gtable-feedstock contains modified content
diff --git a/r-gtable-feedstock/recipe/meta.yaml b/r-gtable-feedstock/recipe/meta.yaml
index fc4a9b0..4e72088 100644
--- a/r-gtable-feedstock/recipe/meta.yaml
+++ b/r-gtable-feedstock/recipe/meta.yaml
@@ -14,14 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/gtable/gtable_{{ version }}.tar.gz
   sha256: 801e4869830ff3da1d38e41f5a2296a54fc10a7419c6ffb108582850c701e76f
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: testthat, covr
 requirements:
   build:
     - r-base
@@ -31,9 +38,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('gtable')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('gtable')"           # [not win]
     - "\"%R%\" -e \"library('gtable')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=gtable
   license: GPL-2
@@ -44,3 +58,28 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: gtable
+# Version: 0.2.0
+# Title: Arrange 'Grobs' in Tables
+# Description: Tools to make it easier to work with "tables" of 'grobs'.
+# Authors@R: person("Hadley", "Wickham", , "hadley@rstudio.com", c("aut", "cre"))
+# Depends: R (>= 2.14)
+# Imports: grid
+# Suggests: testthat, covr
+# License: GPL-2
+# Collate: 'add-grob.r' 'add-rows-cols.r' 'add-space.r' 'grid.r' 'gtable-layouts.r' 'gtable.r' 'rbind-cbind.r' 'utils.r' 'trim.r' 'filter.r' 'align.r' 'padding.r' 'z.r'
+# RoxygenNote: 5.0.1
+# NeedsCompilation: no
+# Packaged: 2016-02-26 13:06:10 UTC; hadley
+# Author: Hadley Wickham [aut, cre]
+# Maintainer: Hadley Wickham <hadley@rstudio.com>
+# Repository: CRAN
+# Date/Publication: 2016-02-26 15:23:14
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-gtools-feedstock contains modified content
diff --git a/r-gtools-feedstock/recipe/meta.yaml b/r-gtools-feedstock/recipe/meta.yaml
index 5e1056a..de5bf45 100644
--- a/r-gtools-feedstock/recipe/meta.yaml
+++ b/r-gtools-feedstock/recipe/meta.yaml
@@ -14,29 +14,43 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/gtools/gtools_{{ version }}.tar.gz
   sha256: 86b6a51a92ddb3c78095e0c5dc20414c67f6e28f915bf0ee11406adad3e476f6
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('gtools')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('gtools')"           # [not win]
     - "\"%R%\" -e \"library('gtools')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=gtools
   license: GPL-2
@@ -65,3 +79,27 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: gtools
+# Title: Various R Programming Tools
+# Description: Functions to assist in R programming, including: - assist in developing, updating, and maintaining R and R packages ('ask', 'checkRVersion', 'getDependencies', 'keywords', 'scat'), - calculate the logit and inverse logit transformations ('logit', 'inv.logit'), - test if a value is missing, empty or contains only NA and NULL values ('invalid'), - manipulate R's .Last function ('addLast'), - define macros ('defmacro'), - detect odd and even integers ('odd', 'even'), - convert strings containing non-ASCII characters (like single quotes) to plain ASCII ('ASCIIfy'), - perform a binary search ('binsearch'), - sort strings containing both numeric and character components ('mixedsort'), - create a factor variable from the quantiles of a continuous variable ('quantcut'), - enumerate permutations and combinations ('combinations', 'permutation'), - calculate and convert between fold-change and log-ratio ('foldchange', 'logratio2foldchange', 'foldchange2logratio'), - calculate probabilities and generate random numbers from Dirichlet distributions ('rdirichlet', 'ddirichlet'), - apply a function over adjacent subsets of a vector ('running'), - modify the TCP\_NODELAY ('de-Nagle') flag for socket objects, - efficient 'rbind' of data frames, even if the column names don't match ('smartbind'), - generate significance stars from p-values ('stars.pval'), - convert characters to/from ASCII codes.
+# Version: 3.5.0
+# Date: 2015-05-26
+# Author: Gregory R. Warnes, Ben Bolker, and Thomas Lumley
+# Maintainer: Gregory R. Warnes <greg@warnes.net>
+# License: GPL-2
+# Repository: CRAN
+# Repository/R-Forge/Project: r-gregmisc
+# Repository/R-Forge/Revision: 2048
+# Repository/R-Forge/DateTimeStamp: 2015-05-27 16:38:37
+# Date/Publication: 2015-05-29 10:36:40
+# NeedsCompilation: yes
+# Packaged: 2015-05-27 16:45:10 UTC; rforge
+# Depends: R (>= 2.10)
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-haven-feedstock contains modified content
diff --git a/r-haven-feedstock/recipe/meta.yaml b/r-haven-feedstock/recipe/meta.yaml
index fa1831c..8e70c83 100644
--- a/r-haven-feedstock/recipe/meta.yaml
+++ b/r-haven-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '1.0.0' %}
+{% set version = '1.1.0' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,49 +12,96 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/haven_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/haven/haven_{{ version }}.tar.gz
-  sha256: b499df0fe8b5921970812e96749699aa8b25391c3d304a9e35353d527198d697
+  sha256: 089fb4d0955f320abc48d0a3031799f96f3a20b82492474743903fdf12001d19
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: testthat, knitr, rmarkdown, covr
 requirements:
   build:
     - r-base
-    - r-bh
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('cxx') }}      # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
     - r-rcpp >=0.11.4
+    - r-forcats >=0.2.0
     - r-hms
     - r-readr >=0.1.0
     - r-tibble
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
-    - r-bh
+    - {{native}}gcc-libs         # [win]
     - r-rcpp >=0.11.4
+    - r-forcats >=0.2.0
     - r-hms
     - r-readr >=0.1.0
     - r-tibble
-    - libgcc  # [not win]
 
 test:
   commands:
-    - $R -e "library('haven')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('haven')"           # [not win]
     - "\"%R%\" -e \"library('haven')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
-  home: https://github.com/hadley/haven, https://github.com/WizardMac/ReadStat
-  license: MIT
-  summary: Import foreign statistical formats into R via the embedded 'ReadStat' C library (https://github.com/WizardMac/ReadStat).
+  home: http://haven.tidyverse.org, https://github.com/tidyverse/haven, https://github.com/WizardMac/ReadStat
+  license: MIT + file LICENSE
+  summary: Import foreign statistical formats into R via the embedded 'ReadStat' C library, <https://github.com/WizardMac/ReadStat>.
   license_family: MIT
 
 extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: haven
+# Version: 1.1.0
+# Title: Import and Export 'SPSS', 'Stata' and 'SAS' Files
+# Description: Import foreign statistical formats into R via the embedded 'ReadStat' C library, <https://github.com/WizardMac/ReadStat>.
+# License: MIT + file LICENSE
+# Authors@R: c( person("Hadley", "Wickham", , "hadley@rstudio.com", role = c("aut", "cre")), person("Evan", "Miller", , role = c("aut", "cph"), comment = "Author of included ReadStat code"), person("RStudio", role = c("cph", "fnd")) )
+# Depends: R (>= 3.1.0)
+# Suggests: testthat, knitr, rmarkdown, covr
+# LinkingTo: Rcpp
+# Imports: Rcpp (>= 0.11.4), readr (>= 0.1.0), hms, tibble, forcats (>= 0.2.0)
+# LazyData: true
+# URL: http://haven.tidyverse.org, https://github.com/tidyverse/haven, https://github.com/WizardMac/ReadStat
+# BugReports: https://github.com/hadley/haven/issues
+# VignetteBuilder: knitr
+# SystemRequirements: GNU make
+# RoxygenNote: 6.0.1
+# Encoding: UTF-8
+# NeedsCompilation: yes
+# Packaged: 2017-07-08 15:47:00 UTC; hadley
+# Author: Hadley Wickham [aut, cre], Evan Miller [aut, cph] (Author of included ReadStat code), RStudio [cph, fnd]
+# Maintainer: Hadley Wickham <hadley@rstudio.com>
+# Repository: CRAN
+# Date/Publication: 2017-07-09 06:40:47 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-hexbin-feedstock contains modified content
diff --git a/r-hexbin-feedstock/recipe/meta.yaml b/r-hexbin-feedstock/recipe/meta.yaml
index 80d04cd..2c5ce6e 100644
--- a/r-hexbin-feedstock/recipe/meta.yaml
+++ b/r-hexbin-feedstock/recipe/meta.yaml
@@ -14,30 +14,45 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/hexbin/hexbin_{{ version }}.tar.gz
   sha256: 075935a3ae2d90e44aca6ebbd368dc6f7e59d322e36e0e0932dedbf01330ad08
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: marray, affy, Biobase, limma
 requirements:
   build:
     - r-base
+    - {{ compiler('fortran') }}  # [not win]
+    - {{native}}toolchain        # [win]
     - r-lattice
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-lattice
 
 test:
   commands:
-    - $R -e "library('hexbin')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('hexbin')"           # [not win]
     - "\"%R%\" -e \"library('hexbin')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://github.com/edzer/hexbin
   license: GPL-2
@@ -49,3 +64,28 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: hexbin
+# Version: 1.27.1
+# Date: 2015-08-19
+# Title: Hexagonal Binning Routines
+# Author: Dan Carr <dcarr@voxel.galaxy.gmu.edu>, ported by Nicholas Lewin-Koh and Martin Maechler <maechler@stat.math.ethz.ch>, contains copies of lattice functions written by Deepayan Sarkar  <deepayan.sarkar@r-project.org>
+# Maintainer: Edzer Pebesma <edzer.pebesma@uni-muenster.de>
+# Depends: R (>= 2.10), methods
+# Imports: lattice, grid, graphics, grDevices, stats, utils
+# Suggests: marray, affy, Biobase, limma
+# Description: Binning and plotting functions for hexagonal bins. Now uses and relies on grid graphics and formal (S4) classes and methods.
+# Collate: lattice.R BTC.R BTY.R grid.hexagons.R grid.hexlegend.R hbox.R hdiffplot.R hexbinList.R hexbinplot.R hexbin.s4.R hexpanel.R hexplom.R hexPlotMA.R hexutil.R hexViewport.R HO.R LINGRAY.R LOCS.R MAG.R RB.R smoothHexbin.R
+# License: GPL-2
+# NeedsCompilation: yes
+# URL: http://github.com/edzer/hexbin
+# Packaged: 2016-12-05 14:59:04 UTC; hornik
+# Repository: CRAN
+# Date/Publication: 2016-12-05 18:28:47
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-highcharter-feedstock contains modified content
diff --git a/r-highcharter-feedstock/recipe/meta.yaml b/r-highcharter-feedstock/recipe/meta.yaml
index aca7b01..e04ee61 100644
--- a/r-highcharter-feedstock/recipe/meta.yaml
+++ b/r-highcharter-feedstock/recipe/meta.yaml
@@ -14,14 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/highcharter/highcharter_{{ version }}.tar.gz
   sha256: d77ab493f6eb8648b352205df1bf60848c8f2155d46920077bac57809cbbded7
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  # igraph is not available for windows
-  skip: true  # [win]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: knitr, rmarkdown, survival, ggplot2, httr, viridisLite
 requirements:
   build:
     - r-base
@@ -42,6 +49,7 @@ requirements:
     - r-tidyr
     - r-xts
     - r-zoo
+
   run:
     - r-base
     - r-assertthat
@@ -64,12 +72,19 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('highcharter')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('highcharter')"           # [not win]
     - "\"%R%\" -e \"library('highcharter')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://jkunst.com/highcharter
-  license: MIT
+  license: MIT + file LICENSE
   summary: A wrapper for the 'Highcharts' library including shortcut functions to plot R objects.
     'Highcharts'  <http://www.highcharts.com/> is a charting library offering numerous
     chart types with a simple configuration syntax.
@@ -79,3 +94,33 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: highcharter
+# Type: Package
+# Version: 0.5.0
+# Title: A Wrapper for the 'Highcharts' Library
+# Description: A wrapper for the 'Highcharts' library including shortcut functions to plot R objects. 'Highcharts'  <http://www.highcharts.com/> is a charting library offering numerous chart types with a simple configuration syntax.
+# Authors@R: c( person("Joshua", "Kunst", role = c("aut", "cre"), email = "jbkunst@gmail.com"), person("Nuno", "Agostinho", role = c("ctb"), comment  = "hchart.survfit, densities and hc_add_series_scatter"),   person("Eduardo", "Flores", role = c("ctb"), comment  = "First version hc_add_series_df_tidy"), person("Dean", "Kilfoyle", role = c("ctb"), comment  = "First version hc_add_series_boxplot"), person("Adline", "Dsilva", role = c("ctb"), comment  = "First version Matrix heatmap") )
+# Date: 2016-09-05
+# URL: http://jkunst.com/highcharter
+# BugReports: https://github.com/jbkunst/highcharter/issues
+# Maintainer: Joshua Kunst <jbkunst@gmail.com>
+# License: MIT + file LICENSE
+# RoxygenNote: 5.0.1
+# Depends: R (>= 2.10)
+# Imports: htmlwidgets, magrittr, purrr, rlist, assertthat, zoo, dplyr, tibble (>= 1.1), stringr, broom, xts, quantmod, tidyr, htmltools, jsonlite, igraph, lubridate
+# Suggests: knitr, rmarkdown, survival, ggplot2, httr, viridisLite
+# VignetteBuilder: knitr
+# LazyData: true
+# NeedsCompilation: no
+# Packaged: 2017-01-17 02:03:58 UTC; Joshua
+# Author: Joshua Kunst [aut, cre], Nuno Agostinho [ctb] (hchart.survfit, densities and hc_add_series_scatter), Eduardo Flores [ctb] (First version hc_add_series_df_tidy), Dean Kilfoyle [ctb] (First version hc_add_series_boxplot), Adline Dsilva [ctb] (First version Matrix heatmap)
+# Repository: CRAN
+# Date/Publication: 2017-01-17 08:36:29
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-highr-feedstock contains modified content
diff --git a/r-highr-feedstock/recipe/meta.yaml b/r-highr-feedstock/recipe/meta.yaml
index b37b5e7..477ce38 100644
--- a/r-highr-feedstock/recipe/meta.yaml
+++ b/r-highr-feedstock/recipe/meta.yaml
@@ -14,14 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/highr/highr_{{ version }}.tar.gz
   sha256: 43e152b2dea596df6e14c44398c74fcd438ece15eaae5bdb84aef8d61b213b59
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: knitr, testit
 requirements:
   build:
     - r-base
@@ -31,14 +38,20 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('highr')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('highr')"           # [not win]
     - "\"%R%\" -e \"library('highr')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/yihui/highr
   license: GPL
-  summary: |
-    Provides syntax highlighting for R source code. Currently it supports LaTeX and HTML
+  summary: Provides syntax highlighting for R source code. Currently it supports LaTeX and HTML
     output. Source code of other languages is supported via Andre Simon's highlight
     package (http://www.andre-simon.de).
   license_family: GPL
@@ -47,3 +60,31 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: highr
+# Type: Package
+# Title: Syntax Highlighting for R Source Code
+# Version: 0.6
+# Date: 2016-05-09
+# Authors@R: c(person("Christopher", "Gandrud", role = "ctb"), person("Qiang", "Li", role = "ctb"), person("Yixuan", "Qiu", role = "aut"), person("Yihui", "Xie", email = "xie@yihui.name", role = c("aut", "cre")))
+# Maintainer: Yihui Xie <xie@yihui.name>
+# Description: Provides syntax highlighting for R source code. Currently it supports LaTeX and HTML output. Source code of other languages is supported via Andre Simon's highlight package (http://www.andre-simon.de).
+# Depends: R (>= 3.0.2)
+# Suggests: knitr, testit
+# License: GPL
+# URL: https://github.com/yihui/highr
+# BugReports: https://github.com/yihui/highr/issues
+# VignetteBuilder: knitr
+# RoxygenNote: 5.0.1
+# NeedsCompilation: no
+# Packaged: 2016-05-09 05:31:15 UTC; yihui
+# Author: Christopher Gandrud [ctb], Qiang Li [ctb], Yixuan Qiu [aut], Yihui Xie [aut, cre]
+# Repository: CRAN
+# Date/Publication: 2016-05-09 17:52:58
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-hmisc-feedstock contains modified content
diff --git a/r-hmisc-feedstock/recipe/meta.yaml b/r-hmisc-feedstock/recipe/meta.yaml
index 7cdece9..357674c 100644
--- a/r-hmisc-feedstock/recipe/meta.yaml
+++ b/r-hmisc-feedstock/recipe/meta.yaml
@@ -14,17 +14,27 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/Hmisc/Hmisc_{{ version }}.tar.gz
   sha256: 1c612a72eeb618e624f131475f15d4a15b7e2044ad2045d56bf49aa7be3af1a8
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: chron, rms, mice, tables, knitr, ff, ffbase, plotly (>= 4.5.6)
 requirements:
   build:
     - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('fortran') }}  # [not win]
+    - {{native}}toolchain        # [win]
     - r-formula
     - r-acepack
     - r-base64enc
@@ -42,12 +52,10 @@ requirements:
     - r-rpart
     - r-survival >=2.40_1
     - r-viridis
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-formula
     - r-acepack
     - r-base64enc
@@ -65,13 +73,19 @@ requirements:
     - r-rpart
     - r-survival >=2.40_1
     - r-viridis
-    - libgcc  # [not win]
 
 test:
   commands:
-    - $R -e "library('Hmisc')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('Hmisc')"           # [not win]
     - "\"%R%\" -e \"library('Hmisc')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://biostat.mc.vanderbilt.edu/Hmisc, https://github.com/harrelfe/Hmisc
   license: GPL (>= 2)
@@ -85,3 +99,28 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: Hmisc
+# Version: 4.0-3
+# Date: 2017-04-30
+# Title: Harrell Miscellaneous
+# Author: Frank E Harrell Jr <f.harrell@vanderbilt.edu>, with contributions from Charles Dupont and many others.
+# Maintainer: Frank E Harrell Jr <f.harrell@vanderbilt.edu>
+# Depends: lattice, survival (>= 2.40-1), Formula, ggplot2 (>= 2.2)
+# Imports: methods, latticeExtra, cluster, rpart, nnet, acepack, foreign, gtable, grid, gridExtra, data.table, htmlTable, viridis, htmltools, base64enc
+# Suggests: chron, rms, mice, tables, knitr, ff, ffbase, plotly (>= 4.5.6)
+# Description: Contains many functions useful for data analysis, high-level graphics, utility operations, functions for computing sample size and power, importing and annotating datasets, imputing missing values, advanced table making, variable clustering, character string manipulation, conversion of R objects to LaTeX and html code, and recoding variables.
+# License: GPL (>= 2)
+# LazyLoad: Yes
+# URL: http://biostat.mc.vanderbilt.edu/Hmisc, https://github.com/harrelfe/Hmisc
+# NeedsCompilation: yes
+# Packaged: 2017-05-01 18:00:23 UTC; harrelfe
+# Repository: CRAN
+# Date/Publication: 2017-05-02 06:31:42 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-hms-feedstock contains modified content
diff --git a/r-hms-feedstock/recipe/meta.yaml b/r-hms-feedstock/recipe/meta.yaml
index 13fa8ec..68b84b3 100644
--- a/r-hms-feedstock/recipe/meta.yaml
+++ b/r-hms-feedstock/recipe/meta.yaml
@@ -14,14 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/hms/hms_{{ version }}.tar.gz
   sha256: 9368259cbc1094ce0e4cf61544875ec30088ef690d6667e6b0b564218ab3ff88
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: testthat, lubridate
 requirements:
   build:
     - r-base
@@ -31,9 +38,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('hms')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('hms')"           # [not win]
     - "\"%R%\" -e \"library('hms')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/rstats-db/hms
   license: GPL-3
@@ -45,3 +59,31 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: hms
+# Title: Pretty Time of Day
+# Date: 2016-11-22
+# Version: 0.3
+# Authors@R: c( person("Kirill", "Muller", role = c("aut", "cre"), email = "krlmlr+r@mailbox.org"), person("The R Consortium", role = "cph") )
+# Description: Implements an S3 class for storing and formatting time-of-day values, based on the 'difftime' class.
+# Imports: methods
+# Suggests: testthat, lubridate
+# License: GPL-3
+# Encoding: UTF-8
+# LazyData: true
+# URL: https://github.com/rstats-db/hms
+# BugReports: https://github.com/rstats-db/hms/issues
+# RoxygenNote: 5.0.1.9000
+# NeedsCompilation: no
+# Packaged: 2016-11-22 14:45:31 UTC; muelleki
+# Author: Kirill Muller [aut, cre], The R Consortium [cph]
+# Maintainer: Kirill Muller <krlmlr+r@mailbox.org>
+# Repository: CRAN
+# Date/Publication: 2016-11-22 17:08:01
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-htmltable-feedstock contains modified content
diff --git a/r-htmltable-feedstock/recipe/meta.yaml b/r-htmltable-feedstock/recipe/meta.yaml
index 05528b7..e0d61e6 100644
--- a/r-htmltable-feedstock/recipe/meta.yaml
+++ b/r-htmltable-feedstock/recipe/meta.yaml
@@ -14,13 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/htmlTable/htmlTable_{{ version }}.tar.gz
   sha256: 5b487a7f33af77db7d987bf61f3ef2ba18bb629fe7b9802409f8b3485c603132
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: testthat, XML, xtable, ztable, Hmisc, reshape, rmarkdown, pander, chron, lubridate
 requirements:
   build:
     - r-base
@@ -29,6 +37,7 @@ requirements:
     - r-knitr >=1.6
     - r-magrittr >=1.5
     - r-stringr
+
   run:
     - r-base
     - r-checkmate
@@ -39,9 +48,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('htmlTable')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('htmlTable')"           # [not win]
     - "\"%R%\" -e \"library('htmlTable')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://gforge.se/packages/
   license: GPL (>= 3)
@@ -56,3 +72,31 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: htmlTable
+# Version: 1.9
+# Date: 2017-01-25
+# Title: Advanced Tables for Markdown/HTML
+# Author: Max Gordon <max@gforge.se>
+# Maintainer: Max Gordon <max@gforge.se>
+# Description: Tables with state-of-the-art layout elements such as row spanners, column spanners, table spanners, zebra striping, and more. While allowing advanced layout, the underlying css-structure is simple in order to maximize compatibility with word processors such as 'MS Word' or 'LibreOffice'. The package also contains a few text formatting functions that help outputting text compatible with HTML/'LaTeX'.
+# License: GPL (>= 3)
+# URL: http://gforge.se/packages/
+# BugReports: https://github.com/gforge/htmlTable/issues
+# Biarch: yes
+# Imports: stringr, knitr (>= 1.6), magrittr (>= 1.5), methods, checkmate, htmlwidgets
+# Suggests: testthat, XML, xtable, ztable, Hmisc, reshape, rmarkdown, pander, chron, lubridate
+# Encoding: UTF-8
+# NeedsCompilation: no
+# VignetteBuilder: knitr
+# RoxygenNote: 5.0.1
+# Packaged: 2017-01-25 10:39:00 UTC; max
+# Repository: CRAN
+# Date/Publication: 2017-01-26 12:08:14
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-htmltools-feedstock contains modified content
diff --git a/r-htmltools-feedstock/recipe/meta.yaml b/r-htmltools-feedstock/recipe/meta.yaml
index b0d5857..7804965 100644
--- a/r-htmltools-feedstock/recipe/meta.yaml
+++ b/r-htmltools-feedstock/recipe/meta.yaml
@@ -14,32 +14,48 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/htmltools/htmltools_{{ version }}.tar.gz
   sha256: 44affb82f9c2fd76c9e2b58f9229adb003217932b68c3fdbf1327c8d74c868a2
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: markdown, testthat
 requirements:
   build:
     - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('cxx') }}      # [not win]
+    - {{native}}toolchain        # [win]
     - r-rcpp
     - r-digest
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-rcpp
     - r-digest
 
 test:
   commands:
-    - $R -e "library('htmltools')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('htmltools')"           # [not win]
     - "\"%R%\" -e \"library('htmltools')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/rstudio/htmltools
   license: GPL (>= 2)
@@ -50,3 +66,32 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: htmltools
+# Type: Package
+# Title: Tools for HTML
+# Version: 0.3.6
+# Date: 2017-04-26
+# Author: RStudio, Inc.
+# Maintainer: Joe Cheng <joe@rstudio.com>
+# Description: Tools for HTML generation and output.
+# Depends: R (>= 2.14.1)
+# Imports: utils, digest, Rcpp
+# Suggests: markdown, testthat
+# Enhances: knitr
+# License: GPL (>= 2)
+# URL: https://github.com/rstudio/htmltools
+# BugReports: https://github.com/rstudio/htmltools/issues
+# RoxygenNote: 6.0.1
+# LinkingTo: Rcpp
+# NeedsCompilation: yes
+# Packaged: 2017-04-27 00:04:32 UTC; jcheng
+# Repository: CRAN
+# Date/Publication: 2017-04-28 07:41:46 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-htmlwidgets-feedstock contains modified content
diff --git a/r-htmlwidgets-feedstock/recipe/meta.yaml b/r-htmlwidgets-feedstock/recipe/meta.yaml
index 82a6d75..1c5a7a5 100644
--- a/r-htmlwidgets-feedstock/recipe/meta.yaml
+++ b/r-htmlwidgets-feedstock/recipe/meta.yaml
@@ -14,14 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/htmlwidgets/htmlwidgets_{{ version }}.tar.gz
   sha256: 1154b541ccd868e41d3cf0d7f188f7275ec99f61fe2c7de21c8a05edb19b985e
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: knitr (>= 1.8)
 requirements:
   build:
     - r-base
@@ -37,12 +44,19 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('htmlwidgets')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('htmlwidgets')"           # [not win]
     - "\"%R%\" -e \"library('htmlwidgets')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/ramnathv/htmlwidgets
-  license: MIT
+  license: MIT + file LICENSE
   summary: A framework for creating HTML widgets that render in various contexts including the
     R console, 'R Markdown' documents, and 'Shiny' web applications.
   license_family: MIT
@@ -51,3 +65,32 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: htmlwidgets
+# Type: Package
+# Title: HTML Widgets for R
+# Version: 0.9
+# Date: 2017-07-10
+# Authors@R: c( person("Ramnath", "Vaidyanathan", role = c("aut", "cph")), person("Yihui", "Xie", role = c("aut")), person("JJ", "Allaire", role = c("aut", "cre"), email = "jj@rstudio.com"), person("Joe", "Cheng", role = c("aut")), person("Kenton", "Russell", role = c("aut", "cph")), person(family = "RStudio", role = "cph") )
+# Description: A framework for creating HTML widgets that render in various contexts including the R console, 'R Markdown' documents, and 'Shiny' web applications.
+# License: MIT + file LICENSE
+# VignetteBuilder: knitr
+# Imports: htmltools (>= 0.3), jsonlite (>= 0.9.16), yaml
+# Suggests: knitr (>= 1.8)
+# Enhances: shiny (>= 0.12)
+# URL: https://github.com/ramnathv/htmlwidgets
+# BugReports: https://github.com/ramnathv/htmlwidgets/issues
+# RoxygenNote: 6.0.1
+# NeedsCompilation: no
+# Packaged: 2017-07-10 10:49:02 UTC; jjallaire
+# Author: Ramnath Vaidyanathan [aut, cph], Yihui Xie [aut], JJ Allaire [aut, cre], Joe Cheng [aut], Kenton Russell [aut, cph], RStudio [cph]
+# Maintainer: JJ Allaire <jj@rstudio.com>
+# Repository: CRAN
+# Date/Publication: 2017-07-10 12:42:24 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-httpuv-feedstock contains modified content
diff --git a/r-httpuv-feedstock/recipe/meta.yaml b/r-httpuv-feedstock/recipe/meta.yaml
index fbc9886..c8b80e8 100644
--- a/r-httpuv-feedstock/recipe/meta.yaml
+++ b/r-httpuv-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '1.3.3' %}
+{% set version = '1.3.5' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,39 +12,89 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/httpuv_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/httpuv/httpuv_{{ version }}.tar.gz
-  sha256: bb37452ddc4d9381bee84cdf524582859af6a988e291debb71c8a2e120d02b2a
+  sha256: 4336b993afccca2a194aca577b1975b89a35ac863423b18a11cdbb3f8470e4e9
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
+
+
 requirements:
   build:
     - r-base
-    - r-rcpp
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('cxx') }}      # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
+    - r-rcpp >=0.11.0
 
   run:
     - r-base
-    - r-rcpp
+    - {{native}}gcc-libs         # [win]
+    - r-rcpp >=0.11.0
 
 test:
   commands:
-    - $R -e "library('httpuv')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('httpuv')"           # [not win]
     - "\"%R%\" -e \"library('httpuv')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/rstudio/httpuv
-  license: GPL-3
+  license: GPL-3 | file LICENSE
   summary: Provides low-level socket and protocol support for handling HTTP and WebSocket requests
-    directly from within R.
+    directly from within R. It is primarily intended as a building block for other packages,
+    rather than making it particularly easy to create complete web applications using
+    httpuv alone. httpuv is built on top of the libuv and http-parser C libraries, both
+    of which were developed by Joyent, Inc. (See LICENSE file for libuv and http-parser
+    license information.)
   license_family: GPL3
 
 extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: httpuv
+# Type: Package
+# Title: HTTP and WebSocket Server Library
+# Version: 1.3.5
+# Author: Joe Cheng, Hector Corrada Bravo [ctb], Jeroen Ooms [ctb], Winston Chang [ctb]
+# Copyright: RStudio, Inc.; Joyent, Inc.; Nginx Inc.; Igor Sysoev; Niels Provos; Internet Systems Consortium, Inc.; Alexander Chemeris
+# Maintainer: Joe Cheng <joe@rstudio.com>
+# Description: Provides low-level socket and protocol support for handling HTTP and WebSocket requests directly from within R. It is primarily intended as a building block for other packages, rather than making it particularly easy to create complete web applications using httpuv alone. httpuv is built on top of the libuv and http-parser C libraries, both of which were developed by Joyent, Inc. (See LICENSE file for libuv and http-parser license information.)
+# License: GPL-3 | file LICENSE
+# Depends: R (>= 2.15.1), methods
+# Imports: Rcpp (>= 0.11.0), utils
+# LinkingTo: Rcpp
+# URL: https://github.com/rstudio/httpuv
+# SystemRequirements: GNU make
+# RoxygenNote: 6.0.1
+# NeedsCompilation: yes
+# Packaged: 2017-07-03 21:47:04 UTC; jcheng
+# Repository: CRAN
+# Date/Publication: 2017-07-04 07:02:15 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-httr-feedstock contains modified content
diff --git a/r-httr-feedstock/recipe/meta.yaml b/r-httr-feedstock/recipe/meta.yaml
index 07e4342..89651fe 100644
--- a/r-httr-feedstock/recipe/meta.yaml
+++ b/r-httr-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '1.2.1' %}
+{% set version = '1.3.1' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,15 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/httr_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/httr/httr_{{ version }}.tar.gz
-  sha256: e7b90b90ee52c826e152efcfadf98e078fa75b65a6baaeb8fd25eeed2195730e
+  sha256: 22134d7426b2eba37f0cc34b99285499b8bac9fe75a7bc3222fbad179bf8f258
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: httpuv, jpeg, knitr, png, testthat (>= 0.8.0), readr, xml2, rmarkdown, covr
 requirements:
   build:
     - r-base
@@ -40,12 +48,19 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('httr')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('httr')"           # [not win]
     - "\"%R%\" -e \"library('httr')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
-  home: https://github.com/hadley/httr
-  license: MIT
+  home: https://github.com/r-lib/httr
+  license: MIT + file LICENSE
   summary: Useful tools for working with HTTP organised by HTTP verbs (GET(), POST(), etc). Configuration
     functions make it easy to control additional request components (authenticate(),
     add_headers() and so on).
@@ -55,3 +70,30 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: httr
+# Version: 1.3.1
+# Title: Tools for Working with URLs and HTTP
+# Description: Useful tools for working with HTTP organised by HTTP verbs (GET(), POST(), etc). Configuration functions make it easy to control additional request components (authenticate(), add_headers() and so on).
+# Authors@R: c( person("Hadley", "Wickham", , "hadley@rstudio.com", role = c("aut", "cre")), person("RStudio", role = "cph") )
+# Depends: R (>= 3.0.0)
+# Imports: jsonlite, mime, curl (>= 0.9.1), openssl (>= 0.8), R6
+# Suggests: httpuv, jpeg, knitr, png, testthat (>= 0.8.0), readr, xml2, rmarkdown, covr
+# VignetteBuilder: knitr
+# License: MIT + file LICENSE
+# RoxygenNote: 6.0.1
+# URL: https://github.com/r-lib/httr
+# BugReports: https://github.com/r-lib/httr/issues
+# NeedsCompilation: no
+# Packaged: 2017-08-18 17:47:58 UTC; hadley
+# Author: Hadley Wickham [aut, cre], RStudio [cph]
+# Maintainer: Hadley Wickham <hadley@rstudio.com>
+# Repository: CRAN
+# Date/Publication: 2017-08-20 14:44:14 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-hunspell-feedstock contains modified content
diff --git a/r-hunspell-feedstock/recipe/meta.yaml b/r-hunspell-feedstock/recipe/meta.yaml
index dcb1fcf..42fdfd6 100644
--- a/r-hunspell-feedstock/recipe/meta.yaml
+++ b/r-hunspell-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '2.3' %}
+{% set version = '2.6' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,38 +12,55 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/hunspell_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/hunspell/hunspell_{{ version }}.tar.gz
-  sha256: 52de433bf928cebff282518626379416798682e2d88f46855b2260f792d2f2e9
+  sha256: 78ba41924f8021241f43efbd2befbd72a79ebbbd9bc3a4a0794fb09af468e70b
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: testthat, devtools, pdftools, janeaustenr, wordcloud2, knitr, rmarkdown
 requirements:
   build:
     - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('cxx') }}      # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
     - r-rcpp
     - r-digest
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-rcpp
     - r-digest
 
 test:
   commands:
-    - $R -e "library('hunspell')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('hunspell')"           # [not win]
     - "\"%R%\" -e \"library('hunspell')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
-  home: https://github.com/ropensci/hunspell#readme
-  license: GPL-2
+  home: https://github.com/ropensci/hunspell#readme (devel) https://hunspell.github.io (upstream)
+  license: GPL-2 | LGPL-2.1 | MPL-1.1
   summary: A spell checker and morphological analyzer library designed for languages with rich
     morphology and complex word compounding or character encoding. The package can check
     and analyze individual words as well as search for incorrect words within a text,
@@ -55,3 +72,30 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: hunspell
+# Type: Package
+# Title: High-Performance Stemmer, Tokenizer, and Spell Checker for R
+# Version: 2.6
+# Author: Jeroen Ooms
+# Maintainer: Jeroen Ooms <jeroen@berkeley.edu>
+# Description: A spell checker and morphological analyzer library designed for languages with rich morphology and complex word compounding or character encoding. The package can check and analyze individual words as well as search for incorrect words within a text, latex, html or xml document. Use the 'devtools' package to spell check R documentation with 'hunspell'.
+# License: GPL-2 | LGPL-2.1 | MPL-1.1
+# URL: https://github.com/ropensci/hunspell#readme (devel) https://hunspell.github.io (upstream)
+# BugReports: https://github.com/ropensci/hunspell/issues
+# Imports: Rcpp, digest
+# LinkingTo: Rcpp
+# Suggests: testthat, devtools, pdftools, janeaustenr, wordcloud2, knitr, rmarkdown
+# VignetteBuilder: knitr
+# RoxygenNote: 6.0.1
+# NeedsCompilation: yes
+# Packaged: 2017-06-27 19:53:00 UTC; jeroen
+# Repository: CRAN
+# Date/Publication: 2017-06-28 06:20:07 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-igraph-feedstock contains modified content
diff --git a/r-igraph-feedstock/recipe/0001-Change-GraphML-namespace-fprintf-calls-to-use-format.patch b/r-igraph-feedstock/recipe/0001-Change-GraphML-namespace-fprintf-calls-to-use-format.patch
new file mode 100644
index 0000000..f54abd3
--- /dev/null
+++ b/r-igraph-feedstock/recipe/0001-Change-GraphML-namespace-fprintf-calls-to-use-format.patch
@@ -0,0 +1,34 @@
+From 0ddc7727381d69eb54a17fa27114554712f6f5d5 Mon Sep 17 00:00:00 2001
+From: Ryan Duffin <rad.duffin@gmail.com>
+Date: Tue, 25 Jul 2017 17:17:29 -0400
+Subject: [PATCH] Change GraphML namespace fprintf calls to use format
+ specifiers (#1020)
+
+---
+ src/foreign-graphml.c | 6 +++---
+ 1 file changed, 3 insertions(+), 3 deletions(-)
+
+diff --git a/src/foreign-graphml.c b/src/foreign-graphml.c
+index 1df2ec8f..13dbced1 100644
+--- a/src/foreign-graphml.c
++++ b/src/foreign-graphml.c
+@@ -1405,13 +1405,13 @@ int igraph_write_graph_graphml(const igraph_t *graph, FILE *outstream,
+   
+   ret=fprintf(outstream, "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n");
+   if (ret<0) IGRAPH_ERROR("Write failed", IGRAPH_EFILE);
+-  ret=fprintf(outstream, "<graphml xmlns=\"" GRAPHML_NAMESPACE_URI "\"\n");
++  ret=fprintf(outstream, "<graphml xmlns=\"%s\"\n", GRAPHML_NAMESPACE_URI);
+   if (ret<0) IGRAPH_ERROR("Write failed", IGRAPH_EFILE);
+   ret=fprintf(outstream, "         xmlns:xsi=\"http://www.w3.org/2001/XMLSchema-instance\"\n");
+   if (ret<0) IGRAPH_ERROR("Write failed", IGRAPH_EFILE);
+-  ret=fprintf(outstream, "         xsi:schemaLocation=\"" GRAPHML_NAMESPACE_URI "\n");
++  ret=fprintf(outstream, "         xsi:schemaLocation=\"%s\n", GRAPHML_NAMESPACE_URI);
+   if (ret<0) IGRAPH_ERROR("Write failed", IGRAPH_EFILE);
+-  ret=fprintf(outstream, "         " GRAPHML_NAMESPACE_URI "/1.0/graphml.xsd\">\n");
++  ret=fprintf(outstream, "         %s/1.0/graphml.xsd\">\n", GRAPHML_NAMESPACE_URI);
+   if (ret<0) IGRAPH_ERROR("Write failed", IGRAPH_EFILE);
+   ret=fprintf(outstream, "<!-- Created by igraph -->\n");
+   if (ret<0) IGRAPH_ERROR("Write failed", IGRAPH_EFILE);
+-- 
+2.13.6
+
diff --git a/r-igraph-feedstock/recipe/build.sh b/r-igraph-feedstock/recipe/build.sh
index af6547b..868576c 100644
--- a/r-igraph-feedstock/recipe/build.sh
+++ b/r-igraph-feedstock/recipe/build.sh
@@ -1,2 +1,4 @@
 #!/bin/bash
+
+export LDFLAGS="${LDFLAGS} -L${PREFIX}/lib"
 $R CMD INSTALL --build .
diff --git a/r-igraph-feedstock/recipe/meta.yaml b/r-igraph-feedstock/recipe/meta.yaml
index a760735..2d4e105 100644
--- a/r-igraph-feedstock/recipe/meta.yaml
+++ b/r-igraph-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '1.0.1' %}
+{% set version = '1.1.2' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,43 +12,61 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/igraph_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/igraph/igraph_{{ version }}.tar.gz
-  sha256: dc64ed09b8b5f8d66ed4936cde3491974d6bc5178dd259b6eab7ef3936aa5602
+  sha256: 89b16b41bc77949ea208419e52a18b78b5d418c7fedc52cd47d06a51a6e746ec
+  patches:
+    - 0001-Change-GraphML-namespace-fprintf-calls-to-use-format.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  # compilation errors on windows
-  skip: true  # [win]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: ape, graph, igraphdata, NMF, rgl, scales, stats4, tcltk, testthat
 requirements:
   build:
     - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('cxx') }}      # [not win]
+    - {{ compiler('fortran') }}  # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}autoconf
+    - {{posix}}automake
+    - {{posix}}pkg-config
+    - {{posix}}make
+    - {{native}}gmp
+    - {{native}}libxml2
     - r-matrix
-    - r-nmf
     - r-irlba
     - r-magrittr
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
-    - {{native}}gmp >=5.0.1,<7
+    - r-pkgconfig >=2.0.0
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
+    - {{native}}gmp
+    - {{native}}libxml2
     - r-matrix
-    - r-nmf
     - r-irlba
     - r-magrittr
-    - libgcc  # [not win]
-    - {{native}}gmp >=5.0.1,<7
+    - r-pkgconfig >=2.0.0
 
 test:
   commands:
-    - $R -e "library('igraph')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('igraph')"           # [not win]
     - "\"%R%\" -e \"library('igraph')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://igraph.org
   license: GPL (>= 2)
@@ -61,3 +79,30 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: igraph
+# Version: 1.1.2
+# Title: Network Analysis and Visualization
+# Author: See AUTHORS file.
+# Maintainer: Gabor Csardi <csardi.gabor@gmail.com>
+# Description: Routines for simple graphs and network analysis. It can handle large graphs very well and provides functions for generating random and regular graphs, graph visualization, centrality methods and much more.
+# Depends: methods
+# Imports: graphics, grDevices, irlba, magrittr, Matrix, pkgconfig (>= 2.0.0), stats, utils
+# Suggests: ape, graph, igraphdata, NMF, rgl, scales, stats4, tcltk, testthat
+# License: GPL (>= 2)
+# URL: http://igraph.org
+# SystemRequirements: gmp, libxml2
+# BugReports: https://github.com/igraph/igraph/issues
+# Encoding: UTF-8
+# Collate: 'adjacency.R' 'auto.R' 'assortativity.R' 'attributes.R' 'basic.R' 'bipartite.R' 'centrality.R' 'centralization.R' 'cliques.R' 'cocitation.R' 'cohesive.blocks.R' 'community.R' 'components.R' 'console.R' 'conversion.R' 'data_frame.R' 'decomposition.R' 'degseq.R' 'demo.R' 'embedding.R' 'epi.R' 'fit.R' 'flow.R' 'foreign.R' 'games.R' 'glet.R' 'hrg.R' 'igraph-package.R' 'incidence.R' 'indexing.R' 'interface.R' 'iterators.R' 'layout.R' 'layout_drl.R' 'lazyeval.R' 'make.R' 'mgclust.R' 'minimum.spanning.tree.R' 'motifs.R' 'nexus.R' 'operators.R' 'other.R' 'package.R' 'palette.R' 'par.R' 'paths.R' 'plot.R' 'plot.common.R' 'plot.shapes.R' 'pp.R' 'print.R' 'printr.R' 'random_walk.R' 'rewire.R' 'scan.R' 'scg.R' 'sgm.R' 'similarity.R' 'simple.R' 'sir.R' 'socnet.R' 'sparsedf.R' 'structural.properties.R' 'structure.info.R' 'test.R' 'tkplot.R' 'topology.R' 'triangles.R' 'utils.R' 'uuid.R' 'versions.R' 'weakref.R' 'zzz-deprecate.R'
+# NeedsCompilation: yes
+# Packaged: 2017-07-20 21:33:52 UTC; gaborcsardi
+# Repository: CRAN
+# Date/Publication: 2017-07-21 11:39:40 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-influencer-feedstock contains modified content
diff --git a/r-influencer-feedstock/recipe/meta.yaml b/r-influencer-feedstock/recipe/meta.yaml
index 480b9e0..a7842cc 100644
--- a/r-influencer-feedstock/recipe/meta.yaml
+++ b/r-influencer-feedstock/recipe/meta.yaml
@@ -14,33 +14,49 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/influenceR/influenceR_{{ version }}.tar.gz
   sha256: 4fc9324179bd8896875fc0e879a8a96b9ef2a6cf42a296c3b7b4d9098519e98a
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  # igraph is not available for windows
-  skip: true  # [win]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: testthat
 requirements:
   build:
     - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
     - r-matrix >=1.1_4
     - r-igraph >=1.0.1
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-matrix >=1.1_4
     - r-igraph >=1.0.1
-    - libgcc  # [not win]
 
 test:
   commands:
-    - $R -e "library('influenceR')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('influenceR')"           # [not win]
     - "\"%R%\" -e \"library('influenceR')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/rcc-uchicago/influenceR
   license: GPL-2
@@ -56,3 +72,28 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: influenceR
+# Title: Software Tools to Quantify Structural Importance of Nodes in a Network
+# Version: 0.1.0
+# Authors@R: c( person("Jacobs", "Simon", email = "sdjacobs@uchicago.edu", role = "aut"), person("Khanna", "Aditya", email = "khanna7@uchicago.edu", role = c("aut", "cre")), person("Madduri", "Kamesh", email = "madduri@cse.psu.edu", role = "ctb"), person("Bader", "David", email = "bader@cc.gatech.edu", role = "ctb"))
+# Description: Provides functionality to compute various node centrality measures on networks. Included are functions to compute betweenness centrality (by utilizing Madduri and Bader's SNAP library), implementations of Burt's constraint and effective network size (ENS) metrics, Borgatti's algorithm to identify key players, and Valente's bridging metric. On Unix systems, the betweenness, Key Players, and bridging implementations are parallelized with OpenMP, which may run faster on systems which have OpenMP configured.
+# Depends: R (>= 3.2.0)
+# License: GPL-2
+# LazyData: true
+# Imports: igraph (>= 1.0.1), Matrix (>= 1.1-4), methods, utils
+# NeedsCompilation: yes
+# Suggests: testthat
+# URL: https://github.com/rcc-uchicago/influenceR
+# Packaged: 2015-09-02 16:20:53 UTC; sdjacobs
+# Author: Jacobs Simon [aut], Khanna Aditya [aut, cre], Madduri Kamesh [ctb], Bader David [ctb]
+# Maintainer: Khanna Aditya <khanna7@uchicago.edu>
+# Repository: CRAN
+# Date/Publication: 2015-09-03 12:20:15
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-inline-feedstock contains modified content
diff --git a/r-inline-feedstock/recipe/meta.yaml b/r-inline-feedstock/recipe/meta.yaml
index 8f98ec2..fe68fc3 100644
--- a/r-inline-feedstock/recipe/meta.yaml
+++ b/r-inline-feedstock/recipe/meta.yaml
@@ -12,19 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/inline_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/inline/inline_{{ version }}.tar.gz
+  sha256: fd34d6bf965148d26d983a022a0ff7bc1a5831f6ca066deee3f6139894dfc931
 
 
-  sha256: fd34d6bf965148d26d983a022a0ff7bc1a5831f6ca066deee3f6139894dfc931
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: True  # [win32]
-
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: Rcpp (>= 0.11.0)
 requirements:
   build:
     - r-base
@@ -34,10 +38,15 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('inline')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('inline')"           # [not win]
     - "\"%R%\" -e \"library('inline')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
 
+  # requires:
+    # Put any additional test requirements here.
 
 about:
   home: https://CRAN.R-project.org/package=inline
@@ -46,8 +55,31 @@ about:
     or Fortran code supporting .C and .Call calling conventions.
   license_family: LGPL
 
-
-
 extra:
   recipe-maintainers:
     - croth1
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: inline
+# Version: 0.3.14
+# Date: 2015-04-11
+# Title: Functions to Inline C, C++, Fortran Function Calls from R
+# Author: Oleg Sklyar, Duncan Murdoch, Mike Smith, Dirk Eddelbuettel,  Romain Francois, Karline Soetaert
+# Maintainer: Dirk Eddelbuettel <edd@debian.org>
+# Depends: R (>= 2.4.0), methods
+# Suggests: Rcpp (>= 0.11.0)
+# Description: Functionality to dynamically define R functions and S4 methods with inlined C, C++ or Fortran code supporting .C and .Call calling conventions.
+# License: LGPL
+# Copyright: Oleg Sklyar, 2005-2010 and other authors per their commits
+# LazyLoad: yes
+# BugReports: https://github.com/eddelbuettel/inline/issues
+# NeedsCompilation: no
+# Packaged: 2015-04-12 14:35:08.942414 UTC; edd
+# Repository: CRAN
+# Date/Publication: 2015-04-13 09:20:41
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
diff --git a/r-ipred-feedstock/recipe/bld.bat b/r-ipred-feedstock/recipe/bld.bat
index e69de29..8b10c94 100644
--- a/r-ipred-feedstock/recipe/bld.bat
+++ b/r-ipred-feedstock/recipe/bld.bat
@@ -0,0 +1,8 @@
+"%R%" CMD INSTALL --build .
+if errorlevel 1 exit 1
+
+@rem Add more build steps here, if they are necessary.
+
+@rem See
+@rem http://docs.continuum.io/conda/build.html
+@rem for a list of environment variables that are set during the build process.
diff --git a/r-ipred-feedstock/recipe/build.sh b/r-ipred-feedstock/recipe/build.sh
index e69de29..0c37df6 100644
--- a/r-ipred-feedstock/recipe/build.sh
+++ b/r-ipred-feedstock/recipe/build.sh
@@ -0,0 +1,13 @@
+#!/bin/bash
+
+# R refuses to build packages that mark themselves as Priority: Recommended
+mv DESCRIPTION DESCRIPTION.old
+grep -v '^Priority: ' DESCRIPTION.old > DESCRIPTION
+
+$R CMD INSTALL --build .
+
+# Add more build steps here, if they are necessary.
+
+# See
+# http://docs.continuum.io/conda/build.html
+# for a list of environment variables that are set during the build process.
diff --git a/r-ipred-feedstock/recipe/meta.yaml b/r-ipred-feedstock/recipe/meta.yaml
index e69de29..ee23a41 100644
--- a/r-ipred-feedstock/recipe/meta.yaml
+++ b/r-ipred-feedstock/recipe/meta.yaml
@@ -0,0 +1,100 @@
+{% set version = '0.9-6' %}
+
+{% set posix = 'm2-' if win else '' %}
+{% set native = 'm2w64-' if win else '' %}
+
+package:
+  name: r-ipred
+  version: {{ version|replace("-", "_") }}
+
+source:
+  fn: ipred_{{ version }}.tar.gz
+  url:
+    - https://cran.r-project.org/src/contrib/ipred_{{ version }}.tar.gz
+    - https://cran.r-project.org/src/contrib/Archive/ipred/ipred_{{ version }}.tar.gz
+  sha256: b8d36438eb9b7209d27b85738dcad03b2e535dcb2f4191432780d9cbf00d3cef
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
+build:
+  # If this is a new build for the same version, increment the build number.
+  number: 0
+
+  # This is required to make R link correctly on Linux.
+  rpaths:
+    - lib/R/lib/
+    - lib/
+
+# Suggests: mvtnorm, mlbench, TH.data
+requirements:
+  build:
+    - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
+    - r-mass
+    - r-class
+    - r-nnet
+    - r-prodlim
+    - r-rpart >=3.1_8
+    - r-survival
+
+  run:
+    - r-base
+    - {{native}}gcc-libs         # [win]
+    - r-mass
+    - r-class
+    - r-nnet
+    - r-prodlim
+    - r-rpart >=3.1_8
+    - r-survival
+
+test:
+  commands:
+    # You can put additional test commands to be run here.
+    - $R -e "library('ipred')"           # [not win]
+    - "\"%R%\" -e \"library('ipred')\""  # [win]
+
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
+about:
+  home: https://CRAN.R-project.org/package=ipred
+  license: GPL (>= 2)
+  summary: 'Improved predictive models by indirect classification and bagging for classification,
+    regression and survival problems  as well as resampling based estimators of prediction
+    error. '
+
+  license_family: GPL3
+
+extra:
+  recipe-maintainers:
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: ipred
+# Title: Improved Predictors
+# Version: 0.9-6
+# Date: 2017-03-01
+# Authors@R: c(person("Andrea", "Peters", role = "aut"), person("Torsten", "Hothorn", role = c("aut", "cre"), email = "Torsten.Hothorn@R-project.org"), person("Brian D.", "Ripley", role = "ctb"), person("Terry", "Therneau", role = "ctb"),  person("Beth", "Atkinson", role = "ctb"))
+# Description: Improved predictive models by indirect classification and bagging for classification, regression and survival problems  as well as resampling based estimators of prediction error.
+# Depends: R (>= 2.10)
+# Imports: rpart (>= 3.1-8), MASS, survival, nnet, class, prodlim
+# Suggests: mvtnorm, mlbench, TH.data
+# License: GPL (>= 2)
+# NeedsCompilation: yes
+# Packaged: 2017-03-01 14:07:15 UTC; hothorn
+# Author: Andrea Peters [aut], Torsten Hothorn [aut, cre], Brian D. Ripley [ctb], Terry Therneau [ctb], Beth Atkinson [ctb]
+# Maintainer: Torsten Hothorn <Torsten.Hothorn@R-project.org>
+# Repository: CRAN
+# Date/Publication: 2017-03-01 19:05:51
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-irdisplay-feedstock contains modified content
diff --git a/r-irdisplay-feedstock/recipe/bld.bat b/r-irdisplay-feedstock/recipe/bld.bat
new file mode 100644
index 0000000..8b10c94
--- /dev/null
+++ b/r-irdisplay-feedstock/recipe/bld.bat
@@ -0,0 +1,8 @@
+"%R%" CMD INSTALL --build .
+if errorlevel 1 exit 1
+
+@rem Add more build steps here, if they are necessary.
+
+@rem See
+@rem http://docs.continuum.io/conda/build.html
+@rem for a list of environment variables that are set during the build process.
diff --git a/r-irdisplay-feedstock/recipe/build.sh b/r-irdisplay-feedstock/recipe/build.sh
new file mode 100644
index 0000000..0c37df6
--- /dev/null
+++ b/r-irdisplay-feedstock/recipe/build.sh
@@ -0,0 +1,13 @@
+#!/bin/bash
+
+# R refuses to build packages that mark themselves as Priority: Recommended
+mv DESCRIPTION DESCRIPTION.old
+grep -v '^Priority: ' DESCRIPTION.old > DESCRIPTION
+
+$R CMD INSTALL --build .
+
+# Add more build steps here, if they are necessary.
+
+# See
+# http://docs.continuum.io/conda/build.html
+# for a list of environment variables that are set during the build process.
diff --git a/r-irdisplay-feedstock/recipe/meta.yaml b/r-irdisplay-feedstock/recipe/meta.yaml
index a7d9287..5c64d3f 100644
--- a/r-irdisplay-feedstock/recipe/meta.yaml
+++ b/r-irdisplay-feedstock/recipe/meta.yaml
@@ -1,44 +1,89 @@
-{% set version = "0.4.4" %}
+{% set version = '0.4.4' %}
+
+{% set posix = 'm2-' if win else '' %}
+{% set native = 'm2w64-' if win else '' %}
 
 package:
   name: r-irdisplay
-  version: {{ version }}
+  version: {{ version|replace("-", "_") }}
 
 source:
-  fn: r-irdisplay-{{ version }}.tar.gz
-  url: https://github.com/IRkernel/IRdisplay/archive/{{ version }}.tar.gz
-  sha256: 9713b552cb741b03b2852b7f31dbfefd3cc17edd9661d918e79e29a23621922e
+  fn: IRdisplay_{{ version }}.tar.gz
+  url:
+    - https://cran.r-project.org/src/contrib/IRdisplay_{{ version }}.tar.gz
+    - https://cran.r-project.org/src/contrib/Archive/IRdisplay/IRdisplay_{{ version }}.tar.gz
+  sha256: e83a0bc52800618bf9a3ac5ef3d432512e00f392b7216fd515fca319377584a6
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: True  # [win32]
-  script: R CMD INSTALL --build .
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
-requirements:  # [not win32]
-  build:  # [not win32]
-    - r-base  # [not win32]
-    - posix  # [win64]
-    - m2-git  # [win64]
-    - r-repr  # [not win32]
-  run:  # [not win32]
-    - r-base  # [not win32]
-    - r-repr  # [not win32]
+# Suggests: testthat, withr
+requirements:
+  build:
+    - r-base
+    - r-repr
+
+  run:
+    - r-base
+    - r-repr
 
 test:
   commands:
-    - R -e "library('IRdisplay')"  # [not win]
-    - R -e \"library('IRdisplay')\"  # [win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('IRdisplay')"           # [not win]
+    - "\"%R%\" -e \"library('IRdisplay')\""  # [win]
+
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
 
 about:
-  home: https://github.com/IRkernel/IRdisplay/
-  license: MIT
-  license_file: LICENSE
-  summary: "An interface to the rich display capabilities of 'Jupyter' front-ends (e.g. 'Jupyter
-    Notebook'). Designed to be used from a running 'IRkernel'."
+  home: https://CRAN.R-project.org/package=IRdisplay
+  license: MIT + file LICENSE
+  summary: ' An interface to the rich display capabilities of ''Jupyter'' front-ends (e.g. ''Jupyter
+    Notebook'') <https://jupyter.org>. Designed to be used from a running ''IRkernel''
+    session <https://irkernel.github.io>.'
+
+  license_family: MIT
 
 extra:
   recipe-maintainers:
     - ocefpaf
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: IRdisplay
+# Title: 'Jupyter' Display Machinery
+# Description:  An interface to the rich display capabilities of 'Jupyter' front-ends (e.g. 'Jupyter Notebook') <https://jupyter.org>. Designed to be used from a running 'IRkernel' session <https://irkernel.github.io>.
+# Version: 0.4.4
+# Authors@R: c( person('Thomas', 'Kluyver', role = c('aut', 'cph'), email = 'thomas@kluyver.me.uk'), person('Philipp', 'Angerer', role = c('aut', 'cph', 'cre'), email = 'phil.angerer@gmail.com'), person('Jan', 'Schulz', role = c('aut', 'cph'), email = 'jasc@gmx.net'))
+# Depends: R (>= 3.0.1)
+# Suggests: testthat, withr
+# Imports: repr
+# License: MIT + file LICENSE
+# LazyData: true
+# RoxygenNote: 5.0.1
+# NeedsCompilation: no
+# Packaged: 2016-08-02 11:52:46 UTC; angerer
+# Author: Thomas Kluyver [aut, cph], Philipp Angerer [aut, cph, cre], Jan Schulz [aut, cph]
+# Maintainer: Philipp Angerer <phil.angerer@gmail.com>
+# Repository: CRAN
+# Date/Publication: 2016-08-02 14:45:35
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-irkernel-feedstock contains modified content
diff --git a/r-irkernel-feedstock/recipe/meta.yaml b/r-irkernel-feedstock/recipe/meta.yaml
index 384b5de..227bae0 100644
--- a/r-irkernel-feedstock/recipe/meta.yaml
+++ b/r-irkernel-feedstock/recipe/meta.yaml
@@ -1,62 +1,98 @@
-{% set version = "0.8.7" %}
+{% set version = '0.8.9' %}
+
+{% set posix = 'm2-' if win else '' %}
+{% set native = 'm2w64-' if win else '' %}
 
 package:
   name: r-irkernel
-  version: {{ version }}
+  version: {{ version|replace("-", "_") }}
 
 source:
 
 
-  git_url: https://github.com/IRkernel/IRkernel
-  git_tag: 0.8.7
   # You can add a hash for the file here, like md5, sha1 or sha256
+  git_url: https://github.com/IRkernel/IRkernel
+  git_tag: 0.8.9
   # patches:
    # List any patch files here
    # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: True  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
-requirements:  # [not win32]
-  build:  # [not win32]
-    - r-base  # [not win32]
-    - posix  # [win64]
-    - m2-git  # [win64]
-    - r-irdisplay >=0.3.0.9999  # [not win32]
-    - r-crayon  # [not win32]
-    - r-digest  # [not win32]
-    - r-evaluate >=0.5.4  # [not win32]
-    - r-jsonlite >=0.9.6  # [not win32]
-    - r-pbdzmq >=0.2_1  # [not win32]
-    - r-repr >=0.4.99  # [not win32]
-    - r-uuid  # [not win32]
-  run:  # [not win32]
-    - r-base  # [not win32]
-    - r-irdisplay >=0.3.0.9999  # [not win32]
-    - r-crayon  # [not win32]
-    - r-digest  # [not win32]
-    - r-evaluate >=0.5.4  # [not win32]
-    - r-jsonlite >=0.9.6  # [not win32]
-    - r-pbdzmq >=0.2_1  # [not win32]
-    - r-repr >=0.4.99  # [not win32]
-    - r-uuid  # [not win32]
+# Suggests: testthat, roxygen2
+requirements:
+  build:
+    - r-base
+    - {{posix}}filesystem        # [win]
+    - {{posix}}git
+    - r-irdisplay >=0.3.0.9999
+    - r-crayon
+    - r-digest
+    - r-evaluate >=0.10
+    - r-jsonlite >=0.9.6
+    - r-pbdzmq >=0.2_1
+    - r-repr >=0.4.99
+    - r-uuid
+
+  run:
+    - r-base
+    - r-irdisplay >=0.3.0.9999
+    - r-crayon
+    - r-digest
+    - r-evaluate >=0.10
+    - r-jsonlite >=0.9.6
+    - r-pbdzmq >=0.2_1
+    - r-repr >=0.4.99
+    - r-uuid
 
 test:
   commands:
-    - R -e "library('IRkernel')"  # [not win]
-    - R -e \"library('IRkernel')\"  # [win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('IRkernel')"           # [not win]
+    - "\"%R%\" -e \"library('IRkernel')\""  # [win]
+
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
 
 about:
   home: https://github.com/IRkernel/IRkernel
-  license: MIT
-  license_file: LICENSE
-  summary: "The R kernel for the 'Jupyter' environment executes R code which the front-end ('Jupyter
-    Notebook' or other front-ends) submits to the kernel via the network."
+  license: MIT + file LICENSE
+  summary: The R kernel for the 'Jupyter' environment executes R code which the front-end ('Jupyter
+    Notebook' or other front-ends) submits to the kernel via the network.
+  license_family: MIT
 
 extra:
   recipe-maintainers:
     - ocefpaf
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: IRkernel
+# Title: Native R Kernel for the 'Jupyter Notebook'
+# Description: The R kernel for the 'Jupyter' environment executes R code which the front-end ('Jupyter Notebook' or other front-ends) submits to the kernel via the network.
+# Version: 0.8.9
+# Authors@R: c( person('Thomas', 'Kluyver', role = c('aut', 'cph'), email = 'thomas@kluyver.me.uk'), person('Philipp', 'Angerer', role = c('aut', 'cph', 'cre'), email = 'phil.angerer@gmail.com'), person('Jan', 'Schulz', role = c('aut', 'cph'), email = 'jasc@gmx.net'), person('Karthik', 'Ram', role = c('aut', 'cph'), email = 'karthik.ram@gmail.com'))
+# Depends: R (>= 3.2.0)
+# Suggests: testthat, roxygen2
+# SystemRequirements: jupyter, jupyter_kernel_test (Python package for testing)
+# License: MIT + file LICENSE
+# LazyData: true
+# Encoding: UTF-8
+# Imports: repr (>= 0.4.99), methods, evaluate (>= 0.10), IRdisplay (>= 0.3.0.9999), pbdZMQ (>= 0.2-1), crayon, jsonlite (>= 0.9.6), uuid, digest
+# Collate: 'class_unions.r' 'logging.r' 'comm_manager.r' 'compat.r' 'environment_runtime.r' 'environment_shadow.r' 'options.r' 'execution.r' 'handlers.r' 'help.r' 'installspec.r' 'utils.r' 'kernel.r' 'main.r'
+# RoxygenNote: 6.0.1.9000
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-irlba-feedstock contains modified content
diff --git a/r-irlba-feedstock/recipe/meta.yaml b/r-irlba-feedstock/recipe/meta.yaml
index 2b8515b..abacf42 100644
--- a/r-irlba-feedstock/recipe/meta.yaml
+++ b/r-irlba-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '2.1.2' %}
+{% set version = '2.2.1' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,42 +12,85 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/irlba_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/irlba/irlba_{{ version }}.tar.gz
-  sha256: 5183e8dd7943df11c0f44460566adf06c03d5320f142699298f516d423b06ce1
+  sha256: 1a00d36128a5ef38c8ad911ad7671768d50db0171e3930b57ac60670423fcb7d
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('fortran') }}  # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
     - r-matrix
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-matrix
-    - libgcc  # [not win]
 
 test:
   commands:
-    - $R -e "library('irlba')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('irlba')"           # [not win]
     - "\"%R%\" -e \"library('irlba')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=irlba
   license: GPL-3
-  summary: Fast and memory efficient methods for truncated singular and eigenvalue decompositions
-    and principal component analysis of large sparse or dense matrices.
+  summary: Fast and memory efficient methods for truncated singular value decomposition and principal
+    components analysis of large sparse and dense matrices.
   license_family: GPL3
 
 extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: irlba
+# Type: Package
+# Title: Fast Truncated Singular Value Decomposition and Principal Components Analysis for Large Dense and Sparse Matrices
+# Version: 2.2.1
+# Date: 2017-05-16
+# Authors@R: c( person("Jim", "Baglama", rol=c("aut", "cph"), email="jbaglama@uri.edu"), person("Lothar", "Reichel", role=c("aut", "cph"), email="reichel@math.kent.edu"), person("B. W.", "Lewis", role=c("aut","cre","cph"), email="blewis@illposed.net"))
+# Description: Fast and memory efficient methods for truncated singular value decomposition and principal components analysis of large sparse and dense matrices.
+# Depends: Matrix
+# LinkingTo: Matrix
+# Imports: stats, methods
+# License: GPL-3
+# BugReports: https://github.com/bwlewis/irlba/issues
+# RoxygenNote: 5.0.1
+# NeedsCompilation: yes
+# Packaged: 2017-05-16 20:24:46 UTC; blewis
+# Author: Jim Baglama [aut, cph], Lothar Reichel [aut, cph], B. W. Lewis [aut, cre, cph]
+# Maintainer: B. W. Lewis <blewis@illposed.net>
+# Repository: CRAN
+# Date/Publication: 2017-05-17 07:28:01 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-iterators-feedstock contains modified content
diff --git a/r-iterators-feedstock/recipe/meta.yaml b/r-iterators-feedstock/recipe/meta.yaml
index 85160c1..18eb677 100644
--- a/r-iterators-feedstock/recipe/meta.yaml
+++ b/r-iterators-feedstock/recipe/meta.yaml
@@ -14,14 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/iterators/iterators_{{ version }}.tar.gz
   sha256: ae4ea23385776eb0c06c992a3da6b0256a6c84558c1061034c5a1fbdd43d05b8
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: RUnit
 requirements:
   build:
     - r-base
@@ -31,9 +38,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('iterators')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('iterators')"           # [not win]
     - "\"%R%\" -e \"library('iterators')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=iterators
   license: Apache License (== 2.0)
@@ -45,3 +59,29 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: iterators
+# Type: Package
+# Title: Provides Iterator Construct for R
+# Version: 1.0.8
+# Authors@R: c(person("Rich", "Calaway", role="cre", email="richcala@microsoft.com"), person("Revolution", "Analytics", role=c("aut", "cph")), person("Steve", "Weston", role="aut"))
+# Description: Support for iterators, which allow a programmer to traverse through all the elements of a vector, list, or other collection of data.
+# Depends: R (>= 2.5.0), utils
+# Suggests: RUnit
+# License: Apache License (== 2.0)
+# Author: Rich Calaway [cre], Revolution Analytics [aut, cph], Steve Weston [aut]
+# Maintainer: Rich Calaway <richcala@microsoft.com>
+# Repository: CRAN
+# Repository/R-Forge/Project: foreach
+# Repository/R-Forge/Revision: 27
+# Repository/R-Forge/DateTimeStamp: 2015-10-12 01:37:31
+# Date/Publication: 2015-10-13 09:12:54
+# NeedsCompilation: no
+# Packaged: 2015-10-12 02:25:27 UTC; rforge
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-janeaustenr-feedstock contains modified content
diff --git a/r-janeaustenr-feedstock/recipe/meta.yaml b/r-janeaustenr-feedstock/recipe/meta.yaml
index d1a5afb..c82cff5 100644
--- a/r-janeaustenr-feedstock/recipe/meta.yaml
+++ b/r-janeaustenr-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '0.1.4' %}
+{% set version = '0.1.5' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,16 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/janeaustenr_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/janeaustenr/janeaustenr_{{ version }}.tar.gz
-  sha256: 318125445be20af3f2d17c384eec0c27b702d0e8c0823af41b0433cd1449b3fb
+  sha256: 992f6673653daf7010fe176993a01cd4127d9a88be428da8da7a28241826d6f3
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: dplyr, testthat
 requirements:
   build:
     - r-base
@@ -31,13 +38,19 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('janeaustenr')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('janeaustenr')"           # [not win]
     - "\"%R%\" -e \"library('janeaustenr')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/juliasilge/janeaustenr
-  license: MIT
-  license_file: LICENSE
+  license: MIT + file LICENSE
   summary: Full texts for Jane Austen's 6 completed novels, ready for text analysis. These novels
     are "Sense and Sensibility", "Pride and Prejudice", "Mansfield Park", "Emma", "Northanger
     Abbey", and "Persuasion".
@@ -47,3 +60,30 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: janeaustenr
+# Title: Jane Austen's Complete Novels
+# Version: 0.1.5
+# Authors@R: person("Julia", "Silge", email = "julia.silge@gmail.com", role = c("aut", "cre"))
+# Description: Full texts for Jane Austen's 6 completed novels, ready for text analysis. These novels are "Sense and Sensibility", "Pride and Prejudice", "Mansfield Park", "Emma", "Northanger Abbey", and "Persuasion".
+# URL: https://github.com/juliasilge/janeaustenr
+# BugReports: https://github.com/juliasilge/janeaustenr/issues
+# Depends: R (>= 3.1.2)
+# Suggests: dplyr, testthat
+# License: MIT + file LICENSE
+# LazyData: true
+# RoxygenNote: 6.0.1
+# Encoding: UTF-8
+# NeedsCompilation: no
+# Packaged: 2017-06-09 16:56:07 UTC; juliasilge
+# Author: Julia Silge [aut, cre]
+# Maintainer: Julia Silge <julia.silge@gmail.com>
+# Repository: CRAN
+# Date/Publication: 2017-06-10 06:29:05 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-jpeg-feedstock contains modified content
diff --git a/r-jpeg-feedstock/recipe/meta.yaml b/r-jpeg-feedstock/recipe/meta.yaml
index c990c5b..024fb78 100644
--- a/r-jpeg-feedstock/recipe/meta.yaml
+++ b/r-jpeg-feedstock/recipe/meta.yaml
@@ -14,29 +14,45 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/jpeg/jpeg_{{ version }}.tar.gz
   sha256: d032befeb3a414cefdbf70ba29a6c01541c54387cc0a1a98a4022d86cbe60a16
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('jpeg')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('jpeg')"           # [not win]
     - "\"%R%\" -e \"library('jpeg')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://www.rforge.net/jpeg/
   license: GPL-2 | GPL-3
@@ -48,3 +64,25 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: jpeg
+# Version: 0.1-8
+# Title: Read and write JPEG images
+# Author: Simon Urbanek <Simon.Urbanek@r-project.org>
+# Maintainer: Simon Urbanek <Simon.Urbanek@r-project.org>
+# Depends: R (>= 2.9.0)
+# Description: This package provides an easy and simple way to read, write and display bitmap images stored in the JPEG format. It can read and write both files and in-memory raw vectors.
+# License: GPL-2 | GPL-3
+# SystemRequirements: libjpeg
+# URL: http://www.rforge.net/jpeg/
+# Packaged: 2014-01-23 19:05:03 UTC; svnuser
+# NeedsCompilation: yes
+# Repository: CRAN
+# Date/Publication: 2014-01-23 20:17:07
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-jsonlite-feedstock contains modified content
diff --git a/r-jsonlite-feedstock/recipe/bld.bat b/r-jsonlite-feedstock/recipe/bld.bat
new file mode 100644
index 0000000..8b10c94
--- /dev/null
+++ b/r-jsonlite-feedstock/recipe/bld.bat
@@ -0,0 +1,8 @@
+"%R%" CMD INSTALL --build .
+if errorlevel 1 exit 1
+
+@rem Add more build steps here, if they are necessary.
+
+@rem See
+@rem http://docs.continuum.io/conda/build.html
+@rem for a list of environment variables that are set during the build process.
diff --git a/r-jsonlite-feedstock/recipe/build.sh b/r-jsonlite-feedstock/recipe/build.sh
new file mode 100644
index 0000000..0c37df6
--- /dev/null
+++ b/r-jsonlite-feedstock/recipe/build.sh
@@ -0,0 +1,13 @@
+#!/bin/bash
+
+# R refuses to build packages that mark themselves as Priority: Recommended
+mv DESCRIPTION DESCRIPTION.old
+grep -v '^Priority: ' DESCRIPTION.old > DESCRIPTION
+
+$R CMD INSTALL --build .
+
+# Add more build steps here, if they are necessary.
+
+# See
+# http://docs.continuum.io/conda/build.html
+# for a list of environment variables that are set during the build process.
diff --git a/r-jsonlite-feedstock/recipe/meta.yaml b/r-jsonlite-feedstock/recipe/meta.yaml
index 8369bfe..9a80f80 100644
--- a/r-jsonlite-feedstock/recipe/meta.yaml
+++ b/r-jsonlite-feedstock/recipe/meta.yaml
@@ -1,43 +1,62 @@
-{% set version = "1.4" %}
+{% set version = '1.5' %}
+
+{% set posix = 'm2-' if win else '' %}
+{% set native = 'm2w64-' if win else '' %}
 
 package:
   name: r-jsonlite
-  version: {{ version }}
+  version: {{ version|replace("-", "_") }}
 
 source:
   fn: jsonlite_{{ version }}.tar.gz
   url:
-    - http://cran.r-project.org/src/contrib/jsonlite_{{ version }}.tar.gz
-    - http://cran.r-project.org/src/contrib/Archive/jsonlite/jsonlite_{{ version }}.tar.gz
-  sha256: 56ab5c29dd86903de3d859db440d9ddb7fb9bbfc5181a6a24d27475cae9c2f87
+    - https://cran.r-project.org/src/contrib/jsonlite_{{ version }}.tar.gz
+    - https://cran.r-project.org/src/contrib/Archive/jsonlite/jsonlite_{{ version }}.tar.gz
+  sha256: 6490371082a387cb1834048ad8cdecacb8b6b6643751b50298c741490c798e02
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: True  # [win32]
-  script: R CMD INSTALL --build .
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
-requirements:  # [not win32]
-  build:  # [not win32]
-    - r-base  # [not win32]
-    - posix  # [win64]
-    - m2w64-toolchain  # [win64]
-    - gcc  # [not win]
-  run:  # [not win32]
-    - r-base  # [not win32]
+# Suggests: httr, curl, plyr, testthat, knitr, rmarkdown, R.rsp, sp
+requirements:
+  build:
+    - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
+
+  run:
+    - r-base
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - R -e "library('jsonlite')"  # [not win]
-    - R -e \"library('jsonlite')\"  # [win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('jsonlite')"           # [not win]
+    - "\"%R%\" -e \"library('jsonlite')\""  # [win]
+
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
 
 about:
-  home: http://arxiv.org/abs/1403.2805, https://www.opencpu.org/posts/jsonlite-a-smarter-json-encoder
-  license: MIT
-  license_file: LICENSE
-  summary: "A fast JSON parser and generator optimized for statistical data and the web. Started
+  home: https://arxiv.org/abs/1403.2805, https://www.opencpu.org/posts/jsonlite-a-smarter-json-encoder
+  license: MIT + file LICENSE
+  summary: A fast JSON parser and generator optimized for statistical data and the web. Started
     out as a fork of 'RJSONIO', but has been completely rewritten in recent versions.
     The package offers flexible, robust, high performance tools for working with JSON
     in R and is particularly powerful for building pipelines and interacting with a
@@ -45,9 +64,35 @@ about:
     2014). In addition to converting JSON data from/to R objects, 'jsonlite' contains
     functions to stream, validate, and prettify JSON data. The unit tests included with
     the package verify that all edge cases are encoded and decoded consistently for
-    use with dynamic data in systems and applications."
+    use with dynamic data in systems and applications.
+  license_family: MIT
 
 extra:
   recipe-maintainers:
     - ocefpaf
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: jsonlite
+# Version: 1.5
+# Title: A Robust, High Performance JSON Parser and Generator for R
+# License: MIT + file LICENSE
+# NeedsCompilation: yes
+# Depends: methods
+# Author: Jeroen Ooms, Duncan Temple Lang, Lloyd Hilaiel
+# URL: https://arxiv.org/abs/1403.2805, https://www.opencpu.org/posts/jsonlite-a-smarter-json-encoder
+# BugReports: http://github.com/jeroen/jsonlite/issues
+# Maintainer: Jeroen Ooms <jeroen@berkeley.edu>
+# VignetteBuilder: knitr, R.rsp
+# Description: A fast JSON parser and generator optimized for statistical data and the web. Started out as a fork of 'RJSONIO', but has been completely rewritten in recent versions. The package offers flexible, robust, high performance tools for working with JSON in R and is particularly powerful for building pipelines and interacting with a web API. The implementation is based on the mapping described in the vignette (Ooms, 2014). In addition to converting JSON data from/to R objects, 'jsonlite' contains functions to stream, validate, and prettify JSON data. The unit tests included with the package verify that all edge cases are encoded and decoded consistently for use with dynamic data in systems and applications.
+# Suggests: httr, curl, plyr, testthat, knitr, rmarkdown, R.rsp, sp
+# RoxygenNote: 6.0.1.9000
+# Packaged: 2017-05-31 14:03:43 UTC; jeroen
+# Repository: CRAN
+# Date/Publication: 2017-06-01 12:03:37 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
diff --git a/r-kernlab-feedstock/recipe/bld.bat b/r-kernlab-feedstock/recipe/bld.bat
index e69de29..8b10c94 100644
--- a/r-kernlab-feedstock/recipe/bld.bat
+++ b/r-kernlab-feedstock/recipe/bld.bat
@@ -0,0 +1,8 @@
+"%R%" CMD INSTALL --build .
+if errorlevel 1 exit 1
+
+@rem Add more build steps here, if they are necessary.
+
+@rem See
+@rem http://docs.continuum.io/conda/build.html
+@rem for a list of environment variables that are set during the build process.
diff --git a/r-kernlab-feedstock/recipe/build.sh b/r-kernlab-feedstock/recipe/build.sh
index e69de29..0c37df6 100644
--- a/r-kernlab-feedstock/recipe/build.sh
+++ b/r-kernlab-feedstock/recipe/build.sh
@@ -0,0 +1,13 @@
+#!/bin/bash
+
+# R refuses to build packages that mark themselves as Priority: Recommended
+mv DESCRIPTION DESCRIPTION.old
+grep -v '^Priority: ' DESCRIPTION.old > DESCRIPTION
+
+$R CMD INSTALL --build .
+
+# Add more build steps here, if they are necessary.
+
+# See
+# http://docs.continuum.io/conda/build.html
+# for a list of environment variables that are set during the build process.
diff --git a/r-kernlab-feedstock/recipe/meta.yaml b/r-kernlab-feedstock/recipe/meta.yaml
index e69de29..64966e8 100644
--- a/r-kernlab-feedstock/recipe/meta.yaml
+++ b/r-kernlab-feedstock/recipe/meta.yaml
@@ -0,0 +1,91 @@
+{% set version = '0.9-25' %}
+
+{% set posix = 'm2-' if win else '' %}
+{% set native = 'm2w64-' if win else '' %}
+
+package:
+  name: r-kernlab
+  version: {{ version|replace("-", "_") }}
+
+source:
+  fn: kernlab_{{ version }}.tar.gz
+  url:
+    - https://cran.r-project.org/src/contrib/kernlab_{{ version }}.tar.gz
+    - https://cran.r-project.org/src/contrib/Archive/kernlab/kernlab_{{ version }}.tar.gz
+  sha256: b9de072754bb03c02c4d6a5ca20f2290fd090de328b55ab334ac0b397ac2ca62
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
+build:
+  # If this is a new build for the same version, increment the build number.
+  number: 0
+
+  # This is required to make R link correctly on Linux.
+  rpaths:
+    - lib/R/lib/
+    - lib/
+
+
+requirements:
+  build:
+    - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('cxx') }}      # [not win]
+    - {{ compiler('fortran') }}  # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
+
+  run:
+    - r-base
+    - {{native}}gcc-libs         # [win]
+
+test:
+  commands:
+    # You can put additional test commands to be run here.
+    - $R -e "library('kernlab')"           # [not win]
+    - "\"%R%\" -e \"library('kernlab')\""  # [win]
+
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
+about:
+  home: https://CRAN.R-project.org/package=kernlab
+  license: GPL-2
+  summary: Kernel-based machine learning methods for classification, regression, clustering,
+    novelty detection, quantile regression and dimensionality reduction.  Among other
+    methods 'kernlab' includes Support Vector Machines, Spectral Clustering, Kernel
+    PCA, Gaussian Processes and a QP solver.
+  license_family: GPL2
+
+extra:
+  recipe-maintainers:
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: kernlab
+# Version: 0.9-25
+# Title: Kernel-Based Machine Learning Lab
+# Authors@R: c(person("Alexandros", "Karatzoglou", role = c("aut", "cre"), email = "alexandros.karatzoglou@gmail.com"), person("Alex", "Smola", role = "aut"), person("Kurt", "Hornik", role = "aut"))
+# Description: Kernel-based machine learning methods for classification, regression, clustering, novelty detection, quantile regression and dimensionality reduction.  Among other methods 'kernlab' includes Support Vector Machines, Spectral Clustering, Kernel PCA, Gaussian Processes and a QP solver.
+# Depends: R (>= 2.10)
+# Imports: methods, stats, grDevices, graphics
+# LazyLoad: Yes
+# License: GPL-2
+# NeedsCompilation: yes
+# Packaged: 2016-10-03 06:52:15 UTC; hornik
+# Author: Alexandros Karatzoglou [aut, cre], Alex Smola [aut], Kurt Hornik [aut]
+# Maintainer: Alexandros Karatzoglou <alexandros.karatzoglou@gmail.com>
+# Repository: CRAN
+# Date/Publication: 2016-10-03 09:43:44
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-kernsmooth-feedstock contains modified content
diff --git a/r-kernsmooth-feedstock/recipe/meta.yaml b/r-kernsmooth-feedstock/recipe/meta.yaml
index dc8901c..292f681 100644
--- a/r-kernsmooth-feedstock/recipe/meta.yaml
+++ b/r-kernsmooth-feedstock/recipe/meta.yaml
@@ -14,29 +14,46 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/KernSmooth/KernSmooth_{{ version }}.tar.gz
   sha256: 8b72d23ed121a54af188b2cda4441e3ce2646359309885f6455b82c0275210f6
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: MASS
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('fortran') }}  # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('KernSmooth')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('KernSmooth')"           # [not win]
     - "\"%R%\" -e \"library('KernSmooth')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=KernSmooth
   license: Unlimited
@@ -49,3 +66,28 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: KernSmooth
+# Priority: recommended
+# Version: 2.23-15
+# Date: 2015-06-29
+# Title: Functions for Kernel Smoothing Supporting Wand & Jones (1995)
+# Authors@R: c(person("Matt", "Wand", role = "aut", email = "Matt.Wand@uts.edu.au"), person("Brian", "Ripley", role = c("trl", "cre", "ctb"), email = "ripley@stats.ox.ac.uk", comment = "R port and updates"))
+# Depends: R (>= 2.5.0), stats
+# Suggests: MASS
+# Description: Functions for kernel smoothing (and density estimation) corresponding to the book:  Wand, M.P. and Jones, M.C. (1995) "Kernel Smoothing".
+# License: Unlimited
+# ByteCompile: yes
+# NeedsCompilation: yes
+# Packaged: 2015-06-29 08:37:00 UTC; ripley
+# Author: Matt Wand [aut], Brian Ripley [trl, cre, ctb] (R port and updates)
+# Maintainer: Brian Ripley <ripley@stats.ox.ac.uk>
+# Repository: CRAN
+# Date/Publication: 2015-06-29 17:15:14
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-knitr-feedstock contains modified content
diff --git a/r-knitr-feedstock/recipe/meta.yaml b/r-knitr-feedstock/recipe/meta.yaml
index a78f012..929806d 100644
--- a/r-knitr-feedstock/recipe/meta.yaml
+++ b/r-knitr-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '1.16' %}
+{% set version = '1.17' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,16 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/knitr_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/knitr/knitr_{{ version }}.tar.gz
-  sha256: 10481fbf266b68402bf1973da99a87576ec2579f1c6cb2f1cca524b661a56a09
-  
+  sha256: 9484a2b2c7b0c2aae24ab7f4eec6db48affbceb0e42bd3d69e34d953fe92f401
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: formatR, testit, rgl (>= 0.95.1201), codetools, rmarkdown, htmlwidgets (>= 0.7), webshot, tikzDevice (>= 0.10), png, jpeg, XML, RCurl, DBI (>= 0.4-1), tibble
 requirements:
   build:
     - r-base
@@ -43,11 +50,18 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('knitr')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('knitr')"           # [not win]
     - "\"%R%\" -e \"library('knitr')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
-  home: http://yihui.name/knitr/
+  home: https://yihui.name/knitr/
   license: GPL
   summary: Provides a general-purpose tool for dynamic report generation in R using Literate
     Programming techniques.
@@ -57,3 +71,34 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: knitr
+# Type: Package
+# Title: A General-Purpose Package for Dynamic Report Generation in R
+# Version: 1.17
+# Date: 2017-08-09
+# Authors@R: c( person("Yihui", "Xie", email = "xie@yihui.name", role = c("aut", "cre")), person("Adam", "Vogt", role = "ctb"), person("Alastair", "Andrew", role = "ctb"), person("Alex", "Zvoleff", role = "ctb"), person("Andre", "Simon", role = "ctb", comment = "the CSS files under inst/themes/ were derived from the Highlight package http://www.andre-simon.de"), person("Aron", "Atkins", role = "ctb"), person("Aaron", "Wolen", role = "ctb"), person("Ashley", "Manton", role = "ctb"), person("Ben", "Baumer", role = "ctb"), person("Brian", "Diggs", role = "ctb"), person("Cassio", "Pereira", role = "ctb"), person("David", "Robinson", role = "ctb"), person("Donald", "Arseneau", role = c("ctb", "cph"), comment = "the framed package at inst/misc/framed.sty"), person("Doug", "Hemken", role = "ctb"), person("Duncan", "Murdoch", role = "ctb"), person("Fabian", "Hirschmann", role = "ctb"), person("Fitch", "Simeon", role = "ctb"), person("Forest", "Fang", role = "ctb"), person(c("Frank", "E", "Harrell", "Jr"), role = "ctb", comment = "the Sweavel package at inst/misc/Sweavel.sty"), person("Garrick", "Aden-Buie", role = "ctb"), person("Gregoire", "Detrez", role = "ctb"), person("Hadley", "Wickham", role = "ctb"), person("Heewon", "Jeon", role = "ctb"), person("Henrik", "Bengtsson", role = "ctb"), person("Hiroaki", "Yutani", role = "ctb"), person("Ian", "Lyttle", role = "ctb"), person("Hodges", "Daniel", role = "ctb"), person("Jake", "Burkhead", role = "ctb"), person("James", "Manton", role = "ctb"), person("Jared", "Lander", role = "ctb"), person("Jason", "Punyon", role = "ctb"), person("Javier", "Luraschi", role = "ctb"), person("Jeff", "Arnold", role = "ctb"), person("Jeremy", "Ashkenas", role = c("ctb", "cph"), comment = "the CSS file at inst/misc/docco-classic.css"), person("Jeremy", "Stephens", role = "ctb"), person("Jim", "Hester", role = "ctb"), person("Joe", "Cheng", role = "ctb"), person("Johannes", "Ranke", role = "ctb"), person("John", "Honaker", role = "ctb"), person("John", "Muschelli", role = "ctb"), person("Jonathan", "Keane", role = "ctb"), person("JJ", "Allaire", role = "ctb"), person("Johan", "Toloe", role = "ctb"), person("Joseph", "Larmarange", role = "ctb"), person("Julien", "Barnier", role = "ctb"), person("Kaiyin", "Zhong", role = "ctb"), person("Kamil", "Slowikowski", role = "ctb"), person("Karl", "Forner", role = "ctb"), person(c("Kevin", "K."), "Smith", role = "ctb"), person("Kirill", "Mueller", role = "ctb"), person("Kohske", "Takahashi", role = "ctb"), person("Michael", "Friendly", role = "ctb"), person("Michal", "Bojanowski", role = "ctb"), person("Michel", "Kuhlmann", role = "ctb"), person("Nacho", "Caballero", role = "ctb"), person("Nick", "Salkowski", role = "ctb"), person("Noam", "Ross", role = "ctb"), person("Qiang", "Li", role = "ctb"), person("Ramnath", "Vaidyanathan", role = "ctb"), person("Richard", "Cotton", role = "ctb"), person("Robert", "Krzyzanowski", role = "ctb"), person("Romain", "Francois", role = "ctb"), person("Scott", "Kostyshak", role = "ctb"), person("Sebastian", "Meyer", role = "ctb"), person("Sietse", "Brouwer", role = "ctb"), person(c("Simon", "de"), "Bernard", role = "ctb"), person("Sylvain", "Rousseau", role = "ctb"), person("Taiyun", "Wei", role = "ctb"), person("Thibaut", "Assus", role = "ctb"), person("Thibaut", "Lamadon", role = "ctb"), person("Thomas", "Leeper", role = "ctb"), person("Tom", "Torsney-Weir", role = "ctb"), person("Trevor", "Davis", role = "ctb"), person("Viktoras", "Veitas", role = "ctb"), person("Weicheng", "Zhu", role = "ctb"), person("Wush", "Wu", role = "ctb"), person("Zachary", "Foster", role = "ctb") )
+# Maintainer: Yihui Xie <xie@yihui.name>
+# Description: Provides a general-purpose tool for dynamic report generation in R using Literate Programming techniques.
+# Depends: R (>= 3.1.0)
+# Imports: evaluate (>= 0.10), digest, highr, markdown, stringr (>= 0.6), yaml, methods, tools
+# Suggests: formatR, testit, rgl (>= 0.95.1201), codetools, rmarkdown, htmlwidgets (>= 0.7), webshot, tikzDevice (>= 0.10), png, jpeg, XML, RCurl, DBI (>= 0.4-1), tibble
+# License: GPL
+# URL: https://yihui.name/knitr/
+# BugReports: https://github.com/yihui/knitr/issues
+# VignetteBuilder: knitr
+# SystemRequirements: Package vignettes based on R Markdown v2 or reStructuredText require Pandoc (http://pandoc.org). The function rst2pdf() require rst2pdf (https://github.com/rst2pdf/rst2pdf).
+# Collate: 'block.R' 'cache.R' 'utils.R' 'citation.R' 'hooks-html.R' 'plot.R' 'defaults.R' 'concordance.R' 'engine.R' 'highlight.R' 'themes.R' 'header.R' 'hooks-asciidoc.R' 'hooks-chunk.R' 'hooks-extra.R' 'hooks-latex.R' 'hooks-md.R' 'hooks-rst.R' 'hooks-textile.R' 'hooks.R' 'output.R' 'package.R' 'pandoc.R' 'params.R' 'parser.R' 'pattern.R' 'rocco.R' 'spin.R' 'table.R' 'template.R' 'utils-base64.R' 'utils-conversion.R' 'utils-rd2html.R' 'utils-sweave.R' 'utils-upload.R' 'utils-vignettes.R' 'zzz.R'
+# RoxygenNote: 6.0.1
+# NeedsCompilation: no
+# Packaged: 2017-08-09 06:32:13 UTC; yihui
+# Author: Yihui Xie [aut, cre], Adam Vogt [ctb], Alastair Andrew [ctb], Alex Zvoleff [ctb], Andre Simon [ctb] (the CSS files under inst/themes/ were derived from the Highlight package http://www.andre-simon.de), Aron Atkins [ctb], Aaron Wolen [ctb], Ashley Manton [ctb], Ben Baumer [ctb], Brian Diggs [ctb], Cassio Pereira [ctb], David Robinson [ctb], Donald Arseneau [ctb, cph] (the framed package at inst/misc/framed.sty), Doug Hemken [ctb], Duncan Murdoch [ctb], Fabian Hirschmann [ctb], Fitch Simeon [ctb], Forest Fang [ctb], Frank E Harrell Jr [ctb] (the Sweavel package at inst/misc/Sweavel.sty), Garrick Aden-Buie [ctb], Gregoire Detrez [ctb], Hadley Wickham [ctb], Heewon Jeon [ctb], Henrik Bengtsson [ctb], Hiroaki Yutani [ctb], Ian Lyttle [ctb], Hodges Daniel [ctb], Jake Burkhead [ctb], James Manton [ctb], Jared Lander [ctb], Jason Punyon [ctb], Javier Luraschi [ctb], Jeff Arnold [ctb], Jeremy Ashkenas [ctb, cph] (the CSS file at inst/misc/docco-classic.css), Jeremy Stephens [ctb], Jim Hester [ctb], Joe Cheng [ctb], Johannes Ranke [ctb], John Honaker [ctb], John Muschelli [ctb], Jonathan Keane [ctb], JJ Allaire [ctb], Johan Toloe [ctb], Joseph Larmarange [ctb], Julien Barnier [ctb], Kaiyin Zhong [ctb], Kamil Slowikowski [ctb], Karl Forner [ctb], Kevin K. Smith [ctb], Kirill Mueller [ctb], Kohske Takahashi [ctb], Michael Friendly [ctb], Michal Bojanowski [ctb], Michel Kuhlmann [ctb], Nacho Caballero [ctb], Nick Salkowski [ctb], Noam Ross [ctb], Qiang Li [ctb], Ramnath Vaidyanathan [ctb], Richard Cotton [ctb], Robert Krzyzanowski [ctb], Romain Francois [ctb], Scott Kostyshak [ctb], Sebastian Meyer [ctb], Sietse Brouwer [ctb], Simon de Bernard [ctb], Sylvain Rousseau [ctb], Taiyun Wei [ctb], Thibaut Assus [ctb], Thibaut Lamadon [ctb], Thomas Leeper [ctb], Tom Torsney-Weir [ctb], Trevor Davis [ctb], Viktoras Veitas [ctb], Weicheng Zhu [ctb], Wush Wu [ctb], Zachary Foster [ctb]
+# Repository: CRAN
+# Date/Publication: 2017-08-10 04:27:37 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-kohonen-feedstock contains modified content
diff --git a/r-kohonen-feedstock/recipe/meta.yaml b/r-kohonen-feedstock/recipe/meta.yaml
index fdd0f1a..28faa40 100644
--- a/r-kohonen-feedstock/recipe/meta.yaml
+++ b/r-kohonen-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '3.0.2' %}
+{% set version = '3.0.4' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,36 +12,52 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/kohonen_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/kohonen/kohonen_{{ version }}.tar.gz
-  sha256: e8689d5a9c90c2e9047d273f47524efec89678e2a8d53b1d5b08a14b5b95e2b5
+  sha256: 429864d72858c29d4187bed48ac1c17cf2ecb08f18090b6bf44dff55174609ab
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('cxx') }}      # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
     - r-mass
     - r-rcpp >=0.12.7
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-mass
     - r-rcpp >=0.12.7
-    - libgcc  # [not win]
 
 test:
   commands:
-    - $R -e "library('kohonen')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('kohonen')"           # [not win]
     - "\"%R%\" -e \"library('kohonen')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=kohonen
   license: GPL (>= 2)
@@ -54,3 +70,25 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: kohonen
+# Version: 3.0.4
+# Title: Supervised and Unsupervised Self-Organising Maps
+# Author: Ron Wehrens and Johannes Kruisselbrink
+# Maintainer: Ron Wehrens <ron.wehrens@gmail.com>
+# Description: Functions to train self-organising maps (SOMs). Also interrogation of the maps and prediction using trained maps are supported. The name of the package refers to Teuvo Kohonen, the inventor of the SOM.
+# License: GPL (>= 2)
+# Depends: R (>= 2.10)
+# Imports: MASS, Rcpp (>= 0.12.7)
+# LinkingTo: Rcpp
+# NeedsCompilation: yes
+# Packaged: 2017-08-28 15:23:15 UTC; ron
+# Repository: CRAN
+# Date/Publication: 2017-08-28 18:51:19 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-labeling-feedstock contains modified content
diff --git a/r-labeling-feedstock/recipe/meta.yaml b/r-labeling-feedstock/recipe/meta.yaml
index 3c8e2be..b5dbf31 100644
--- a/r-labeling-feedstock/recipe/meta.yaml
+++ b/r-labeling-feedstock/recipe/meta.yaml
@@ -14,14 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/labeling/labeling_{{ version }}.tar.gz
   sha256: 0d8069eb48e91f6f6d6a9148f4e2dc5026cabead15dd15fc343eff9cf33f538f
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
@@ -31,13 +38,19 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('labeling')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('labeling')"           # [not win]
     - "\"%R%\" -e \"library('labeling')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=labeling
-  license: MIT
-  license_file: LICENSE
+  license: MIT + file LICENSE | Unlimited
   summary: Provides a range of axis labeling algorithms
   license_family: MIT
 
@@ -45,3 +58,29 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: labeling
+# Type: Package
+# Title: Axis Labeling
+# Version: 0.3
+# Date: 2014-08-22
+# Author: Justin Talbot
+# Maintainer: Justin Talbot <justintalbot@gmail.com>
+# Description: Provides a range of axis labeling algorithms
+# License: MIT + file LICENSE | Unlimited
+# LazyLoad: no
+# Collate: 'labeling.R'
+# Repository: CRAN
+# Repository/R-Forge/Project: labeling
+# Repository/R-Forge/Revision: 16
+# Repository/R-Forge/DateTimeStamp: 2014-08-23 05:53:23
+# Date/Publication: 2014-08-23 14:57:53
+# Packaged: 2014-08-23 06:15:07 UTC; rforge
+# NeedsCompilation: no
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-lahman-feedstock contains modified content
diff --git a/r-lahman-feedstock/recipe/meta.yaml b/r-lahman-feedstock/recipe/meta.yaml
index 53afc3a..85fa528 100644
--- a/r-lahman-feedstock/recipe/meta.yaml
+++ b/r-lahman-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '5.0-0' %}
+{% set version = '6.0-0' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,16 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/Lahman_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/Lahman/Lahman_{{ version }}.tar.gz
-  sha256: 9d0ef0a8309e3fdf7598477fdf4d834d946ff1b5013a9ca9d4969ae19e7eee0f
+  sha256: 6662a3674adc274aed42d6769fea3c6b0cc95561d1d6bd59cfdc7c24948a2698
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: lattice, ggplot2, googleVis, data.table, vcd, reshape2, tidyr, zipcode
 requirements:
   build:
     - r-base
@@ -33,9 +40,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('Lahman')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('Lahman')"           # [not win]
     - "\"%R%\" -e \"library('Lahman')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://lahman.r-forge.r-project.org/
   license: GPL
@@ -48,3 +62,35 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: Lahman
+# Type: Package
+# Title: Sean 'Lahman' Baseball Database
+# Version: 6.0-0
+# Date: 2017-08-07
+# Authors@R: c(person(given = "Michael", family = "Friendly", role="aut", email="friendly@yorku.ca"),  person(given= "Dennis", family = "Murphy", role="ctb"),  person(given ="Martin", family = "Monkman", role="ctb"), person(given ="Chris", family = "Dalzell", role=c("cre","ctb"), email="cdalzell@gmail.com"))
+# Author: Michael Friendly [aut], Dennis Murphy [ctb], Martin Monkman [ctb], Chris Dalzell [cre, ctb]
+# Maintainer: Chris Dalzell <cdalzell@gmail.com>
+# Description: Provides the tables from the 'Sean Lahman Baseball Database' as a set of R data.frames. It uses the data on pitching, hitting and fielding performance and other tables from 1871 through 2015, as recorded in the 2016 version of the database.
+# Depends: R (>= 2.10)
+# Suggests: lattice, ggplot2, googleVis, data.table, vcd, reshape2, tidyr, zipcode
+# Imports: dplyr
+# License: GPL
+# URL: http://lahman.r-forge.r-project.org/
+# LazyLoad: yes
+# LazyData: yes
+# Repository: CRAN
+# Repository/R-Forge/Project: lahman
+# Repository/R-Forge/Revision: 39
+# Repository/R-Forge/DateTimeStamp: 2013-06-01 03:33:30
+# Date/Publication: 2017-08-15 05:14:24 UTC
+# Packaged: 2017-08-15 01:17:49 UTC; dz
+# NeedsCompilation: no
+# RoxygenNote: 6.0.1
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-lars-feedstock contains modified content
diff --git a/r-lars-feedstock/recipe/meta.yaml b/r-lars-feedstock/recipe/meta.yaml
index d7cde09..ef5f901 100644
--- a/r-lars-feedstock/recipe/meta.yaml
+++ b/r-lars-feedstock/recipe/meta.yaml
@@ -14,29 +14,43 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/lars/lars_{{ version }}.tar.gz
   sha256: 64745b568f20b2cfdae3dad02fba92ebf78ffee466a71aaaafd4f48c3921922e
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{ compiler('fortran') }}  # [not win]
+    - {{native}}toolchain        # [win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('lars')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('lars')"           # [not win]
     - "\"%R%\" -e \"library('lars')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://www-stat.stanford.edu/~hastie/Papers/#LARS
   license: GPL-2
@@ -49,3 +63,26 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: lars
+# Type: Package
+# Version: 1.2
+# Date: 2013-04-23
+# Title: Least Angle Regression, Lasso and Forward Stagewise
+# Author: Trevor Hastie <hastie@stanford.edu> and Brad Efron <brad@stat.stanford.edu>
+# Maintainer: Trevor Hastie <hastie@stanford.edu>
+# Description: Efficient procedures for fitting an entire lasso sequence with the cost of a single least squares fit. Least angle regression and infinitesimal forward stagewise regression are related to the lasso, as described in the paper below.
+# Depends: R (>= 2.10)
+# License: GPL-2
+# URL: http://www-stat.stanford.edu/~hastie/Papers/#LARS
+# Packaged: 2013-04-24 05:12:53 UTC; hastie
+# NeedsCompilation: yes
+# Repository: CRAN
+# Date/Publication: 2013-04-24 09:46:12
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-lattice-feedstock contains modified content
diff --git a/r-lattice-feedstock/recipe/bld.bat b/r-lattice-feedstock/recipe/bld.bat
new file mode 100644
index 0000000..8b10c94
--- /dev/null
+++ b/r-lattice-feedstock/recipe/bld.bat
@@ -0,0 +1,8 @@
+"%R%" CMD INSTALL --build .
+if errorlevel 1 exit 1
+
+@rem Add more build steps here, if they are necessary.
+
+@rem See
+@rem http://docs.continuum.io/conda/build.html
+@rem for a list of environment variables that are set during the build process.
diff --git a/r-lattice-feedstock/recipe/build.sh b/r-lattice-feedstock/recipe/build.sh
new file mode 100644
index 0000000..0c37df6
--- /dev/null
+++ b/r-lattice-feedstock/recipe/build.sh
@@ -0,0 +1,13 @@
+#!/bin/bash
+
+# R refuses to build packages that mark themselves as Priority: Recommended
+mv DESCRIPTION DESCRIPTION.old
+grep -v '^Priority: ' DESCRIPTION.old > DESCRIPTION
+
+$R CMD INSTALL --build .
+
+# Add more build steps here, if they are necessary.
+
+# See
+# http://docs.continuum.io/conda/build.html
+# for a list of environment variables that are set during the build process.
diff --git a/r-lattice-feedstock/recipe/meta.yaml b/r-lattice-feedstock/recipe/meta.yaml
index 6b99a90..8e5c205 100644
--- a/r-lattice-feedstock/recipe/meta.yaml
+++ b/r-lattice-feedstock/recipe/meta.yaml
@@ -1,46 +1,94 @@
-{% set version = "0.20-34" %}
+{% set version = '0.20-35' %}
+
+{% set posix = 'm2-' if win else '' %}
+{% set native = 'm2w64-' if win else '' %}
 
 package:
   name: r-lattice
-  version: {{ version.replace("-", "_") }}
+  version: {{ version|replace("-", "_") }}
 
 source:
   fn: lattice_{{ version }}.tar.gz
   url:
-    - http://cran.r-project.org/src/contrib/lattice_{{ version }}.tar.gz
-    - http://cran.r-project.org/src/contrib/Archive/lattice/lattice_{{ version }}.tar.gz
-  sha256: 4a1a1cafa9c6660fb9a433b3a51898b8ec8e83abf143c80f99e3e4cf92812518
+    - https://cran.r-project.org/src/contrib/lattice_{{ version }}.tar.gz
+    - https://cran.r-project.org/src/contrib/Archive/lattice/lattice_{{ version }}.tar.gz
+  sha256: 0829ab0f4dec55aac6a73bc3411af68441ddb1b5b078d680a7c2643abeaa965d
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: True  # [win32]
-  script: R CMD INSTALL --build .
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
-requirements:  # [not win32]
-  build:  # [not win32]
-    - r-base  # [not win32]
-    - posix  # [win64]
-    - m2w64-toolchain  # [win64]
-    - gcc  # [not win]
-  run:  # [not win32]
-    - r-base  # [not win32]
+# Suggests: KernSmooth, MASS, latticeExtra
+requirements:
+  build:
+    - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
+
+  run:
+    - r-base
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - R -e "library('lattice')"  # [not win]
-    - R -e \"library('lattice')\"  # [win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('lattice')"           # [not win]
+    - "\"%R%\" -e \"library('lattice')\""  # [win]
+
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
 
 about:
   home: http://lattice.r-forge.r-project.org/
   license: GPL (>= 2)
-  summary: 'A powerful and elegant high-level data visualization system inspired by Trellis graphics,
+  summary: A powerful and elegant high-level data visualization system inspired by Trellis graphics,
     with an emphasis on multivariate data. Lattice is sufficient for typical graphics
     needs, and is also flexible enough to handle most nonstandard requirements. See
-    ?Lattice for an introduction.'
+    ?Lattice for an introduction.
+  license_family: GPL3
 
 extra:
   recipe-maintainers:
     - ocefpaf
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: lattice
+# Version: 0.20-35
+# Date: 2017-03-23
+# Priority: recommended
+# Title: Trellis Graphics for R
+# Author: Deepayan Sarkar <deepayan.sarkar@r-project.org>
+# Maintainer: Deepayan Sarkar <deepayan.sarkar@r-project.org>
+# Description: A powerful and elegant high-level data visualization system inspired by Trellis graphics, with an emphasis on multivariate data. Lattice is sufficient for typical graphics needs, and is also flexible enough to handle most nonstandard requirements. See ?Lattice for an introduction.
+# Depends: R (>= 3.0.0)
+# Suggests: KernSmooth, MASS, latticeExtra
+# Imports: grid, grDevices, graphics, stats, utils
+# Enhances: chron
+# LazyLoad: yes
+# LazyData: yes
+# License: GPL (>= 2)
+# URL: http://lattice.r-forge.r-project.org/
+# BugReports: https://github.com/deepayan/lattice/issues
+# NeedsCompilation: yes
+# Packaged: 2017-03-25 11:25:40 UTC; deepayan
+# Repository: CRAN
+# Date/Publication: 2017-03-25 18:05:47 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-latticeextra-feedstock contains modified content
diff --git a/r-latticeextra-feedstock/recipe/meta.yaml b/r-latticeextra-feedstock/recipe/meta.yaml
index d56e2f4..b503f8e 100644
--- a/r-latticeextra-feedstock/recipe/meta.yaml
+++ b/r-latticeextra-feedstock/recipe/meta.yaml
@@ -14,14 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/latticeExtra/latticeExtra_{{ version }}.tar.gz
   sha256: 780695323dfadac108fb27000011c734e2927b1e0f069f247d65d27994c67ec2
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: maps, mapproj, deldir, tripack, zoo, MASS, quantreg, mgcv
 requirements:
   build:
     - r-base
@@ -35,9 +42,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('latticeExtra')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('latticeExtra')"           # [not win]
     - "\"%R%\" -e \"library('latticeExtra')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://latticeextra.r-forge.r-project.org/
   license: GPL (>= 2)
@@ -50,3 +64,29 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: latticeExtra
+# Version: 0.6-28
+# Date: 2016-01-09
+# Title: Extra Graphical Utilities Based on Lattice
+# Author: Deepayan Sarkar <deepayan.sarkar@r-project.org>, Felix Andrews <felix@nfrac.org>
+# Maintainer: Deepayan Sarkar <deepayan.sarkar@r-project.org>
+# Description: Building on the infrastructure provided by the lattice package, this package provides several new high-level functions and methods, as well as additional utilities such as panel and axis annotation functions.
+# Depends: R (>= 2.10.0), lattice, RColorBrewer
+# Imports: grid, stats, utils, grDevices
+# Suggests: maps, mapproj, deldir, tripack, zoo, MASS, quantreg, mgcv
+# URL: http://latticeextra.r-forge.r-project.org/
+# LazyLoad: yes
+# LazyData: yes
+# License: GPL (>= 2)
+# NeedsCompilation: no
+# Packaged: 2016-02-09 11:06:00 UTC; deepayan
+# Repository: CRAN
+# Date/Publication: 2016-02-09 14:36:31
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
diff --git a/r-lava-feedstock/recipe/bld.bat b/r-lava-feedstock/recipe/bld.bat
index e69de29..8b10c94 100644
--- a/r-lava-feedstock/recipe/bld.bat
+++ b/r-lava-feedstock/recipe/bld.bat
@@ -0,0 +1,8 @@
+"%R%" CMD INSTALL --build .
+if errorlevel 1 exit 1
+
+@rem Add more build steps here, if they are necessary.
+
+@rem See
+@rem http://docs.continuum.io/conda/build.html
+@rem for a list of environment variables that are set during the build process.
diff --git a/r-lava-feedstock/recipe/build.sh b/r-lava-feedstock/recipe/build.sh
index e69de29..0c37df6 100644
--- a/r-lava-feedstock/recipe/build.sh
+++ b/r-lava-feedstock/recipe/build.sh
@@ -0,0 +1,13 @@
+#!/bin/bash
+
+# R refuses to build packages that mark themselves as Priority: Recommended
+mv DESCRIPTION DESCRIPTION.old
+grep -v '^Priority: ' DESCRIPTION.old > DESCRIPTION
+
+$R CMD INSTALL --build .
+
+# Add more build steps here, if they are necessary.
+
+# See
+# http://docs.continuum.io/conda/build.html
+# for a list of environment variables that are set during the build process.
diff --git a/r-lava-feedstock/recipe/meta.yaml b/r-lava-feedstock/recipe/meta.yaml
index e69de29..97fb9dd 100644
--- a/r-lava-feedstock/recipe/meta.yaml
+++ b/r-lava-feedstock/recipe/meta.yaml
@@ -0,0 +1,97 @@
+{% set version = '1.5.1' %}
+
+{% set posix = 'm2-' if win else '' %}
+{% set native = 'm2w64-' if win else '' %}
+
+package:
+  name: r-lava
+  version: {{ version|replace("-", "_") }}
+
+source:
+  fn: lava_{{ version }}.tar.gz
+  url:
+    - https://cran.r-project.org/src/contrib/lava_{{ version }}.tar.gz
+    - https://cran.r-project.org/src/contrib/Archive/lava/lava_{{ version }}.tar.gz
+  sha256: c41da42c9d90d81de876c29bfca736fa111a8924add07c85ab2f8d98200195ed
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
+build:
+  # If this is a new build for the same version, increment the build number.
+  number: 0
+
+  # This is required to make R link correctly on Linux.
+  rpaths:
+    - lib/R/lib/
+    - lib/
+
+# Suggests: KernSmooth, Matrix, Rgraphviz, ascii, data.table, fields, foreach, geepack, gof (>= 0.9), graph, igraph (>= 0.6), lava.tobit, lme4, mets (>= 1.1), optimx, quantreg, rgl, testthat (>= 0.11), visNetwork, zoo
+requirements:
+  build:
+    - r-base
+    - r-numderiv
+    - r-survival
+
+  run:
+    - r-base
+    - r-numderiv
+    - r-survival
+
+test:
+  commands:
+    # You can put additional test commands to be run here.
+    - $R -e "library('lava')"           # [not win]
+    - "\"%R%\" -e \"library('lava')\""  # [win]
+
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
+about:
+  home: https://github.com/kkholst/lava
+  license: GPL-3
+  summary: A general implementation of Structural Equation Models with latent variables (MLE,
+    2SLS, and composite likelihood estimators) with both continuous, censored, and ordinal
+    outcomes (Holst and Budtz-Joergensen (2013) <doi:10.1007/s00180-012-0344-y>). The
+    package also provides methods for graph exploration (d-separation, back-door criterion),
+    simulation of general non-linear latent variable models, and estimation of influence
+    functions for a broad range of statistical  models.
+  license_family: GPL3
+
+extra:
+  recipe-maintainers:
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: lava
+# Type: Package
+# Title: Latent Variable Models
+# Version: 1.5.1
+# Date: 2017-09-25
+# Authors@R: c(person("Klaus K.", "Holst", email="klaus@holst.it", role=c("aut", "cre")), person("Brice", "Ozenne", role = "ctb"), person("Thomas", "Gerds", role = "ctb"))
+# Author: Klaus K. Holst [aut, cre], Brice Ozenne [ctb], Thomas Gerds [ctb]
+# Maintainer: Klaus K. Holst <klaus@holst.it>
+# Description: A general implementation of Structural Equation Models with latent variables (MLE, 2SLS, and composite likelihood estimators) with both continuous, censored, and ordinal outcomes (Holst and Budtz-Joergensen (2013) <doi:10.1007/s00180-012-0344-y>). The package also provides methods for graph exploration (d-separation, back-door criterion), simulation of general non-linear latent variable models, and estimation of influence functions for a broad range of statistical  models.
+# URL: https://github.com/kkholst/lava
+# BugReports: https://github.com/kkholst/lava/issues
+# License: GPL-3
+# LazyLoad: yes
+# Depends: R (>= 3.0)
+# Imports: grDevices, graphics, methods, numDeriv, stats, survival, utils
+# Suggests: KernSmooth, Matrix, Rgraphviz, ascii, data.table, fields, foreach, geepack, gof (>= 0.9), graph, igraph (>= 0.6), lava.tobit, lme4, mets (>= 1.1), optimx, quantreg, rgl, testthat (>= 0.11), visNetwork, zoo
+# ByteCompile: yes
+# RoxygenNote: 6.0.1
+# NeedsCompilation: no
+# Packaged: 2017-09-27 20:46:26 UTC; klaus
+# Repository: CRAN
+# Date/Publication: 2017-09-27 21:25:09 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-lazyeval-feedstock contains modified content
diff --git a/r-lazyeval-feedstock/recipe/meta.yaml b/r-lazyeval-feedstock/recipe/meta.yaml
index eec33c3..3966470 100644
--- a/r-lazyeval-feedstock/recipe/meta.yaml
+++ b/r-lazyeval-feedstock/recipe/meta.yaml
@@ -14,29 +14,43 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/lazyeval/lazyeval_{{ version }}.tar.gz
   sha256: 13738f55b2044184fe91f53d17516a445dfb508227527921218cda6f01f98dcb
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: knitr, rmarkdown (>= 0.2.65), testthat, covr
 requirements:
   build:
-    - {{ compiler('c') }}  # [not win]
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
 
   run:
     - r-base
-    - {{native}}gcc-libs   # [win]
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('lazyeval')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('lazyeval')"           # [not win]
     - "\"%R%\" -e \"library('lazyeval')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=lazyeval
   license: GPL-3
@@ -49,3 +63,28 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: lazyeval
+# Version: 0.2.0
+# Title: Lazy (Non-Standard) Evaluation
+# Description: An alternative approach to non-standard evaluation using formulas. Provides a full implementation of LISP style 'quasiquotation', making it easier to generate code with other code.
+# Authors@R: c( person("Hadley", "Wickham", ,"hadley@rstudio.com", c("aut", "cre")), person("RStudio", role = "cph") )
+# License: GPL-3
+# LazyData: true
+# Depends: R (>= 3.1.0)
+# Suggests: knitr, rmarkdown (>= 0.2.65), testthat, covr
+# VignetteBuilder: knitr
+# RoxygenNote: 5.0.1
+# NeedsCompilation: yes
+# Packaged: 2016-06-10 21:38:52 UTC; hadley
+# Author: Hadley Wickham [aut, cre], RStudio [cph]
+# Maintainer: Hadley Wickham <hadley@rstudio.com>
+# Repository: CRAN
+# Date/Publication: 2016-06-12 19:03:08
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-leaflet-feedstock contains modified content
diff --git a/r-leaflet-feedstock/recipe/meta.yaml b/r-leaflet-feedstock/recipe/meta.yaml
index f6cd2aa..a570ccb 100644
--- a/r-leaflet-feedstock/recipe/meta.yaml
+++ b/r-leaflet-feedstock/recipe/meta.yaml
@@ -14,14 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/leaflet/leaflet_{{ version }}.tar.gz
   sha256: f474be80f40f62175c5a08aab7e549251ced9faa70d340374d522fdaf08cca15
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: knitr, maps, sf, shiny, testit (>= 0.4), rgdal, rgeos, R6, RJSONIO, purrr, testthat
 requirements:
   build:
     - r-base
@@ -55,9 +62,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('leaflet')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('leaflet')"           # [not win]
     - "\"%R%\" -e \"library('leaflet')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://rstudio.github.io/leaflet/
   license: GPL-3
@@ -70,3 +84,32 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: leaflet
+# Type: Package
+# Title: Create Interactive Web Maps with the JavaScript 'Leaflet' Library
+# Version: 1.1.0
+# Date: 2017-02-17
+# Authors@R: c( person("Joe", "Cheng", email = "joe@rstudio.com", role = c("aut", "cre")), person("Bhaskar", "Karambelkar", role = c("aut")), person("Yihui", "Xie", role = c("aut")), person("Hadley", "Wickham", role = c("ctb")), person("Kenton", "Russell", role = c("ctb")), person("Kent", "Johnson", role=c("ctb")), person("jQuery Foundation and contributors", role = c("ctb", "cph"), comment = "jQuery library"), person("Vladimir", "Agafonkin", role = c("ctb", "cph"), comment = "Leaflet library"), person("CloudMade", role = c("cph"), comment = "Leaflet library"), person("Leaflet contributors", role = c("ctb"), comment = "Leaflet library"), person("Leaflet Providers contributors", role = c("ctb", "cph"), comment = "Leaflet Providers plugin"), person("Brandon Copeland", role = c("ctb", "cph"), comment = "leaflet-measure plugin"), person("Jacob Toye", role = c("ctb", "cph"), comment = "Leaflet.label plugin"), person("Joerg Dietrich", role = c("ctb", "cph"), comment = "Leaflet.Terminator plugin"), person("Benjamin Becquet", role = c("ctb", "cph"), comment = "Leaflet.MagnifyingGlass plugin"), person("Norkart AS", role = c("ctb", "cph"), comment = "Leaflet.MiniMap plugin"), person("L. Voogdt", role = c("ctb", "cph"), comment = "Leaflet.awesome-markers plugin"), person("Daniel Montague", role = c("ctb", "cph"), comment = "Leaflet.EasyButton plugin"), person("Kartena AB", role = c("ctb", "cph"), comment = "Proj4Leaflet plugin"), person("Robert Kajic", role = c("ctb", "cph"), comment = "leaflet-locationfilter plugin"), person("Mapbox", role = c("ctb", "cph"), comment = "leaflet-omnivore plugin"), person("Michael Bostock", role = c("ctb", "cph"), comment = "topojson"), person("RStudio", role = c("cph")) )
+# Maintainer: Joe Cheng <joe@rstudio.com>
+# Description: Create and customize interactive maps using the 'Leaflet' JavaScript library and the 'htmlwidgets' package. These maps can be used directly from the R console, from 'RStudio', in Shiny apps and R Markdown documents.
+# License: GPL-3
+# URL: http://rstudio.github.io/leaflet/
+# BugReports: https://github.com/rstudio/leaflet/issues
+# Depends: R (>= 3.1.0)
+# Imports: base64enc, crosstalk, htmlwidgets, htmltools, magrittr, markdown, methods, png, RColorBrewer, raster, scales (>= 0.2.5), sp, stats, viridis
+# Suggests: knitr, maps, sf, shiny, testit (>= 0.4), rgdal, rgeos, R6, RJSONIO, purrr, testthat
+# RoxygenNote: 5.0.1
+# LazyData: true
+# NeedsCompilation: no
+# Packaged: 2017-02-21 16:56:25 UTC; jcheng
+# Author: Joe Cheng [aut, cre], Bhaskar Karambelkar [aut], Yihui Xie [aut], Hadley Wickham [ctb], Kenton Russell [ctb], Kent Johnson [ctb], jQuery Foundation and contributors [ctb, cph] (jQuery library), Vladimir Agafonkin [ctb, cph] (Leaflet library), CloudMade [cph] (Leaflet library), Leaflet contributors [ctb] (Leaflet library), Leaflet Providers contributors [ctb, cph] (Leaflet Providers plugin), Brandon Copeland [ctb, cph] (leaflet-measure plugin), Jacob Toye [ctb, cph] (Leaflet.label plugin), Joerg Dietrich [ctb, cph] (Leaflet.Terminator plugin), Benjamin Becquet [ctb, cph] (Leaflet.MagnifyingGlass plugin), Norkart AS [ctb, cph] (Leaflet.MiniMap plugin), L. Voogdt [ctb, cph] (Leaflet.awesome-markers plugin), Daniel Montague [ctb, cph] (Leaflet.EasyButton plugin), Kartena AB [ctb, cph] (Proj4Leaflet plugin), Robert Kajic [ctb, cph] (leaflet-locationfilter plugin), Mapbox [ctb, cph] (leaflet-omnivore plugin), Michael Bostock [ctb, cph] (topojson), RStudio [cph]
+# Repository: CRAN
+# Date/Publication: 2017-02-21 19:08:12
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-leaps-feedstock contains modified content
diff --git a/r-leaps-feedstock/recipe/meta.yaml b/r-leaps-feedstock/recipe/meta.yaml
index 2843d17..d9c20ef 100644
--- a/r-leaps-feedstock/recipe/meta.yaml
+++ b/r-leaps-feedstock/recipe/meta.yaml
@@ -14,29 +14,43 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/leaps/leaps_{{ version }}.tar.gz
   sha256: 55a879cdad5a4c9bc3b5697dd4d364b3a094a49d8facb6692f5ce6af82adf285
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: biglm
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{ compiler('fortran') }}  # [not win]
+    - {{native}}toolchain        # [win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('leaps')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('leaps')"           # [not win]
     - "\"%R%\" -e \"library('leaps')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=leaps
   license: GPL (>= 2)
@@ -47,3 +61,24 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: leaps
+# Title: Regression Subset Selection
+# Version: 3.0
+# Author: Thomas Lumley based on Fortran code by Alan Miller
+# Description: Regression subset selection, including exhaustive search.
+# Depends:
+# Suggests: biglm
+# License: GPL (>= 2)
+# Maintainer: Thomas Lumley <t.lumley@auckland.ac.nz>
+# NeedsCompilation: yes
+# Packaged: 2017-01-09 20:18:20 UTC; tlumley
+# Repository: CRAN
+# Date/Publication: 2017-01-10 00:04:32
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-lintr-feedstock contains modified content
diff --git a/r-lintr-feedstock/recipe/meta.yaml b/r-lintr-feedstock/recipe/meta.yaml
index f71514f..848cf8b 100644
--- a/r-lintr-feedstock/recipe/meta.yaml
+++ b/r-lintr-feedstock/recipe/meta.yaml
@@ -14,14 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/lintr/lintr_{{ version }}.tar.gz
   sha256: ec732d35c7ab4e9ef914cd3067b5031a724dc9e4f0eb73219c3b1c572340dc23
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  # igraph is not available for windows
-  skip: true  # [win]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: rmarkdown
 requirements:
   build:
     - r-base
@@ -53,17 +60,53 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('lintr')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('lintr')"           # [not win]
     - "\"%R%\" -e \"library('lintr')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/jimhester/lintr
-  license: MIT
+  license: MIT + file LICENSE
   summary: Checks adherence to a given style, syntax errors and possible semantic issues.  Supports
-    on the fly checking of R code edited with Emacs, Vim and Sublime Text.
+    on the fly checking of R code edited with 'RStudio IDE', 'Emacs', 'Vim', 'Sublime
+    Text' and 'Atom'.
   license_family: MIT
 
 extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: lintr
+# Title: A 'Linter' for R Code
+# Version: 1.0.1
+# Authors@R: person("Jim", "Hester", email = "james.f.hester@gmail.com", role = c("aut", "cre"))
+# URL: https://github.com/jimhester/lintr
+# BugReports: https://github.com/jimhester/lintr/issues
+# Description: Checks adherence to a given style, syntax errors and possible semantic issues.  Supports on the fly checking of R code edited with 'RStudio IDE', 'Emacs', 'Vim', 'Sublime Text' and 'Atom'.
+# Depends: R (>= 3.1.1)
+# Imports: rex, crayon, codetools, stringdist, testthat, digest, igraph, rstudioapi (>= 0.2), httr, jsonlite, knitr, stats, utils
+# Suggests: rmarkdown
+# License: MIT + file LICENSE
+# LazyData: true
+# VignetteBuilder: knitr
+# RoxygenNote: 6.0.1
+# NeedsCompilation: no
+# Packaged: 2017-08-10 17:38:23 UTC; jhester
+# Author: Jim Hester [aut, cre]
+# Maintainer: Jim Hester <james.f.hester@gmail.com>
+# Repository: CRAN
+# Date/Publication: 2017-08-10 18:26:05 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-lme4-feedstock contains modified content
diff --git a/r-lme4-feedstock/recipe/meta.yaml b/r-lme4-feedstock/recipe/meta.yaml
index 2dd6226..17552a1 100644
--- a/r-lme4-feedstock/recipe/meta.yaml
+++ b/r-lme4-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '1.1-13' %}
+{% set version = '1.1-14' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,19 +12,30 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/lme4_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/lme4/lme4_{{ version }}.tar.gz
-  sha256: 5fa8b63d15882d153ee105d399b559dd942d97b2e6e7deae3802f41cc451448e
+  sha256: 8a9638ffbefe3341c700e8e885e49bb2006a1072c8056fbe9f07136046d259ed
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: knitr, boot, PKPDmodels, MEMSS, testthat (>= 0.8.1), ggplot2, mlmRev, optimx (>= 2013.8.6), gamm4, pbkrtest, HSAUR2, numDeriv
 requirements:
   build:
     - r-base
+    - {{ compiler('cxx') }}      # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
     - r-mass
     - r-matrix >=1.1.1
     - r-rcpp >=0.10.5
@@ -33,12 +44,10 @@ requirements:
     - r-minqa >=1.1.15
     - r-nlme >=3.1_123
     - r-nloptr >=1.0.4
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-mass
     - r-matrix >=1.1.1
     - r-rcpp >=0.10.5
@@ -47,13 +56,19 @@ requirements:
     - r-minqa >=1.1.15
     - r-nlme >=3.1_123
     - r-nloptr >=1.0.4
-    - libgcc  # [not win]
 
 test:
   commands:
-    - $R -e "library('lme4')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('lme4')"           # [not win]
     - "\"%R%\" -e \"library('lme4')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/lme4/lme4/ http://lme4.r-forge.r-project.org/
   license: GPL (>= 2)
@@ -67,3 +82,32 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: lme4
+# Version: 1.1-14
+# Title: Linear Mixed-Effects Models using 'Eigen' and S4
+# Authors@R: c(person("Douglas","Bates", role="aut"), person("Martin","Maechler", role="aut"), person("Ben","Bolker",email="bbolker+lme4@gmail.com", role=c("aut","cre")), person("Steven","Walker",role="aut"), person("Rune Haubo Bojesen","Christensen", role="ctb"), person("Henrik","Singmann", role="ctb"), person("Bin", "Dai", role="ctb"), person("Gabor", "Grothendieck", role="ctb"), person("Peter", "Green", role="ctb"))
+# Contact: LME4 Authors <lme4-authors@lists.r-forge.r-project.org>
+# Description: Fit linear and generalized linear mixed-effects models. The models and their components are represented using S4 classes and methods.  The core computational algorithms are implemented using the 'Eigen' C++ library for numerical linear algebra and 'RcppEigen' "glue".
+# Depends: R (>= 3.0.2), Matrix (>= 1.1.1), methods, stats
+# LinkingTo: Rcpp (>= 0.10.5), RcppEigen
+# Imports: graphics, grid, splines, utils, parallel, MASS, lattice, nlme (>= 3.1-123), minqa (>= 1.1.15), nloptr (>= 1.0.4)
+# Suggests: knitr, boot, PKPDmodels, MEMSS, testthat (>= 0.8.1), ggplot2, mlmRev, optimx (>= 2013.8.6), gamm4, pbkrtest, HSAUR2, numDeriv
+# VignetteBuilder: knitr
+# LazyData: yes
+# License: GPL (>= 2)
+# URL: https://github.com/lme4/lme4/ http://lme4.r-forge.r-project.org/
+# BugReports: https://github.com/lme4/lme4/issues
+# NeedsCompilation: yes
+# Packaged: 2017-09-20 02:40:54 UTC; bolker
+# Author: Douglas Bates [aut], Martin Maechler [aut], Ben Bolker [aut, cre], Steven Walker [aut], Rune Haubo Bojesen Christensen [ctb], Henrik Singmann [ctb], Bin Dai [ctb], Gabor Grothendieck [ctb], Peter Green [ctb]
+# Maintainer: Ben Bolker <bbolker+lme4@gmail.com>
+# Repository: CRAN
+# Date/Publication: 2017-09-27 08:28:13 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-lmertest-feedstock contains modified content
diff --git a/r-lmertest-feedstock/recipe/meta.yaml b/r-lmertest-feedstock/recipe/meta.yaml
index 2534932..bafa968 100644
--- a/r-lmertest-feedstock/recipe/meta.yaml
+++ b/r-lmertest-feedstock/recipe/meta.yaml
@@ -14,14 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/lmerTest/lmerTest_{{ version }}.tar.gz
   sha256: 15d1371f40a4c9f22c39c390c3f8141e402889cd56c02b390cc4423370e78be4
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: pbkrtest, nlme, estimability
 requirements:
   build:
     - r-base
@@ -43,9 +50,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('lmerTest')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('lmerTest')"           # [not win]
     - "\"%R%\" -e \"library('lmerTest')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=lmerTest
   license: GPL (>= 2)
@@ -60,3 +74,28 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: lmerTest
+# Type: Package
+# Title: Tests in Linear Mixed Effects Models
+# Version: 2.0-33
+# Authors@R: c(person("Alexandra", "Kuznetsova", role = c("aut")), person("Per", "Bruun Brockhoff", role = c("aut", "ths", "cre"),  email = "perbb@dtu.dk"), person("Rune", "Haubo Bojesen Christensen", role = "aut"))
+# Maintainer: Per Bruun Brockhoff <perbb@dtu.dk>
+# Depends: R (>= 3.0.0), Matrix, stats, methods, lme4 (>= 1.0)
+# Imports: plyr, MASS, Hmisc, ggplot2
+# Suggests: pbkrtest, nlme, estimability
+# Description: Different kinds of tests for linear mixed effects models as implemented  in 'lme4' package are provided. The tests comprise types I - III F tests  for fixed effects, LR tests for random effects.  The package also provides the calculation of population means for fixed factors  with confidence intervals  and corresponding plots. Finally the backward  elimination of non-significant effects is implemented.
+# LazyData: TRUE
+# License: GPL (>= 2)
+# Repository: CRAN
+# NeedsCompilation: no
+# Packaged: 2016-12-01 21:17:22 UTC; akuzn_000
+# Author: Alexandra Kuznetsova [aut], Per Bruun Brockhoff [aut, ths, cre], Rune Haubo Bojesen Christensen [aut]
+# Date/Publication: 2016-12-03 10:52:58
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-lmtest-feedstock contains modified content
diff --git a/r-lmtest-feedstock/recipe/meta.yaml b/r-lmtest-feedstock/recipe/meta.yaml
index e3adfa2..979cd0c 100644
--- a/r-lmtest-feedstock/recipe/meta.yaml
+++ b/r-lmtest-feedstock/recipe/meta.yaml
@@ -14,35 +14,48 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/lmtest/lmtest_{{ version }}.tar.gz
   sha256: 2f4982f1b6cd133ef36fe9718f4ff2f4fa5c20716100fdd5ee5c947b68c8eb80
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: car, strucchange, sandwich, dynlm, stats4, survival, AER
 requirements:
   build:
     - r-base
+    - {{ compiler('fortran') }}  # [not win]
+    - {{native}}toolchain        # [win]
     - r-zoo
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-zoo
-    - libgcc  # [not win]
 
 test:
   commands:
-    - $R -e "library('lmtest')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('lmtest')"           # [not win]
     - "\"%R%\" -e \"library('lmtest')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=lmtest
-  license: GPL (>= 2)
+  license: GPL-2 | GPL-3
   summary: A collection of tests, data sets, and examples for diagnostic checking in linear regression
     models. Furthermore, some generic tools for inference in parametric models are provided.
   license_family: GPL3
@@ -51,3 +64,28 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: lmtest
+# Title: Testing Linear Regression Models
+# Version: 0.9-35
+# Date: 2017-02-11
+# Authors@R: c(person(given = "Torsten", family = "Hothorn", role = "aut", email = "Torsten.Hothorn@R-project.org"), person(given = "Achim", family = "Zeileis", role = c("aut", "cre"), email = "Achim.Zeileis@R-project.org"), person(given = c("Richard", "W."), family = "Farebrother", role = "aut", comment = "pan.f"), person(given = "Clint", family = "Cummins", role = "aut", comment = "pan.f"), person(given = "Giovanni", family = "Millo", role = "ctb"), person(given = "David", family = "Mitchell", role = "ctb"))
+# Description: A collection of tests, data sets, and examples for diagnostic checking in linear regression models. Furthermore, some generic tools for inference in parametric models are provided.
+# LazyData: yes
+# Depends: R (>= 2.10.0), stats, zoo
+# Suggests: car, strucchange, sandwich, dynlm, stats4, survival, AER
+# Imports: graphics
+# License: GPL-2 | GPL-3
+# NeedsCompilation: yes
+# Packaged: 2017-02-11 09:30:28 UTC; zeileis
+# Author: Torsten Hothorn [aut], Achim Zeileis [aut, cre], Richard W. Farebrother [aut] (pan.f), Clint Cummins [aut] (pan.f), Giovanni Millo [ctb], David Mitchell [ctb]
+# Maintainer: Achim Zeileis <Achim.Zeileis@R-project.org>
+# Repository: CRAN
+# Date/Publication: 2017-02-11 10:43:09
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-logging-feedstock contains modified content
diff --git a/r-logging-feedstock/recipe/meta.yaml b/r-logging-feedstock/recipe/meta.yaml
index 257b1e0..556fd7f 100644
--- a/r-logging-feedstock/recipe/meta.yaml
+++ b/r-logging-feedstock/recipe/meta.yaml
@@ -14,14 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/logging/logging_{{ version }}.tar.gz
   sha256: aca8153206cc815a49470844c48f44f84e6d7a6ce52686a8593f727582c0e7ea
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: svUnit, XML
 requirements:
   build:
     - r-base
@@ -31,9 +38,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('logging')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('logging')"           # [not win]
     - "\"%R%\" -e \"library('logging')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=logging
   license: GPL (>= 2)
@@ -44,3 +58,28 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: logging
+# Version: 0.7-103
+# Date: 2013-04-08
+# Title: R logging package
+# Author: Mario Frasca <mariotomo@gmail.com>
+# Maintainer: Mario Frasca <mariotomo@gmail.com>
+# Description: logging is a pure R package that implements the ubiquitous log4j package.
+# License: GPL (>= 2)
+# Depends: R (>= 2.12.0), methods
+# Suggests: svUnit, XML
+# Repository: CRAN
+# Repository/R-Forge/Project: logging
+# Repository/R-Forge/Revision: 103
+# Repository/R-Forge/DateTimeStamp: 2013-04-08 09:57:38
+# Date/Publication: 2013-04-12 08:30:00
+# Packaged: 2013-04-10 01:16:35 UTC; rforge
+# NeedsCompilation: no
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-lsmeans-feedstock contains modified content
diff --git a/r-lsmeans-feedstock/recipe/meta.yaml b/r-lsmeans-feedstock/recipe/meta.yaml
index 49df8cf..efd26b6 100644
--- a/r-lsmeans-feedstock/recipe/meta.yaml
+++ b/r-lsmeans-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '2.25-5' %}
+{% set version = '2.27-2' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,16 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/lsmeans_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/lsmeans/lsmeans_{{ version }}.tar.gz
-  sha256: 7c332a972bb3f261e484256f494289c388cc1eda1c86669a404193017eba3cce
+  sha256: e0da43976ffcdf1df2e77927f76de378a5fcdf29010db436ed6cfe041e4b66a1
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: car, lattice, MCMCpack, mediation, multcompView, ordinal, pbkrtest (>= 0.4-1), CARBayes, coxme, gee, geepack, glmmADMB, lme4.0, lme4, lmerTest (>= 2.0.32), MASS, MCMCglmm, nnet, pscl, rsm, rstan, rstanarm, survival
 requirements:
   build:
     - r-base
@@ -45,12 +52,19 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('lsmeans')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('lsmeans')"           # [not win]
     - "\"%R%\" -e \"library('lsmeans')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=lsmeans
-  license: GPL (>= 2)
+  license: GPL-2 | GPL-3
   summary: Obtain least-squares means for many linear, generalized linear,  and mixed models.
     Compute contrasts or linear functions of least-squares means, and comparisons of
     slopes. Plots and compact letter displays.
@@ -60,3 +74,32 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: lsmeans
+# Type: Package
+# Title: Least-Squares Means
+# Version: 2.27-2
+# Date: 2017-08-28
+# Authors@R: c(person("Russell", "Lenth", role = c("aut", "cre", "cph"),  email = "russell-lenth@uiowa.edu"), person("Jonathon", "Love", role = "ctb"))
+# Depends: estimability, methods, R (>= 3.2)
+# Suggests: car, lattice, MCMCpack, mediation, multcompView, ordinal, pbkrtest (>= 0.4-1), CARBayes, coxme, gee, geepack, glmmADMB, lme4.0, lme4, lmerTest (>= 2.0.32), MASS, MCMCglmm, nnet, pscl, rsm, rstan, rstanarm, survival
+# Imports: graphics, stats, utils, nlme, coda (>= 0.17), multcomp, plyr, mvtnorm, xtable (>= 1.8-2)
+# Additional_repositories: http://glmmadmb.r-forge.r-project.org/repos, http://lme4.0.r-forge.r-project.org/repos
+# BugReports: https://github.com/rvlenth/lsmeans/issues
+# LazyData: yes
+# ByteCompile: yes
+# Description: Obtain least-squares means for many linear, generalized linear,  and mixed models. Compute contrasts or linear functions of least-squares means, and comparisons of slopes. Plots and compact letter displays.
+# License: GPL-2 | GPL-3
+# NeedsCompilation: no
+# Packaged: 2017-08-28 13:26:59 UTC; rlenth
+# Author: Russell Lenth [aut, cre, cph], Jonathon Love [ctb]
+# Maintainer: Russell Lenth <russell-lenth@uiowa.edu>
+# Repository: CRAN
+# Date/Publication: 2017-08-28 14:41:31 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-lubridate-feedstock contains modified content
diff --git a/r-lubridate-feedstock/recipe/meta.yaml b/r-lubridate-feedstock/recipe/meta.yaml
index be1e180..b2f8749 100644
--- a/r-lubridate-feedstock/recipe/meta.yaml
+++ b/r-lubridate-feedstock/recipe/meta.yaml
@@ -14,41 +14,53 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/lubridate/lubridate_{{ version }}.tar.gz
   sha256: 1a87edfc2c57b22b6d9b49354545dd063e6c03a415544090b531c42423cf2632
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: testthat, knitr, covr
 requirements:
   build:
     - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
     - r-stringr
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-stringr
-    - libgcc  # [not win]
 
 test:
   commands:
-    - $R -e "library('lubridate')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('lubridate')"           # [not win]
     - "\"%R%\" -e \"library('lubridate')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=lubridate
   license: GPL-2
-  summary: |
-    Functions to work with date-times and time-spans: fast and user friendly parsing
+  summary: 'Functions to work with date-times and time-spans: fast and user friendly parsing
     of date-time data, extraction and updating of components of a date-time (years,
     months, days, hours, minutes, and seconds), algebraic manipulation on date-time
     and time-span objects. The ''lubridate'' package has a consistent and memorable
-    syntax that makes working with dates easy and fun.
+    syntax that makes working with dates easy and fun.'
 
   license_family: GPL2
 
@@ -56,3 +68,33 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: lubridate
+# Maintainer: Vitalie Spinu <spinuvit@gmail.com>
+# License: GPL-2
+# Title: Make Dealing with Dates a Little Easier
+# LazyData: true
+# Type: Package
+# Authors@R: c(person("Garrett", "Grolemund", role = "aut"), person("Vitalie", "Spinu", email = "spinuvit@gmail.com", role = c("aut","cre")), person("Hadley", "Wickham", role = "aut"), person("Ian", "Lyttle", role="ctb"), person("Imanuel", "Constigan", role = "ctb"), person("Jason", "Law", role="ctb"), person("Doug","Mitarotonda", role="ctb"), person("Joseph", "Larmarange", role="ctb"), person("Jonathan", "Boiser", role="ctb"), person("Chel Hee", "Lee", role = "ctb"))
+# Description: Functions to work with date-times and time-spans: fast and user friendly parsing of date-time data, extraction and updating of components of a date-time (years, months, days, hours, minutes, and seconds), algebraic manipulation on date-time and time-span objects. The 'lubridate' package has a consistent and memorable syntax that makes working with dates easy and fun.
+# Enhances: chron, timeDate, zoo, xts, its, tis, timeSeries, fts, tseries
+# Version: 1.6.0
+# Depends: methods, R (>= 3.0.0)
+# Imports: stringr
+# Suggests: testthat, knitr, covr
+# BugReports: https://github.com/hadley/lubridate/issues
+# VignetteBuilder: knitr
+# Collate: 'Dates.r' 'POSIXt.r' 'util.r' 'timespans.r' 'intervals.r' 'difftimes.r' 'durations.r' 'periods.r' 'accessors-date.R' 'accessors-day.r' 'accessors-dst.r' 'accessors-hour.r' 'accessors-minute.r' 'accessors-month.r' 'accessors-quarter.r' 'accessors-second.r' 'accessors-tz.r' 'accessors-week.r' 'accessors-year.r' 'am-pm.r' 'time-zones.r' 'numeric.r' 'coercion.r' 'constants.r' 'data.r' 'decimal-dates.r' 'deprecated.r' 'guess.r' 'help.r' 'hidden.r' 'instants.r' 'leap-years.r' 'ops-addition.r' 'ops-%m+%.r' 'ops-compare.r' 'ops-division.r' 'ops-integer-division.r' 'ops-modulo.r' 'ops-multiplication.r' 'ops-subtraction.r' 'parse.r' 'pretty.r' 'round.r' 'stamp.r' 'update.r'
+# RoxygenNote: 5.0.1
+# NeedsCompilation: yes
+# Packaged: 2016-09-12 17:23:27 UTC; vspinu
+# Author: Garrett Grolemund [aut], Vitalie Spinu [aut, cre], Hadley Wickham [aut], Ian Lyttle [ctb], Imanuel Constigan [ctb], Jason Law [ctb], Doug Mitarotonda [ctb], Joseph Larmarange [ctb], Jonathan Boiser [ctb], Chel Hee Lee [ctb]
+# Repository: CRAN
+# Date/Publication: 2016-09-13 13:11:52
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-magrittr-feedstock contains modified content
diff --git a/r-magrittr-feedstock/recipe/bld.bat b/r-magrittr-feedstock/recipe/bld.bat
new file mode 100644
index 0000000..8b10c94
--- /dev/null
+++ b/r-magrittr-feedstock/recipe/bld.bat
@@ -0,0 +1,8 @@
+"%R%" CMD INSTALL --build .
+if errorlevel 1 exit 1
+
+@rem Add more build steps here, if they are necessary.
+
+@rem See
+@rem http://docs.continuum.io/conda/build.html
+@rem for a list of environment variables that are set during the build process.
diff --git a/r-magrittr-feedstock/recipe/build.sh b/r-magrittr-feedstock/recipe/build.sh
new file mode 100644
index 0000000..0c37df6
--- /dev/null
+++ b/r-magrittr-feedstock/recipe/build.sh
@@ -0,0 +1,13 @@
+#!/bin/bash
+
+# R refuses to build packages that mark themselves as Priority: Recommended
+mv DESCRIPTION DESCRIPTION.old
+grep -v '^Priority: ' DESCRIPTION.old > DESCRIPTION
+
+$R CMD INSTALL --build .
+
+# Add more build steps here, if they are necessary.
+
+# See
+# http://docs.continuum.io/conda/build.html
+# for a list of environment variables that are set during the build process.
diff --git a/r-magrittr-feedstock/recipe/meta.yaml b/r-magrittr-feedstock/recipe/meta.yaml
index bd7aeda..608b06f 100644
--- a/r-magrittr-feedstock/recipe/meta.yaml
+++ b/r-magrittr-feedstock/recipe/meta.yaml
@@ -1,44 +1,86 @@
-{% set version = "1.5" %}
+{% set version = '1.5' %}
+
+{% set posix = 'm2-' if win else '' %}
+{% set native = 'm2w64-' if win else '' %}
 
 package:
   name: r-magrittr
-  version: {{ version }}
+  version: {{ version|replace("-", "_") }}
 
 source:
   fn: magrittr_{{ version }}.tar.gz
   url:
-    - http://cran.r-project.org/src/contrib/magrittr_{{ version }}.tar.gz
-    - http://cran.r-project.org/src/contrib/Archive/magrittr/magrittr_{{ version }}.tar.gz
+    - https://cran.r-project.org/src/contrib/magrittr_{{ version }}.tar.gz
+    - https://cran.r-project.org/src/contrib/Archive/magrittr/magrittr_{{ version }}.tar.gz
   sha256: 05c45943ada9443134caa0ab24db4a962b629f00b755ccf039a2a2a7b2c92ae8
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  script: R CMD INSTALL --build .
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: testthat, knitr
 requirements:
   build:
     - r-base
+
   run:
     - r-base
 
 test:
   commands:
-    - R -e "library('magrittr')"  # [not win]
-    - R -e \"library('magrittr')\"  # [win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('magrittr')"           # [not win]
+    - "\"%R%\" -e \"library('magrittr')\""  # [win]
+
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
 
 about:
-  home: https://cran.r-project.org/web/packages/magrittr/vignettes/magrittr.html
-  license: MIT
-  license_file: LICENSE
-  summary: "Provides a mechanism for chaining commands with a new forward-pipe operator, %>%.
+  home: https://CRAN.R-project.org/package=magrittr
+  license: MIT + file LICENSE
+  summary: Provides a mechanism for chaining commands with a new forward-pipe operator, %>%.
     This operator will forward a value, or the result of an expression, into the next
     function call/expression. There is flexible support for the type of right-hand side
     expressions. For more information, see package vignette. To quote Rene Magritte,
-    Ceci n'est pas un pipe."
+    "Ceci n'est pas un pipe."
+  license_family: MIT
 
 extra:
   recipe-maintainers:
     - ocefpaf
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: magrittr
+# Type: Package
+# Title: A Forward-Pipe Operator for R
+# Version: 1.5
+# Author: Stefan Milton Bache <stefan@stefanbache.dk> and Hadley Wickham <h.wickham@gmail.com>
+# Maintainer: Stefan Milton Bache <stefan@stefanbache.dk>
+# Description: Provides a mechanism for chaining commands with a new forward-pipe operator, %>%. This operator will forward a value, or the result of an expression, into the next function call/expression. There is flexible support for the type of right-hand side expressions. For more information, see package vignette. To quote Rene Magritte, "Ceci n'est pas un pipe."
+# Suggests: testthat, knitr
+# VignetteBuilder: knitr
+# License: MIT + file LICENSE
+# ByteCompile: Yes
+# Packaged: 2014-11-22 08:50:53 UTC; shb
+# NeedsCompilation: no
+# Repository: CRAN
+# Date/Publication: 2014-11-22 19:15:57
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-manipulate-feedstock contains modified content
diff --git a/r-manipulate-feedstock/recipe/meta.yaml b/r-manipulate-feedstock/recipe/meta.yaml
index 91a2347..c17cc3f 100644
--- a/r-manipulate-feedstock/recipe/meta.yaml
+++ b/r-manipulate-feedstock/recipe/meta.yaml
@@ -14,14 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/manipulate/manipulate_{{ version }}.tar.gz
   sha256: 7e7e829252c3b3c1f768bb76a5d952f0e98f95c843b5f58da325deeb1ab693ce
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
@@ -31,9 +38,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('manipulate')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('manipulate')"           # [not win]
     - "\"%R%\" -e \"library('manipulate')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=manipulate
   license: GPL-2
@@ -48,3 +62,28 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: manipulate
+# Type: Package
+# Title: Interactive Plots for RStudio
+# Version: 1.0.1
+# Date: 2014-12-23
+# Authors@R: c( person("JJ", "Allaire", role = c("aut", "cre"), comment = "R interface", email = "jj@rstudio.com"), person(family = "RStudio", role = "cph") )
+# Maintainer: JJ Allaire <jj@rstudio.com>
+# Description: Interactive plotting functions for use within RStudio. The manipulate function accepts a plotting expression and a set of controls (e.g. slider, picker, checkbox, or button) which are used to dynamically change values within the expression. When a value is changed using its corresponding control the expression is automatically re-executed and the plot is redrawn.
+# Depends: R (>= 2.11.1)
+# SystemRequirements: RStudio - http://www.rstudio.com/products/rstudio/
+# License: GPL-2
+# LazyLoad: yes
+# Packaged: 2014-12-23 18:00:41 UTC; jjallaire
+# Author: JJ Allaire [aut, cre] (R interface), RStudio [cph]
+# NeedsCompilation: no
+# Repository: CRAN
+# Date/Publication: 2014-12-24 01:18:44
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-mapproj-feedstock contains modified content
diff --git a/r-mapproj-feedstock/recipe/meta.yaml b/r-mapproj-feedstock/recipe/meta.yaml
index 320b49e..ca245df 100644
--- a/r-mapproj-feedstock/recipe/meta.yaml
+++ b/r-mapproj-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '1.2-4' %}
+{% set version = '1.2-5' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,36 +12,50 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/mapproj_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/mapproj/mapproj_{{ version }}.tar.gz
-  sha256: cf8a1535f57e7cca0a71b3a551e77ad3e7a78f61a94bb19effd3de19dbe7dceb
+  sha256: f3026a3a69a550c923b44c18b1ccc60d98e52670a438250d13f3c74cf2195f66
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  # r-maps is missing in win64 as well
-  skip: true  # [win]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
     - r-maps >=2.3_0
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-maps >=2.3_0
 
 test:
   commands:
-    - $R -e "library('mapproj')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('mapproj')"           # [not win]
     - "\"%R%\" -e \"library('mapproj')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=mapproj
-  license: Lucent Public
+  license: Lucent Public License
   summary: Converts latitude/longitude into projected coordinates.
   license_family: OTHER
 
@@ -49,3 +63,25 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: mapproj
+# Title: Map Projections
+# Version: 1.2-5
+# Date: 2017-06-06
+# Author: Doug McIlroy.  Packaged for R by Ray Brownrigg and Thomas P Minka, transition to Plan 9 codebase by Roger Bivand.
+# Description: Converts latitude/longitude into projected coordinates.
+# Depends: maps (>= 2.3-0)
+# Imports: stats, graphics
+# License: Lucent Public License
+# Maintainer: Alex Deckmyn <alex.deckmyn@meteo.be>
+# NeedsCompilation: yes
+# Packaged: 2017-06-07 08:34:30 UTC; dalex
+# Repository: CRAN
+# Date/Publication: 2017-06-08 04:43:11 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
diff --git a/r-maps-feedstock/recipe/meta.yaml b/r-maps-feedstock/recipe/meta.yaml
index 214aacb..b70bb39 100644
--- a/r-maps-feedstock/recipe/meta.yaml
+++ b/r-maps-feedstock/recipe/meta.yaml
@@ -12,9 +12,9 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/maps_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/maps/maps_{{ version }}.tar.gz
+  sha256: 437abeb4fa4ad4a36af6165d319634b89bfc6bf2b1827ca86c478d56d670e714
 
 
-  sha256: 437abeb4fa4ad4a36af6165d319634b89bfc6bf2b1827ca86c478d56d670e714
   # patches:
    # List any patch files here
    # - fix.patch
@@ -32,19 +32,22 @@ build:
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}autoconf
+    - {{posix}}automake
+    - {{posix}}pkg-config
+    - {{posix}}make
 
   run:
     - r-base
-    - {{native}}gcc-libs   # [win]
-    - libgcc               # [not win]
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
     # You can put additional test commands to be run here.
-    - $R -e "library('maps')"  # [not win]
+    - $R -e "library('maps')"           # [not win]
     - "\"%R%\" -e \"library('maps')\""  # [win]
 
   # You can also put a file called run_test.py, run_test.sh, or run_test.bat
@@ -60,6 +63,10 @@ about:
     and 'mapdata').
   license_family: GPL2
 
+extra:
+  recipe-maintainers:
+    - mingwandroid
+
 # The original CRAN metadata for this package was:
 
 # Package: maps
Submodule r-maptools-feedstock contains modified content
diff --git a/r-maptools-feedstock/recipe/meta.yaml b/r-maptools-feedstock/recipe/meta.yaml
index 9221f43..3461621 100644
--- a/r-maptools-feedstock/recipe/meta.yaml
+++ b/r-maptools-feedstock/recipe/meta.yaml
@@ -14,36 +14,49 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/maptools/maptools_{{ version }}.tar.gz
   sha256: 3bf88cd06f69168d169727ca90d378dfedca77d80ebd26380b2708b3c6aab41c
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: rgeos (>= 0.1-8), spatstat (>= 1.18), PBSmapping, maps, RColorBrewer, raster, polyclip, spatstat.utils
 requirements:
   build:
     - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
     - r-foreign >=0.8
     - r-lattice
     - r-sp >=1.0_11
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-foreign >=0.8
     - r-lattice
     - r-sp >=1.0_11
-    - libgcc  # [not win]
 
 test:
   commands:
-    - $R -e "library('maptools')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('maptools')"           # [not win]
     - "\"%R%\" -e \"library('maptools')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://r-forge.r-project.org/projects/maptools/
   license: GPL (>= 2)
@@ -58,3 +71,30 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: maptools
+# Version: 0.9-2
+# Date: 2017-03-24
+# Title: Tools for Reading and Handling Spatial Objects
+# Authors@R: c( person("Roger", "Bivand", role = c("cre", "aut"), email = "Roger.Bivand@nhh.no"), person("Nicholas", "Lewin-Koh", role = "aut"), person("Edzer", "Pebesma", role = "ctb"), person("Eric", "Archer", role = "ctb"), person("Adrian", "Baddeley", role = "ctb"), person("Nick", "Bearman", role = "ctb"), person("Hans-Jorg", "Bibiko", role = "ctb"), person("Steven", "Brey", role = "ctb"), person("Jonathan", "Callahan", role = "ctb"), person("German", "Carrillo", role = "ctb"), person("Stephane", "Dray", role = "ctb"), person("David", "Forrest", role = "ctb"), person("Michael", "Friendly", role = "ctb"), person("Patrick", "Giraudoux", role = "ctb"), person("Duncan", "Golicher", role = "ctb"), person("Virgilio", "Gomez Rubio", role = "ctb"), person("Patrick", "Hausmann", role = "ctb"), person("Karl Ove", "Hufthammer", role = "ctb"), person("Thomas", "Jagger", role = "ctb"), person("Kent", "Johnson", role = "ctb"), person("Sebastian", "Luque", role = "ctb"), person("Don", "MacQueen", role = "ctb"), person("Andrew", "Niccolai", role = "ctb"), person("Edzer", "Pebesma", role = "ctb"), person("Oscar", "Perpinan Lamigueiro", role = "ctb"), person("Tom", "Short", role = "ctb"), person("Greg", "Snow", role = "ctb"), person("Ben", "Stabler", role = "ctb"), person("Murray", "Stokely", role = "ctb"), person("Rolf", "Turner", role = "ctb"))
+# Encoding: UTF-8
+# Depends: R (>= 2.10), sp (>= 1.0-11)
+# Imports: foreign (>= 0.8), methods, grid, lattice, stats, utils, grDevices
+# Suggests: rgeos (>= 0.1-8), spatstat (>= 1.18), PBSmapping, maps, RColorBrewer, raster, polyclip, spatstat.utils
+# Enhances: gpclib, RArcInfo
+# Description: Set of tools for manipulating and reading geographic data, in particular 'ESRI Shapefiles'; C code used from 'shapelib'. It includes binary access to 'GSHHG' shoreline files. The package also provides interface wrappers for exchanging spatial objects with packages such as 'PBSmapping', 'spatstat', 'maps', 'RArcInfo', 'Stata tmap', 'WinBUGS', 'Mondrian', and others.
+# License: GPL (>= 2)
+# URL: http://r-forge.r-project.org/projects/maptools/
+# NeedsCompilation: yes
+# Packaged: 2017-03-25 12:06:53 UTC; rsb
+# Author: Roger Bivand [cre, aut], Nicholas Lewin-Koh [aut], Edzer Pebesma [ctb], Eric Archer [ctb], Adrian Baddeley [ctb], Nick Bearman [ctb], Hans-Jorg Bibiko [ctb], Steven Brey [ctb], Jonathan Callahan [ctb], German Carrillo [ctb], Stephane Dray [ctb], David Forrest [ctb], Michael Friendly [ctb], Patrick Giraudoux [ctb], Duncan Golicher [ctb], Virgilio Gomez Rubio [ctb], Patrick Hausmann [ctb], Karl Ove Hufthammer [ctb], Thomas Jagger [ctb], Kent Johnson [ctb], Sebastian Luque [ctb], Don MacQueen [ctb], Andrew Niccolai [ctb], Edzer Pebesma [ctb], Oscar Perpinan Lamigueiro [ctb], Tom Short [ctb], Greg Snow [ctb], Ben Stabler [ctb], Murray Stokely [ctb], Rolf Turner [ctb]
+# Maintainer: Roger Bivand <Roger.Bivand@nhh.no>
+# Repository: CRAN
+# Date/Publication: 2017-03-25 16:04:54 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-markdown-feedstock contains modified content
diff --git a/r-markdown-feedstock/recipe/meta.yaml b/r-markdown-feedstock/recipe/meta.yaml
index 8d31f93..4094693 100644
--- a/r-markdown-feedstock/recipe/meta.yaml
+++ b/r-markdown-feedstock/recipe/meta.yaml
@@ -14,30 +14,45 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/markdown/markdown_{{ version }}.tar.gz
   sha256: 538fd912b2220f2df344c6cca58304ce11e0960de7bd7bd573b3385105d48fed
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: knitr, RCurl
 requirements:
   build:
     - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
     - r-mime >=0.3
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-mime >=0.3
 
 test:
   commands:
-    - $R -e "library('markdown')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('markdown')"           # [not win]
     - "\"%R%\" -e \"library('markdown')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/rstudio/markdown
   license: GPL-2
@@ -51,3 +66,32 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: markdown
+# Type: Package
+# Title: 'Markdown' Rendering for R
+# Version: 0.8
+# Date: 2017-04-20
+# Authors@R: c( person("JJ", "Allaire", role = "aut"), person("Jeffrey", "Horner", role = "aut"), person("Henrik", "Bengtsson", role = "ctb"), person("Jim", "Hester", role = "ctb"), person("Yixuan", "Qiu", role = "ctb"), person("Kohske", "Takahashi", role = "ctb"), person("Adam", "November", role = "ctb"), person("Nacho", "Caballero", role = "ctb"), person("Jeroen", "Ooms", role = "ctb"), person("Thomas", "Leeper", role = "ctb"), person("Joe", "Cheng", role = "ctb"), person("Andrzej", "Oles", role = "ctb"), person("Vicent", "Marti", role = c("aut", "cph"), comment = "The Sundown library"), person("Natacha", "Porte", role = c("aut", "cph"), comment = "The Sundown library"), person(family = "RStudio", role = "cph"), person("Yihui", "Xie", role = c("cre", "ctb"), email = "xie@yihui.name") )
+# Description: Provides R bindings to the 'Sundown' 'Markdown' rendering library (https://github.com/vmg/sundown). 'Markdown' is a plain-text formatting syntax that can be converted to 'XHTML' or other formats. See http://en.wikipedia.org/wiki/Markdown for more information about 'Markdown'.
+# Depends: R (>= 2.11.1)
+# Imports: utils, mime (>= 0.3)
+# Suggests: knitr, RCurl
+# License: GPL-2
+# URL: https://github.com/rstudio/markdown
+# BugReports: https://github.com/rstudio/markdown/issues
+# VignetteBuilder: knitr
+# RoxygenNote: 6.0.1
+# NeedsCompilation: yes
+# Packaged: 2017-04-19 20:14:01 UTC; yihui
+# Author: JJ Allaire [aut], Jeffrey Horner [aut], Henrik Bengtsson [ctb], Jim Hester [ctb], Yixuan Qiu [ctb], Kohske Takahashi [ctb], Adam November [ctb], Nacho Caballero [ctb], Jeroen Ooms [ctb], Thomas Leeper [ctb], Joe Cheng [ctb], Andrzej Oles [ctb], Vicent Marti [aut, cph] (The Sundown library), Natacha Porte [aut, cph] (The Sundown library), RStudio [cph], Yihui Xie [cre, ctb]
+# Maintainer: Yihui Xie <xie@yihui.name>
+# Repository: CRAN
+# Date/Publication: 2017-04-20 09:07:54 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-mass-feedstock contains modified content
diff --git a/r-mass-feedstock/recipe/meta.yaml b/r-mass-feedstock/recipe/meta.yaml
index c9cc5a0..964cdba 100644
--- a/r-mass-feedstock/recipe/meta.yaml
+++ b/r-mass-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '7.3-45' %}
+{% set version = '7.3-47' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,31 +12,45 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/MASS_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/MASS/MASS_{{ version }}.tar.gz
-  sha256: 5de3241407c6e12de99ed7986e9d30dd07a25056b3ae8c5cbd760998422a978e
+  sha256: ed44cdabe84fff3553122267ade61d5cc68071c435f7645d36c8f2e4e9f9c6bf
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: lattice, nlme, nnet, survival
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('MASS')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('MASS')"           # [not win]
     - "\"%R%\" -e \"library('MASS')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://www.stats.ox.ac.uk/pub/MASS4/
   license: GPL-2 | GPL-3
@@ -48,3 +62,33 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: MASS
+# Priority: recommended
+# Version: 7.3-47
+# Date: 2017-02-26
+# Revision: $Rev: 3474 $
+# Depends: R (>= 3.1.0), grDevices, graphics, stats, utils
+# Imports: methods
+# Suggests: lattice, nlme, nnet, survival
+# Authors@R: c(person("Brian", "Ripley", role = c("aut", "cre", "cph"), email = "ripley@stats.ox.ac.uk"), person("Bill", "Venables", role = "ctb"), person(c("Douglas", "M."), "Bates", role = "ctb"), person("Kurt", "Hornik", role = "trl", comment = "partial port ca 1998"), person("Albrecht", "Gebhardt", role = "trl", comment = "partial port ca 1998"), person("David", "Firth", role = "ctb"))
+# Description: Functions and datasets to support Venables and Ripley, "Modern Applied Statistics with S" (4th edition, 2002).
+# Title: Support Functions and Datasets for Venables and Ripley's MASS
+# LazyData: yes
+# ByteCompile: yes
+# License: GPL-2 | GPL-3
+# URL: http://www.stats.ox.ac.uk/pub/MASS4/
+# Contact: <MASS@stats.ox.ac.uk>
+# NeedsCompilation: yes
+# Packaged: 2017-02-26 08:41:42 UTC; ripley
+# Author: Brian Ripley [aut, cre, cph], Bill Venables [ctb], Douglas M. Bates [ctb], Kurt Hornik [trl] (partial port ca 1998), Albrecht Gebhardt [trl] (partial port ca 1998), David Firth [ctb]
+# Maintainer: Brian Ripley <ripley@stats.ox.ac.uk>
+# Repository: CRAN
+# Date/Publication: 2017-04-21 13:33:21 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-matrix-feedstock contains modified content
diff --git a/r-matrix-feedstock/recipe/bld.bat b/r-matrix-feedstock/recipe/bld.bat
new file mode 100644
index 0000000..8b10c94
--- /dev/null
+++ b/r-matrix-feedstock/recipe/bld.bat
@@ -0,0 +1,8 @@
+"%R%" CMD INSTALL --build .
+if errorlevel 1 exit 1
+
+@rem Add more build steps here, if they are necessary.
+
+@rem See
+@rem http://docs.continuum.io/conda/build.html
+@rem for a list of environment variables that are set during the build process.
diff --git a/r-matrix-feedstock/recipe/build.sh b/r-matrix-feedstock/recipe/build.sh
new file mode 100644
index 0000000..0c37df6
--- /dev/null
+++ b/r-matrix-feedstock/recipe/build.sh
@@ -0,0 +1,13 @@
+#!/bin/bash
+
+# R refuses to build packages that mark themselves as Priority: Recommended
+mv DESCRIPTION DESCRIPTION.old
+grep -v '^Priority: ' DESCRIPTION.old > DESCRIPTION
+
+$R CMD INSTALL --build .
+
+# Add more build steps here, if they are necessary.
+
+# See
+# http://docs.continuum.io/conda/build.html
+# for a list of environment variables that are set during the build process.
diff --git a/r-matrix-feedstock/recipe/meta.yaml b/r-matrix-feedstock/recipe/meta.yaml
index 06a8bdd..033f90e 100644
--- a/r-matrix-feedstock/recipe/meta.yaml
+++ b/r-matrix-feedstock/recipe/meta.yaml
@@ -1,47 +1,99 @@
-{% set version = "1.2-7.1" %}
+{% set version = '1.2-11' %}
+
+{% set posix = 'm2-' if win else '' %}
+{% set native = 'm2w64-' if win else '' %}
 
 package:
   name: r-matrix
-  version: {{ version.replace("-", "_") }}
+  version: {{ version|replace("-", "_") }}
 
 source:
   fn: Matrix_{{ version }}.tar.gz
   url:
-    - http://cran.r-project.org/src/contrib/Matrix_{{ version }}.tar.gz
-    - http://cran.r-project.org/src/contrib/Archive/Matrix/Matrix_{{ version }}.tar.gz
-  sha256: 4aeb66dabae3ddfa9e96a40eb48448ecf0413874937a1a2a05ffeaa46c282d27
+    - https://cran.r-project.org/src/contrib/Matrix_{{ version }}.tar.gz
+    - https://cran.r-project.org/src/contrib/Archive/Matrix/Matrix_{{ version }}.tar.gz
+  sha256: cf8ccba0a89eaa261f6332ce013c82255f34a21ce4d9fcf72332253bba336dfb
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: True  # [win32]
-  script: R CMD INSTALL --build .
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
-requirements:  # [not win32]
-  build:  # [not win32]
-    - r-base  # [not win32]
-    - posix  # [win64]
-    - m2w64-toolchain  # [win64]
-    - gcc  # [not win]
-    - r-lattice  # [not win32]
-  run:  # [not win32]
-    - r-base  # [not win32]
-    - r-lattice  # [not win32]
+# Suggests: expm, MASS
+requirements:
+  build:
+    - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('fortran') }}  # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
+    - r-lattice
+
+  run:
+    - r-base
+    - {{native}}gcc-libs         # [win]
+    - r-lattice
 
 test:
   commands:
-    - R -e "library('Matrix')"  # [not win]
-    - R -e \"library('Matrix')\"  # [win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('Matrix')"           # [not win]
+    - "\"%R%\" -e \"library('Matrix')\""  # [win]
+
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
 
 about:
   home: http://Matrix.R-forge.R-project.org/
-  license: GPL-2.0
-  license_file: LICENCE
-  summary: "Classes and methods for dense and sparse matrices and operations on them using 'LAPACK'
-    and 'SuiteSparse'."
+  license: GPL (>= 2) | file LICENCE
+  summary: Classes and methods for dense and sparse matrices and operations on them using 'LAPACK'
+    and 'SuiteSparse'.
+  license_family: GPL3
 
 extra:
   recipe-maintainers:
     - ocefpaf
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: Matrix
+# Version: 1.2-11
+# Date: 2017-08-08
+# Priority: recommended
+# Title: Sparse and Dense Matrix Classes and Methods
+# Author: Douglas Bates <bates@stat.wisc.edu> and Martin Maechler
+# Maintainer: Martin Maechler <mmaechler+Matrix@gmail.com>
+# Contact: Doug and Martin <Matrix-authors@R-project.org>
+# Description: Classes and methods for dense and sparse matrices and operations on them using 'LAPACK' and 'SuiteSparse'.
+# Depends: R (>= 3.0.1)
+# Imports: methods, graphics, grid, stats, utils, lattice
+# Suggests: expm, MASS
+# Enhances: MatrixModels, graph, SparseM, sfsmisc
+# Encoding: UTF-8
+# LazyData: no
+# LazyDataNote: not possible, since we use data/*.R *and* our classes
+# BuildResaveData: no
+# License: GPL (>= 2) | file LICENCE
+# URL: http://Matrix.R-forge.R-project.org/
+# NeedsCompilation: yes
+# Packaged: 2017-08-10 15:34:00 UTC; maechler
+# Repository: CRAN
+# Date/Publication: 2017-08-16 08:50:22 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-matrixmodels-feedstock contains modified content
diff --git a/r-matrixmodels-feedstock/recipe/meta.yaml b/r-matrixmodels-feedstock/recipe/meta.yaml
index d8fa824..c757cc4 100644
--- a/r-matrixmodels-feedstock/recipe/meta.yaml
+++ b/r-matrixmodels-feedstock/recipe/meta.yaml
@@ -14,13 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/MatrixModels/MatrixModels_{{ version }}.tar.gz
   sha256: fe878e401e697992a480cd146421c3a10fa331f6b37a51bac83b5c1119dcce33
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
+
+
 requirements:
   build:
     - r-base
@@ -32,9 +40,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('MatrixModels')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('MatrixModels')"           # [not win]
     - "\"%R%\" -e \"library('MatrixModels')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://Matrix.R-forge.R-project.org/
   license: GPL (>= 2)
@@ -46,3 +61,29 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: MatrixModels
+# Version: 0.4-1
+# Date: 2015-08-22
+# Title: Modelling with Sparse And Dense Matrices
+# Author: Douglas Bates <bates@stat.wisc.edu> and Martin Maechler <maechler@stat.math.ethz.ch>
+# Maintainer: Martin Maechler <mmaechler+Matrix@gmail.com>
+# Contact: Doug and Martin <Matrix-authors@R-project.org>
+# Description: Modelling with sparse and dense 'Matrix' matrices, using modular prediction and response module classes.
+# Depends: R (>= 3.0.1)
+# Imports: stats, methods, Matrix (>= 1.1-5)
+# Encoding: UTF-8
+# LazyLoad: yes
+# License: GPL (>= 2)
+# URL: http://Matrix.R-forge.R-project.org/
+# NeedsCompilation: no
+# Packaged: 2015-08-22 20:32:14 UTC; maechler
+# Repository: CRAN
+# Date/Publication: 2015-08-22 23:37:45
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-memoise-feedstock contains modified content
diff --git a/r-memoise-feedstock/recipe/meta.yaml b/r-memoise-feedstock/recipe/meta.yaml
index 0679a86..0802a64 100644
--- a/r-memoise-feedstock/recipe/meta.yaml
+++ b/r-memoise-feedstock/recipe/meta.yaml
@@ -14,13 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/memoise/memoise_{{ version }}.tar.gz
   sha256: b276f9452a26aeb79e12dd7227fcc8712832781a42f92d70e86040da0573980c
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: testthat, aws.s3, httr, covr
 requirements:
   build:
     - r-base
@@ -32,12 +40,19 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('memoise')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('memoise')"           # [not win]
     - "\"%R%\" -e \"library('memoise')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/hadley/memoise
-  license: MIT
+  license: MIT + file LICENSE
   summary: Cache the results of a function so that when you call it again with the same arguments
     it returns the pre-computed value.
   license_family: MIT
@@ -46,3 +61,30 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Encoding: UTF-8
+# Package: memoise
+# Title: Memoisation of Functions
+# Version: 1.1.0
+# Authors@R: c( person("Hadley", "Wickham", , "hadley@rstudio.com", role = "aut"), person("Jim", "Hester", , "jim.hester@rstudio.com", role = c("aut", "cre")), person("Kirill", "Muller", , "krlmlr+r@mailbox.org", role = "aut"), person("Daniel", "Cook", , "danielecook@gmail.com", role = "aut"))
+# Description: Cache the results of a function so that when you call it again with the same arguments it returns the pre-computed value.
+# URL: https://github.com/hadley/memoise
+# BugReports: https://github.com/hadley/memoise/issues
+# Imports: digest (>= 0.6.3)
+# Suggests: testthat, aws.s3, httr, covr
+# Additional_repositories: http://cloudyr.github.io/drat
+# License: MIT + file LICENSE
+# RoxygenNote: 6.0.1
+# NeedsCompilation: no
+# Packaged: 2017-04-20 17:35:47 UTC; jhester
+# Author: Hadley Wickham [aut], Jim Hester [aut, cre], Kirill Muller [aut], Daniel Cook [aut]
+# Maintainer: Jim Hester <jim.hester@rstudio.com>
+# Repository: CRAN
+# Date/Publication: 2017-04-21 05:54:22 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-mgcv-feedstock contains modified content
diff --git a/r-mgcv-feedstock/recipe/meta.yaml b/r-mgcv-feedstock/recipe/meta.yaml
index 679c8ce..762c440 100644
--- a/r-mgcv-feedstock/recipe/meta.yaml
+++ b/r-mgcv-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '1.8-17' %}
+{% set version = '1.8-22' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,44 +12,88 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/mgcv_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/mgcv/mgcv_{{ version }}.tar.gz
-  sha256: 8ff3eb28c83ba7c9003005e7fe08028627fb673b9b07c0462b410e45e81042fe
+  sha256: 5a3bb235cc798f617392ef4996f2d502233998b041db2a68377f3cea94b98694
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: splines, parallel, survival, MASS
 requirements:
   build:
     - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('fortran') }}  # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
     - r-matrix
     - r-nlme >=3.1_64
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-matrix
     - r-nlme >=3.1_64
 
 test:
   commands:
-    - $R -e "library('mgcv')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('mgcv')"           # [not win]
     - "\"%R%\" -e \"library('mgcv')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=mgcv
   license: GPL (>= 2)
-  summary: GAMs, GAMMs and other generalized ridge regression with  multiple smoothing parameter
-    estimation by GCV, REML or UBRE/AIC.  Includes a gam() function, a wide variety
-    of smoothers, JAGS  support and distributions beyond the exponential family.
+  summary: Generalized additive (mixed) models, some of their extensions and  other generalized
+    ridge regression with multiple smoothing  parameter estimation by (Restricted) Marginal
+    Likelihood,  Generalized Cross Validation and similar. Includes a gam()  function,
+    a wide variety of smoothers, JAGS support and  distributions beyond the exponential
+    family.
   license_family: GPL3
 
 extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: mgcv
+# Version: 1.8-22
+# Author: Simon Wood <simon.wood@r-project.org>
+# Maintainer: Simon Wood <simon.wood@r-project.org>
+# Title: Mixed GAM Computation Vehicle with Automatic Smoothness Estimation
+# Description: Generalized additive (mixed) models, some of their extensions and  other generalized ridge regression with multiple smoothing  parameter estimation by (Restricted) Marginal Likelihood,  Generalized Cross Validation and similar. Includes a gam()  function, a wide variety of smoothers, JAGS support and  distributions beyond the exponential family.
+# Priority: recommended
+# Depends: R (>= 2.14.0), nlme (>= 3.1-64)
+# Imports: methods, stats, graphics, Matrix
+# Suggests: splines, parallel, survival, MASS
+# LazyLoad: yes
+# ByteCompile: yes
+# License: GPL (>= 2)
+# NeedsCompilation: yes
+# Packaged: 2017-09-18 10:38:41 UTC; sw283
+# Repository: CRAN
+# Date/Publication: 2017-09-19 00:27:56 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-microbenchmark-feedstock contains modified content
diff --git a/r-microbenchmark-feedstock/recipe/meta.yaml b/r-microbenchmark-feedstock/recipe/meta.yaml
index 424affa..8c07f35 100644
--- a/r-microbenchmark-feedstock/recipe/meta.yaml
+++ b/r-microbenchmark-feedstock/recipe/meta.yaml
@@ -14,33 +14,50 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/microbenchmark/microbenchmark_{{ version }}.tar.gz
   sha256: 6feb06819b6a57346f24d7fc3f588ff60a236b5b170aa62383af456c54c8c562
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: multcomp
 requirements:
   build:
     - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
     - r-ggplot2
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-ggplot2
-    - libgcc  # [not win]
 
 test:
   commands:
-    - $R -e "library('microbenchmark')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('microbenchmark')"           # [not win]
     - "\"%R%\" -e \"library('microbenchmark')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=microbenchmark
-  license: BSD_2_clause
+  license: BSD_2_clause + file LICENSE
   summary: Provides infrastructure to accurately measure and compare the execution time of R
     expressions.
   license_family: BSD
@@ -49,3 +66,28 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: microbenchmark
+# Title: Accurate Timing Functions
+# Description: Provides infrastructure to accurately measure and compare the execution time of R expressions.
+# Authors@R: c( person("Olaf", "Mersmann", role=c("aut", "cre"), email="olafm@p-value.net"), person("Claudia", "Beleites", role=c("ctb")), person("Rainer", "Hurling", role=c("ctb")), person("Ari", "Friedman", role=c("ctb")))
+# License: BSD_2_clause + file LICENSE
+# Imports: graphics, ggplot2, stats
+# Suggests: multcomp
+# ByteCompile: yes
+# LazyData: yes
+# Version: 1.4-2.1
+# SystemRequirements: GNU Make
+# NeedsCompilation: yes
+# Packaged: 2015-11-25 10:59:00 UTC; ligges
+# Author: Olaf Mersmann [aut, cre], Claudia Beleites [ctb], Rainer Hurling [ctb], Ari Friedman [ctb]
+# Maintainer: Olaf Mersmann <olafm@p-value.net>
+# Repository: CRAN
+# Date/Publication: 2015-11-25 12:02:00
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-mime-feedstock contains modified content
diff --git a/r-mime-feedstock/recipe/meta.yaml b/r-mime-feedstock/recipe/meta.yaml
index 5dbb389..1476d52 100644
--- a/r-mime-feedstock/recipe/meta.yaml
+++ b/r-mime-feedstock/recipe/meta.yaml
@@ -14,29 +14,43 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/mime/mime_{{ version }}.tar.gz
   sha256: fcc72115afb0eb43237da872754464f37ae9ae097f332ec7984149b5e3a82145
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('mime')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('mime')"           # [not win]
     - "\"%R%\" -e \"library('mime')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/yihui/mime
   license: GPL
@@ -48,3 +62,30 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: mime
+# Type: Package
+# Title: Map Filenames to MIME Types
+# Version: 0.5
+# Date: 2016-07-07
+# Authors@R: c(person("Jeffrey", "Horner", role = "ctb"), person("Yihui", "Xie", role = c("aut", "cre"), email = "xie@yihui.name"))
+# Maintainer: Yihui Xie <xie@yihui.name>
+# Description: Guesses the MIME type from a filename extension using the data derived from /etc/mime.types in UNIX-type systems.
+# Imports: tools
+# License: GPL
+# URL: https://github.com/yihui/mime
+# BugReports: https://github.com/yihui/mime/issues
+# LazyData: TRUE
+# RoxygenNote: 5.0.1
+# NeedsCompilation: yes
+# Packaged: 2016-07-07 04:44:41 UTC; yihui
+# Author: Jeffrey Horner [ctb], Yihui Xie [aut, cre]
+# Repository: CRAN
+# Date/Publication: 2016-07-07 07:38:00
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-miniui-feedstock contains modified content
diff --git a/r-miniui-feedstock/recipe/meta.yaml b/r-miniui-feedstock/recipe/meta.yaml
index d2b142f..ffc4ffd 100644
--- a/r-miniui-feedstock/recipe/meta.yaml
+++ b/r-miniui-feedstock/recipe/meta.yaml
@@ -14,13 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/miniUI/miniUI_{{ version }}.tar.gz
   sha256: 9c92032071ee75ee9c26c4cfd3115877fbcb15de2fd2023109c670784b51cce1
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
@@ -34,9 +42,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('miniUI')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('miniUI')"           # [not win]
     - "\"%R%\" -e \"library('miniUI')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=miniUI
   license: GPL-3
@@ -48,3 +63,27 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: miniUI
+# Type: Package
+# Title: Shiny UI Widgets for Small Screens
+# Version: 0.1.1
+# Authors@R: c( person("Joe", "Cheng", role = c("cre", "aut"), email = "joe@rstudio.com"), person(family = "RStudio", role = "cph") )
+# Description: Provides UI widget and layout functions for writing Shiny apps that work well on small screens.
+# License: GPL-3
+# LazyData: TRUE
+# Imports: shiny (>= 0.13), htmltools (>= 0.3), utils
+# RoxygenNote: 5.0.1
+# NeedsCompilation: no
+# Packaged: 2016-01-15 17:31:47 UTC; jcheng
+# Author: Joe Cheng [cre, aut], RStudio [cph]
+# Maintainer: Joe Cheng <joe@rstudio.com>
+# Repository: CRAN
+# Date/Publication: 2016-01-15 19:33:16
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-minqa-feedstock contains modified content
diff --git a/r-minqa-feedstock/recipe/meta.yaml b/r-minqa-feedstock/recipe/meta.yaml
index 79b67f7..d6a60de 100644
--- a/r-minqa-feedstock/recipe/meta.yaml
+++ b/r-minqa-feedstock/recipe/meta.yaml
@@ -14,30 +14,48 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/minqa/minqa_{{ version }}.tar.gz
   sha256: cfa193a4a9c55cb08f3faf4ab09c11b70412523767f19894e4eafc6e94cccd0c
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
+
+
 requirements:
   build:
     - r-base
+    - {{ compiler('cxx') }}      # [not win]
+    - {{ compiler('fortran') }}  # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
     - r-rcpp >=0.9.10
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-rcpp >=0.9.10
 
 test:
   commands:
-    - $R -e "library('minqa')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('minqa')"           # [not win]
     - "\"%R%\" -e \"library('minqa')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://optimizer.r-forge.r-project.org
   license: GPL-2
@@ -49,3 +67,27 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: minqa
+# Type: Package
+# Title: Derivative-free optimization algorithms by quadratic approximation
+# Version: 1.2.4
+# Author: Douglas Bates, Katharine M. Mullen, John C. Nash, Ravi Varadhan
+# Maintainer: Katharine M. Mullen <katharine.mullen@stat.ucla.edu>
+# Description: Derivative-free optimization by quadratic approximation based on an interface to Fortran implementations by M. J. D. Powell.
+# License: GPL-2
+# URL: http://optimizer.r-forge.r-project.org
+# Imports: Rcpp (>= 0.9.10)
+# LinkingTo: Rcpp
+# SystemRequirements: GNU make
+# NeedsCompilation: yes
+# Packaged: 2014-10-09 07:29:53 UTC; kmm
+# Repository: CRAN
+# Date/Publication: 2014-10-09 15:29:18
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-mlmrev-feedstock contains modified content
diff --git a/r-mlmrev-feedstock/recipe/meta.yaml b/r-mlmrev-feedstock/recipe/meta.yaml
index 37f94f6..e4c04d7 100644
--- a/r-mlmrev-feedstock/recipe/meta.yaml
+++ b/r-mlmrev-feedstock/recipe/meta.yaml
@@ -14,31 +14,46 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/mlmRev/mlmRev_{{ version }}.tar.gz
   sha256: 0796b6be29f162f6fa1bc41ca0a153649315a5a69cc846794787dcb52c547557
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: lattice
 requirements:
   build:
     - r-base
     - r-lme4
+
   run:
     - r-base
     - r-lme4
 
 test:
   commands:
-    - $R -e "library('mlmRev')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('mlmRev')"           # [not win]
     - "\"%R%\" -e \"library('mlmRev')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=mlmRev
   license: GPL (>= 2)
-  summary: |
-    Data and examples from a multilevel modelling software review as well as other well-known
+  summary: Data and examples from a multilevel modelling software review as well as other well-known
     data sets from the multilevel modelling literature.
   license_family: GPL3
 
@@ -46,3 +61,27 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: mlmRev
+# Version: 1.0-6
+# Date: 2014-03-11
+# Title: Examples from Multilevel Modelling Software Review
+# Author: Douglas Bates <bates@stat.wisc.edu>, Martin Maechler <maechler@R-project.org> and Ben Bolker <bolker@mcmaster.ca>
+# Contact: LME4 Authors <lme4-authors@lists.r-forge.r-project.org>
+# Maintainer: Steve Walker <steve.walker@utoronto.ca>
+# Description: Data and examples from a multilevel modelling software review as well as other well-known data sets from the multilevel modelling literature.
+# Depends: lme4, R (>= 2.10)
+# Suggests: lattice
+# LazyData: yes
+# License: GPL (>= 2)
+# Packaged: 2014-03-11 14:11:54 UTC; stevenwalker
+# NeedsCompilation: no
+# Repository: CRAN
+# Date/Publication: 2014-03-11 16:41:16
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-mnormt-feedstock contains modified content
diff --git a/r-mnormt-feedstock/recipe/meta.yaml b/r-mnormt-feedstock/recipe/meta.yaml
index 71caf10..12c1961 100644
--- a/r-mnormt-feedstock/recipe/meta.yaml
+++ b/r-mnormt-feedstock/recipe/meta.yaml
@@ -14,29 +14,43 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/mnormt/mnormt_{{ version }}.tar.gz
   sha256: ff78d5f935278935f1814a69e5a913d93d6dd2ac1b5681ba86b30c6773ef64ac
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{ compiler('fortran') }}  # [not win]
+    - {{native}}toolchain        # [win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('mnormt')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('mnormt')"           # [not win]
     - "\"%R%\" -e \"library('mnormt')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://azzalini.stat.unipd.it/SW/Pkg-mnormt
   license: GPL-2 | GPL-3
@@ -51,3 +65,25 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: mnormt
+# Version: 1.5-5
+# Date: 2016-10-15
+# Title: The Multivariate Normal and t Distributions
+# Author: Fortran code by Alan Genz, R code by Adelchi Azzalini
+# Maintainer: Adelchi Azzalini <adelchi.azzalini@unipd.it>
+# Depends: R (>= 2.2.0)
+# Description: Functions are provided for computing the density and the distribution function of multivariate normal and "t" random variables, and for generating random vectors sampled from these distributions.   Probabilities are computed via non-Monte Carlo methods; different routines  are used in the case d=1, d=2, d>2, if d denotes the number of dimensions.
+# License: GPL-2 | GPL-3
+# URL: http://azzalini.stat.unipd.it/SW/Pkg-mnormt
+# NeedsCompilation: yes
+# Packaged: 2016-10-14 12:26:21 UTC; aa
+# Repository: CRAN
+# Date/Publication: 2016-10-15 13:08:50
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-modelmetrics-feedstock contains modified content
diff --git a/r-modelmetrics-feedstock/recipe/meta.yaml b/r-modelmetrics-feedstock/recipe/meta.yaml
index d70087b..d04ea3c 100644
--- a/r-modelmetrics-feedstock/recipe/meta.yaml
+++ b/r-modelmetrics-feedstock/recipe/meta.yaml
@@ -14,30 +14,45 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/ModelMetrics/ModelMetrics_{{ version }}.tar.gz
   sha256: 487d53fda57da4b29f83a927dda8b1ae6655ab044ee3eec33c38aeb27eed3d85
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: testthat
 requirements:
   build:
     - r-base
+    - {{ compiler('cxx') }}      # [not win]
+    - {{native}}toolchain        # [win]
     - r-rcpp
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-rcpp
 
 test:
   commands:
-    - $R -e "library('ModelMetrics')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('ModelMetrics')"           # [not win]
     - "\"%R%\" -e \"library('ModelMetrics')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=ModelMetrics
   license: GPL (>= 2)
@@ -48,3 +63,30 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: ModelMetrics
+# Title: Rapid Calculation of Model Metrics
+# Version: 1.1.0
+# Authors@R: person("Tyler", "Hunt", email = "thunt@snapfinance.com", role = c("aut", "cre"))
+# Description: Collection of metrics for evaluating models written in C++ using 'Rcpp'.
+# Depends: R (>= 3.2.2)
+# License: GPL (>= 2)
+# Encoding: UTF-8
+# LazyData: true
+# LinkingTo: Rcpp
+# Imports: Rcpp
+# RoxygenNote: 5.0.1
+# Suggests: testthat
+# NeedsCompilation: yes
+# Packaged: 2016-08-26 15:27:50 UTC; snapadmin
+# Author: Tyler Hunt [aut, cre]
+# Maintainer: Tyler Hunt <thunt@snapfinance.com>
+# Repository: CRAN
+# Date/Publication: 2016-08-26 20:35:22
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-modelr-feedstock contains modified content
diff --git a/r-modelr-feedstock/recipe/meta.yaml b/r-modelr-feedstock/recipe/meta.yaml
index d735df0..5f1017a 100644
--- a/r-modelr-feedstock/recipe/meta.yaml
+++ b/r-modelr-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '0.1.0' %}
+{% set version = '0.1.1' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,15 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/modelr_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/modelr/modelr_{{ version }}.tar.gz
-  sha256: 7e8b83961ec3aee322005386e7a2b1afcd7a17959b101005b805cfd0e3d730ef
+  sha256: 25b95198d6aa23e28a0bd97dcdc78264ef168ae403928bff01e1ee81ca021ce7
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: testthat, ggplot2, covr, compiler
 requirements:
   build:
     - r-base
@@ -44,9 +52,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('modelr')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('modelr')"           # [not win]
     - "\"%R%\" -e \"library('modelr')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/hadley/modelr
   license: GPL-3
@@ -58,3 +73,30 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: modelr
+# Version: 0.1.1
+# Title: Modelling Functions that Work with the Pipe
+# Description: Functions for modelling that help you seamlessly integrate modelling into a pipeline of data manipulation and visualisation.
+# Authors@R: c( person("Hadley", "Wickham", , "hadley@rstudio.com", role = c("aut", "cre")), person("RStudio", role = "cph") )
+# License: GPL-3
+# LazyData: true
+# Depends: R (>= 3.1.0)
+# Imports: magrittr, purrr (>= 0.2.2), lazyeval (>= 0.2.0), tibble, broom, dplyr, tidyr (>= 0.6.0)
+# Suggests: testthat, ggplot2, covr, compiler
+# URL: https://github.com/hadley/modelr
+# BugReports: https://github.com/hadley/modelr/issues
+# RoxygenNote: 6.0.1
+# NeedsCompilation: no
+# Packaged: 2017-07-22 21:47:57 UTC; hadley
+# Author: Hadley Wickham [aut, cre], RStudio [cph]
+# Maintainer: Hadley Wickham <hadley@rstudio.com>
+# Repository: CRAN
+# Date/Publication: 2017-07-24 18:28:20 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-modeltools-feedstock contains modified content
diff --git a/r-modeltools-feedstock/recipe/meta.yaml b/r-modeltools-feedstock/recipe/meta.yaml
index cb665d6..ae8ae2f 100644
--- a/r-modeltools-feedstock/recipe/meta.yaml
+++ b/r-modeltools-feedstock/recipe/meta.yaml
@@ -14,14 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/modeltools/modeltools_{{ version }}.tar.gz
   sha256: 07b331475625674ab00e6ddfc479cbdbf0b22d5d237e8c25d83ddf3e0ad1cd7a
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
@@ -31,9 +38,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('modeltools')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('modeltools')"           # [not win]
     - "\"%R%\" -e \"library('modeltools')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=modeltools
   license: GPL-2
@@ -48,3 +62,26 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: modeltools
+# Title: Tools and Classes for Statistical Models
+# Date: 2013-09-02
+# Version: 0.2-21
+# Author: Torsten Hothorn, Friedrich Leisch, Achim Zeileis
+# Maintainer: Torsten Hothorn <Torsten.Hothorn@R-project.org>
+# Description: A collection of tools to deal with statistical models.  The functionality is experimental and the user interface is likely to change in the future. The documentation is rather terse, but packages `coin' and `party' have some working examples. However, if you find the implemented ideas interesting we would be very interested in a discussion of this proposal. Contributions are more than welcome!
+# Depends: stats, stats4
+# Imports: methods
+# LazyLoad: yes
+# License: GPL-2
+# Packaged: 2013-09-02 11:15:31 UTC; hothorn
+# NeedsCompilation: no
+# Repository: CRAN
+# Date/Publication: 2013-09-02 14:29:21
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-multcomp-feedstock contains modified content
diff --git a/r-multcomp-feedstock/recipe/meta.yaml b/r-multcomp-feedstock/recipe/meta.yaml
index 047472b..e48b56b 100644
--- a/r-multcomp-feedstock/recipe/meta.yaml
+++ b/r-multcomp-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '1.4-6' %}
+{% set version = '1.4-7' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,15 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/multcomp_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/multcomp/multcomp_{{ version }}.tar.gz
-  sha256: fe9efbe671416a49819cbdb9137cc218faebcd76e0f170fd1c8d3c84c42eeda2
+  sha256: a3bfb66c71e172af2fff5ca095f761f31ab6b1ade8397de40dd7da0f56aa505a
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: lme4 (>= 0.999375-16), nlme, robustbase, coin, MASS, car, foreign, xtable, lmtest, coxme (>= 2.2-1), SimComp, ISwR
 requirements:
   build:
     - r-base
@@ -29,6 +37,7 @@ requirements:
     - r-mvtnorm >=1.0_3
     - r-sandwich >=2.3_0
     - r-survival >=2.39_4
+
   run:
     - r-base
     - r-th.data >=1.0_2
@@ -39,9 +48,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('multcomp')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('multcomp')"           # [not win]
     - "\"%R%\" -e \"library('multcomp')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://multcomp.R-forge.R-project.org The publishers web page is http://www.crcpress.com/product/isbn/9781584885740
   license: GPL-2
@@ -55,3 +71,29 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: multcomp
+# Title: Simultaneous Inference in General Parametric Models
+# Version: 1.4-7
+# Date: 2017-09-05
+# Authors@R: c(person("Torsten", "Hothorn", role = c("aut", "cre"),  email = "Torsten.Hothorn@R-project.org"),  person("Frank", "Bretz", role = "aut"),  person("Peter", "Westfall", role = "aut"), person("Richard M.", "Heiberger", role = "ctb"), person("Andre", "Schuetzenmeister", role = "ctb"), person("Susan", "Scheibe", role = "ctb"))
+# Description: Simultaneous tests and confidence intervals for general linear hypotheses in parametric models, including  linear, generalized linear, linear mixed effects, and survival models. The package includes demos reproducing analyzes presented in the book "Multiple Comparisons Using R" (Bretz, Hothorn,  Westfall, 2010, CRC Press).
+# Depends: stats, graphics, mvtnorm (>= 1.0-3), survival (>= 2.39-4), TH.data (>= 1.0-2)
+# Imports: sandwich (>= 2.3-0), codetools
+# Suggests: lme4 (>= 0.999375-16), nlme, robustbase, coin, MASS, car, foreign, xtable, lmtest, coxme (>= 2.2-1), SimComp, ISwR
+# URL: http://multcomp.R-forge.R-project.org The publishers web page is http://www.crcpress.com/product/isbn/9781584885740
+# LazyData: yes
+# License: GPL-2
+# NeedsCompilation: no
+# Packaged: 2017-09-07 15:32:23 UTC; hothorn
+# Author: Torsten Hothorn [aut, cre], Frank Bretz [aut], Peter Westfall [aut], Richard M. Heiberger [ctb], Andre Schuetzenmeister [ctb], Susan Scheibe [ctb]
+# Maintainer: Torsten Hothorn <Torsten.Hothorn@R-project.org>
+# Repository: CRAN
+# Date/Publication: 2017-09-07 18:25:39 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-munsell-feedstock contains modified content
diff --git a/r-munsell-feedstock/recipe/meta.yaml b/r-munsell-feedstock/recipe/meta.yaml
index a42af68..e3979a9 100644
--- a/r-munsell-feedstock/recipe/meta.yaml
+++ b/r-munsell-feedstock/recipe/meta.yaml
@@ -14,13 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/munsell/munsell_{{ version }}.tar.gz
   sha256: 397c3c90af966f48eebe8f5d9e40c41b17541f0baaa102eec3ea4faae5a2bd49
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: ggplot2, testthat
 requirements:
   build:
     - r-base
@@ -32,12 +40,19 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('munsell')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('munsell')"           # [not win]
     - "\"%R%\" -e \"library('munsell')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=munsell
-  license: MIT
+  license: MIT + file LICENSE
   summary: Provides easy access to, and manipulation of, the Munsell  colours. Provides a mapping
     between Munsell's  original notation (e.g. "5R 5/10") and hexadecimal strings suitable  for
     use directly in R graphics. Also provides utilities  to explore slices through the
@@ -48,3 +63,26 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: munsell
+# Type: Package
+# Title: Utilities for Using Munsell Colours
+# Version: 0.4.3
+# Author: Charlotte Wickham <cwickham@gmail.com>
+# Maintainer: Charlotte Wickham <cwickham@gmail.com>
+# Description: Provides easy access to, and manipulation of, the Munsell  colours. Provides a mapping between Munsell's  original notation (e.g. "5R 5/10") and hexadecimal strings suitable  for use directly in R graphics. Also provides utilities  to explore slices through the Munsell colour tree, to transform  Munsell colours and display colour palettes.
+# Suggests: ggplot2, testthat
+# Imports: colorspace, methods
+# License: MIT + file LICENSE
+# RoxygenNote: 5.0.1
+# NeedsCompilation: no
+# Packaged: 2016-02-12 19:58:18 UTC; wickhamc
+# Repository: CRAN
+# Date/Publication: 2016-02-13 00:46:00
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-mvtnorm-feedstock contains modified content
diff --git a/r-mvtnorm-feedstock/recipe/meta.yaml b/r-mvtnorm-feedstock/recipe/meta.yaml
index cddb96e..301f1ed 100644
--- a/r-mvtnorm-feedstock/recipe/meta.yaml
+++ b/r-mvtnorm-feedstock/recipe/meta.yaml
@@ -14,29 +14,44 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/mvtnorm/mvtnorm_{{ version }}.tar.gz
   sha256: 4a015b57b645b520151b213eb04b7331598c06442a3f652c7dc149425bd2e444
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('fortran') }}  # [not win]
+    - {{native}}toolchain        # [win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('mvtnorm')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('mvtnorm')"           # [not win]
     - "\"%R%\" -e \"library('mvtnorm')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://mvtnorm.R-forge.R-project.org
   license: GPL-2
@@ -47,3 +62,27 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: mvtnorm
+# Title: Multivariate Normal and t Distributions
+# Version: 1.0-6
+# Date: 2017-03-01
+# Authors@R: c(person("Alan", "Genz", role = "aut"), person("Frank", "Bretz", role = "aut"), person("Tetsuhisa", "Miwa", role = "aut"), person("Xuefei", "Mi", role = "aut"), person("Friedrich", "Leisch", role = "ctb"), person("Fabian", "Scheipl", role = "ctb"), person("Bjoern", "Bornkamp", role = "ctb"), person("Martin", "Maechler", role = "ctb"), person("Torsten", "Hothorn", role = c("aut", "cre"), email = "Torsten.Hothorn@R-project.org"))
+# Description: Computes multivariate normal and t probabilities, quantiles, random deviates and densities.
+# Imports: stats, methods
+# Depends: R(>= 1.9.0)
+# License: GPL-2
+# URL: http://mvtnorm.R-forge.R-project.org
+# NeedsCompilation: yes
+# Packaged: 2017-03-02 13:15:25 UTC; hothorn
+# Author: Alan Genz [aut], Frank Bretz [aut], Tetsuhisa Miwa [aut], Xuefei Mi [aut], Friedrich Leisch [ctb], Fabian Scheipl [ctb], Bjoern Bornkamp [ctb], Martin Maechler [ctb], Torsten Hothorn [aut, cre]
+# Maintainer: Torsten Hothorn <Torsten.Hothorn@R-project.org>
+# Repository: CRAN
+# Date/Publication: 2017-03-02 16:14:20
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-networkd3-feedstock contains modified content
diff --git a/r-networkd3-feedstock/recipe/meta.yaml b/r-networkd3-feedstock/recipe/meta.yaml
index f69e342..8bd5ac4 100644
--- a/r-networkd3-feedstock/recipe/meta.yaml
+++ b/r-networkd3-feedstock/recipe/meta.yaml
@@ -14,20 +14,28 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/networkD3/networkD3_{{ version }}.tar.gz
   sha256: 33b82585f1eec6233303ec14033a703d0b17def441c7a0a67bf7e6764c9c9d0b
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  # igraph is not available for windows
-  skip: true  # [win]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: htmltools (>= 0.2.6), jsonlite,
 requirements:
   build:
     - r-base
     - r-htmlwidgets >=0.3.2
     - r-igraph
     - r-magrittr
+
   run:
     - r-base
     - r-htmlwidgets >=0.3.2
@@ -36,16 +44,53 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('networkD3')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('networkD3')"           # [not win]
     - "\"%R%\" -e \"library('networkD3')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=networkD3
   license: GPL (>= 3)
-  summary: Creates 'D3 JavaScript network, tree, dendrogram, and Sankey graphs from R.
+  summary: Creates 'D3' 'JavaScript' network, tree, dendrogram, and Sankey graphs from 'R'.
   license_family: GPL3
 
 extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: networkD3
+# Type: Package
+# Title: D3 JavaScript Network Graphs from R
+# Description: Creates 'D3' 'JavaScript' network, tree, dendrogram, and Sankey graphs from 'R'.
+# Version: 0.4
+# Date: 2017-03-18
+# Authors@R: c( person("J.J.", "Allaire", role = "aut"), person("Peter", "Ellis", role = "ctb"), person("Christopher", "Gandrud", email = "christopher.gandrud@gmail.com", role = c("aut", "cre")), person("Kevin", "Kuo", role = "ctb"), person("B.W.", "Lewis", role = "ctb"), person("Jonathan", "Owen", role = "ctb"), person("Kenton", "Russell", role = "aut"), person("Jennifer","Rogers", role = "ctb"), person("Charles", "Sese", role = "ctb"), person("CJ", "Yetman", role = "aut") )
+# URL: https://CRAN.R-project.org/package=networkD3
+# BugReports: https://github.com/christophergandrud/networkD3/issues
+# License: GPL (>= 3)
+# Depends: R (>= 3.0.0)
+# Imports: htmlwidgets (>= 0.3.2), igraph, magrittr
+# Suggests: htmltools (>= 0.2.6), jsonlite,
+# Enhances: knitr, shiny
+# LazyData: true
+# RoxygenNote: 6.0.1
+# NeedsCompilation: no
+# Packaged: 2017-03-18 15:59:43 UTC; cgandrud
+# Author: J.J. Allaire [aut], Peter Ellis [ctb], Christopher Gandrud [aut, cre], Kevin Kuo [ctb], B.W. Lewis [ctb], Jonathan Owen [ctb], Kenton Russell [aut], Jennifer Rogers [ctb], Charles Sese [ctb], CJ Yetman [aut]
+# Maintainer: Christopher Gandrud <christopher.gandrud@gmail.com>
+# Repository: CRAN
+# Date/Publication: 2017-03-18 17:31:09 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-nlme-feedstock contains modified content
diff --git a/r-nlme-feedstock/recipe/meta.yaml b/r-nlme-feedstock/recipe/meta.yaml
index 10ffa1f..2da4c08 100644
--- a/r-nlme-feedstock/recipe/meta.yaml
+++ b/r-nlme-feedstock/recipe/meta.yaml
@@ -14,31 +14,48 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/nlme/nlme_{{ version }}.tar.gz
   sha256: 79daa167eb9bc7d8dba506da4b24b5250665b051d4e0a51dfccbb0087fdb564c
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: Hmisc, MASS
 requirements:
   build:
     - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('fortran') }}  # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
     - r-lattice
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-lattice
 
 test:
   commands:
-    - $R -e "library('nlme')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('nlme')"           # [not win]
     - "\"%R%\" -e \"library('nlme')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=nlme
   license: GPL (>= 2) | file LICENCE
@@ -49,3 +66,32 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: nlme
+# Version: 3.1-131
+# Date: 2017-02-06
+# Priority: recommended
+# Title: Linear and Nonlinear Mixed Effects Models
+# Authors@R: c(person("Jose", "Pinheiro", role = "aut", comment = "S version"), person("Douglas", "Bates", role = "aut", comment = "up to 2007"), person("Saikat", "DebRoy", role = "ctb", comment = "up to 2002"), person("Deepayan", "Sarkar", role = "ctb", comment = "up to 2005"), person("EISPACK authors", role = "ctb", comment = "src/rs.f"), person("Siem", "Heisterkamp", role = "ctb", comment = "Author fixed sigma"), person("Bert", "Van Willigen",role = "ctb", comment = "Programmer fixed sigma"), person("R-core", email = "R-core@R-project.org", role = c("aut", "cre")))
+# Description: Fit and compare Gaussian linear and nonlinear mixed-effects models.
+# Depends: R (>= 3.0.2)
+# Imports: graphics, stats, utils, lattice
+# Suggests: Hmisc, MASS
+# LazyData: yes
+# ByteCompile: yes
+# Encoding: UTF-8
+# License: GPL (>= 2) | file LICENCE
+# BugReports: https://bugs.r-project.org
+# NeedsCompilation: yes
+# Packaged: 2017-02-06 10:56:42 UTC; maechler
+# Author: Jose Pinheiro [aut] (S version), Douglas Bates [aut] (up to 2007), Saikat DebRoy [ctb] (up to 2002), Deepayan Sarkar [ctb] (up to 2005), EISPACK authors [ctb] (src/rs.f), Siem Heisterkamp [ctb] (Author fixed sigma), Bert Van Willigen [ctb] (Programmer fixed sigma), R-core [aut, cre]
+# Maintainer: R-core <R-core@R-project.org>
+# Repository: CRAN
+# Date/Publication: 2017-02-06 21:34:58
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-nloptr-feedstock contains modified content
diff --git a/r-nloptr-feedstock/recipe/meta.yaml b/r-nloptr-feedstock/recipe/meta.yaml
index 94ba8dc..29a0854 100644
--- a/r-nloptr-feedstock/recipe/meta.yaml
+++ b/r-nloptr-feedstock/recipe/meta.yaml
@@ -12,33 +12,51 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/nloptr_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/nloptr/nloptr_{{ version }}.tar.gz
-  sha256: 0981a80c46c174a5458bdb8427b2db5428c3daca12ff2c30df7023f143fad7b3
+  sha256: 84225b993cb1ef7854edda9629858662cc8592b0d1344baadea4177486ece1eb
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: testthat (>= 0.8.1)
 requirements:
   build:
     - r-base
-    - {{native}}nlopt
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('cxx') }}      # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}autoconf
+    - {{posix}}automake
+    - {{posix}}pkg-config
+    - {{posix}}make
 
   run:
     - r-base
-    - {{native}}nlopt
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('nloptr')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('nloptr')"           # [not win]
     - "\"%R%\" -e \"library('nloptr')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=nloptr
   license: LGPL-3
@@ -55,3 +73,26 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: nloptr
+# Type: Package
+# Title: R interface to NLopt
+# Version: 1.0.4
+# Date: 2014-08-02
+# Author: Jelmer Ypma <uctpjyy@ucl.ac.uk>, with contributions by Hans W. Borchers <hwborchers@googlemail.com> and  Dirk Eddelbuettel <edd@debian.org>
+# Maintainer: Jelmer Ypma <uctpjyy@ucl.ac.uk>
+# Description:  nloptr is an R interface to NLopt. NLopt is a free/open-source library for nonlinear optimization, providing a common interface for a number of different free optimization routines available online as well as original implementations of various other algorithms. See http://ab-initio.mit.edu/wiki/index.php/NLopt_Introduction for more information on the available algorithms. During installation on Unix the NLopt code is downloaded and compiled from the NLopt website.
+# License: LGPL-3
+# Suggests: testthat (>= 0.8.1)
+# LazyLoad: yes
+# Packaged: 2017-08-22 10:59:12 UTC; ripley
+# NeedsCompilation: yes
+# Repository: CRAN
+# Date/Publication: 2017-08-22 13:02:45
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-nlp-feedstock contains modified content
diff --git a/r-nlp-feedstock/recipe/meta.yaml b/r-nlp-feedstock/recipe/meta.yaml
index 8d4f1d5..24ba22a 100644
--- a/r-nlp-feedstock/recipe/meta.yaml
+++ b/r-nlp-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '0.1-10' %}
+{% set version = '0.1-11' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,16 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/NLP_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/NLP/NLP_{{ version }}.tar.gz
-  sha256: c3a8e5dc473f2a8bb8fa2358d919e43fc026d2e4ebf54cf9bda0fe262ab409cb
+  sha256: 580551a463c14d7186f62e029504f2e95dd9cbbaaef3f979221ddffafb036597
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
@@ -31,9 +38,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('NLP')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('NLP')"           # [not win]
     - "\"%R%\" -e \"library('NLP')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=NLP
   license: GPL-3
@@ -44,3 +58,25 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: NLP
+# Version: 0.1-11
+# Title: Natural Language Processing Infrastructure
+# Authors@R: person("Kurt", "Hornik", role = c("aut", "cre"), email = "Kurt.Hornik@R-project.org")
+# Description: Basic classes and methods for Natural Language Processing.
+# License: GPL-3
+# Imports: utils
+# Depends: R (>= 3.2.0)
+# NeedsCompilation: no
+# Packaged: 2017-08-15 08:54:14 UTC; hornik
+# Author: Kurt Hornik [aut, cre]
+# Maintainer: Kurt Hornik <Kurt.Hornik@R-project.org>
+# Repository: CRAN
+# Date/Publication: 2017-08-15 09:13:23 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-nmf-feedstock contains modified content
diff --git a/r-nmf-feedstock/recipe/meta.yaml b/r-nmf-feedstock/recipe/meta.yaml
index 231f627..47a9a21 100644
--- a/r-nmf-feedstock/recipe/meta.yaml
+++ b/r-nmf-feedstock/recipe/meta.yaml
@@ -14,16 +14,26 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/NMF/NMF_{{ version }}.tar.gz
   sha256: dfccd4b4d6ec90cd7628fd128d2456d792f406cee4d32b13448dcb0ffe4ab056
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: RcppOctave (>= 0.11), fastICA, doMPI, bigmemory (>= 4.2), synchronicity, corpcor, xtable, devtools, knitr, bibtex, RUnit, mail, Biobase
 requirements:
   build:
     - r-base
+    - {{ compiler('cxx') }}      # [not win]
+    - {{native}}toolchain        # [win]
     - r-rcolorbrewer
     - r-cluster
     - r-colorspace
@@ -37,11 +47,10 @@ requirements:
     - r-reshape2
     - r-rngtools >=1.2.3
     - r-stringr >=1.0.0
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-rcolorbrewer
     - r-cluster
     - r-colorspace
@@ -55,13 +64,19 @@ requirements:
     - r-reshape2
     - r-rngtools >=1.2.3
     - r-stringr >=1.0.0
-    - libgcc  # [not win]
 
 test:
   commands:
-    - $R -e "library('NMF')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('NMF')"           # [not win]
     - "\"%R%\" -e \"library('NMF')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://renozao.github.io/NMF
   license: GPL (>= 2)
@@ -76,3 +91,33 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: NMF
+# Type: Package
+# Title: Algorithms and Framework for Nonnegative Matrix Factorization (NMF)
+# Version: 0.20.6
+# Date: 2015-05-25
+# Author: Renaud Gaujoux, Cathal Seoighe
+# Maintainer: Renaud Gaujoux <renaud@tx.technion.ac.il>
+# Description: Provides a framework to perform Non-negative Matrix Factorization (NMF). The package implements a set of already published algorithms and seeding methods, and provides a framework to test, develop and plug new/custom algorithms. Most of the built-in algorithms have been optimized in C++, and the main interface function provides an easy way of performing parallel computations on multicore machines.
+# License: GPL (>= 2)
+# URL: http://renozao.github.io/NMF
+# BugReports: http://github.com/renozao/NMF/issues
+# SCM: github:renozao, r-forge
+# LazyLoad: yes
+# VignetteBuilder: knitr
+# Depends: R (>= 3.0.0), methods, utils, pkgmaker (>= 0.20), registry, rngtools (>= 1.2.3), cluster
+# Imports: graphics, stats, stringr (>= 1.0.0), digest, grid, grDevices, gridBase, colorspace, RColorBrewer, foreach, doParallel, ggplot2, reshape2
+# Suggests: RcppOctave (>= 0.11), fastICA, doMPI, bigmemory (>= 4.2), synchronicity, corpcor, xtable, devtools, knitr, bibtex, RUnit, mail, Biobase
+# Collate: 'rmatrix.R' 'nmf-package.R' 'utils.R' 'versions.R' 'algorithmic.R' 'options.R' 'grid.R' 'colorcode.R' 'atracks.R' 'aheatmap.R' 'NMF-class.R' 'transforms.R' 'Bioc-layer.R' 'NMFstd-class.R' 'NMFOffset-class.R' 'registry.R' 'heatmaps.R' 'NMFns-class.R' 'nmfModel.R' 'fixed-terms.R' 'NMFfit-class.R' 'NMFSet-class.R' 'NMFStrategy-class.R' 'NMFSeed-class.R' 'NMFStrategyFunction-class.R' 'NMFStrategyIterative-class.R' 'NMFStrategyOctave-class.R' 'NMFplots.R' 'registry-algorithms.R' 'algorithms-base.R' 'algorithms-brunet.R' 'algorithms-lnmf.R' 'algorithms-lsnmf.R' 'algorithms-pe-nmf.R' 'algorithms-siNMF.R' 'algorithms-snmf.R' 'data.R' 'extractFeatures.R' 'registry-seed.R' 'parallel.R' 'nmf.R' 'rnmf.R' 'run.R' 'seed-base.R' 'seed-ica.R' 'seed-nndsvd.R' 'setNMFClass.R' 'simulation.R' 'tests.R'
+# Packaged: 2015-05-25 21:34:47 UTC; renaud
+# NeedsCompilation: yes
+# Repository: CRAN
+# Date/Publication: 2015-05-26 08:12:33
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-nnet-feedstock contains modified content
diff --git a/r-nnet-feedstock/recipe/meta.yaml b/r-nnet-feedstock/recipe/meta.yaml
index 5673c11..c5dbd0e 100644
--- a/r-nnet-feedstock/recipe/meta.yaml
+++ b/r-nnet-feedstock/recipe/meta.yaml
@@ -14,29 +14,43 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/nnet/nnet_{{ version }}.tar.gz
   sha256: 2723523e8581cc0e2215435ac773033577a16087a3f41d111757dd96b8c5559d
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: MASS
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('nnet')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('nnet')"           # [not win]
     - "\"%R%\" -e \"library('nnet')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://www.stats.ox.ac.uk/pub/MASS4/
   license: GPL-2 | GPL-3
@@ -48,3 +62,29 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: nnet
+# Priority: recommended
+# Version: 7.3-12
+# Date: 2016-02-02
+# Depends: R (>= 2.14.0), stats, utils
+# Suggests: MASS
+# Authors@R: c(person("Brian", "Ripley", role = c("aut", "cre", "cph"), email = "ripley@stats.ox.ac.uk"), person("William", "Venables", role = "cph"))
+# Description: Software for feed-forward neural networks with a single hidden layer, and for multinomial log-linear models.
+# Title: Feed-Forward Neural Networks and Multinomial Log-Linear Models
+# ByteCompile: yes
+# License: GPL-2 | GPL-3
+# URL: http://www.stats.ox.ac.uk/pub/MASS4/
+# NeedsCompilation: yes
+# Packaged: 2016-02-02 09:12:29 UTC; ripley
+# Author: Brian Ripley [aut, cre, cph], William Venables [cph]
+# Maintainer: Brian Ripley <ripley@stats.ox.ac.uk>
+# Repository: CRAN
+# Date/Publication: 2016-02-02 14:52:39
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-numderiv-feedstock contains modified content
diff --git a/r-numderiv-feedstock/recipe/meta.yaml b/r-numderiv-feedstock/recipe/meta.yaml
index dddcdfc..8c860dd 100644
--- a/r-numderiv-feedstock/recipe/meta.yaml
+++ b/r-numderiv-feedstock/recipe/meta.yaml
@@ -14,14 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/numDeriv/numDeriv_{{ version }}.tar.gz
   sha256: 1b681d273697dc780a3ac5bedabb4a257785732d9ca4ef68e4e4aac8b328d11e
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
@@ -31,9 +38,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('numDeriv')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('numDeriv')"           # [not win]
     - "\"%R%\" -e \"library('numDeriv')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://optimizer.r-forge.r-project.org/
   license: GPL-2
@@ -50,3 +64,27 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: numDeriv
+# Version: 2016.8-1
+# Title: Accurate Numerical Derivatives
+# Description: Methods for calculating (usually) accurate numerical first and second order derivatives. Accurate calculations  are done using 'Richardson''s' extrapolation or, when applicable, a complex step derivative is available. A simple difference  method is also provided. Simple difference is (usually) less accurate but is much quicker than 'Richardson''s' extrapolation and provides a  useful cross-check.  Methods are provided for real scalar and vector valued functions.
+# Depends: R (>= 2.11.1)
+# LazyLoad: yes
+# ByteCompile: yes
+# License: GPL-2
+# Copyright: 2006-2011, Bank of Canada. 2012-2016, Paul Gilbert
+# Author: Paul Gilbert and Ravi Varadhan
+# Maintainer: Paul Gilbert <pgilbert.ttv9z@ncf.ca>
+# URL: http://optimizer.r-forge.r-project.org/
+# NeedsCompilation: no
+# Packaged: 2016-08-21 23:03:32 UTC; paul
+# Repository: CRAN
+# Date/Publication: 2016-08-27 00:25:32
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-nycflights13-feedstock contains modified content
diff --git a/r-nycflights13-feedstock/recipe/meta.yaml b/r-nycflights13-feedstock/recipe/meta.yaml
index 7dc99f3..da1cf6c 100644
--- a/r-nycflights13-feedstock/recipe/meta.yaml
+++ b/r-nycflights13-feedstock/recipe/meta.yaml
@@ -14,14 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/nycflights13/nycflights13_{{ version }}.tar.gz
   sha256: cc13051face739eecf3a5a2654a53d51110b0aa3968cb47b5bfb95053dbfa7a0
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: dplyr
 requirements:
   build:
     - r-base
@@ -33,9 +40,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('nycflights13')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('nycflights13')"           # [not win]
     - "\"%R%\" -e \"library('nycflights13')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://github.com/hadley/nycflights13
   license: CC0
@@ -47,3 +61,30 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: nycflights13
+# Title: Flights that Departed NYC in 2013
+# Version: 0.2.2
+# Authors@R: c( person("Hadley", "Wickham", , "hadley@rstudio.com", c("aut", "cre")), person("RStudio", role = "cph") )
+# Description: Airline on-time data for all flights departing NYC in 2013. Also includes useful 'metadata' on airlines, airports, weather, and planes.
+# License: CC0
+# LazyData: true
+# Depends: R (>= 2.10)
+# Imports: tibble
+# Suggests: dplyr
+# URL: http://github.com/hadley/nycflights13
+# BugReports: https://github.com/hadley/nycflights13/issues
+# RoxygenNote: 5.0.1.9000
+# NeedsCompilation: no
+# Packaged: 2017-01-27 14:44:04 UTC; hadley
+# Author: Hadley Wickham [aut, cre], RStudio [cph]
+# Maintainer: Hadley Wickham <hadley@rstudio.com>
+# Repository: CRAN
+# Date/Publication: 2017-01-27 15:54:20
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-oce-feedstock contains modified content
diff --git a/r-oce-feedstock/recipe/bld.bat b/r-oce-feedstock/recipe/bld.bat
new file mode 100644
index 0000000..8b10c94
--- /dev/null
+++ b/r-oce-feedstock/recipe/bld.bat
@@ -0,0 +1,8 @@
+"%R%" CMD INSTALL --build .
+if errorlevel 1 exit 1
+
+@rem Add more build steps here, if they are necessary.
+
+@rem See
+@rem http://docs.continuum.io/conda/build.html
+@rem for a list of environment variables that are set during the build process.
diff --git a/r-oce-feedstock/recipe/build.sh b/r-oce-feedstock/recipe/build.sh
new file mode 100644
index 0000000..0c37df6
--- /dev/null
+++ b/r-oce-feedstock/recipe/build.sh
@@ -0,0 +1,13 @@
+#!/bin/bash
+
+# R refuses to build packages that mark themselves as Priority: Recommended
+mv DESCRIPTION DESCRIPTION.old
+grep -v '^Priority: ' DESCRIPTION.old > DESCRIPTION
+
+$R CMD INSTALL --build .
+
+# Add more build steps here, if they are necessary.
+
+# See
+# http://docs.continuum.io/conda/build.html
+# for a list of environment variables that are set during the build process.
diff --git a/r-oce-feedstock/recipe/meta.yaml b/r-oce-feedstock/recipe/meta.yaml
index bb07f78..c50ce47 100644
--- a/r-oce-feedstock/recipe/meta.yaml
+++ b/r-oce-feedstock/recipe/meta.yaml
@@ -1,50 +1,103 @@
-{% set version = "0.9-20" %}
+{% set version = '0.9-22' %}
+
+{% set posix = 'm2-' if win else '' %}
+{% set native = 'm2w64-' if win else '' %}
 
 package:
   name: r-oce
-  version: {{ version.replace("-", "_") }}
+  version: {{ version|replace("-", "_") }}
 
 source:
   fn: oce_{{ version }}.tar.gz
   url:
-    - http://cran.r-project.org/src/contrib/oce_{{ version }}.tar.gz
-    - http://cran.r-project.org/src/contrib/Archive/oce/oce_{{ version }}.tar.gz
-  sha256: 85a6444d6f15510162dd17e10c8c9066c26565ec42261c6ace42ac3767ee93dd
+    - https://cran.r-project.org/src/contrib/oce_{{ version }}.tar.gz
+    - https://cran.r-project.org/src/contrib/Archive/oce/oce_{{ version }}.tar.gz
+  sha256: 50da1c1d047b82045f513fd7632e599cdd76d1cdfc1fbff0f813e8922199af73
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: True  # [win32]
-  script: R CMD INSTALL --build .
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
-requirements:  # [not win32]
-  build:  # [not win32]
-    - r-base  # [not win32]
-    - posix  # [win64]
-    - m2w64-toolchain  # [win64]
-    - gcc  # [not win]
-    - r-gsw  # [not win32]
-  run:  # [not win32]
-    - r-base  # [not win32]
-    - r-gsw  # [not win32]
+# Suggests: ocedata, foreign, ncdf4, tiff, akima, RSQLite, DBI, rgdal (>= 1.1-9), R.utils, knitr, rmarkdown, marmap, lubridate
+requirements:
+  build:
+    - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('cxx') }}      # [not win]
+    - {{ compiler('fortran') }}  # [not win]
+    - {{native}}toolchain        # [win]
+    - r-gsw
+    - r-testthat
+
+  run:
+    - r-base
+    - {{native}}gcc-libs         # [win]
+    - r-gsw
+    - r-testthat
 
 test:
   commands:
-    - R -e "library('oce')"  # [not win]
-    - R -e \"library('oce')\"  # [win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('oce')"           # [not win]
+    - "\"%R%\" -e \"library('oce')\""  # [win]
+
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
 
 about:
-  home: http://dankelley.github.com/oce/
-  license: GPL-3.0
-  summary: "Supports the analysis of Oceanographic data, including 'ADCP' measurements,
-    measurements made with 'argo' floats, 'CTD' measurements, sectional data,
-    sea-level time series, coastline and topographic data, etc. Provides specialized
-    functions for calculating seawater properties such as potential temperature
-    in either the 'UNESCO' or 'TEOS-10' equation of state. Produces graphical
-    displays that conform to the conventions of the Oceanographic literature."
+  home: https://dankelley.github.io/oce
+  license: GPL (>= 2)
+  summary: Supports the analysis of Oceanographic data, including 'ADCP' measurements, measurements
+    made with 'argo' floats, 'CTD' measurements, sectional data, sea-level time series,
+    coastline and topographic data, etc. Provides specialized functions for calculating
+    seawater properties such as potential temperature in either the 'UNESCO' or 'TEOS-10'
+    equation of state. Produces graphical displays that conform to the conventions of
+    the Oceanographic literature.
+  license_family: GPL3
 
 extra:
   recipe-maintainers:
     - ocefpaf
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: oce
+# Type: Package
+# Title: Analysis of Oceanographic Data
+# Version: 0.9-22
+# Authors@R: c(person(given="Dan", family="Kelley", email="Dan.Kelley@Dal.Ca", role=c("aut", "cre")), person(given="Clark", family="Richards", email="clark.richards@gmail.com", role=c("aut")), person(given="Chantelle", family="Layton", email="chantelle.layton@dal.ca", role=c("ctb"), comment="curl() coauthor"), person(given="British Geological Survey", role=c("ctb","cph"), comment="magnetic-field subroutine"))
+# Maintainer: Dan Kelley <Dan.Kelley@Dal.Ca>
+# Depends: R (>= 2.15), utils, methods, gsw, testthat
+# Suggests: ocedata, foreign, ncdf4, tiff, akima, RSQLite, DBI, rgdal (>= 1.1-9), R.utils, knitr, rmarkdown, marmap, lubridate
+# BugReports: https://github.com/dankelley/oce/issues
+# Description: Supports the analysis of Oceanographic data, including 'ADCP' measurements, measurements made with 'argo' floats, 'CTD' measurements, sectional data, sea-level time series, coastline and topographic data, etc. Provides specialized functions for calculating seawater properties such as potential temperature in either the 'UNESCO' or 'TEOS-10' equation of state. Produces graphical displays that conform to the conventions of the Oceanographic literature.
+# License: GPL (>= 2)
+# Encoding: UTF-8
+# URL: https://dankelley.github.io/oce
+# LazyData: false
+# RoxygenNote: 6.0.1
+# BuildVignettes: true
+# VignetteBuilder: knitr
+# NeedsCompilation: yes
+# Packaged: 2017-08-28 15:25:38 UTC; kelley
+# Author: Dan Kelley [aut, cre], Clark Richards [aut], Chantelle Layton [ctb] (curl() coauthor), British Geological Survey [ctb, cph] (magnetic-field subroutine)
+# Repository: CRAN
+# Date/Publication: 2017-08-28 18:50:30 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-openssl-feedstock contains modified content
diff --git a/r-openssl-feedstock/recipe/meta.yaml b/r-openssl-feedstock/recipe/meta.yaml
index 97e8198..003d395 100644
--- a/r-openssl-feedstock/recipe/meta.yaml
+++ b/r-openssl-feedstock/recipe/meta.yaml
@@ -14,36 +14,51 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/openssl/openssl_{{ version }}.tar.gz
   sha256: 697d9e86f99270163744538dc3dc4d19d00af89a8570a1d304b110e1d2650e9d
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: testthat, digest, knitr, rmarkdown, jsonlite, jose
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
-    - openssl 1.0.*
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}autoconf
+    - {{posix}}automake
+    - {{posix}}pkg-config
+    - {{posix}}make
 
   run:
     - r-base
-    - {{native}}gcc-libs   # [win]
-    - libgcc               # [not win]
-    - openssl 1.0.*
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('openssl')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('openssl')"           # [not win]
     - "\"%R%\" -e \"library('openssl')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/jeroen/openssl#readme
-  license: MIT
+  license: MIT + file LICENSE
   summary: Bindings to OpenSSL libssl and libcrypto, plus custom SSH pubkey parsers. Supports
     RSA, DSA and EC curves P-256, P-384 and P-521. Cryptographic signatures can either
     be created and verified manually or via x509 certificates. AES can be used in cbc,
@@ -61,3 +76,30 @@ extra:
     - bgruening
     - daler
     - jdblischak
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: openssl
+# Type: Package
+# Title: Toolkit for Encryption, Signatures and Certificates Based on OpenSSL
+# Version: 0.9.7
+# Authors@R: c( person("Jeroen", "Ooms", , "jeroen@berkeley.edu", role = c("cre", "aut")), person("Oliver", "Keyes", role = "ctb"))
+# Description: Bindings to OpenSSL libssl and libcrypto, plus custom SSH pubkey parsers. Supports RSA, DSA and EC curves P-256, P-384 and P-521. Cryptographic signatures can either be created and verified manually or via x509 certificates. AES can be used in cbc, ctr or gcm mode for symmetric encryption; RSA for asymmetric (public key) encryption or EC for Diffie Hellman. High-level envelope functions combine RSA and AES for encrypting arbitrary sized data. Other utilities include key generators, hash functions (md5, sha1, sha256, etc), base64 encoder, a secure random number generator, and 'bignum' math methods for manually performing crypto calculations on large multibyte integers.
+# License: MIT + file LICENSE
+# URL: https://github.com/jeroen/openssl#readme
+# BugReports: https://github.com/jeroen/openssl/issues
+# SystemRequirements: OpenSSL >= 1.0.1
+# VignetteBuilder: knitr
+# Suggests: testthat, digest, knitr, rmarkdown, jsonlite, jose
+# RoxygenNote: 6.0.1
+# NeedsCompilation: yes
+# Packaged: 2017-09-06 15:02:37 UTC; jeroen
+# Author: Jeroen Ooms [cre, aut], Oliver Keyes [ctb]
+# Maintainer: Jeroen Ooms <jeroen@berkeley.edu>
+# Repository: CRAN
+# Date/Publication: 2017-09-06 20:56:56 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-packrat-feedstock contains modified content
diff --git a/r-packrat-feedstock/recipe/meta.yaml b/r-packrat-feedstock/recipe/meta.yaml
index d2175df..46c6215 100644
--- a/r-packrat-feedstock/recipe/meta.yaml
+++ b/r-packrat-feedstock/recipe/meta.yaml
@@ -12,16 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/packrat_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/packrat/packrat_{{ version }}.tar.gz
-
   sha256: a283caf4fda419e6571ae9ca6210a59002a030721feb8a50c0d0787fd6f672f3
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: testthat (>= 0.7), devtools, httr, knitr, rmarkdown
 requirements:
   build:
     - r-base
@@ -31,17 +38,51 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('packrat')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('packrat')"           # [not win]
     - "\"%R%\" -e \"library('packrat')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/rstudio/packrat/
-  license: GPL 2.0
+  license: GPL-2
   summary: Manage the R packages your project depends on in an isolated, portable, and reproducible
     way.
-  license_family: GPL
+  license_family: GPL2
 
 extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: packrat
+# Type: Package
+# Title: A Dependency Management System for Projects and their R Package Dependencies
+# Version: 0.4.8-1
+# Author: Kevin Ushey, Jonathan McPherson, Joe Cheng, Aron Atkins, JJ Allaire
+# Maintainer: Kevin Ushey <kevin@rstudio.com>
+# Description: Manage the R packages your project depends on in an isolated, portable, and reproducible way.
+# License: GPL-2
+# Depends: R (>= 3.0.0)
+# Imports: tools, utils
+# Suggests: testthat (>= 0.7), devtools, httr, knitr, rmarkdown
+# Enhances: BiocInstaller
+# URL: https://github.com/rstudio/packrat/
+# BugReports: https://github.com/rstudio/packrat/issues
+# RoxygenNote: 5.0.1
+# NeedsCompilation: no
+# Packaged: 2016-09-07 21:30:04 UTC; kevin
+# Repository: CRAN
+# Date/Publication: 2016-09-07 23:37:52
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-pbdzmq-feedstock contains modified content
diff --git a/r-pbdzmq-feedstock/recipe/bld.bat b/r-pbdzmq-feedstock/recipe/bld.bat
new file mode 100644
index 0000000..8b10c94
--- /dev/null
+++ b/r-pbdzmq-feedstock/recipe/bld.bat
@@ -0,0 +1,8 @@
+"%R%" CMD INSTALL --build .
+if errorlevel 1 exit 1
+
+@rem Add more build steps here, if they are necessary.
+
+@rem See
+@rem http://docs.continuum.io/conda/build.html
+@rem for a list of environment variables that are set during the build process.
diff --git a/r-pbdzmq-feedstock/recipe/build.sh b/r-pbdzmq-feedstock/recipe/build.sh
new file mode 100644
index 0000000..0c37df6
--- /dev/null
+++ b/r-pbdzmq-feedstock/recipe/build.sh
@@ -0,0 +1,13 @@
+#!/bin/bash
+
+# R refuses to build packages that mark themselves as Priority: Recommended
+mv DESCRIPTION DESCRIPTION.old
+grep -v '^Priority: ' DESCRIPTION.old > DESCRIPTION
+
+$R CMD INSTALL --build .
+
+# Add more build steps here, if they are necessary.
+
+# See
+# http://docs.continuum.io/conda/build.html
+# for a list of environment variables that are set during the build process.
diff --git a/r-pbdzmq-feedstock/recipe/meta.yaml b/r-pbdzmq-feedstock/recipe/meta.yaml
index a9d0a16..8f5c6ec 100644
--- a/r-pbdzmq-feedstock/recipe/meta.yaml
+++ b/r-pbdzmq-feedstock/recipe/meta.yaml
@@ -1,56 +1,109 @@
-{% set version = "0.2-4" %}
+{% set version = '0.2-6' %}
+
+{% set posix = 'm2-' if win else '' %}
+{% set native = 'm2w64-' if win else '' %}
 
 package:
   name: r-pbdzmq
-  version: {{ version.replace("-", "_") }}
+  version: {{ version|replace("-", "_") }}
 
 source:
   fn: pbdZMQ_{{ version }}.tar.gz
   url:
-    - http://cran.r-project.org/src/contrib/pbdZMQ_{{ version }}.tar.gz
-    - http://cran.r-project.org/src/contrib/Archive/pbdZMQ/pbdZMQ_{{ version }}.tar.gz
-  sha256: bfacac88b0d4156c70cf63fc4cb9969a950693996901a4fa3dcd59949ec065f6
+    - https://cran.r-project.org/src/contrib/pbdZMQ_{{ version }}.tar.gz
+    - https://cran.r-project.org/src/contrib/Archive/pbdZMQ/pbdZMQ_{{ version }}.tar.gz
+  sha256: c1f837b72a65f36d532855c8a524e793f1de1f4e8b9b02271635a9523ca217cf
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: True  # [win32]
-  script: R CMD INSTALL --build .
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
-# unlike r-rzmq, this package pre-bakes a whole set of Windows
-# Makefiles, and so it doesn't depend on our shared
-# m2-mingw-w64-zeromq package. This is beyond horrible.
-requirements:  # [not win32]
-  build:  # [not win32]
-    - r-base  # [not win32]
-    - r-r6  # [not win32]
-    - posix  # [win64]
-    - pkg-config  # [not win]
-    - m2w64-toolchain  # [win64]
-    - gcc  # [not win]
-    - zeromq 4.1.*  # [not win]
-  run:  # [not win32]
-    - r-base  # [not win32]
-    - r-r6  # [not win32]
-    - zeromq 4.1.*  # [not win]
+# Suggests: pbdRPC
+requirements:
+  build:
+    - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('cxx') }}      # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}autoconf
+    - {{posix}}automake
+    - {{posix}}pkg-config
+    - {{posix}}make
+    - r-r6
+
+  run:
+    - r-base
+    - {{native}}gcc-libs         # [win]
+    - r-r6
 
 test:
   commands:
-    - R -e "library('pbdZMQ')"  # [not win]
-    - R -e \"library('pbdZMQ')\"  # [win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('pbdZMQ')"           # [not win]
+    - "\"%R%\" -e \"library('pbdZMQ')\""  # [win]
+
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
 
 about:
   home: http://r-pbd.org/
-  license: GPL-3.0
-  summary: "ZeroMQ' is a well-known library for high-performance asynchronous messaging in
+  license: GPL-3
+  summary: '''ZeroMQ'' is a well-known library for high-performance asynchronous messaging in
     scalable, distributed applications.  This package provides high level R wrapper
-    functions to easily utilize 'ZeroMQ'. We mainly focus on interactive client/server
-    programming frameworks. For convenience, a minimal 'ZeroMQ' library (4.1.0 rc1)
-    is shipped with 'pbdZMQ', which can be used if no system installation of 'ZeroMQ'
-    is available.  A few wrapper functions compatible with 'rzmq' are also provided."
+    functions to easily utilize ''ZeroMQ''. We mainly focus on interactive client/server
+    programming frameworks. For convenience, a minimal ''ZeroMQ'' library (4.1.0 rc1)
+    is shipped with ''pbdZMQ'', which can be used if no system installation of ''ZeroMQ''
+    is available.  A few wrapper functions compatible with ''rzmq'' are also provided.'
+
+  license_family: GPL3
 
 extra:
   recipe-maintainers:
     - ocefpaf
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: pbdZMQ
+# Version: 0.2-6
+# Date: 2017-05-14
+# Title: Programming with Big Data -- Interface to ZeroMQ
+# Authors@R: c(person("Wei-Chen", "Chen", role = c("aut", "cre"), email = "wccsnow@gmail.com"), person("Drew", "Schmidt", role = "aut"), person("Christian", "Heckendorf", role = "aut", comment = "file transfer"), person("George", "Ostrouchov", role = "aut", comment = "Mac OSX"), person("Whit", "Armstrong", role = "ctb", comment = "some functions are modified from the rzmq package for backwards compatibility"), person("Brian", "Ripley", role = "ctb", comment = "C code of shellexec, and Solaris"), person("R Core team", role = "ctb", comment = "some functions are modified from the R source code"), person("Philipp", "A.", role = "ctb", comment = "Fedora"))
+# Depends: R (>= 3.2.0)
+# Imports: R6
+# Suggests: pbdRPC
+# Enhances: remoter, pbdMPI
+# LazyLoad: yes
+# LazyData: yes
+# Copyright: See files AUTHORS, COPYING, COPYING.LESSER, and MAINTAINERS in pbdZMQ/inst/zmq_copyright/ for the ZeroMQ source files in src/zmq_src/ which are under GPL-3.
+# Description: 'ZeroMQ' is a well-known library for high-performance asynchronous messaging in scalable, distributed applications.  This package provides high level R wrapper functions to easily utilize 'ZeroMQ'. We mainly focus on interactive client/server programming frameworks. For convenience, a minimal 'ZeroMQ' library (4.1.0 rc1) is shipped with 'pbdZMQ', which can be used if no system installation of 'ZeroMQ' is available.  A few wrapper functions compatible with 'rzmq' are also provided.
+# SystemRequirements: Linux, Mac OSX, and Windows, or ZeroMQ library >= 4.0.4. Solaris 10 needs ZeroMQ library 4.0.7 and OpenCSW.
+# License: GPL-3
+# URL: http://r-pbd.org/
+# BugReports: http://group.r-pbd.org/
+# MailingList: Please send questions and comments regarding pbdR to RBigData@gmail.com
+# NeedsCompilation: yes
+# Maintainer: Wei-Chen Chen <wccsnow@gmail.com>
+# RoxygenNote: 6.0.1
+# Packaged: 2017-05-15 04:21:10 UTC; snoweye
+# Author: Wei-Chen Chen [aut, cre], Drew Schmidt [aut], Christian Heckendorf [aut] (file transfer), George Ostrouchov [aut] (Mac OSX), Whit Armstrong [ctb] (some functions are modified from the rzmq package for backwards compatibility), Brian Ripley [ctb] (C code of shellexec, and Solaris), R Core team [ctb] (some functions are modified from the R source code), Philipp A. [ctb] (Fedora)
+# Repository: CRAN
+# Date/Publication: 2017-05-15 05:29:06 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-pbkrtest-feedstock contains modified content
diff --git a/r-pbkrtest-feedstock/recipe/meta.yaml b/r-pbkrtest-feedstock/recipe/meta.yaml
index 7a1dda6..a7eba11 100644
--- a/r-pbkrtest-feedstock/recipe/meta.yaml
+++ b/r-pbkrtest-feedstock/recipe/meta.yaml
@@ -14,19 +14,28 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/pbkrtest/pbkrtest_{{ version }}.tar.gz
   sha256: 5cbb03ad2b2468720a5a610a0ebda48ac08119a34fca77810a85f554225c23ea
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
     - r-mass
     - r-matrix >=1.2.3
     - r-lme4 >=1.1.10
+
   run:
     - r-base
     - r-mass
@@ -35,9 +44,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('pbkrtest')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('pbkrtest')"           # [not win]
     - "\"%R%\" -e \"library('pbkrtest')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://people.math.aau.dk/~sorenh/software/pbkrtest/
   license: GPL (>= 2)
@@ -51,3 +67,28 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: pbkrtest
+# Version: 0.4-7
+# Title: Parametric Bootstrap and Kenward Roger Based Methods for Mixed Model Comparison
+# Author: Ulrich Halekoh <uhalekoh@health.sdu.dk> Sren Hjsgaard <sorenh@math.aau.dk>
+# Maintainer: Sren Hjsgaard <sorenh@math.aau.dk>
+# Description: Test in mixed effects models. Attention is on mixed effects models as implemented in the 'lme4' package. This package implements a parametric bootstrap test and a Kenward Roger modification of F-tests for linear mixed effects models and a parametric bootstrap test for generalized linear mixed models.
+# URL: http://people.math.aau.dk/~sorenh/software/pbkrtest/
+# Depends: R (>= 3.2.3), lme4 (>= 1.1.10)
+# Imports: Matrix (>= 1.2.3), parallel, MASS, methods
+# Encoding: UTF-8
+# ZipData: no
+# License: GPL (>= 2)
+# RoxygenNote: 5.0.1
+# NeedsCompilation: no
+# Packaged: 2017-03-12 20:05:38 UTC; sorenh
+# Repository: CRAN
+# Date/Publication: 2017-03-15 08:49:25
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-pcapp-feedstock contains modified content
diff --git a/r-pcapp-feedstock/recipe/meta.yaml b/r-pcapp-feedstock/recipe/meta.yaml
index 334737d..e332986 100644
--- a/r-pcapp-feedstock/recipe/meta.yaml
+++ b/r-pcapp-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '1.9-61' %}
+{% set version = '1.9-72' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,39 +12,81 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/pcaPP_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/pcaPP/pcaPP_{{ version }}.tar.gz
-  sha256: 7dc395e159ff1a56135baaf0b1bea40f871c30f6dadd38992f4ccdfc4e88dc29
+  sha256: 58bd0bfb5931aecd734801654bac95f28dab6d30fd043c66c5b719b497104844
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('cxx') }}      # [not win]
+    - {{ compiler('fortran') }}  # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
     - r-mvtnorm
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-mvtnorm
 
 test:
   commands:
-    - $R -e "library('pcaPP')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('pcaPP')"           # [not win]
     - "\"%R%\" -e \"library('pcaPP')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=pcaPP
   license: GPL (>= 3)
-  summary: Provides functions for robust PCA by projection pursuit.
+  summary: Provides functions for robust PCA by projection pursuit. The methods are described
+    in Croux et al. (2006) <doi:10.2139/ssrn.968376>, Croux et al. (2013) <doi:10.1080/00401706.2012.727746>,
+    Todorov and Filzmoser (2013) <doi:10.1007/978-3-642-33042-1_31>.
   license_family: GPL3
 
 extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: pcaPP
+# Version: 1.9-72
+# Date: 2017-06-27
+# Title: Robust PCA by Projection Pursuit
+# Author: Peter Filzmoser, Heinrich Fritz, Klaudius Kalcher
+# Maintainer: Valentin Todorov <valentin.todorov@chello.at>
+# Imports: mvtnorm
+# Description: Provides functions for robust PCA by projection pursuit. The methods are described in Croux et al. (2006) <doi:10.2139/ssrn.968376>, Croux et al. (2013) <doi:10.1080/00401706.2012.727746>, Todorov and Filzmoser (2013) <doi:10.1007/978-3-642-33042-1_31>.
+# License: GPL (>= 3)
+# NeedsCompilation: yes
+# Repository: CRAN
+# Packaged: 2017-06-27 15:36:56 UTC; TodorovV
+# Date/Publication: 2017-06-27 21:53:35 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-perm-feedstock contains modified content
diff --git a/r-perm-feedstock/recipe/meta.yaml b/r-perm-feedstock/recipe/meta.yaml
index 15cbe9a..e6c58fe 100644
--- a/r-perm-feedstock/recipe/meta.yaml
+++ b/r-perm-feedstock/recipe/meta.yaml
@@ -12,12 +12,18 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/perm_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/perm/perm_{{ version }}.tar.gz
-
   sha256: 53c6ee598d5b7b04388915dddc5adf1f8f313b73c3bceb3708db7a632857e500
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
@@ -32,9 +38,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('perm')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('perm')"           # [not win]
     - "\"%R%\" -e \"library('perm')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=perm
   license: GPL
@@ -45,3 +58,26 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: perm
+# Type: Package
+# Title: Exact or Asymptotic permutation tests
+# Version: 1.0-0.0
+# Date: 2010-07-29
+# Author: Michael Fay
+# Maintainer: Michael Fay <mfay@niaid.nih.gov>
+# Depends: R (>= 2.2.1), stats
+# Suggests: coin
+# Description: Perform Exact or Asymptotic permutation tests
+# License: GPL
+# Packaged: 2010-07-29 15:28:08 UTC; faym
+# Repository: CRAN
+# Date/Publication: 2010-07-29 15:46:08
+# NeedsCompilation: no
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-pkgconfig-feedstock contains modified content
diff --git a/r-pkgconfig-feedstock/recipe/meta.yaml b/r-pkgconfig-feedstock/recipe/meta.yaml
index 76409e6..2eecde5 100644
--- a/r-pkgconfig-feedstock/recipe/meta.yaml
+++ b/r-pkgconfig-feedstock/recipe/meta.yaml
@@ -14,13 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/pkgconfig/pkgconfig_{{ version }}.tar.gz
   sha256: ab02b2a4b639ba94dcba882a059fe9cddae5498a4309841f764b62ec46ba5a40
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: covr, testthat, disposables (>= 1.0.3)
 requirements:
   build:
     - r-base
@@ -30,12 +38,19 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('pkgconfig')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('pkgconfig')"           # [not win]
     - "\"%R%\" -e \"library('pkgconfig')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/gaborcsardi/pkgconfig
-  license: MIT
+  license: MIT + file LICENSE
   summary: Set configuration options on a per-package basis. Options set by a given package only
     apply to that package, other packages are unaffected.
   license_family: MIT
@@ -46,3 +61,28 @@ extra:
     - bgruening
     - daler
     - jdblischak
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: pkgconfig
+# Title: Private Configuration for 'R' Packages
+# Version: 2.0.1
+# Author: Gabor Csardi
+# Maintainer: Gabor Csardi <csardi.gabor@gmail.com>
+# Description: Set configuration options on a per-package basis. Options set by a given package only apply to that package, other packages are unaffected.
+# License: MIT + file LICENSE
+# LazyData: true
+# Imports: utils
+# Suggests: covr, testthat, disposables (>= 1.0.3)
+# URL: https://github.com/gaborcsardi/pkgconfig
+# BugReports: https://github.com/gaborcsardi/pkgconfig/issues
+# Encoding: UTF-8
+# NeedsCompilation: no
+# Packaged: 2017-03-21 10:15:26 UTC; gaborcsardi
+# Repository: CRAN
+# Date/Publication: 2017-03-21 15:20:20 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-pkgmaker-feedstock contains modified content
diff --git a/r-pkgmaker-feedstock/recipe/meta.yaml b/r-pkgmaker-feedstock/recipe/meta.yaml
index 39d2a44..df188b1 100644
--- a/r-pkgmaker-feedstock/recipe/meta.yaml
+++ b/r-pkgmaker-feedstock/recipe/meta.yaml
@@ -14,14 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/pkgmaker/pkgmaker_{{ version }}.tar.gz
   sha256: 19ad78c16bd5757333e7abbd5eebcec081deb494c9a4b6eec6919a3747b3386f
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: devtools (>= 0.8), bibtex, RUnit, testthat, knitr, ReportingTools, hwriter, argparse
 requirements:
   build:
     - r-base
@@ -41,14 +48,20 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('pkgmaker')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('pkgmaker')"           # [not win]
     - "\"%R%\" -e \"library('pkgmaker')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://renozao.github.io/pkgmaker
   license: GPL (>= 2)
-  summary: |
-    This package provides some low-level utilities to use for package development. It
+  summary: This package provides some low-level utilities to use for package development. It
     currently provides managers for multiple package specific options and registries,
     vignette, unit test and bibtex related utilities. It serves as a base package for
     packages like NMF, RcppOctave, doRNG, and as an incubator package for other general
@@ -60,3 +73,33 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: pkgmaker
+# Type: Package
+# Title: Package development utilities
+# Version: 0.22
+# Date: 2013-09-17
+# Author: Renaud Gaujoux
+# Maintainer: Renaud Gaujoux <renaud@tx.technion.ac.il>
+# Description: This package provides some low-level utilities to use for package development. It currently provides managers for multiple package specific options and registries, vignette, unit test and bibtex related utilities. It serves as a base package for packages like NMF, RcppOctave, doRNG, and as an incubator package for other general purposes utilities, that will eventually be packaged separately. It is still under heavy development and changes in the interface(s) are more than likely to happen.
+# License: GPL (>= 2)
+# URL: https://renozao.github.io/pkgmaker
+# BugReports: http://github.com/renozao/pkgmaker/issues
+# SCM: github:renozao, r-forge
+# LazyLoad: yes
+# Depends: R (>= 3.0.0), stats, registry
+# Imports: methods, tools, codetools, digest, stringr, xtable, grDevices
+# Suggests: devtools (>= 0.8), bibtex, RUnit, testthat, knitr, ReportingTools, hwriter, argparse
+# Collate: 'utils.R' 'logging.R' 'unitTests.R' 'data.R' 'namespace.R' 'devutils.R' 'package.R' 'options.R' 'is.R' 'registry.R' 'bibtex.R' 'packages.R' 'vignette.R' 'files.R' 'package-extra.R' 'colors.R' 'graphics.R' 'rd.R' 'project.R' 'CLI.R' 'knitr.R' 'repositories.R'
+# VignetteBuilder: knitr
+# Packaged: 2014-05-14 11:49:09 UTC; renaud
+# NeedsCompilation: no
+# Repository: CRAN
+# Date/Publication: 2014-05-14 16:18:51
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-pki-feedstock contains modified content
diff --git a/r-pki-feedstock/recipe/build.sh b/r-pki-feedstock/recipe/build.sh
index 32bc29a..8e0ecd1 100644
--- a/r-pki-feedstock/recipe/build.sh
+++ b/r-pki-feedstock/recipe/build.sh
@@ -1,5 +1,7 @@
 #!/bin/bash
 
+export CPPFLAGS="${CPPFLAGS} -I${PREFIX}/include -L${PREFIX}/lib"
+
 mv DESCRIPTION DESCRIPTION.old
 grep -v '^Priority: ' DESCRIPTION.old > DESCRIPTION
 
diff --git a/r-pki-feedstock/recipe/meta.yaml b/r-pki-feedstock/recipe/meta.yaml
index a37bc4b..9bc24e8 100644
--- a/r-pki-feedstock/recipe/meta.yaml
+++ b/r-pki-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '0.1-3' %}
+{% set version = '0.1-5.1' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,35 +12,57 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/PKI_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/PKI/PKI_{{ version }}.tar.gz
-  sha256: 2fcca38b387ecaae6bbfea795f175adb06a21d653c7fceb972fbeb4826410cf6
+  sha256: d2f76d17388aae17695073e97693174941ce64805e70cfceb57dc2cf569743f7
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}autoconf
+    - {{posix}}automake
+    - {{posix}}pkg-config
+    - {{posix}}make
+    - {{native}}openssl
     - r-base64enc
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
+    - {{native}}openssl
     - r-base64enc
 
 test:
   commands:
-    - $R -e "library('PKI')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('PKI')"           # [not win]
     - "\"%R%\" -e \"library('PKI')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://www.rforge.net/PKI
-  license: GPL-2 (>= 2)
+  license: GPL-2 | GPL-3 | file LICENSE
   summary: PKI functions such as verifying certificates, RSA encription and signing which can
     be used to build PKI infrastructure and perform cryptographic tasks.
   license_family: GPL3
@@ -49,3 +71,26 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: PKI
+# Version: 0.1-5.1
+# Title: Public Key Infrastucture for R Based on the X.509 Standard
+# Author: Simon Urbanek <Simon.Urbanek@r-project.org>
+# Maintainer: Simon Urbanek <Simon.Urbanek@r-project.org>
+# Depends: R (>= 2.9.0), base64enc
+# Enhances: gmp
+# Description: PKI functions such as verifying certificates, RSA encription and signing which can be used to build PKI infrastructure and perform cryptographic tasks.
+# License: GPL-2 | GPL-3 | file LICENSE
+# URL: http://www.rforge.net/PKI
+# SystemRequirements: OpenSSL library and headers
+# NeedsCompilation: yes
+# Packaged: 2017-09-15 18:16:25 UTC; ripley
+# Repository: CRAN
+# Date/Publication: 2017-09-16 10:16:16 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-plm-feedstock contains modified content
diff --git a/r-plm-feedstock/recipe/meta.yaml b/r-plm-feedstock/recipe/meta.yaml
index 636b072..526d6e6 100644
--- a/r-plm-feedstock/recipe/meta.yaml
+++ b/r-plm-feedstock/recipe/meta.yaml
@@ -14,13 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/plm/plm_{{ version }}.tar.gz
   sha256: a2df49b3c1210fbfcaf868c67b0368c5fab9ccc6141a5460341cb5f0fabff103
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: AER
 requirements:
   build:
     - r-base
@@ -33,6 +41,7 @@ requirements:
     - r-nlme
     - r-sandwich
     - r-zoo
+
   run:
     - r-base
     - r-formula >=0.2_0
@@ -47,9 +56,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('plm')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('plm')"           # [not win]
     - "\"%R%\" -e \"library('plm')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://cran.r-project.org/package=plm, https://r-forge.r-project.org/projects/plm/
   license: GPL (>= 2)
@@ -60,3 +76,31 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: plm
+# Version: 1.6-5
+# Date: 2016-12-02
+# Title: Linear Models for Panel Data
+# Authors@R: c(person(given = "Yves",      family = "Croissant",    role = c("aut", "cre"), email = "yves.croissant@univ-reunion.fr"), person(given = "Giovanni",  family = "Millo",        role = "aut", email = "Giovanni.Millo@Generali.com"), person(given = "Kevin",     family = "Tappe",        role = "aut", email = "kevin.tappe@bwi.uni-stuttgart.de"), person(given = "Ott",       family = "Toomet",       role = "ctb", email = "otoomet@gmail.com"), person(given = "Christian", family = "Kleiber",      role = "ctb", email = "Christian.Kleiber@unibas.ch"),  person(given = "Achim",     family = "Zeileis",      role = "ctb", email = "Achim.Zeileis@R-project.org"), person(given = "Arne",      family = "Henningsen",   role = "ctb", email = "arne.henningsen@googlemail.com"), person(given = "Liviu",     family = "Andronic",     role = "ctb", email = "landronimirc@gmail.com"), person(given = "Nina",      family = "Schoenfelder", role = "ctb", email = "nina.schoenfelder@fernuni-hagen.de"))
+# Depends: R (>= 2.12.0), Formula (>= 0.2-0), stats
+# Imports: MASS, bdsmatrix, zoo, nlme, sandwich, lattice, car, lmtest
+# Suggests: AER
+# Description: A set of estimators and tests for panel data.
+# License: GPL (>= 2)
+# URL: https://cran.r-project.org/package=plm, https://r-forge.r-project.org/projects/plm/
+# Author: Yves Croissant [aut, cre], Giovanni Millo [aut], Kevin Tappe [aut], Ott Toomet [ctb], Christian Kleiber [ctb], Achim Zeileis [ctb], Arne Henningsen [ctb], Liviu Andronic [ctb], Nina Schoenfelder [ctb]
+# Maintainer: Yves Croissant <yves.croissant@univ-reunion.fr>
+# Repository: CRAN
+# Repository/R-Forge/Project: plm
+# Repository/R-Forge/Revision: 371
+# Repository/R-Forge/DateTimeStamp: 2016-12-02 10:18:21
+# Date/Publication: 2016-12-02 23:31:22
+# NeedsCompilation: no
+# Packaged: 2016-12-02 13:10:45 UTC; rforge
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-plogr-feedstock contains modified content
diff --git a/r-plogr-feedstock/recipe/meta.yaml b/r-plogr-feedstock/recipe/meta.yaml
index 46a6a8c..1f10555 100644
--- a/r-plogr-feedstock/recipe/meta.yaml
+++ b/r-plogr-feedstock/recipe/meta.yaml
@@ -12,15 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/plogr_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/plogr/plogr_{{ version }}.tar.gz
-
   sha256: 22755c93c76c26252841f43195df31681ea865e91aa89726010bd1b9288ef48f
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: Rcpp
 requirements:
   build:
     - r-base
@@ -30,18 +38,52 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('plogr')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('plogr')"           # [not win]
     - "\"%R%\" -e \"library('plogr')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/krlmlr/plogr#readme
-  license: MIT
-  license_family: MIT
-  license_file: LICENSE
+  license: MIT + file LICENSE
   summary: ' A simple header-only logging library for C++. Add ''LinkingTo: plogr'' to ''DESCRIPTION'',
     and ''#include <plogr.h>'' in your C++ modules to use it.'
 
+  license_family: MIT
+
 extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: plogr
+# Title: The 'plog' C++ Logging Library
+# Version: 0.1-1
+# Date: 2016-09-24
+# Authors@R: c( person("Kirill", "Muller", role = c("aut", "cre"), email = "krlmlr+r@mailbox.org"), person("Sergey", "Podobry", role = "cph", comment = "Author of the bundled plog library"))
+# Description:  A simple header-only logging library for C++. Add 'LinkingTo: plogr' to 'DESCRIPTION', and '#include <plogr.h>' in your C++ modules to use it.
+# Suggests: Rcpp
+# License: MIT + file LICENSE
+# Encoding: UTF-8
+# LazyData: true
+# URL: https://github.com/krlmlr/plogr#readme
+# BugReports: https://github.com/krlmlr/plogr/issues
+# RoxygenNote: 5.0.1.9000
+# NeedsCompilation: no
+# Packaged: 2016-09-24 12:08:49 UTC; muelleki
+# Author: Kirill Muller [aut, cre], Sergey Podobry [cph] (Author of the bundled plog library)
+# Maintainer: Kirill Muller <krlmlr+r@mailbox.org>
+# Repository: CRAN
+# Date/Publication: 2016-09-24 18:23:26
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-plyr-feedstock contains modified content
diff --git a/r-plyr-feedstock/recipe/meta.yaml b/r-plyr-feedstock/recipe/meta.yaml
index 3276f70..fb3ef13 100644
--- a/r-plyr-feedstock/recipe/meta.yaml
+++ b/r-plyr-feedstock/recipe/meta.yaml
@@ -14,39 +14,55 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/plyr/plyr_{{ version }}.tar.gz
   sha256: 60b522d75961007658c9806f8394db27989f1154727cb0bb970062c96ec9eac5
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: abind, testthat, tcltk, foreach, doParallel, itertools, iterators, covr
 requirements:
   build:
     - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('cxx') }}      # [not win]
+    - {{native}}toolchain        # [win]
     - r-rcpp >=0.11.0
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-rcpp >=0.11.0
 
 test:
   commands:
-    - $R -e "library('plyr')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('plyr')"           # [not win]
     - "\"%R%\" -e \"library('plyr')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://had.co.nz/plyr, https://github.com/hadley/plyr
-  license: MIT
-  summary: |
-    A set of tools that solves a common set of problems: you need to break a big problem
+  license: MIT + file LICENSE
+  summary: 'A set of tools that solves a common set of problems: you need to break a big problem
     down into manageable pieces, operate on each piece and then put all the pieces back
     together.  For example, you might want to fit a model to each spatial location or
     time point in your study, summarise data by panels or collapse high-dimensional
-    arrays to simpler summary statistics.'
+    arrays to simpler summary statistics. The development of ''plyr'' has been generously
+    supported by ''Becton Dickinson''.'
 
   license_family: MIT
 
@@ -54,3 +70,31 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: plyr
+# Version: 1.8.4
+# Title: Tools for Splitting, Applying and Combining Data
+# Description: A set of tools that solves a common set of problems: you need to break a big problem down into manageable pieces, operate on each piece and then put all the pieces back together.  For example, you might want to fit a model to each spatial location or time point in your study, summarise data by panels or collapse high-dimensional arrays to simpler summary statistics. The development of 'plyr' has been generously supported by 'Becton Dickinson'.
+# Authors@R: person("Hadley", "Wickham", , "hadley@rstudio.com", c("aut", "cre"))
+# URL: http://had.co.nz/plyr, https://github.com/hadley/plyr
+# BugReports: https://github.com/hadley/plyr/issues
+# Depends: R (>= 3.1.0)
+# Imports: Rcpp (>= 0.11.0)
+# LinkingTo: Rcpp
+# Suggests: abind, testthat, tcltk, foreach, doParallel, itertools, iterators, covr
+# License: MIT + file LICENSE
+# LazyData: true
+# RoxygenNote: 5.0.1
+# NeedsCompilation: yes
+# Packaged: 2016-06-07 19:58:36 UTC; hadley
+# Author: Hadley Wickham [aut, cre]
+# Maintainer: Hadley Wickham <hadley@rstudio.com>
+# Repository: CRAN
+# Date/Publication: 2016-06-08 10:40:15
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-png-feedstock contains modified content
diff --git a/r-png-feedstock/recipe/meta.yaml b/r-png-feedstock/recipe/meta.yaml
index 6a9139e..8daf9ba 100644
--- a/r-png-feedstock/recipe/meta.yaml
+++ b/r-png-feedstock/recipe/meta.yaml
@@ -12,34 +12,50 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/png_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/png/png_{{ version }}.tar.gz
-
   sha256: e269ff968f04384fc9421d17cfc7c10cf7756b11c2d6d126e9776f5aca65553c
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('png')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('png')"           # [not win]
     - "\"%R%\" -e \"library('png')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://www.rforge.net/png/
-  license: GPL2 and GPL3
+  license: GPL-2 | GPL-3
   summary: This package provides an easy and simple way to read, write and display bitmap images
     stored in the PNG format. It can read and write both files and in-memory raw vectors.
   license_family: GPL3
@@ -48,3 +64,25 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: png
+# Version: 0.1-7
+# Title: Read and write PNG images
+# Author: Simon Urbanek <Simon.Urbanek@r-project.org>
+# Maintainer: Simon Urbanek <Simon.Urbanek@r-project.org>
+# Depends: R (>= 2.9.0)
+# Description: This package provides an easy and simple way to read, write and display bitmap images stored in the PNG format. It can read and write both files and in-memory raw vectors.
+# License: GPL-2 | GPL-3
+# SystemRequirements: libpng
+# URL: http://www.rforge.net/png/
+# Packaged: 2013-12-03 20:09:14 UTC; svnuser
+# NeedsCompilation: yes
+# Repository: CRAN
+# Date/Publication: 2013-12-03 22:25:05
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
diff --git a/r-polspline-feedstock/recipe/meta.yaml b/r-polspline-feedstock/recipe/meta.yaml
index d1afad7..1dee2d0 100644
--- a/r-polspline-feedstock/recipe/meta.yaml
+++ b/r-polspline-feedstock/recipe/meta.yaml
@@ -12,9 +12,9 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/polspline_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/polspline/polspline_{{ version }}.tar.gz
+  sha256: fc6bd2e0cca8c13cf099c54fe1e740730e26bb9793d439c395cf16ec8c2b0f32
 
 
-  sha256: fc6bd2e0cca8c13cf099c54fe1e740730e26bb9793d439c395cf16ec8c2b0f32
   # patches:
    # List any patch files here
    # - fix.patch
@@ -32,19 +32,20 @@ build:
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('fortran') }}  # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
 
   run:
     - r-base
-    - {{native}}gcc-libs   # [win]
-    - libgcc               # [not win]
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
     # You can put additional test commands to be run here.
-    - $R -e "library('polspline')"  # [not win]
+    - $R -e "library('polspline')"           # [not win]
     - "\"%R%\" -e \"library('polspline')\""  # [win]
 
   # You can also put a file called run_test.py, run_test.sh, or run_test.bat
@@ -61,6 +62,10 @@ about:
     and co-authors.
   license_family: GPL3
 
+extra:
+  recipe-maintainers:
+    - mingwandroid
+
 # The original CRAN metadata for this package was:
 
 # Package: polspline
Submodule r-praise-feedstock contains modified content
diff --git a/r-praise-feedstock/recipe/meta.yaml b/r-praise-feedstock/recipe/meta.yaml
index c987efb..93dc8e2 100644
--- a/r-praise-feedstock/recipe/meta.yaml
+++ b/r-praise-feedstock/recipe/meta.yaml
@@ -12,19 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/praise_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/praise/praise_{{ version }}.tar.gz
+  sha256: 5c035e74fd05dfa59b03afe0d5f4c53fbf34144e175e90c53d09c6baedf5debd
 
 
-  sha256: 5c035e74fd05dfa59b03afe0d5f4c53fbf34144e175e90c53d09c6baedf5debd
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: True  # [win32]
-
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: testthat
 requirements:
   build:
     - r-base
@@ -34,21 +38,47 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('praise')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('praise')"           # [not win]
     - "\"%R%\" -e \"library('praise')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
 
+  # requires:
+    # Put any additional test requirements here.
 
 about:
   home: https://github.com/gaborcsardi/praise
-  license: MIT
+  license: MIT + file LICENSE
   summary: Build friendly R packages that praise their users if they have done something good,
     or they just need it to feel better.
   license_family: MIT
-  license_file: LICENSE
-
-
 
 extra:
   recipe-maintainers:
     - croth1
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: praise
+# Title: Praise Users
+# Version: 1.0.0
+# Author: Gabor Csardi, Sindre Sorhus
+# Maintainer: Gabor Csardi <csardi.gabor@gmail.com>
+# Description: Build friendly R packages that praise their users if they have done something good, or they just need it to feel better.
+# License: MIT + file LICENSE
+# LazyData: true
+# URL: https://github.com/gaborcsardi/praise
+# BugReports: https://github.com/gaborcsardi/praise/issues
+# Suggests: testthat
+# Collate: 'adjective.R' 'adverb.R' 'exclamation.R' 'verb.R' 'rpackage.R' 'package.R'
+# NeedsCompilation: no
+# Packaged: 2015-08-11 02:01:43 UTC; gaborcsardi
+# Repository: CRAN
+# Date/Publication: 2015-08-11 08:22:28
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-proc-feedstock contains modified content
diff --git a/r-proc-feedstock/recipe/meta.yaml b/r-proc-feedstock/recipe/meta.yaml
index 7e2a038..34f5ae0 100644
--- a/r-proc-feedstock/recipe/meta.yaml
+++ b/r-proc-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '1.9.1' %}
+{% set version = '1.10.0' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,41 +12,55 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/pROC_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/pROC/pROC_{{ version }}.tar.gz
-  sha256: f8c2d1ba4d6cfc9ff62f219528477a41ebcdbb6fe436caa514c5e7a909e685b9
+  sha256: 4039fb71e531b1fcff319fc656657813465bf818476535d02a16f7271c44a066
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: microbenchmark, tcltk, MASS, logcondens, doParallel, testthat
 requirements:
   build:
     - r-base
+    - {{ compiler('cxx') }}      # [not win]
+    - {{native}}toolchain        # [win]
     - r-rcpp >=0.11.1
+    - r-ggplot2
     - r-plyr
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-rcpp >=0.11.1
+    - r-ggplot2
     - r-plyr
-    - libgcc  # [not win]
 
 test:
   commands:
-    - $R -e "library('pROC')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('pROC')"           # [not win]
     - "\"%R%\" -e \"library('pROC')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://expasy.org/tools/pROC/
   license: GPL (>= 3)
-  summary: |
-    Tools for visualizing, smoothing and comparing receiver operating characteristic (ROC
+  summary: Tools for visualizing, smoothing and comparing receiver operating characteristic (ROC
     curves). (Partial) area under the curve (AUC) can be compared with statistical tests
     based on U-statistics or bootstrap. Confidence intervals can be computed for (p)AUC
     or ROC curves.
@@ -56,3 +70,32 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: pROC
+# Type: Package
+# Title: Display and Analyze ROC Curves
+# Version: 1.10.0
+# Date: 2017-06-10
+# Encoding: UTF-8
+# Depends: R (>= 2.14)
+# Imports: plyr, utils, methods, Rcpp (>= 0.11.1), ggplot2,
+# Suggests: microbenchmark, tcltk, MASS, logcondens, doParallel, testthat
+# LinkingTo: Rcpp
+# Authors@R: c(person("Xavier", "Robin", role = c("cre", "aut"),  email = "robin@lindinglab.org"), person("Natacha", "Turck", role = "aut"), person("Alexandre", "Hainard", role = "aut"), person("Natalia", "Tiberti", role = "aut"), person("Frederique", "Lisacek", role = "aut"), person("Jean-Charles", "Sanchez", role = "aut"), person("Markus", "Muller", role = "aut"), person("Stefan", "Siegert", role = "ctb",  comment = "Fast DeLong code",  email = "stefan_siegert@gmx.de"))
+# Description: Tools for visualizing, smoothing and comparing receiver operating characteristic (ROC curves). (Partial) area under the curve (AUC) can be compared with statistical tests based on U-statistics or bootstrap. Confidence intervals can be computed for (p)AUC or ROC curves.
+# License: GPL (>= 3)
+# URL: http://expasy.org/tools/pROC/
+# LazyLoad: yes
+# NeedsCompilation: yes
+# Packaged: 2017-06-10 14:21:52 UTC; xavier
+# Author: Xavier Robin [cre, aut], Natacha Turck [aut], Alexandre Hainard [aut], Natalia Tiberti [aut], Frederique Lisacek [aut], Jean-Charles Sanchez [aut], Markus Muller [aut], Stefan Siegert [ctb] (Fast DeLong code)
+# Maintainer: Xavier Robin <robin@lindinglab.org>
+# Repository: CRAN
+# Date/Publication: 2017-06-10 21:17:21 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
diff --git a/r-processx-feedstock/recipe/bld.bat b/r-processx-feedstock/recipe/bld.bat
index e69de29..8b10c94 100644
--- a/r-processx-feedstock/recipe/bld.bat
+++ b/r-processx-feedstock/recipe/bld.bat
@@ -0,0 +1,8 @@
+"%R%" CMD INSTALL --build .
+if errorlevel 1 exit 1
+
+@rem Add more build steps here, if they are necessary.
+
+@rem See
+@rem http://docs.continuum.io/conda/build.html
+@rem for a list of environment variables that are set during the build process.
diff --git a/r-processx-feedstock/recipe/build.sh b/r-processx-feedstock/recipe/build.sh
index e69de29..0c37df6 100644
--- a/r-processx-feedstock/recipe/build.sh
+++ b/r-processx-feedstock/recipe/build.sh
@@ -0,0 +1,13 @@
+#!/bin/bash
+
+# R refuses to build packages that mark themselves as Priority: Recommended
+mv DESCRIPTION DESCRIPTION.old
+grep -v '^Priority: ' DESCRIPTION.old > DESCRIPTION
+
+$R CMD INSTALL --build .
+
+# Add more build steps here, if they are necessary.
+
+# See
+# http://docs.continuum.io/conda/build.html
+# for a list of environment variables that are set during the build process.
diff --git a/r-processx-feedstock/recipe/meta.yaml b/r-processx-feedstock/recipe/meta.yaml
index e69de29..50b53b3 100644
--- a/r-processx-feedstock/recipe/meta.yaml
+++ b/r-processx-feedstock/recipe/meta.yaml
@@ -0,0 +1,102 @@
+{% set version = '2.0.0.1' %}
+
+{% set posix = 'm2-' if win else '' %}
+{% set native = 'm2w64-' if win else '' %}
+
+package:
+  name: r-processx
+  version: {{ version|replace("-", "_") }}
+
+source:
+  fn: processx_{{ version }}.tar.gz
+  url:
+    - https://cran.r-project.org/src/contrib/processx_{{ version }}.tar.gz
+    - https://cran.r-project.org/src/contrib/Archive/processx/processx_{{ version }}.tar.gz
+  sha256: 8f61b2952d0f2d13c74465bfba174ce11eee559475c2f7b9be6bcb9e2e1d827b
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
+build:
+  # If this is a new build for the same version, increment the build number.
+  number: 0
+
+  # This is required to make R link correctly on Linux.
+  rpaths:
+    - lib/R/lib/
+    - lib/
+
+# Suggests: covr, testthat, withr
+requirements:
+  build:
+    - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
+    - r-r6
+    - r-assertthat
+    - r-crayon
+    - r-debugme
+
+  run:
+    - r-base
+    - {{native}}gcc-libs         # [win]
+    - r-r6
+    - r-assertthat
+    - r-crayon
+    - r-debugme
+
+test:
+  commands:
+    # You can put additional test commands to be run here.
+    - $R -e "library('processx')"           # [not win]
+    - "\"%R%\" -e \"library('processx')\""  # [win]
+
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
+about:
+  home: https://github.com/r-pkgs/processx#readme
+  license: MIT + file LICENSE
+  summary: Portable tools to run system processes in the background. It can check if a background
+    process is running; wait on a background process to finish; get the exit status
+    of finished processes; kill background processes and their children; restart processes.
+    It can read the standard output and error of the processes, using non-blocking connections.
+    'processx' can poll a process for standard output or error, with a timeout. It can
+    also poll several processes at once.
+  license_family: MIT
+
+extra:
+  recipe-maintainers:
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: processx
+# Title: Execute and Control System Processes
+# Version: 2.0.0.1
+# Author: Gabor Csardi
+# Maintainer: Gabor Csardi <csardi.gabor@gmail.com>
+# Description: Portable tools to run system processes in the background. It can check if a background process is running; wait on a background process to finish; get the exit status of finished processes; kill background processes and their children; restart processes. It can read the standard output and error of the processes, using non-blocking connections. 'processx' can poll a process for standard output or error, with a timeout. It can also poll several processes at once.
+# License: MIT + file LICENSE
+# LazyData: true
+# URL: https://github.com/r-pkgs/processx#readme
+# BugReports: https://github.com/r-pkgs/processx/issues
+# RoxygenNote: 6.0.1.9000
+# Suggests: covr, testthat, withr
+# Imports: assertthat, crayon, debugme, R6, utils
+# Encoding: UTF-8
+# NeedsCompilation: yes
+# Packaged: 2017-07-30 06:19:45 UTC; ripley
+# Repository: CRAN
+# Date/Publication: 2017-07-30 06:34:51 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
diff --git a/r-prodlim-feedstock/recipe/bld.bat b/r-prodlim-feedstock/recipe/bld.bat
index e69de29..8b10c94 100644
--- a/r-prodlim-feedstock/recipe/bld.bat
+++ b/r-prodlim-feedstock/recipe/bld.bat
@@ -0,0 +1,8 @@
+"%R%" CMD INSTALL --build .
+if errorlevel 1 exit 1
+
+@rem Add more build steps here, if they are necessary.
+
+@rem See
+@rem http://docs.continuum.io/conda/build.html
+@rem for a list of environment variables that are set during the build process.
diff --git a/r-prodlim-feedstock/recipe/build.sh b/r-prodlim-feedstock/recipe/build.sh
index e69de29..0c37df6 100644
--- a/r-prodlim-feedstock/recipe/build.sh
+++ b/r-prodlim-feedstock/recipe/build.sh
@@ -0,0 +1,13 @@
+#!/bin/bash
+
+# R refuses to build packages that mark themselves as Priority: Recommended
+mv DESCRIPTION DESCRIPTION.old
+grep -v '^Priority: ' DESCRIPTION.old > DESCRIPTION
+
+$R CMD INSTALL --build .
+
+# Add more build steps here, if they are necessary.
+
+# See
+# http://docs.continuum.io/conda/build.html
+# for a list of environment variables that are set during the build process.
diff --git a/r-prodlim-feedstock/recipe/meta.yaml b/r-prodlim-feedstock/recipe/meta.yaml
index e69de29..5801021 100644
--- a/r-prodlim-feedstock/recipe/meta.yaml
+++ b/r-prodlim-feedstock/recipe/meta.yaml
@@ -0,0 +1,93 @@
+{% set version = '1.6.1' %}
+
+{% set posix = 'm2-' if win else '' %}
+{% set native = 'm2w64-' if win else '' %}
+
+package:
+  name: r-prodlim
+  version: {{ version|replace("-", "_") }}
+
+source:
+  fn: prodlim_{{ version }}.tar.gz
+  url:
+    - https://cran.r-project.org/src/contrib/prodlim_{{ version }}.tar.gz
+    - https://cran.r-project.org/src/contrib/Archive/prodlim/prodlim_{{ version }}.tar.gz
+  sha256: 3f2665257118a3db8682731a500b1ae4d669af344672dc2037f987bee3cca154
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
+build:
+  # If this is a new build for the same version, increment the build number.
+  number: 0
+
+  # This is required to make R link correctly on Linux.
+  rpaths:
+    - lib/R/lib/
+    - lib/
+
+
+requirements:
+  build:
+    - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
+    - r-kernsmooth
+    - r-rcpp >=0.11.5
+    - r-lava
+    - r-survival
+
+  run:
+    - r-base
+    - {{native}}gcc-libs         # [win]
+    - r-kernsmooth
+    - r-rcpp >=0.11.5
+    - r-lava
+    - r-survival
+
+test:
+  commands:
+    # You can put additional test commands to be run here.
+    - $R -e "library('prodlim')"           # [not win]
+    - "\"%R%\" -e \"library('prodlim')\""  # [win]
+
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
+about:
+  home: https://CRAN.R-project.org/package=prodlim
+  license: GPL (>= 2)
+  summary: Fast and user friendly implementation of nonparametric estimators for censored event
+    history (survival) analysis. Kaplan-Meier and Aalen-Johansen method.
+  license_family: GPL3
+
+extra:
+  recipe-maintainers:
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: prodlim
+# Title: Product-Limit Estimation for Censored Event History Analysis
+# Version: 1.6.1
+# Author: Thomas A. Gerds
+# Description: Fast and user friendly implementation of nonparametric estimators for censored event history (survival) analysis. Kaplan-Meier and Aalen-Johansen method.
+# Depends: R (>= 2.9.0)
+# Imports: Rcpp (>= 0.11.5), stats, graphics, survival, KernSmooth, lava
+# LinkingTo: Rcpp
+# Maintainer: Thomas A. Gerds <tag@biostat.ku.dk>
+# License: GPL (>= 2)
+# Packaged: 2017-03-06 12:06:02 UTC; tag
+# RoxygenNote: 5.0.1
+# NeedsCompilation: yes
+# Repository: CRAN
+# Date/Publication: 2017-03-06 13:53:09
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-profilemodel-feedstock contains modified content
diff --git a/r-profilemodel-feedstock/recipe/meta.yaml b/r-profilemodel-feedstock/recipe/meta.yaml
index 30162b0..0370fab 100644
--- a/r-profilemodel-feedstock/recipe/meta.yaml
+++ b/r-profilemodel-feedstock/recipe/meta.yaml
@@ -12,16 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/profileModel_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/profileModel/profileModel_{{ version }}.tar.gz
-
   sha256: c94f25d222521867ddc86021bd0b9a6b9f7b055212985652a8511054b24c2bdd
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: MASS, gnm
 requirements:
   build:
     - r-base
@@ -31,18 +38,49 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('profileModel')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('profileModel')"           # [not win]
     - "\"%R%\" -e \"library('profileModel')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://www.ucl.ac.uk/~ucakiko/index.html
-  license: GPL 2.0
+  license: GPL (>= 2)
   summary: profileModel provides tools that can be used to calculate, evaluate, plot and use
     for inference the profiles of *arbitrary* inference functions for *arbitrary* 'glm'-like
     fitted models with linear predictors.
-  license_family: GPL
+  license_family: GPL3
 
 extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: profileModel
+# Type: Package
+# Title: Tools for profiling inference functions for various model classes
+# Version: 0.5-9
+# Date: 2013-08-07
+# Author: Ioannis Kosmidis <i.kosmidis@ucl.ac.uk>
+# Maintainer: Ioannis Kosmidis <i.kosmidis@ucl.ac.uk>
+# URL: http://www.ucl.ac.uk/~ucakiko/index.html
+# Description: profileModel provides tools that can be used to calculate, evaluate, plot and use for inference the profiles of *arbitrary* inference functions for *arbitrary* 'glm'-like fitted models with linear predictors.
+# License: GPL (>= 2)
+# Depends: R (>= 2.6.0)
+# Suggests: MASS, gnm
+# Packaged: 2013-08-14 23:43:03 UTC; yiannis
+# NeedsCompilation: no
+# Repository: CRAN
+# Date/Publication: 2013-08-17 09:49:42
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-proto-feedstock contains modified content
diff --git a/r-proto-feedstock/recipe/meta.yaml b/r-proto-feedstock/recipe/meta.yaml
index 0fe5a18..daad9e8 100644
--- a/r-proto-feedstock/recipe/meta.yaml
+++ b/r-proto-feedstock/recipe/meta.yaml
@@ -12,19 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/proto_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/proto/proto_{{ version }}.tar.gz
+  sha256: 9294d9a3b2b680bb6fac17000bfc97453d77c87ef68cfd609b4c4eb6d11d04d1
 
 
-  sha256: 9294d9a3b2b680bb6fac17000bfc97453d77c87ef68cfd609b4c4eb6d11d04d1
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: True  # [win32]
-
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: testthat, covr
 requirements:
   build:
     - r-base
@@ -34,10 +38,15 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('proto')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('proto')"           # [not win]
     - "\"%R%\" -e \"library('proto')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
 
+  # requires:
+    # Put any additional test requirements here.
 
 about:
   home: https://github.com/hadley/proto
@@ -46,8 +55,30 @@ about:
     than class-based object oriented ideas.
   license_family: GPL2
 
-
-
 extra:
   recipe-maintainers:
     - croth1
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: proto
+# Version: 1.0.0
+# Title: Prototype Object-Based Programming
+# Description: An object oriented system using object-based, also called prototype-based, rather than class-based object oriented ideas.
+# Authors@R: c( person("Hadley", "Wickham", , "hadley@rstudio.com", "cre"), person("Gabor", "Grothendieck", , "ggrothendieck@gmail.com", "aut"), person("Louis", "Kates", role = "aut"), person("Thomas", "Petzoldt", role = "aut") )
+# License: GPL-2
+# URL: https://github.com/hadley/proto
+# BugReports: https://github.com/hadley/proto/issues
+# Suggests: testthat, covr
+# RoxygenNote: 5.0.1.9000
+# NeedsCompilation: no
+# Packaged: 2016-10-28 22:08:08 UTC; hadley
+# Author: Hadley Wickham [cre], Gabor Grothendieck [aut], Louis Kates [aut], Thomas Petzoldt [aut]
+# Maintainer: Hadley Wickham <hadley@rstudio.com>
+# Repository: CRAN
+# Date/Publication: 2016-10-29 00:23:07
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-pryr-feedstock contains modified content
diff --git a/r-pryr-feedstock/recipe/meta.yaml b/r-pryr-feedstock/recipe/meta.yaml
index b39cbe1..ba9d4bd 100644
--- a/r-pryr-feedstock/recipe/meta.yaml
+++ b/r-pryr-feedstock/recipe/meta.yaml
@@ -14,36 +14,49 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/pryr/pryr_{{ version }}.tar.gz
   sha256: 65c2b7c9f96e2aa683ac9cdab3c215fd3039ecd66a2ba7002a8e77881428c3c6
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: testthat (>= 0.8.0)
 requirements:
   build:
     - r-base
-    - r-rcpp
+    - {{ compiler('cxx') }}      # [not win]
+    - {{native}}toolchain        # [win]
+    - r-rcpp >=0.11.0
     - r-codetools
     - r-stringr
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
-    - r-rcpp
+    - {{native}}gcc-libs         # [win]
+    - r-rcpp >=0.11.0
     - r-codetools
     - r-stringr
-    - libgcc  # [not win]
 
 test:
   commands:
-    - $R -e "library('pryr')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('pryr')"           # [not win]
     - "\"%R%\" -e \"library('pryr')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/hadley/pryr
   license: GPL-2
@@ -54,3 +67,29 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: pryr
+# Version: 0.1.2
+# Title: Tools for Computing on the Language
+# Description: Useful tools to pry back the covers of R and understand the language at a deeper level.
+# Authors@R: c( person("Hadley", "Wickham", ,"hadley@rstudio.com", c("aut", "cre")), person("R Core team", role = "ctb", comment = "Some code extracted from base R") )
+# License: GPL-2
+# URL: https://github.com/hadley/pryr
+# BugReports: https://github.com/hadley/pryr/issues
+# LinkingTo: Rcpp
+# Depends: R (>= 3.1.0)
+# Imports: stringr, codetools, methods, Rcpp (>= 0.11.0)
+# Suggests: testthat (>= 0.8.0)
+# NeedsCompilation: yes
+# Packaged: 2015-06-19 11:38:23 UTC; hadley
+# Author: Hadley Wickham [aut, cre], R Core team [ctb] (Some code extracted from base R)
+# Maintainer: Hadley Wickham <hadley@rstudio.com>
+# Repository: CRAN
+# Date/Publication: 2015-06-20 00:35:21
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-pspline-feedstock contains modified content
diff --git a/r-pspline-feedstock/recipe/meta.yaml b/r-pspline-feedstock/recipe/meta.yaml
index 5a4fe48..f8f214f 100644
--- a/r-pspline-feedstock/recipe/meta.yaml
+++ b/r-pspline-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '1.0-17' %}
+{% set version = '1.0-18' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,37 +12,74 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/pspline_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/pspline/pspline_{{ version }}.tar.gz
+  sha256: f71cf293bd5462e510ac5ad16c4a96eda18891a0bfa6447dd881c65845e19ac7
 
-  sha256: 39b859bc5a96784842d7a05c5e081a9ec1d88e8892afd9d1143ba8838d8475d8
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('fortran') }}  # [not win]
+    - {{native}}toolchain        # [win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('pspline')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('pspline')"           # [not win]
     - "\"%R%\" -e \"library('pspline')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=pspline
   license: Unlimited
   summary: Smoothing splines with penalties on order m derivatives.
+  license_family: MIT
 
 extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: pspline
+# Version: 1.0-18
+# Date: 2017-06-12
+# Title: Penalized Smoothing Splines
+# Author: S original by Jim Ramsey <ramsay@psych.mcgill.ca>.  R port by  Brian Ripley <ripley@stats.ox.ac.uk>.
+# Maintainer: Brian Ripley <ripley@stats.ox.ac.uk>
+# Depends: R (>= 2.0.0), stats, graphics
+# Description: Smoothing splines with penalties on order m derivatives.
+# License: Unlimited
+# NeedsCompilation: yes
+# Packaged: 2017-06-12 07:54:59 UTC; ripley
+# Repository: CRAN
+# Date/Publication: 2017-06-12 10:10:40 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-psych-feedstock contains modified content
diff --git a/r-psych-feedstock/recipe/meta.yaml b/r-psych-feedstock/recipe/meta.yaml
index 8f9c2d6..9375780 100644
--- a/r-psych-feedstock/recipe/meta.yaml
+++ b/r-psych-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '1.7.3.21' %}
+{% set version = '1.7.8' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,16 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/psych_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/psych/psych_{{ version }}.tar.gz
-  sha256: 89717bd54c097f417884de4358b4df6f0e016678cf61fca0d01ae97a0054ccea
+  sha256: f328ea602e22b0e7e5f310a8d19f305d8e0a3a86040cdfb64863b68b56d55135
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: GPArotation, lavaan, sem, lme4,Rcsdp, graph, Rgraphviz
 requirements:
   build:
     - r-base
@@ -39,9 +46,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('psych')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('psych')"           # [not win]
     - "\"%R%\" -e \"library('psych')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://personality-project.org/r/psych http://personality-project.org/r/psych-manual.pdf
   license: GPL (>= 2)
@@ -64,3 +78,29 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: psych
+# Version: 1.7.8
+# Date: 2017-08-17
+# Title: Procedures for Psychological, Psychometric, and Personality Research
+# Author: William Revelle <revelle@northwestern.edu>
+# Maintainer: William Revelle <revelle@northwestern.edu>
+# Description: A general purpose toolbox for personality, psychometric theory and experimental psychology.   Functions are primarily for multivariate analysis and scale construction using factor analysis, principal component analysis, cluster analysis and reliability analysis, although others provide basic descriptive statistics. Item Response Theory is done using  factor analysis of tetrachoric and polychoric correlations. Functions for analyzing data at multiple levels include within and between group statistics, including correlations and factor analysis.   Functions for simulating and testing particular item and test structures are included. Several functions  serve as a useful front end for structural equation modeling.  Graphical displays of path diagrams, factor analysis and structural equation models are created using basic graphics. Some of the functions are written to support a book on psychometric theory as well as publications in personality research. For more information, see the <http://personality-project.org/r> web page.
+# License: GPL (>= 2)
+# Imports: mnormt,parallel,stats,graphics,grDevices,methods,foreign,lattice,nlme
+# Suggests: GPArotation, lavaan, sem, lme4,Rcsdp, graph, Rgraphviz
+# LazyData: true
+# ByteCompile: TRUE
+# URL: http://personality-project.org/r/psych http://personality-project.org/r/psych-manual.pdf
+# NeedsCompilation: no
+# Packaged: 2017-09-08 23:08:04 UTC; WR
+# Depends: R (>= 2.10)
+# Repository: CRAN
+# Date/Publication: 2017-09-09 14:12:52 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-purrr-feedstock contains modified content
diff --git a/r-purrr-feedstock/recipe/meta.yaml b/r-purrr-feedstock/recipe/meta.yaml
index b8e032b..db48952 100644
--- a/r-purrr-feedstock/recipe/meta.yaml
+++ b/r-purrr-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '0.2.2' %}
+{% set version = '0.2.3' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,49 +12,85 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/purrr_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/purrr/purrr_{{ version }}.tar.gz
-  sha256: 83756d90075b12d93a5a71aa6e9fc7a5e06eb7a92f2b4e4ad067db310e465a53
+  sha256: aace6ae2f3429d0e453bfa73c02f6c5133aaff5a9cb0f860d29149ba97053e48
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: covr, dplyr (>= 0.4.3), testthat, knitr, rmarkdown
 requirements:
   build:
     - r-base
-    - r-bh
-    - r-rcpp
-    - r-dplyr
-    - r-lazyeval >=0.2.0
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
     - r-magrittr >=1.5
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - r-rlang >=0.1
+    - r-tibble
+
   run:
     - r-base
-    - r-bh
-    - r-rcpp
-    - r-dplyr
-    - r-lazyeval >=0.2.0
+    - {{native}}gcc-libs         # [win]
     - r-magrittr >=1.5
-    - libgcc  # [not win]
+    - r-rlang >=0.1
+    - r-tibble
 
 test:
   commands:
-    - $R -e "library('purrr')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('purrr')"           # [not win]
     - "\"%R%\" -e \"library('purrr')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
-  home: https://github.com/hadley/purrr
-  license: GPL-3
-  summary: Make your pure functions purr with the 'purrr' package. This package completes R's
-    functional programming tools with missing features present in other programming
-    languages.
+  home: http://purrr.tidyverse.org, https://github.com/tidyverse/purrr
+  license: GPL-3 | file LICENSE
+  summary: A complete and consistent functional programming toolkit for R.
   license_family: GPL3
 
 extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: purrr
+# Title: Functional Programming Tools
+# Version: 0.2.3
+# Authors@R: c( person("Lionel", "Henry", , "lionel@rstudio.com", c("aut", "cre")), person("Hadley", "Wickham", , "hadley@rstudio.com", "aut"), person("RStudio", role = c("cph", "fnd")) )
+# Description: A complete and consistent functional programming toolkit for R.
+# URL: http://purrr.tidyverse.org, https://github.com/tidyverse/purrr
+# BugReports: https://github.com/tidyverse/purrr/issues
+# License: GPL-3 | file LICENSE
+# LazyData: true
+# Imports: magrittr (>= 1.5), tibble, rlang (>= 0.1)
+# Suggests: covr, dplyr (>= 0.4.3), testthat, knitr, rmarkdown
+# RoxygenNote: 6.0.1
+# VignetteBuilder: knitr
+# NeedsCompilation: yes
+# Packaged: 2017-08-02 09:31:27 UTC; lionel
+# Author: Lionel Henry [aut, cre], Hadley Wickham [aut], RStudio [cph, fnd]
+# Maintainer: Lionel Henry <lionel@rstudio.com>
+# Repository: CRAN
+# Date/Publication: 2017-08-02 18:31:42 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-quadprog-feedstock contains modified content
diff --git a/r-quadprog-feedstock/recipe/meta.yaml b/r-quadprog-feedstock/recipe/meta.yaml
index ceb88d6..a18f0ec 100644
--- a/r-quadprog-feedstock/recipe/meta.yaml
+++ b/r-quadprog-feedstock/recipe/meta.yaml
@@ -12,44 +12,60 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/quadprog_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/quadprog/quadprog_{{ version }}.tar.gz
-
   sha256: d999620688354c283de5bb305203f5db70271b4dfdc23577cae8c2ba94c9e349
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('fortran') }}  # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
     # You can put additional test commands to be run here.
-    - $R -e "library('quadprog')"  # [not win]
+    - $R -e "library('quadprog')"           # [not win]
     - "\"%R%\" -e \"library('quadprog')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=quadprog
-  license: GPL 2.0
+  license: GPL (>= 2)
   summary: This package contains routines and documentation for solving quadratic programming
     problems.
-  license_family: GPL
+  license_family: GPL3
 
 extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
-
+    - mingwandroid
 
 # The original CRAN metadata for this package was:
 
Submodule r-quantmod-feedstock contains modified content
diff --git a/r-quantmod-feedstock/recipe/meta.yaml b/r-quantmod-feedstock/recipe/meta.yaml
index a5f52e7..a52538a 100644
--- a/r-quantmod-feedstock/recipe/meta.yaml
+++ b/r-quantmod-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '0.4-8' %}
+{% set version = '0.4-11' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,36 +12,50 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/quantmod_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/quantmod/quantmod_{{ version }}.tar.gz
-  sha256: 625c6629336b3b53475065478587a2dce08a8ce9146886ad46f19e395214ed49
+  sha256: c73672966194a41c375fab0d7bb54efc1ec513161247fd720670bbdf3a499c40
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: DBI,RMySQL,RSQLite,timeSeries,XML,downloader,jsonlite(>= 1.1)
 requirements:
   build:
     - r-base
     - r-ttr >=0.2
+    - r-curl
     - r-xts >=0.9_0
     - r-zoo
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+
   run:
     - r-base
     - r-ttr >=0.2
+    - r-curl
     - r-xts >=0.9_0
     - r-zoo
-    - libgcc  # [not win]
 
 test:
   commands:
-    - $R -e "library('quantmod')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('quantmod')"           # [not win]
     - "\"%R%\" -e \"library('quantmod')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://www.quantmod.com https://github.com/joshuaulrich/quantmod
   license: GPL-3
@@ -52,3 +66,31 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: quantmod
+# Type: Package
+# Title: Quantitative Financial Modelling Framework
+# Version: 0.4-11
+# Date: 2017-10-06
+# Authors@R: c( person(given=c("Jeffrey","A."), family="Ryan", role=c("aut","cph")), person(given=c("Joshua","M."), family="Ulrich", role=c("cre","aut"), email="josh.m.ulrich@gmail.com"), person(given="Wouter", family="Thielen", role="ctb"), person(given="Paul", family="Teetor", role="ctb") )
+# Depends: R (>= 3.2.0), xts(>= 0.9-0), zoo, TTR(>= 0.2), methods
+# Imports: curl
+# Suggests: DBI,RMySQL,RSQLite,timeSeries,XML,downloader,jsonlite(>= 1.1)
+# Description: Specify, build, trade, and analyse quantitative financial trading strategies.
+# LazyLoad: yes
+# License: GPL-3
+# URL: http://www.quantmod.com https://github.com/joshuaulrich/quantmod
+# BugReports: https://github.com/joshuaulrich/quantmod/issues
+# NeedsCompilation: yes
+# Packaged: 2017-10-06 17:56:02 UTC; josh
+# Author: Jeffrey A. Ryan [aut, cph], Joshua M. Ulrich [cre, aut], Wouter Thielen [ctb], Paul Teetor [ctb]
+# Maintainer: Joshua M. Ulrich <josh.m.ulrich@gmail.com>
+# Repository: CRAN
+# Date/Publication: 2017-10-06 20:33:46 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-quantreg-feedstock contains modified content
diff --git a/r-quantreg-feedstock/recipe/meta.yaml b/r-quantreg-feedstock/recipe/meta.yaml
index a1363ef..66a69d9 100644
--- a/r-quantreg-feedstock/recipe/meta.yaml
+++ b/r-quantreg-feedstock/recipe/meta.yaml
@@ -14,34 +14,52 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/quantreg/quantreg_{{ version }}.tar.gz
   sha256: 3a12012caeca2c1b688f542c26f21779c2c27734ea81edc1b205344654af39b6
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: tripack, akima, MASS, survival, rgl, logspline, nor1mix, Formula, zoo
 requirements:
   build:
     - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('fortran') }}  # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
     - r-matrix
     - r-matrixmodels
     - r-sparsem
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-matrix
     - r-matrixmodels
     - r-sparsem
-    - libgcc  # [not win]
 
 test:
   commands:
-    - $R -e "library('quantreg')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('quantreg')"           # [not win]
     - "\"%R%\" -e \"library('quantreg')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://www.r-project.org
   license: GPL (>= 2)
@@ -57,3 +75,27 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: quantreg
+# Title: Quantile Regression
+# Description: Estimation and inference methods for models of conditional quantiles:  Linear and nonlinear parametric and non-parametric (total variation penalized) models  for conditional quantiles of a univariate response and several methods for handling censored survival data.  Portfolio selection methods based on expected shortfall risk are also included.
+# Version: 5.33
+# Authors@R: c( person("Roger", "Koenker",  role = c("cre","aut"), email =  "rkoenker@illinois.edu"), person("Stephen", "Portnoy",  role = c("ctb"),  comment = "Contributions to Censored QR code", email =  "sportnoy@illinois.edu"), person(c("Pin", "Tian"), "Ng",  role = c("ctb"),  comment = "Contributions to Sparse QR code", email =  "pin.ng@nau.edu"), person("Achim", "Zeileis",  role = c("ctb"),  comment = "Contributions to dynrq code essentially identical to his dynlm code",  email =  "Achim.Zeileis@uibk.ac.at"), person("Philip", "Grosjean",  role = c("ctb"),  comment = "Contributions to nlrq code", email =  "phgrosjean@sciviews.org"), person(c("Brian", "D"), "Ripley",  role = c("trl","ctb"),  comment = "Initial (2001) R port from S (to my everlasting shame --  how could I have been so slow to adopt R!) and for numerous other  suggestions and useful advice", email =  "ripley@stats.ox.ac.uk"))
+# Maintainer: Roger Koenker <rkoenker@illinois.edu>
+# Repository: CRAN
+# Depends: R (>= 2.6), stats, SparseM
+# Imports: methods, graphics, Matrix, MatrixModels
+# Suggests: tripack, akima, MASS, survival, rgl, logspline, nor1mix, Formula, zoo
+# License: GPL (>= 2)
+# URL: https://www.r-project.org
+# NeedsCompilation: yes
+# Packaged: 2017-04-17 15:11:28 UTC; roger
+# Author: Roger Koenker [cre, aut], Stephen Portnoy [ctb] (Contributions to Censored QR code), Pin Tian Ng [ctb] (Contributions to Sparse QR code), Achim Zeileis [ctb] (Contributions to dynrq code essentially identical to his dynlm code), Philip Grosjean [ctb] (Contributions to nlrq code), Brian D Ripley [trl, ctb] (Initial (2001) R port from S (to my everlasting shame -- how could I have been so slow to adopt R!) and for numerous other suggestions and useful advice)
+# Date/Publication: 2017-04-18 06:41:23 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
diff --git a/r-qvcalc-feedstock/recipe/bld.bat b/r-qvcalc-feedstock/recipe/bld.bat
index e69de29..8b10c94 100644
--- a/r-qvcalc-feedstock/recipe/bld.bat
+++ b/r-qvcalc-feedstock/recipe/bld.bat
@@ -0,0 +1,8 @@
+"%R%" CMD INSTALL --build .
+if errorlevel 1 exit 1
+
+@rem Add more build steps here, if they are necessary.
+
+@rem See
+@rem http://docs.continuum.io/conda/build.html
+@rem for a list of environment variables that are set during the build process.
diff --git a/r-qvcalc-feedstock/recipe/build.sh b/r-qvcalc-feedstock/recipe/build.sh
index e69de29..0c37df6 100644
--- a/r-qvcalc-feedstock/recipe/build.sh
+++ b/r-qvcalc-feedstock/recipe/build.sh
@@ -0,0 +1,13 @@
+#!/bin/bash
+
+# R refuses to build packages that mark themselves as Priority: Recommended
+mv DESCRIPTION DESCRIPTION.old
+grep -v '^Priority: ' DESCRIPTION.old > DESCRIPTION
+
+$R CMD INSTALL --build .
+
+# Add more build steps here, if they are necessary.
+
+# See
+# http://docs.continuum.io/conda/build.html
+# for a list of environment variables that are set during the build process.
diff --git a/r-qvcalc-feedstock/recipe/meta.yaml b/r-qvcalc-feedstock/recipe/meta.yaml
index e69de29..e436787 100644
--- a/r-qvcalc-feedstock/recipe/meta.yaml
+++ b/r-qvcalc-feedstock/recipe/meta.yaml
@@ -0,0 +1,82 @@
+{% set version = '0.9-1' %}
+
+{% set posix = 'm2-' if win else '' %}
+{% set native = 'm2w64-' if win else '' %}
+
+package:
+  name: r-qvcalc
+  version: {{ version|replace("-", "_") }}
+
+source:
+  fn: qvcalc_{{ version }}.tar.gz
+  url:
+    - https://cran.r-project.org/src/contrib/qvcalc_{{ version }}.tar.gz
+    - https://cran.r-project.org/src/contrib/Archive/qvcalc/qvcalc_{{ version }}.tar.gz
+  sha256: de2311ade76dae311b2b5632633003667aa46cdb8703a330b84b67c4b90f87d2
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
+build:
+  # If this is a new build for the same version, increment the build number.
+  number: 0
+
+  # This is required to make R link correctly on Linux.
+  rpaths:
+    - lib/R/lib/
+    - lib/
+
+# Suggests: relimp, MASS
+requirements:
+  build:
+    - r-base
+
+  run:
+    - r-base
+
+test:
+  commands:
+    # You can put additional test commands to be run here.
+    - $R -e "library('qvcalc')"           # [not win]
+    - "\"%R%\" -e \"library('qvcalc')\""  # [win]
+
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
+about:
+  home: https://github.com/DavidFirth/qvcalc
+  license: GPL-2 | GPL-3
+  summary: Functions to compute quasi variances and associated measures of approximation error.
+  license_family: GPL3
+
+extra:
+  recipe-maintainers:
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: qvcalc
+# Version: 0.9-1
+# Date: 2017-09-18
+# Title: Quasi Variances for Factor Effects in Statistical Models
+# Author: David Firth
+# Maintainer: David Firth <d.firth@warwick.ac.uk>
+# URL: https://github.com/DavidFirth/qvcalc
+# Description: Functions to compute quasi variances and associated measures of approximation error.
+# Suggests: relimp, MASS
+# Enhances: psychotools
+# License: GPL-2 | GPL-3
+# RoxygenNote: 6.0.1
+# NeedsCompilation: no
+# Packaged: 2017-09-18 12:24:45 UTC; david
+# Repository: CRAN
+# Date/Publication: 2017-09-19 16:18:11 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-r.methodss3-feedstock contains modified content
diff --git a/r-r.methodss3-feedstock/recipe/meta.yaml b/r-r.methodss3-feedstock/recipe/meta.yaml
index b0fd935..b953cc7 100644
--- a/r-r.methodss3-feedstock/recipe/meta.yaml
+++ b/r-r.methodss3-feedstock/recipe/meta.yaml
@@ -12,16 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/R.methodsS3_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/R.methodsS3/R.methodsS3_{{ version }}.tar.gz
-
   sha256: 44b840399266cd27f8f9157777b4d9d85ab7bd31bfdc143b3fc45079a2d8e687
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
@@ -31,9 +38,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('R.methodsS3')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('R.methodsS3')"           # [not win]
     - "\"%R%\" -e \"library('R.methodsS3')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=R.methodsS3
   license: LGPL (>= 2.1)
@@ -50,3 +64,27 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: R.methodsS3
+# Version: 1.7.1
+# Depends: R (>= 2.13.0)
+# Imports: utils
+# Date: 2016-02-15
+# Title: S3 Methods Simplified
+# Authors@R: c(person("Henrik", "Bengtsson", role=c("aut", "cre", "cph"), email = "henrikb@braju.com"))
+# Author: Henrik Bengtsson [aut, cre, cph]
+# Maintainer: Henrik Bengtsson <henrikb@braju.com>
+# Description: Methods that simplify the setup of S3 generic functions and S3 methods.  Major effort has been made in making definition of methods as simple as possible with a minimum of maintenance for package developers.  For example, generic functions are created automatically, if missing, and naming conflict are automatically solved, if possible.  The method setMethodS3() is a good start for those who in the future may want to migrate to S4.  This is a cross-platform package implemented in pure R that generates standard S3 methods.
+# License: LGPL (>= 2.1)
+# LazyLoad: TRUE
+# NeedsCompilation: no
+# Packaged: 2016-02-16 03:21:35 UTC; hb
+# Repository: CRAN
+# Date/Publication: 2016-02-16 13:48:09
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-r.oo-feedstock contains modified content
diff --git a/r-r.oo-feedstock/recipe/meta.yaml b/r-r.oo-feedstock/recipe/meta.yaml
index fe8eb64..ac9bdbe 100644
--- a/r-r.oo-feedstock/recipe/meta.yaml
+++ b/r-r.oo-feedstock/recipe/meta.yaml
@@ -14,14 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/R.oo/R.oo_{{ version }}.tar.gz
   sha256: 645ceec2f815ed39650ca72db87fb4ece7357857875a4ec73e18bfaf647f431c
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: tools
 requirements:
   build:
     - r-base
@@ -33,9 +40,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('R.oo')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('R.oo')"           # [not win]
     - "\"%R%\" -e \"library('R.oo')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/HenrikBengtsson/R.oo
   license: LGPL (>= 2.1)
@@ -50,3 +64,30 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: R.oo
+# Version: 1.21.0
+# Depends: R (>= 2.13.0), R.methodsS3 (>= 1.7.1)
+# Imports: methods, utils
+# Suggests: tools
+# Date: 2016-10-30
+# Title: R Object-Oriented Programming with or without References
+# Authors@R: c(person("Henrik", "Bengtsson", role=c("aut", "cre", "cph"), email = "henrikb@braju.com"))
+# Author: Henrik Bengtsson [aut, cre, cph]
+# Maintainer: Henrik Bengtsson <henrikb@braju.com>
+# Description: Methods and classes for object-oriented programming in R with or without references.  Large effort has been made on making definition of methods as simple as possible with a minimum of maintenance for package developers.  The package has been developed since 2001 and is now considered very stable.  This is a cross-platform package implemented in pure R that defines standard S3 classes without any tricks.
+# License: LGPL (>= 2.1)
+# LazyLoad: TRUE
+# URL: https://github.com/HenrikBengtsson/R.oo
+# BugReports: https://github.com/HenrikBengtsson/R.oo/issues
+# NeedsCompilation: no
+# Packaged: 2016-10-31 16:46:50 UTC; hb
+# Repository: CRAN
+# Date/Publication: 2016-11-01 00:03:19
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-r.utils-feedstock contains modified content
diff --git a/r-r.utils-feedstock/recipe/meta.yaml b/r-r.utils-feedstock/recipe/meta.yaml
index cfda9a2..88e400c 100644
--- a/r-r.utils-feedstock/recipe/meta.yaml
+++ b/r-r.utils-feedstock/recipe/meta.yaml
@@ -14,14 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/R.utils/R.utils_{{ version }}.tar.gz
   sha256: 1ae1a0f0c6a4972bb2369a2dbccd29ade87d747255ff9cb5a0bd784a5be1039f
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: digest (>= 0.6.10)
 requirements:
   build:
     - r-base
@@ -35,9 +42,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('R.utils')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('R.utils')"           # [not win]
     - "\"%R%\" -e \"library('R.utils')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/HenrikBengtsson/R.utils
   license: LGPL (>= 2.1)
@@ -48,3 +62,30 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: R.utils
+# Version: 2.5.0
+# Depends: R (>= 2.14.0), R.oo (>= 1.20.0)
+# Imports: methods, utils, tools, R.methodsS3 (>= 1.7.1)
+# Suggests: digest (>= 0.6.10)
+# Date: 2016-11-07
+# Title: Various Programming Utilities
+# Authors@R: c(person("Henrik", "Bengtsson", role=c("aut", "cre", "cph"), email = "henrikb@braju.com"))
+# Author: Henrik Bengtsson [aut, cre, cph]
+# Maintainer: Henrik Bengtsson <henrikb@braju.com>
+# Description: Utility functions useful when programming and developing R packages.
+# License: LGPL (>= 2.1)
+# LazyLoad: TRUE
+# URL: https://github.com/HenrikBengtsson/R.utils
+# BugReports: https://github.com/HenrikBengtsson/R.utils/issues
+# NeedsCompilation: no
+# Packaged: 2016-11-07 15:58:57 UTC; hb
+# Repository: CRAN
+# Date/Publication: 2016-11-07 21:07:28
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-r6-feedstock contains modified content
diff --git a/r-r6-feedstock/recipe/bld.bat b/r-r6-feedstock/recipe/bld.bat
new file mode 100644
index 0000000..8b10c94
--- /dev/null
+++ b/r-r6-feedstock/recipe/bld.bat
@@ -0,0 +1,8 @@
+"%R%" CMD INSTALL --build .
+if errorlevel 1 exit 1
+
+@rem Add more build steps here, if they are necessary.
+
+@rem See
+@rem http://docs.continuum.io/conda/build.html
+@rem for a list of environment variables that are set during the build process.
diff --git a/r-r6-feedstock/recipe/build.sh b/r-r6-feedstock/recipe/build.sh
new file mode 100644
index 0000000..0c37df6
--- /dev/null
+++ b/r-r6-feedstock/recipe/build.sh
@@ -0,0 +1,13 @@
+#!/bin/bash
+
+# R refuses to build packages that mark themselves as Priority: Recommended
+mv DESCRIPTION DESCRIPTION.old
+grep -v '^Priority: ' DESCRIPTION.old > DESCRIPTION
+
+$R CMD INSTALL --build .
+
+# Add more build steps here, if they are necessary.
+
+# See
+# http://docs.continuum.io/conda/build.html
+# for a list of environment variables that are set during the build process.
diff --git a/r-r6-feedstock/recipe/meta.yaml b/r-r6-feedstock/recipe/meta.yaml
index 162d8c1..9a99b4b 100644
--- a/r-r6-feedstock/recipe/meta.yaml
+++ b/r-r6-feedstock/recipe/meta.yaml
@@ -1,44 +1,89 @@
-{% set version = "2.2.0" %}
+{% set version = '2.2.2' %}
+
+{% set posix = 'm2-' if win else '' %}
+{% set native = 'm2w64-' if win else '' %}
 
 package:
   name: r-r6
-  version: {{ version }}
+  version: {{ version|replace("-", "_") }}
 
 source:
   fn: R6_{{ version }}.tar.gz
   url:
-    - http://cran.r-project.org/src/contrib/R6_{{ version }}.tar.gz
-    - http://cran.r-project.org/src/contrib/Archive/R6/R6_{{ version }}.tar.gz
-  sha256: 7d7bddc4303fafa99954182ccad938166d681499d4e9ae7001d21b0fd60d25c7
+    - https://cran.r-project.org/src/contrib/R6_{{ version }}.tar.gz
+    - https://cran.r-project.org/src/contrib/Archive/R6/R6_{{ version }}.tar.gz
+  sha256: 087756f471884c3b3ead80215a7cc5636a78b8a956e91675acfe2896426eae8f
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  script: R CMD INSTALL --build .
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: knitr, microbenchmark, pryr, testthat, ggplot2, scales
 requirements:
   build:
     - r-base
+
   run:
     - r-base
 
 test:
   commands:
-    - R -e "library('R6')"  # [not win]
-    - R -e \"library('R6')\"  # [win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('R6')"           # [not win]
+    - "\"%R%\" -e \"library('R6')\""  # [win]
+
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
 
 about:
   home: https://github.com/wch/R6/
-  license: MIT
-  license_file: LICENSE
-  summary: "The R6 package allows the creation of classes with reference semantics, similar to
+  license: MIT + file LICENSE
+  summary: The R6 package allows the creation of classes with reference semantics, similar to
     R's built-in reference classes. Compared to reference classes, R6 classes are simpler
     and lighter-weight, and they are not built on S4 classes so they do not require
     the methods package. These classes allow public and private members, and they support
-    inheritance, even when the classes are defined in different packages."
+    inheritance, even when the classes are defined in different packages.
+  license_family: MIT
 
 extra:
   recipe-maintainers:
     - ocefpaf
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: R6
+# Title: Classes with Reference Semantics
+# Version: 2.2.2
+# Authors@R: person("Winston", "Chang", role = c("aut", "cre"), email = "winston@stdout.org")
+# Description: The R6 package allows the creation of classes with reference semantics, similar to R's built-in reference classes. Compared to reference classes, R6 classes are simpler and lighter-weight, and they are not built on S4 classes so they do not require the methods package. These classes allow public and private members, and they support inheritance, even when the classes are defined in different packages.
+# Depends: R (>= 3.0)
+# Suggests: knitr, microbenchmark, pryr, testthat, ggplot2, scales
+# License: MIT + file LICENSE
+# URL: https://github.com/wch/R6/
+# LazyData: true
+# VignetteBuilder: knitr
+# RoxygenNote: 6.0.1
+# NeedsCompilation: no
+# Packaged: 2017-06-12 18:48:43 UTC; winston
+# Author: Winston Chang [aut, cre]
+# Maintainer: Winston Chang <winston@stdout.org>
+# Repository: CRAN
+# Date/Publication: 2017-06-17 08:22:59 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-randomforest-feedstock contains modified content
diff --git a/r-randomforest-feedstock/recipe/meta.yaml b/r-randomforest-feedstock/recipe/meta.yaml
index 384dfb1..e20162d 100644
--- a/r-randomforest-feedstock/recipe/meta.yaml
+++ b/r-randomforest-feedstock/recipe/meta.yaml
@@ -12,38 +12,76 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/randomForest_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/randomForest/randomForest_{{ version }}.tar.gz
-
   sha256: 6e512f8f88a51c01a918360acba61f1f39432f6e690bc231b7864218558b83c4
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: RColorBrewer, MASS
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('fortran') }}  # [not win]
+    - {{native}}toolchain        # [win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('randomForest')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('randomForest')"           # [not win]
     - "\"%R%\" -e \"library('randomForest')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://www.stat.berkeley.edu/~breiman/RandomForests/
-  license: GPL 2.0
+  license: GPL (>= 2)
   summary: Classification and regression based on a forest of trees using random inputs.
-  license_family: GPL
+  license_family: GPL3
 
 extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: randomForest
+# Title: Breiman and Cutler's Random Forests for Classification and Regression
+# Version: 4.6-12
+# Date: 2015-10-06
+# Depends: R (>= 2.5.0), stats
+# Suggests: RColorBrewer, MASS
+# Author: Fortran original by Leo Breiman and Adele Cutler, R port by Andy Liaw and Matthew Wiener.
+# Description: Classification and regression based on a forest of trees using random inputs.
+# Maintainer: Andy Liaw <andy_liaw@merck.com>
+# License: GPL (>= 2)
+# URL: https://www.stat.berkeley.edu/~breiman/RandomForests/
+# NeedsCompilation: yes
+# Packaged: 2015-10-06 18:28:23 UTC; Liawand
+# Repository: CRAN
+# Date/Publication: 2015-10-07 08:38:34
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-rappdirs-feedstock contains modified content
diff --git a/r-rappdirs-feedstock/recipe/meta.yaml b/r-rappdirs-feedstock/recipe/meta.yaml
index 7e578d9..3a6927a 100644
--- a/r-rappdirs-feedstock/recipe/meta.yaml
+++ b/r-rappdirs-feedstock/recipe/meta.yaml
@@ -12,41 +12,81 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/rappdirs_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/rappdirs/rappdirs_{{ version }}.tar.gz
-
   sha256: 2fd891ec16d28862f65bb57e4a78f77a597930abb59380e757afd8b6c6d3264a
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: testthat, roxygen2
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('rappdirs')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('rappdirs')"           # [not win]
     - "\"%R%\" -e \"library('rappdirs')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/hadley/rappdirs https://github.com/ActiveState/appdirs
-  license: MIT
-  license_family: MIT
-  license_file: LICENSE
+  license: MIT + file LICENSE
   summary: An easy way to determine which directories on the users computer you should use to
     save data, caches and logs. A port of Python's 'Appdirs' (\url{https://github.com/ActiveState/appdirs})
     to R.
+  license_family: MIT
 
 extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: rappdirs
+# Type: Package
+# Title: Application Directories: Determine Where to Save Data, Caches, and Logs
+# Version: 0.3.1
+# Authors@R: c( person("Hadley", "Wickham", email = "hadley@rstudio.com", role = c("trl", "cre", "cph")), person("RStudio", role = "cph"), person("Sridhar", "Ratnakumar", role = "aut"), person("Trent", "Mick", role = "aut"), person("ActiveState", role = "cph", comment = "R/appdir.r, R/cache.r, R/data.r, R/log.r translated from appdirs"), person("Eddy", "Petrisor", role = "ctb"), person("Trevor", "Davis", role = c("trl", "aut")), person("Gabor", "Csardi", role = "ctb"), person("Gregory", "Jefferis", role = "ctb") )
+# Depends: R (>= 2.14), methods
+# Suggests: testthat, roxygen2
+# Description: An easy way to determine which directories on the users computer you should use to save data, caches and logs. A port of Python's 'Appdirs' (\url{https://github.com/ActiveState/appdirs}) to R.
+# BugReports: https://github.com/hadley/rappdirs/issues
+# URL: https://github.com/hadley/rappdirs https://github.com/ActiveState/appdirs
+# License: MIT + file LICENSE
+# Copyright: Original python appdirs module copyright (c) 2010 ActiveState Software Inc. R port copyright Hadley Wickham, RStudio. See file LICENSE for details.
+# RoxygenNote: 5.0.1
+# NeedsCompilation: yes
+# Packaged: 2016-03-28 19:04:15 UTC; hadley
+# Author: Hadley Wickham [trl, cre, cph], RStudio [cph], Sridhar Ratnakumar [aut], Trent Mick [aut], ActiveState [cph] (R/appdir.r, R/cache.r, R/data.r, R/log.r translated from appdirs), Eddy Petrisor [ctb], Trevor Davis [trl, aut], Gabor Csardi [ctb], Gregory Jefferis [ctb]
+# Maintainer: Hadley Wickham <hadley@rstudio.com>
+# Repository: CRAN
+# Date/Publication: 2016-03-28 23:10:10
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-raster-feedstock contains modified content
diff --git a/r-raster-feedstock/recipe/meta.yaml b/r-raster-feedstock/recipe/meta.yaml
index a1662f0..7313904 100644
--- a/r-raster-feedstock/recipe/meta.yaml
+++ b/r-raster-feedstock/recipe/meta.yaml
@@ -14,34 +14,48 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/raster/raster_{{ version }}.tar.gz
   sha256: 47992abd783450513fbce3770298cc257030bf0eb77e42aa3a4b3924b16264cc
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: rgdal (>= 0.9-1), rgeos (>= 0.3-8), ncdf4, igraph, tcltk, parallel, rasterVis
 requirements:
   build:
     - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('cxx') }}      # [not win]
+    - {{native}}toolchain        # [win]
     - r-rcpp
     - r-sp >=1.2_0
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-rcpp
     - r-sp >=1.2_0
-    - libgcc  # [not win]
 
 test:
   commands:
-    - $R -e "library('raster')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('raster')"           # [not win]
     - "\"%R%\" -e \"library('raster')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://cran.r-project.org/package=raster
   license: GPL (>= 3)
@@ -54,3 +68,31 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: raster
+# Type: Package
+# Title: Geographic Data Analysis and Modeling
+# Version: 2.5-8
+# Date: 2016-05-31
+# Depends: methods, sp (>= 1.2-0), R (>= 3.0.0)
+# Suggests: rgdal (>= 0.9-1), rgeos (>= 0.3-8), ncdf4, igraph, tcltk, parallel, rasterVis
+# LinkingTo: Rcpp
+# Imports: Rcpp
+# Authors@R: c( person("Robert J.", "Hijmans", role = c("cre", "aut"),  email = "r.hijmans@gmail.com"), person("Jacob", "van Etten", role = "ctb"), person("Joe", "Cheng", role = "ctb"), person("Matteo", "Mattiuzzi", role = "ctb"), person("Michael", "Sumner", role = "ctb"), person("Jonathan A.", "Greenberg", role = "ctb"), person("Oscar", "Perpinan Lamigueiro", role = "ctb"), person("Andrew", "Bevan", role = "ctb"), person("Etienne B.", "Racine", role = "ctb"), person("Ashton", "Shortridge", role = "ctb"))
+# Description: Reading, writing, manipulating, analyzing and modeling of gridded spatial data. The package implements basic and high-level functions. Processing of very large files is supported.
+# License: GPL (>= 3)
+# URL: http://cran.r-project.org/package=raster
+# ByteCompile: TRUE
+# NeedsCompilation: yes
+# Packaged: 2016-05-31 23:09:23 UTC; rhijmans
+# Author: Robert J. Hijmans [cre, aut], Jacob van Etten [ctb], Joe Cheng [ctb], Matteo Mattiuzzi [ctb], Michael Sumner [ctb], Jonathan A. Greenberg [ctb], Oscar Perpinan Lamigueiro [ctb], Andrew Bevan [ctb], Etienne B. Racine [ctb], Ashton Shortridge [ctb]
+# Maintainer: Robert J. Hijmans <r.hijmans@gmail.com>
+# Repository: CRAN
+# Date/Publication: 2016-06-02 18:31:24
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-rbokeh-feedstock contains modified content
diff --git a/r-rbokeh-feedstock/recipe/meta.yaml b/r-rbokeh-feedstock/recipe/meta.yaml
index f4afc10..4e83bf1 100644
--- a/r-rbokeh-feedstock/recipe/meta.yaml
+++ b/r-rbokeh-feedstock/recipe/meta.yaml
@@ -14,14 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/rbokeh/rbokeh_{{ version }}.tar.gz
   sha256: 499c3224a7dcaeb4bb60fd645b3ef528a20e59437747a073713941b80cbcebd2
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  # r-maps is not available under windows
-  skip: true  # [win]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: testthat, data.table, lattice, lintr, roxygen2 (>= 5.0.0), knitr
 requirements:
   build:
     - r-base
@@ -36,6 +43,7 @@ requirements:
     - r-maps
     - r-pryr
     - r-scales
+
   run:
     - r-base
     - r-digest
@@ -52,12 +60,19 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('rbokeh')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('rbokeh')"           # [not win]
     - "\"%R%\" -e \"library('rbokeh')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://hafen.github.io/rbokeh https://github.com/bokeh/rbokeh
-  license: MIT
+  license: MIT + file LICENSE
   summary: A native R plotting library that provides a flexible declarative interface for creating
     interactive web-based graphics, backed by the Bokeh visualization library <http://bokeh.pydata.org/>.
   license_family: MIT
@@ -66,3 +81,31 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: rbokeh
+# Title: R Interface for Bokeh
+# Version: 0.5.0
+# Authors@R: c( person("Ryan", "Hafen", email = "rhafen@gmail.com", role = c("aut", "cre")), person("Kenton", "Russell", email = "kent.russell@timelyportfolio.com", role = "ctb"), person("Jonathan", "Owen", email = "jonathanro@gmail.com", role = "ctb"), person("Barret", "Schloerke", email = "schloerke@gmail.com", role = "ctb"), person("Saptasrhi", "Guha", email = "saptarshi.guha@gmail.com", role = "ctb"), person("Continuum Analytics, Inc.", role = c("aut", "cph"), comment = "Bokeh library in htmlwidgets/lib, http://bokeh.pydata.org" ))
+# Description: A native R plotting library that provides a flexible declarative interface for creating interactive web-based graphics, backed by the Bokeh visualization library <http://bokeh.pydata.org/>.
+# URL: https://hafen.github.io/rbokeh https://github.com/bokeh/rbokeh
+# BugReports: https://github.com/bokeh/rbokeh/issues
+# License: MIT + file LICENSE
+# LazyData: true
+# NeedsCompilation: no
+# Imports: htmlwidgets (>= 0.5), maps, methods, jsonlite, digest, hexbin, lazyeval, pryr, magrittr, ggplot2, scales, gistr
+# Suggests: testthat, data.table, lattice, lintr, roxygen2 (>= 5.0.0), knitr
+# Enhances: shiny (>= 0.12)
+# RoxygenNote: 5.0.1
+# VignetteBuilder: knitr
+# Packaged: 2016-10-11 17:12:44 UTC; hafen
+# Author: Ryan Hafen [aut, cre], Kenton Russell [ctb], Jonathan Owen [ctb], Barret Schloerke [ctb], Saptasrhi Guha [ctb], Continuum Analytics, Inc. [aut, cph] (Bokeh library in htmlwidgets/lib, http://bokeh.pydata.org)
+# Maintainer: Ryan Hafen <rhafen@gmail.com>
+# Repository: CRAN
+# Date/Publication: 2016-10-12 00:08:32
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-rcolorbrewer-feedstock contains modified content
diff --git a/r-rcolorbrewer-feedstock/recipe/meta.yaml b/r-rcolorbrewer-feedstock/recipe/meta.yaml
index 915f4d9..53c1731 100644
--- a/r-rcolorbrewer-feedstock/recipe/meta.yaml
+++ b/r-rcolorbrewer-feedstock/recipe/meta.yaml
@@ -12,16 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/RColorBrewer_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/RColorBrewer/RColorBrewer_{{ version }}.tar.gz
-
   sha256: f3e9781e84e114b7a88eb099825936cc5ae7276bbba5af94d35adb1b3ea2ccdd
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
@@ -31,17 +38,46 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('RColorBrewer')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('RColorBrewer')"           # [not win]
     - "\"%R%\" -e \"library('RColorBrewer')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=RColorBrewer
-  license: Apache 2.0
-  license_family: APACHE
+  license: Apache License 2.0
   summary: Provides color schemes for maps (and other graphics) designed by Cynthia Brewer as
     described at http://colorbrewer2.org
+  license_family: APACHE
 
 extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: RColorBrewer
+# Version: 1.1-2
+# Date: 2014-12-07
+# Title: ColorBrewer Palettes
+# Authors@R: c(person(given = "Erich", family = "Neuwirth", role = c("aut", "cre"), email = "erich.neuwirth@univie.ac.at"))
+# Author: Erich Neuwirth [aut, cre]
+# Maintainer: Erich Neuwirth <erich.neuwirth@univie.ac.at>
+# Depends: R (>= 2.0.0)
+# Description: Provides color schemes for maps (and other graphics) designed by Cynthia Brewer as described at http://colorbrewer2.org
+# License: Apache License 2.0
+# Packaged: 2014-12-06 23:59:42 UTC; neuwirth
+# NeedsCompilation: no
+# Repository: CRAN
+# Date/Publication: 2014-12-07 08:28:55
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-rcpp-feedstock contains modified content
diff --git a/r-rcpp-feedstock/recipe/meta.yaml b/r-rcpp-feedstock/recipe/meta.yaml
index 3d64372..c6abcd7 100644
--- a/r-rcpp-feedstock/recipe/meta.yaml
+++ b/r-rcpp-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '0.12.10' %}
+{% set version = '0.12.13' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,48 +12,90 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/Rcpp_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/Rcpp/Rcpp_{{ version }}.tar.gz
+  sha256: a570ad88282fb961ba32c867c49dbd3dce6f4dfc7b59ab1fbde510449827a8ae
 
 
-  sha256: 4ef811a87be7d4d6c2d9084c5a23a3267192bcd79b949ba8ad8ff7a70a221781
   patches:
     - 0001-Add-CXX1XSTD-to-PKG_CPPFLAGS.patch
     - 0002-macOS-When-target-lt-10.9-libstdc-supports-only-some.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: RUnit, inline, rbenchmark, knitr, rmarkdown, pinp, pkgKitten (>= 0.1.2)
 requirements:
   build:
-    - {{ compiler('cxx') }}  # [not win]
     - r-base
-    - posix                  # [win]
-    - {{native}}toolchain    # [win]
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('cxx') }}      # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
 
   run:
     - r-base
-    - {{native}}gcc-libs     # [win]
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('Rcpp')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('Rcpp')"           # [not win]
     - "\"%R%\" -e \"library('Rcpp')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://www.rcpp.org, http://dirk.eddelbuettel.com/code/rcpp.html, https://github.com/RcppCore/Rcpp
-  license: GPL 2.0
-  license_family: GPL
+  license: GPL (>= 2)
   summary: The 'Rcpp' package provides R functions as well as C++ classes which offer a seamless
     integration of R and C++. Many R data types and objects can be mapped back and forth
     to C++ equivalents which facilitates both writing of new code as well as easier
     integration of third-party libraries. Documentation  about 'Rcpp' is provided by
     several vignettes included in this package, via the  'Rcpp Gallery' site at <http://gallery.rcpp.org>,
-    the paper by Eddelbuettel and  Francois (2011, JSS), and the book by Eddelbuettel
-    (2013, Springer); see  'citation("Rcpp")' for details on these last two.
+    the paper by Eddelbuettel and  Francois (2011, JSS), the book by Eddelbuettel (2013,
+    Springer) and the paper by Eddelbuettel and Balamuta (2017, PeerJ); see 'citation("Rcpp")'
+    for details.
+  license_family: GPL3
 
 extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: Rcpp
+# Title: Seamless R and C++ Integration
+# Version: 0.12.13
+# Date: 2017-09-24
+# Author: Dirk Eddelbuettel, Romain Francois, JJ Allaire, Kevin Ushey, Qiang Kou, Nathan Russell, Douglas Bates and John Chambers
+# Maintainer: Dirk Eddelbuettel <edd@debian.org>
+# Description: The 'Rcpp' package provides R functions as well as C++ classes which offer a seamless integration of R and C++. Many R data types and objects can be mapped back and forth to C++ equivalents which facilitates both writing of new code as well as easier integration of third-party libraries. Documentation  about 'Rcpp' is provided by several vignettes included in this package, via the  'Rcpp Gallery' site at <http://gallery.rcpp.org>, the paper by Eddelbuettel and  Francois (2011, JSS), the book by Eddelbuettel (2013, Springer) and the paper by Eddelbuettel and Balamuta (2017, PeerJ); see 'citation("Rcpp")' for details.
+# Depends: R (>= 3.0.0)
+# Imports: methods, utils
+# Suggests: RUnit, inline, rbenchmark, knitr, rmarkdown, pinp, pkgKitten (>= 0.1.2)
+# VignetteBuilder: knitr
+# URL: http://www.rcpp.org, http://dirk.eddelbuettel.com/code/rcpp.html, https://github.com/RcppCore/Rcpp
+# License: GPL (>= 2)
+# BugReports: https://github.com/RcppCore/Rcpp/issues
+# MailingList: Please send questions and comments regarding Rcpp to rcpp-devel@lists.r-forge.r-project.org
+# RoxygenNote: 6.0.1
+# NeedsCompilation: yes
+# Packaged: 2017-09-24 20:35:24 UTC; edd
+# Repository: CRAN
+# Date/Publication: 2017-09-28 08:09:26 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-rcpparmadillo-feedstock contains modified content
diff --git a/r-rcpparmadillo-feedstock/recipe/meta.yaml b/r-rcpparmadillo-feedstock/recipe/meta.yaml
index 0b7f17e..e85e21f 100644
--- a/r-rcpparmadillo-feedstock/recipe/meta.yaml
+++ b/r-rcpparmadillo-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '0.7.800.2.0' %}
+{% set version = '0.7.960.1.2' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,43 +12,99 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/RcppArmadillo_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/RcppArmadillo/RcppArmadillo_{{ version }}.tar.gz
-  sha256: 99ffdfb0d5498905b450c3badfc5cae0e584a7db16eb2e5cc8f1704b4081b408
+  sha256: 8ca5bb59cc8302792b9d6b03ed8cbbae4efc3e1cc20d24e008647c55d5dae84d
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: RUnit, Matrix, pkgKitten, reticulate, highlight
 requirements:
   build:
     - r-base
+    - {{ compiler('cxx') }}      # [not win]
+    - {{ compiler('fortran') }}  # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}autoconf
+    - {{posix}}automake
+    - {{posix}}pkg-config
+    - {{posix}}make
     - r-rcpp >=0.11.0
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-rcpp >=0.11.0
 
 test:
   commands:
-    - $R -e "library('RcppArmadillo')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('RcppArmadillo')"           # [not win]
     - "\"%R%\" -e \"library('RcppArmadillo')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://dirk.eddelbuettel.com/code/rcpp.armadillo.html
   license: GPL (>= 2)
-  summary: Armadillo is a templated C++ linear algebra library that
+  summary: '''Armadillo'' is a templated C++ linear algebra library (by Conrad Sanderson) that
     aims towards a good balance between speed and ease of use. Integer, floating point
     and complex numbers are supported, as well as a subset of trigonometric and statistics
     functions. Various matrix decompositions are provided through optional integration
-    with LAPACK and ATLAS libraries.
+    with LAPACK and ATLAS libraries. The ''RcppArmadillo'' package includes the header
+    files from the templated ''Armadillo'' library. Thus users do not need to install
+    ''Armadillo'' itself in order to use ''RcppArmadillo''. From release 7.800.0 on,
+    ''Armadillo'' is licensed under Apache License 2; previous releases were under licensed
+    as MPL 2.0 from version 3.800.0 onwards and LGPL-3 prior to that; ''RcppArmadillo''
+    (the ''Rcpp'' bindings/bridge to Armadillo) is licensed under the GNU GPL version
+    2 or later, as is the rest of ''Rcpp''. Note that Armadillo requires a fairly recent
+    compiler; for the g++ family at least version 4.6.* is required. '
+
   license_family: GPL3
 
 extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: RcppArmadillo
+# Type: Package
+# Title: 'Rcpp' Integration for the 'Armadillo' Templated Linear Algebra Library
+# Version: 0.7.960.1.2
+# Date: 2017-08-29
+# Author: Dirk Eddelbuettel, Romain Francois, Doug Bates and Binxiang Ni
+# Maintainer: Dirk Eddelbuettel <edd@debian.org>
+# Description: 'Armadillo' is a templated C++ linear algebra library (by Conrad Sanderson) that aims towards a good balance between speed and ease of use. Integer, floating point and complex numbers are supported, as well as a subset of trigonometric and statistics functions. Various matrix decompositions are provided through optional integration with LAPACK and ATLAS libraries. The 'RcppArmadillo' package includes the header files from the templated 'Armadillo' library. Thus users do not need to install 'Armadillo' itself in order to use 'RcppArmadillo'. From release 7.800.0 on, 'Armadillo' is licensed under Apache License 2; previous releases were under licensed as MPL 2.0 from version 3.800.0 onwards and LGPL-3 prior to that; 'RcppArmadillo' (the 'Rcpp' bindings/bridge to Armadillo) is licensed under the GNU GPL version 2 or later, as is the rest of 'Rcpp'. Note that Armadillo requires a fairly recent compiler; for the g++ family at least version 4.6.* is required.
+# License: GPL (>= 2)
+# LazyLoad: yes
+# LinkingTo: Rcpp
+# Imports: Rcpp (>= 0.11.0), stats, utils, methods
+# Suggests: RUnit, Matrix, pkgKitten, reticulate, highlight
+# URL: http://dirk.eddelbuettel.com/code/rcpp.armadillo.html
+# BugReports: https://github.com/RcppCore/RcppArmadillo/issues
+# NeedsCompilation: yes
+# Packaged: 2017-08-29 11:59:50.489326 UTC; edd
+# Repository: CRAN
+# Date/Publication: 2017-08-29 15:10:17 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-rcppeigen-feedstock contains modified content
diff --git a/r-rcppeigen-feedstock/recipe/meta.yaml b/r-rcppeigen-feedstock/recipe/meta.yaml
index 95129fb..43df000 100644
--- a/r-rcppeigen-feedstock/recipe/meta.yaml
+++ b/r-rcppeigen-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '0.3.2.9.1' %}
+{% set version = '0.3.3.3.0' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,55 +12,101 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/RcppEigen_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/RcppEigen/RcppEigen_{{ version }}.tar.gz
-  sha256: fd4c3a74a906fe88c5f03befc283bee2dfc3a20c7d1a7a2b03a3b0253d2009c6
+  sha256: c88e863f144ce255b7c935214ad056d5ca4d97f39386b431849b5a8b1cd8e97f
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: inline, RUnit, pkgKitten
 requirements:
   build:
     - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('cxx') }}      # [not win]
+    - {{ compiler('fortran') }}  # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
     - r-matrix >=1.1_0
     - r-rcpp >=0.11.0
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-matrix >=1.1_0
     - r-rcpp >=0.11.0
-    - libgcc  # [not win]
 
 test:
   commands:
-    - $R -e "library('RcppEigen')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('RcppEigen')"           # [not win]
     - "\"%R%\" -e \"library('RcppEigen')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://dirk.eddelbuettel.com/code/rcpp.eigen.html
-  license: GPL (>= 2)
-  summary: |
-    R and ''Eigen'' integration using ''Rcpp''. ''Eigen'' is a C++ template library for
+  license: GPL (>= 2) | file LICENSE
+  summary: 'R and ''Eigen'' integration using ''Rcpp''. ''Eigen'' is a C++ template library for
     linear algebra: matrices, vectors, numerical solvers and related algorithms.  It
     supports dense and sparse matrices on integer, floating point and complex numbers,
     decompositions of such matrices, and solutions of linear systems. Its performance
     on many algorithms is comparable with some of the best implementations based on
-    ''Lapack'' and level-3 ''BLAS''. . The ''RcppEigen'' package includes the header
-    files from the ''Eigen'' C++ template library (currently version 3.2.9). Thus users
-    do not need to install ''Eigen'' itself in order to use ''RcppEigen''. . Since version
+    ''Lapack'' and level-3 ''BLAS''. The ''RcppEigen'' package includes the header files
+    from the ''Eigen'' C++ template library (currently version 3.3.3). Thus users do
+    not need to install ''Eigen'' itself in order to use ''RcppEigen''. Since version
     3.1.1, ''Eigen'' is licensed under the Mozilla Public License (version 2); earlier
     version were licensed under the GNU LGPL version 3 or later. ''RcppEigen'' (the
     ''Rcpp'' bindings/bridge to ''Eigen'') is licensed under the GNU GPL version 2 or
-    later, as is the rest of ''Rcpp''
+    later, as is the rest of ''Rcpp''.'
+
   license_family: GPL3
 
 extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: RcppEigen
+# Type: Package
+# Title: 'Rcpp' Integration for the 'Eigen' Templated Linear Algebra Library
+# Version: 0.3.3.3.0
+# Date: 2017-04-29
+# Author: Douglas Bates, Dirk Eddelbuettel, Romain Francois, and Yixuan Qiu; the authors of Eigen for the included version of Eigen
+# Maintainer: Dirk Eddelbuettel <edd@debian.org>
+# Copyright: See the file COPYRIGHTS for various Eigen copyright details
+# Description: R and 'Eigen' integration using 'Rcpp'. 'Eigen' is a C++ template library for linear algebra: matrices, vectors, numerical solvers and related algorithms.  It supports dense and sparse matrices on integer, floating point and complex numbers, decompositions of such matrices, and solutions of linear systems. Its performance on many algorithms is comparable with some of the best implementations based on 'Lapack' and level-3 'BLAS'. The 'RcppEigen' package includes the header files from the 'Eigen' C++ template library (currently version 3.3.3). Thus users do not need to install 'Eigen' itself in order to use 'RcppEigen'. Since version 3.1.1, 'Eigen' is licensed under the Mozilla Public License (version 2); earlier version were licensed under the GNU LGPL version 3 or later. 'RcppEigen' (the 'Rcpp' bindings/bridge to 'Eigen') is licensed under the GNU GPL version 2 or later, as is the rest of 'Rcpp'.
+# License: GPL (>= 2) | file LICENSE
+# Depends: R (>= 2.15.1)
+# LazyLoad: yes
+# LinkingTo: Rcpp
+# Imports: Matrix (>= 1.1-0), Rcpp (>= 0.11.0), stats, utils
+# Suggests: inline, RUnit, pkgKitten
+# URL: http://dirk.eddelbuettel.com/code/rcpp.eigen.html
+# BugReports: https://github.com/RcppCore/RcppEigen/issues
+# NeedsCompilation: yes
+# Packaged: 2017-04-29 11:47:36.63527 UTC; edd
+# Repository: CRAN
+# Date/Publication: 2017-05-01 15:10:35 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
diff --git a/r-rcpproll-feedstock/recipe/bld.bat b/r-rcpproll-feedstock/recipe/bld.bat
index e69de29..8b10c94 100644
--- a/r-rcpproll-feedstock/recipe/bld.bat
+++ b/r-rcpproll-feedstock/recipe/bld.bat
@@ -0,0 +1,8 @@
+"%R%" CMD INSTALL --build .
+if errorlevel 1 exit 1
+
+@rem Add more build steps here, if they are necessary.
+
+@rem See
+@rem http://docs.continuum.io/conda/build.html
+@rem for a list of environment variables that are set during the build process.
diff --git a/r-rcpproll-feedstock/recipe/build.sh b/r-rcpproll-feedstock/recipe/build.sh
index e69de29..0c37df6 100644
--- a/r-rcpproll-feedstock/recipe/build.sh
+++ b/r-rcpproll-feedstock/recipe/build.sh
@@ -0,0 +1,13 @@
+#!/bin/bash
+
+# R refuses to build packages that mark themselves as Priority: Recommended
+mv DESCRIPTION DESCRIPTION.old
+grep -v '^Priority: ' DESCRIPTION.old > DESCRIPTION
+
+$R CMD INSTALL --build .
+
+# Add more build steps here, if they are necessary.
+
+# See
+# http://docs.continuum.io/conda/build.html
+# for a list of environment variables that are set during the build process.
diff --git a/r-rcpproll-feedstock/recipe/meta.yaml b/r-rcpproll-feedstock/recipe/meta.yaml
index e69de29..8bcf689 100644
--- a/r-rcpproll-feedstock/recipe/meta.yaml
+++ b/r-rcpproll-feedstock/recipe/meta.yaml
@@ -0,0 +1,90 @@
+{% set version = '0.2.2' %}
+
+{% set posix = 'm2-' if win else '' %}
+{% set native = 'm2w64-' if win else '' %}
+
+package:
+  name: r-rcpproll
+  version: {{ version|replace("-", "_") }}
+
+source:
+  fn: RcppRoll_{{ version }}.tar.gz
+  url:
+    - https://cran.r-project.org/src/contrib/RcppRoll_{{ version }}.tar.gz
+    - https://cran.r-project.org/src/contrib/Archive/RcppRoll/RcppRoll_{{ version }}.tar.gz
+  sha256: 51c76812687e2a8572c6037b0c095c0a30ee6a24783edf2c7c717d547ddfbfa7
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
+build:
+  # If this is a new build for the same version, increment the build number.
+  number: 0
+
+  # This is required to make R link correctly on Linux.
+  rpaths:
+    - lib/R/lib/
+    - lib/
+
+# Suggests: zoo, microbenchmark, testthat, RcppArmadillo
+requirements:
+  build:
+    - r-base
+    - {{ compiler('cxx') }}      # [not win]
+    - {{native}}toolchain        # [win]
+    - r-rcpp
+
+  run:
+    - r-base
+    - {{native}}gcc-libs         # [win]
+    - r-rcpp
+
+test:
+  commands:
+    # You can put additional test commands to be run here.
+    - $R -e "library('RcppRoll')"           # [not win]
+    - "\"%R%\" -e \"library('RcppRoll')\""  # [win]
+
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
+about:
+  home: https://CRAN.R-project.org/package=RcppRoll
+  license: GPL (>= 2)
+  summary: Provides fast and efficient routines for common rolling / windowed operations. Routines
+    for the efficient computation of windowed mean, median, sum, product, minimum, maximum,
+    standard deviation and variance are provided.
+  license_family: GPL3
+
+extra:
+  recipe-maintainers:
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: RcppRoll
+# Type: Package
+# Title: Efficient Rolling / Windowed Operations
+# Version: 0.2.2
+# Date: 2015-04-04
+# Author: Kevin Ushey
+# Maintainer: Kevin Ushey <kevinushey@gmail.com>
+# Description: Provides fast and efficient routines for common rolling / windowed operations. Routines for the efficient computation of windowed mean, median, sum, product, minimum, maximum, standard deviation and variance are provided.
+# License: GPL (>= 2)
+# Depends: R (>= 2.15.1)
+# Suggests: zoo, microbenchmark, testthat, RcppArmadillo
+# Imports: Rcpp
+# LinkingTo: Rcpp
+# NeedsCompilation: yes
+# Packaged: 2015-04-04 23:07:51 UTC; kevinushey
+# Repository: CRAN
+# Date/Publication: 2015-04-05 01:21:30
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-rcurl-feedstock contains modified content
diff --git a/r-rcurl-feedstock/recipe/meta.yaml b/r-rcurl-feedstock/recipe/meta.yaml
index 5fbb72b..2c42dbd 100644
--- a/r-rcurl-feedstock/recipe/meta.yaml
+++ b/r-rcurl-feedstock/recipe/meta.yaml
@@ -14,37 +14,54 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/RCurl/RCurl_{{ version }}.tar.gz
   sha256: e72243251bbbec341bc5864305bb8cc23d311d19c5d0d9310afec7eb35aa2bfb
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  # skip windows due to compilation failures
-  skip: true  # [win]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: Rcompression, XML
 requirements:
   build:
     - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}autoconf
+    - {{posix}}automake
+    - {{posix}}pkg-config
+    - {{posix}}make
     - r-bitops
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
-    - curl
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-bitops
-    - curl
 
 test:
   commands:
-    - $R -e "library('RCurl')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('RCurl')"           # [not win]
     - "\"%R%\" -e \"library('RCurl')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://www.omegahat.net/RCurl
   license: BSD
-  summary: A wrapper for libcurl. Provides functions to allow
+  summary: A wrapper for 'libcurl' <http://curl.haxx.se/libcurl/> Provides functions to allow
     one to compose general HTTP requests and provides convenient functions to fetch
     URIs, get & post forms, etc. and process the results returned by the Web server.
     This provides a great deal of control over the HTTP/FTP/... connection and the form
@@ -58,3 +75,31 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: RCurl
+# Version: 1.95-4.8
+# Title: General Network (HTTP/FTP/...) Client Interface for R
+# Author: Duncan Temple Lang and the CRAN team
+# SystemRequirements: GNU make, libcurl
+# Maintainer: Duncan Temple Lang <duncan@r-project.org>
+# Description: A wrapper for 'libcurl' <http://curl.haxx.se/libcurl/> Provides functions to allow one to compose general HTTP requests and provides convenient functions to fetch URIs, get & post forms, etc. and process the results returned by the Web server. This provides a great deal of control over the HTTP/FTP/... connection and the form of the request while providing a higher-level interface than is available just using R socket connections.  Additionally, the underlying implementation is robust and extensive, supporting FTP/FTPS/TFTP (uploads and downloads), SSL/HTTPS, telnet, dict, ldap, and also supports cookies, redirects, authentication, etc.
+# License: BSD
+# Depends: R (>= 3.0.0), methods, bitops
+# Suggests: Rcompression, XML
+# URL: http://www.omegahat.net/RCurl
+# Additional_repositories: http://www.omegahat.net/R/
+# Limitations: One doesn't yet have full control over the contents of a POST form such as specifying files, content type. Error handling uses a single global variable at present.
+# Note: This will be used generally to communicate with HTTP servers and specifically in the SOAP package, HTML forms and for accessing data on Web sites that require more state information than simply URL access (e.g. cookies).
+# Check: To run R CMD check, one really needs network access. It is an HTTP client! This can be problematic as a Web site may be down when you run but the real tests are important.
+# Collate: aclassesEnums.R bitClasses.R xbits.R base64.R binary.S classes.S curl.S curlAuthConstants.R curlEnums.R curlError.R curlInfo.S dynamic.R form.S getFormParams.R getURLContent.R header.R http.R httpError.R httpErrors.R iconv.R info.S mime.R multi.S options.S scp.R support.S upload.R urlExists.R zclone.R zzz.R
+# NeedsCompilation: yes
+# Packaged: 2016-03-01 07:37:59 UTC; ripley
+# Repository: CRAN
+# Date/Publication: 2016-03-01 10:54:28
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-readr-feedstock contains modified content
diff --git a/r-readr-feedstock/recipe/meta.yaml b/r-readr-feedstock/recipe/meta.yaml
index d43c2ee..1a40469 100644
--- a/r-readr-feedstock/recipe/meta.yaml
+++ b/r-readr-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '1.1.0' %}
+{% set version = '1.1.1' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,45 +12,59 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/readr_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/readr/readr_{{ version }}.tar.gz
-  sha256: 47633f022c7b5398e7c3d62493bc06823034e608d119ae1592f6e05ddb1befbc
+  sha256: 1a29b99009a06f2cee18d08bc6201fd4985b6d45c76cefca65084dcc1a2f7cb3
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: curl, testthat, knitr, rmarkdown, stringi, covr
 requirements:
   build:
     - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('cxx') }}      # [not win]
+    - {{native}}toolchain        # [win]
     - r-bh
     - r-r6
     - r-rcpp >=0.12.0.5
     - r-hms
     - r-tibble
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-bh
     - r-r6
     - r-rcpp >=0.12.0.5
     - r-hms
     - r-tibble
-    - libgcc  # [not win]
 
 test:
   commands:
-    - $R -e "library('readr')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('readr')"           # [not win]
     - "\"%R%\" -e \"library('readr')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://readr.tidyverse.org, https://github.com/tidyverse/readr
-  license: GPL (>= 2)
+  license: GPL (>= 2) | file LICENSE
   summary: The goal of 'readr' is to provide a fast and friendly way to read rectangular data
     (like 'csv', 'tsv', and 'fwf'). It is designed to flexibly parse many types of data
     found in the wild, while still cleanly failing when data unexpectedly changes.
@@ -60,3 +74,32 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: readr
+# Version: 1.1.1
+# Title: Read Rectangular Text Data
+# Description: The goal of 'readr' is to provide a fast and friendly way to read rectangular data (like 'csv', 'tsv', and 'fwf'). It is designed to flexibly parse many types of data found in the wild, while still cleanly failing when data unexpectedly changes.
+# Authors@R: c( person("Hadley", "Wickham", , "hadley@rstudio.com", "aut"), person("Jim", "Hester", , "james.hester@rstudio.com", c("aut", "cre")), person("Romain", "Francois", role = "aut"), person("R Core Team", role = "ctb", comment = "Date time code adapted from R"), person("RStudio", role = c("cph", "fnd")), person("Jukka", "Jylanki", role = c("ctb", "cph"), comment = "grisu3 implementation"), person("Mikkel", "Jrgensen", role = c("ctb", "cph"), comment = "grisu3 implementation"))
+# Encoding: UTF-8
+# Depends: R (>= 3.0.2)
+# LinkingTo: Rcpp, BH
+# Imports: Rcpp (>= 0.12.0.5), tibble, hms, R6
+# Suggests: curl, testthat, knitr, rmarkdown, stringi, covr
+# License: GPL (>= 2) | file LICENSE
+# BugReports: https://github.com/tidyverse/readr/issues
+# URL: http://readr.tidyverse.org, https://github.com/tidyverse/readr
+# VignetteBuilder: knitr
+# RoxygenNote: 6.0.1
+# NeedsCompilation: yes
+# Packaged: 2017-05-16 16:03:56 UTC; jhester
+# Author: Hadley Wickham [aut], Jim Hester [aut, cre], Romain Francois [aut], R Core Team [ctb] (Date time code adapted from R), RStudio [cph, fnd], Jukka Jylanki [ctb, cph] (grisu3 implementation), Mikkel Jrgensen [ctb, cph] (grisu3 implementation)
+# Maintainer: Jim Hester <james.hester@rstudio.com>
+# Repository: CRAN
+# Date/Publication: 2017-05-16 19:03:57 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-readxl-feedstock contains modified content
diff --git a/r-readxl-feedstock/recipe/meta.yaml b/r-readxl-feedstock/recipe/meta.yaml
index 0df04c4..d1f3297 100644
--- a/r-readxl-feedstock/recipe/meta.yaml
+++ b/r-readxl-feedstock/recipe/meta.yaml
@@ -14,35 +14,52 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/readxl/readxl_{{ version }}.tar.gz
   sha256: fbd62f07fed7946363698a57be88f4ef3fa254ecf456ef292535849c787fc7ad
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: covr, knitr, rmarkdown, rprojroot (>= 1.1), testthat
 requirements:
   build:
     - r-base
-    - r-rcpp
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('cxx') }}      # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
+    - r-rcpp >=0.11.6
     - r-cellranger
     - r-tibble >=1.1
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
-    - r-rcpp
+    - {{native}}gcc-libs         # [win]
+    - r-rcpp >=0.11.6
     - r-cellranger
     - r-tibble >=1.1
 
 test:
   commands:
-    - $R -e "library('readxl')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('readxl')"           # [not win]
     - "\"%R%\" -e \"library('readxl')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://readxl.tidyverse.org, https://github.com/tidyverse/readxl
   license: GPL-3
@@ -55,3 +72,31 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: readxl
+# Title: Read Excel Files
+# Version: 1.0.0
+# Authors@R: c( person("Hadley", "Wickham", ,"hadley@rstudio.com", role = c("aut")), person("Jennifer", "Bryan", ,"jenny@rstudio.com", role = c("aut", "cre")), person("RStudio", role = c("cph"), comment = "Copyright holder of all R code and all C/C++ code without explicit copyright attribution"), person("Marcin", "Kalicinski", role = c("ctb", "cph"), comment = "Author of included RapidXML code"), person("Komarov Valery", role = c("ctb", "cph"), comment = "Author of included libxls code"), person("Christophe Leitienne", role = c("ctb", "cph"), comment = "Author of included libxls code"), person("Bob Colbert", role = c("ctb", "cph"), comment = "Author of included libxls code"), person("David Hoerl", role = c("ctb", "cph"), comment = "Author of included libxls code") )
+# Description: Import excel files into R. Supports '.xls' via the embedded 'libxls' C library <https://sourceforge.net/projects/libxls/> and '.xlsx' via the embedded 'RapidXML' C++ library <https://rapidxml.sourceforge.net>. Works on Windows, Mac and Linux without external dependencies.
+# License: GPL-3
+# LazyData: true
+# LinkingTo: Rcpp
+# Imports: cellranger, Rcpp (>= 0.11.6), tibble (>= 1.1)
+# Suggests: covr, knitr, rmarkdown, rprojroot (>= 1.1), testthat
+# URL: http://readxl.tidyverse.org, https://github.com/tidyverse/readxl
+# BugReports: https://github.com/tidyverse/readxl/issues
+# VignetteBuilder: knitr
+# RoxygenNote: 6.0.1
+# NeedsCompilation: yes
+# Packaged: 2017-04-18 06:15:07 UTC; jenny
+# Author: Hadley Wickham [aut], Jennifer Bryan [aut, cre], RStudio [cph] (Copyright holder of all R code and all C/C++ code without explicit copyright attribution), Marcin Kalicinski [ctb, cph] (Author of included RapidXML code), Komarov Valery [ctb, cph] (Author of included libxls code), Christophe Leitienne [ctb, cph] (Author of included libxls code), Bob Colbert [ctb, cph] (Author of included libxls code), David Hoerl [ctb, cph] (Author of included libxls code)
+# Maintainer: Jennifer Bryan <jenny@rstudio.com>
+# Repository: CRAN
+# Date/Publication: 2017-04-18 07:21:33 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
diff --git a/r-recipes-feedstock/recipe/bld.bat b/r-recipes-feedstock/recipe/bld.bat
index e69de29..8b10c94 100644
--- a/r-recipes-feedstock/recipe/bld.bat
+++ b/r-recipes-feedstock/recipe/bld.bat
@@ -0,0 +1,8 @@
+"%R%" CMD INSTALL --build .
+if errorlevel 1 exit 1
+
+@rem Add more build steps here, if they are necessary.
+
+@rem See
+@rem http://docs.continuum.io/conda/build.html
+@rem for a list of environment variables that are set during the build process.
diff --git a/r-recipes-feedstock/recipe/build.sh b/r-recipes-feedstock/recipe/build.sh
index e69de29..0c37df6 100644
--- a/r-recipes-feedstock/recipe/build.sh
+++ b/r-recipes-feedstock/recipe/build.sh
@@ -0,0 +1,13 @@
+#!/bin/bash
+
+# R refuses to build packages that mark themselves as Priority: Recommended
+mv DESCRIPTION DESCRIPTION.old
+grep -v '^Priority: ' DESCRIPTION.old > DESCRIPTION
+
+$R CMD INSTALL --build .
+
+# Add more build steps here, if they are necessary.
+
+# See
+# http://docs.continuum.io/conda/build.html
+# for a list of environment variables that are set during the build process.
diff --git a/r-recipes-feedstock/recipe/meta.yaml b/r-recipes-feedstock/recipe/meta.yaml
index e69de29..cbe34ec 100644
--- a/r-recipes-feedstock/recipe/meta.yaml
+++ b/r-recipes-feedstock/recipe/meta.yaml
@@ -0,0 +1,118 @@
+{% set version = '0.1.0' %}
+
+{% set posix = 'm2-' if win else '' %}
+{% set native = 'm2w64-' if win else '' %}
+
+package:
+  name: r-recipes
+  version: {{ version|replace("-", "_") }}
+
+source:
+  fn: recipes_{{ version }}.tar.gz
+  url:
+    - https://cran.r-project.org/src/contrib/recipes_{{ version }}.tar.gz
+    - https://cran.r-project.org/src/contrib/Archive/recipes/recipes_{{ version }}.tar.gz
+  sha256: 6ba689b5101f54d8b32844ee15cf0e8f4cee471e62fc3f36ab1d463c0099cd67
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
+build:
+  # If this is a new build for the same version, increment the build number.
+  number: 0
+
+  # This is required to make R link correctly on Linux.
+  rpaths:
+    - lib/R/lib/
+    - lib/
+
+# Suggests: testthat, rpart, kernlab, fastICA, RANN, igraph, knitr, caret, ggplot2, rmarkdown
+requirements:
+  build:
+    - r-base
+    - r-rcpproll
+    - r-ddalpha
+    - r-dimred >=0.1.0
+    - r-dplyr
+    - r-gower
+    - r-ipred
+    - r-lubridate
+    - r-magrittr
+    - r-purrr
+    - r-rlang >=0.1.1
+    - r-tibble
+    - r-tidyselect >=0.1.1
+    - r-timedate
+
+  run:
+    - r-base
+    - r-rcpproll
+    - r-ddalpha
+    - r-dimred >=0.1.0
+    - r-dplyr
+    - r-gower
+    - r-ipred
+    - r-lubridate
+    - r-magrittr
+    - r-purrr
+    - r-rlang >=0.1.1
+    - r-tibble
+    - r-tidyselect >=0.1.1
+    - r-timedate
+
+test:
+  commands:
+    # You can put additional test commands to be run here.
+    - $R -e "library('recipes')"           # [not win]
+    - "\"%R%\" -e \"library('recipes')\""  # [win]
+
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
+about:
+  home: https://github.com/topepo/recipes
+  license: GPL-2
+  summary: 'An extensible framework to create and preprocess  design matrices. Recipes consist
+    of one or more data manipulation  and analysis "steps". Statistical parameters for
+    the steps can  be estimated from an initial data set and then applied to  other
+    data sets. The resulting design matrices can then be used  as inputs into statistical
+    or machine learning models. '
+
+  license_family: GPL2
+
+extra:
+  recipe-maintainers:
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: recipes
+# Title: Preprocessing Tools to Create Design Matrices
+# Version: 0.1.0
+# Authors@R: c( person("Max", "Kuhn", , "max@rstudio.com", c("aut", "cre")), person("Hadley", "Wickham", , "hadley@rstudio.com", "aut"), person("RStudio", role = "cph"))
+# Description: An extensible framework to create and preprocess  design matrices. Recipes consist of one or more data manipulation  and analysis "steps". Statistical parameters for the steps can  be estimated from an initial data set and then applied to  other data sets. The resulting design matrices can then be used  as inputs into statistical or machine learning models.
+# URL: https://github.com/topepo/recipes
+# BugReports: https://github.com/topepo/recipes/issues
+# Depends: R (>= 3.2.3), dplyr
+# Imports: tibble, stats, ipred, dimRed (>= 0.1.0), lubridate, timeDate, ddalpha, purrr, rlang (>= 0.1.1), gower, RcppRoll, tidyselect (>= 0.1.1), magrittr
+# Suggests: testthat, rpart, kernlab, fastICA, RANN, igraph, knitr, caret, ggplot2, rmarkdown
+# License: GPL-2
+# VignetteBuilder: knitr
+# Encoding: UTF-8
+# LazyData: true
+# RoxygenNote: 6.0.1
+# NeedsCompilation: no
+# Packaged: 2017-07-27 01:40:39 UTC; max
+# Author: Max Kuhn [aut, cre], Hadley Wickham [aut], RStudio [cph]
+# Maintainer: Max Kuhn <max@rstudio.com>
+# Repository: CRAN
+# Date/Publication: 2017-07-27 10:46:19 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-recommended-feedstock contains modified content
diff --git a/r-recommended-feedstock/recipe/meta.yaml b/r-recommended-feedstock/recipe/meta.yaml
index 559730f..8f557b3 100644
--- a/r-recommended-feedstock/recipe/meta.yaml
+++ b/r-recommended-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = "3.3.2" %}
+{% set version = "3.4.2" %}
 
 package:
   name: r-recommended
@@ -6,7 +6,6 @@ package:
 
 build:
   number: 0
-  skip:  # [win32]
 
 requirements:
   run:
@@ -34,5 +33,6 @@ about:
 
 extra:
   recipe-maintainers:
-    - johanneskoester
     - bgruening
+    - johanneskoester
+    - mingwandroid
Submodule r-registry-feedstock contains modified content
diff --git a/r-registry-feedstock/recipe/meta.yaml b/r-registry-feedstock/recipe/meta.yaml
index 67009ed..638ffe2 100644
--- a/r-registry-feedstock/recipe/meta.yaml
+++ b/r-registry-feedstock/recipe/meta.yaml
@@ -12,16 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/registry_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/registry/registry_{{ version }}.tar.gz
-
   sha256: 58a5c43b8012ca5e509fa29a8daf6f24f097b8eb021a723f6a9c33db1dd3f430
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
@@ -31,16 +38,45 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('registry')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('registry')"           # [not win]
     - "\"%R%\" -e \"library('registry')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=registry
-  license: GPL 2.0
-  license_family: GPL
+  license: GPL-2
   summary: Provides a generic infrastructure for creating and using registries.
+  license_family: GPL2
 
 extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: registry
+# Type: Package
+# Title: Infrastructure for R Package Registries
+# Version: 0.3
+# Author: David Meyer
+# Maintainer: David Meyer <David.Meyer@R-project.org>
+# Description: Provides a generic infrastructure for creating and using registries.
+# Depends: R (>= 2.6.0)
+# Imports: utils
+# License: GPL-2
+# NeedsCompilation: no
+# Packaged: 2015-07-07 16:27:02 UTC; david
+# Repository: CRAN
+# Date/Publication: 2015-07-08 17:11:04
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-rematch-feedstock contains modified content
diff --git a/r-rematch-feedstock/recipe/meta.yaml b/r-rematch-feedstock/recipe/meta.yaml
index 89dcc54..10145dd 100644
--- a/r-rematch-feedstock/recipe/meta.yaml
+++ b/r-rematch-feedstock/recipe/meta.yaml
@@ -14,14 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/rematch/rematch_{{ version }}.tar.gz
   sha256: a409dec978cd02914cdddfedc974d9b45bd2975a124d8870d52cfd7d37d47578
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: covr, testthat
 requirements:
   build:
     - r-base
@@ -31,12 +38,19 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('rematch')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('rematch')"           # [not win]
     - "\"%R%\" -e \"library('rematch')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/MangoTheCat/rematch
-  license: MIT
+  license: MIT + file LICENSE
   summary: A small wrapper on 'regexpr' to extract the matches and captured groups from the match
     of a regular expression to a character vector.
   license_family: MIT
@@ -45,3 +59,28 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: rematch
+# Title: Match Regular Expressions with a Nicer 'API'
+# Version: 1.0.1
+# Author: Gabor Csardi
+# Maintainer: Gabor Csardi <gcsardi@mango-solutions.com>
+# Description: A small wrapper on 'regexpr' to extract the matches and captured groups from the match of a regular expression to a character vector.
+# License: MIT + file LICENSE
+# LazyData: true
+# URL: https://github.com/MangoTheCat/rematch
+# BugReports: https://github.com/MangoTheCat/rematch/issues
+# RoxygenNote: 5.0.1.9000
+# Suggests: covr, testthat
+# Encoding: UTF-8
+# NeedsCompilation: no
+# Packaged: 2016-04-20 19:54:37 UTC; gaborcsardi
+# Repository: CRAN
+# Date/Publication: 2016-04-21 08:20:46
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-repr-feedstock contains modified content
diff --git a/r-repr-feedstock/recipe/bld.bat b/r-repr-feedstock/recipe/bld.bat
new file mode 100644
index 0000000..8b10c94
--- /dev/null
+++ b/r-repr-feedstock/recipe/bld.bat
@@ -0,0 +1,8 @@
+"%R%" CMD INSTALL --build .
+if errorlevel 1 exit 1
+
+@rem Add more build steps here, if they are necessary.
+
+@rem See
+@rem http://docs.continuum.io/conda/build.html
+@rem for a list of environment variables that are set during the build process.
diff --git a/r-repr-feedstock/recipe/build.sh b/r-repr-feedstock/recipe/build.sh
new file mode 100644
index 0000000..0c37df6
--- /dev/null
+++ b/r-repr-feedstock/recipe/build.sh
@@ -0,0 +1,13 @@
+#!/bin/bash
+
+# R refuses to build packages that mark themselves as Priority: Recommended
+mv DESCRIPTION DESCRIPTION.old
+grep -v '^Priority: ' DESCRIPTION.old > DESCRIPTION
+
+$R CMD INSTALL --build .
+
+# Add more build steps here, if they are necessary.
+
+# See
+# http://docs.continuum.io/conda/build.html
+# for a list of environment variables that are set during the build process.
diff --git a/r-repr-feedstock/recipe/meta.yaml b/r-repr-feedstock/recipe/meta.yaml
index cd3ba1a..0274461 100644
--- a/r-repr-feedstock/recipe/meta.yaml
+++ b/r-repr-feedstock/recipe/meta.yaml
@@ -1,40 +1,87 @@
-{% set version = "0.10" %}
+{% set version = '0.12.0' %}
+
+{% set posix = 'm2-' if win else '' %}
+{% set native = 'm2w64-' if win else '' %}
 
 package:
   name: r-repr
-  version: {{ version }}
+  version: {{ version|replace("-", "_") }}
 
 source:
-  fn: r-repr-{{ version }}.tar.gz
-  url: https://github.com/IRkernel/repr/archive/{{ version }}.tar.gz
-  sha256: f84e66280bcba162d79f1e39de640c7df47cc0aee06e7386766d5443e0981754
+  fn: repr_{{ version }}.tar.gz
+  url:
+    - https://cran.r-project.org/src/contrib/repr_{{ version }}.tar.gz
+    - https://cran.r-project.org/src/contrib/Archive/repr/repr_{{ version }}.tar.gz
+  sha256: 03af0059ecb9b49f3d2c625eceecae7c20b88cd82a066b4d244ec5b27c16cadc
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: True  # [win32]
-  script: R CMD INSTALL --build .
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
-requirements:  # [not win32]
-  build:  # [not win32]
-    - r-base  # [not win32]
-    - posix  # [win64]
-    - m2-git  # [win64]
-  run:  # [not win32]
-    - r-base  # [not win32]
+# Suggests: methods, highr, Cairo, testthat
+requirements:
+  build:
+    - r-base
+
+  run:
+    - r-base
 
 test:
   commands:
-    - R -e "library('repr')"  # [not win]
-    - R -e \"library('repr')\"  # [win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('repr')"           # [not win]
+    - "\"%R%\" -e \"library('repr')\""  # [win]
+
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
 
 about:
-  home: https://github.com/IRkernel/repr
+  home: https://CRAN.R-project.org/package=repr
   license: GPL-3
-  summary: 'String and binary representations of objects for several formats / mime types.'
+  summary: String and binary representations of objects for several formats / mime types.
+  license_family: GPL3
 
 extra:
   recipe-maintainers:
     - ocefpaf
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: repr
+# Title: Serializable Representations
+# Version: 0.12.0
+# Authors@R: c( person('Philipp', 'Angerer', email = 'phil.angerer@gmail.com', role = c('aut', 'cre')), person('Thomas', 'Kluyver', email = 'thomas@kluyver.me.uk', role = 'aut'), person('Jan', 'Schulz', email = 'jasc@gmx.net', role = 'aut'), person('abielr', role = 'ctb'), person('Denilson', 'Figueiredo de Sa', role = 'ctb'), person('Jim', 'Hester', role = 'ctb'), person('karldw', role = 'ctb') )
+# Maintainer: Philipp Angerer <phil.angerer@gmail.com>
+# Description: String and binary representations of objects for several formats / mime types.
+# Depends: R (>= 3.0.1)
+# Imports: utils, grDevices
+# Suggests: methods, highr, Cairo, testthat
+# Enhances: data.table, dplyr, htmlwidgets
+# License: GPL-3
+# LazyData: true
+# Encoding: UTF-8
+# Collate: 'generics.r' 'options.r' 'package.r' 'repr_datatable.r' 'utils.r' 'repr_function.r' 'repr_help_files_with_topic.r' 'repr_htmlwidget.r' 'repr_list.r' 'repr_matrix_df.r' 'repr_packageIQR.r' 'repr_recordedplot.r' 'repr_vector.r'
+# RoxygenNote: 6.0.1.9000
+# NeedsCompilation: no
+# Packaged: 2017-04-07 09:59:00 UTC; angerer
+# Author: Philipp Angerer [aut, cre], Thomas Kluyver [aut], Jan Schulz [aut], abielr [ctb], Denilson Figueiredo de Sa [ctb], Jim Hester [ctb], karldw [ctb]
+# Repository: CRAN
+# Date/Publication: 2017-04-07 13:08:00 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-reshape-feedstock contains modified content
diff --git a/r-reshape-feedstock/recipe/meta.yaml b/r-reshape-feedstock/recipe/meta.yaml
index afb1dc0..f5e7034 100644
--- a/r-reshape-feedstock/recipe/meta.yaml
+++ b/r-reshape-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '0.8.6' %}
+{% set version = '0.8.7' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,40 +12,76 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/reshape_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/reshape/reshape_{{ version }}.tar.gz
-  sha256: d89db98ea1025e38d59daeff965dd8174bbb94a961cb966a83760ac1a87a36b8
+  sha256: 2fa6c87d1e89f182e51bc5a4fcda3d42d83b8fb4474ca525fa7a8db5081f3992
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
+
+
 requirements:
   build:
     - r-base
     - r-plyr
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
     - r-plyr
-    - libgcc  # [not win]
 
 test:
   commands:
-    - $R -e "library('reshape')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('reshape')"           # [not win]
     - "\"%R%\" -e \"library('reshape')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://had.co.nz/reshape
-  license: MIT
-  summary: Flexibly restructure and aggregate data using just melt() and cast().
+  license: MIT + file LICENSE
+  summary: 'Flexibly restructure and aggregate data using  just two functions: melt and cast.'
+
   license_family: MIT
 
 extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: reshape
+# Version: 0.8.7
+# Title: Flexibly Reshape Data
+# Description: Flexibly restructure and aggregate data using  just two functions: melt and cast.
+# Authors@R:  person("Hadley", "Wickham", , "hadley@rstudio.com", c("aut", "cre"))
+# URL: http://had.co.nz/reshape
+# Depends: R (>= 2.6.1)
+# Imports: plyr
+# License: MIT + file LICENSE
+# LazyData: true
+# NeedsCompilation: yes
+# Packaged: 2017-08-04 16:36:57 UTC; hadley
+# Author: Hadley Wickham [aut, cre]
+# Maintainer: Hadley Wickham <hadley@rstudio.com>
+# Repository: CRAN
+# Date/Publication: 2017-08-06 16:08:19 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-reshape2-feedstock contains modified content
diff --git a/r-reshape2-feedstock/recipe/meta.yaml b/r-reshape2-feedstock/recipe/meta.yaml
index 5584cd8..924011c 100644
--- a/r-reshape2-feedstock/recipe/meta.yaml
+++ b/r-reshape2-feedstock/recipe/meta.yaml
@@ -14,39 +14,52 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/reshape2/reshape2_{{ version }}.tar.gz
   sha256: 6d3783610379be4c5676d9236cf66276a166b5b96c18f2759e9b219758959b6b
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: testthat (>= 0.8.0), lattice
 requirements:
   build:
     - r-base
+    - {{ compiler('cxx') }}      # [not win]
+    - {{native}}toolchain        # [win]
     - r-rcpp
     - r-plyr >=1.8.1
     - r-stringr
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-rcpp
     - r-plyr >=1.8.1
     - r-stringr
-    - libgcc               # [not win]
 
 test:
   commands:
-    - $R -e "library('reshape2')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('reshape2')"           # [not win]
     - "\"%R%\" -e \"library('reshape2')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/hadley/reshape
-  license: MIT
+  license: MIT + file LICENSE
   summary: 'Flexibly restructure and aggregate data using just two functions: melt and ''dcast''
     (or ''acast'').'
 
@@ -57,3 +70,29 @@ extra:
     - johanneskoester
     - bgruening
     - lpantano
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: reshape2
+# Title: Flexibly Reshape Data: A Reboot of the Reshape Package
+# Version: 1.4.2
+# Author: Hadley Wickham <h.wickham@gmail.com>
+# Maintainer: Hadley Wickham <h.wickham@gmail.com>
+# Description: Flexibly restructure and aggregate data using just two functions: melt and 'dcast' (or 'acast').
+# URL: https://github.com/hadley/reshape
+# BugReports: https://github.com/hadley/reshape/issues
+# LinkingTo: Rcpp
+# Imports: plyr (>= 1.8.1), stringr, Rcpp
+# Suggests: testthat (>= 0.8.0), lattice
+# License: MIT + file LICENSE
+# LazyData: true
+# RoxygenNote: 5.0.1.9000
+# NeedsCompilation: yes
+# Packaged: 2016-10-21 19:43:35 UTC; hadley
+# Repository: CRAN
+# Date/Publication: 2016-10-22 12:17:37
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-reticulate-feedstock contains modified content
diff --git a/r-reticulate-feedstock/recipe/meta.yaml b/r-reticulate-feedstock/recipe/meta.yaml
index f22274c..ac2fca6 100644
--- a/r-reticulate-feedstock/recipe/meta.yaml
+++ b/r-reticulate-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '0.7' %}
+{% set version = '1.2' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,36 +12,52 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/reticulate_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/reticulate/reticulate_{{ version }}.tar.gz
-  sha256: e1a4740011853f7bc3a76dc868a239c2936e904fa99522b7d04c6e0bdd8df7cb
+  sha256: 07dcf7adf42b5f88dc205fa0b68624845d93582082bbcc613e17b329d76febc0
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: testthat, knitr
 requirements:
   build:
     - r-base
-    - r-rcpp
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{ compiler('cxx') }}      # [not win]
+    - {{native}}toolchain        # [win]
+    - r-rcpp >=0.12.7
+    - r-jsonlite
 
   run:
     - r-base
-    - r-rcpp
+    - {{native}}gcc-libs         # [win]
+    - r-rcpp >=0.12.7
+    - r-jsonlite
 
 test:
   commands:
-    - $R -e "library('reticulate')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('reticulate')"           # [not win]
     - "\"%R%\" -e \"library('reticulate')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/rstudio/reticulate
-  license: Apache 2.0
+  license: Apache License 2.0
   summary: R interface to Python modules, classes, and functions. When calling into Python R
     data types are automatically converted to their equivalent Python types. When values
     are returned from Python to R they are converted back to R types. Compatible with
@@ -52,3 +68,35 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: reticulate
+# Type: Package
+# Title: R Interface to Python
+# Version: 1.2
+# Authors@R: c( person("JJ", "Allaire", role = c("aut", "cre"), email = "jj@rstudio.com"), person("Kevin", "Ushey", role = c("ctb"), email = "kevin@rstudio.com"), person("RStudio", role = c("cph", "fnd")), person("Yuan", "Tang", role = c("aut", "cph"), email = "terrytangyuan@gmail.com"), person("Dirk", "Eddelbuettel", role = c("ctb", "cph"), email = "edd@debian.org"), person("Bryan", "Lewis", role = c("ctb", "cph"), email = "blewis@illposed.net"), person("Marcus", "Geelnard", role = c("aut", "cph"), comment = "TinyThread library, http://tinythreadpp.bitsnbites.eu/") )
+# Description: R interface to Python modules, classes, and functions. When calling into Python R data types are automatically converted to their equivalent Python types. When values are returned from Python to R they are converted back to R types. Compatible with all versions of Python >= 2.7.
+# License: Apache License 2.0
+# URL: https://github.com/rstudio/reticulate
+# BugReports: https://github.com/rstudio/reticulate/issues
+# SystemRequirements: Python (>= 2.7.0)
+# Encoding: UTF-8
+# LazyData: true
+# Depends: R (>= 3.0)
+# Imports: utils, graphics, jsonlite, Rcpp (>= 0.12.7)
+# Suggests: testthat, knitr
+# LinkingTo: Rcpp
+# RoxygenNote: 6.0.1
+# VignetteBuilder: knitr
+# NeedsCompilation: yes
+# Packaged: 2017-09-26 10:12:29 UTC; jjallaire
+# Author: JJ Allaire [aut, cre], Kevin Ushey [ctb], RStudio [cph, fnd], Yuan Tang [aut, cph], Dirk Eddelbuettel [ctb, cph], Bryan Lewis [ctb, cph], Marcus Geelnard [aut, cph] (TinyThread library, http://tinythreadpp.bitsnbites.eu/)
+# Maintainer: JJ Allaire <jj@rstudio.com>
+# Repository: CRAN
+# Date/Publication: 2017-09-27 09:10:30 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-rex-feedstock contains modified content
diff --git a/r-rex-feedstock/recipe/meta.yaml b/r-rex-feedstock/recipe/meta.yaml
index fac7f22..88d4266 100644
--- a/r-rex-feedstock/recipe/meta.yaml
+++ b/r-rex-feedstock/recipe/meta.yaml
@@ -14,14 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/rex/rex_{{ version }}.tar.gz
   sha256: 6c821ed7e68ccf791b60f3d6d30a9e8081b3d8470967afda10e8404170cfd0db
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: testthat, knitr, rmarkdown, dplyr, ggplot2, lintr, Hmisc, stringr, rvest, roxygen2
 requirements:
   build:
     - r-base
@@ -35,12 +42,19 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('rex')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('rex')"           # [not win]
     - "\"%R%\" -e \"library('rex')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/kevinushey/rex
-  license: MIT
+  license: MIT + file LICENSE
   summary: A friendly interface for the construction of regular expressions.
   license_family: MIT
 
@@ -48,3 +62,31 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: rex
+# Type: Package
+# Title: Friendly Regular Expressions
+# Version: 1.1.1
+# Authors@R: c( person("Kevin", "Ushey", email = "kevinushey@gmail.com", role = "aut"), person("Jim", "Hester", email = "james.f.hester@gmail.com", role = c("aut", "cre")), person("Robert", "Krzyzanowski", email = "rkrzyzanowski@gmail.com", role = "aut") )
+# Maintainer: Jim Hester <james.f.hester@gmail.com>
+# URL: https://github.com/kevinushey/rex
+# BugReports: https://github.com/kevinushey/rex/issues
+# Description: A friendly interface for the construction of regular expressions.
+# Imports: magrittr, lazyeval
+# License: MIT + file LICENSE
+# Suggests: testthat, knitr, rmarkdown, dplyr, ggplot2, lintr, Hmisc, stringr, rvest, roxygen2
+# VignetteBuilder: knitr
+# Collate: 'aaa.R' 'utils.R' 'escape.R' 'capture.R' 'character_class.R' 'counts.R' 'lookarounds.R' 'match.R' 'or.R' 'rex-mode.R' 'rex.R' 'shortcuts.R' 'wildcards.R' 'zzz.R'
+# RoxygenNote: 5.0.1.9000
+# NeedsCompilation: no
+# Packaged: 2016-12-05 15:10:53 UTC; hornik
+# Author: Kevin Ushey [aut], Jim Hester [aut, cre], Robert Krzyzanowski [aut]
+# Repository: CRAN
+# Date/Publication: 2016-12-05 18:28:48
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-rgexf-feedstock contains modified content
diff --git a/r-rgexf-feedstock/recipe/meta.yaml b/r-rgexf-feedstock/recipe/meta.yaml
index f9e9c7b..260a056 100644
--- a/r-rgexf-feedstock/recipe/meta.yaml
+++ b/r-rgexf-feedstock/recipe/meta.yaml
@@ -14,40 +14,53 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/rgexf/rgexf_{{ version }}.tar.gz
   sha256: 2e8a7978d1fb977318e6310ba65b70a9c8890185c819a7951ac23425c6dc8147
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  # r-igraph is not available under Windows
-  skip: true  # [win]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
     - r-rook
     - r-xml
     - r-igraph
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-rook
     - r-xml
     - r-igraph
-    - libgcc  # [not win]
 
 test:
   commands:
-    - $R -e "library('rgexf')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('rgexf')"           # [not win]
     - "\"%R%\" -e \"library('rgexf')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://bitbucket.org/gvegayon/rgexf, http://www.ggvega.com
   license: GPL (>= 3)
-  summary: |
-    Create, read and write GEXF (Graph Exchange XML Format) graph files (used in Gephi
+  summary: Create, read and write GEXF (Graph Exchange XML Format) graph files (used in Gephi
     and others). Using the XML package, it allows the user to easily build/read graph
     files including attributes, GEXF viz attributes (such as color, size, and position),
     network dynamics (for both edges and nodes) and edge weighting. Users can build/handle
@@ -60,3 +73,28 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: rgexf
+# Type: Package
+# Encoding: UTF-8
+# Title: Build, Import and Export GEXF Graph Files
+# Version: 0.15.3
+# Date: 2015-03-24
+# Author: George Vega Yon <gvegayon@caltech.edu> [aut], Jorge FAbrega Lacoa <jorge.fabrega@uai.cl> [ctb], Joshua B. Kunst <jbkunst@gmail.com> [ctb]
+# Maintainer: George Vega Yon <gvegayon@caltech.edu>
+# Description: Create, read and write GEXF (Graph Exchange XML Format) graph files (used in Gephi and others). Using the XML package, it allows the user to easily build/read graph files including attributes, GEXF viz attributes (such as color, size, and position), network dynamics (for both edges and nodes) and edge weighting. Users can build/handle graphs element-by-element or massively through data-frames, visualize the graph on a web browser through "sigmajs" (a javascript library) and interact with the igraph package.
+# URL: http://bitbucket.org/gvegayon/rgexf, http://www.ggvega.com
+# Depends: XML, Rook, igraph
+# License: GPL (>= 3)
+# LazyLoad: yes
+# Packaged: 2015-03-24 15:40:34 UTC; george
+# NeedsCompilation: yes
+# Repository: CRAN
+# Date/Publication: 2015-03-24 18:27:03
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-rgl-feedstock contains modified content
diff --git a/r-rgl-feedstock/recipe/meta.yaml b/r-rgl-feedstock/recipe/meta.yaml
index 7fb75ae..9cdc59c 100644
--- a/r-rgl-feedstock/recipe/meta.yaml
+++ b/r-rgl-feedstock/recipe/meta.yaml
@@ -14,44 +14,87 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/rgl/rgl_{{ version }}.tar.gz
   sha256: 5f49bed9e092e672f73c8a1a5365cdffcda06db0315ac087e95ab9c9c71a6986
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  # libglu and/or X-server not available for windows and osx
-  skip: true  # [win or osx]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: MASS, rmarkdown, deldir, orientlib, lattice, misc3d, rstudioapi, magick
 requirements:
   build:
     - r-base
+    - {{ compiler('c') }}                # [not win]
+    - {{ compiler('cxx') }}              # [not win]
+    - {{native}}toolchain                # [win]
+    - {{ cdt('xorg-x11-proto-devel') }}  # [linux]
+    - {{ cdt('mesa-libgl-devel') }}      # [linux]
+    - {{ cdt('libx11-devel') }}          # [linux]
+    - {{ cdt('libxext-devel') }}         # [linux]
+    - {{ cdt('libxrender-devel') }}      # [linux]
+    - {{ cdt('mesa-libgl-devel') }}      # [linux]
+    - {{ cdt('mesa-libegl-devel') }}     # [linux]
+    - {{ cdt('mesa-dri-drivers') }}      # [linux]
+    - {{ cdt('libxau-devel') }}          # [linux]
+    - {{ cdt('libdrm-devel') }}          # [linux]
+    - {{ cdt('libxcomposite-devel') }}   # [linux]
+    - {{ cdt('libxcursor-devel') }}      # [linux]
+    - {{ cdt('libxi-devel') }}           # [linux]
+    - {{ cdt('libxrandr-devel') }}       # [linux]
+    - {{ cdt('libxscrnsaver-devel') }}   # [linux]
+    - {{ cdt('libxtst-devel') }}         # [linux]
+    - {{ cdt('libselinux-devel') }}      # [linux]
+    - {{ cdt('libxdamage') }}            # [linux]
+    - {{ cdt('libxfixes') }}             # [linux]
+    - {{ cdt('libxxf86vm') }}            # [linux]
+    - expat                              # [linux]
+    - libglu                             # [linux]
+    - {{posix}}filesystem                # [win]
+    - {{posix}}autoconf
+    - {{posix}}automake
+    - {{posix}}pkg-config
+    - {{posix}}make
+    - libxcb                             # [linux]
     - r-htmltools
     - r-htmlwidgets
     - r-jsonlite >=0.9.20
     - r-knitr
     - r-magrittr
     - r-shiny
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
-    - libglu               # [linux]
 
   run:
     - r-base
+    - {{native}}gcc-libs                 # [win]
+    - libxcb                             # [linux]
+    - expat                              # [linux]
+    - libglu                             # [linux]
     - r-htmltools
     - r-htmlwidgets
     - r-jsonlite >=0.9.20
     - r-knitr
     - r-magrittr
     - r-shiny
-    - libgcc  # [not win]
-    - libglu  # [linux]
 
 test:
   commands:
-    - $R -e "library('rgl')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('rgl')"           # [not win]
     - "\"%R%\" -e \"library('rgl')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://r-forge.r-project.org/projects/rgl/
   license: GPL
@@ -66,3 +109,29 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: rgl
+# Version: 0.98.1
+# Title: 3D Visualization Using OpenGL
+# Author: Daniel Adler <dadler@uni-goettingen.de>, Duncan Murdoch <murdoch@stats.uwo.ca>, and others (see README)
+# Maintainer: Duncan Murdoch <murdoch@stats.uwo.ca>
+# Depends: R (>= 3.2.0)
+# Suggests: MASS, rmarkdown, deldir, orientlib, lattice, misc3d, rstudioapi, magick
+# Imports: graphics, grDevices, stats, utils, htmlwidgets, htmltools, knitr, jsonlite (>= 0.9.20), shiny, magrittr
+# Description: Provides medium to high level functions for 3D interactive graphics, including functions modelled on base graphics (plot3d(), etc.) as well as functions for constructing representations of geometric objects (cube3d(), etc.).  Output may be on screen using OpenGL, or to various standard 3D file formats including WebGL, PLY, OBJ, STL as well as 2D image formats, including PNG, Postscript, SVG, PGF.
+# License: GPL
+# URL: https://r-forge.r-project.org/projects/rgl/
+# SystemRequirements: OpenGL, GLU Library, XQuartz (on OSX), zlib (optional), libpng (>=1.2.9, optional), FreeType (optional), pandoc (>=1.13.1, needed for vignettes)
+# BugReports: https://r-forge.r-project.org/projects/rgl/
+# VignetteBuilder: knitr
+# NeedsCompilation: yes
+# Packaged: 2017-03-08 10:19:55 UTC; murdoch
+# Repository: CRAN
+# Date/Publication: 2017-03-08 19:22:56
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
diff --git a/r-rhive-feedstock/recipe/meta.yaml b/r-rhive-feedstock/recipe/meta.yaml
index fc9b688..6efd499 100644
--- a/r-rhive-feedstock/recipe/meta.yaml
+++ b/r-rhive-feedstock/recipe/meta.yaml
@@ -1,43 +1,38 @@
+{% set version = '2.0-0.10' %}
+
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
 
 package:
   name: r-rhive
-  # Note that conda versions cannot contain -, so any -'s in the version have
-  # been replaced with _'s.
-  version: "2.0_0.10"
+  version: {{ version|replace("-", "_") }}
 
 source:
 
 
+  # You can add a hash for the file here, like md5, sha1 or sha256
   git_url: https://github.com/nexr/RHive
   git_tag: nexr-rhive-2.0.10
-  # You can add a hash for the file here, like md5 or sha1
-  # md5: 49448ba4863157652311cc5ea4fea3ea
-  # sha1: 3bcfbee008276084cbb37a2b453963c61176a322
   # patches:
    # List any patch files here
    # - fix.patch
 
 build:
-  # If this is a new build for the same version, increment the build
-  # number. If you do not include this key, it defaults to 0.
-  # number: 1
+  # If this is a new build for the same version, increment the build number.
+  number: 0
 
   # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
-
   script_env:
     - JAVA_HOME
-
 # Suggests: RUnit
 requirements:
   build:
     - r-base
-    - posix               # [win]
-    - {{posix}}git        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}git
     - r-rserve >=0.6_0
     - r-rjava >=0.9_0
 
@@ -49,8 +44,8 @@ requirements:
 test:
   commands:
     # You can put additional test commands to be run here.
-    - $R -e "library('RHive')" # [not win]
-    - "\"%R%\" -e \"library('RHive')\"" # [win]
+    - $R -e "library('RHive')"           # [not win]
+    - "\"%R%\" -e \"library('RHive')\""  # [win]
 
   # You can also put a file called run_test.py, run_test.sh, or run_test.bat
   # in the recipe that will be run at test time.
@@ -59,11 +54,15 @@ test:
     # Put any additional test requirements here.
 
 about:
-  #home:
+  home: https://github.com/nexr/RHive
   license: Apache License (== 2.0)
   summary: RHive is an R extension facilitating distributed computing via HIVE query. It provides
     an easy to use HQL like SQL and R objects and functions in HQL.
-  license_family: Apache
+  license_family: APACHE
+
+extra:
+  recipe-maintainers:
+    - mingwandroid
 
 # The original CRAN metadata for this package was:
 
Submodule r-rjava-feedstock ab9241b..afdbb3c:
diff --git a/r-rjava-feedstock/recipe/0001-Improve-lib-jvm.-so-dylib-dll-loading.patch b/r-rjava-feedstock/recipe/0001-Improve-lib-jvm.-so-dylib-dll-loading.patch
new file mode 100644
index 0000000..1a7f8e5
--- /dev/null
+++ b/r-rjava-feedstock/recipe/0001-Improve-lib-jvm.-so-dylib-dll-loading.patch
@@ -0,0 +1,103 @@
+From 9bf634515a5d28e70532ef2b0d1d96cd79767873 Mon Sep 17 00:00:00 2001
+From: Ray Donnelly <mingw.android@gmail.com>
+Date: Thu, 8 Jun 2017 15:42:06 +0100
+Subject: [PATCH] Improve {lib,}jvm.{so,dylib,dll} loading
+
+Use the values from  Makeconf to find java (Unix), falling back to
+JAVA_HOME (which is needed on Windows). Add the directory to PATH
+on Windows or dyn.load the shared library it on Linux. macOS is
+kept largely the same as it was except we check Makeconf before
+JAVA_HOME.
+
+macOS needs to use a stub dylib instead, e.g.:
+
+ld -reexport_library /System/Library/Frameworks/JavaVM.framework/Versions/A/JavaVM -o ${CONDA_PREFIX}/lib/libjvm.dylib -dylib -macosx_version_min 10.9 -arch x86_64
+
+.. then rJava needs to link to that, then activation and deactivation of
+environments containing Java need to change the target via install_name_tool
+(or something that does the same thing). This will be painful though.
+---
+ R/zzz.R.in | 66 ++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++--
+ 1 file changed, 64 insertions(+), 2 deletions(-)
+
+diff --git a/R/zzz.R.in b/R/zzz.R.in
+index eb8aba7..f00e1bd 100644
+--- a/R/zzz.R.in
++++ b/R/zzz.R.in
+@@ -1,9 +1,71 @@
++getMakeconfVar <- function(makeconf, var) {
++  # With some assistance from:
++  # https://github.com/r-hub/sysreqs/blob/master/R/platform.R
++  result <- grep(paste("^", var, "[ ]?=",sep=''), makeconf, value = TRUE)
++  sub(paste("^", var, "[ ]?=[ ]?", sep=''), "", result)
++}
++
+ .onLoad <- function(libname, pkgname) {
+-  Sys.setenv("LD_LIBRARY_PATH"=paste(Sys.getenv("LD_LIBRARY_PATH"),"@JAVA_LD@",sep=':'))
++  # Conda refuses to mess with LD_LIBRARY_PATH (etc/ldconfig does not do that now)
++  # as it is the Linux equivalent of DLL-hell and has caused real problems with
++  # R, instead we pre-load libjvm.so from the location detected by javareconf.
++  Makeconf <- readLines(file.path(R.home(), "etc", Sys.getenv('R_ARCH'), "Makeconf"))
++  dylib_ext <- getMakeconfVar(Makeconf, "DYLIB_EXT")
++  if (length(grep("^mingw", R.version$os))) {
++    exe_ext <- ".exe"
++    lib_prefix <- ""
++  } else {
++    exe_ext <- ""
++    lib_prefix <- "lib"
++  }
++  jvm_fname = paste(lib_prefix, "jvm", dylib_ext, sep='')
++  jh <- getMakeconfVar(Makeconf, "JAVA_HOME")
++  j_ld_lp <- gsub("$(JAVA_HOME)", jh, getMakeconfVar(Makeconf, "JAVA_LD_LIBRARY_PATH"), fixed=TRUE)
++  jvm <- file.path(j_ld_lp, jvm_fname)
++  if (!length(j_ld_lp) || !file.exists(jvm)) {
++    if (!length(jh)) {
++      # In this case (which will always happen on Windows) fall back to JAVA_HOME
++      jh <- Sys.getenv("JAVA_HOME")
++      java_exe <- file.path(jh, 'bin', paste('java', exe_ext, sep=''))
++      Javaconf <- system2(java_exe, args=c("-XshowSettings:properties", "-version"), stderr = TRUE, stdout = TRUE, minimized = TRUE)
++      j_ld_lp <- grep(' *sun\\.boot\\.library\\.path[ ]?=', Javaconf, perl = TRUE, value = TRUE)
++      j_ld_lp <- sub(' *sun\\.boot\\.library\\.path[ ]?=[ ]?', "", j_ld_lp)
++      if (file.exists(file.path(j_ld_lp, "server", jvm_fname))) {
++        j_ld_lp <- file.path(j_ld_lp, "server")
++      } else if (file.exists(file.path(j_ld_lp, "client", jvm_fname))) {
++        j_ld_lp <- file.path(j_ld_lp, "client")
++      }
++    }
++  }
++  jvm <- file.path(j_ld_lp, jvm_fname)
++
++  if (length(grep("^darwin", R.version$os)))  {
++    # NYI, need to re-create the dylib stub to point to the real JVM dylib
++  } else if (length(grep("^linux", R.version$os))) {
++    if (file.exists(jvm)) {
++      dyn.load(jvm, local=FALSE, now=TRUE)
++    } else {
++      # This is what the code originally did. Kept as a last resort only, but if it is run
++      # then something bad has happened.
++      Sys.setenv("LD_LIBRARY_PATH"=paste(Sys.getenv("LD_LIBRARY_PATH"),"@JAVA_LD@",sep=':'))
++    }
++  } else if (length(grep("^mingw", R.version$os))) {
++    # When all you have is a hammer. If the dir is not on PATH then add it.
++    # It would be nice here to find the first jvm.dll on PATH and if that
++    # entry comes before our directory (or our directory is not on PATH) then
++    # re-arrange the entries so that ours appears before it. Overkill though?
++    win_j_ld_lp <- gsub("/", "\\\\", j_ld_lp)
++    split_path <- strsplit(Sys.getenv("PATH"), ";")
++    if (!win_j_ld_lp %in% unlist(split_path)) {
++      if (file.exists(win_j_ld_lp) && file.info(win_j_ld_lp)$isdir == TRUE) {
++        Sys.setenv("PATH"=paste(win_j_ld_lp, Sys.getenv("PATH"), sep=';'))
++      }
++    }
++  }
++
+   ## On OS X with Oracle Java we may need to work around Oracle bug:
+   ## https://bugs.openjdk.java.net/browse/JDK-7131356
+   if (length(grep("^darwin", R.version$os)) && file.exists("/usr/libexec/java_home")) {
+-     jh <- Sys.getenv("JAVA_HOME")
+      if (!nzchar(jh)) jh <- system("/usr/libexec/java_home", intern=TRUE)[1L]
+      if (file.exists(file.path(jh, "jre/lib"))) jh <- file.path(jh, "jre")
+      if (file.exists(jli <- file.path(jh, "lib/jli/libjli.dylib"))) {
+-- 
+2.11.1
+
diff --git a/r-rjava-feedstock/recipe/bld.bat b/r-rjava-feedstock/recipe/bld.bat
index 227a2d0..8b10c94 100644
--- a/r-rjava-feedstock/recipe/bld.bat
+++ b/r-rjava-feedstock/recipe/bld.bat
@@ -1,2 +1,8 @@
 "%R%" CMD INSTALL --build .
 if errorlevel 1 exit 1
+
+@rem Add more build steps here, if they are necessary.
+
+@rem See
+@rem http://docs.continuum.io/conda/build.html
+@rem for a list of environment variables that are set during the build process.
diff --git a/r-rjava-feedstock/recipe/build.sh b/r-rjava-feedstock/recipe/build.sh
index 4bc8e18..b6a05e8 100644
--- a/r-rjava-feedstock/recipe/build.sh
+++ b/r-rjava-feedstock/recipe/build.sh
@@ -1,3 +1,38 @@
 #!/bin/bash
 
+# All this (and it is fairly brittle) just to get the
+# value for JAVA_LD_LIBRARY_PATH as determined by:
+# R CMD javareconf
+# Find Java in the compiler's sysroot
+if [[ ${HOST} =~ .*linux.* ]]; then
+  export JAVAC=$(find ${PREFIX}/${HOST}/sysroot -path "*/bin/javac")
+  export JAVAH=$(find ${PREFIX}/${HOST}/sysroot -path "*/bin/javah")
+  export JAVA=${JAVAC: : -1}
+  export JAR=$(find ${PREFIX}/${HOST}/sysroot -path "*/bin/jar")
+  # export JAVA_HOME=$(dirname $(dirname ${JAVA}))
+fi
+echo 'echo $JAVA_LD_LIBRARY_PATH' > $PWD/java_reconf
+chmod +x $PWD/java_reconf
+JAVA_LD_LIBRARY_PATH=$($R CMD javareconf -e $PWD/java_reconf | tail -n1)
+echo "JAVA_LD_LIBRARY_PATH as determined by R CMD javareconf: $JAVA_LD_LIBRARY_PATH"
+
+# Now re-configure to set the correct values.
+R CMD javareconf
+
+if [[ $(uname) == Linux ]]; then
+  export LD_LIBRARY_PATH=${JAVA_LD_LIBRARY_PATH}:${LD_LIBRARY_PATH}
+elif [[ $(uname) == Darwin ]]; then
+  export DYLD_FALLBACK_LIBRARY_PATH=${JAVA_LD_LIBRARY_PATH}:${DYLD_FALLBACK_LIBRARY_PATH}
+fi
+
+# R refuses to build packages that mark themselves as Priority: Recommended
+mv DESCRIPTION DESCRIPTION.old
+grep -v '^Priority: ' DESCRIPTION.old > DESCRIPTION
+
 $R CMD INSTALL --build .
+
+# Add more build steps here, if they are necessary.
+
+# See
+# http://docs.continuum.io/conda/build.html
+# for a list of environment variables that are set during the build process.
diff --git a/r-rjava-feedstock/recipe/meta.yaml b/r-rjava-feedstock/recipe/meta.yaml
index 5d9c447..cb1f406 100644
--- a/r-rjava-feedstock/recipe/meta.yaml
+++ b/r-rjava-feedstock/recipe/meta.yaml
@@ -12,32 +12,54 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/rJava_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/rJava/rJava_{{ version }}.tar.gz
+
+
   sha256: dada5e031414da54eb80b9024d51866c20b92d41d68da65789fe0130bc54bd8a
+  patches:
+    - 0001-Improve-lib-jvm.-so-dylib-dll-loading.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+  script_env:
+    - JAVA_HOME
+
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
-    - openjdk
+    - {{ compiler('c') }}                # [not win]
+    - {{native}}toolchain                # [win]
+    - {{posix}}curl                      # [win]
+    - {{posix}}filesystem                # [win]
+    - {{posix}}autoconf
+    - {{posix}}automake
+    - {{posix}}pkg-config
+    - {{posix}}make
+    - {{ cdt('java-1.7.0-openjdk') }}        # [linux]
+    - {{ cdt('java-1.7.0-openjdk-devel') }}  # [linux]
 
   run:
     - r-base
-    - openjdk
+    - {{native}}gcc-libs                 # [win]
 
 test:
   commands:
-    - $R -e "library('rJava')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('rJava')"           # [not win]
     - "\"%R%\" -e \"library('rJava')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://www.rforge.net/rJava/
   license: GPL-2
@@ -47,6 +69,28 @@ about:
 
 extra:
   recipe-maintainers:
-    - johanneskoester
     - bgruening
+    - johanneskoester
+    - mingwandroid
     - sodre
+
+# The original CRAN metadata for this package was:
+
+# Package: rJava
+# Version: 0.9-8
+# Title: Low-Level R to Java Interface
+# Author: Simon Urbanek <simon.urbanek@r-project.org>
+# Maintainer: Simon Urbanek <simon.urbanek@r-project.org>
+# Depends: R (>= 2.5.0), methods
+# Description: Low-level interface to Java VM very much like .C/.Call and friends. Allows creation of objects, calling methods and accessing fields.
+# License: GPL-2
+# URL: http://www.rforge.net/rJava/
+# SystemRequirements: Java JDK 1.2 or higher (for JRI/REngine JDK 1.4 or higher), GNU make
+# NeedsCompilation: yes
+# Packaged: 2015-11-16 02:41:10 UTC; svnuser
+# Repository: CRAN
+# Date/Publication: 2016-01-07 15:09:40
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-rjson-feedstock contains modified content
diff --git a/r-rjson-feedstock/recipe/meta.yaml b/r-rjson-feedstock/recipe/meta.yaml
index 0e4afe6..16ba0aa 100644
--- a/r-rjson-feedstock/recipe/meta.yaml
+++ b/r-rjson-feedstock/recipe/meta.yaml
@@ -12,15 +12,18 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/rjson_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/rjson/rjson_{{ version }}.tar.gz
+  sha256: 77d00d8f6a1c936329b46f3b8b0be79a165f8c5f1989497f942ecc53dcf6f2ef
 
 
-  sha256: 77d00d8f6a1c936329b46f3b8b0be79a165f8c5f1989497f942ecc53dcf6f2ef
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: True  # [win32]
-
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
@@ -29,19 +32,25 @@ build:
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('cxx') }}      # [not win]
+    - {{native}}toolchain        # [win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('rjson')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('rjson')"           # [not win]
     - "\"%R%\" -e \"library('rjson')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
 
+  # requires:
+    # Put any additional test requirements here.
 
 about:
   home: https://CRAN.R-project.org/package=rjson
@@ -49,8 +58,27 @@ about:
   summary: Converts R object into JSON objects and vice-versa
   license_family: GPL2
 
-
-
 extra:
   recipe-maintainers:
     - croth1
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: rjson
+# Version: 0.2.15
+# Date: 2014-11-02
+# Title: JSON for R
+# Author: Alex Couture-Beil <rjson_pkg@mofo.ca>
+# Maintainer: Alex Couture-Beil <rjson_pkg@mofo.ca>
+# Depends: R (>= 3.1.0)
+# Description: Converts R object into JSON objects and vice-versa
+# License: GPL-2
+# Repository: CRAN
+# Date/Publication: 2014-11-03 00:35:00
+# Packaged: 2014-11-02 21:27:41 UTC; alex
+# NeedsCompilation: yes
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-rjsonio-feedstock contains modified content
diff --git a/r-rjsonio-feedstock/recipe/meta.yaml b/r-rjsonio-feedstock/recipe/meta.yaml
index ae6736a..bc7da15 100644
--- a/r-rjsonio-feedstock/recipe/meta.yaml
+++ b/r-rjsonio-feedstock/recipe/meta.yaml
@@ -12,39 +12,54 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/RJSONIO_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/RJSONIO/RJSONIO_{{ version }}.tar.gz
+  sha256: 119334b7761c6c1c3cec52fa17dbc1b72eaebb520c53e68d873dea147cf48fb7
 
 
-  sha256: 119334b7761c6c1c3cec52fa17dbc1b72eaebb520c53e68d873dea147cf48fb7
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
-  skip: True  # [win32]
 
 
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('cxx') }}      # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}autoconf
+    - {{posix}}automake
+    - {{posix}}pkg-config
+    - {{posix}}make
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('RJSONIO')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('RJSONIO')"           # [not win]
     - "\"%R%\" -e \"library('RJSONIO')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
 
+  # requires:
+    # Put any additional test requirements here.
 
 about:
   home: https://CRAN.R-project.org/package=RJSONIO
-  license: BSD_3_clause
+  license: BSD_3_clause + file LICENSE
   summary: This is a package that allows conversion to and from  data in Javascript object notation
     (JSON) format. This allows R objects to be inserted into Javascript/ECMAScript/ActionScript
     code and allows R programmers to read and convert JSON content to R objects. This
@@ -59,10 +74,30 @@ about:
     JSON parser. The aim is to support other general projects by building on their work,
     providing feedback and benefit from their ongoing development.
   license_family: BSD
-  license_file: LICENSE
-
-
 
 extra:
   recipe-maintainers:
     - croth1
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: RJSONIO
+# Version: 1.3-0
+# Title: Serialize R objects to JSON, JavaScript Object Notation
+# Description: This is a package that allows conversion to and from  data in Javascript object notation (JSON) format. This allows R objects to be inserted into Javascript/ECMAScript/ActionScript code and allows R programmers to read and convert JSON content to R objects. This is an alternative to rjson package. Originally, that was too slow for converting large R objects to JSON and was not extensible.  rjson's performance is now similar to this package, and perhaps slightly faster in some cases. This package uses methods and is readily extensible by defining methods for different classes,  vectorized operations, and C code and callbacks to R functions for deserializing JSON objects to R.  The two packages intentionally share the same basic interface. This package (RJSONIO) has many additional options to allow customizing the generation and processing of JSON content. This package uses libjson rather than implementing yet another JSON parser. The aim is to support other general projects by building on their work, providing feedback and benefit from their ongoing development.
+# Note: See http://www.json.org/JSON_checker/test.zip for tests. We now use code from the libjson project (http://libjson.sourceforge.net).
+# License: BSD_3_clause + file LICENSE
+# Imports: methods
+# Author: Duncan Temple Lang
+# Maintainer: Duncan Temple Lang <duncan@r-project.org>
+# Collate: readJSON.R asVars.R isValid.R json.R simpleHandler.R stream.R
+# Biarch: true
+# Packaged: 2014-07-28 15:19:10 UTC; ripley
+# NeedsCompilation: yes
+# Repository: CRAN
+# Date/Publication: 2014-07-28 17:26:19
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-rlang-feedstock contains modified content
diff --git a/r-rlang-feedstock/recipe/meta.yaml b/r-rlang-feedstock/recipe/meta.yaml
index 3ea6191..f110d65 100644
--- a/r-rlang-feedstock/recipe/meta.yaml
+++ b/r-rlang-feedstock/recipe/meta.yaml
@@ -14,29 +14,43 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/rlang/rlang_{{ version }}.tar.gz
   sha256: 90cfcd88cae6fff044fca64b24a8e6bdc09fc276163b518ff2d90268b0c785f9
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: knitr, rmarkdown (>= 0.2.65), testthat, covr
 requirements:
   build:
-    - {{ compiler('c') }}  # [not win]
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
 
   run:
     - r-base
-    - {{native}}gcc-libs   # [win]
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('rlang')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('rlang')"           # [not win]
     - "\"%R%\" -e \"library('rlang')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://rlang.tidyverse.org, https://github.com/tidyverse/rlang
   license: GPL-3
@@ -48,3 +62,30 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: rlang
+# Version: 0.1.2
+# Title: Functions for Base Types and Core R and 'Tidyverse' Features
+# Description: A toolbox for working with base types, core R features like the condition system, and core 'Tidyverse' features like tidy evaluation.
+# Authors@R: c( person("Lionel", "Henry", ,"lionel@rstudio.com", c("aut", "cre")), person("Hadley", "Wickham", ,"hadley@rstudio.com", "aut"), person("RStudio", role = "cph") )
+# License: GPL-3
+# LazyData: true
+# Depends: R (>= 3.1.0)
+# Suggests: knitr, rmarkdown (>= 0.2.65), testthat, covr
+# VignetteBuilder: knitr
+# RoxygenNote: 6.0.1
+# URL: http://rlang.tidyverse.org, https://github.com/tidyverse/rlang
+# BugReports: https://github.com/tidyverse/rlang/issues
+# NeedsCompilation: yes
+# Packaged: 2017-08-09 16:20:26 UTC; lionel
+# Author: Lionel Henry [aut, cre], Hadley Wickham [aut], RStudio [cph]
+# Maintainer: Lionel Henry <lionel@rstudio.com>
+# Repository: CRAN
+# Date/Publication: 2017-08-09 20:37:03 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-rlist-feedstock contains modified content
diff --git a/r-rlist-feedstock/recipe/meta.yaml b/r-rlist-feedstock/recipe/meta.yaml
index 7fb434d..a75475f 100644
--- a/r-rlist-feedstock/recipe/meta.yaml
+++ b/r-rlist-feedstock/recipe/meta.yaml
@@ -14,14 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/rlist/rlist_{{ version }}.tar.gz
   sha256: bb0148b124997aa4e1c89c6c7cf45927afaf5b23caefcdc5116cfa7caef05c21
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: testthat, stringdist, pipeR
 requirements:
   build:
     - r-base
@@ -39,12 +46,19 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('rlist')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('rlist')"           # [not win]
     - "\"%R%\" -e \"library('rlist')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://renkun.me/rlist, https://github.com/renkun-ken/rlist, https://renkun.me/rlist-tutorial
-  license: MIT
+  license: MIT + file LICENSE
   summary: Provides a set of functions for data manipulation with list objects, including mapping,
     filtering, grouping, sorting, updating, searching, and other useful functions. Most
     functions are designed to be pipeline friendly so that data processing with lists
@@ -55,3 +69,32 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: rlist
+# Type: Package
+# Title: A Toolbox for Non-Tabular Data Manipulation
+# Version: 0.4.6.1
+# Author: Kun Ren <ken@renkun.me>
+# Maintainer: Kun Ren <ken@renkun.me>
+# Description: Provides a set of functions for data manipulation with list objects, including mapping, filtering, grouping, sorting, updating, searching, and other useful functions. Most functions are designed to be pipeline friendly so that data processing with lists can be chained.
+# Depends: R (>= 2.15)
+# Date: 2016-04-04
+# Suggests: testthat, stringdist, pipeR
+# Imports: yaml, jsonlite, XML, data.table
+# License: MIT + file LICENSE
+# URL: https://renkun.me/rlist, https://github.com/renkun-ken/rlist, https://renkun.me/rlist-tutorial
+# BugReports: https://github.com/renkun-ken/rlist/issues
+# ByteCompile: TRUE
+# LazyData: true
+# RoxygenNote: 5.0.1
+# NeedsCompilation: no
+# Packaged: 2016-04-04 08:55:10 UTC; Kun
+# Repository: CRAN
+# Date/Publication: 2016-04-04 11:49:36
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-rmarkdown-feedstock contains modified content
diff --git a/r-rmarkdown-feedstock/recipe/meta.yaml b/r-rmarkdown-feedstock/recipe/meta.yaml
index ca3ab6a..11bef6c 100644
--- a/r-rmarkdown-feedstock/recipe/meta.yaml
+++ b/r-rmarkdown-feedstock/recipe/meta.yaml
@@ -14,14 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/rmarkdown/rmarkdown_{{ version }}.tar.gz
   sha256: 8765875573a29622c822c6a6ab0c93afe418b49bf7aec0ba0dd3c55b7e81175d
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 2
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: shiny (>= 0.11), tufte, testthat, digest, tibble
 requirements:
   build:
     - r-base
@@ -36,7 +43,6 @@ requirements:
     - r-yaml >=2.1.5
 
   run:
-    - pandoc >=1.12.3
     - r-base
     - r-base64enc
     - r-catools
@@ -50,9 +56,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('rmarkdown')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('rmarkdown')"           # [not win]
     - "\"%R%\" -e \"library('rmarkdown')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://rmarkdown.rstudio.com
   license: GPL-3
@@ -65,3 +78,30 @@ extra:
     - bgruening
     - mdehollander
     - jdblischak
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: rmarkdown
+# Type: Package
+# Title: Dynamic Documents for R
+# Version: 1.6
+# Authors@R: c( person("JJ", "Allaire", role = c("aut", "cre"), email = "jj@rstudio.com"), person("Joe", "Cheng", role = "aut", email = "joe@rstudio.com"), person("Yihui", "Xie", role = "aut", email = "yihui@rstudio.com"), person("Jonathan", "McPherson", role = "aut", email = "jonathan@rstudio.com"), person("Winston", "Chang", role = "aut", email = "winston@rstudio.com"), person("Jeff", "Allen", role = "aut", email = "jeff@rstudio.com"), person("Hadley", "Wickham", role = "aut", email = "hadley@rstudio.com"), person("Aron", "Atkins", role = "aut", email = "aron@rstudio.com"), person("Rob", "Hyndman", role = "aut", email = "Rob.Hyndman@monash.edu"), person("Ruben", "Arslan", role = "aut", email = "ruben.arslan@uni-goettingen.de"), person(family = "RStudio, Inc.", role = "cph"), person(family = "jQuery Foundation", role = "cph", comment = "jQuery library"), person(family = "jQuery contributors", role = c("ctb", "cph"), comment = "jQuery library; authors listed in inst/rmd/h/jquery-AUTHORS.txt"), person(family = "jQuery UI contributors", role = c("ctb", "cph"), comment = "jQuery UI library; authors listed in inst/rmd/h/jqueryui-AUTHORS.txt"), person("Mark", "Otto", role = "ctb", comment = "Bootstrap library"), person("Jacob", "Thornton", role = "ctb", comment = "Bootstrap library"), person(family = "Bootstrap contributors", role = "ctb", comment = "Bootstrap library"), person(family = "Twitter, Inc", role = "cph", comment = "Bootstrap library"), person("Alexander", "Farkas", role = c("ctb", "cph"), comment = "html5shiv library"), person("Scott", "Jehl", role = c("ctb", "cph"), comment = "Respond.js library"), person("Ivan", "Sagalaev", role = c("ctb", "cph"), comment = "highlight.js library"), person("Greg", "Franko", role = c("ctb", "cph"), comment = "tocify library"), person("Eli", "Grey", role = c("ctb", "cph"), comment = "FileSaver library"), person("John", "MacFarlane", role = c("ctb", "cph"), comment = "Pandoc templates"), person(family = "Google, Inc.", role = c("ctb", "cph"), comment = "ioslides library"), person("Dave", "Raggett", role = "ctb", comment = "slidy library"), person(family = "W3C", role = "cph", comment = "slidy library"), person("Dave", "Gandy", role = c("ctb", "cph"), comment = "Font-Awesome"), person("Ben", "Sperry", role = "ctb", comment = "Ionicons"), person(family = "Drifty", role = "cph", comment = "Ionicons"), person("Aidan", "Lister", role = c("ctb", "cph"),  comment = "jQuery StickyTabs") )
+# Description: Convert R Markdown documents into a variety of formats.
+# Depends: R (>= 3.0)
+# Imports: tools, utils, knitr (>= 1.14), yaml (>= 2.1.5), htmltools (>= 0.3.5), caTools, evaluate (>= 0.8), base64enc, jsonlite, rprojroot, methods, stringr (>= 1.2.0)
+# Suggests: shiny (>= 0.11), tufte, testthat, digest, tibble
+# SystemRequirements: pandoc (>= 1.12.3) - http://pandoc.org
+# URL: http://rmarkdown.rstudio.com
+# License: GPL-3
+# RoxygenNote: 6.0.1
+# NeedsCompilation: no
+# Packaged: 2017-06-15 12:12:24 UTC; jjallaire
+# Author: JJ Allaire [aut, cre], Joe Cheng [aut], Yihui Xie [aut], Jonathan McPherson [aut], Winston Chang [aut], Jeff Allen [aut], Hadley Wickham [aut], Aron Atkins [aut], Rob Hyndman [aut], Ruben Arslan [aut], RStudio, Inc. [cph], jQuery Foundation [cph] (jQuery library), jQuery contributors [ctb, cph] (jQuery library; authors listed in inst/rmd/h/jquery-AUTHORS.txt), jQuery UI contributors [ctb, cph] (jQuery UI library; authors listed in inst/rmd/h/jqueryui-AUTHORS.txt), Mark Otto [ctb] (Bootstrap library), Jacob Thornton [ctb] (Bootstrap library), Bootstrap contributors [ctb] (Bootstrap library), Twitter, Inc [cph] (Bootstrap library), Alexander Farkas [ctb, cph] (html5shiv library), Scott Jehl [ctb, cph] (Respond.js library), Ivan Sagalaev [ctb, cph] (highlight.js library), Greg Franko [ctb, cph] (tocify library), Eli Grey [ctb, cph] (FileSaver library), John MacFarlane [ctb, cph] (Pandoc templates), Google, Inc. [ctb, cph] (ioslides library), Dave Raggett [ctb] (slidy library), W3C [cph] (slidy library), Dave Gandy [ctb, cph] (Font-Awesome), Ben Sperry [ctb] (Ionicons), Drifty [cph] (Ionicons), Aidan Lister [ctb, cph] (jQuery StickyTabs)
+# Maintainer: JJ Allaire <jj@rstudio.com>
+# Repository: CRAN
+# Date/Publication: 2017-06-15 18:16:52 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-rmr2-feedstock contains modified content
diff --git a/r-rmr2-feedstock/recipe/meta.yaml b/r-rmr2-feedstock/recipe/meta.yaml
index 773a8f5..5e97b38 100644
--- a/r-rmr2-feedstock/recipe/meta.yaml
+++ b/r-rmr2-feedstock/recipe/meta.yaml
@@ -23,6 +23,7 @@ build:
 
 requirements:
   build:
+    - {{ compiler('cxx') }}
     - r-base
     - r-rjsonio >=0.8_2
     - r-rcpp
@@ -45,7 +46,7 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('rmr2')"  # [not win]
+    - $R -e "library('rmr2')"           # [not win]
     - "\"%R%\" -e \"library('rmr2')\""  # [win]
 
 about:
@@ -56,5 +57,6 @@ about:
 
 extra:
   recipe-maintainers:
-    - johanneskoester
     - bgruening
+    - johanneskoester
+    - mingwandroid
diff --git a/r-rms-feedstock/recipe/meta.yaml b/r-rms-feedstock/recipe/meta.yaml
index 34ec45f..fa08cd5 100644
--- a/r-rms-feedstock/recipe/meta.yaml
+++ b/r-rms-feedstock/recipe/meta.yaml
@@ -12,9 +12,9 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/rms_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/rms/rms_{{ version }}.tar.gz
+  sha256: c489948df5c434b40bcf5288844f5b4e08d157f36939d09230c1600f88d1bfe3
 
 
-  sha256: c489948df5c434b40bcf5288844f5b4e08d157f36939d09230c1600f88d1bfe3
   # patches:
    # List any patch files here
    # - fix.patch
@@ -32,6 +32,9 @@ build:
 requirements:
   build:
     - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('fortran') }}  # [not win]
+    - {{native}}toolchain        # [win]
     - r-hmisc >=4.0_2
     - r-sparsem
     - r-ggplot2 >=2.2
@@ -44,12 +47,10 @@ requirements:
     - r-quantreg
     - r-rpart
     - r-survival >=2.40_1
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-hmisc >=4.0_2
     - r-sparsem
     - r-ggplot2 >=2.2
@@ -62,13 +63,11 @@ requirements:
     - r-quantreg
     - r-rpart
     - r-survival >=2.40_1
-    - {{native}}gcc-libs   # [win]
-    - libgcc               # [not win]
 
 test:
   commands:
     # You can put additional test commands to be run here.
-    - $R -e "library('rms')"  # [not win]
+    - $R -e "library('rms')"           # [not win]
     - "\"%R%\" -e \"library('rms')\""  # [win]
 
   # You can also put a file called run_test.py, run_test.sh, or run_test.bat
@@ -94,6 +93,10 @@ about:
 
   license_family: GPL3
 
+extra:
+  recipe-maintainers:
+    - mingwandroid
+
 # The original CRAN metadata for this package was:
 
 # Package: rms
Submodule r-rngtools-feedstock contains modified content
diff --git a/r-rngtools-feedstock/recipe/meta.yaml b/r-rngtools-feedstock/recipe/meta.yaml
index a1dafd7..358aaa4 100644
--- a/r-rngtools-feedstock/recipe/meta.yaml
+++ b/r-rngtools-feedstock/recipe/meta.yaml
@@ -14,19 +14,28 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/rngtools/rngtools_{{ version }}.tar.gz
   sha256: 27019835b750f470b13dbb7fecd3b839a61b52774e23fffa191f919533768fb9
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: parallel, RUnit, knitr
 requirements:
   build:
     - r-base
     - r-digest
     - r-pkgmaker >=0.20
     - r-stringr
+
   run:
     - r-base
     - r-digest
@@ -35,9 +44,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('rngtools')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('rngtools')"           # [not win]
     - "\"%R%\" -e \"library('rngtools')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://renozao.github.io/rngtools
   license: GPL-3
@@ -52,3 +68,30 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: rngtools
+# Maintainer: Renaud Gaujoux <renaud@tx.technion.ac.il>
+# Author: Renaud Gaujoux
+# Version: 1.2.4
+# License: GPL-3
+# Title: Utility functions for working with Random Number Generators
+# Description: This package contains a set of functions for working with Random Number Generators (RNGs). In particular, it defines a generic S4 framework for getting/setting the current RNG, or RNG data that are embedded into objects for reproducibility. Notably, convenient default methods greatly facilitate the way current RNG settings can be changed.
+# URL: https://renozao.github.io/rngtools
+# BugReports: http://github.com/renozao/rngtools/issues
+# SCM: github:renozao, r-forge
+# Depends: R (>= 3.0.0), methods, pkgmaker (>= 0.20)
+# Imports: stringr, digest
+# Suggests: parallel, RUnit, knitr
+# Collate: 'rngtools-package.r' 'format.R' 'RNG.R' 'RNGseq.R'
+# VignetteBuilder: knitr
+# Packaged: 2014-03-06 13:11:11 UTC; renaud
+# NeedsCompilation: no
+# Repository: CRAN
+# Date/Publication: 2014-03-06 22:18:10
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-robustbase-feedstock contains modified content
diff --git a/r-robustbase-feedstock/recipe/meta.yaml b/r-robustbase-feedstock/recipe/meta.yaml
index 138d4f3..177c97e 100644
--- a/r-robustbase-feedstock/recipe/meta.yaml
+++ b/r-robustbase-feedstock/recipe/meta.yaml
@@ -14,39 +14,88 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/robustbase/robustbase_{{ version }}.tar.gz
   sha256: fcbd6ccbb0291b599fe6a674a91344511e0a691b9cadba0a9d40037faa22bf8f
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: grid, MASS, lattice, boot, cluster, Matrix, robust, fit.models, MPV, xtable, ggplot2, GGally, RColorBrewer, reshape2, sfsmisc, catdata
 requirements:
   build:
     - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('fortran') }}  # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
     - r-deoptimr
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-deoptimr
 
 test:
   commands:
-    - $R -e "library('robustbase')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('robustbase')"           # [not win]
     - "\"%R%\" -e \"library('robustbase')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://robustbase.r-forge.r-project.org/
   license: GPL (>= 2)
-  summary: |
-    Essential Robust Statistics. Tools allowing to analyze data with robust methods.  This
-    includes regression methodology including model selections and multivariate statistics.
+  summary: '"Essential" Robust Statistics. Tools allowing to analyze data with robust methods.  This
+    includes regression methodology including model selections and multivariate statistics
+    where we strive to cover the book "Robust Statistics, Theory and Methods" by ''Maronna,
+    Martin and Yohai''; Wiley 2006.'
+
   license_family: GPL3
 
 extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: robustbase
+# Version: 0.92-7
+# VersionNote: 0.92-6 on 2016-05-28 on CRAN
+# Date: 2016-12-08
+# Title: Basic Robust Statistics
+# Authors@R: c(person("Martin","Maechler", role=c("aut","cre"), email="maechler@stat.math.ethz.ch") , person("Peter", "Rousseeuw", role="ctb", comment = "Qn and Sn") , person("Christophe", "Croux", role="ctb", comment = "Qn and Sn") , person("Valentin", "Todorov", role = "aut", email = "valentin.todorov@chello.at", comment = "most robust Cov") , person("Andreas", "Ruckstuhl", role = "aut", email = "andreas.ruckstuhl@zhaw.ch", comment = "nlrob, anova, glmrob") , person("Matias", "Salibian-Barrera", role = "aut", email = "matias@stat.ubc.ca", comment = "lmrob orig.") , person("Tobias", "Verbeke", role = "ctb", email = "tobias.verbeke@openanalytics.eu", comment = "mc, adjbox") , person("Manuel", "Koller", role = "aut", email = "koller.manuel@gmail.com", comment = "lmrob, psi-func.") , person("Eduardo L. T.", "Conceicao", role = "aut", email = "mail@eduardoconceicao.org", comment = "MM-, tau-, CM-, and MTL- nlrob") , person("Maria", "Anna di Palma", role = "ctb", comment = "initial version of Comedian") )
+# Maintainer: Martin Maechler <maechler@stat.math.ethz.ch>
+# URL: http://robustbase.r-forge.r-project.org/
+# Description: "Essential" Robust Statistics. Tools allowing to analyze data with robust methods.  This includes regression methodology including model selections and multivariate statistics where we strive to cover the book "Robust Statistics, Theory and Methods" by 'Maronna, Martin and Yohai'; Wiley 2006.
+# Depends: R (>= 3.0.2)
+# Imports: stats, graphics, utils, methods, DEoptimR
+# Suggests: grid, MASS, lattice, boot, cluster, Matrix, robust, fit.models, MPV, xtable, ggplot2, GGally, RColorBrewer, reshape2, sfsmisc, catdata
+# SuggestsNote: mostly only because of vignette graphics
+# LazyData: yes
+# License: GPL (>= 2)
+# ByteCompile: yes
+# NeedsCompilation: yes
+# Packaged: 2016-12-08 20:55:09 UTC; maechler
+# Author: Martin Maechler [aut, cre], Peter Rousseeuw [ctb] (Qn and Sn), Christophe Croux [ctb] (Qn and Sn), Valentin Todorov [aut] (most robust Cov), Andreas Ruckstuhl [aut] (nlrob, anova, glmrob), Matias Salibian-Barrera [aut] (lmrob orig.), Tobias Verbeke [ctb] (mc, adjbox), Manuel Koller [aut] (lmrob, psi-func.), Eduardo L. T. Conceicao [aut] (MM-, tau-, CM-, and MTL- nlrob), Maria Anna di Palma [ctb] (initial version of Comedian)
+# Repository: CRAN
+# Date/Publication: 2016-12-09 17:25:25
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-rocr-feedstock contains modified content
diff --git a/r-rocr-feedstock/recipe/meta.yaml b/r-rocr-feedstock/recipe/meta.yaml
index e0a5e0e..ab05f63 100644
--- a/r-rocr-feedstock/recipe/meta.yaml
+++ b/r-rocr-feedstock/recipe/meta.yaml
@@ -14,26 +14,42 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/ROCR/ROCR_{{ version }}.tar.gz
   sha256: e7ef710f847e441a48b20fdc781dbc1377f5a060a5ee635234053f7a2a435ec9
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
     - r-gplots
+
   run:
     - r-base
     - r-gplots
 
 test:
   commands:
-    - $R -e "library('ROCR')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('ROCR')"           # [not win]
     - "\"%R%\" -e \"library('ROCR')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://rocr.bioinf.mpi-sb.mpg.de/
   license: GPL (>= 2)
@@ -55,3 +71,25 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: ROCR
+# Title: Visualizing the Performance of Scoring Classifiers
+# Version: 1.0-7
+# Date: 2015-03-26
+# Depends: gplots, methods
+# Author: Tobias Sing, Oliver Sander, Niko Beerenwinkel, Thomas Lengauer
+# Description: ROC graphs, sensitivity/specificity curves, lift charts, and precision/recall plots are popular examples of trade-off visualizations for specific pairs of performance measures. ROCR is a flexible tool for creating cutoff-parameterized 2D performance curves by freely combining two from over 25 performance measures (new performance measures can be added using a standard interface). Curves from different cross-validation or bootstrapping runs can be averaged by different methods, and standard deviations, standard errors or box plots can be used to visualize the variability across the runs. The parameterization can be visualized by printing cutoff values at the corresponding curve positions, or by coloring the curve according to cutoff. All components of a performance plot can be quickly adjusted using a flexible parameter dispatching mechanism. Despite its flexibility, ROCR is easy to use, with only three commands and reasonable default values for all optional parameters.
+# Maintainer: Tobias Sing <tobias.sing@gmail.com>
+# License: GPL (>= 2)
+# URL: http://rocr.bioinf.mpi-sb.mpg.de/
+# Packaged: 2015-03-26 10:34:10 UTC; singto1
+# NeedsCompilation: no
+# Repository: CRAN
+# Date/Publication: 2015-03-26 17:12:17
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-rodbc-feedstock contains modified content
diff --git a/r-rodbc-feedstock/recipe/build.sh b/r-rodbc-feedstock/recipe/build.sh
index af6547b..3a06ba0 100644
--- a/r-rodbc-feedstock/recipe/build.sh
+++ b/r-rodbc-feedstock/recipe/build.sh
@@ -1,2 +1,6 @@
 #!/bin/bash
+
+export CFLAGS="${CFLAGS} -L${PREFIX}/include"
+export LDFLAGS="${LDFLAGS} -L${PREFIX}/lib"
+
 $R CMD INSTALL --build .
diff --git a/r-rodbc-feedstock/recipe/meta.yaml b/r-rodbc-feedstock/recipe/meta.yaml
index a1ace12..7b18a4a 100644
--- a/r-rodbc-feedstock/recipe/meta.yaml
+++ b/r-rodbc-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '1.3-14' %}
+{% set version = '1.3-15' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,37 +12,51 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/RODBC_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/RODBC/RODBC_{{ version }}.tar.gz
+  sha256: c43e5a2f0aa2f46607e664bfc0bb3caa230bbb779f4ff084e01727642da136e1
 
 
-  sha256: ed4d5bfddf7dffbd3eacdc0f084d48b04e7620ee9c36d8699a4776ca1cf4f28b
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
-  skip: True  # [win32]
 
 
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
-    - unixodbc             # [unix]
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}autoconf
+    - {{posix}}automake
+    - {{posix}}pkg-config
+    - {{posix}}make
+    - unixodbc                   # [unix]
 
   run:
     - r-base
-    - unixodbc             # [unix]
+    - {{native}}gcc-libs         # [win]
+    - unixodbc                   # [unix]
 
 test:
   commands:
-    - $R -e "library('RODBC')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('RODBC')"           # [not win]
     - "\"%R%\" -e \"library('RODBC')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
 
+  # requires:
+    # Put any additional test requirements here.
 
 about:
   home: https://CRAN.R-project.org/package=RODBC
@@ -50,8 +64,33 @@ about:
   summary: An ODBC database interface.
   license_family: GPL3
 
-
-
 extra:
   recipe-maintainers:
     - croth1
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: RODBC
+# Version: 1.3-15
+# Revision: $Rev: 3476 $
+# Date: 2017-04-13
+# Authors@R: c(person("Brian", "Ripley", role = c("aut", "cre"), email = "ripley@stats.ox.ac.uk"), person("Michael", "Lapsley", role = "aut",  comment = "1999 to Oct 2002"))
+# Title: ODBC Database Access
+# Description: An ODBC database interface.
+# SystemRequirements: An ODBC3 driver manager and drivers.
+# Depends: R (>= 3.0.0)
+# Imports: stats
+# LazyLoad: yes
+# Biarch: yes
+# License: GPL-2 | GPL-3
+# NeedsCompilation: yes
+# Packaged: 2017-05-05 10:50:09 UTC; ripley
+# Author: Brian Ripley [aut, cre], Michael Lapsley [aut] (1999 to Oct 2002)
+# Maintainer: Brian Ripley <ripley@stats.ox.ac.uk>
+# Repository: CRAN
+# Date/Publication: 2017-05-05 12:56:27
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-rook-feedstock contains modified content
diff --git a/r-rook-feedstock/recipe/meta.yaml b/r-rook-feedstock/recipe/meta.yaml
index b99a0e0..e1753cf 100644
--- a/r-rook-feedstock/recipe/meta.yaml
+++ b/r-rook-feedstock/recipe/meta.yaml
@@ -14,35 +14,49 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/Rook/Rook_{{ version }}.tar.gz
   sha256: 00f4ecfa4c5c57018acbb749080c07154549a6ecaa8d4130dd9de79427504903
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
     - r-brew
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-brew
-    - libgcc  # [not win]
 
 test:
   commands:
-    - $R -e "library('Rook')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('Rook')"           # [not win]
     - "\"%R%\" -e \"library('Rook')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=Rook
   license: GPL-2
-  summary: |
-    This package contains the Rook specification and convenience software for building
+  summary: This package contains the Rook specification and convenience software for building
     and running Rook applications. To get started, be sure and read the 'Rook' help
     file first.
   license_family: GPL2
@@ -51,3 +65,27 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: Rook
+# Type: Package
+# Title: Rook - a web server interface for R
+# Version: 1.1-1
+# Date: 2014-10-20
+# Author: Jeffrey Horner <jeffrey.horner@gmail.com>
+# Maintainer: Jeffrey Horner <jeffrey.horner@gmail.com>
+# Description: This package contains the Rook specification and convenience software for building and running Rook applications. To get started, be sure and read the 'Rook' help file first.
+# Depends: R (>= 2.13.0)
+# Imports: utils, tools, methods, brew
+# License: GPL-2
+# LazyLoad: yes
+# Packaged: 2014-10-20 16:54:31 UTC; jeffrey
+# NeedsCompilation: yes
+# Repository: CRAN
+# Date/Publication: 2014-10-20 19:18:06
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-roxygen2-feedstock contains modified content
diff --git a/r-roxygen2-feedstock/recipe/meta.yaml b/r-roxygen2-feedstock/recipe/meta.yaml
index ec1add5..3f7c5af 100644
--- a/r-roxygen2-feedstock/recipe/meta.yaml
+++ b/r-roxygen2-feedstock/recipe/meta.yaml
@@ -14,19 +14,28 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/roxygen2/roxygen2_{{ version }}.tar.gz
   sha256: d22ddc05cd5308a48b8359b932e1f7f4c23fdf0c857ac8e52c42381b6bfcff76
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: testthat (>= 0.8.0), knitr, devtools, rmarkdown, covr
 requirements:
   build:
     - r-base
+    - {{ compiler('cxx') }}      # [not win]
+    - {{native}}toolchain        # [win]
     - r-r6
-    - r-rcpp
+    - r-rcpp >=0.11.0
     - r-brew
     - r-commonmark
     - r-desc
@@ -34,14 +43,12 @@ requirements:
     - r-stringi
     - r-stringr >=0.5
     - r-xml2
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-r6
-    - r-rcpp
+    - r-rcpp >=0.11.0
     - r-brew
     - r-commonmark
     - r-desc
@@ -49,13 +56,19 @@ requirements:
     - r-stringi
     - r-stringr >=0.5
     - r-xml2
-    - libgcc               # [not win]
 
 test:
   commands:
-    - $R -e "library('roxygen2')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('roxygen2')"           # [not win]
     - "\"%R%\" -e \"library('roxygen2')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/klutometis/roxygen
   license: GPL (>= 2)
@@ -64,7 +77,36 @@ about:
     your documentation up-to-date as your requirements change. 'Roxygen2' is inspired
     by the 'Doxygen' system for C++.
   license_family: GPL3
+
 extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: roxygen2
+# Title: In-Line Documentation for R
+# Description: Generate your Rd documentation, 'NAMESPACE' file, and collation  field using specially formatted comments. Writing documentation in-line with code makes it easier to keep your documentation up-to-date as your requirements change. 'Roxygen2' is inspired by the 'Doxygen' system for C++.
+# Version: 6.0.1
+# License: GPL (>= 2)
+# Authors@R: c( person("Hadley", "Wickham",, "hadley@rstudio.com", c("aut", "cre", "cph")), person("Peter", "Danenberg",, "pcd@roxygen.org", c("aut", "cph")), person("Manuel", "Eugster", role = c("aut", "cph")), person("RStudio", role = "cph") )
+# Depends: R (>= 3.0.2)
+# Imports: stringr (>= 0.5), stringi, brew, digest, methods, Rcpp (>= 0.11.0), R6, desc, commonmark, xml2, utils
+# Suggests: testthat (>= 0.8.0), knitr, devtools, rmarkdown, covr
+# VignetteBuilder: knitr
+# LinkingTo: Rcpp
+# RoxygenNote: 6.0.1
+# URL: https://github.com/klutometis/roxygen
+# BugReports: https://github.com/klutometis/roxygen/issues
+# NeedsCompilation: yes
+# Packaged: 2017-02-06 14:33:02 UTC; hadley
+# Author: Hadley Wickham [aut, cre, cph], Peter Danenberg [aut, cph], Manuel Eugster [aut, cph], RStudio [cph]
+# Maintainer: Hadley Wickham <hadley@rstudio.com>
+# Repository: CRAN
+# Date/Publication: 2017-02-06 16:37:05
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-rpart-feedstock contains modified content
diff --git a/r-rpart-feedstock/recipe/meta.yaml b/r-rpart-feedstock/recipe/meta.yaml
index 3decc90..7d5b46c 100644
--- a/r-rpart-feedstock/recipe/meta.yaml
+++ b/r-rpart-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '4.1-10' %}
+{% set version = '4.1-11' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,31 +12,45 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/rpart_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/rpart/rpart_{{ version }}.tar.gz
-  sha256: c5ddaed288d38118876a94c7aa5000dce0070b8d736dba12de64a9cb04dc2d85
+  sha256: 38ab80959f59bcdd2c4c72860e8dd0deab0307668cbbf24f96014d7a2496ad98
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: survival
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('rpart')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('rpart')"           # [not win]
     - "\"%R%\" -e \"library('rpart')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=rpart
   license: GPL-2 | GPL-3
@@ -49,3 +63,30 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: rpart
+# Priority: recommended
+# Version: 4.1-11
+# Date: 2017-03-12
+# Authors@R: c(person("Terry", "Therneau", role = "aut", email = "therneau@mayo.edu"), person("Beth", "Atkinson", role = "aut",	 email = "atkinson@mayo.edu"), person("Brian", "Ripley", role = c("aut", "trl", "cre"), email = "ripley@stats.ox.ac.uk", comment = "author of initial R port"))
+# Description: Recursive partitioning for classification,  regression and survival trees.  An implementation of most of the  functionality of the 1984 book by Breiman, Friedman, Olshen and Stone.
+# Title: Recursive Partitioning and Regression Trees
+# Depends: R (>= 2.15.0), graphics, stats, grDevices
+# Suggests: survival
+# License: GPL-2 | GPL-3
+# LazyData: yes
+# ByteCompile: yes
+# Note: Maintainers are not available to give advice on using a package they did not author.
+# NeedsCompilation: yes
+# Packaged: 2017-03-12 21:36:08 UTC; ripley
+# Author: Terry Therneau [aut], Beth Atkinson [aut], Brian Ripley [aut, trl, cre] (author of initial R port)
+# Maintainer: Brian Ripley <ripley@stats.ox.ac.uk>
+# Repository: CRAN
+# Date/Publication: 2017-04-21 13:33:37 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-rprojroot-feedstock contains modified content
diff --git a/r-rprojroot-feedstock/recipe/meta.yaml b/r-rprojroot-feedstock/recipe/meta.yaml
index be34783..c59e329 100644
--- a/r-rprojroot-feedstock/recipe/meta.yaml
+++ b/r-rprojroot-feedstock/recipe/meta.yaml
@@ -14,14 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/rprojroot/rprojroot_{{ version }}.tar.gz
   sha256: 28b4d235ce67314528a0c1cc7e98faed42437b42e07fca18a59a80fdc3eefeb9
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: testthat, knitr, withr, rmarkdown
 requirements:
   build:
     - r-base
@@ -33,9 +40,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('rprojroot')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('rprojroot')"           # [not win]
     - "\"%R%\" -e \"library('rprojroot')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/krlmlr/rprojroot, https://krlmlr.github.io/rprojroot
   license: GPL-3
@@ -48,3 +62,33 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: rprojroot
+# Title: Finding Files in Project Subdirectories
+# Version: 1.2
+# Authors@R: person(given = "Kirill", family = "Muller", role = c("aut", "cre"), email = "krlmlr+r@mailbox.org")
+# Description: Robust, reliable and flexible paths to files below a project root. The 'root' of a project is defined as a directory that matches a certain criterion, e.g., it contains a certain regular file.
+# Depends: R (>= 3.0.0)
+# Imports: backports
+# Suggests: testthat, knitr, withr, rmarkdown
+# VignetteBuilder: knitr
+# License: GPL-3
+# LazyData: true
+# Encoding: UTF-8
+# URL: https://github.com/krlmlr/rprojroot, https://krlmlr.github.io/rprojroot
+# BugReports: https://github.com/krlmlr/rprojroot/issues
+# RoxygenNote: 5.0.1.9000
+# Collate: 'rrmake.R' 'criterion.R' 'file.R' 'has-file.R' 'root.R' 'rprojroot-package.R' 'shortcut.R'
+# NeedsCompilation: no
+# Packaged: 2017-01-16 11:50:23 UTC; muelleki
+# Author: Kirill Muller [aut, cre]
+# Maintainer: Kirill Muller <krlmlr+r@mailbox.org>
+# Repository: CRAN
+# Date/Publication: 2017-01-16 14:16:04
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-rsconnect-feedstock contains modified content
diff --git a/r-rsconnect-feedstock/recipe/meta.yaml b/r-rsconnect-feedstock/recipe/meta.yaml
index f9283f9..1c15753 100644
--- a/r-rsconnect-feedstock/recipe/meta.yaml
+++ b/r-rsconnect-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '0.7' %}
+{% set version = '0.8.5' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,17 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/rsconnect_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/rsconnect/rsconnect_{{ version }}.tar.gz
-  sha256: 162e9475339e1336d51cad3ddc226f5edf98e9b7661eed3e0c778a80f1408c80
+  sha256: b8726b2f06c711e5d253ee10ea46e84b8e9438fb7d1d6ac08bcffac099545a78
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win]
-# r-rcurl is not available under windows
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: scrypt, knitr, testthat, rmarkdown (>= 1.1), plumber (>= 0.3.2)
 requirements:
   build:
     - r-base
@@ -46,18 +52,50 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('rsconnect')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('rsconnect')"           # [not win]
     - "\"%R%\" -e \"library('rsconnect')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=rsconnect
   license: GPL-2
   summary: Programmatic deployment interface for 'RPubs', 'shinyapps.io', and 'RStudio Connect'.
-    Supported content types include R Markdown documents, Shiny applications, plots,
-    and static web content.
+    Supported content types include R Markdown documents, Shiny applications, Plumber
+    APIs, plots, and static web content.
   license_family: GPL2
 
 extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: rsconnect
+# Type: Package
+# Title: Deployment Interface for R Markdown Documents and Shiny Applications
+# Version: 0.8.5
+# Author: JJ Allaire
+# Maintainer: JJ Allaire <jj@rstudio.com>
+# Description: Programmatic deployment interface for 'RPubs', 'shinyapps.io', and 'RStudio Connect'. Supported content types include R Markdown documents, Shiny applications, Plumber APIs, plots, and static web content.
+# Depends: R (>= 3.0.0)
+# Imports: digest, PKI, RCurl, RJSONIO, packrat (>= 0.4.8-1), yaml (>= 2.1.5), rstudioapi (>= 0.5)
+# Suggests: scrypt, knitr, testthat, rmarkdown (>= 1.1), plumber (>= 0.3.2)
+# Enhances: BiocInstaller
+# License: GPL-2
+# RoxygenNote: 6.0.1
+# NeedsCompilation: no
+# Packaged: 2017-08-22 17:27:13 UTC; jmcphers
+# Repository: CRAN
+# Date/Publication: 2017-08-23 07:21:28 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-rserve-feedstock contains modified content
diff --git a/r-rserve-feedstock/recipe/meta.yaml b/r-rserve-feedstock/recipe/meta.yaml
index 9a1f85a..c59002c 100644
--- a/r-rserve-feedstock/recipe/meta.yaml
+++ b/r-rserve-feedstock/recipe/meta.yaml
@@ -12,41 +12,54 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/Rserve_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/Rserve/Rserve_{{ version }}.tar.gz
+  sha256: f6d636c736c3f16487d9987e54554fe0c55257b9bc0671b43e536d832e513027
 
 
-  sha256: f6d636c736c3f16487d9987e54554fe0c55257b9bc0671b43e536d832e513027
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
-  skip: True  # [win32]
 
+# Suggests: RSclient
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
-    - make
-    - libiconv             # [osx]
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('cxx') }}      # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}autoconf
+    - {{posix}}automake
+    - {{posix}}pkg-config
+    - {{posix}}make
 
   run:
     - r-base
-    - libiconv             # [osx]
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('Rserve')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('Rserve')"           # [not win]
     - "\"%R%\" -e \"library('Rserve')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
 
+  # requires:
+    # Put any additional test requirements here.
 
 about:
   home: http://www.rforge.net/Rserve/
-  license: GPL-2
+  license: GPL-2 | file LICENSE
   summary: Rserve acts as a socket server (TCP/IP or local sockets)  which allows binary requests
     to be sent to R. Every connection has a separate workspace and working directory.
     Client-side implementations are available for popular languages such as C/C++ and
@@ -54,10 +67,29 @@ about:
     to R code. Rserve supports remote connection, user authentication and file transfer.
     A simple R client is included in this package as well.
   license_family: GPL2
-  license_file: LICENSE
-
-
 
 extra:
   recipe-maintainers:
     - croth1
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: Rserve
+# Version: 1.7-3
+# Title: Binary R server
+# Author: Simon Urbanek <Simon.Urbanek@r-project.org>
+# Maintainer: Simon Urbanek <Simon.Urbanek@r-project.org>
+# Depends: R (>= 1.5.0)
+# Suggests: RSclient
+# SystemRequirements: libR, GNU make
+# Description: Rserve acts as a socket server (TCP/IP or local sockets)  which allows binary requests to be sent to R. Every connection has a separate workspace and working directory. Client-side implementations are available for popular languages such as C/C++ and Java, allowing any application to use facilities of R without the need of linking to R code. Rserve supports remote connection, user authentication and file transfer. A simple R client is included in this package as well.
+# License: GPL-2 | file LICENSE
+# URL: http://www.rforge.net/Rserve/
+# NeedsCompilation: yes
+# Repository: CRAN
+# Date/Publication: 2013-08-21 23:35:21
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-rsqlite-feedstock contains modified content
diff --git a/r-rsqlite-feedstock/recipe/meta.yaml b/r-rsqlite-feedstock/recipe/meta.yaml
index a15e38f..264c73b 100644
--- a/r-rsqlite-feedstock/recipe/meta.yaml
+++ b/r-rsqlite-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '1.1-2' %}
+{% set version = '2.0' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,42 +12,64 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/RSQLite_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/RSQLite/RSQLite_{{ version }}.tar.gz
-  sha256: e4022e6a134bb0ff00d33474d2189a7bb48ee12b513d258e957b78bb1bf4e955
+  sha256: 7f0fe629f34641c6af1e8a34412f3089ee2d184853843209d97ffe29430ceff6
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: DBItest, knitr, rmarkdown, testthat
 requirements:
   build:
     - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('cxx') }}      # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
     - r-bh
     - r-dbi >=0.4_9
-    - r-rcpp
+    - r-rcpp >=0.12.7
+    - r-bit64
+    - r-blob >=1.1
     - r-memoise
+    - r-pkgconfig
     - r-plogr
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-bh
     - r-dbi >=0.4_9
-    - r-rcpp
+    - r-rcpp >=0.12.7
+    - r-bit64
+    - r-blob >=1.1
     - r-memoise
+    - r-pkgconfig
     - r-plogr
-    - libgcc               # [not win]
 
 test:
   commands:
-    - $R -e "library('RSQLite')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('RSQLite')"           # [not win]
     - "\"%R%\" -e \"library('RSQLite')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/rstats-db/RSQLite
   license: LGPL (>= 2)
@@ -59,3 +81,34 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: RSQLite
+# Version: 2.0
+# Date: 2017-06-18
+# Title: 'SQLite' Interface for R
+# Authors@R: c( person("Kirill", "Muller", role = c("aut", "cre"), email = "krlmlr+r@mailbox.org"), person("Hadley", "Wickham", role = c("aut")), person(c("David", "A."), "James", role = "aut"), person("Seth", "Falcon", role = "aut"), person(family = "SQLite Authors", role = "ctb", comment = "for the included SQLite sources"), person("Liam", "Healy", role = "ctb", comment = "for the included SQLite sources"), person(family = "R Consortium", role = "cph"), person(family = "RStudio", role = "cph") )
+# Description: Embeds the 'SQLite' database engine in R and provides an interface compliant with the 'DBI' package. The source for the 'SQLite' engine (version 3.8.8.2) is included.
+# Depends: R (>= 3.1.0)
+# Suggests: DBItest, knitr, rmarkdown, testthat
+# Imports: bit64, blob (>= 1.1), DBI (>= 0.4-9), memoise, methods, pkgconfig, Rcpp (>= 0.12.7)
+# LinkingTo: Rcpp, BH, plogr
+# Encoding: UTF-8
+# License: LGPL (>= 2)
+# URL: https://github.com/rstats-db/RSQLite
+# BugReports: https://github.com/rstats-db/RSQLite/issues
+# Collate: 'RcppExports.R' 'SQLiteConnection.R' 'SQLiteDriver.R' 'SQLiteResult.R' 'connect.R' 'copy.R' 'datasetsDb.R' 'deprecated.R' 'export.R' 'extensions.R' 'query.R' 'rownames.R' 'table.R' 'transactions.R' 'utils.R' 'zzz.R'
+# VignetteBuilder: knitr
+# RoxygenNote: 6.0.1
+# NeedsCompilation: yes
+# Packaged: 2017-06-18 22:12:09 UTC; muelleki
+# Author: Kirill Muller [aut, cre], Hadley Wickham [aut], David A. James [aut], Seth Falcon [aut], SQLite Authors [ctb] (for the included SQLite sources), Liam Healy [ctb] (for the included SQLite sources), R Consortium [cph], RStudio [cph]
+# Maintainer: Kirill Muller <krlmlr+r@mailbox.org>
+# Repository: CRAN
+# Date/Publication: 2017-06-19 11:51:07 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-rstan-feedstock contains modified content
diff --git a/r-rstan-feedstock/recipe/0001-fix-compiler-error.patch b/r-rstan-feedstock/recipe/0001-fix-compiler-error.patch
new file mode 100644
index 0000000..ffbbb1b
--- /dev/null
+++ b/r-rstan-feedstock/recipe/0001-fix-compiler-error.patch
@@ -0,0 +1,35 @@
+From 18d20546e9c5349df5d7e294b0f548df526902dd Mon Sep 17 00:00:00 2001
+From: Ben Goodrich <goodrich.ben@gmail.com>
+Date: Thu, 7 Sep 2017 10:00:55 -0400
+Subject: [PATCH] fix compiler error
+
+---
+ rstan/rstan/src/Makevars     | 2 +-
+ rstan/rstan/src/Makevars.win | 2 +-
+ 2 files changed, 2 insertions(+), 2 deletions(-)
+
+diff --git a/rstan/rstan/src/Makevars b/rstan/rstan/src/Makevars
+index cb7955c..3bea009 100644
+--- a/rstan/rstan/src/Makevars
++++ b/rstan/rstan/src/Makevars
+@@ -1,5 +1,5 @@
+ STANHEADERS_SRC = `"$(R_HOME)/bin$(R_ARCH_BIN)/Rscript" --vanilla -e "cat(system.file('include', 'src', package = 'StanHeaders'))"`
+ 
+-PKG_CPPFLAGS = -I"../inst/include" -I"../inst/include/boost_not_in_BH" -I"$(STANHEADERS_SRC)" -DBOOST_RESULT_OF_USE_TR1 -DBOOST_NO_DECLTYPE -DBOOST_DISABLE_ASSERTS -DFUSION_MAX_VECTOR_SIZE=12
++PKG_CPPFLAGS = -I"../inst/include" -I"../inst/include/boost_not_in_BH" -I"$(STANHEADERS_SRC)" -DBOOST_RESULT_OF_USE_TR1 -DBOOST_NO_DECLTYPE -DBOOST_DISABLE_ASSERTS -DFUSION_MAX_VECTOR_SIZE=12 -DBOOST_PHOENIX_NO_VARIADIC_EXPRESSION
+ SHLIB_LDFLAGS = $(SHLIB_CXXLDFLAGS) 
+ SHLIB_LD = $(SHLIB_CXXLD)
+diff --git a/rstan/rstan/src/Makevars.win b/rstan/rstan/src/Makevars.win
+index cb7955c..3bea009 100644
+--- a/rstan/rstan/src/Makevars.win
++++ b/rstan/rstan/src/Makevars.win
+@@ -1,5 +1,5 @@
+ STANHEADERS_SRC = `"$(R_HOME)/bin$(R_ARCH_BIN)/Rscript" --vanilla -e "cat(system.file('include', 'src', package = 'StanHeaders'))"`
+ 
+-PKG_CPPFLAGS = -I"../inst/include" -I"../inst/include/boost_not_in_BH" -I"$(STANHEADERS_SRC)" -DBOOST_RESULT_OF_USE_TR1 -DBOOST_NO_DECLTYPE -DBOOST_DISABLE_ASSERTS -DFUSION_MAX_VECTOR_SIZE=12
++PKG_CPPFLAGS = -I"../inst/include" -I"../inst/include/boost_not_in_BH" -I"$(STANHEADERS_SRC)" -DBOOST_RESULT_OF_USE_TR1 -DBOOST_NO_DECLTYPE -DBOOST_DISABLE_ASSERTS -DFUSION_MAX_VECTOR_SIZE=12 -DBOOST_PHOENIX_NO_VARIADIC_EXPRESSION
+ SHLIB_LDFLAGS = $(SHLIB_CXXLDFLAGS) 
+ SHLIB_LD = $(SHLIB_CXXLD)
+-- 
+2.13.6
+
diff --git a/r-rstan-feedstock/recipe/meta.yaml b/r-rstan-feedstock/recipe/meta.yaml
index d112ff3..b315e71 100644
--- a/r-rstan-feedstock/recipe/meta.yaml
+++ b/r-rstan-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '2.15.1' %}
+{% set version = '2.16.2' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,46 +12,60 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/rstan_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/rstan/rstan_{{ version }}.tar.gz
-  sha256: 2f3f68adbfd364713fb1e8b87ddde03b258abbf0dbb120cdbbbc31fc5bec71eb
+  sha256: 6b8b4c8a308a619895b6afb77d64817d560c2ada1cc61b011b7a88f91e813847
+  patches:
+    - 0001-fix-compiler-error.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: RUnit, RcppEigen (>= 0.3.3.3.0), BH (>= 1.62), parallel, KernSmooth, loo (>= 1.0.0), shinystan (>= 2.2.1), bayesplot, rstantools, rstudioapi, Matrix, knitr (>= 1.15.1)
 requirements:
   build:
     - r-base
+    - {{ compiler('cxx') }}      # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
     - r-bh >=1.62
     - r-rcpp >=0.12.0
-    - r-rcppeigen
-    - r-stanheaders >=2.15.0
+    - r-rcppeigen >=0.3.3.3.0
+    - r-stanheaders >=2.16.0
     - r-ggplot2 >=2.0.0
     - r-gridextra >=2.0.0
     - r-inline
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-bh >=1.62
     - r-rcpp >=0.12.0
-    - r-rcppeigen
-    - r-stanheaders >=2.15.0
+    - r-rcppeigen >=0.3.3.3.0
+    - r-stanheaders >=2.16.0
     - r-ggplot2 >=2.0.0
     - r-gridextra >=2.0.0
     - r-inline
-    - libgcc  # [not win]
 
 test:
   commands:
-    - $R -e "library('rstan')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('rstan')"           # [not win]
     - "\"%R%\" -e \"library('rstan')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
-  home: https://groups.google.com/forum/#!forum/stan-users, http://mc-stan.org
+  home: http://discourse.mc-stan.org, http://mc-stan.org
   license: GPL (>= 3)
   summary: User-facing R functions are provided to parse, compile, test,  estimate, and analyze
     Stan models by accessing the header-only Stan library  provided by the 'StanHeaders'
@@ -67,3 +81,33 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: rstan
+# Type: Package
+# Title: R Interface to Stan
+# Version: 2.16.2
+# Date: 2017-07-03
+# Authors@R: c(person("Jiqiang", "Guo", email = "guojq28@gmail.com", role = "aut"), person("Jonah", "Gabry", email = "jsg2201@columbia.edu", role = "aut"), person("Ben", "Goodrich", email = "benjamin.goodrich@columbia.edu", role = c("cre", "aut")), person("Daniel", "Lee", role = "ctb"), person("Krzysztof", "Sakrejda", role = "ctb"), person("Trustees of", "Columbia University", role = "cph"), person("Oleg", "Sklyar", role = "cph", comment = "R/cxxfunplus.R"), person("The R", "Core Team", role = "cph", comment = "R/pairs.R, R/dynGet.R"), person("Jens", "Oehlschlaegel-Akiyoshi", role = "cph", comment = "R/pairs.R"), person("Hadley", "Wickham", role = "cph", comment = "R/rtools.R"), person("Joel", "de Guzman", role = "cph", comment = "Boost"), person("John", "Fletcher", role = "cph",  comment = "Boost"), person("Thomas", "Heller", role = "cph",  comment = "Boost"), person("Eric", "Niebler", role = "cph",   comment = "Boost"))
+# Description: User-facing R functions are provided to parse, compile, test,  estimate, and analyze Stan models by accessing the header-only Stan library  provided by the 'StanHeaders' package. The Stan project develops a probabilistic programming language that implements full Bayesian statistical  inference via Markov Chain Monte Carlo, rough Bayesian inference via 'variational' approximation, and (optionally penalized) maximum likelihood estimation via  optimization. In all three cases, automatic differentiation is used to quickly  and accurately evaluate gradients without burdening the user with the need  to derive the partial derivatives.
+# License: GPL (>= 3)
+# NeedsCompilation: yes
+# Imports: methods, stats4, inline, gridExtra (>= 2.0.0), Rcpp (>= 0.12.0)
+# Depends: R (>= 3.0.2), ggplot2 (>= 2.0.0), StanHeaders (>= 2.16.0)
+# LinkingTo: Rcpp (>= 0.12.0), RcppEigen (>= 0.3.3.3.0) , BH (>= 1.62), StanHeaders (>= 2.16.0)
+# Suggests: RUnit, RcppEigen (>= 0.3.3.3.0), BH (>= 1.62), parallel, KernSmooth, loo (>= 1.0.0), shinystan (>= 2.2.1), bayesplot, rstantools, rstudioapi, Matrix, knitr (>= 1.15.1)
+# URL: http://discourse.mc-stan.org, http://mc-stan.org
+# BugReports: https://github.com/stan-dev/rstan/issues/
+# VignetteBuilder: knitr
+# SystemRequirements: pandoc
+# Packaged: 2017-07-03 09:24:58 UTC; goodrich
+# Author: Jiqiang Guo [aut], Jonah Gabry [aut], Ben Goodrich [cre, aut], Daniel Lee [ctb], Krzysztof Sakrejda [ctb], Trustees of Columbia University [cph], Oleg Sklyar [cph] (R/cxxfunplus.R), The R Core Team [cph] (R/pairs.R, R/dynGet.R), Jens Oehlschlaegel-Akiyoshi [cph] (R/pairs.R), Hadley Wickham [cph] (R/rtools.R), Joel de Guzman [cph] (Boost), John Fletcher [cph] (Boost), Thomas Heller [cph] (Boost), Eric Niebler [cph] (Boost)
+# Maintainer: Ben Goodrich <benjamin.goodrich@columbia.edu>
+# Repository: CRAN
+# Date/Publication: 2017-07-03 10:51:15 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
diff --git a/r-rstudioapi-feedstock/recipe/meta.yaml b/r-rstudioapi-feedstock/recipe/meta.yaml
index 8995989..a9f9387 100644
--- a/r-rstudioapi-feedstock/recipe/meta.yaml
+++ b/r-rstudioapi-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '0.6' %}
+{% set version = '0.7' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,9 +12,9 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/rstudioapi_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/rstudioapi/rstudioapi_{{ version }}.tar.gz
+  sha256: a541bc76ef082d2c27e42fd683f8262cb195b1497af3509178d2642870397a8c
 
 
-  sha256: da24c6cdb13af1bdf4261671a065dcca4c1b7af1412cb810eb805bf3c5f97bfe
   # patches:
    # List any patch files here
    # - fix.patch
@@ -28,7 +28,7 @@ build:
     - lib/R/lib/
     - lib/
 
-# Suggests: testthat
+# Suggests: testthat, knitr, rmarkdown
 requirements:
   build:
     - r-base
@@ -39,7 +39,7 @@ requirements:
 test:
   commands:
     # You can put additional test commands to be run here.
-    - $R -e "library('rstudioapi')"  # [not win]
+    - $R -e "library('rstudioapi')"           # [not win]
     - "\"%R%\" -e \"library('rstudioapi')\""  # [win]
 
   # You can also put a file called run_test.py, run_test.sh, or run_test.bat
@@ -49,28 +49,35 @@ test:
     # Put any additional test requirements here.
 
 about:
-  home: https://CRAN.R-project.org/package=rstudioapi
+  home: https://github.com/rstudio/rstudioapi
   license: MIT + file LICENSE
   summary: Access the RStudio API (if available) and provide informative error messages when
     it's not.
   license_family: MIT
 
+extra:
+  recipe-maintainers:
+    - mingwandroid
+
 # The original CRAN metadata for this package was:
 
 # Package: rstudioapi
 # Title: Safely Access the RStudio API
 # Description: Access the RStudio API (if available) and provide informative error messages when it's not.
-# Version: 0.6
-# Authors@R: c( person("Hadley", "Wickham", role = c("aut"), email = "hadley@rstudio.com"), person("JJ", "Allaire", role = c("aut", "cre"), email = "jj@rstudio.com"), person("Kevin", "Ushey", role = c("ctb"), email = "kevin@rstudio.com"), person(family = "RStudio", role = "cph") )
+# Version: 0.7
+# Authors@R: c( person("JJ", "Allaire", role = c("aut", "cre"), email = "jj@rstudio.com"), person("Hadley", "Wickham", role = c("aut"), email = "hadley@rstudio.com"), person("Kevin", "Ushey", role = c("aut"), email = "kevin@rstudio.com"), person("Gary", "Ritchie", role = c("aut"), email = "gary@rstudio.com"), person(family = "RStudio", role = "cph") )
 # Maintainer: JJ Allaire <jj@rstudio.com>
 # License: MIT + file LICENSE
-# RoxygenNote: 5.0.1
-# Suggests: testthat
+# URL: https://github.com/rstudio/rstudioapi
+# BugReports: https://github.com/rstudio/rstudioapi/issues
+# RoxygenNote: 6.0.1
+# Suggests: testthat, knitr, rmarkdown
+# VignetteBuilder: knitr
 # NeedsCompilation: no
-# Packaged: 2016-06-27 03:13:50 UTC; jjallaire
-# Author: Hadley Wickham [aut], JJ Allaire [aut, cre], Kevin Ushey [ctb], RStudio [cph]
+# Packaged: 2017-09-07 17:48:48 UTC; kevin
+# Author: JJ Allaire [aut, cre], Hadley Wickham [aut], Kevin Ushey [aut], Gary Ritchie [aut], RStudio [cph]
 # Repository: CRAN
-# Date/Publication: 2016-06-27 07:00:05
+# Date/Publication: 2017-09-07 21:41:46 UTC
 
 # See
 # http://docs.continuum.io/conda/build.html for
Submodule r-rversions-feedstock contains modified content
diff --git a/r-rversions-feedstock/recipe/meta.yaml b/r-rversions-feedstock/recipe/meta.yaml
index e629c41..f69b959 100644
--- a/r-rversions-feedstock/recipe/meta.yaml
+++ b/r-rversions-feedstock/recipe/meta.yaml
@@ -14,13 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/rversions/rversions_{{ version }}.tar.gz
   sha256: 21d0809f46505de89a2be7be9449e39c39cff5bc77e584dec976ee6c0b884f44
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
+
+
 requirements:
   build:
     - r-base
@@ -34,12 +42,19 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('rversions')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('rversions')"           # [not win]
     - "\"%R%\" -e \"library('rversions')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/metacran/rversions
-  license: MIT
+  license: MIT + file LICENSE
   summary: Query the main 'R' 'SVN' repository to find the versions 'r-release' and 'r-oldrel'
     refer to, and also all previous 'R' versions and their release dates.
   license_family: MIT
@@ -48,3 +63,28 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: rversions
+# Title: Query 'R' Versions, Including 'r-release' and 'r-oldrel'
+# Version: 1.0.3
+# Authors@R: c( person("Gabor", "Csardi", , "csardi.gabor@gmail.com", role = c("aut", "cre")), person("Jeroen", "Ooms", , "jeroen.ooms@stat.ucla.edu", role = "ctb"))
+# Description: Query the main 'R' 'SVN' repository to find the versions 'r-release' and 'r-oldrel' refer to, and also all previous 'R' versions and their release dates.
+# License: MIT + file LICENSE
+# URL: https://github.com/metacran/rversions
+# BugReports: https://github.com/metacran/rversions/issues
+# Imports: curl, xml2 (>= 1.0.0), utils
+# Encoding: UTF-8
+# RoxygenNote: 5.0.1
+# NeedsCompilation: no
+# Packaged: 2016-08-02 07:56:18 UTC; gaborcsardi
+# Author: Gabor Csardi [aut, cre], Jeroen Ooms [ctb]
+# Maintainer: Gabor Csardi <csardi.gabor@gmail.com>
+# Repository: CRAN
+# Date/Publication: 2016-08-02 13:05:29
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-rvest-feedstock contains modified content
diff --git a/r-rvest-feedstock/recipe/meta.yaml b/r-rvest-feedstock/recipe/meta.yaml
index 87ec2d9..22ac5e2 100644
--- a/r-rvest-feedstock/recipe/meta.yaml
+++ b/r-rvest-feedstock/recipe/meta.yaml
@@ -14,13 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/rvest/rvest_{{ version }}.tar.gz
   sha256: 0d6e8837fb1df79b1c83e7b48d8f1e6245f34a10c4bb6952e7bec7867e4abb12
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: testthat, knitr, png, stringi (>= 0.3.1), rmarkdown, covr
 requirements:
   build:
     - r-base
@@ -28,6 +36,7 @@ requirements:
     - r-magrittr
     - r-selectr
     - r-xml2
+
   run:
     - r-base
     - r-httr >=0.5
@@ -37,9 +46,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('rvest')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('rvest')"           # [not win]
     - "\"%R%\" -e \"library('rvest')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/hadley/rvest
   license: GPL-3
@@ -51,3 +67,32 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: rvest
+# Version: 0.3.2
+# Title: Easily Harvest (Scrape) Web Pages
+# Description: Wrappers around the 'xml2' and 'httr' packages to make it easy to download, then manipulate, HTML and XML.
+# Authors@R: c( person("Hadley", "Wickham", , "hadley@rstudio.com", c("aut", "cre")), person("RStudio", role = "cph") )
+# Depends: R (>= 3.0.1), xml2
+# Imports: httr (>= 0.5), selectr, magrittr
+# Suggests: testthat, knitr, png, stringi (>= 0.3.1), rmarkdown, covr
+# Encoding: UTF-8
+# License: GPL-3
+# LazyData: true
+# VignetteBuilder: knitr
+# RoxygenNote: 5.0.1
+# URL: https://github.com/hadley/rvest
+# BugReports: https://github.com/hadley/rvest/issues
+# NeedsCompilation: no
+# Packaged: 2016-06-16 16:20:43 UTC; hadley
+# Author: Hadley Wickham [aut, cre], RStudio [cph]
+# Maintainer: Hadley Wickham <hadley@rstudio.com>
+# Repository: CRAN
+# Date/Publication: 2016-06-17 08:57:12
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-rzmq-feedstock contains modified content
diff --git a/r-rzmq-feedstock/recipe/meta.yaml b/r-rzmq-feedstock/recipe/meta.yaml
index 23e4224..54bdcb5 100644
--- a/r-rzmq-feedstock/recipe/meta.yaml
+++ b/r-rzmq-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '0.9.1' %}
+{% set version = '0.9.3' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,50 +12,84 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/rzmq_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/rzmq/rzmq_{{ version }}.tar.gz
+  sha256: ab6f6da1fcd0c11003517a21bd85f5c56495e6926542db3fb320f3901f292b13
 
 
-  sha256: 7d15f5856a8338ed343b909a03075ac5146ad27ff8eadb3d4c8c0025633b0b4c
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
-  skip: True  # [win32]
 
 
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
-    - zeromq >=3           # [not win]
-    - {{native}}zeromq     # [win]
-    - pkg-config           # [unix]
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('cxx') }}      # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}autoconf
+    - {{posix}}automake
+    - {{posix}}pkg-config
+    - {{posix}}make
+    - zeromq
 
   run:
     - r-base
-    - zeromq >=3           # [not win]
-    - {{native}}zeromq     # [win]
+    - {{native}}gcc-libs         # [win]
+    - zeromq
 
 test:
   commands:
-    - $R -e "library('rzmq')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('rzmq')"           # [not win]
     - "\"%R%\" -e \"library('rzmq')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
 
+  # requires:
+    # Put any additional test requirements here.
 
 about:
-  home: http://github.com/ropensci/rzmq
+  home: http://github.com/ropensci/rzmq#readme (devel) http://www.zeromq.org (upstream)
   license: GPL-3
   summary: Interface to the 'ZeroMQ' lightweight messaging kernel (see <http://www.zeromq.org/>
     for more information).
   license_family: GPL3
 
-
-
 extra:
   recipe-maintainers:
     - croth1
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: rzmq
+# Title: R Bindings for 'ZeroMQ'
+# Version: 0.9.3
+# Authors@R: c( person("Whit", "Armstrong", , "armstrong.whit@gmail.com", role = "aut"), person("Jeroen", "Ooms", , "jeroen@berkeley.edu", role = c("aut", "cre")))
+# Description: Interface to the 'ZeroMQ' lightweight messaging kernel (see <http://www.zeromq.org/> for more information).
+# License: GPL-3
+# Depends: R (>= 3.1.0)
+# SystemRequirements: ZeroMQ >= 3.0.0: libzmq3-dev (deb) or zeromq3-devel (rpm)
+# URL: http://github.com/ropensci/rzmq#readme (devel) http://www.zeromq.org (upstream)
+# BugReports: http://github.com/ropensci/rzmq/issues
+# NeedsCompilation: yes
+# Packaged: 2017-10-01 20:43:27 UTC; jeroen
+# Author: Whit Armstrong [aut], Jeroen Ooms [aut, cre]
+# Maintainer: Jeroen Ooms <jeroen@berkeley.edu>
+# Repository: CRAN
+# Date/Publication: 2017-10-02 07:29:15 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-sandwich-feedstock contains modified content
diff --git a/r-sandwich-feedstock/recipe/meta.yaml b/r-sandwich-feedstock/recipe/meta.yaml
index 61ebf5d..0f7d658 100644
--- a/r-sandwich-feedstock/recipe/meta.yaml
+++ b/r-sandwich-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '2.3-4' %}
+{% set version = '2.4-0' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,16 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/sandwich_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/sandwich/sandwich_{{ version }}.tar.gz
-  sha256: 2052f7e3d19a05c372f422c5480f1058a4107e420cd038a9bd7240c4f0746d4d
+  sha256: 34315b9ced76a3ed109aead7b8f77eea6398a6eb2e1ad71515a48bed9808ccc0
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: AER, car, geepack, lattice, lmtest, MASS, multiwayvcov, pcse, plm, pscl, scatterplot3d, stats4, strucchange, survival
 requirements:
   build:
     - r-base
@@ -33,17 +40,48 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('sandwich')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('sandwich')"           # [not win]
     - "\"%R%\" -e \"library('sandwich')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=sandwich
   license: GPL-2 | GPL-3
-  summary: Model-robust standard error estimators for cross-sectional, time series, and longitudinal
-    data.
+  summary: Model-robust standard error estimators for cross-sectional, time series, clustered,
+    panel, and longitudinal data.
   license_family: GPL3
 
 extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: sandwich
+# Version: 2.4-0
+# Date: 2017-07-26
+# Title: Robust Covariance Matrix Estimators
+# Authors@R: c(person(given = "Achim", family = "Zeileis", role = c("aut", "cre"), email = "Achim.Zeileis@R-project.org"), person(given = "Thomas", family = "Lumley", role = "aut", email = "tlumley@u.washington.edu"), person(given = "Susanne", family = "Berger", role = "ctb", email = "Susanne.Berger@uibk.ac.at"), person(given = "Nathaniel", family = "Graham", role = "ctb", email = "npgraham1@gmail.com"))
+# Description: Model-robust standard error estimators for cross-sectional, time series, clustered, panel, and longitudinal data.
+# Depends: R (>= 2.10.0)
+# Imports: stats, utils, zoo
+# Suggests: AER, car, geepack, lattice, lmtest, MASS, multiwayvcov, pcse, plm, pscl, scatterplot3d, stats4, strucchange, survival
+# License: GPL-2 | GPL-3
+# NeedsCompilation: no
+# Packaged: 2017-07-26 19:08:36 UTC; zeileis
+# Author: Achim Zeileis [aut, cre], Thomas Lumley [aut], Susanne Berger [ctb], Nathaniel Graham [ctb]
+# Maintainer: Achim Zeileis <Achim.Zeileis@R-project.org>
+# Repository: CRAN
+# Date/Publication: 2017-07-26 19:36:29 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-scales-feedstock contains modified content
diff --git a/r-scales-feedstock/recipe/meta.yaml b/r-scales-feedstock/recipe/meta.yaml
index 49e3583..cef02b7 100644
--- a/r-scales-feedstock/recipe/meta.yaml
+++ b/r-scales-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '0.4.1' %}
+{% set version = '0.5.0' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,47 +12,64 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/scales_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/scales/scales_{{ version }}.tar.gz
-  sha256: 642b88fb1fce7bac72a0038ce532b65b8a79dffe826fec25033cf386ab630cd3
+  sha256: dbfcc0817c4ab8b8777ec7d68ebfe220177c193cfb5bd0e8ba5d365dbfe3e97d
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: testthat (>= 0.8), bit64, covr, hms
 requirements:
   build:
     - r-base
+    - {{ compiler('cxx') }}      # [not win]
+    - {{native}}toolchain        # [win]
+    - r-r6
     - r-rcolorbrewer
     - r-rcpp
     - r-dichromat
     - r-labeling
     - r-munsell >=0.2
     - r-plyr
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - r-viridislite
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
+    - r-r6
     - r-rcolorbrewer
     - r-rcpp
     - r-dichromat
     - r-labeling
     - r-munsell >=0.2
     - r-plyr
-    - libgcc               # [not win]
+    - r-viridislite
 
 test:
   commands:
-    - $R -e "library('scales')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('scales')"           # [not win]
     - "\"%R%\" -e \"library('scales')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/hadley/scales
-  license: MIT
+  license: MIT + file LICENSE
   summary: Graphical scales map data to aesthetics, and provide methods for automatically determining
     breaks and labels for axes and legends.
   license_family: MIT
@@ -61,3 +78,31 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: scales
+# Version: 0.5.0
+# Authors@R: c( person("Hadley", "Wickham", , "hadley@rstudio.com", c("aut", "cre")), person("RStudio", role = "cph") )
+# Title: Scale Functions for Visualization
+# Description: Graphical scales map data to aesthetics, and provide methods for automatically determining breaks and labels for axes and legends.
+# URL: https://github.com/hadley/scales
+# BugReports: https://github.com/hadley/scales/issues
+# Depends: R (>= 2.13)
+# Imports: RColorBrewer, dichromat, plyr, munsell (>= 0.2), labeling, Rcpp, R6, viridisLite
+# LinkingTo: Rcpp
+# Suggests: testthat (>= 0.8), bit64, covr, hms
+# License: MIT + file LICENSE
+# LazyLoad: yes
+# RoxygenNote: 6.0.1
+# NeedsCompilation: yes
+# Packaged: 2017-08-23 21:21:31 UTC; Kara
+# Author: Hadley Wickham [aut, cre], RStudio [cph]
+# Maintainer: Hadley Wickham <hadley@rstudio.com>
+# Repository: CRAN
+# Date/Publication: 2017-08-24 14:35:38 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-seacarb-feedstock contains modified content
diff --git a/r-seacarb-feedstock/recipe/meta.yaml b/r-seacarb-feedstock/recipe/meta.yaml
index 2b4e342..3d9dcd7 100644
--- a/r-seacarb-feedstock/recipe/meta.yaml
+++ b/r-seacarb-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '3.1.1' %}
+{% set version = '3.2.2' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,29 +12,46 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/seacarb_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/seacarb/seacarb_{{ version }}.tar.gz
-  sha256: f297bdd028ffbe1d5413002e2a7eba0ddea800835b7a17dd5296a2e1cc4af9e0
+  sha256: b462a1f8f8e45e4b020c510b5eedb641be45f433842d09a2806504c4fa9c55f8
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
+
+
 requirements:
   build:
     - r-base
+    - r-gsw
     - r-oce
 
   run:
     - r-base
+    - r-gsw
     - r-oce
 
 test:
   commands:
-    - $R -e "library('seacarb')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('seacarb')"           # [not win]
     - "\"%R%\" -e \"library('seacarb')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=seacarb
   license: GPL (>= 2)
@@ -46,3 +63,29 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: seacarb
+# Title: Seawater Carbonate Chemistry
+# Version: 3.2.2
+# Date: 2017-08-22
+# LazyData: true
+# Authors@R: c(person("Jean-Pierre", "Gattuso", role = c("aut", "cre", "cph"), email = "gattuso@obs-vlfr.fr"), person("Jean-Marie", "Epitalon", role = "aut", email = "Jean-Marie.Epitalon@orange.fr"), person("Heloise", "Lavigne", role = "aut", email = "lavigne@obs-vlfr.fr"), person("James", "Orr", role = "aut"), person("Bernard", "Gentili", role = "ctb"), person("Mathilde", "Hagens", role = "ctb"), person("Andreas", "Hofmann", role = "ctb"), person("Aurelien", "Proye", role = "ctb"), person("Karline", "Soetaert", role = "ctb"), person("James", "Rae", role = "ctb") )
+# Depends: R (>= 2.10), oce, gsw
+# Description: Calculates parameters of the seawater carbonate system and assists the design of ocean acidification perturbation experiments.
+# Encoding: UTF-8
+# URL: https://CRAN.R-project.org/package=seacarb
+# License: GPL (>= 2)
+# Repository: CRAN
+# RoxygenNote: 5.0.1
+# NeedsCompilation: no
+# Packaged: 2017-09-26 04:29:34 UTC; gattuso
+# Author: Jean-Pierre Gattuso [aut, cre, cph], Jean-Marie Epitalon [aut], Heloise Lavigne [aut], James Orr [aut], Bernard Gentili [ctb], Mathilde Hagens [ctb], Andreas Hofmann [ctb], Aurelien Proye [ctb], Karline Soetaert [ctb], James Rae [ctb]
+# Maintainer: Jean-Pierre Gattuso <gattuso@obs-vlfr.fr>
+# Date/Publication: 2017-09-27 09:15:37 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-selectr-feedstock contains modified content
diff --git a/r-selectr-feedstock/recipe/meta.yaml b/r-selectr-feedstock/recipe/meta.yaml
index 3354fd9..b93242d 100644
--- a/r-selectr-feedstock/recipe/meta.yaml
+++ b/r-selectr-feedstock/recipe/meta.yaml
@@ -14,14 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/selectr/selectr_{{ version }}.tar.gz
   sha256: db4f7ceea4b522a54c3ae7709787b0b7fcf389c5d945c5a278e3625388218949
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: testthat, XML, xml2
 requirements:
   build:
     - r-base
@@ -33,12 +40,19 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('selectr')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('selectr')"           # [not win]
     - "\"%R%\" -e \"library('selectr')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://sjp.co.nz/projects/selectr
-  license: BSD_3_clause
+  license: BSD_3_clause + file LICENCE
   summary: Translates a CSS3 selector into an equivalent XPath expression. This allows us to
     use CSS selectors when working with the XML package as it can only evaluate XPath
     expressions. Also provided are convenience functions useful for using CSS selectors
@@ -49,3 +63,30 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: selectr
+# Type: Package
+# Title: Translate CSS Selectors to XPath Expressions
+# Version: 0.3-1
+# Date: 2016-12-18
+# Authors@R: c(person("Simon", "Potter", role = c("aut", "trl", "cre"), email = "simon@sjp.co.nz"), person("Simon", "Sapin", role = "aut"), person("Ian", "Bicking", role = "aut"))
+# License: BSD_3_clause + file LICENCE
+# Depends: R (>= 2.15.2)
+# Imports: methods, stringr
+# Suggests: testthat, XML, xml2
+# URL: https://sjp.co.nz/projects/selectr
+# BugReports: https://github.com/sjp/selectr/issues
+# Description: Translates a CSS3 selector into an equivalent XPath expression. This allows us to use CSS selectors when working with the XML package as it can only evaluate XPath expressions. Also provided are convenience functions useful for using CSS selectors on XML nodes. This package is a port of the Python package 'cssselect' (<https://pythonhosted.org/cssselect/>).
+# NeedsCompilation: no
+# Packaged: 2016-12-19 02:54:30 UTC; sjp
+# Author: Simon Potter [aut, trl, cre], Simon Sapin [aut], Ian Bicking [aut]
+# Maintainer: Simon Potter <simon@sjp.co.nz>
+# Repository: CRAN
+# Date/Publication: 2016-12-19 08:34:09
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-sf-feedstock contains modified content
diff --git a/r-sf-feedstock/recipe/meta.yaml b/r-sf-feedstock/recipe/meta.yaml
index 82cc3a3..db56ebf 100644
--- a/r-sf-feedstock/recipe/meta.yaml
+++ b/r-sf-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '0.4-1' %}
+{% set version = '0.5-4' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,48 +12,66 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/sf_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/sf/sf_{{ version }}.tar.gz
-  sha256: 5e4d5728565a6eb1905e6014288fde3c2cb0c5ea097d011f6baec10cfd795e63
+  sha256: fc31ea46c075a1de4f958d57b4fbd4bb9d5befe4d29288706ebb9a0e6239aac2
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: geosphere (>= 1.5-5), maptools, maps, rgdal, rgeos, sp (>= 1.2-4), RSQLite, RPostgreSQL, lazyeval, tibble, rlang, ggplot2, dplyr (>= 0.7-0), tidyr (>= 0.7-0), testthat, knitr, microbenchmark
 requirements:
   build:
     - r-base
-    - r-dbi
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('cxx') }}      # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}autoconf
+    - {{posix}}automake
+    - {{posix}}pkg-config
+    - {{posix}}make
+    - {{native}}libgdal
+    - r-dbi >=0.5
     - r-rcpp
     - r-magrittr
-    - r-units >=0.4
-    - gdal >=2.1.3
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - r-units >=0.4_6
 
   run:
     - r-base
-    - r-dbi
+    - {{native}}gcc-libs         # [win]
+    - {{native}}libgdal
+    - r-dbi >=0.5
     - r-rcpp
     - r-magrittr
-    - r-units >=0.4
-    - gdal >=2.1.3
-    - libgcc  # [not win]
+    - r-units >=0.4_6
 
 test:
   commands:
-    - $R -e "library('sf')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('sf')"           # [not win]
     - "\"%R%\" -e \"library('sf')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
-  home: https://github.com/edzer/sfr/
-  license: GPL-2
-  summary: |
-    Support for simple features, a standardized way to encode spatial vector data.
-    Binds  to GDAL for reading and writing data, to GEOS for geometrical operations, and to Proj.4
+  home: https://github.com/r-spatial/sf/
+  license: GPL-2 | MIT + file LICENSE
+  summary: Support for simple features, a standardized way to encode spatial vector data. Binds  to
+    GDAL for reading and writing data, to GEOS for geometrical operations, and to Proj.4
     for projection conversions and datum transformations.
   license_family: GPL2
 
@@ -61,3 +79,33 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: sf
+# Version: 0.5-4
+# Title: Simple Features for R
+# Description: Support for simple features, a standardized way to encode spatial vector data. Binds  to GDAL for reading and writing data, to GEOS for geometrical operations, and to Proj.4 for projection conversions and datum transformations.
+# Authors@R: c(person("Edzer", "Pebesma", role = c("aut", "cre"), email = "edzer.pebesma@uni-muenster.de"), person("Roger", "Bivand", role = "ctb"), person("Ian", "Cook", role = "ctb"), person("Tim", "Keitt", role = "ctb"), person("Michael", "Sumner", role = "ctb"), person("Robin", "Lovelace", role = "ctb"), person("Hadley", "Wickham", role = "ctb"), person("Jeroen", "Ooms", role = "ctb"), person("Etienne", "Racine", role = "ctb"))
+# Depends: R (>= 3.3.0)
+# Imports: utils, stats, tools, graphics, grDevices, grid, methods, Rcpp, DBI (>= 0.5), units (>= 0.4-6), magrittr
+# Suggests: geosphere (>= 1.5-5), maptools, maps, rgdal, rgeos, sp (>= 1.2-4), RSQLite, RPostgreSQL, lazyeval, tibble, rlang, ggplot2, dplyr (>= 0.7-0), tidyr (>= 0.7-0), testthat, knitr, microbenchmark
+# LinkingTo: Rcpp
+# VignetteBuilder: knitr
+# SystemRequirements: GDAL (>= 2.0.0), GEOS (>= 3.3.0), PROJ.4 (>= 4.8.0)
+# License: GPL-2 | MIT + file LICENSE
+# URL: https://github.com/r-spatial/sf/
+# BugReports: https://github.com/r-spatial/sf/issues/
+# Collate: RcppExports.R init.R crs.R bbox.R read.R db.R sfc.R sfg.R sf.R bind.R wkb.R wkt.R plot.R geom.R transform.R sp.R grid.R arith.R tidyverse.R cast_sfg.R cast_sfc.R graticule.R datasets.R aggregate.R agr.R maps.R join.R sample.R valid.R geohash.R split.R
+# RoxygenNote: 6.0.1
+# NeedsCompilation: yes
+# Packaged: 2017-08-28 06:25:10 UTC; edzer
+# Author: Edzer Pebesma [aut, cre], Roger Bivand [ctb], Ian Cook [ctb], Tim Keitt [ctb], Michael Sumner [ctb], Robin Lovelace [ctb], Hadley Wickham [ctb], Jeroen Ooms [ctb], Etienne Racine [ctb]
+# Maintainer: Edzer Pebesma <edzer.pebesma@uni-muenster.de>
+# Repository: CRAN
+# Date/Publication: 2017-08-28 15:44:19 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
diff --git a/r-sfsmisc-feedstock/recipe/bld.bat b/r-sfsmisc-feedstock/recipe/bld.bat
index e69de29..8b10c94 100644
--- a/r-sfsmisc-feedstock/recipe/bld.bat
+++ b/r-sfsmisc-feedstock/recipe/bld.bat
@@ -0,0 +1,8 @@
+"%R%" CMD INSTALL --build .
+if errorlevel 1 exit 1
+
+@rem Add more build steps here, if they are necessary.
+
+@rem See
+@rem http://docs.continuum.io/conda/build.html
+@rem for a list of environment variables that are set during the build process.
diff --git a/r-sfsmisc-feedstock/recipe/build.sh b/r-sfsmisc-feedstock/recipe/build.sh
index e69de29..0c37df6 100644
--- a/r-sfsmisc-feedstock/recipe/build.sh
+++ b/r-sfsmisc-feedstock/recipe/build.sh
@@ -0,0 +1,13 @@
+#!/bin/bash
+
+# R refuses to build packages that mark themselves as Priority: Recommended
+mv DESCRIPTION DESCRIPTION.old
+grep -v '^Priority: ' DESCRIPTION.old > DESCRIPTION
+
+$R CMD INSTALL --build .
+
+# Add more build steps here, if they are necessary.
+
+# See
+# http://docs.continuum.io/conda/build.html
+# for a list of environment variables that are set during the build process.
diff --git a/r-sfsmisc-feedstock/recipe/meta.yaml b/r-sfsmisc-feedstock/recipe/meta.yaml
index e69de29..de8e2ab 100644
--- a/r-sfsmisc-feedstock/recipe/meta.yaml
+++ b/r-sfsmisc-feedstock/recipe/meta.yaml
@@ -0,0 +1,85 @@
+{% set version = '1.1-1' %}
+
+{% set posix = 'm2-' if win else '' %}
+{% set native = 'm2w64-' if win else '' %}
+
+package:
+  name: r-sfsmisc
+  version: {{ version|replace("-", "_") }}
+
+source:
+  fn: sfsmisc_{{ version }}.tar.gz
+  url:
+    - https://cran.r-project.org/src/contrib/sfsmisc_{{ version }}.tar.gz
+    - https://cran.r-project.org/src/contrib/Archive/sfsmisc/sfsmisc_{{ version }}.tar.gz
+  sha256: 0eaab03d3023233f4e869616a5cbc58939f8ac8fa0f94ae1d1dd7d4fb95ff54b
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
+build:
+  # If this is a new build for the same version, increment the build number.
+  number: 0
+
+  # This is required to make R link correctly on Linux.
+  rpaths:
+    - lib/R/lib/
+    - lib/
+
+# Suggests: datasets, tcltk, cluster, lattice, MASS, Matrix, nlme, lokern
+requirements:
+  build:
+    - r-base
+
+  run:
+    - r-base
+
+test:
+  commands:
+    # You can put additional test commands to be run here.
+    - $R -e "library('sfsmisc')"           # [not win]
+    - "\"%R%\" -e \"library('sfsmisc')\""  # [win]
+
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
+about:
+  home: https://CRAN.R-project.org/package=sfsmisc
+  license: GPL (>= 2)
+  summary: Useful utilities ['goodies'] from Seminar fuer Statistik ETH Zurich, quite a few related
+    to graphics; some were ported from S-plus.
+  license_family: GPL3
+
+extra:
+  recipe-maintainers:
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: sfsmisc
+# Title: Utilities from 'Seminar fuer Statistik' ETH Zurich
+# Version: 1.1-1
+# Date: 2017-06-08
+# Author: Martin Maechler et al.
+# Maintainer: Martin Maechler <maechler@stat.math.ethz.ch>
+# Description: Useful utilities ['goodies'] from Seminar fuer Statistik ETH Zurich, quite a few related to graphics; some were ported from S-plus.
+# Depends: R (>= 3.0.1)
+# Imports: grDevices, methods, utils, stats
+# Suggests: datasets, tcltk, cluster, lattice, MASS, Matrix, nlme, lokern
+# Enhances: mgcv, rpart, nor1mix, polycor, sm, tikzDevice
+# Encoding: latin1
+# ByteCompile: yes
+# License: GPL (>= 2)
+# NeedsCompilation: no
+# Packaged: 2017-06-08 08:37:43 UTC; maechler
+# Repository: CRAN
+# Date/Publication: 2017-06-08 16:46:33 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-shiny-feedstock contains modified content
diff --git a/r-shiny-feedstock/recipe/meta.yaml b/r-shiny-feedstock/recipe/meta.yaml
index b806fb5..751cb1a 100644
--- a/r-shiny-feedstock/recipe/meta.yaml
+++ b/r-shiny-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '1.0.3' %}
+{% set version = '1.0.5' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,23 +12,30 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/shiny_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/shiny/shiny_{{ version }}.tar.gz
-  sha256: 3a2a5a4437dbcfba1b640ef858667a59ee593754a17b73169b10003183574486
+  sha256: 20e25f3f72f3608a2151663f7836f2e0c6da32683a555d7541063ae7a935fa42
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: datasets, Cairo (>= 1.5-5), testthat, knitr (>= 1.6), markdown, rmarkdown, ggplot2, magrittr
 requirements:
   build:
     - r-base
     - r-r6 >=2.0
     - r-digest
     - r-htmltools >=0.3.5
-    - r-httpuv >=1.3.3
+    - r-httpuv >=1.3.5
     - r-jsonlite >=0.9.16
     - r-mime >=0.3
     - r-sourcetools
@@ -39,7 +46,7 @@ requirements:
     - r-r6 >=2.0
     - r-digest
     - r-htmltools >=0.3.5
-    - r-httpuv >=1.3.3
+    - r-httpuv >=1.3.5
     - r-jsonlite >=0.9.16
     - r-mime >=0.3
     - r-sourcetools
@@ -47,12 +54,19 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('shiny')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('shiny')"           # [not win]
     - "\"%R%\" -e \"library('shiny')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://shiny.rstudio.com
-  license: GPL-3
+  license: GPL-3 | file LICENSE
   summary: Makes it incredibly easy to build interactive web applications with R. Automatic "reactive"
     binding between inputs and outputs and extensive prebuilt widgets make it possible
     to build beautiful, responsive, and powerful applications with minimal effort.
@@ -62,3 +76,31 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: shiny
+# Type: Package
+# Title: Web Application Framework for R
+# Version: 1.0.5
+# Authors@R: c( person("Winston", "Chang", role = c("aut", "cre"), email = "winston@rstudio.com"), person("Joe", "Cheng", role = "aut", email = "joe@rstudio.com"), person("JJ", "Allaire", role = "aut", email = "jj@rstudio.com"), person("Yihui", "Xie", role = "aut", email = "yihui@rstudio.com"), person("Jonathan", "McPherson", role = "aut", email = "jonathan@rstudio.com"), person(family = "RStudio", role = "cph"), person(family = "jQuery Foundation", role = "cph", comment = "jQuery library and jQuery UI library"), person(family = "jQuery contributors", role = c("ctb", "cph"), comment = "jQuery library; authors listed in inst/www/shared/jquery-AUTHORS.txt"), person(family = "jQuery UI contributors", role = c("ctb", "cph"), comment = "jQuery UI library; authors listed in inst/www/shared/jqueryui/AUTHORS.txt"), person("Mark", "Otto", role = "ctb", comment = "Bootstrap library"), person("Jacob", "Thornton", role = "ctb", comment = "Bootstrap library"), person(family = "Bootstrap contributors", role = "ctb", comment = "Bootstrap library"), person(family = "Twitter, Inc", role = "cph", comment = "Bootstrap library"), person("Alexander", "Farkas", role = c("ctb", "cph"), comment = "html5shiv library"), person("Scott", "Jehl", role = c("ctb", "cph"), comment = "Respond.js library"), person("Stefan", "Petre", role = c("ctb", "cph"), comment = "Bootstrap-datepicker library"), person("Andrew", "Rowls", role = c("ctb", "cph"), comment = "Bootstrap-datepicker library"), person("Dave", "Gandy", role = c("ctb", "cph"), comment = "Font-Awesome font"), person("Brian", "Reavis", role = c("ctb", "cph"), comment = "selectize.js library"), person("Kristopher Michael", "Kowal", role = c("ctb", "cph"), comment = "es5-shim library"), person(family = "es5-shim contributors", role = c("ctb", "cph"), comment = "es5-shim library"), person("Denis", "Ineshin", role = c("ctb", "cph"), comment = "ion.rangeSlider library"), person("Sami", "Samhuri", role = c("ctb", "cph"), comment = "Javascript strftime library"), person(family = "SpryMedia Limited", role = c("ctb", "cph"), comment = "DataTables library"), person("John", "Fraser", role = c("ctb", "cph"), comment = "showdown.js library"), person("John", "Gruber", role = c("ctb", "cph"), comment = "showdown.js library"), person("Ivan", "Sagalaev", role = c("ctb", "cph"), comment = "highlight.js library"), person(family = "R Core Team", role = c("ctb", "cph"), comment = "tar implementation from R") )
+# Description: Makes it incredibly easy to build interactive web applications with R. Automatic "reactive" binding between inputs and outputs and extensive prebuilt widgets make it possible to build beautiful, responsive, and powerful applications with minimal effort.
+# License: GPL-3 | file LICENSE
+# Depends: R (>= 3.0.2), methods
+# Imports: utils, httpuv (>= 1.3.5), mime (>= 0.3), jsonlite (>= 0.9.16), xtable, digest, htmltools (>= 0.3.5), R6 (>= 2.0), sourcetools, tools
+# Suggests: datasets, Cairo (>= 1.5-5), testthat, knitr (>= 1.6), markdown, rmarkdown, ggplot2, magrittr
+# URL: http://shiny.rstudio.com
+# BugReports: https://github.com/rstudio/shiny/issues
+# Collate: 'app.R' 'bookmark-state-local.R' 'stack.R' 'bookmark-state.R' 'bootstrap-layout.R' 'conditions.R' 'map.R' 'globals.R' 'utils.R' 'bootstrap.R' 'cache.R' 'diagnose.R' 'fileupload.R' 'graph.R' 'reactives.R' 'reactive-domains.R' 'history.R' 'hooks.R' 'html-deps.R' 'htmltools.R' 'image-interact-opts.R' 'image-interact.R' 'imageutils.R' 'input-action.R' 'input-checkbox.R' 'input-checkboxgroup.R' 'input-date.R' 'input-daterange.R' 'input-file.R' 'input-numeric.R' 'input-password.R' 'input-radiobuttons.R' 'input-select.R' 'input-slider.R' 'input-submit.R' 'input-text.R' 'input-textarea.R' 'input-utils.R' 'insert-tab.R' 'insert-ui.R' 'jqueryui.R' 'middleware-shiny.R' 'middleware.R' 'modal.R' 'modules.R' 'notifications.R' 'priorityqueue.R' 'progress.R' 'react.R' 'render-plot.R' 'render-table.R' 'run-url.R' 'serializers.R' 'server-input-handlers.R' 'server.R' 'shiny-options.R' 'shiny.R' 'shinyui.R' 'shinywrappers.R' 'showcase.R' 'snapshot.R' 'tar.R' 'test-export.R' 'timer.R' 'update-input.R'
+# RoxygenNote: 6.0.1
+# NeedsCompilation: no
+# Packaged: 2017-08-23 18:15:30 UTC; winston
+# Author: Winston Chang [aut, cre], Joe Cheng [aut], JJ Allaire [aut], Yihui Xie [aut], Jonathan McPherson [aut], RStudio [cph], jQuery Foundation [cph] (jQuery library and jQuery UI library), jQuery contributors [ctb, cph] (jQuery library; authors listed in inst/www/shared/jquery-AUTHORS.txt), jQuery UI contributors [ctb, cph] (jQuery UI library; authors listed in inst/www/shared/jqueryui/AUTHORS.txt), Mark Otto [ctb] (Bootstrap library), Jacob Thornton [ctb] (Bootstrap library), Bootstrap contributors [ctb] (Bootstrap library), Twitter, Inc [cph] (Bootstrap library), Alexander Farkas [ctb, cph] (html5shiv library), Scott Jehl [ctb, cph] (Respond.js library), Stefan Petre [ctb, cph] (Bootstrap-datepicker library), Andrew Rowls [ctb, cph] (Bootstrap-datepicker library), Dave Gandy [ctb, cph] (Font-Awesome font), Brian Reavis [ctb, cph] (selectize.js library), Kristopher Michael Kowal [ctb, cph] (es5-shim library), es5-shim contributors [ctb, cph] (es5-shim library), Denis Ineshin [ctb, cph] (ion.rangeSlider library), Sami Samhuri [ctb, cph] (Javascript strftime library), SpryMedia Limited [ctb, cph] (DataTables library), John Fraser [ctb, cph] (showdown.js library), John Gruber [ctb, cph] (showdown.js library), Ivan Sagalaev [ctb, cph] (highlight.js library), R Core Team [ctb, cph] (tar implementation from R)
+# Maintainer: Winston Chang <winston@rstudio.com>
+# Repository: CRAN
+# Date/Publication: 2017-08-23 20:20:10 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-shinybs-feedstock contains modified content
diff --git a/r-shinybs-feedstock/recipe/meta.yaml b/r-shinybs-feedstock/recipe/meta.yaml
index 7560536..7ddb06a 100644
--- a/r-shinybs-feedstock/recipe/meta.yaml
+++ b/r-shinybs-feedstock/recipe/meta.yaml
@@ -14,18 +14,27 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/shinyBS/shinyBS_{{ version }}.tar.gz
   sha256: 51be29541e066d30c66e243393f20b0da705eba1b7ce7eeadea993bb2aa91166
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
     - r-htmltools
     - r-shiny >=0.11
+
   run:
     - r-base
     - r-htmltools
@@ -33,16 +42,48 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('shinyBS')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('shinyBS')"           # [not win]
     - "\"%R%\" -e \"library('shinyBS')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://ebailey78.github.io/shinyBS
   license: GPL-3
-  summary: Adds additional Twitter Bootstrap components to Shiny.
+  summary: 'Adds additional Twitter Bootstrap components to Shiny. '
+
   license_family: GPL3
 
 extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: shinyBS
+# Type: Package
+# Title: Twitter Bootstrap Components for Shiny
+# Version: 0.61
+# Date: 2015-03-30
+# Author: Eric Bailey
+# Maintainer: Eric Bailey <ebailey@idem.in.gov>
+# Description: Adds additional Twitter Bootstrap components to Shiny.
+# Imports: shiny (>= 0.11), htmltools
+# URL: https://ebailey78.github.io/shinyBS
+# BugReports: https://github.com/ebailey78/shinyBS/issues
+# License: GPL-3
+# Packaged: 2015-03-30 12:16:24 UTC; ebailey
+# NeedsCompilation: no
+# Repository: CRAN
+# Date/Publication: 2015-03-31 07:52:38
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
diff --git a/r-shinycssloaders-feedstock/recipe/meta.yaml b/r-shinycssloaders-feedstock/recipe/meta.yaml
index 71164c1..af80ecd 100644
--- a/r-shinycssloaders-feedstock/recipe/meta.yaml
+++ b/r-shinycssloaders-feedstock/recipe/meta.yaml
@@ -12,9 +12,9 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/shinycssloaders_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/shinycssloaders/shinycssloaders_{{ version }}.tar.gz
+  sha256: e4890ceeea49c9186cf2edc98c4ca55bbc562ab9cde240a53666b0534fd5ffae
 
 
-  sha256: e4890ceeea49c9186cf2edc98c4ca55bbc562ab9cde240a53666b0534fd5ffae
   # patches:
    # List any patch files here
    # - fix.patch
@@ -45,7 +45,7 @@ requirements:
 test:
   commands:
     # You can put additional test commands to be run here.
-    - $R -e "library('shinycssloaders')"  # [not win]
+    - $R -e "library('shinycssloaders')"           # [not win]
     - "\"%R%\" -e \"library('shinycssloaders')\""  # [win]
 
   # You can also put a file called run_test.py, run_test.sh, or run_test.bat
@@ -55,11 +55,16 @@ test:
     # Put any additional test requirements here.
 
 about:
-  home: https
+  home: https://github.com/andrewsali/shinycssloaders
   license: GPL-3
-  summary: Create a lightweight Shiny wrapper for the css-loaders created by Luke Hass <https
+  summary: Create a lightweight Shiny wrapper for the css-loaders created by Luke Hass <https://github.com/lukehaas/css-loaders>.
+    Wrapping a Shiny output will automatically show a loader when the output is (re)calculating.
   license_family: GPL3
 
+extra:
+  recipe-maintainers:
+    - mingwandroid
+
 # The original CRAN metadata for this package was:
 
 # Package: shinycssloaders
Submodule r-shinydashboard-feedstock contains modified content
diff --git a/r-shinydashboard-feedstock/recipe/meta.yaml b/r-shinydashboard-feedstock/recipe/meta.yaml
index 4706c9b..62a16dc 100644
--- a/r-shinydashboard-feedstock/recipe/meta.yaml
+++ b/r-shinydashboard-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '0.5.3' %}
+{% set version = '0.6.1' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,35 +12,50 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/shinydashboard_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/shinydashboard/shinydashboard_{{ version }}.tar.gz
-  sha256: 8292c7103ef35e92370fbb16fd363cc25a3598a6ca680aea4f46c7d5b110dc2a
+  sha256: 1ee38f257433d24455426bc9d85c36f588735a54fbf6143935fed9cccb3bf193
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
     - r-htmltools >=0.2.6
-    - r-shiny >=0.12.1
+    - r-shiny >=1.0.0
+
   run:
     - r-base
     - r-htmltools >=0.2.6
-    - r-shiny >=0.12.1
+    - r-shiny >=1.0.0
 
 test:
   commands:
-    - $R -e "library('shinydashboard')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('shinydashboard')"           # [not win]
     - "\"%R%\" -e \"library('shinydashboard')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://rstudio.github.io/shinydashboard/
-  license: GPL-2
-  summary: |
-    Create dashboards with 'Shiny'. This package provides a theme on top of 'Shiny', making
+  license: GPL-2 | file LICENSE
+  summary: Create dashboards with 'Shiny'. This package provides a theme on top of 'Shiny', making
     it easy to create attractive dashboards.
   license_family: GPL2
 
@@ -48,3 +63,28 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: shinydashboard
+# Title: Create Dashboards with 'Shiny'
+# Version: 0.6.1
+# Authors@R: c( person("Winston", "Chang", role = c("aut", "cre"), email = "winston@rstudio.com"), person("Barbara", "Borges Ribeiro", role = "aut", email = "barbara@rstudio.com"), person(family = "RStudio", role = "cph"), person(family = "Almasaeed Studio", role = c("ctb", "cph"), comment = "AdminLTE theme for Bootstrap"), person(family = "Adobe Systems Incorporated", role = c("ctb", "cph"), comment = "Source Sans Pro font") )
+# Description: Create dashboards with 'Shiny'. This package provides a theme on top of 'Shiny', making it easy to create attractive dashboards.
+# URL: http://rstudio.github.io/shinydashboard/
+# Depends: R (>= 3.0)
+# License: GPL-2 | file LICENSE
+# Imports: utils, shiny (>= 1.0.0), htmltools (>= 0.2.6)
+# BugReports: https://github.com/rstudio/shinydashboard
+# RoxygenNote: 6.0.1
+# NeedsCompilation: no
+# Packaged: 2017-06-14 17:18:24 UTC; barbaraborges
+# Author: Winston Chang [aut, cre], Barbara Borges Ribeiro [aut], RStudio [cph], Almasaeed Studio [ctb, cph] (AdminLTE theme for Bootstrap), Adobe Systems Incorporated [ctb, cph] (Source Sans Pro font)
+# Maintainer: Winston Chang <winston@rstudio.com>
+# Repository: CRAN
+# Date/Publication: 2017-06-14 23:37:34 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-shinyjs-feedstock contains modified content
diff --git a/r-shinyjs-feedstock/recipe/meta.yaml b/r-shinyjs-feedstock/recipe/meta.yaml
index 8ffb7ca..26574a0 100644
--- a/r-shinyjs-feedstock/recipe/meta.yaml
+++ b/r-shinyjs-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '0.9' %}
+{% set version = '0.9.1' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,15 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/shinyjs_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/shinyjs/shinyjs_{{ version }}.tar.gz
-  sha256: 739a1af0d76ce4c594047ac3da43af85f9c8ed1d5e7be37bcf1a20ccae6c09c2
+  sha256: aa2a0f17d00848703e5ed9bfb3231c8c942cbd848b76d9a8ca6ed1911c992041
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: knitr (>= 1.7), rmarkdown, rstudioapi (>= 0.5), shinyAce, testthat (>= 0.9.1), V8 (>= 0.6)
 requirements:
   build:
     - r-base
@@ -29,6 +37,7 @@ requirements:
     - r-jsonlite
     - r-miniui >=0.1.1
     - r-shiny >=0.11.1
+
   run:
     - r-base
     - r-digest >=0.6.8
@@ -39,18 +48,58 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('shinyjs')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('shinyjs')"           # [not win]
     - "\"%R%\" -e \"library('shinyjs')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://deanattali.com/shinyjs
-  license: MIT
-  summary: |
-    Perform common useful JavaScript operations in Shiny apps that will greatly improve
-    your apps without having to know any JavaScript.
-  license_family: MIT
+  license: AGPL-3
+  summary: 'Perform common useful JavaScript operations in Shiny apps that will greatly improve
+    your apps without having to know any JavaScript. Examples include: hiding an element,
+    disabling an input, resetting an input back to its original value, delaying code
+    execution by a few seconds, and many more useful functions for both the end user
+    and the developer. ''shinyjs'' can also be used to easily call your own custom JavaScript
+    functions from R.'
+
+  license_family: AGPL
 
 extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: shinyjs
+# Title: Easily Improve the User Experience of Your Shiny Apps in Seconds
+# Version: 0.9.1
+# Authors@R: person("Dean", "Attali", email = "daattali@gmail.com", role = c("aut", "cre"))
+# Description: Perform common useful JavaScript operations in Shiny apps that will greatly improve your apps without having to know any JavaScript. Examples include: hiding an element, disabling an input, resetting an input back to its original value, delaying code execution by a few seconds, and many more useful functions for both the end user and the developer. 'shinyjs' can also be used to easily call your own custom JavaScript functions from R.
+# URL: http://deanattali.com/shinyjs
+# BugReports: https://github.com/daattali/shinyjs/issues
+# Depends: R (>= 3.1.0)
+# Imports: digest (>= 0.6.8), htmltools (>= 0.2.6), jsonlite, miniUI (>= 0.1.1), shiny (>= 0.11.1), stats
+# Suggests: knitr (>= 1.7), rmarkdown, rstudioapi (>= 0.5), shinyAce, testthat (>= 0.9.1), V8 (>= 0.6)
+# License: AGPL-3
+# SystemRequirements: pandoc with https support
+# LazyData: true
+# VignetteBuilder: knitr
+# RoxygenNote: 5.0.1
+# NeedsCompilation: no
+# Packaged: 2017-06-29 15:03:56 UTC; Dean
+# Author: Dean Attali [aut, cre]
+# Maintainer: Dean Attali <daattali@gmail.com>
+# Repository: CRAN
+# Date/Publication: 2017-06-29 22:26:58 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-shinythemes-feedstock contains modified content
diff --git a/r-shinythemes-feedstock/recipe/meta.yaml b/r-shinythemes-feedstock/recipe/meta.yaml
index cdbfe44..f2dbcad 100644
--- a/r-shinythemes-feedstock/recipe/meta.yaml
+++ b/r-shinythemes-feedstock/recipe/meta.yaml
@@ -14,13 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/shinythemes/shinythemes_{{ version }}.tar.gz
   sha256: e6dfa2bd6f4a259feb54a9c910b21cfbb2b22efc4506d8e23007b75080074244
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
@@ -32,14 +40,20 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('shinythemes')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('shinythemes')"           # [not win]
     - "\"%R%\" -e \"library('shinythemes')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=shinythemes
-  license: GPL-3
-  summary: |
-    Themes for use with Shiny. Includes several Bootstrap themes from <http://bootswatch.com/>,
+  license: GPL-3 | file LICENSE
+  summary: Themes for use with Shiny. Includes several Bootstrap themes from <http://bootswatch.com/>,
     which are packaged for use with Shiny applications.
   license_family: GPL3
 
@@ -47,3 +61,26 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: shinythemes
+# Title: Themes for Shiny
+# Version: 1.1.1
+# Authors@R: c( person("Winston", "Chang", role = c("aut", "cre"), email = "winston@rstudio.com"), person(family = "RStudio", role = "cph"), person("Thomas", "Park", role = c("ctb", "cph"), comment = "Bootswatch themes"), person("Lukasz", "Dziedzic", role = c("ctb", "cph"), comment = "Lato font"), person("Nathan", "Willis", role = c("ctb", "cph"), comment = "News Cycle font"), person(family = "Google Corporation", role = c("ctb", "cph"), comment = "Open Sans and Roboto fonts"), person("Matt", "McInerney", role = c("ctb", "cph"), comment = "Raleway font"), person(family = "Adobe Systems Incorporated", role = c("ctb", "cph"), comment = "Source Sans Pro font"), person(family = "Canonical Ltd", role = c("ctb", "cph"), comment = "Ubuntu font") )
+# Description: Themes for use with Shiny. Includes several Bootstrap themes from <http://bootswatch.com/>, which are packaged for use with Shiny applications.
+# Depends: R (>= 3.0.0)
+# Imports: shiny (>= 0.11)
+# License: GPL-3 | file LICENSE
+# RoxygenNote: 5.0.1
+# NeedsCompilation: no
+# Packaged: 2016-10-12 01:29:50 UTC; winston
+# Author: Winston Chang [aut, cre], RStudio [cph], Thomas Park [ctb, cph] (Bootswatch themes), Lukasz Dziedzic [ctb, cph] (Lato font), Nathan Willis [ctb, cph] (News Cycle font), Google Corporation [ctb, cph] (Open Sans and Roboto fonts), Matt McInerney [ctb, cph] (Raleway font), Adobe Systems Incorporated [ctb, cph] (Source Sans Pro font), Canonical Ltd [ctb, cph] (Ubuntu font)
+# Maintainer: Winston Chang <winston@rstudio.com>
+# Repository: CRAN
+# Date/Publication: 2016-10-12 09:00:52
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-slam-feedstock contains modified content
diff --git a/r-slam-feedstock/recipe/meta.yaml b/r-slam-feedstock/recipe/meta.yaml
index 08ef377..2ec6a2e 100644
--- a/r-slam-feedstock/recipe/meta.yaml
+++ b/r-slam-feedstock/recipe/meta.yaml
@@ -12,35 +12,47 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/slam_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/slam/slam_{{ version }}.tar.gz
+  sha256: afaca1d4712aa977028215d968c71c20c7de5663b2ce51cc217091af91c625b6
 
 
-  sha256: afaca1d4712aa977028215d968c71c20c7de5663b2ce51cc217091af91c625b6
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
-  skip: True  # [win32]
 
 
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('fortran') }}  # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('slam')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('slam')"           # [not win]
     - "\"%R%\" -e \"library('slam')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
 
+  # requires:
+    # Put any additional test requirements here.
 
 about:
   home: https://CRAN.R-project.org/package=slam
@@ -49,8 +61,29 @@ about:
     and simple triplet representations, respectively.
   license_family: GPL2
 
-
-
 extra:
   recipe-maintainers:
     - croth1
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: slam
+# Version: 0.1-40
+# Title: Sparse Lightweight Arrays and Matrices
+# Authors@R: c(person("Kurt", "Hornik", role = c("aut", "cre"), email = "Kurt.Hornik@R-project.org"), person("David", "Meyer", role = "aut"), person("Christian", "Buchta", role = "aut"))
+# Description: Data structures and algorithms for sparse arrays and matrices, based on index arrays and simple triplet representations, respectively.
+# Depends: R (>= 3.3.1)
+# Imports: stats
+# Enhances: Matrix, SparseM, spam
+# License: GPL-2
+# NeedsCompilation: yes
+# Packaged: 2016-12-01 07:00:30 UTC; hornik
+# Author: Kurt Hornik [aut, cre], David Meyer [aut], Christian Buchta [aut]
+# Maintainer: Kurt Hornik <Kurt.Hornik@R-project.org>
+# Repository: CRAN
+# Date/Publication: 2016-12-01 16:51:19
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-snowballc-feedstock contains modified content
diff --git a/r-snowballc-feedstock/recipe/meta.yaml b/r-snowballc-feedstock/recipe/meta.yaml
index 3c7c861..1df69f2 100644
--- a/r-snowballc-feedstock/recipe/meta.yaml
+++ b/r-snowballc-feedstock/recipe/meta.yaml
@@ -12,49 +12,80 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/SnowballC_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/SnowballC/SnowballC_{{ version }}.tar.gz
+  sha256: 44170624fd906ecc67959fed5dbf80068b59b1b2a6fbab65da5054e3e1186f4d
 
 
-  sha256: 44170624fd906ecc67959fed5dbf80068b59b1b2a6fbab65da5054e3e1186f4d
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
-  skip: True  # [win32]
 
 
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('SnowballC')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('SnowballC')"           # [not win]
     - "\"%R%\" -e \"library('SnowballC')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
 
+  # requires:
+    # Put any additional test requirements here.
 
 about:
   home: https://r-forge.r-project.org/projects/r-temis/
-  license: BSD_2_clause
+  license: BSD_2_clause + file LICENSE
   summary: An R interface to the C libstemmer library that implements Porter's word stemming
     algorithm for collapsing words to a common root to aid comparison of vocabulary.
     Currently supported languages are Danish, Dutch, English, Finnish, French, German,
     Hungarian, Italian, Norwegian, Portuguese, Romanian, Russian, Spanish, Swedish and
     Turkish.
   license_family: BSD
-  license_file: LICENSE
-
-
 
 extra:
   recipe-maintainers:
     - croth1
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: SnowballC
+# Type: Package
+# Version: 0.5.1
+# Date: 2014-08-08
+# Title: Snowball stemmers based on the C libstemmer UTF-8 library
+# Authors@R: person("Milan", "Bouchet-Valat", email="nalimilan@club.fr", role=c("aut", "cre"))
+# Description: An R interface to the C libstemmer library that implements Porter's word stemming algorithm for collapsing words to a common root to aid comparison of vocabulary. Currently supported languages are Danish, Dutch, English, Finnish, French, German, Hungarian, Italian, Norwegian, Portuguese, Romanian, Russian, Spanish, Swedish and Turkish.
+# License: BSD_2_clause + file LICENSE
+# Copyright: Dr Martin Porter (2001) for the libstemmer C library, and Milan Bouchet-Valat (2013) for the R package contents
+# URL: https://r-forge.r-project.org/projects/r-temis/
+# BugReports: https://r-forge.r-project.org/tracker/?group_id=1437
+# Packaged: 2014-08-08 17:33:26 UTC; milan
+# Author: Milan Bouchet-Valat [aut, cre]
+# Maintainer: Milan Bouchet-Valat <nalimilan@club.fr>
+# NeedsCompilation: yes
+# Repository: CRAN
+# Date/Publication: 2014-08-09 00:17:57
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-sourcetools-feedstock contains modified content
diff --git a/r-sourcetools-feedstock/recipe/meta.yaml b/r-sourcetools-feedstock/recipe/meta.yaml
index 4d526b8..2de0c2d 100644
--- a/r-sourcetools-feedstock/recipe/meta.yaml
+++ b/r-sourcetools-feedstock/recipe/meta.yaml
@@ -12,46 +12,82 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/sourcetools_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/sourcetools/sourcetools_{{ version }}.tar.gz
+  sha256: c9f48d2f0b7f7ed0e7fecdf8e730b0b80c4d567f0e1e880d118b0944b1330c51
 
 
-  sha256: c9f48d2f0b7f7ed0e7fecdf8e730b0b80c4d567f0e1e880d118b0944b1330c51
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
-  skip: True  # [win32]
 
+# Suggests: testthat
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('cxx') }}      # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('sourcetools')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('sourcetools')"           # [not win]
     - "\"%R%\" -e \"library('sourcetools')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
 
+  # requires:
+    # Put any additional test requirements here.
 
 about:
   home: https://CRAN.R-project.org/package=sourcetools
-  license: MIT
+  license: MIT + file LICENSE
   summary: Tools for the reading and tokenization of R code. The 'sourcetools' package provides
     both an R and C++ interface for the tokenization of R code, and helpers for interacting
     with the tokenized representation of R code.
   license_family: MIT
-  license_file: LICENSE
-
-
 
 extra:
   recipe-maintainers:
     - croth1
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: sourcetools
+# Type: Package
+# Title: Tools for Reading, Tokenizing and Parsing R Code
+# Version: 0.1.6
+# Author: Kevin Ushey
+# Maintainer: Kevin Ushey <kevinushey@gmail.com>
+# Description: Tools for the reading and tokenization of R code. The 'sourcetools' package provides both an R and C++ interface for the tokenization of R code, and helpers for interacting with the tokenized representation of R code.
+# License: MIT + file LICENSE
+# LazyData: TRUE
+# Depends: R (>= 3.0.2)
+# Suggests: testthat
+# RoxygenNote: 5.0.1
+# BugReports: https://github.com/kevinushey/sourcetools/issues
+# Encoding: UTF-8
+# NeedsCompilation: yes
+# Packaged: 2017-04-05 19:41:15 UTC; kevin
+# Repository: CRAN
+# Date/Publication: 2017-04-05 21:57:43 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-sp-feedstock contains modified content
diff --git a/r-sp-feedstock/recipe/meta.yaml b/r-sp-feedstock/recipe/meta.yaml
index a9e3a72..cbf590e 100644
--- a/r-sp-feedstock/recipe/meta.yaml
+++ b/r-sp-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '1.2-4' %}
+{% set version = '1.2-5' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,32 +12,47 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/sp_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/sp/sp_{{ version }}.tar.gz
-  sha256: 62dac9c4fe5de22f59ba460d8a09ee27d9778aba1998e9c5ee625500e4502b33
+  sha256: 0d2f84102591ed57d4b5d361a7a42bc72d2a9d49f38c6227e87e8ccb946dde27
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: RColorBrewer, rgdal (>= 0.8-7), rgeos (>= 0.3-13), gstat, maptools, deldir
 requirements:
   build:
     - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
     - r-lattice
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-lattice
 
 test:
   commands:
-    - $R -e "library('sp')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('sp')"           # [not win]
     - "\"%R%\" -e \"library('sp')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/edzer/sp/ https://edzer.github.io/sp/
   license: GPL (>= 2)
@@ -51,3 +66,29 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: sp
+# Version: 1.2-5
+# Title: Classes and Methods for Spatial Data
+# Authors@R: c(person("Edzer", "Pebesma", role = c("aut", "cre"), email = "edzer.pebesma@uni-muenster.de"), person("Roger", "Bivand", role = "aut", email = "Roger.Bivand@nhh.no"), person("Barry", "Rowlingson", role = "ctb"), person("Virgilio", "Gomez-Rubio", role = "ctb"), person("Robert", "Hijmans", role = "ctb"), person("Michael", "Sumner", role = "ctb"), person("Don", "MacQueen", role = "ctb"), person("Jim", "Lemon", role = "ctb"), person("Josh", "O'Brien", role = "ctb"))
+# Depends: R (>= 3.0.0), methods
+# Imports: utils, stats, graphics, grDevices, lattice, grid
+# Suggests: RColorBrewer, rgdal (>= 0.8-7), rgeos (>= 0.3-13), gstat, maptools, deldir
+# Description: Classes and methods for spatial data; the classes document where the spatial location information resides, for 2D or 3D data. Utility functions are provided, e.g. for plotting data as maps, spatial selection, as well as methods for retrieving coordinates, for subsetting, print, summary, etc.
+# License: GPL (>= 2)
+# URL: https://github.com/edzer/sp/ https://edzer.github.io/sp/
+# BugReports: https://github.com/edzer/sp/issues
+# Collate: bpy.colors.R AAA.R Class-CRS.R CRS-methods.R Class-Spatial.R Spatial-methods.R projected.R Class-SpatialPoints.R SpatialPoints-methods.R Class-SpatialPointsDataFrame.R SpatialPointsDataFrame-methods.R Class-SpatialMultiPoints.R SpatialMultiPoints-methods.R Class-SpatialMultiPointsDataFrame.R SpatialMultiPointsDataFrame-methods.R Class-GridTopology.R Class-SpatialGrid.R Class-SpatialGridDataFrame.R Class-SpatialLines.R SpatialLines-methods.R Class-SpatialLinesDataFrame.R SpatialLinesDataFrame-methods.R Class-SpatialPolygons.R Class-SpatialPolygonsDataFrame.R SpatialPolygons-methods.R SpatialPolygonsDataFrame-methods.R GridTopology-methods.R SpatialGrid-methods.R SpatialGridDataFrame-methods.R SpatialPolygons-internals.R point.in.polygon.R SpatialPolygons-displayMethods.R zerodist.R image.R stack.R bubble.R mapasp.R select.spatial.R gridded.R asciigrid.R spplot.R over.R spsample.R recenter.R dms.R gridlines.R spdists.R rbind.R flipSGDF.R chfids.R loadmeuse.R compassRose.R surfaceArea.R spOptions.R subset.R disaggregate.R sp_spat1.R merge.R aggregate.R
+# NeedsCompilation: yes
+# Packaged: 2017-06-27 12:59:17 UTC; edzer
+# Author: Edzer Pebesma [aut, cre], Roger Bivand [aut], Barry Rowlingson [ctb], Virgilio Gomez-Rubio [ctb], Robert Hijmans [ctb], Michael Sumner [ctb], Don MacQueen [ctb], Jim Lemon [ctb], Josh O'Brien [ctb]
+# Maintainer: Edzer Pebesma <edzer.pebesma@uni-muenster.de>
+# Repository: CRAN
+# Date/Publication: 2017-06-29 09:07:36 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-sparklyr-feedstock contains modified content
diff --git a/r-sparklyr-feedstock/recipe/meta.yaml b/r-sparklyr-feedstock/recipe/meta.yaml
index 55a5e42..066a6a5 100644
--- a/r-sparklyr-feedstock/recipe/meta.yaml
+++ b/r-sparklyr-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '0.5.4' %}
+{% set version = '0.6.3' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,59 +12,87 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/sparklyr_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/sparklyr/sparklyr_{{ version }}.tar.gz
-  sha256: 571472524d368308fbc2a1269bfdbf4ba9484f3e382a22501b8d0e696df84e98
+  sha256: e72c6ae132934d0bbb1a8894702fa9cc75eae82d06c972c5597ad8f428946886
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: ggplot2, janeaustenr, nycflights13, testthat, RCurl
 requirements:
   build:
     - r-base
-    - r-dbi >=0.6
+    - r-dbi >=0.6_1
     - r-assertthat
     - r-base64enc
-    - r-config
+    - r-broom
+    - r-config >=0.2
+    - r-dbplyr >=1.1.0
     - r-digest
-    - r-dplyr >=0.5.0
-    - r-httr
-    - r-jsonlite
+    - r-dplyr >=0.7.2
+    - r-httr >=1.2.1
+    - r-jsonlite >=1.4
     - r-lazyeval >=0.2.0
+    - r-openssl >=0.8
     - r-rappdirs
-    - r-readr >=0.2.0
+    - r-readr >=1.1.0
+    - r-rlang >=0.1
     - r-rprojroot
+    - r-rstudioapi
+    - r-shiny >=1.0.1
     - r-withr
+    - r-xml2
 
   run:
     - r-base
-    - r-dbi >=0.6
+    - r-dbi >=0.6_1
     - r-assertthat
     - r-base64enc
-    - r-config
+    - r-broom
+    - r-config >=0.2
+    - r-dbplyr >=1.1.0
     - r-digest
-    - r-dplyr >=0.5.0
-    - r-httr
-    - r-jsonlite
+    - r-dplyr >=0.7.2
+    - r-httr >=1.2.1
+    - r-jsonlite >=1.4
     - r-lazyeval >=0.2.0
+    - r-openssl >=0.8
     - r-rappdirs
-    - r-readr >=0.2.0
+    - r-readr >=1.1.0
+    - r-rlang >=0.1
     - r-rprojroot
+    - r-rstudioapi
+    - r-shiny >=1.0.1
     - r-withr
+    - r-xml2
 
 test:
   commands:
-    - $R -e "library('sparklyr')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('sparklyr')"           # [not win]
     - "\"%R%\" -e \"library('sparklyr')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://spark.rstudio.com
-  license: Apache 2.0
-  summary: |
-    R interface to Apache Spark, a fast and general engine for big data processing.
-    This package supports connecting to local and remote
+  license: Apache License 2.0 | file LICENSE
+  summary: R interface to Apache Spark, a fast and general engine for big data processing, see
+    <http://spark.apache.org>. This package supports connecting to local and remote
     Apache Spark clusters, provides a 'dplyr' compatible back-end, and provides an interface
     to Spark's built-in machine learning algorithms.
   license_family: APACHE
@@ -73,3 +101,32 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: sparklyr
+# Type: Package
+# Title: R Interface to Apache Spark
+# Version: 0.6.3
+# Authors@R: c( person("Javier", "Luraschi", email = "javier@rstudio.com", role = c("aut", "cre")), person("Kevin", "Ushey", role = "aut", email = "kevin@rstudio.com"), person("JJ", "Allaire", role = "aut", email = "jj@rstudio.com"), person(family = "RStudio", role = c("cph")), person(family = "The Apache Software Foundation", role = c("aut", "cph")) )
+# Maintainer: Javier Luraschi <javier@rstudio.com>
+# Description: R interface to Apache Spark, a fast and general engine for big data processing, see <http://spark.apache.org>. This package supports connecting to local and remote Apache Spark clusters, provides a 'dplyr' compatible back-end, and provides an interface to Spark's built-in machine learning algorithms.
+# License: Apache License 2.0 | file LICENSE
+# SystemRequirements: Spark: 1.6.x or 2.x
+# URL: http://spark.rstudio.com
+# BugReports: https://github.com/rstudio/sparklyr/issues
+# LazyData: TRUE
+# RoxygenNote: 6.0.1.9000
+# Depends: R (>= 3.1.2)
+# Imports: assertthat, base64enc, broom, config (>= 0.2), DBI (>= 0.6-1), dplyr (>= 0.7.2), dbplyr (>= 1.1.0), digest, httr (>= 1.2.1), jsonlite (>= 1.4), lazyeval (>= 0.2.0), methods, openssl (>= 0.8), rappdirs, readr (>= 1.1.0), rlang (>= 0.1), rprojroot, rstudioapi, shiny (>= 1.0.1), withr, xml2
+# Suggests: ggplot2, janeaustenr, nycflights13, testthat, RCurl
+# NeedsCompilation: no
+# Packaged: 2017-09-18 20:32:51 UTC; javierluraschi
+# Author: Javier Luraschi [aut, cre], Kevin Ushey [aut], JJ Allaire [aut], RStudio [cph], The Apache Software Foundation [aut, cph]
+# Repository: CRAN
+# Date/Publication: 2017-09-19 16:07:44 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-sparsem-feedstock contains modified content
diff --git a/r-sparsem-feedstock/recipe/meta.yaml b/r-sparsem-feedstock/recipe/meta.yaml
index 2c27b91..be0951b 100644
--- a/r-sparsem-feedstock/recipe/meta.yaml
+++ b/r-sparsem-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '1.76' %}
+{% set version = '1.77' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,35 +12,45 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/SparseM_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/SparseM/SparseM_{{ version }}.tar.gz
+  sha256: a9329fef14ae4fc646df1f4f6e57efb0211811599d015f7bc04c04285495d45c
 
 
-  sha256: c2c8e44376936a5fe6f09a37f3668016e66cbc687519cc952aa346a658a2b69b
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
-  skip: True  # [win32]
 
 
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('fortran') }}  # [not win]
+    - {{native}}toolchain        # [win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('SparseM')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('SparseM')"           # [not win]
     - "\"%R%\" -e \"library('SparseM')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
 
+  # requires:
+    # Put any additional test requirements here.
 
 about:
   home: http://www.econ.uiuc.edu/~roger/research/sparse/sparse.html
@@ -51,8 +61,28 @@ about:
 
   license_family: GPL3
 
-
-
 extra:
   recipe-maintainers:
     - croth1
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: SparseM
+# Version: 1.77
+# Author: Roger Koenker <rkoenker@uiuc.edu> and Pin Ng <Pin.Ng@NAU.EDU>
+# Maintainer: Roger Koenker <rkoenker@uiuc.edu>
+# Depends: R (>= 2.15), methods
+# Imports: graphics, stats, utils
+# Description: Some basic linear algebra functionality for sparse matrices is provided:  including Cholesky decomposition and backsolving as well as  standard R subsetting and Kronecker products.
+# License: GPL (>= 2)
+# Title: Sparse Linear Algebra
+# URL: http://www.econ.uiuc.edu/~roger/research/sparse/sparse.html
+# NeedsCompilation: yes
+# Repository: CRAN
+# Packaged: 2017-04-23 17:52:11 UTC; roger
+# Date/Publication: 2017-04-23 18:26:30 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-spatial-feedstock contains modified content
diff --git a/r-spatial-feedstock/recipe/meta.yaml b/r-spatial-feedstock/recipe/meta.yaml
index e332e9d..df5e863 100644
--- a/r-spatial-feedstock/recipe/meta.yaml
+++ b/r-spatial-feedstock/recipe/meta.yaml
@@ -12,32 +12,45 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/spatial_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/spatial/spatial_{{ version }}.tar.gz
-
   sha256: 624448d2ac22e1798097d09fc5dc4605908a33f490b8ec971fc6ea318a445c11
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: MASS
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('spatial')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('spatial')"           # [not win]
     - "\"%R%\" -e \"library('spatial')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://www.stats.ox.ac.uk/pub/MASS4/
   license: GPL-2 | GPL-3
@@ -48,3 +61,30 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: spatial
+# Priority: recommended
+# Version: 7.3-11
+# Date: 2015-08-29
+# Depends: R (>= 3.0.0), graphics, stats, utils
+# Suggests: MASS
+# Authors@R: c(person("Brian", "Ripley", role = c("aut", "cre", "cph"), email = "ripley@stats.ox.ac.uk"), person("Roger", "Bivand", role = "ctb"), person("William", "Venables", role = "cph"))
+# Description: Functions for kriging and point pattern analysis.
+# Title: Functions for Kriging and Point Pattern Analysis
+# LazyLoad: yes
+# ByteCompile: yes
+# License: GPL-2 | GPL-3
+# URL: http://www.stats.ox.ac.uk/pub/MASS4/
+# NeedsCompilation: yes
+# Packaged: 2015-08-28 15:25:37 UTC; ripley
+# Author: Brian Ripley [aut, cre, cph], Roger Bivand [ctb], William Venables [cph]
+# Maintainer: Brian Ripley <ripley@stats.ox.ac.uk>
+# Repository: CRAN
+# Date/Publication: 2015-08-30 08:59:41
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-stabledist-feedstock contains modified content
diff --git a/r-stabledist-feedstock/recipe/meta.yaml b/r-stabledist-feedstock/recipe/meta.yaml
index f9e24a0..864f631 100644
--- a/r-stabledist-feedstock/recipe/meta.yaml
+++ b/r-stabledist-feedstock/recipe/meta.yaml
@@ -12,18 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/stabledist_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/stabledist/stabledist_{{ version }}.tar.gz
+  sha256: 06c5704d3a3c179fa389675c537c39a006867bc6e4f23dd7e406476ed2c88a69
 
 
-  sha256: 06c5704d3a3c179fa389675c537c39a006867bc6e4f23dd7e406476ed2c88a69
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
-  skip: True  # [win32]
 
+# Suggests: Matrix, fBasics, FMStable, RUnit, Rmpfr, sfsmisc
 requirements:
   build:
     - r-base
@@ -33,10 +38,15 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('stabledist')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('stabledist')"           # [not win]
     - "\"%R%\" -e \"library('stabledist')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
 
+  # requires:
+    # Put any additional test requirements here.
 
 about:
   home: http://www.rmetrics.org, https://r-forge.r-project.org/scm/viewvc.php/pkg/stabledist/?root=rmetrics
@@ -45,8 +55,31 @@ about:
     stable distributions, using the parametrizations of Nolan.
   license_family: GPL3
 
-
-
 extra:
   recipe-maintainers:
     - croth1
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: stabledist
+# Version: 0.7-1
+# Date: 2016-09-12
+# Title: Stable Distribution Functions
+# Author: Diethelm Wuertz, Martin Maechler and Rmetrics core team members.
+# Maintainer: Martin Maechler <maechler@stat.math.ethz.ch>
+# Depends: R (>= 3.1.0)
+# Imports: stats
+# Suggests: Matrix, fBasics, FMStable, RUnit, Rmpfr, sfsmisc
+# Description: Density, Probability and Quantile functions, and random number generation for (skew) stable distributions, using the parametrizations of Nolan.
+# LazyData: yes
+# License: GPL (>= 2)
+# URL: http://www.rmetrics.org, https://r-forge.r-project.org/scm/viewvc.php/pkg/stabledist/?root=rmetrics
+# NeedsCompilation: no
+# Packaged: 2016-09-12 13:21:29 UTC; maechler
+# Repository: CRAN
+# Date/Publication: 2016-09-12 20:55:10
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-stanheaders-feedstock contains modified content
diff --git a/r-stanheaders-feedstock/recipe/meta.yaml b/r-stanheaders-feedstock/recipe/meta.yaml
index 2002759..e73bffc 100644
--- a/r-stanheaders-feedstock/recipe/meta.yaml
+++ b/r-stanheaders-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '2.15.0-1' %}
+{% set version = '2.16.0-1' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,35 +12,51 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/StanHeaders_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/StanHeaders/StanHeaders_{{ version }}.tar.gz
-  sha256: eea2412909bba4bdf80fe7d2c62e9d248a20c3fc644fb0a09b892954dd386f48
+  sha256: f92cca71e5674da6ac6fc318bae46158153911e706319edb7a3c1eedc5c02f44
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
-  skip: True  # [win32]
 
+# Suggests: RcppEigen, BH
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('cxx') }}      # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('StanHeaders')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('StanHeaders')"           # [not win]
     - "\"%R%\" -e \"library('StanHeaders')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
 
+  # requires:
+    # Put any additional test requirements here.
 
 about:
   home: http://mc-stan.org/
-  license: BSD_3_clause
+  license: BSD_3_clause + file LICENSE
   summary: The C++ header files of the Stan project are provided by this package, but it contains
     no R code, vignettes, or function documentation. There is a shared object containing
     part of the 'CVODES' library, but it is not accessible from R. 'StanHeaders' is
@@ -55,10 +71,32 @@ about:
     for the Stan language. The 'rstan' package provides user-facing R functions to parse,
     compile, test, estimate, and analyze Stan models.
   license_family: BSD
-  license_file: LICENSE
 
 extra:
   recipe-maintainers:
     - croth1
     - bgruening
     - johanneskoester
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: StanHeaders
+# Date: 2017-07-02
+# Title: C++ Header Files for Stan
+# Authors@R: c(person("Ben",family="Goodrich", email="benjamin.goodrich@columbia.edu", role=c('cre','aut')), person("Joshua", "Pritikin", role = "ctb"), person("Andrew", "Gelman", role = "aut"), person("Bob", "Carpenter",  role = "aut"), person("Matt", "Hoffman", role = "aut"), person("Daniel", "Lee", role = "aut"), person("Michael", "Betancourt", role = "aut"), person("Marcus", "Brubaker", role = "aut"), person("Jiqiang", "Guo", role = "aut"), person("Peter", "Li", role = "aut"), person("Allen", "Riddell", role = "aut"), person("Marco", "Inacio", role = "aut"), person("Mitzi", "Morris", role = "aut"), person("Jeffrey", "Arnold", role = "aut"), person("Rob", "Goedman", role = "aut"), person("Brian", "Lau", role = "aut"), person("Rob", "Trangucci", role = "aut"), person("Jonah", "Gabry", role = "aut"), person("Alp", "Kucukelbir", role = "aut"), person("Robert", "Grant", role = "aut"), person("Dustin", "Tran", role = "aut"), person("Michael", "Malecki", role = "aut"), person("Yuanjun", "Gao", role = "aut"), person("Trustees of", "Columbia University", role = "cph"), person("Lawrence Livermore", "National Security", role = "cph", comment = "CVODES"), person("The Regents of the", "University of California", role = "cph", comment = "CVODES"), person("Southern Methodist", "University", role = "cph", comment = "CVODES"))
+# URL: http://mc-stan.org/
+# Description: The C++ header files of the Stan project are provided by this package, but it contains no R code, vignettes, or function documentation. There is a shared object containing part of the 'CVODES' library, but it is not accessible from R. 'StanHeaders' is only useful for developers who want to utilize the 'LinkingTo' directive of their package's DESCRIPTION file to build on the Stan library without incurring unnecessary dependencies. The Stan project develops a probabilistic programming language that implements full or approximate Bayesian statistical inference via Markov Chain Monte Carlo or 'variational' methods and implements (optionally penalized) maximum likelihood estimation via optimization. The Stan library includes an advanced automatic differentiation scheme, 'templated' statistical and linear algebra functions that can handle the automatically 'differentiable' scalar types (and doubles, 'ints', etc.), and a parser for the Stan language. The 'rstan' package provides user-facing R functions to parse, compile, test, estimate, and analyze Stan models.
+# Suggests: RcppEigen, BH
+# Version: 2.16.0-1
+# License: BSD_3_clause + file LICENSE
+# NeedsCompilation: yes
+# Packaged: 2017-07-02 16:51:25 UTC; goodrich
+# Author: Ben Goodrich [cre, aut], Joshua Pritikin [ctb], Andrew Gelman [aut], Bob Carpenter [aut], Matt Hoffman [aut], Daniel Lee [aut], Michael Betancourt [aut], Marcus Brubaker [aut], Jiqiang Guo [aut], Peter Li [aut], Allen Riddell [aut], Marco Inacio [aut], Mitzi Morris [aut], Jeffrey Arnold [aut], Rob Goedman [aut], Brian Lau [aut], Rob Trangucci [aut], Jonah Gabry [aut], Alp Kucukelbir [aut], Robert Grant [aut], Dustin Tran [aut], Michael Malecki [aut], Yuanjun Gao [aut], Trustees of Columbia University [cph], Lawrence Livermore National Security [cph] (CVODES), The Regents of the University of California [cph] (CVODES), Southern Methodist University [cph] (CVODES)
+# Maintainer: Ben Goodrich <benjamin.goodrich@columbia.edu>
+# Repository: CRAN
+# Date/Publication: 2017-07-03 10:51:13 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-stringdist-feedstock contains modified content
diff --git a/r-stringdist-feedstock/recipe/meta.yaml b/r-stringdist-feedstock/recipe/meta.yaml
index 6a67e38..b689aee 100644
--- a/r-stringdist-feedstock/recipe/meta.yaml
+++ b/r-stringdist-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '0.9.4.4' %}
+{% set version = '0.9.4.6' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,34 +12,46 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/stringdist_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/stringdist/stringdist_{{ version }}.tar.gz
+  sha256: 91c1ddee426ee246f92dd12ae746ea114e123fbfb32a4708ee29db51323cab48
 
 
-  sha256: 97d88229255f87e2ebd4dfbfaea025f13b0d203b171e45dd9413cf6023cccfe2
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
-  skip: True  # [win32]
 
+# Suggests: testthat
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('stringdist')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('stringdist')"           # [not win]
     - "\"%R%\" -e \"library('stringdist')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
 
+  # requires:
+    # Put any additional test requirements here.
 
 about:
   home: https://github.com/markvanderloo/stringdist
@@ -52,8 +64,36 @@ about:
     care of encoding or between integer vectors representing generic sequences.
   license_family: GPL3
 
-
-
 extra:
   recipe-maintainers:
     - croth1
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: stringdist
+# Maintainer: Mark van der Loo <mark.vanderloo@gmail.com>
+# License: GPL-3
+# Title: Approximate String Matching and String Distance Functions
+# LazyData: no
+# Type: Package
+# LazyLoad: yes
+# Authors@R: c( person("Mark", "van der Loo", role=c("aut","cre"),email="mark.vanderloo@gmail.com") , person("Jan", "van der Laan", role="ctb"),person("R Core Team","",role=c("ctb")),person("Nick","Logan",role="ctb"))
+# Description: Implements an approximate string matching version of R's native 'match' function. Can calculate various string distances based on edits (Damerau-Levenshtein, Hamming, Levenshtein, optimal sting alignment), qgrams (q- gram, cosine, jaccard distance) or heuristic metrics (Jaro, Jaro-Winkler). An implementation of soundex is provided as well. Distances can be computed between character vectors while taking proper care of encoding or between integer vectors representing generic sequences.
+# Version: 0.9.4.6
+# Depends: R (>= 2.15.3)
+# Imports: parallel
+# URL: https://github.com/markvanderloo/stringdist
+# BugReports: https://github.com/markvanderloo/stringdist/issues
+# Date: 2017-07-26
+# Suggests: testthat
+# RoxygenNote: 6.0.1
+# NeedsCompilation: yes
+# Packaged: 2017-07-31 09:18:23 UTC; mark
+# Author: Mark van der Loo [aut, cre], Jan van der Laan [ctb], R Core Team [ctb], Nick Logan [ctb]
+# Repository: CRAN
+# Date/Publication: 2017-07-31 10:35:34 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-stringi-feedstock contains modified content
diff --git a/r-stringi-feedstock/recipe/bld.bat b/r-stringi-feedstock/recipe/bld.bat
new file mode 100644
index 0000000..8b10c94
--- /dev/null
+++ b/r-stringi-feedstock/recipe/bld.bat
@@ -0,0 +1,8 @@
+"%R%" CMD INSTALL --build .
+if errorlevel 1 exit 1
+
+@rem Add more build steps here, if they are necessary.
+
+@rem See
+@rem http://docs.continuum.io/conda/build.html
+@rem for a list of environment variables that are set during the build process.
diff --git a/r-stringi-feedstock/recipe/build.sh b/r-stringi-feedstock/recipe/build.sh
new file mode 100644
index 0000000..0c37df6
--- /dev/null
+++ b/r-stringi-feedstock/recipe/build.sh
@@ -0,0 +1,13 @@
+#!/bin/bash
+
+# R refuses to build packages that mark themselves as Priority: Recommended
+mv DESCRIPTION DESCRIPTION.old
+grep -v '^Priority: ' DESCRIPTION.old > DESCRIPTION
+
+$R CMD INSTALL --build .
+
+# Add more build steps here, if they are necessary.
+
+# See
+# http://docs.continuum.io/conda/build.html
+# for a list of environment variables that are set during the build process.
diff --git a/r-stringi-feedstock/recipe/meta.yaml b/r-stringi-feedstock/recipe/meta.yaml
index fb0080d..658bc5c 100644
--- a/r-stringi-feedstock/recipe/meta.yaml
+++ b/r-stringi-feedstock/recipe/meta.yaml
@@ -1,50 +1,104 @@
-{% set version = "1.1.2" %}
+{% set version = '1.1.5' %}
+
+{% set posix = 'm2-' if win else '' %}
+{% set native = 'm2w64-' if win else '' %}
 
 package:
   name: r-stringi
-  version: {{ version }}
+  version: {{ version|replace("-", "_") }}
 
 source:
   fn: stringi_{{ version }}.tar.gz
   url:
-    - http://cran.r-project.org/src/contrib/stringi_{{ version }}.tar.gz
-    - http://cran.r-project.org/src/contrib/Archive/stringi/stringi_{{ version }}.tar.gz
-  sha256: e50b7162ceb7ebae403475f6f8a76a39532a2abc82112db88661f48aa4b9218e
+    - https://cran.r-project.org/src/contrib/stringi_{{ version }}.tar.gz
+    - https://cran.r-project.org/src/contrib/Archive/stringi/stringi_{{ version }}.tar.gz
+  sha256: 651e85fc4ec6cf71ad8a4347f2bd4b00a490cf9eec20921a83bf5222740402f2
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: True  # [win32]
-  script: R CMD INSTALL --build .
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
-requirements:  # [not win32]
-  build:  # [not win32]
-    - r-base  # [not win32]
-    - posix  # [win64]
-    - m2w64-toolchain  # [win64]
-    - gcc  # [not win]
-  run:  # [not win32]
-    - r-base  # [not win32]
+
+requirements:
+  build:
+    - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('cxx') }}      # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}autoconf
+    - {{posix}}automake
+    - {{posix}}pkg-config
+    - {{posix}}make
+
+  run:
+    - r-base
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - R -e "library('stringi')"  # [not win]
-    - R -e \"library('stringi')\"  # [win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('stringi')"           # [not win]
+    - "\"%R%\" -e \"library('stringi')\""  # [win]
+
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
 
 about:
-  home: http://www.gagolewski.com/software/stringi
-  license: FOSS
-  license_file: LICENSE
+  home: http://www.gagolewski.com/software/stringi/ http://site.icu-project.org/ http://www.unicode.org/
+  license: file LICENSE (FOSS)
   summary: 'Allows for fast, correct, consistent, portable, as well as convenient character string/text
     processing in every locale and any native encoding. Owing to the use of the ICU
     library, the package provides R users with platform-independent functions known
-    to Java, Perl, Python, PHP, and Ruby programmers. Among available features there
-    are: pattern searching (e.g., with ICU Java-like regular expressions or the Unicode
-    Collation Algorithm), random string generation, case mapping, string transliteration,
-    concatenation, Unicode normalization, date-time formatting and parsing, etc.'
+    to Java, Perl, Python, PHP, and Ruby programmers. Available features include: pattern
+    searching (e.g., with ICU Java-like regular expressions or the Unicode Collation
+    Algorithm), random string generation, case mapping, string transliteration, concatenation,
+    Unicode normalization, date-time formatting and parsing, etc.'
+
+  license_family: OTHER
 
 extra:
   recipe-maintainers:
     - ocefpaf
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: stringi
+# Version: 1.1.5
+# Date: 2017-04-07
+# Title: Character String Processing Facilities
+# Description: Allows for fast, correct, consistent, portable, as well as convenient character string/text processing in every locale and any native encoding. Owing to the use of the ICU library, the package provides R users with platform-independent functions known to Java, Perl, Python, PHP, and Ruby programmers. Available features include: pattern searching (e.g., with ICU Java-like regular expressions or the Unicode Collation Algorithm), random string generation, case mapping, string transliteration, concatenation, Unicode normalization, date-time formatting and parsing, etc.
+# URL: http://www.gagolewski.com/software/stringi/ http://site.icu-project.org/ http://www.unicode.org/
+# BugReports: http://github.com/gagolews/stringi/issues
+# SystemRequirements: ICU4C (>= 52, optional)
+# Type: Package
+# Depends: R (>= 2.14)
+# Imports: tools, utils, stats
+# Biarch: TRUE
+# License: file LICENSE
+# Author: Marek Gagolewski [aut, cre], Bartek Tartanus [ctb], and other contributors (stringi source code); IBM and other contributors (ICU4C 55.1 source code); Unicode, Inc. (Unicode Character Database)
+# Maintainer: Marek Gagolewski <gagolews@rexamine.com>
+# RoxygenNote: 6.0.1
+# NeedsCompilation: yes
+# Packaged: 2017-04-07 09:50:47 UTC; gagolews
+# License_is_FOSS: yes
+# Repository: CRAN
+# Date/Publication: 2017-04-07 13:08:03 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-stringr-feedstock contains modified content
diff --git a/r-stringr-feedstock/recipe/bld.bat b/r-stringr-feedstock/recipe/bld.bat
new file mode 100644
index 0000000..8b10c94
--- /dev/null
+++ b/r-stringr-feedstock/recipe/bld.bat
@@ -0,0 +1,8 @@
+"%R%" CMD INSTALL --build .
+if errorlevel 1 exit 1
+
+@rem Add more build steps here, if they are necessary.
+
+@rem See
+@rem http://docs.continuum.io/conda/build.html
+@rem for a list of environment variables that are set during the build process.
diff --git a/r-stringr-feedstock/recipe/build.sh b/r-stringr-feedstock/recipe/build.sh
new file mode 100644
index 0000000..0c37df6
--- /dev/null
+++ b/r-stringr-feedstock/recipe/build.sh
@@ -0,0 +1,13 @@
+#!/bin/bash
+
+# R refuses to build packages that mark themselves as Priority: Recommended
+mv DESCRIPTION DESCRIPTION.old
+grep -v '^Priority: ' DESCRIPTION.old > DESCRIPTION
+
+$R CMD INSTALL --build .
+
+# Add more build steps here, if they are necessary.
+
+# See
+# http://docs.continuum.io/conda/build.html
+# for a list of environment variables that are set during the build process.
diff --git a/r-stringr-feedstock/recipe/meta.yaml b/r-stringr-feedstock/recipe/meta.yaml
index f3021c1..6d34bf8 100644
--- a/r-stringr-feedstock/recipe/meta.yaml
+++ b/r-stringr-feedstock/recipe/meta.yaml
@@ -14,38 +14,82 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/stringr/stringr_{{ version }}.tar.gz
   sha256: 61d0b30768bbfd7c0bb89310e2de5b7b457ac504538acbcca50374b46b16129a
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: True  # [win32]
-  script: R CMD INSTALL --build .
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
-requirements:  # [not win32]
-  build:  # [not win32]
-    - r-base  # [not win32]
-    - r-magrittr  # [not win32]
-    - r-stringi >=0.4.1  # [not win32]
-  run:  # [not win32]
-    - r-base  # [not win32]
-    - r-magrittr  # [not win32]
-    - r-stringi >=0.4.1  # [not win32]
+# Suggests: testthat, knitr, htmltools, htmlwidgets, rmarkdown, covr
+requirements:
+  build:
+    - r-base
+    - r-magrittr
+    - r-stringi >=0.4.1
+
+  run:
+    - r-base
+    - r-magrittr
+    - r-stringi >=0.4.1
 
 test:
   commands:
-    - R -e "library('stringr')"  # [not win]
-    - R -e \"library('stringr')\"  # [win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('stringr')"           # [not win]
+    - "\"%R%\" -e \"library('stringr')\""  # [win]
+
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
 
 about:
-  home: https://github.com/hadley/stringr
-  license: GPL-2
-  summary: "A consistent, simple and easy to use set of wrappers around the fantastic 'stringi'
+  home: http://stringr.tidyverse.org, https://github.com/tidyverse/stringr
+  license: GPL-2 | file LICENSE
+  summary: A consistent, simple and easy to use set of wrappers around the fantastic 'stringi'
     package. All function and argument names (and positions) are consistent, all functions
-    deal with NA's and zero length vectors in the same way, and the output from one
-    function is easy to feed into the input of another."
+    deal with "NA"'s and zero length vectors in the same way, and the output from one
+    function is easy to feed into the input of another.
+  license_family: GPL2
 
 extra:
   recipe-maintainers:
     - ocefpaf
     - jdblischak
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: stringr
+# Version: 1.2.0
+# Title: Simple, Consistent Wrappers for Common String Operations
+# Description: A consistent, simple and easy to use set of wrappers around the fantastic 'stringi' package. All function and argument names (and positions) are consistent, all functions deal with "NA"'s and zero length vectors in the same way, and the output from one function is easy to feed into the input of another.
+# Authors@R: c( person("Hadley", "Wickham", , "hadley@rstudio.com", c("aut", "cre", "cph")), person("RStudio", role = "cph") )
+# License: GPL-2 | file LICENSE
+# Depends: R (>= 2.14)
+# Imports: stringi (>= 0.4.1), magrittr
+# Suggests: testthat, knitr, htmltools, htmlwidgets, rmarkdown, covr
+# VignetteBuilder: knitr
+# URL: http://stringr.tidyverse.org, https://github.com/tidyverse/stringr
+# BugReports: https://github.com/tidyverse/stringr/issues
+# RoxygenNote: 6.0.1
+# LazyData: true
+# NeedsCompilation: no
+# Packaged: 2017-02-17 15:23:03 UTC; hadley
+# Author: Hadley Wickham [aut, cre, cph], RStudio [cph]
+# Maintainer: Hadley Wickham <hadley@rstudio.com>
+# Repository: CRAN
+# Date/Publication: 2017-02-18 21:23:06
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-strucchange-feedstock contains modified content
diff --git a/r-strucchange-feedstock/recipe/meta.yaml b/r-strucchange-feedstock/recipe/meta.yaml
index 572bb95..d2856ec 100644
--- a/r-strucchange-feedstock/recipe/meta.yaml
+++ b/r-strucchange-feedstock/recipe/meta.yaml
@@ -14,18 +14,27 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/strucchange/strucchange_{{ version }}.tar.gz
   sha256: 740e2e20477b9fceeef767ae1002adc5ec397cb0f7daba5289a2c23b0dddaf31
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: stats4, car, dynlm, e1071, foreach, lmtest, mvtnorm, tseries
 requirements:
   build:
     - r-base
     - r-sandwich
     - r-zoo
+
   run:
     - r-base
     - r-sandwich
@@ -33,14 +42,20 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('strucchange')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('strucchange')"           # [not win]
     - "\"%R%\" -e \"library('strucchange')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=strucchange
-  license: GPL-3
-  summary: |
-    Testing, monitoring and dating structural changes in (linear) regression models. strucchange
+  license: GPL-2 | GPL-3
+  summary: Testing, monitoring and dating structural changes in (linear) regression models. strucchange
     features tests/methods from the generalized fluctuation test framework as well as
     from the F test (Chow test) framework. This includes methods to fit, plot and test
     fluctuation processes (e.g., CUSUM, MOSUM, recursive/moving estimates) and F statistics,
@@ -54,3 +69,28 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: strucchange
+# Version: 1.5-1
+# Date: 2015-06-06
+# Title: Testing, Monitoring, and Dating Structural Changes
+# Authors@R: c(person(given = "Achim", family = "Zeileis", role = c("aut", "cre"), email = "Achim.Zeileis@R-project.org"), person(given = "Friedrich", family = "Leisch", role = "aut", email = "Friedrich.Leisch@R-project.org"), person(given = "Kurt", family = "Hornik", role = "aut", email = "Kurt.Hornik@R-project.org"), person(given = "Christian", family = "Kleiber", role = "aut", email = "Christian.Kleiber@unibas.ch"), person(given = "Bruce", family = "Hansen", role = "ctb"), person(give = c("Edgar", "C."), family = "Merkle", role = "ctb"))
+# Description: Testing, monitoring and dating structural changes in (linear) regression models. strucchange features tests/methods from the generalized fluctuation test framework as well as from the F test (Chow test) framework. This includes methods to fit, plot and test fluctuation processes (e.g., CUSUM, MOSUM, recursive/moving estimates) and F statistics, respectively. It is possible to monitor incoming data online using fluctuation processes. Finally, the breakpoints in regression models with structural changes can be estimated together with confidence intervals. Emphasis is always given to methods for visualizing the data.
+# LazyData: yes
+# Depends: R (>= 2.10.0), zoo, sandwich
+# Suggests: stats4, car, dynlm, e1071, foreach, lmtest, mvtnorm, tseries
+# Imports: graphics, stats
+# License: GPL-2 | GPL-3
+# NeedsCompilation: no
+# Packaged: 2015-06-06 10:01:15 UTC; zeileis
+# Author: Achim Zeileis [aut, cre], Friedrich Leisch [aut], Kurt Hornik [aut], Christian Kleiber [aut], Bruce Hansen [ctb], Edgar C. Merkle [ctb]
+# Maintainer: Achim Zeileis <Achim.Zeileis@R-project.org>
+# Repository: CRAN
+# Date/Publication: 2015-06-06 13:03:15
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-survival-feedstock contains modified content
diff --git a/r-survival-feedstock/recipe/bld.bat b/r-survival-feedstock/recipe/bld.bat
new file mode 100644
index 0000000..8b10c94
--- /dev/null
+++ b/r-survival-feedstock/recipe/bld.bat
@@ -0,0 +1,8 @@
+"%R%" CMD INSTALL --build .
+if errorlevel 1 exit 1
+
+@rem Add more build steps here, if they are necessary.
+
+@rem See
+@rem http://docs.continuum.io/conda/build.html
+@rem for a list of environment variables that are set during the build process.
diff --git a/r-survival-feedstock/recipe/build.sh b/r-survival-feedstock/recipe/build.sh
new file mode 100644
index 0000000..0c37df6
--- /dev/null
+++ b/r-survival-feedstock/recipe/build.sh
@@ -0,0 +1,13 @@
+#!/bin/bash
+
+# R refuses to build packages that mark themselves as Priority: Recommended
+mv DESCRIPTION DESCRIPTION.old
+grep -v '^Priority: ' DESCRIPTION.old > DESCRIPTION
+
+$R CMD INSTALL --build .
+
+# Add more build steps here, if they are necessary.
+
+# See
+# http://docs.continuum.io/conda/build.html
+# for a list of environment variables that are set during the build process.
diff --git a/r-survival-feedstock/recipe/meta.yaml b/r-survival-feedstock/recipe/meta.yaml
index 6e7037b..a4a1276 100644
--- a/r-survival-feedstock/recipe/meta.yaml
+++ b/r-survival-feedstock/recipe/meta.yaml
@@ -1,47 +1,95 @@
-{% set version = "2.40-1" %}
+{% set version = '2.41-3' %}
+
+{% set posix = 'm2-' if win else '' %}
+{% set native = 'm2w64-' if win else '' %}
 
 package:
   name: r-survival
-  version: {{ version.replace("-", "_") }}
+  version: {{ version|replace("-", "_") }}
 
 source:
   fn: survival_{{ version }}.tar.gz
   url:
-    - http://cran.r-project.org/src/contrib/survival_{{ version }}.tar.gz
-    - http://cran.r-project.org/src/contrib/Archive/survival/survival_{{ version }}.tar.gz
-  sha256: 91d5217847e39bebcbce4f0a2e295304e5816b1270e71f5f2ed39807f004ee82
+    - https://cran.r-project.org/src/contrib/survival_{{ version }}.tar.gz
+    - https://cran.r-project.org/src/contrib/Archive/survival/survival_{{ version }}.tar.gz
+  sha256: f3797c344de93abd2ba8c89568770a13524a8b2694144ae55adec46921c8961d
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  script: R CMD INSTALL --build .
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
-    - {{ compiler('c') }}  # [not win]
     - r-base
-    - posix                # [win]
-    - m2w64-toolchain      # [win]
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
     - r-matrix
+
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-matrix
-    - m2w64-gcc-libs       # [win]
 
 test:
   commands:
-    - R -e "library('survival')"    # [not win]
-    - R -e \"library('survival')\"  # [win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('survival')"           # [not win]
+    - "\"%R%\" -e \"library('survival')\""  # [win]
+
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
 
 about:
   home: https://github.com/therneau/survival
-  license: LGPL-2.0
-  summary: 'Contains the core survival analysis routines, including definition of Surv objects,  Kaplan-Meier
+  license: LGPL (>= 2)
+  summary: Contains the core survival analysis routines, including definition of Surv objects,  Kaplan-Meier
     and Aalen-Johansen (multi-state) curves, Cox models, and parametric accelerated
-    failure time models.'
+    failure time models.
+  license_family: LGPL
 
 extra:
   recipe-maintainers:
     - ocefpaf
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Title: Survival Analysis
+# Maintainer: Terry M Therneau <therneau.terry@mayo.edu>
+# Priority: recommended
+# Package: survival
+# Version: 2.41-3
+# Depends: R (>= 2.13.0)
+# Imports: graphics, Matrix, methods, splines, stats, utils
+# LazyData: Yes
+# LazyLoad: Yes
+# ByteCompile: Yes
+# Authors@R: c(person(c("Terry", "M"), "Therneau", email="therneau.terry@mayo.edu", role=c("aut", "cre")), person("Thomas", "Lumley", role=c("ctb", "trl"), comment="original S->R port and maintainer until 2009"))
+# Description: Contains the core survival analysis routines, including definition of Surv objects,  Kaplan-Meier and Aalen-Johansen (multi-state) curves, Cox models, and parametric accelerated failure time models.
+# License: LGPL (>= 2)
+# URL: https://github.com/therneau/survival
+# NeedsCompilation: yes
+# Packaged: 2017-04-04 12:56:36 UTC; therneau
+# Author: Terry M Therneau [aut, cre], Thomas Lumley [ctb, trl] (original S->R port and maintainer until 2009)
+# Repository: CRAN
+# Date/Publication: 2017-04-04 19:31:37 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-tensorflow-feedstock contains modified content
diff --git a/r-tensorflow-feedstock/recipe/meta.yaml b/r-tensorflow-feedstock/recipe/meta.yaml
index 1b9b0aa..d60e615 100644
--- a/r-tensorflow-feedstock/recipe/meta.yaml
+++ b/r-tensorflow-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '0.7' %}
+{% set version = '1.4' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,35 +12,57 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/tensorflow_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/tensorflow/tensorflow_{{ version }}.tar.gz
-  sha256: 1b15123f689a452f55db3ac0908e4dd64e8a628dc6e57bd927d3ea7472123f42
+  sha256: 3deffc78131438dd3039a565a09da8913d6555e1b7fa1b892c046d8df348cfc8
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: testthat
 requirements:
   build:
     - r-base
-    - r-reticulate >=0.7
+    - r-config
+    - r-jsonlite
+    - r-processx
+    - r-reticulate >=1.2
+    - r-tfruns >=0.9.1
     - r-yaml
 
   run:
     - r-base
-    - r-reticulate >=0.7
+    - r-config
+    - r-jsonlite
+    - r-processx
+    - r-reticulate >=1.2
+    - r-tfruns >=0.9.1
     - r-yaml
 
 test:
   commands:
-    - $R -e "library('tensorflow')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('tensorflow')"           # [not win]
     - "\"%R%\" -e \"library('tensorflow')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/rstudio/tensorflow
-  license: Apache 2.0
+  license: Apache License 2.0
   summary: Interface to 'TensorFlow' <https://www.tensorflow.org/>,  an open source software
     library for numerical computation using data flow graphs. Nodes in the graph represent
     mathematical operations,  while the graph edges represent the multidimensional data
@@ -57,3 +79,33 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: tensorflow
+# Type: Package
+# Title: R Interface to 'TensorFlow'
+# Version: 1.4
+# Authors@R: c( person("JJ", "Allaire", role = c("aut", "cre"), email = "jj@rstudio.com"), person(family = "RStudio", role = c("cph", "fnd")), person("Yuan", "Tang", role = c("aut", "cph"), email = "terrytangyuan@gmail.com"), person("Dirk", "Eddelbuettel", role = c("ctb", "cph"), email = "edd@debian.org"), person("Nick", "Golding", role = c("ctb", "cph"), email = "nick.golding.research@gmail.com"), person(family = "Google Inc.", role = c("ctb", "cph"), comment = "Examples and Tutorials") )
+# Description: Interface to 'TensorFlow' <https://www.tensorflow.org/>,  an open source software library for numerical computation using data flow graphs. Nodes in the graph represent mathematical operations,  while the graph edges represent the multidimensional data arrays  (tensors) communicated between them. The flexible architecture allows you to deploy computation to one or more 'CPUs' or 'GPUs' in a desktop,  server, or mobile device with a single 'API'. 'TensorFlow' was originally developed by researchers and engineers working on the Google Brain Team  within Google's Machine Intelligence research organization for the  purposes of conducting machine learning and deep neural networks research, but the system is general enough to be applicable in a wide variety of other domains as well.
+# License: Apache License 2.0
+# URL: https://github.com/rstudio/tensorflow
+# BugReports: https://github.com/rstudio/tensorflow/issues
+# SystemRequirements: TensorFlow (https://www.tensorflow.org/)
+# Encoding: UTF-8
+# LazyData: true
+# Depends: R (>= 3.1)
+# Imports: config, jsonlite, processx, reticulate (>= 1.2), tfruns (>= 0.9.1), utils, yaml
+# Suggests: testthat
+# RoxygenNote: 6.0.1
+# NeedsCompilation: no
+# Packaged: 2017-09-27 10:26:42 UTC; jjallaire
+# Author: JJ Allaire [aut, cre], RStudio [cph, fnd], Yuan Tang [aut, cph], Dirk Eddelbuettel [ctb, cph], Nick Golding [ctb, cph], Google Inc. [ctb, cph] (Examples and Tutorials)
+# Maintainer: JJ Allaire <jj@rstudio.com>
+# Repository: CRAN
+# Date/Publication: 2017-09-27 12:23:38 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-testit-feedstock contains modified content
diff --git a/r-testit-feedstock/recipe/meta.yaml b/r-testit-feedstock/recipe/meta.yaml
index c4b0946..29191ff 100644
--- a/r-testit-feedstock/recipe/meta.yaml
+++ b/r-testit-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '0.6' %}
+{% set version = '0.7' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,18 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/testit_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/testit/testit_{{ version }}.tar.gz
+  sha256: 03332889bffe8b69d36e696e3a6a1a29713cdcbccf1efced6cddbf061fb41a1f
 
 
-  sha256: 9b220f7dbfcca49cdf838af4ef0c92bd5a92742dca50754f176b3d30de069416
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
-  skip: True  # [win32]
 
+# Suggests: rstudioapi
 requirements:
   build:
     - r-base
@@ -33,10 +38,15 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('testit')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('testit')"           # [not win]
     - "\"%R%\" -e \"library('testit')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
 
+  # requires:
+    # Put any additional test requirements here.
 
 about:
   home: https://github.com/yihui/testit
@@ -45,8 +55,32 @@ about:
     packages.
   license_family: GPL
 
-
-
 extra:
   recipe-maintainers:
     - croth1
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: testit
+# Type: Package
+# Title: A Simple Package for Testing R Packages
+# Version: 0.7
+# Date: 2017-05-21
+# Author: Yihui Xie
+# Maintainer: Yihui Xie <xie@yihui.name>
+# Description: Provides two convenience functions assert() and test_pkg() to facilitate testing R packages.
+# License: GPL
+# URL: https://github.com/yihui/testit
+# BugReports: https://github.com/yihui/testit/issues
+# Suggests: rstudioapi
+# Collate: 'testit.R' 'utils.R'
+# RoxygenNote: 6.0.1
+# NeedsCompilation: no
+# Packaged: 2017-05-21 04:53:18 UTC; yihui
+# Repository: CRAN
+# Date/Publication: 2017-05-22 05:16:36 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-testthat-feedstock contains modified content
diff --git a/r-testthat-feedstock/recipe/meta.yaml b/r-testthat-feedstock/recipe/meta.yaml
index c2747f9..2346a5c 100644
--- a/r-testthat-feedstock/recipe/meta.yaml
+++ b/r-testthat-feedstock/recipe/meta.yaml
@@ -14,28 +14,38 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/testthat/testthat_{{ version }}.tar.gz
   sha256: 0ef7df0ace1fddf821d329f9d9a5d42296085350ae0d94af62c45bd203c8415e
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: devtools, withr, covr
 requirements:
   build:
     - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('cxx') }}      # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
     - r-r6
     - r-crayon
     - r-digest
     - r-magrittr
     - r-praise
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-r6
     - r-crayon
     - r-digest
@@ -44,12 +54,19 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('testthat')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('testthat')"           # [not win]
     - "\"%R%\" -e \"library('testthat')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/hadley/testthat
-  license: MIT
+  license: MIT + file LICENSE
   summary: A unit testing system designed to be fun, flexible and easy to set up.
   license_family: MIT
 
@@ -57,3 +74,30 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: testthat
+# Version: 1.0.2
+# Title: Unit Testing for R
+# Description: A unit testing system designed to be fun, flexible and easy to set up.
+# Authors@R: c( person("Hadley", "Wickham", , "hadley@rstudio.com", c("aut", "cre")), person("RStudio", role = "cph") )
+# URL: https://github.com/hadley/testthat
+# BugReports: https://github.com/hadley/testthat/issues
+# Depends: R (>= 3.1.0)
+# Imports: digest, crayon, praise, magrittr, R6, methods
+# Suggests: devtools, withr, covr
+# License: MIT + file LICENSE
+# Collate: 'auto-test.R' 'colour-text.R' 'compare.R' 'compare-character.R' 'compare-numeric.R' 'compare-time.R' 'context.R' 'describe.R' 'evaluate-promise.R' 'expect-comparison.R' 'expect-equal-to-reference.R' 'expect-equality.R' 'expect-inheritance.R' 'expect-length.R' 'expect-logical.R' 'expect-named.R' 'expect-output.R' 'reporter.R' 'expect-self-test.R' 'expect-that.R' 'expectation.R' 'expectations-matches.R' 'make-expectation.R' 'mock.R' 'old-school.R' 'praise.R' 'reporter-check.R' 'reporter-fail.R' 'reporter-list.R' 'reporter-minimal.R' 'reporter-multi.R' 'reporter-rstudio.R' 'reporter-silent.R' 'reporter-stop.R' 'stack.R' 'reporter-summary.R' 'reporter-tap.R' 'reporter-teamcity.R' 'reporter-zzz.R' 'skip.R' 'source.R' 'test-compiled-code.R' 'test-example.R' 'test-files.R' 'test-package.R' 'test-path.R' 'test-that.R' 'traceback.R' 'try-again.R' 'utils.R' 'watcher.R'
+# RoxygenNote: 5.0.1
+# NeedsCompilation: yes
+# Packaged: 2016-04-22 20:14:31 UTC; hadley
+# Author: Hadley Wickham [aut, cre], RStudio [cph]
+# Maintainer: Hadley Wickham <hadley@rstudio.com>
+# Repository: CRAN
+# Date/Publication: 2016-04-23 08:37:40
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
diff --git a/r-tfruns-feedstock/recipe/bld.bat b/r-tfruns-feedstock/recipe/bld.bat
index e69de29..8b10c94 100644
--- a/r-tfruns-feedstock/recipe/bld.bat
+++ b/r-tfruns-feedstock/recipe/bld.bat
@@ -0,0 +1,8 @@
+"%R%" CMD INSTALL --build .
+if errorlevel 1 exit 1
+
+@rem Add more build steps here, if they are necessary.
+
+@rem See
+@rem http://docs.continuum.io/conda/build.html
+@rem for a list of environment variables that are set during the build process.
diff --git a/r-tfruns-feedstock/recipe/build.sh b/r-tfruns-feedstock/recipe/build.sh
index e69de29..0c37df6 100644
--- a/r-tfruns-feedstock/recipe/build.sh
+++ b/r-tfruns-feedstock/recipe/build.sh
@@ -0,0 +1,13 @@
+#!/bin/bash
+
+# R refuses to build packages that mark themselves as Priority: Recommended
+mv DESCRIPTION DESCRIPTION.old
+grep -v '^Priority: ' DESCRIPTION.old > DESCRIPTION
+
+$R CMD INSTALL --build .
+
+# Add more build steps here, if they are necessary.
+
+# See
+# http://docs.continuum.io/conda/build.html
+# for a list of environment variables that are set during the build process.
diff --git a/r-tfruns-feedstock/recipe/meta.yaml b/r-tfruns-feedstock/recipe/meta.yaml
index e69de29..d0da072 100644
--- a/r-tfruns-feedstock/recipe/meta.yaml
+++ b/r-tfruns-feedstock/recipe/meta.yaml
@@ -0,0 +1,99 @@
+{% set version = '0.9.1' %}
+
+{% set posix = 'm2-' if win else '' %}
+{% set native = 'm2w64-' if win else '' %}
+
+package:
+  name: r-tfruns
+  version: {{ version|replace("-", "_") }}
+
+source:
+  fn: tfruns_{{ version }}.tar.gz
+  url:
+    - https://cran.r-project.org/src/contrib/tfruns_{{ version }}.tar.gz
+    - https://cran.r-project.org/src/contrib/Archive/tfruns/tfruns_{{ version }}.tar.gz
+  sha256: 1f9e549d198ed6113c7e24b942e18752df27919ca66b7daff235fd4e3abc20c9
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
+build:
+  # If this is a new build for the same version, increment the build number.
+  number: 0
+
+  # This is required to make R link correctly on Linux.
+  rpaths:
+    - lib/R/lib/
+    - lib/
+
+# Suggests: testthat
+requirements:
+  build:
+    - r-base
+    - r-config
+    - r-jsonlite
+    - r-magrittr
+    - r-tibble
+    - r-yaml
+
+  run:
+    - r-base
+    - r-config
+    - r-jsonlite
+    - r-magrittr
+    - r-tibble
+    - r-yaml
+
+test:
+  commands:
+    # You can put additional test commands to be run here.
+    - $R -e "library('tfruns')"           # [not win]
+    - "\"%R%\" -e \"library('tfruns')\""  # [win]
+
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
+about:
+  home: https://github.com/rstudio/tfruns
+  license: Apache License 2.0
+  summary: 'Create and manage unique directories for each ''TensorFlow''  training run. Provides
+    a unique, time stamped directory for each run along with functions to retrieve the
+    directory of the latest run or  latest several runs. '
+
+  license_family: APACHE
+
+extra:
+  recipe-maintainers:
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: tfruns
+# Type: Package
+# Title: Training Run Tools for 'TensorFlow'
+# Version: 0.9.1
+# Authors@R: c( person("JJ", "Allaire", role = c("aut", "cre"), email = "jj@rstudio.com"), person(family = "RStudio", role = c("cph", "fnd")), person("Mike", "Bostock", role = c("cph"), comment = "D3 library - https://d3js.org/"), person("Masayuki", "Tanaka", role = c("cph"), comment = "C3 library - http://c3js.org/") )
+# Description: Create and manage unique directories for each 'TensorFlow'  training run. Provides a unique, time stamped directory for each run along with functions to retrieve the directory of the latest run or  latest several runs.
+# License: Apache License 2.0
+# URL: https://github.com/rstudio/tfruns
+# BugReports: https://github.com/rstudio/tfruns/issues
+# Imports: utils, jsonlite, yaml, config, magrittr, tibble
+# Encoding: UTF-8
+# LazyData: true
+# Suggests: testthat
+# RoxygenNote: 6.0.1
+# NeedsCompilation: no
+# Packaged: 2017-08-23 21:27:30 UTC; jjallaire
+# Author: JJ Allaire [aut, cre], RStudio [cph, fnd], Mike Bostock [cph] (D3 library - https://d3js.org/), Masayuki Tanaka [cph] (C3 library - http://c3js.org/)
+# Maintainer: JJ Allaire <jj@rstudio.com>
+# Repository: CRAN
+# Date/Publication: 2017-08-24 14:37:53 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-th.data-feedstock contains modified content
diff --git a/r-th.data-feedstock/recipe/meta.yaml b/r-th.data-feedstock/recipe/meta.yaml
index c025d28..fd7e0d8 100644
--- a/r-th.data-feedstock/recipe/meta.yaml
+++ b/r-th.data-feedstock/recipe/meta.yaml
@@ -14,18 +14,27 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/TH.data/TH.data_{{ version }}.tar.gz
   sha256: 478f109fcc1226500ead8e3bd6e047cecde2294fde4df8ec216d38313db79a9d
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: dplyr, gdata, plyr
 requirements:
   build:
     - r-base
     - r-mass
     - r-survival
+
   run:
     - r-base
     - r-mass
@@ -33,9 +42,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('TH.data')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('TH.data')"           # [not win]
     - "\"%R%\" -e \"library('TH.data')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=TH.data
   license: GPL-3
@@ -46,3 +62,27 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: TH.data
+# Title: TH's Data Archive
+# Date: 2017-01-23
+# Version: 1.0-8
+# Authors@R: c(person("Torsten", "Hothorn", role = c("aut", "cre"), email = "Torsten.Hothorn@R-project.org"))
+# Description: Contains data sets used in other packages Torsten Hothorn maintains.
+# Depends: R (>= 2.10.0), survival, MASS
+# Suggests: dplyr, gdata, plyr
+# LazyData: yes
+# License: GPL-3
+# NeedsCompilation: no
+# Packaged: 2017-01-23 13:13:20 UTC; hothorn
+# Author: Torsten Hothorn [aut, cre]
+# Maintainer: Torsten Hothorn <Torsten.Hothorn@R-project.org>
+# Repository: CRAN
+# Date/Publication: 2017-01-23 21:08:04
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-threejs-feedstock contains modified content
diff --git a/r-threejs-feedstock/recipe/meta.yaml b/r-threejs-feedstock/recipe/meta.yaml
index 0181fc9..3359b8c 100644
--- a/r-threejs-feedstock/recipe/meta.yaml
+++ b/r-threejs-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '0.2.2' %}
+{% set version = '0.3.1' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,43 +12,87 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/threejs_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/threejs/threejs_{{ version }}.tar.gz
-  sha256: 41fe949490fbe0f71e39b0a144791da427bd7361d027579cb4a002ed53520cc5
+  sha256: 71750b741672a435ecf749b69c72f0681aa8bb795e317f4e3056d5e33f6d79e8
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: maps
 requirements:
   build:
     - r-base
-    - r-matrix
     - r-base64enc
+    - r-crosstalk
     - r-htmlwidgets >=0.3.2
-    - r-jsonlite
+    - r-igraph >=1.0.0
+
   run:
     - r-base
-    - r-matrix
     - r-base64enc
+    - r-crosstalk
     - r-htmlwidgets >=0.3.2
-    - r-jsonlite
+    - r-igraph >=1.0.0
 
 test:
   commands:
-    - $R -e "library('threejs')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('threejs')"           # [not win]
     - "\"%R%\" -e \"library('threejs')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
-  home: http://bwlewis.github.io/rthreejs
-  license: MIT
+  home: https://bwlewis.github.io/rthreejs
+  license: MIT + file LICENSE
   summary: Create interactive 3D scatter plots, network plots, and globes using the 'three.js'
-    visualization library ("http://threejs.org").
+    visualization library (<https://threejs.org>).
   license_family: MIT
 
 extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: threejs
+# Type: Package
+# Title: Interactive 3D Scatter Plots, Networks and Globes
+# Description: Create interactive 3D scatter plots, network plots, and globes using the 'three.js' visualization library (<https://threejs.org>).
+# Version: 0.3.1
+# Date: 2017-08-11
+# Authors@R: c( person("B. W.", "Lewis", role=c("aut","cre"), email="blewis@illposed.net"), person(given="Three.js authors", role="cph", comment="three.js JavaScript library"), person("Alexey", "Stukalov", role=("ctb"), email="astukalov@gmail.com"), person("Yihui","Xie", role=("ctb"), email="xie@yihui.name"), person("Andreas", "Briese", role=("ctb"), email="ab@edutoolbox.de"), person("B.",  "Thieurmel", role=("ctb"), email="bthieurmel@gmail.com") )
+# URL: https://bwlewis.github.io/rthreejs
+# BugReports: https://github.com/bwlewis/rthreejs/issues
+# License: MIT + file LICENSE
+# Depends: R (>= 3.0.0), igraph (>= 1.0.0)
+# Imports: htmlwidgets (>= 0.3.2), base64enc, crosstalk, methods, stats
+# Suggests: maps
+# Enhances: knitr, shiny
+# RoxygenNote: 5.0.1
+# NeedsCompilation: no
+# Packaged: 2017-08-12 13:04:06 UTC; blewis
+# Author: B. W. Lewis [aut, cre], Three.js authors [cph] (three.js JavaScript library), Alexey Stukalov [ctb], Yihui Xie [ctb], Andreas Briese [ctb], B. Thieurmel [ctb]
+# Maintainer: B. W. Lewis <blewis@illposed.net>
+# Repository: CRAN
+# Date/Publication: 2017-08-13 05:42:40 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-tibble-feedstock contains modified content
diff --git a/r-tibble-feedstock/recipe/meta.yaml b/r-tibble-feedstock/recipe/meta.yaml
index 3e273bc..0554e0b 100644
--- a/r-tibble-feedstock/recipe/meta.yaml
+++ b/r-tibble-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '1.3.3' %}
+{% set version = '1.3.4' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,45 +12,87 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/tibble_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/tibble/tibble_{{ version }}.tar.gz
-  sha256: 802ec8346eae2397a2fc64b254cf80a67c7832cb5211328336f45053ea2518ae
+  sha256: a7eef7018a68fc07c17c583fb7821a08d6bc381f5961258bffaa6ef6b137760b
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: covr, dplyr, knitr (>= 1.5.32), microbenchmark, nycflights13, testthat, rmarkdown, withr
 requirements:
   build:
-    - {{ compiler('c') }}    # [not win]
-    - {{ compiler('cxx') }}  # [not win]
     - r-base
+    - {{ compiler('cxx') }}      # [not win]
+    - {{native}}toolchain        # [win]
     - r-rcpp >=0.12.3
     - r-rlang
-    - r-lazyeval >=0.1.10
-    - posix                  # [win]
-    - {{native}}toolchain    # [win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-rcpp >=0.12.3
     - r-rlang
-    - {{native}}gcc-libs     # [win]
 
 test:
   commands:
-    - $R -e "library('tibble')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('tibble')"           # [not win]
     - "\"%R%\" -e \"library('tibble')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
-  home: https://github.com/tidyverse/tibble
-  license: MIT
-  summary: Provides a 'tbl_df' class that offers better checking and printing capabilities than
-    traditional data frames.
+  home: http://tibble.tidyverse.org/, https://github.com/tidyverse/tibble
+  license: MIT + file LICENSE
+  summary: Provides a 'tbl_df' class (the 'tibble') that provides stricter checking and better
+    formatting than the traditional data frame.
   license_family: MIT
 
 extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: tibble
+# Encoding: UTF-8
+# Version: 1.3.4
+# Title: Simple Data Frames
+# Description: Provides a 'tbl_df' class (the 'tibble') that provides stricter checking and better formatting than the traditional data frame.
+# Authors@R: c( person("Kirill", "Muller", , "krlmlr+r@mailbox.org", c("aut", "cre")), person("Hadley", "Wickham", , "hadley@rstudio.com", "aut"), person("Romain", "Francois", , "romain@r-enthusiasts.com", "ctb"), person("RStudio", role = "cph") )
+# URL: http://tibble.tidyverse.org/, https://github.com/tidyverse/tibble
+# BugReports: https://github.com/tidyverse/tibble/issues
+# Depends: R (>= 3.1.0)
+# Imports: methods, rlang, Rcpp (>= 0.12.3), utils
+# Suggests: covr, dplyr, knitr (>= 1.5.32), microbenchmark, nycflights13, testthat, rmarkdown, withr
+# LinkingTo: Rcpp
+# LazyData: yes
+# License: MIT + file LICENSE
+# RoxygenNote: 6.0.1
+# VignetteBuilder: knitr
+# NeedsCompilation: yes
+# Packaged: 2017-08-21 16:04:36 UTC; muelleki
+# Author: Kirill Muller [aut, cre], Hadley Wickham [aut], Romain Francois [ctb], RStudio [cph]
+# Maintainer: Kirill Muller <krlmlr+r@mailbox.org>
+# Repository: CRAN
+# Date/Publication: 2017-08-22 07:16:29 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-tidyr-feedstock contains modified content
diff --git a/r-tidyr-feedstock/recipe/meta.yaml b/r-tidyr-feedstock/recipe/meta.yaml
index 3d071d0..c931b6c 100644
--- a/r-tidyr-feedstock/recipe/meta.yaml
+++ b/r-tidyr-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '0.6.1' %}
+{% set version = '0.7.1' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,46 +12,66 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/tidyr_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/tidyr/tidyr_{{ version }}.tar.gz
-  sha256: 2708f92dbfc250d0b4f48b5fd755e991a462933c95681590e083ab75f11b1a86
+  sha256: b9c2430c72fe66c65a6aa83d835d3ea333ab09391c58ae0d57fd98e45088d1a1
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: knitr, testthat, covr, gapminder, rmarkdown
 requirements:
   build:
     - r-base
+    - {{ compiler('cxx') }}      # [not win]
+    - {{native}}toolchain        # [win]
     - r-rcpp
-    - r-dplyr >=0.4
-    - r-lazyeval
+    - r-dplyr >=0.7.0
+    - r-glue
     - r-magrittr
+    - r-purrr
+    - r-rlang
     - r-stringi
     - r-tibble
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - r-tidyselect
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-rcpp
-    - r-dplyr >=0.4
-    - r-lazyeval
+    - r-dplyr >=0.7.0
+    - r-glue
     - r-magrittr
+    - r-purrr
+    - r-rlang
     - r-stringi
     - r-tibble
-    - libgcc  # [not win]
+    - r-tidyselect
 
 test:
   commands:
-    - $R -e "library('tidyr')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('tidyr')"           # [not win]
     - "\"%R%\" -e \"library('tidyr')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://tidyr.tidyverse.org, https://github.com/tidyverse/tidyr
-  license: MIT
+  license: MIT + file LICENSE
   summary: An evolution of 'reshape2'. It's designed specifically for data tidying (not general
     reshaping or aggregating) and works well with 'dplyr' data pipelines.
   license_family: MIT
@@ -60,3 +80,32 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: tidyr
+# Title: Easily Tidy Data with 'spread()' and 'gather()' Functions
+# Version: 0.7.1
+# Authors@R: c( person("Hadley", "Wickham", , "hadley@rstudio.com", c("aut", "cre")), person("Lionel", "Henry", , "lionel@rstudio.com", "aut"), person("RStudio", role = "cph") )
+# Description: An evolution of 'reshape2'. It's designed specifically for data tidying (not general reshaping or aggregating) and works well with 'dplyr' data pipelines.
+# Depends: R (>= 3.1.0)
+# License: MIT + file LICENSE
+# LazyData: true
+# Imports: dplyr (>= 0.7.0), glue, magrittr, purrr, rlang, Rcpp, stringi, tibble, tidyselect
+# URL: http://tidyr.tidyverse.org, https://github.com/tidyverse/tidyr
+# BugReports: https://github.com/tidyverse/tidyr/issues
+# Suggests: knitr, testthat, covr, gapminder, rmarkdown
+# VignetteBuilder: knitr
+# LinkingTo: Rcpp
+# RoxygenNote: 6.0.1
+# NeedsCompilation: yes
+# Packaged: 2017-08-24 14:15:41 UTC; lionel
+# Author: Hadley Wickham [aut, cre], Lionel Henry [aut], RStudio [cph]
+# Maintainer: Hadley Wickham <hadley@rstudio.com>
+# Repository: CRAN
+# Date/Publication: 2017-09-01 15:15:47 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
diff --git a/r-tidyselect-feedstock/recipe/bld.bat b/r-tidyselect-feedstock/recipe/bld.bat
index e69de29..8b10c94 100644
--- a/r-tidyselect-feedstock/recipe/bld.bat
+++ b/r-tidyselect-feedstock/recipe/bld.bat
@@ -0,0 +1,8 @@
+"%R%" CMD INSTALL --build .
+if errorlevel 1 exit 1
+
+@rem Add more build steps here, if they are necessary.
+
+@rem See
+@rem http://docs.continuum.io/conda/build.html
+@rem for a list of environment variables that are set during the build process.
diff --git a/r-tidyselect-feedstock/recipe/build.sh b/r-tidyselect-feedstock/recipe/build.sh
index e69de29..0c37df6 100644
--- a/r-tidyselect-feedstock/recipe/build.sh
+++ b/r-tidyselect-feedstock/recipe/build.sh
@@ -0,0 +1,13 @@
+#!/bin/bash
+
+# R refuses to build packages that mark themselves as Priority: Recommended
+mv DESCRIPTION DESCRIPTION.old
+grep -v '^Priority: ' DESCRIPTION.old > DESCRIPTION
+
+$R CMD INSTALL --build .
+
+# Add more build steps here, if they are necessary.
+
+# See
+# http://docs.continuum.io/conda/build.html
+# for a list of environment variables that are set during the build process.
diff --git a/r-tidyselect-feedstock/recipe/meta.yaml b/r-tidyselect-feedstock/recipe/meta.yaml
index e69de29..5dd553f 100644
--- a/r-tidyselect-feedstock/recipe/meta.yaml
+++ b/r-tidyselect-feedstock/recipe/meta.yaml
@@ -0,0 +1,99 @@
+{% set version = '0.2.0' %}
+
+{% set posix = 'm2-' if win else '' %}
+{% set native = 'm2w64-' if win else '' %}
+
+package:
+  name: r-tidyselect
+  version: {{ version|replace("-", "_") }}
+
+source:
+  fn: tidyselect_{{ version }}.tar.gz
+  url:
+    - https://cran.r-project.org/src/contrib/tidyselect_{{ version }}.tar.gz
+    - https://cran.r-project.org/src/contrib/Archive/tidyselect/tidyselect_{{ version }}.tar.gz
+  sha256: 396117335269021d8709e2b1ddd613da81e2bfe56fdc58e107bbccbe0ac320c0
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
+build:
+  # If this is a new build for the same version, increment the build number.
+  number: 0
+
+  # This is required to make R link correctly on Linux.
+  rpaths:
+    - lib/R/lib/
+    - lib/
+
+# Suggests: testthat
+requirements:
+  build:
+    - r-base
+    - {{ compiler('cxx') }}      # [not win]
+    - {{native}}toolchain        # [win]
+    - r-rcpp >=0.12.0
+    - r-glue
+    - r-purrr
+    - r-rlang >=0.1
+
+  run:
+    - r-base
+    - {{native}}gcc-libs         # [win]
+    - r-rcpp >=0.12.0
+    - r-glue
+    - r-purrr
+    - r-rlang >=0.1
+
+test:
+  commands:
+    # You can put additional test commands to be run here.
+    - $R -e "library('tidyselect')"           # [not win]
+    - "\"%R%\" -e \"library('tidyselect')\""  # [win]
+
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
+about:
+  home: https://CRAN.R-project.org/package=tidyselect
+  license: GPL-3
+  summary: A backend for the selecting functions of the 'tidyverse'. It makes it easy to implement
+    select-like functions in your own packages in a way that is consistent with other
+    'tidyverse' interfaces for selection.
+  license_family: GPL3
+
+extra:
+  recipe-maintainers:
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: tidyselect
+# Title: Select from a Set of Strings
+# Version: 0.2.0
+# Authors@R: c( person("Lionel", "Henry", ,"lionel@rstudio.com", c("aut", "cre")), person("Hadley", "Wickham", ,"hadley@rstudio.com", "aut"), person("RStudio", role = "cph") )
+# Description: A backend for the selecting functions of the 'tidyverse'. It makes it easy to implement select-like functions in your own packages in a way that is consistent with other 'tidyverse' interfaces for selection.
+# Depends: R (>= 3.1.0)
+# Imports: glue, purrr, rlang (>= 0.1), Rcpp (>= 0.12.0)
+# Suggests: testthat
+# LinkingTo: Rcpp (>= 0.12.0),
+# License: GPL-3
+# Encoding: UTF-8
+# LazyData: true
+# ByteCompile: true
+# RoxygenNote: 6.0.1
+# NeedsCompilation: yes
+# Packaged: 2017-08-30 12:08:48 UTC; lionel
+# Author: Lionel Henry [aut, cre], Hadley Wickham [aut], RStudio [cph]
+# Maintainer: Lionel Henry <lionel@rstudio.com>
+# Repository: CRAN
+# Date/Publication: 2017-08-30 13:54:39 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-tidytext-feedstock contains modified content
diff --git a/r-tidytext-feedstock/recipe/meta.yaml b/r-tidytext-feedstock/recipe/meta.yaml
index 6d17fb5..f1c50b3 100644
--- a/r-tidytext-feedstock/recipe/meta.yaml
+++ b/r-tidytext-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '0.1.2' %}
+{% set version = '0.1.4' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,16 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/tidytext_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/tidytext/tidytext_{{ version }}.tar.gz
-  sha256: 9581ad8f5b03a29be15952053f1dad98e9218e61e1c78a05d162332ad53ab9de
+  sha256: 2e952a8d64a22a054112e16f90efe6e526829949092c9eb040cfce34ff8d5354
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: readr, tidyr, XML, tm, quanteda, wordcloud, knitr, rmarkdown, ggplot2, reshape2, topicmodels, NLP, scales, gutenbergr, testthat, mallet, stm
 requirements:
   build:
     - r-base
@@ -31,6 +38,7 @@ requirements:
     - r-hunspell
     - r-janeaustenr
     - r-purrr >=0.1.1
+    - r-rlang
     - r-stringr
     - r-tokenizers
 
@@ -42,17 +50,25 @@ requirements:
     - r-hunspell
     - r-janeaustenr
     - r-purrr >=0.1.1
+    - r-rlang
     - r-stringr
     - r-tokenizers
 
 test:
   commands:
-    - $R -e "library('tidytext')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('tidytext')"           # [not win]
     - "\"%R%\" -e \"library('tidytext')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://github.com/juliasilge/tidytext
-  license: MIT
+  license: MIT + file LICENSE
   summary: Text mining for word processing and sentiment analysis using 'dplyr', 'ggplot2', and
     other tidy tools.
   license_family: MIT
@@ -61,3 +77,32 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: tidytext
+# Type: Package
+# Title: Text Mining using 'dplyr', 'ggplot2', and Other Tidy Tools
+# Version: 0.1.4
+# Authors@R: c(person("Gabriela", "De Queiroz", email = "gabidequeiroz@gmail.com", role = c("ctb")), person("Oliver", "Keyes", email = "ironholds@gmail.com", role = c("ctb")), person("David", "Robinson", email = "admiral.david@gmail.com", role = c("aut")), person("Julia", "Silge", email = "julia.silge@gmail.com", role = c("aut", "cre")))
+# Description: Text mining for word processing and sentiment analysis using 'dplyr', 'ggplot2', and other tidy tools.
+# License: MIT + file LICENSE
+# LazyData: TRUE
+# URL: http://github.com/juliasilge/tidytext
+# BugReports: http://github.com/juliasilge/tidytext/issues
+# RoxygenNote: 6.0.1
+# Depends: R (>= 2.10)
+# Imports: rlang, dplyr, stringr, hunspell, broom, Matrix, tokenizers, janeaustenr, purrr (>= 0.1.1), methods
+# Suggests: readr, tidyr, XML, tm, quanteda, wordcloud, knitr, rmarkdown, ggplot2, reshape2, topicmodels, NLP, scales, gutenbergr, testthat, mallet, stm
+# VignetteBuilder: knitr
+# NeedsCompilation: no
+# Packaged: 2017-09-29 20:56:37 UTC; juliasilge
+# Author: Gabriela De Queiroz [ctb], Oliver Keyes [ctb], David Robinson [aut], Julia Silge [aut, cre]
+# Maintainer: Julia Silge <julia.silge@gmail.com>
+# Repository: CRAN
+# Date/Publication: 2017-09-29 21:57:43 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-tidyverse-feedstock contains modified content
diff --git a/r-tidyverse-feedstock/recipe/meta.yaml b/r-tidyverse-feedstock/recipe/meta.yaml
index 489139e..201ba9e 100644
--- a/r-tidyverse-feedstock/recipe/meta.yaml
+++ b/r-tidyverse-feedstock/recipe/meta.yaml
@@ -14,13 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/tidyverse/tidyverse_{{ version }}.tar.gz
   sha256: 1d6b3f2e62257692206ca30be43ab2f9db59f0cc8c05f657eb47ccf8fbd11daa
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: knitr, rmarkdown
 requirements:
   build:
     - r-base
@@ -43,6 +51,7 @@ requirements:
     - r-tibble
     - r-tidyr
     - r-xml2
+
   run:
     - r-base
     - r-broom
@@ -67,12 +76,19 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('tidyverse')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('tidyverse')"           # [not win]
     - "\"%R%\" -e \"library('tidyverse')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://tidyverse.org, https://github.com/tidyverse/tidyverse
-  license: GPL-3
+  license: GPL-3 | file LICENSE
   summary: The 'tidyverse' is a set of packages that work in harmony because they share common
     data representations and 'API' design. This package is designed to make it easy
     to install and load multiple 'tidyverse' packages in a single step. Learn more about
@@ -83,3 +99,31 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: tidyverse
+# Title: Easily Install and Load 'Tidyverse' Packages
+# Version: 1.1.1
+# Authors@R: c( person("Hadley", "Wickham", , "hadley@rstudio.com", role = c("aut", "cre")), person("RStudio", role = "cph") )
+# Description: The 'tidyverse' is a set of packages that work in harmony because they share common data representations and 'API' design. This package is designed to make it easy to install and load multiple 'tidyverse' packages in a single step. Learn more about the 'tidyverse' at <https://github.com/hadley/tidyverse>.
+# Imports: broom, dplyr, forcats, ggplot2, haven, httr, hms, jsonlite, lubridate, magrittr, modelr, purrr, readr, readxl, stringr, tibble, rvest, tidyr, xml2
+# Suggests: knitr, rmarkdown
+# License: GPL-3 | file LICENSE
+# Encoding: UTF-8
+# LazyData: true
+# RoxygenNote: 5.0.1.9000
+# URL: http://tidyverse.org, https://github.com/tidyverse/tidyverse
+# BugReports: https://github.com/tidyverse/tidyverse/issues
+# VignetteBuilder: knitr
+# NeedsCompilation: no
+# Packaged: 2017-01-27 17:02:48 UTC; hadley
+# Author: Hadley Wickham [aut, cre], RStudio [cph]
+# Maintainer: Hadley Wickham <hadley@rstudio.com>
+# Repository: CRAN
+# Date/Publication: 2017-01-27 18:47:50
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-tilegramsr-feedstock contains modified content
diff --git a/r-tilegramsr-feedstock/recipe/meta.yaml b/r-tilegramsr-feedstock/recipe/meta.yaml
index 96ce4b1..0a28b8a 100644
--- a/r-tilegramsr-feedstock/recipe/meta.yaml
+++ b/r-tilegramsr-feedstock/recipe/meta.yaml
@@ -14,15 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/tilegramsR/tilegramsR_{{ version }}.tar.gz
   sha256: 22ee5ec89158e7ad366e9e357e8ec6dde22cfd95fa4395323d1f3df8e8f69685
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  # r-sf is not available under windows
-  skip: true  # [win]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: dplyr, tidyr, knitr, rmarkdown, leaflet (>= 1.1.0),
 requirements:
   build:
     - r-base
@@ -36,12 +42,19 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('tilegramsR')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('tilegramsR')"           # [not win]
     - "\"%R%\" -e \"library('tilegramsR')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/bhaskarvk/tilegramsR
-  license: MIT
+  license: MIT + file LICENSE
   summary: R spatial objects for Tilegrams. Tilegrams are tiled maps where the region size is
     proportional to the certain characteristics of the dataset.
   license_family: MIT
@@ -50,3 +63,33 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: tilegramsR
+# Type: Package
+# Title: R Spatial Data for Tilegrams
+# Version: 0.2.0
+# Authors@R: person("Bhaskar", "Karambelkar", email = "bhaskarvk@gmail.com", role = c("aut", "cre"))
+# Description: R spatial objects for Tilegrams. Tilegrams are tiled maps where the region size is proportional to the certain characteristics of the dataset.
+# License: MIT + file LICENSE
+# Encoding: UTF-8
+# LazyData: true
+# Depends: R (>= 3.1.0), sf
+# Imports: sp
+# Suggests: dplyr, tidyr, knitr, rmarkdown, leaflet (>= 1.1.0),
+# VignetteBuilder: knitr
+# URL: https://github.com/bhaskarvk/tilegramsR
+# BugReports: https://github.com/bhaskarvk/tilegramsR/issues
+# RoxygenNote: 6.0.1
+# NeedsCompilation: no
+# Packaged: 2017-03-29 17:17:22 UTC; user001
+# Author: Bhaskar Karambelkar [aut, cre]
+# Maintainer: Bhaskar Karambelkar <bhaskarvk@gmail.com>
+# Repository: CRAN
+# Date/Publication: 2017-03-29 17:43:16 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-timedate-feedstock contains modified content
diff --git a/r-timedate-feedstock/recipe/meta.yaml b/r-timedate-feedstock/recipe/meta.yaml
index 5cbf0e1..ab3d27d 100644
--- a/r-timedate-feedstock/recipe/meta.yaml
+++ b/r-timedate-feedstock/recipe/meta.yaml
@@ -12,18 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/timeDate_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/timeDate/timeDate_{{ version }}.tar.gz
+  sha256: 6262ef7ca9f5eeb9db8229d6bb7a51d46d467a4fa73e2ccc5b4b78e18780c432
 
 
-  sha256: 6262ef7ca9f5eeb9db8229d6bb7a51d46d467a4fa73e2ccc5b4b78e18780c432
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
-  skip: True  # [win32]
 
+# Suggests: date, RUnit
 requirements:
   build:
     - r-base
@@ -33,10 +38,15 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('timeDate')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('timeDate')"           # [not win]
     - "\"%R%\" -e \"library('timeDate')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
 
+  # requires:
+    # Put any additional test requirements here.
 
 about:
   home: https://www.rmetrics.org
@@ -45,8 +55,31 @@ about:
     chronological and calendar objects.
   license_family: GPL3
 
-
-
 extra:
   recipe-maintainers:
     - croth1
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: timeDate
+# Title: Rmetrics - Chronological and Calendar Objects
+# Date: 2015-01-22
+# Version: 3012.100
+# Author: Rmetrics Core Team, Diethelm Wuertz [aut], Tobias Setz [cre], Yohan Chalabi [ctb], Martin Maechler [ctb], Joe W. Byers [ctb]
+# Maintainer: Tobias Setz <tobias.setz@rmetrics.org>
+# Description: Environment for teaching  "Financial Engineering and Computational Finance". Managing chronological and calendar objects.
+# Depends: R (>= 2.15.1), graphics, utils, stats, methods
+# Suggests: date, RUnit
+# Note: SEVERAL PARTS ARE STILL PRELIMINARY AND MAY BE CHANGED IN THE FUTURE. THIS TYPICALLY INCLUDES FUNCTION AND ARGUMENT NAMES, AS WELL AS DEFAULTS FOR ARGUMENTS AND RETURN VALUES.
+# LazyData: yes
+# License: GPL (>= 2)
+# URL: https://www.rmetrics.org
+# Packaged: 2015-01-23 00:36:51 UTC; Tobi
+# NeedsCompilation: no
+# Repository: CRAN
+# Date/Publication: 2015-01-23 09:30:30
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-tm-feedstock contains modified content
diff --git a/r-tm-feedstock/recipe/meta.yaml b/r-tm-feedstock/recipe/meta.yaml
index cfb7109..43a4ed8 100644
--- a/r-tm-feedstock/recipe/meta.yaml
+++ b/r-tm-feedstock/recipe/meta.yaml
@@ -14,27 +14,35 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/tm/tm_{{ version }}.tar.gz
   sha256: 184dc285d3c53b8b4adacfd78b2b098c1bf8ead948d18719efa979a4a6cfe6fc
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: filehash, methods, Rcampdf, Rgraphviz, Rpoppler, SnowballC, tm.lexicon.GeneralInquirer, XML
 requirements:
   build:
     - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('cxx') }}      # [not win]
+    - {{native}}toolchain        # [win]
     - r-bh
     - r-nlp >=0.1_6.2
     - r-rcpp
     - r-slam >=0.1_37
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-bh
     - r-nlp >=0.1_6.2
     - r-rcpp
@@ -42,9 +50,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('tm')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('tm')"           # [not win]
     - "\"%R%\" -e \"library('tm')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://tm.r-forge.r-project.org/
   license: GPL-3
@@ -55,3 +70,31 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: tm
+# Title: Text Mining Package
+# Version: 0.7-1
+# Date: 2017-03-02
+# Authors@R: c(person("Ingo", "Feinerer", role = c("aut", "cre"), email = "feinerer@logic.at"), person("Kurt", "Hornik", role = "aut"), person("Artifex Software, Inc.", role = c("ctb", "cph"), comment = "pdf_info.ps taken from GPL Ghostscript"))
+# Depends: R (>= 3.2.0), NLP (>= 0.1-6.2)
+# Imports: Rcpp, parallel, slam (>= 0.1-37), stats, tools, utils, graphics
+# LinkingTo: BH, Rcpp
+# Suggests: filehash, methods, Rcampdf, Rgraphviz, Rpoppler, SnowballC, tm.lexicon.GeneralInquirer, XML
+# SystemRequirements: Antiword (<http://www.winfield.demon.nl/>) for reading MS Word files, pdfinfo and pdftotext from Poppler (<http://poppler.freedesktop.org/>) for reading PDF
+# Description: A framework for text mining applications within R.
+# License: GPL-3
+# URL: http://tm.r-forge.r-project.org/
+# Additional_repositories: http://datacube.wu.ac.at
+# NeedsCompilation: yes
+# Packaged: 2017-03-02 14:36:20 UTC; hornik
+# Author: Ingo Feinerer [aut, cre], Kurt Hornik [aut], Artifex Software, Inc. [ctb, cph] (pdf_info.ps taken from GPL Ghostscript)
+# Maintainer: Ingo Feinerer <feinerer@logic.at>
+# Repository: CRAN
+# Date/Publication: 2017-03-02 17:45:01
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-tokenizers-feedstock contains modified content
diff --git a/r-tokenizers-feedstock/recipe/meta.yaml b/r-tokenizers-feedstock/recipe/meta.yaml
index 64c3f9d..7be309c 100644
--- a/r-tokenizers-feedstock/recipe/meta.yaml
+++ b/r-tokenizers-feedstock/recipe/meta.yaml
@@ -14,38 +14,54 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/tokenizers/tokenizers_{{ version }}.tar.gz
   sha256: 693e19e32605f4c66a51b1eb84258a973ac2fe1d2f919b28e66944721aab9ae1
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: testthat, covr, knitr, rmarkdown
 requirements:
   build:
     - r-base
+    - {{ compiler('cxx') }}      # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
     - r-rcpp >=0.12.3
     - r-snowballc >=0.5.1
     - r-stringi >=1.0.1
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-rcpp >=0.12.3
     - r-snowballc >=0.5.1
     - r-stringi >=1.0.1
 
 test:
   commands:
-    - $R -e "library('tokenizers')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('tokenizers')"           # [not win]
     - "\"%R%\" -e \"library('tokenizers')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/ropensci/tokenizers
-  license: MIT
+  license: MIT + file LICENSE
   summary: Convert natural language text into tokens. The tokenizers have a consistent interface
     and are compatible with Unicode, thanks to being built on the 'stringi' package.
     Includes tokenizers for shingled n-grams, skip n-grams, words, word stems, sentences,
@@ -56,3 +72,33 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: tokenizers
+# Type: Package
+# Title: A Consistent Interface to Tokenize Natural Language Text
+# Version: 0.1.4
+# Description: Convert natural language text into tokens. The tokenizers have a consistent interface and are compatible with Unicode, thanks to being built on the 'stringi' package. Includes tokenizers for shingled n-grams, skip n-grams, words, word stems, sentences, paragraphs, characters, lines, and regular expressions.
+# License: MIT + file LICENSE
+# LazyData: yes
+# Authors@R: c(person("Lincoln", "Mullen", role = c("aut", "cre"), email = "lincoln@lincolnmullen.com"), person("Dmitriy", "Selivanov", role = c("ctb"), email = "selivanov.dmitriy@gmail.com"))
+# URL: https://github.com/ropensci/tokenizers
+# BugReports: https://github.com/ropensci/tokenizers/issues
+# RoxygenNote: 5.0.1
+# Depends: R (>= 3.1.3)
+# Imports: stringi (>= 1.0.1), Rcpp (>= 0.12.3), SnowballC (>= 0.5.1)
+# LinkingTo: Rcpp
+# Suggests: testthat, covr, knitr, rmarkdown
+# VignetteBuilder: knitr
+# NeedsCompilation: yes
+# Packaged: 2016-08-29 19:15:04 UTC; lmullen
+# Author: Lincoln Mullen [aut, cre], Dmitriy Selivanov [ctb]
+# Maintainer: Lincoln Mullen <lincoln@lincolnmullen.com>
+# Repository: CRAN
+# Date/Publication: 2016-08-29 22:59:29
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-tseries-feedstock contains modified content
diff --git a/r-tseries-feedstock/recipe/meta.yaml b/r-tseries-feedstock/recipe/meta.yaml
index 7d29e81..208d637 100644
--- a/r-tseries-feedstock/recipe/meta.yaml
+++ b/r-tseries-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '0.10-40' %}
+{% set version = '0.10-42' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,36 +12,54 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/tseries_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/tseries/tseries_{{ version }}.tar.gz
-  sha256: 6428124cd082fed3375a9c3627008b93ecd886c18ef086dba1a0224126216bd5
+  sha256: 827f79858715c700e8cabd2c27853ba88ad0e05eb043bc94e126b155a75546c4
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('fortran') }}  # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
     - r-quadprog
+    - r-quantmod >=0.4.9
     - r-zoo
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-quadprog
+    - r-quantmod >=0.4.9
     - r-zoo
-    - libgcc  # [not win]
 
 test:
   commands:
-    - $R -e "library('tseries')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('tseries')"           # [not win]
     - "\"%R%\" -e \"library('tseries')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=tseries
   license: GPL-2
@@ -52,3 +70,25 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: tseries
+# Version: 0.10-42
+# Title: Time Series Analysis and Computational Finance
+# Authors@R: c(person("Adrian", "Trapletti", role = "aut", email = "adrian@trapletti.org"), person("Kurt", "Hornik", role = c("aut", "cre"), email = "Kurt.Hornik@R-project.org"), person("Blake", "LeBaron", role = "ctb", comment = "BDS test code"))
+# Description: Time series analysis and computational finance.
+# Depends: R (>= 2.10.0)
+# Imports: graphics, stats, utils, quadprog, zoo, quantmod (>= 0.4.9)
+# License: GPL-2
+# NeedsCompilation: yes
+# Packaged: 2017-06-21 18:26:43 UTC; hornik
+# Author: Adrian Trapletti [aut], Kurt Hornik [aut, cre], Blake LeBaron [ctb] (BDS test code)
+# Maintainer: Kurt Hornik <Kurt.Hornik@R-project.org>
+# Repository: CRAN
+# Date/Publication: 2017-06-22 05:37:54 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-ttr-feedstock contains modified content
diff --git a/r-ttr-feedstock/recipe/meta.yaml b/r-ttr-feedstock/recipe/meta.yaml
index 9edcf92..28578f8 100644
--- a/r-ttr-feedstock/recipe/meta.yaml
+++ b/r-ttr-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '0.23-1' %}
+{% set version = '0.23-2' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,35 +12,52 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/TTR_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/TTR/TTR_{{ version }}.tar.gz
-  sha256: 699798f06ceae9663da47b67d1bc8679fc1c0776d12afd054d6ac4d19e05b2ae
+  sha256: 1a1fc1d2361d58d573d6ec645633521840d3f606406314a992967e8c17c75436
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: RUnit
 requirements:
   build:
     - r-base
-    - r-xts >=0.9_3
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('fortran') }}  # [not win]
+    - {{native}}toolchain        # [win]
+    - r-curl
+    - r-xts >=0.10_0
     - r-zoo
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
-    - r-xts >=0.9_3
+    - {{native}}gcc-libs         # [win]
+    - r-curl
+    - r-xts >=0.10_0
     - r-zoo
-    - libgcc  # [not win]
 
 test:
   commands:
-    - $R -e "library('TTR')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('TTR')"           # [not win]
     - "\"%R%\" -e \"library('TTR')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/joshuaulrich/TTR
   license: GPL-2
@@ -51,3 +68,31 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: TTR
+# Type: Package
+# Title: Technical Trading Rules
+# Version: 0.23-2
+# Date: 2017-07-07
+# Author: Joshua Ulrich
+# Maintainer: Joshua Ulrich <josh.m.ulrich@gmail.com>
+# Imports: xts (>= 0.10-0), zoo, curl
+# LinkingTo: xts
+# Enhances: quantmod
+# Suggests: RUnit
+# Description: Functions and data to construct technical trading rules with R.
+# License: GPL-2
+# URL: https://github.com/joshuaulrich/TTR
+# BugReports: https://github.com/joshuaulrich/TTR/issues
+# RoxygenNote: 6.0.1
+# NeedsCompilation: yes
+# Packaged: 2017-07-10 17:18:49 UTC; josh
+# Repository: CRAN
+# Date/Publication: 2017-07-11 05:34:20 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-udunits2-feedstock contains modified content
diff --git a/r-udunits2-feedstock/recipe/meta.yaml b/r-udunits2-feedstock/recipe/meta.yaml
index 0ef0f9e..52322cd 100644
--- a/r-udunits2-feedstock/recipe/meta.yaml
+++ b/r-udunits2-feedstock/recipe/meta.yaml
@@ -14,32 +14,50 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/udunits2/udunits2_{{ version }}.tar.gz
   sha256: d155d3c07f6202b65dec4075ffd1e1c3f4f35f5fdece8cfb319d39256a3e5b79
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
-    - udunits2
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}autoconf
+    - {{posix}}automake
+    - {{posix}}pkg-config
+    - {{posix}}make
+    - {{native}}udunits2
 
   run:
     - r-base
-    - libgcc  # [not win]
-    - udunits2
+    - {{native}}gcc-libs         # [win]
+    - {{native}}udunits2
 
 test:
   commands:
-    - $R -e "library('udunits2')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('udunits2')"           # [not win]
     - "\"%R%\" -e \"library('udunits2')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/pacificclimate/Rudunits2 https://www.unidata.ucar.edu/software/udunits/
   license: GPL-2
@@ -50,3 +68,28 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: udunits2
+# Type: Package
+# Title: Udunits-2 Bindings for R
+# Version: 0.13
+# Date: 2016-11-16
+# Author: James Hiebert <hiebert@uvic.ca>
+# Maintainer: James Hiebert <hiebert@uvic.ca>
+# Description: Provides simple bindings to Unidata's udunits library.
+# URL: https://github.com/pacificclimate/Rudunits2 https://www.unidata.ucar.edu/software/udunits/
+# SystemRequirements: udunits-2
+# License: GPL-2
+# LazyLoad: yes
+# Depends: R (>= 2.10.0)
+# NeedsCompilation: yes
+# Packaged: 2016-11-16 23:23:15 UTC; james
+# Repository: CRAN
+# Date/Publication: 2016-11-17 08:27:56
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-units-feedstock contains modified content
diff --git a/r-units-feedstock/recipe/meta.yaml b/r-units-feedstock/recipe/meta.yaml
index ab818fc..69c50e0 100644
--- a/r-units-feedstock/recipe/meta.yaml
+++ b/r-units-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '0.4-4' %}
+{% set version = '0.4-6' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,16 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/units_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/units/units_{{ version }}.tar.gz
-  sha256: a5bafe44acf0d0ebeb39122a98bb43756b4697fa245e8328038349772288ca8e
+  sha256: db383c9b7ec221a5da29a2ddf4f74f9064c44ea2102ea7e07cc1cc5bb30fa1ef
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: NISTunits, measurements, xml2, tibble, knitr, testthat, ggforce, magrittr
 requirements:
   build:
     - r-base
@@ -33,19 +40,55 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('units')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('units')"           # [not win]
     - "\"%R%\" -e \"library('units')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/edzer/units/
   license: GPL-2
-  summary: Support for measurement units in R vectors; automatic propagation, conversion, derivation
-    and simplification of units; raising errors in case of unit incompatibility. Compatible
-    with the difftime class. Uses the UNIDATA udunits library and unit database for
-    unit conversion and compatibility checking.
+  summary: 'Support for measurement units in R vectors, matrices and arrays: automatic propagation,
+    conversion, derivation and simplification of units; raising errors in case of unit
+    incompatibility. Compatible with the POSIXct, Date and difftime  classes. Uses the
+    UNIDATA udunits library and unit database for  unit compatibility checking and conversion.'
+
   license_family: GPL2
 
 extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: units
+# Version: 0.4-6
+# Title: Measurement Units for R Vectors
+# Authors@R: c(person("Edzer", "Pebesma", role = c("aut", "cre"), email = "edzer.pebesma@uni-muenster.de"), person("Thomas", "Mailund", role = "aut", email = "mailund@birc.au.dk"))
+# Depends: R (>= 3.0.0)
+# Imports: udunits2 (>= 0.13)
+# Suggests: NISTunits, measurements, xml2, tibble, knitr, testthat, ggforce, magrittr
+# VignetteBuilder: knitr
+# Description: Support for measurement units in R vectors, matrices and arrays: automatic propagation, conversion, derivation and simplification of units; raising errors in case of unit incompatibility. Compatible with the POSIXct, Date and difftime  classes. Uses the UNIDATA udunits library and unit database for  unit compatibility checking and conversion.
+# License: GPL-2
+# URL: https://github.com/edzer/units/
+# BugReports: https://github.com/edzer/units/issues/
+# RoxygenNote: 6.0.1
+# Encoding: UTF-8
+# NeedsCompilation: no
+# Packaged: 2017-08-27 10:21:13 UTC; edzer
+# Author: Edzer Pebesma [aut, cre], Thomas Mailund [aut]
+# Maintainer: Edzer Pebesma <edzer.pebesma@uni-muenster.de>
+# Repository: CRAN
+# Date/Publication: 2017-08-27 16:21:36 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-urca-feedstock contains modified content
diff --git a/r-urca-feedstock/recipe/meta.yaml b/r-urca-feedstock/recipe/meta.yaml
index 914f53f..9242638 100644
--- a/r-urca-feedstock/recipe/meta.yaml
+++ b/r-urca-feedstock/recipe/meta.yaml
@@ -14,32 +14,47 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/urca/urca_{{ version }}.tar.gz
   sha256: 621cc82398e25b58b4a16edf000ed0a1484d9a0bc458f734e97b6f371cc76aaa
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
+    - {{ compiler('fortran') }}  # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
     - r-nlme
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-nlme
-    - libgcc  # [not win]
 
 test:
   commands:
-    - $R -e "library('urca')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('urca')"           # [not win]
     - "\"%R%\" -e \"library('urca')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=urca
   license: GPL (>= 2)
@@ -51,3 +66,27 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: urca
+# Version: 1.3-0
+# Date: 2016-09-06
+# Title: Unit Root and Cointegration Tests for Time Series Data
+# Authors@R: c(person("Bernhard", "Pfaff", email = "bernhard@pfaffikus.de", role = c("aut", "cre")), person("Eric", "Zivot",email = "ezivot@u.washington.edu", role = "ctb"), person("Matthieu", "Stigler", role = "ctb"))
+# Depends: R (>= 2.0.0), methods
+# Imports: nlme, graphics, stats
+# LazyLoad: yes
+# Description: Unit root and cointegration tests encountered in applied  econometric analysis are implemented.
+# License: GPL (>= 2)
+# NeedsCompilation: yes
+# Packaged: 2016-09-06 20:57:19 UTC; bp
+# Author: Bernhard Pfaff [aut, cre], Eric Zivot [ctb], Matthieu Stigler [ctb]
+# Maintainer: Bernhard Pfaff <bernhard@pfaffikus.de>
+# Repository: CRAN
+# Date/Publication: 2016-09-06 23:26:02
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-uuid-feedstock contains modified content
diff --git a/r-uuid-feedstock/recipe/bld.bat b/r-uuid-feedstock/recipe/bld.bat
new file mode 100644
index 0000000..8b10c94
--- /dev/null
+++ b/r-uuid-feedstock/recipe/bld.bat
@@ -0,0 +1,8 @@
+"%R%" CMD INSTALL --build .
+if errorlevel 1 exit 1
+
+@rem Add more build steps here, if they are necessary.
+
+@rem See
+@rem http://docs.continuum.io/conda/build.html
+@rem for a list of environment variables that are set during the build process.
diff --git a/r-uuid-feedstock/recipe/build.sh b/r-uuid-feedstock/recipe/build.sh
new file mode 100644
index 0000000..0c37df6
--- /dev/null
+++ b/r-uuid-feedstock/recipe/build.sh
@@ -0,0 +1,13 @@
+#!/bin/bash
+
+# R refuses to build packages that mark themselves as Priority: Recommended
+mv DESCRIPTION DESCRIPTION.old
+grep -v '^Priority: ' DESCRIPTION.old > DESCRIPTION
+
+$R CMD INSTALL --build .
+
+# Add more build steps here, if they are necessary.
+
+# See
+# http://docs.continuum.io/conda/build.html
+# for a list of environment variables that are set during the build process.
diff --git a/r-uuid-feedstock/recipe/meta.yaml b/r-uuid-feedstock/recipe/meta.yaml
index d6f2401..c1e6a24 100644
--- a/r-uuid-feedstock/recipe/meta.yaml
+++ b/r-uuid-feedstock/recipe/meta.yaml
@@ -1,44 +1,88 @@
-{% set version = "0.1-2" %}
+{% set version = '0.1-2' %}
+
+{% set posix = 'm2-' if win else '' %}
+{% set native = 'm2w64-' if win else '' %}
 
 package:
   name: r-uuid
-  version: {{ version.replace("-", "_") }}
+  version: {{ version|replace("-", "_") }}
 
 source:
   fn: uuid_{{ version }}.tar.gz
   url:
-    - http://cran.r-project.org/src/contrib/uuid_{{ version }}.tar.gz
-    - http://cran.r-project.org/src/contrib/Archive/uuid/uuid_{{ version }}.tar.gz
+    - https://cran.r-project.org/src/contrib/uuid_{{ version }}.tar.gz
+    - https://cran.r-project.org/src/contrib/Archive/uuid/uuid_{{ version }}.tar.gz
   sha256: dd71704dc336b0857981b92a75ed9877d4ca47780b1682def28839304cd3b1be
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: True  # [win32]
-  script: R CMD INSTALL --build .
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
-requirements:  # [not win32]
-  build:  # [not win32]
-    - r-base  # [not win32]
-    - posix  # [win64]
-    - m2w64-toolchain  # [win64]
-    - gcc  # [not win]
-  run:  # [not win32]
-    - r-base  # [not win32]
+
+requirements:
+  build:
+    - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}autoconf
+    - {{posix}}automake
+    - {{posix}}pkg-config
+    - {{posix}}make
+
+  run:
+    - r-base
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - R -e "library('uuid')"  # [not win]
-    - R -e \"library('uuid')\"  # [win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('uuid')"           # [not win]
+    - "\"%R%\" -e \"library('uuid')\""  # [win]
+
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
 
 about:
   home: http://www.rforge.net/uuid
-  license: MIT
-  license_file: LICENSE
-  summary: 'Tools for generating and handling of UUIDs (Universally Unique Identifiers).'
+  license: MIT + file LICENSE
+  summary: Tools for generating and handling of UUIDs (Universally Unique Identifiers).
+  license_family: MIT
 
 extra:
   recipe-maintainers:
     - ocefpaf
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: uuid
+# Version: 0.1-2
+# Title: Tools for generating and handling of UUIDs
+# Author: Simon Urbanek <Simon.Urbanek@r-project.org> (R package), Theodore Ts'o <tytso@thunk.org> (libuuid)
+# Maintainer: Simon Urbanek <Simon.Urbanek@r-project.org>
+# Depends: R (>= 2.9.0)
+# Description: Tools for generating and handling of UUIDs (Universally Unique Identifiers).
+# License: MIT + file LICENSE
+# URL: http://www.rforge.net/uuid
+# NeedsCompilation: yes
+# Packaged: 2015-07-27 20:57:43 UTC; svnuser
+# Repository: CRAN
+# Date/Publication: 2015-07-28 08:03:46
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-vars-feedstock contains modified content
diff --git a/r-vars-feedstock/recipe/meta.yaml b/r-vars-feedstock/recipe/meta.yaml
index 3476655..72fb013 100644
--- a/r-vars-feedstock/recipe/meta.yaml
+++ b/r-vars-feedstock/recipe/meta.yaml
@@ -14,13 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/vars/vars_{{ version }}.tar.gz
   sha256: 50fa365a24885f021f7f5a3bd4d898ecfec0c14784ca9d9db284f30356f985e0
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+
 requirements:
   build:
     - r-base
@@ -29,6 +37,7 @@ requirements:
     - r-sandwich >=2.2_4
     - r-strucchange
     - r-urca >=1.1_6
+
   run:
     - r-base
     - r-mass
@@ -39,9 +48,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('vars')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('vars')"           # [not win]
     - "\"%R%\" -e \"library('vars')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://www.pfaffikus.de
   license: GPL (>= 2)
@@ -54,3 +70,31 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: vars
+# Type: Package
+# Title: VAR Modelling
+# Version: 1.5-2
+# Date: 2013-07-22
+# Authors@R: c(person("Bernhard", "Pfaff", email = "bernhard@pfaffikus.de", role = c("aut", "cre")), person("Matthieu", "Stigler", role = "ctb"))
+# Depends: R (>= 2.0.0), MASS, strucchange, urca (>= 1.1-6), lmtest (>= 0.9-26), sandwich (>= 2.2-4)
+# LazyLoad: yes
+# Description: Estimation, lag selection, diagnostic testing, forecasting, causality analysis, forecast error variance decomposition and impulse response functions of VAR models and estimation of SVAR and SVEC models.
+# License: GPL (>= 2)
+# URL: http://www.pfaffikus.de
+# Author: Bernhard Pfaff [aut, cre], Matthieu Stigler [ctb]
+# Maintainer: Bernhard Pfaff <bernhard@pfaffikus.de>
+# Repository: CRAN
+# Repository/R-Forge/Project: vars
+# Repository/R-Forge/Revision: 99
+# Repository/R-Forge/DateTimeStamp: 2013-07-21 14:35:42
+# Date/Publication: 2013-07-22 10:42:44
+# Packaged: 2013-07-21 18:20:24 UTC; rforge
+# NeedsCompilation: no
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-vgam-feedstock contains modified content
diff --git a/r-vgam-feedstock/recipe/meta.yaml b/r-vgam-feedstock/recipe/meta.yaml
index 0e31128..4c84368 100644
--- a/r-vgam-feedstock/recipe/meta.yaml
+++ b/r-vgam-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '1.0-3' %}
+{% set version = '1.0-4' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,38 +12,49 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/VGAM_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/VGAM/VGAM_{{ version }}.tar.gz
+  sha256: e581985f78ef8b866d0e810b2727061bb9c9bc177b2c9090aebb3a35ae87a964
 
 
-  sha256: 23bb6690ae15e9ede3198ef55d5d3236c279aa8fa6bd4f7350242379d9d72673
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: True  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: VGAMdata, MASS, mgcv
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('fortran') }}  # [not win]
+    - {{native}}toolchain        # [win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('VGAM')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('VGAM')"           # [not win]
     - "\"%R%\" -e \"library('VGAM')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
 
+  # requires:
+    # Put any additional test requirements here.
 
 about:
   home: https://www.stat.auckland.ac.nz/~yee/VGAM
-  license: GPL-2 | GPL-3
+  license: GPL-3
   summary: 'An implementation of about 6 major classes of statistical regression models. At the
     heart of it are the vector generalized linear and additive model (VGLM/VGAM) classes,
     and the book "Vector Generalized Linear and Additive Models: With an Implementation
@@ -63,3 +74,29 @@ about:
 extra:
   recipe-maintainers:
     - croth1
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: VGAM
+# Version: 1.0-4
+# Date: 2017-07-24
+# Title: Vector Generalized Linear and Additive Models
+# Author: Thomas W. Yee <t.yee@auckland.ac.nz>
+# Maintainer: Thomas Yee <t.yee@auckland.ac.nz>
+# Depends: R (>= 3.4.0), methods, stats, stats4, splines
+# Suggests: VGAMdata, MASS, mgcv
+# Description: An implementation of about 6 major classes of statistical regression models. At the heart of it are the vector generalized linear and additive model (VGLM/VGAM) classes, and the book "Vector Generalized Linear and Additive Models: With an Implementation in R" (Yee, 2015) <DOI:10.1007/978-1-4939-2818-7> gives details of the statistical framework and VGAM package. Currently only fixed-effects models are implemented, i.e., no random-effects models. Many (150+) models and distributions are estimated by maximum likelihood estimation (MLE) or penalized MLE, using Fisher scoring. VGLMs can be loosely thought of as multivariate GLMs. VGAMs are data-driven VGLMs (i.e., with smoothing). The other classes are RR-VGLMs (reduced-rank VGLMs), quadratic RR-VGLMs, reduced-rank VGAMs, RCIMs (row-column interaction models)---these classes perform constrained and unconstrained quadratic ordination (CQO/UQO) models in ecology, as well as constrained additive ordination (CAO). Note that these functions are subject to change; see the NEWS and ChangeLog files for latest changes.
+# License: GPL-3
+# URL: https://www.stat.auckland.ac.nz/~yee/VGAM
+# NeedsCompilation: yes
+# BuildVignettes: yes
+# LazyLoad: yes
+# LazyData: yes
+# Packaged: 2017-07-24 04:56:17 UTC; tyee001
+# Repository: CRAN
+# Date/Publication: 2017-07-25 21:01:44 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-viridis-feedstock contains modified content
diff --git a/r-viridis-feedstock/recipe/meta.yaml b/r-viridis-feedstock/recipe/meta.yaml
index 4d8c2ce..cb74047 100644
--- a/r-viridis-feedstock/recipe/meta.yaml
+++ b/r-viridis-feedstock/recipe/meta.yaml
@@ -14,19 +14,28 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/viridis/viridis_{{ version }}.tar.gz
   sha256: 93d2ded68ed7cec5633c260dbc47051416147aae074f29ebe135cc329250b00e
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: hexbin (>= 1.27.0), scales, MASS, knitr, dichromat, colorspace, rasterVis, httr, mapproj, vdiffr, svglite (>= 1.2.0), testthat, covr, rmarkdown
 requirements:
   build:
     - r-base
     - r-ggplot2 >=1.0.1
     - r-gridextra
     - r-viridislite >=0.2.0
+
   run:
     - r-base
     - r-ggplot2 >=1.0.1
@@ -35,16 +44,22 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('viridis')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('viridis')"           # [not win]
     - "\"%R%\" -e \"library('viridis')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/sjmgarnier/viridis
-  license: MIT
-  summary: |
-    Port of the new matplotlib color maps ('viridis' - the default -, 'magma', 'plasma'
-    and 'inferno') to R. matplotlib is a popular plotting
-    library for python. These color maps are designed in such a way that they will
+  license: MIT + file LICENSE
+  summary: Port of the new 'matplotlib' color maps ('viridis' - the default -, 'magma', 'plasma'
+    and 'inferno') to 'R'. 'matplotlib' <http://matplotlib.org/ > is a popular plotting
+    library for 'python'. These color maps are designed in such a way that they will
     analytically be perfectly perceptually-uniform, both in regular form and also when
     converted to black-and-white. They are also designed to be perceived by readers
     with the most common form of color blindness.
@@ -54,3 +69,33 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: viridis
+# Type: Package
+# Title: Default Color Maps from 'matplotlib'
+# Version: 0.4.0
+# Authors@R: c( person("Simon", "Garnier", email = "garnier@njit.edu", role = c("aut", "cre")), person("Noam", "Ross", email = "noam.ross@gmail.com", role = c("ctb", "cph")), person("Bob", "Rudis", email = "bob@rud.is", role = c("ctb", "cph")) )
+# Maintainer: Simon Garnier <garnier@njit.edu>
+# Description: Port of the new 'matplotlib' color maps ('viridis' - the default -, 'magma', 'plasma' and 'inferno') to 'R'. 'matplotlib' <http://matplotlib.org/ > is a popular plotting library for 'python'. These color maps are designed in such a way that they will analytically be perfectly perceptually-uniform, both in regular form and also when converted to black-and-white. They are also designed to be perceived by readers with the most common form of color blindness.
+# License: MIT + file LICENSE
+# LazyData: TRUE
+# Encoding: UTF-8
+# Depends: R (>= 2.10), viridisLite (>= 0.2.0)
+# Imports: stats, ggplot2 (>= 1.0.1), gridExtra
+# Suggests: hexbin (>= 1.27.0), scales, MASS, knitr, dichromat, colorspace, rasterVis, httr, mapproj, vdiffr, svglite (>= 1.2.0), testthat, covr, rmarkdown
+# VignetteBuilder: knitr
+# URL: https://github.com/sjmgarnier/viridis
+# BugReports: https://github.com/sjmgarnier/viridis/issues
+# RoxygenNote: 6.0.1
+# NeedsCompilation: no
+# Packaged: 2017-03-27 03:03:30 UTC; simon
+# Author: Simon Garnier [aut, cre], Noam Ross [ctb, cph], Bob Rudis [ctb, cph]
+# Repository: CRAN
+# Date/Publication: 2017-03-27 14:09:22 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-viridislite-feedstock contains modified content
diff --git a/r-viridislite-feedstock/recipe/meta.yaml b/r-viridislite-feedstock/recipe/meta.yaml
index 7c90141..d05b255 100644
--- a/r-viridislite-feedstock/recipe/meta.yaml
+++ b/r-viridislite-feedstock/recipe/meta.yaml
@@ -12,18 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/viridisLite_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/viridisLite/viridisLite_{{ version }}.tar.gz
+  sha256: 2d4d909f21c51e720bd685f05041ba158294e0a4064e0946d0bd916709818694
 
 
-  sha256: 2d4d909f21c51e720bd685f05041ba158294e0a4064e0946d0bd916709818694
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
-  skip: True  # [win32]
 
+# Suggests: hexbin (>= 1.27.0), ggplot2 (>= 1.0.1), testthat, covr
 requirements:
   build:
     - r-base
@@ -33,14 +38,19 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('viridisLite')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('viridisLite')"           # [not win]
     - "\"%R%\" -e \"library('viridisLite')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
 
+  # requires:
+    # Put any additional test requirements here.
 
 about:
   home: https://github.com/sjmgarnier/viridisLite
-  license: MIT
+  license: MIT + file LICENSE
   summary: Port of the new 'matplotlib' color maps ('viridis' - the default -, 'magma', 'plasma'
     and 'inferno') to 'R'. 'matplotlib' <http://matplotlib.org/ > is a popular plotting
     library for 'python'. These color maps are designed in such a way that they will
@@ -49,10 +59,35 @@ about:
     with the most common form of color blindness. This is the 'lite' version of the
     more complete 'viridis' package that can be found at <https://cran.r-project.org/package=viridis>.
   license_family: MIT
-  license_file: LICENSE
-
-
 
 extra:
   recipe-maintainers:
     - croth1
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: viridisLite
+# Type: Package
+# Title: Default Color Maps from 'matplotlib' (Lite Version)
+# Version: 0.2.0
+# Authors@R: c( person("Simon", "Garnier", email = "garnier@njit.edu", role = c("aut", "cre")), person("Noam", "Ross", email = "noam.ross@gmail.com", role = c("ctb", "cph")), person("Bob", "Rudis", email = "bob@rud.is", role = c("ctb", "cph")) )
+# Maintainer: Simon Garnier <garnier@njit.edu>
+# Description: Port of the new 'matplotlib' color maps ('viridis' - the default -, 'magma', 'plasma' and 'inferno') to 'R'. 'matplotlib' <http://matplotlib.org/ > is a popular plotting library for 'python'. These color maps are designed in such a way that they will analytically be perfectly perceptually-uniform, both in regular form and also when converted to black-and-white. They are also designed to be perceived by readers with the most common form of color blindness. This is the 'lite' version of the more complete 'viridis' package that can be found at <https://cran.r-project.org/package=viridis>.
+# License: MIT + file LICENSE
+# LazyData: TRUE
+# Encoding: UTF-8
+# Depends: R (>= 2.10)
+# Suggests: hexbin (>= 1.27.0), ggplot2 (>= 1.0.1), testthat, covr
+# URL: https://github.com/sjmgarnier/viridisLite
+# BugReports: https://github.com/sjmgarnier/viridisLite/issues
+# RoxygenNote: 6.0.1
+# NeedsCompilation: no
+# Packaged: 2017-03-23 21:36:52 UTC; simon
+# Author: Simon Garnier [aut, cre], Noam Ross [ctb, cph], Bob Rudis [ctb, cph]
+# Repository: CRAN
+# Date/Publication: 2017-03-24 05:37:14 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-visnetwork-feedstock contains modified content
diff --git a/r-visnetwork-feedstock/recipe/meta.yaml b/r-visnetwork-feedstock/recipe/meta.yaml
index e7310ef..77c912c 100644
--- a/r-visnetwork-feedstock/recipe/meta.yaml
+++ b/r-visnetwork-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '1.0.3' %}
+{% set version = '2.0.1' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,15 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/visNetwork_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/visNetwork/visNetwork_{{ version }}.tar.gz
-  sha256: 210e11a7364a69d158caf98fa39318a55ddcb6a3858d36f8ffdba86cb33e0373
+  sha256: 589791c58f9cae1f581757b26d867c6719efa7c00189bc5b7defabc0ffc976b7
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
+
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: knitr, igraph, rpart, shiny, shinyWidgets, colourpicker
 requirements:
   build:
     - r-base
@@ -28,6 +36,7 @@ requirements:
     - r-htmlwidgets
     - r-jsonlite
     - r-magrittr
+
   run:
     - r-base
     - r-htmltools
@@ -37,12 +46,19 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('visNetwork')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('visNetwork')"           # [not win]
     - "\"%R%\" -e \"library('visNetwork')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/datastorm-open/visNetwork
-  license: MIT
+  license: MIT + file LICENSE
   summary: Provides an R interface to the 'vis.js' JavaScript charting library. It allows an
     interactive visualization of networks.
   license_family: MIT
@@ -51,3 +67,31 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: visNetwork
+# Title: Network Visualization using 'vis.js' Library
+# Version: 2.0.1
+# Date: 2017-07-30
+# Authors@R: c( person(family = "Almende B.V.", role = c("aut", "cph"), comment = "vis.js library in htmlwidgets/lib, http://visjs.org, http://www.almende.com/home"), person("Benoit", "Thieurmel", role = c("aut", "cre"), comment = "R interface", email = "benoit.thieurmel@datastorm.fr"), person(given = "Titouan", family = "Robert",  email = "titouan.robert@datastorm.fr", role = c("aut", "ctb")) )
+# Maintainer: Benoit Thieurmel <benoit.thieurmel@datastorm.fr>
+# Description: Provides an R interface to the 'vis.js' JavaScript charting library. It allows an interactive visualization of networks.
+# BugReports: https://github.com/datastorm-open/visNetwork/issues
+# URL: https://github.com/datastorm-open/visNetwork
+# Depends: R (>= 3.0)
+# Imports: htmlwidgets, htmltools, jsonlite, magrittr, utils, methods, grDevices, stats
+# License: MIT + file LICENSE
+# Suggests: knitr, igraph, rpart, shiny, shinyWidgets, colourpicker
+# VignetteBuilder: knitr
+# RoxygenNote: 6.0.1
+# NeedsCompilation: no
+# Packaged: 2017-07-30 10:36:32 UTC; Datastorm
+# Author: Almende B.V. [aut, cph] (vis.js library in htmlwidgets/lib, http://visjs.org, http://www.almende.com/home), Benoit Thieurmel [aut, cre] (R interface), Titouan Robert [aut, ctb]
+# Repository: CRAN
+# Date/Publication: 2017-07-30 16:09:37 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-weatherdata-feedstock contains modified content
diff --git a/r-weatherdata-feedstock/recipe/meta.yaml b/r-weatherdata-feedstock/recipe/meta.yaml
index 089e817..b45f2c4 100644
--- a/r-weatherdata-feedstock/recipe/meta.yaml
+++ b/r-weatherdata-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '0.4.1' %}
+{% set version = '0.5.0' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,30 +12,46 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/weatherData_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/weatherData/weatherData_{{ version }}.tar.gz
-  sha256: 8c42b735eefcea2c0db1372a4fc199d7702169aa7811a7b82ac12b51785ad6a7
+  sha256: 49ff581367f13eeec1e04b9fafa4149b44660ce690dfa9c7944072034a3d40d0
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: testthat
 requirements:
   build:
     - r-base
+    - r-curl
     - r-plyr
 
   run:
     - r-base
+    - r-curl
     - r-plyr
 
 test:
   commands:
-    - $R -e "library('weatherData')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('weatherData')"           # [not win]
     - "\"%R%\" -e \"library('weatherData')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://ram-n.github.io/weatherData/
   license: GPL
@@ -48,3 +64,32 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: weatherData
+# Type: Package
+# Title: Get Weather Data from the Web
+# Description: Functions that help in fetching weather data from websites. Given a location and a date range, these functions help fetch weather data (temperature, pressure etc.) for any weather related analysis.
+# URL: http://ram-n.github.io/weatherData/
+# BugReports: https://github.com/ram-n/weatherData/issues
+# Version: 0.5.0
+# Date: 2017-05-16
+# Authors@R: person("Ram", "Narasimhan", email = "ramnarasimhan@gmail.com", role = c("aut", "cre"))
+# Suggests: testthat
+# Depends: R (>= 2.10)
+# Imports: curl, plyr, utils
+# LazyData: TRUE
+# License: GPL
+# RoxygenNote: 6.0.1
+# NeedsCompilation: no
+# Packaged: 2017-06-05 20:11:46 UTC; Ram
+# Author: Ram Narasimhan [aut, cre]
+# Maintainer: Ram Narasimhan <ramnarasimhan@gmail.com>
+# Repository: CRAN
+# Date/Publication: 2017-06-05 21:55:33 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-whisker-feedstock contains modified content
diff --git a/r-whisker-feedstock/recipe/meta.yaml b/r-whisker-feedstock/recipe/meta.yaml
index 041ab31..b53fd39 100644
--- a/r-whisker-feedstock/recipe/meta.yaml
+++ b/r-whisker-feedstock/recipe/meta.yaml
@@ -12,18 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/whisker_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/whisker/whisker_{{ version }}.tar.gz
+  sha256: 484836510fcf123a66ddd13cdc8f32eb98e814cad82ed30c0294f55742b08c7c
 
 
-  sha256: 484836510fcf123a66ddd13cdc8f32eb98e814cad82ed30c0294f55742b08c7c
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
-  skip: True  # [win32]
 
+# Suggests: markdown
 requirements:
   build:
     - r-base
@@ -33,10 +38,15 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('whisker')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('whisker')"           # [not win]
     - "\"%R%\" -e \"library('whisker')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
 
+  # requires:
+    # Put any additional test requirements here.
 
 about:
   home: http://github.com/edwindj/whisker
@@ -44,8 +54,31 @@ about:
   summary: logicless templating, reuse templates in many programming languages including R
   license_family: GPL3
 
-
-
 extra:
   recipe-maintainers:
     - croth1
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: whisker
+# Maintainer: Edwin de Jonge <edwindjonge@gmail.com>
+# License: GPL-3
+# Title: {{mustache}} for R, logicless templating
+# Type: Package
+# LazyLoad: yes
+# Author: Edwin de Jonge
+# Description: logicless templating, reuse templates in many programming languages including R
+# Version: 0.3-2
+# URL: http://github.com/edwindj/whisker
+# Date: 2010-12-23
+# Collate: 'whisker.R' 'section.R' 'parseTemplate.R' 'partials.R' 'inverted.R' 'pkg.R' 'iteratelist.R' 'delim.R' 'markdown.R' 'rowsplit.R'
+# Suggests: markdown
+# Packaged: 2013-04-27 22:49:33 UTC; Edwin
+# NeedsCompilation: no
+# Repository: CRAN
+# Date/Publication: 2013-04-28 07:55:20
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-withr-feedstock contains modified content
diff --git a/r-withr-feedstock/recipe/meta.yaml b/r-withr-feedstock/recipe/meta.yaml
index cf4ad28..dca3bfd 100644
--- a/r-withr-feedstock/recipe/meta.yaml
+++ b/r-withr-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '1.0.2' %}
+{% set version = '2.0.0' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,18 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/withr_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/withr/withr_{{ version }}.tar.gz
+  sha256: 36be55ca44cdf30517677f2e6e32d8bde4b581027714fb953a3d8fd19f4f9859
 
 
-  sha256: 2391545020adc4256ee7c2e31c30ff6f688f0b6032e355e1ce8f468cab455f10
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
-  skip: True  # [win32]
 
+# Suggests: testthat
 requirements:
   build:
     - r-base
@@ -33,21 +38,53 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('withr')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('withr')"           # [not win]
     - "\"%R%\" -e \"library('withr')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
 
+  # requires:
+    # Put any additional test requirements here.
 
 about:
-  home: http://github.com/jimhester/withr
+  home: http://github.com/r-lib/withr#readme
   license: GPL (>= 2)
   summary: A set of functions to run code 'with' safely and temporarily modified global state.
     Many of these functions were originally a part of the 'devtools' package, this provides
     a simple package with limited dependencies to provide access to these functions.
   license_family: GPL3
 
-
-
 extra:
   recipe-maintainers:
     - croth1
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Encoding: UTF-8
+# Package: withr
+# Title: Run Code 'With' Temporarily Modified Global State
+# Version: 2.0.0
+# Authors@R: c( person("Jim", "Hester", , "james.f.hester@gmail.com", role = c("aut", "cre")), person("Kirill", "M<U+00FC>ller", , "krlmlr+r@mailbox.org", role = "aut"), person("Kevin", "Ushey", email = "kevinushey@gmail.com", role = c("aut")), person("Hadley", "Wickham", , "hadley@rstudio.com", role = "aut"), person("Winston", "Chang", role = "aut"), person("RStudio", role = "cph"))
+# Description: A set of functions to run code 'with' safely and temporarily modified global state. Many of these functions were originally a part of the 'devtools' package, this provides a simple package with limited dependencies to provide access to these functions.
+# URL: http://github.com/r-lib/withr#readme
+# BugReports: http://github.com/r-lib/withr/issues
+# Depends: R (>= 3.0.2)
+# License: GPL (>= 2)
+# LazyData: true
+# Imports: stats, graphics
+# Suggests: testthat
+# Collate: 'local_.R' 'with_.R' 'collate.R' 'defer.R' 'dir.R' 'env.R' 'libpaths.R' 'locale.R' 'makevars.R' 'options.R' 'par.R' 'path.R' 'seed.R' 'wrap.R' 'sink.R' 'utils.R' 'with.R'
+# RoxygenNote: 6.0.1
+# NeedsCompilation: no
+# Packaged: 2017-07-28 19:07:05 UTC; jhester
+# Author: Jim Hester [aut, cre], Kirill M<U+00FC>ller [aut], Kevin Ushey [aut], Hadley Wickham [aut], Winston Chang [aut], RStudio [cph]
+# Maintainer: Jim Hester <james.f.hester@gmail.com>
+# Repository: CRAN
+# Date/Publication: 2017-07-28 22:56:28 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-xlsx-feedstock contains modified content
diff --git a/r-xlsx-feedstock/recipe/meta.yaml b/r-xlsx-feedstock/recipe/meta.yaml
index 75b6c4d..49521aa 100644
--- a/r-xlsx-feedstock/recipe/meta.yaml
+++ b/r-xlsx-feedstock/recipe/meta.yaml
@@ -14,14 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/xlsx/xlsx_{{ version }}.tar.gz
   sha256: 36727bc330c281dab8a028fab2dde92d032345974b15e3dde920ee16dc6db363
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  # r-rjava and r-xlsxjars not available under win
-  skip: true  # [win]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
+
+
 requirements:
   build:
     - r-base
@@ -35,9 +42,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('xlsx')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('xlsx')"           # [not win]
     - "\"%R%\" -e \"library('xlsx')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: http://code.google.com/p/rexcel/, http://groups.google.com/group/R-package-xlsx
   license: GPL-3
@@ -49,3 +63,28 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: xlsx
+# Type: Package
+# Title: Read, write, format Excel 2007 and Excel 97/2000/XP/2003 files
+# Version: 0.5.7
+# Date: 2014-08-01
+# Depends: rJava, xlsxjars
+# LazyLoad: yes
+# Author: Adrian A. Dragulescu
+# Maintainer: Adrian A. Dragulescu <adrian.dragulescu@gmail.com>
+# Description: Provide R functions to read/write/format Excel 2007 and Excel 97/2000/XP/2003 file formats.
+# License: GPL-3
+# URL: http://code.google.com/p/rexcel/, http://groups.google.com/group/R-package-xlsx
+# BugReports: https://code.google.com/p/rexcel/issues/list
+# Packaged: 2014-08-02 18:06:51 UTC; adrian
+# NeedsCompilation: no
+# Repository: CRAN
+# Date/Publication: 2014-08-02 23:32:07
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-xlsxjars-feedstock contains modified content
diff --git a/r-xlsxjars-feedstock/recipe/meta.yaml b/r-xlsxjars-feedstock/recipe/meta.yaml
index edfdb65..b982d51 100644
--- a/r-xlsxjars-feedstock/recipe/meta.yaml
+++ b/r-xlsxjars-feedstock/recipe/meta.yaml
@@ -14,14 +14,21 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/xlsxjars/xlsxjars_{{ version }}.tar.gz
   sha256: 37c1517f95f8bca6e3514429394d2457b9e62383305eba288416fb53ab2e6ae6
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  # r-rjava is not available on win64
-  skip: true  # [win]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
+
+
 requirements:
   build:
     - r-base
@@ -33,9 +40,16 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('xlsxjars')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('xlsxjars')"           # [not win]
     - "\"%R%\" -e \"library('xlsxjars')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://CRAN.R-project.org/package=xlsxjars
   license: GPL-3
@@ -47,3 +61,28 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: xlsxjars
+# Type: Package
+# Title: Package required POI jars for the xlsx package
+# Version: 0.6.1
+# Date: 2014-08-21
+# Depends: rJava
+# LazyLoad: yes
+# Authors@R: c(person(c("Adrian", "A."), "Dragulescu",  role = c("aut", "cre"), email = "adrian.dragulescu@gmail.com"),  person("The Apache Software Foundation",  role = c("ctb", "cph"),  comment = "Apache POI, Apache XMLBeans", "Apache Commons Codec"),  person("MetaStuff, Ltd.",  role = c("ctb", "cph"), comment = "Dom4j"))
+# Description: The xlsxjars package collects all the external jars required for the xlxs package. This release corresponds to POI 3.10.1.
+# License: GPL-3
+# Copyright: See file COPYRIGHTS
+# NeedsCompilation: no
+# Repository: CRAN
+# Packaged: 2014-08-21 21:18:37 UTC; adrian
+# Author: Adrian A. Dragulescu [aut, cre], The Apache Software Foundation Apache Commons Codec [ctb, cph] (Apache POI, Apache XMLBeans), MetaStuff, Ltd. [ctb, cph] (Dom4j)
+# Maintainer: Adrian A. Dragulescu <adrian.dragulescu@gmail.com>
+# Date/Publication: 2014-08-22 07:19:16
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-xml-feedstock contains modified content
diff --git a/r-xml-feedstock/recipe/meta.yaml b/r-xml-feedstock/recipe/meta.yaml
index 9e71f6c..f893173 100644
--- a/r-xml-feedstock/recipe/meta.yaml
+++ b/r-xml-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '3.98-1.6' %}
+{% set version = '3.98-1.9' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,48 +12,85 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/XML_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/XML/XML_{{ version }}.tar.gz
+  sha256: a3b70169cb2fbd8d61a41ff222d27922829864807e9ecad373f55ba0df6cf3c3
 
 
-  sha256: dff2a9891f9597186e77843cdf384b1d66b3372df5e45ca31526cc78dde9bdaa
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
-  skip: True  # [win32]
 
+# Suggests: bitops, RCurl
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
-    - libxml2 2.9.*
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}autoconf
+    - {{posix}}automake
+    - {{posix}}pkg-config
+    - {{posix}}make
 
   run:
     - r-base
-    - libxml2 2.9.*
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('XML')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('XML')"           # [not win]
     - "\"%R%\" -e \"library('XML')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
 
+  # requires:
+    # Put any additional test requirements here.
 
 about:
   home: http://www.omegahat.net/RSXML
-  license: BSD_2_clause
+  license: BSD_2_clause + file LICENSE
   summary: Many approaches for both reading and creating XML (and HTML) documents (including
     DTDs), both local and accessible via HTTP or FTP.  Also offers access to an 'XPath'
     "interpreter".
   license_family: BSD
-  license_file: LICENSE
-
-
 
 extra:
   recipe-maintainers:
     - croth1
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: XML
+# Version: 3.98-1.9
+# Author: Duncan Temple Lang and the CRAN Team
+# Maintainer: ORPHANED
+# Title: Tools for Parsing and Generating XML Within R and S-Plus
+# Depends: R (>= 2.13.0), methods, utils
+# Suggests: bitops, RCurl
+# SystemRequirements: libxml2 (>= 2.6.3)
+# Description: Many approaches for both reading and creating XML (and HTML) documents (including DTDs), both local and accessible via HTTP or FTP.  Also offers access to an 'XPath' "interpreter".
+# Note: In version 2.4.0 of this package, a new approach to garbage collection has been implemented and it is experimental. You can disable it via the configuration option --enable-nodegc=no. However, you are encouraged to use this and report problems as the results are beneficial and being able to produce any errors should they occur will be very helpful.
+# URL: http://www.omegahat.net/RSXML
+# License: BSD_2_clause + file LICENSE
+# Collate: AAA.R DTD.R DTDClasses.R DTDRef.R SAXMethods.S XMLClasses.R applyDOM.R assignChild.R catalog.R createNode.R dynSupports.R error.R flatTree.R nodeAccessors.R parseDTD.R schema.S summary.R tangle.R toString.S tree.R version.R xmlErrorEnums.R xmlEventHandler.R xmlEventParse.R xmlHandler.R xmlInternalSource.R xmlOutputDOM.R xmlNodes.R xmlOutputBuffer.R xmlTree.R xmlTreeParse.R htmlParse.R hashTree.R zzz.R supports.R parser.R libxmlFeatures.R xmlString.R saveXML.R namespaces.R readHTMLTable.R reflection.R xmlToDataFrame.R bitList.R compare.R encoding.R fixNS.R xmlRoot.R serialize.R xmlMemoryMgmt.R keyValueDB.R solrDocs.R XMLRErrorInfo.R xincludes.R namespaceHandlers.R tangle1.R htmlLinks.R htmlLists.R getDependencies.R getRelativeURL.R xmlIncludes.R simplifyPath.R
+# NeedsCompilation: yes
+# Packaged: 2017-06-19 08:26:01 UTC; ripley
+# X-CRAN-Original-Maintainer: Duncan Temple Lang <duncan@r-project.org>
+# X-CRAN-Comment: Orphaned on 2017-06-19: all updates have been by the CRAN team for some years.
+# Repository: CRAN
+# Date/Publication: 2017-06-19 12:43:32 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-xml2-feedstock contains modified content
diff --git a/r-xml2-feedstock/recipe/meta.yaml b/r-xml2-feedstock/recipe/meta.yaml
index 9d8cc54..b7a4909 100644
--- a/r-xml2-feedstock/recipe/meta.yaml
+++ b/r-xml2-feedstock/recipe/meta.yaml
@@ -14,33 +14,52 @@ source:
     - https://cran.r-project.org/src/contrib/Archive/xml2/xml2_{{ version }}.tar.gz
   sha256: 00f3e3b66b76760c19da5f6dddc98e6f30de36a96b211e59e1a3f4ff58763116
 
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
+
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: testthat, curl, covr, knitr, rmarkdown, magrittr, httr
 requirements:
   build:
     - r-base
+    - {{ compiler('cxx') }}      # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}autoconf
+    - {{posix}}automake
+    - {{posix}}pkg-config
+    - {{posix}}make
     - r-bh
-    - r-rcpp >=0.11.4.6
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - r-rcpp
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-bh
-    - r-rcpp >=0.11.4.6
+    - r-rcpp
 
 test:
   commands:
-    - $R -e "library('xml2')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('xml2')"           # [not win]
     - "\"%R%\" -e \"library('xml2')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
   home: https://github.com/hadley/xml2/
   license: GPL (>= 2)
@@ -52,3 +71,33 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: xml2
+# Version: 1.1.1
+# Title: Parse XML
+# Description: Work with XML files using a simple, consistent interface. Built on top of the 'libxml2' C library.
+# Authors@R: c( person("Hadley", "Wickham", , "hadley@rstudio.com", "aut"), person("James", "Hester", , "james.hester@rstudio.com", c("aut", "cre")), person("Jeroen", "Ooms", role = "aut"), person("RStudio", role = "cph"), person("R Foundation", role = "ctb", comment = "Copy of R-project homepage cached as example") )
+# URL: https://github.com/hadley/xml2/
+# BugReports: https://github.com/hadley/xml2/issues/
+# Depends: R (>= 3.1.0)
+# Imports: Rcpp
+# LinkingTo: Rcpp (>= 0.11.4.6), BH
+# Suggests: testthat, curl, covr, knitr, rmarkdown, magrittr, httr
+# SystemRequirements: libxml2: libxml2-dev (deb), libxml2-devel (rpm)
+# License: GPL (>= 2)
+# RoxygenNote: 5.0.1.9000
+# VignetteBuilder: knitr
+# Collate: 'RcppExports.R' 'as_list.R' 'xml_parse.R' 'as_xml_document.R' 'classes.R' 'paths.R' 'utils.R' 'xml_attr.R' 'xml_children.R' 'xml_find.R' 'xml_modify.R' 'xml_name.R' 'xml_namespaces.R' 'xml_path.R' 'xml_schema.R' 'xml_serialize.R' 'xml_structure.R' 'xml_text.R' 'xml_type.R' 'xml_url.R' 'xml_write.R' 'zzz.R'
+# NeedsCompilation: yes
+# Packaged: 2017-01-23 18:59:10 UTC; jhester
+# Author: Hadley Wickham [aut], James Hester [aut, cre], Jeroen Ooms [aut], RStudio [cph], R Foundation [ctb] (Copy of R-project homepage cached as example)
+# Maintainer: James Hester <james.hester@rstudio.com>
+# Repository: CRAN
+# Date/Publication: 2017-01-24 08:12:30
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-xtable-feedstock contains modified content
diff --git a/r-xtable-feedstock/recipe/meta.yaml b/r-xtable-feedstock/recipe/meta.yaml
index 08ff911..304ac74 100644
--- a/r-xtable-feedstock/recipe/meta.yaml
+++ b/r-xtable-feedstock/recipe/meta.yaml
@@ -12,18 +12,23 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/xtable_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/xtable/xtable_{{ version }}.tar.gz
+  sha256: 1623a1cde2e130fedb46f98840c3a882f1cbb167b292ef2bd86d70baefc4280d
 
 
-  sha256: 1623a1cde2e130fedb46f98840c3a882f1cbb167b292ef2bd86d70baefc4280d
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
-  skip: True  # [win32]
 
+# Suggests: knitr, lsmeans, spdep, splm, sphet, plm, zoo, survival
 requirements:
   build:
     - r-base
@@ -33,10 +38,15 @@ requirements:
 
 test:
   commands:
-    - $R -e "library('xtable')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('xtable')"           # [not win]
     - "\"%R%\" -e \"library('xtable')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
 
+  # requires:
+    # Put any additional test requirements here.
 
 about:
   home: http://xtable.r-forge.r-project.org/
@@ -44,8 +54,31 @@ about:
   summary: Coerce data to LaTeX and HTML tables.
   license_family: GPL3
 
-
-
 extra:
   recipe-maintainers:
     - croth1
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: xtable
+# Version: 1.8-2
+# Date: 2016-01-08
+# Title: Export Tables to LaTeX or HTML
+# Author: David B. Dahl <dahl@stat.byu.edu>
+# Maintainer: David Scott <d.scott@auckland.ac.nz>
+# Imports: stats, utils
+# Suggests: knitr, lsmeans, spdep, splm, sphet, plm, zoo, survival
+# VignetteBuilder: knitr
+# Description: Coerce data to LaTeX and HTML tables.
+# URL: http://xtable.r-forge.r-project.org/
+# Depends: R (>= 2.10.0)
+# License: GPL (>= 2)
+# Repository: CRAN
+# NeedsCompilation: no
+# Packaged: 2016-02-02 02:58:02 UTC; dsco036
+# Date/Publication: 2016-02-05 19:22:18
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-xts-feedstock contains modified content
diff --git a/r-xts-feedstock/recipe/meta.yaml b/r-xts-feedstock/recipe/meta.yaml
index 7d0266d..0d3460f 100644
--- a/r-xts-feedstock/recipe/meta.yaml
+++ b/r-xts-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '0.9-7' %}
+{% set version = '0.10-0' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,36 +12,52 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/xts_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/xts/xts_{{ version }}.tar.gz
-  sha256: f11f7cb98f4b92b7f6632a2151257914130880c267736ef5a264b5dc2dfb7098
+  sha256: 049b4d54a265341dfb5b3ba140f401b12f89a4fd91af7fb72b00b80587eff4f0
+
+
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: true  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: timeSeries, timeDate, tseries, chron, fts, tis, RUnit
 requirements:
   build:
     - r-base
-    - r-zoo >=1.7_10
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{ compiler('c') }}        # [not win]
+    - {{ compiler('fortran') }}  # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
+    - r-zoo >=1.7_12
 
   run:
     - r-base
-    - r-zoo >=1.7_10
-    - libgcc  # [not win]
+    - {{native}}gcc-libs         # [win]
+    - r-zoo >=1.7_12
 
 test:
   commands:
-    - $R -e "library('xts')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('xts')"           # [not win]
     - "\"%R%\" -e \"library('xts')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
+
+  # requires:
+    # Put any additional test requirements here.
+
 about:
-  home: http://r-forge.r-project.org/projects/xts/
+  home: https://github.com/joshuaulrich/xts
   license: GPL (>= 2)
   summary: Provide for uniform handling of R's different time-based data classes by extending
     zoo, maximizing native format information preservation and allowing for user level
@@ -52,3 +68,32 @@ extra:
   recipe-maintainers:
     - johanneskoester
     - bgruening
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: xts
+# Type: Package
+# Title: eXtensible Time Series
+# Version: 0.10-0
+# Date: 2017-07-03
+# Authors@R: c( person(given=c("Jeffrey","A."), family="Ryan", role=c("aut","cph")), person(given=c("Joshua","M."), family="Ulrich", role=c("cre","aut"), email="josh.m.ulrich@gmail.com"), person(given="Ross", family="Bennett", role="ctb") )
+# Depends: zoo (>= 1.7-12)
+# Imports: methods
+# LinkingTo: zoo
+# Suggests: timeSeries, timeDate, tseries, chron, fts, tis, RUnit
+# LazyLoad: yes
+# Description: Provide for uniform handling of R's different time-based data classes by extending zoo, maximizing native format information preservation and allowing for user level customization and extension, while simplifying cross-class interoperability.
+# License: GPL (>= 2)
+# URL: https://github.com/joshuaulrich/xts
+# BugReports: https://github.com/joshuaulrich/xts/issues
+# NeedsCompilation: yes
+# Packaged: 2017-07-04 20:22:54 UTC; josh
+# Author: Jeffrey A. Ryan [aut, cph], Joshua M. Ulrich [cre, aut], Ross Bennett [ctb]
+# Maintainer: Joshua M. Ulrich <josh.m.ulrich@gmail.com>
+# Repository: CRAN
+# Date/Publication: 2017-07-07 07:00:40 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-yaml-feedstock contains modified content
diff --git a/r-yaml-feedstock/recipe/meta.yaml b/r-yaml-feedstock/recipe/meta.yaml
index 9cf8f99..599ec83 100644
--- a/r-yaml-feedstock/recipe/meta.yaml
+++ b/r-yaml-feedstock/recipe/meta.yaml
@@ -12,45 +12,76 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/yaml_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/yaml/yaml_{{ version }}.tar.gz
+  sha256: 41a559846f6d44cc2dbcb3fc0becbc50d2766d3dc2aad7cfb97c1f9759ec0875
 
 
-  sha256: 41a559846f6d44cc2dbcb3fc0becbc50d2766d3dc2aad7cfb97c1f9759ec0875
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
-  skip: True  # [win32]
 
+# Suggests: testthat
 requirements:
   build:
     - r-base
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
 
 test:
   commands:
-    - $R -e "library('yaml')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('yaml')"           # [not win]
     - "\"%R%\" -e \"library('yaml')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
 
+  # requires:
+    # Put any additional test requirements here.
 
 about:
   home: https://CRAN.R-project.org/package=yaml
-  license: BSD_3_clause
+  license: BSD_3_clause + file LICENSE
   summary: Implements the 'libyaml' 'YAML' 1.1 parser and emitter (<http://pyyaml.org/wiki/LibYAML>)
     for R.
   license_family: BSD
-  license_file: LICENSE
-
-
 
 extra:
   recipe-maintainers:
     - croth1
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: yaml
+# Type: Package
+# Title: Methods to Convert R Data to YAML and Back
+# Version: 2.1.14
+# Suggests: testthat
+# Date: 2016-11-12
+# Author: Jeremy Stephens <jeremy.stephens@vanderbilt.edu>
+# Maintainer: Jeremy Stephens <jeremy.stephens@vanderbilt.edu>
+# License: BSD_3_clause + file LICENSE
+# Description: Implements the 'libyaml' 'YAML' 1.1 parser and emitter (<http://pyyaml.org/wiki/LibYAML>) for R.
+# NeedsCompilation: yes
+# Packaged: 2016-11-12 15:41:59 UTC; viking
+# Repository: CRAN
+# Date/Publication: 2016-11-12 19:55:17
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule r-zoo-feedstock contains modified content
diff --git a/r-zoo-feedstock/recipe/meta.yaml b/r-zoo-feedstock/recipe/meta.yaml
index c2bc1f2..6868687 100644
--- a/r-zoo-feedstock/recipe/meta.yaml
+++ b/r-zoo-feedstock/recipe/meta.yaml
@@ -1,4 +1,4 @@
-{% set version = '1.7-14' %}
+{% set version = '1.8-0' %}
 
 {% set posix = 'm2-' if win else '' %}
 {% set native = 'm2w64-' if win else '' %}
@@ -12,36 +12,48 @@ source:
   url:
     - https://cran.r-project.org/src/contrib/zoo_{{ version }}.tar.gz
     - https://cran.r-project.org/src/contrib/Archive/zoo/zoo_{{ version }}.tar.gz
+  sha256: 09922c46f15de378bd955dfe526e028aef9632a9f79dca1550dc4591faa21357
 
 
-  sha256: 4858675fed056a4329c4998517cc944db386447483390bd342de719e0509f598
+  # patches:
+   # List any patch files here
+   # - fix.patch
 
 build:
+  # If this is a new build for the same version, increment the build number.
   number: 0
-  skip: True  # [win32]
 
+  # This is required to make R link correctly on Linux.
   rpaths:
     - lib/R/lib/
     - lib/
 
+# Suggests: coda, chron, DAAG, fts, ggplot2, mondate, scales, strucchange, timeDate, timeSeries, tis, tseries, xts
 requirements:
   build:
     - r-base
+    - {{ compiler('c') }}        # [not win]
+    - {{native}}toolchain        # [win]
+    - {{posix}}filesystem        # [win]
+    - {{posix}}make
     - r-lattice >=0.20_27
-    - posix                # [win]
-    - {{native}}toolchain  # [win]
-    - gcc                  # [not win]
 
   run:
     - r-base
+    - {{native}}gcc-libs         # [win]
     - r-lattice >=0.20_27
 
 test:
   commands:
-    - $R -e "library('zoo')"  # [not win]
+    # You can put additional test commands to be run here.
+    - $R -e "library('zoo')"           # [not win]
     - "\"%R%\" -e \"library('zoo')\""  # [win]
 
+  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
+  # in the recipe that will be run at test time.
 
+  # requires:
+    # Put any additional test requirements here.
 
 about:
   home: http://zoo.R-Forge.R-project.org/
@@ -55,3 +67,28 @@ about:
 extra:
   recipe-maintainers:
     - croth1
+    - mingwandroid
+
+# The original CRAN metadata for this package was:
+
+# Package: zoo
+# Version: 1.8-0
+# Date: 2017-04-13
+# Title: S3 Infrastructure for Regular and Irregular Time Series (Z's Ordered Observations)
+# Authors@R: c(person(given = "Achim", family = "Zeileis", role = c("aut", "cre"), email = "Achim.Zeileis@R-project.org"), person(given = "Gabor", family = "Grothendieck", role = "aut", email = "ggrothendieck@gmail.com"), person(given = c("Jeffrey", "A."), family = "Ryan", role = "aut", email = "jeff.a.ryan@gmail.com"), person(given = c("Joshua", "M."), family = "Ulrich", role = "ctb", email = "josh.m.ulrich@gmail.com"), person(given = "Felix", family = "Andrews", role = "ctb", email = "felix@nfrac.org"))
+# Description: An S3 class with methods for totally ordered indexed observations. It is particularly aimed at irregular time series of numeric vectors/matrices and factors. zoo's key design goals are independence of a particular index/date/time class and consistency with ts and base R by providing methods to extend standard generics.
+# Depends: R (>= 2.10.0), stats
+# Suggests: coda, chron, DAAG, fts, ggplot2, mondate, scales, strucchange, timeDate, timeSeries, tis, tseries, xts
+# Imports: utils, graphics, grDevices, lattice (>= 0.20-27)
+# License: GPL-2 | GPL-3
+# URL: http://zoo.R-Forge.R-project.org/
+# NeedsCompilation: yes
+# Packaged: 2017-04-12 22:23:52 UTC; zeileis
+# Author: Achim Zeileis [aut, cre], Gabor Grothendieck [aut], Jeffrey A. Ryan [aut], Joshua M. Ulrich [ctb], Felix Andrews [ctb]
+# Maintainer: Achim Zeileis <Achim.Zeileis@R-project.org>
+# Repository: CRAN
+# Date/Publication: 2017-04-12 23:15:13 UTC
+
+# See
+# http://docs.continuum.io/conda/build.html for
+# more information about meta.yaml
Submodule rpy2-feedstock c066fdf..56d3947:
diff --git a/rpy2-feedstock/recipe/0005-Win32__Hack_sysconfig_so_that__CC__is_set_to__gcc_.patch b/rpy2-feedstock/recipe/0005-Win32__Hack_sysconfig_so_that__CC__is_set_to__gcc_.patch
index 8e44454..0e891de 100644
--- a/rpy2-feedstock/recipe/0005-Win32__Hack_sysconfig_so_that__CC__is_set_to__gcc_.patch
+++ b/rpy2-feedstock/recipe/0005-Win32__Hack_sysconfig_so_that__CC__is_set_to__gcc_.patch
@@ -17,14 +17,14 @@ Otherwise distutils/unixccompiler.py throws an exception at:
 diff --git a/setup.py b/setup.py
 --- a/setup.py
 +++ b/setup.py
-@@ -6,7 +6,9 @@
- 
+@@ -7,6 +7,9 @@
  from setuptools import setup, Extension
  from setuptools.command.build_ext import build_ext as _build_ext
--
+ 
 +if sys.platform == "win32":
 +    from distutils.sysconfig import _config_vars
 +    _config_vars['CC'] = 'gcc'
- pack_name = 'rpy2'
- pack_version = __import__('rpy').__version__
  
+ if sys.version_info[0] < 3:
+     print("rpy2 is no longer supporting Python < 3. Consider using an older rpy2 release when using an older Python release.")
+
diff --git a/rpy2-feedstock/recipe/meta.yaml b/rpy2-feedstock/recipe/meta.yaml
index eacf787..ffc370e 100644
--- a/rpy2-feedstock/recipe/meta.yaml
+++ b/rpy2-feedstock/recipe/meta.yaml
@@ -1,5 +1,5 @@
-{% set version = '2.8.6' %}
-{% set versionu = '2_8_6' %}
+{% set version = '2.9.0' %}
+{% set versionu = '2_9_0' %}
 {% set pfx = 'm2w64-' if win else '' %}
 
 package:
@@ -9,7 +9,7 @@ package:
 source:
   fn: rpy2-{{version}}.tar.gz
   url: https://pypi.io/packages/source/r/rpy2/rpy2-{{ version }}.tar.gz
-  sha256: 09c63bc934d666f4ec16e74caf52eb516e035557407a93591e8da694a1718ca6
+  sha256: d1e6c109093965f717293b6c3a8d22d9c172c3721b6a8360c84572dcb284112f
   # Regenerate the patches using the following command:
   # mercurial_program export -r 3066:3072 -o 000%n-%m.patch -g
   patches:
@@ -23,12 +23,10 @@ source:
     - 0007-Turn_off_some_pedantic_warnings__unused_variables_and_functions_.patch
     - 0008-Win32__Change_R_USER_check_for_HOMEDIR_to_HOMEDRIVE.patch
     - 0009-Win32__Use_get_R_HOME___if_R_HOME_is_not_set_.patch
-    # Patches from upstream:
-    - 0110-52b73245e093d4cf32dfeb7c77e9f4da954316dc.patch
-    - 0111-c090410d52149121083ce87387c93ea91514c39f.patch
 
 build:
-  number: 1
+  number: 0
+  skip: True  # [py2k]
 
 requirements:
   build:
@@ -46,15 +44,18 @@ requirements:
   run:
     - python
     - r-base
+    - r-rsqlite
+    - r-dbplyr
     - singledispatch       # [not (py>=34)]
     - six
+    - jinja2
     - readline             # [not win]
     - {{pfx}}gcc-libs      # [win]
 
 test:
   requires:
     - r-survival
-    - r-ggplot2 2.2.0
+    - r-ggplot2 2.2.1
     - r-dplyr
     - ipython
     - numpy
@@ -75,7 +76,9 @@ test:
     - rpy2.robjects.lib.tests
     - rpy2.robjects.tests
   commands:
-    - python -m rpy2.tests
+    # Cannot test on linux because of:
+    # https://bitbucket.org/rpy2/rpy2/issues/418/cannot-get-dataframe-with-dates-out-of-r#comment-40400178
+    - python -m rpy2.tests           # [not linux]
 
 about:
   home: http://rpy.sourceforge.net
