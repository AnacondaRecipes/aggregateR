{% set version = '1.1.1' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-trimr
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/trimr_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/trimr/trimr_{{ version }}.tar.gz
  sha256: 7bebdd9cf48e17d284ebb36d1647e2b0266d2bfcf01813f6260fad631e49e18b

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: knitr, rmarkdown
requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-dplyr

  run:
    - r-base
    - r-dplyr

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('trimr')"           # [not win]
    - "\"%R%\" -e \"library('trimr')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://github.com/JimGrange/trimr
  license: GPL-3
  summary: Provides various commonly-used response time trimming methods, including the recursive
    / moving-criterion methods reported by Van Selst and Jolicoeur (1994). By passing
    trimming functions raw data files, the package will return trimmed data ready for
    inferential testing.
  license_family: GPL3
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-3'

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: trimr
# Version: 1.1.1
# Date: 2022-05-03
# Authors@R: c( person("James", "Grange", email = "grange.jim@gmail.com", role = c("cre", "aut")), person("Ed", "Berry", role = "ctb"))
# Title: An Implementation of Common Response Time Trimming Methods
# Description: Provides various commonly-used response time trimming methods, including the recursive / moving-criterion methods reported by Van Selst and Jolicoeur (1994). By passing trimming functions raw data files, the package will return trimmed data ready for inferential testing.
# Depends: R (>= 4.0)
# Imports: stats, dplyr
# License: GPL-3
# LazyData: true
# URL: https://github.com/JimGrange/trimr
# BugReports: https://github.com/JimGrange/trimr/issues
# Suggests: knitr, rmarkdown
# VignetteBuilder: knitr
# RoxygenNote: 7.1.1
# NeedsCompilation: no
# Packaged: 2022-05-03 15:30:09 UTC; jimgrange
# Author: James Grange [cre, aut], Ed Berry [ctb]
# Maintainer: James Grange <grange.jim@gmail.com>
# Repository: CRAN
# Date/Publication: 2022-05-05 02:20:02 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
