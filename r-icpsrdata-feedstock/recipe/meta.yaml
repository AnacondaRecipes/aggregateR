{% set version = '0.6.1' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-icpsrdata
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/icpsrdata_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/icpsrdata/icpsrdata_{{ version }}.tar.gz
  sha256: 363a095fc21b4c7fa0a363da860f134a30745b52f029b8881ea6510ba4378955

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: knitr, rmarkdown
requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-httr
    - r-purrr
    - r-rvest

  run:
    - r-base
    - r-httr
    - r-purrr
    - r-rvest

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('icpsrdata')"           # [not win]
    - "\"%R%\" -e \"library('icpsrdata')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://github.com/fsolt/icpsrdata, https://fsolt.org/icpsrdata/
  license: MIT
  summary: Reproducible, programmatic retrieval of datasets from the Inter-university Consortium
    for Political and Social Research archive.
  license_family: MIT
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/MIT'
    - LICENSE

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: icpsrdata
# Type: Package
# Title: Reproducible Data Retrieval from the ICPSR Archive
# Version: 0.6.1
# Date: 2023-07-04
# Authors@R: c( person("Frederick", "Solt", email = "frederick-solt@uiowa.edu", role = c("aut", "cre", "cph")), person("Kellen", "Gracey", email = "kellen-gracey@uiowa.edu", role = c("aut")), person("Eric", "Persson", email = "expersso5@gmail.com", role = c("ctb")), person("Joanna", "Pepin", email = "jpepin@prc.utexas.edu", role = c("ctb")), person("J.", "Hathaway", email = "hathawayj@byui.edu", role = c("ctb")), person("Jennifer", "Lin", email = "jenniferlin2025@u.northwestern.edu", role = c("ctb")), person("John", "Bulloch", email = "john@johnbullock.org", role = c("ctb")))
# Description: Reproducible, programmatic retrieval of datasets from the Inter-university Consortium for Political and Social Research archive.
# License: MIT + file LICENSE
# Suggests: knitr, rmarkdown
# VignetteBuilder: knitr
# Imports: rvest, purrr, httr, utils
# Encoding: UTF-8
# RoxygenNote: 7.2.3
# URL: https://github.com/fsolt/icpsrdata, https://fsolt.org/icpsrdata/
# BugReports: https://github.com/fsolt/icpsrdata/issues
# NeedsCompilation: no
# Packaged: 2023-07-04 15:12:54 UTC; fredsolt
# Author: Frederick Solt [aut, cre, cph], Kellen Gracey [aut], Eric Persson [ctb], Joanna Pepin [ctb], J. Hathaway [ctb], Jennifer Lin [ctb], John Bulloch [ctb]
# Maintainer: Frederick Solt <frederick-solt@uiowa.edu>
# Repository: CRAN
# Date/Publication: 2023-07-04 15:30:02 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
