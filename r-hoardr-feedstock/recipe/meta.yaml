{% set version = '0.5.3' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-hoardr
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/hoardr_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/hoardr/hoardr_{{ version }}.tar.gz
  sha256: b9e4d1350e1fde7db922e55128306e3768ee46ff9532f05c96543dcae383647c

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: testthat, knitr, rmarkdown
requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-r6
    - r-digest
    - r-rappdirs

  run:
    - r-base
    - r-r6
    - r-digest
    - r-rappdirs

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('hoardr')"           # [not win]
    - "\"%R%\" -e \"library('hoardr')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://docs.ropensci.org/hoardr/, https://github.com/ropensci/hoardr
  license: MIT
  summary: Suite of tools for managing cached files, targeting use in other R packages. Uses
    'rappdirs' for cross-platform paths. Provides utilities to manage cache directories,
    including targeting files by path or by key; cached directories can be compressed
    and uncompressed easily to save disk space.
  license_family: MIT
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/MIT'
    - LICENSE

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: hoardr
# Type: Package
# Title: Manage Cached Files
# Description: Suite of tools for managing cached files, targeting use in other R packages. Uses 'rappdirs' for cross-platform paths. Provides utilities to manage cache directories, including targeting files by path or by key; cached directories can be compressed and uncompressed easily to save disk space.
# Version: 0.5.3
# Authors@R: c(person("Scott", "Chamberlain", role = "aut", comment = c(ORCID="0000-0003-2542-2202")), person("Tamas", "Stirling", role = c("ctb", "cre"), email = "stirling.tamas@gmail.com"))
# License: MIT + file LICENSE
# URL: https://docs.ropensci.org/hoardr/, https://github.com/ropensci/hoardr
# BugReports: https://github.com/ropensci/hoardr/issues
# VignetteBuilder: knitr
# Encoding: UTF-8
# Imports: R6, rappdirs, digest
# Suggests: testthat, knitr, rmarkdown
# RoxygenNote: 7.2.3
# X-schema.org-applicationCategory: Data
# X-schema.org-keywords: caching, data, files, xml, pdf
# X-schema.org-isPartOf: https://ropensci.org
# NeedsCompilation: no
# Packaged: 2023-01-26 09:34:04 UTC; tamas
# Author: Scott Chamberlain [aut] (<https://orcid.org/0000-0003-2542-2202>), Tamas Stirling [ctb, cre]
# Maintainer: Tamas Stirling <stirling.tamas@gmail.com>
# Repository: CRAN
# Date/Publication: 2023-01-26 16:00:02 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
