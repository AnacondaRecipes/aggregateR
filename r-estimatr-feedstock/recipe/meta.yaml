{% set version = '1.0.0' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-estimatr
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/estimatr_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/estimatr/estimatr_{{ version }}.tar.gz
  sha256: a9c8ce7a7f77d4e030052cced599e41376a91de8179e12dd79a5bf4eed91a9df

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: fabricatr (>= 0.10.0), randomizr (>= 0.20.0), AER, clubSandwich, emmeans (>= 1.4), estimability, margins, modelsummary, prediction, RcppEigen, sandwich, stargazer, testthat, car
requirements:
  build:
    - {{ compiler('c') }}              # [not win]
    - {{ compiler('m2w64_c') }}        # [win]
    - {{ compiler('cxx') }}            # [not win]
    - {{ compiler('m2w64_cxx') }}      # [win]
    - {{ posix }}filesystem        # [win]
    - {{ posix }}make
    - {{ posix }}sed               # [win]
    - {{ posix }}coreutils         # [win]
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-formula
    - r-rcpp >=0.12.16
    - r-rcppeigen
    - r-generics
    - r-rlang >=0.2.0

  run:
    - r-base
    - {{native}}gcc-libs         # [win]
    - r-formula
    - r-rcpp >=0.12.16
    - r-rcppeigen
    - r-generics
    - r-rlang >=0.2.0

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('estimatr')"           # [not win]
    - "\"%R%\" -e \"library('estimatr')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://declaredesign.org/r/estimatr/, https://github.com/DeclareDesign/estimatr
  license: MIT
  summary: Fast procedures for small set of commonly-used, design-appropriate estimators with
    robust standard errors and confidence intervals. Includes estimators for linear
    regression, instrumental variables regression, difference-in-means, Horvitz-Thompson
    estimation, and regression improving precision of experimental estimates by interacting
    treatment with centered pre-treatment covariates introduced by Lin (2013) <doi:10.1214/12-AOAS583>.
  license_family: MIT
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/MIT'
    - LICENSE

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: estimatr
# Type: Package
# Title: Fast Estimators for Design-Based Inference
# Version: 1.0.0
# Authors@R: c(person("Graeme", "Blair", email = "graeme.blair@gmail.com", role = c("aut", "cre")), person("Jasper", "Cooper", email = "jjc2247@columbia.edu", role = c("aut")), person("Alexander", "Coppock", email = "alex.coppock@yale.edu", role = c("aut")), person("Macartan", "Humphreys", email = "macartan@gmail.com", role = c("aut")), person("Luke", "Sonnet", email = "luke.sonnet@gmail.com", role = c("aut")), person("Neal", "Fultz", email = "nfultz@gmail.com", role = c("ctb")), person("Lily", "Medina", email = "lilymiru@gmail.com", role = c("ctb")), person("Russell", "Lenth", email = "russell-lenth@uiowa.edu", role = c("ctb")))
# Description: Fast procedures for small set of commonly-used, design-appropriate estimators with robust standard errors and confidence intervals. Includes estimators for linear regression, instrumental variables regression, difference-in-means, Horvitz-Thompson estimation, and regression improving precision of experimental estimates by interacting treatment with centered pre-treatment covariates introduced by Lin (2013) <doi:10.1214/12-AOAS583>.
# URL: https://declaredesign.org/r/estimatr/, https://github.com/DeclareDesign/estimatr
# BugReports: https://github.com/DeclareDesign/estimatr/issues
# License: MIT + file LICENSE
# Depends: R (>= 3.5.0)
# Imports: Formula, generics, methods, Rcpp (>= 0.12.16), rlang (>= 0.2.0)
# LinkingTo: Rcpp, RcppEigen
# Encoding: UTF-8
# RoxygenNote: 7.2.0
# LazyData: true
# Suggests: fabricatr (>= 0.10.0), randomizr (>= 0.20.0), AER, clubSandwich, emmeans (>= 1.4), estimability, margins, modelsummary, prediction, RcppEigen, sandwich, stargazer, testthat, car
# Enhances: texreg
# NeedsCompilation: yes
# Packaged: 2022-07-04 00:32:13 UTC; graeme
# Author: Graeme Blair [aut, cre], Jasper Cooper [aut], Alexander Coppock [aut], Macartan Humphreys [aut], Luke Sonnet [aut], Neal Fultz [ctb], Lily Medina [ctb], Russell Lenth [ctb]
# Maintainer: Graeme Blair <graeme.blair@gmail.com>
# Repository: CRAN
# Date/Publication: 2022-07-04 12:40:05 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
