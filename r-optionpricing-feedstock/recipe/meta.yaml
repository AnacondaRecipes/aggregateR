{% set version = '0.1.2' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-optionpricing
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/OptionPricing_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/OptionPricing/OptionPricing_{{ version }}.tar.gz
  sha256: 4f5205b7b718f15fc0964d48d7498fba2c26fbb43b098cdb731adbf571c29730

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base

  run:
    - r-base

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('OptionPricing')"           # [not win]
    - "\"%R%\" -e \"library('OptionPricing')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://CRAN.R-project.org/package=OptionPricing
  license: GPL-2 | GPL-3
  summary: Efficient Monte Carlo Algorithms for the price and the sensitivities of Asian and
    European Options under Geometric Brownian Motion.
  license_family: GPL3
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-2'
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-3'

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: OptionPricing
# Type: Package
# Title: Option Pricing with Efficient Simulation Algorithms
# Version: 0.1.2
# Authors@R: c(person("Wolfgang", "Hormann", email = "hormanngw@yahoo.com", role = c("aut", "cre")), person("Kemal","Dingec",role = c("aut")))
# Maintainer: Wolfgang Hormann <hormanngw@yahoo.com>
# Description: Efficient Monte Carlo Algorithms for the price and the sensitivities of Asian and European Options under Geometric Brownian Motion.
# License: GPL-2 | GPL-3
# Copyright: Wolfgang Hormann
# Packaged: 2023-09-16 05:49:57 UTC; hormannw
# NeedsCompilation: no
# Repository: CRAN
# Date/Publication: 2023-09-16 08:40:08 UTC
# Author: Wolfgang Hormann [aut, cre], Kemal Dingec [aut]

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
