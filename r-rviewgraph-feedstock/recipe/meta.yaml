{% set version = '1.4.2' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-rviewgraph
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/rviewgraph_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/rviewgraph/rviewgraph_{{ version }}.tar.gz
  sha256: 0fd1439d66e5c043fd20af66ff73b92b79b03de0a24da84bf8b5d817e66e2e98

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: knitr, rmarkdown
requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-rjava
    - openjdk  # [linux]

  run:
    - r-base
    - r-rjava

test:
  requires:
    - openjdk  # [linux]
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('rviewgraph')"           # [not win]
    - "\"%R%\" -e \"library('rviewgraph')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://CRAN.R-project.org/package=rviewgraph
  license: GPL-2
  summary: Provides 'Java' graphical user interfaces for viewing, manipulating and plotting graphs.
    Graphs may be directed or undirected.
  license_family: GPL2
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-2'

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: rviewgraph
# Type: Package
# Title: Animated Graph Layout Viewer
# Version: 1.4.2
# Date: 2023-04-25
# Author: Alun Thomas
# Maintainer: Alun Thomas <Alun.Thomas@utah.edu>
# Description: Provides 'Java' graphical user interfaces for viewing, manipulating and plotting graphs. Graphs may be directed or undirected.
# Suggests: knitr, rmarkdown
# Enhances: igraph, Matrix
# Depends: rJava
# SystemRequirements: Java (>= 8)
# License: GPL-2
# LazyLoad: yes
# Packaged: 2023-05-10 17:35:48 UTC; alun
# Collate: 'onLoad.R' 'rviewgraph-package.R' 'rViewGraph.R' 'vg.R'
# Encoding: UTF-8
# RoxygenNote: 7.1.2
# VignetteBuilder: knitr
# NeedsCompilation: no
# Repository: CRAN
# Date/Publication: 2023-05-10 17:50:02 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
