{% set version = '1.2.1' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-extremis
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/extremis_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/extremis/extremis_{{ version }}.tar.gz
  sha256: 4211dcf33018ddc3f8452aa075779f11e838be5384e4e65cbb596ff3cb62c387

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

requirements:
  build:
    - {{ compiler('c') }}              # [not win]
    - {{ compiler('m2w64_c') }}        # [win]
    - {{ posix }}filesystem        # [win]
    - {{ posix }}make
    - {{ posix }}sed               # [win]
    - {{ posix }}coreutils         # [win]
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-mass
    - r-emplik
    - r-evd

  run:
    - r-base
    - {{native}}gcc-libs         # [win]
    - r-mass
    - r-emplik
    - r-evd

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('extremis')"           # [not win]
    - "\"%R%\" -e \"library('extremis')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://CRAN.R-project.org/package=extremis
  license: GPL-3
  summary: Conducts inference in statistical models for extreme values (de Carvalho et al (2012),
    <doi:10.1080/03610926.2012.709905>; de Carvalho and Davison (2014), <doi:10.1080/01621459.2013.872651>;
    Einmahl et al (2016), <doi:10.1111/rssb.12099>).
  license_family: GPL3
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-3'

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: extremis
# Version: 1.2.1
# Date: 2022-12-06
# Title: Statistics of Extremes
# Description: Conducts inference in statistical models for extreme values (de Carvalho et al (2012), <doi:10.1080/03610926.2012.709905>; de Carvalho and Davison (2014), <doi:10.1080/01621459.2013.872651>; Einmahl et al (2016), <doi:10.1111/rssb.12099>).
# Authors@R: c(person("Miguel", "de Carvalho", role = c("aut", "cre"), email = "miguel.decarvalho@ed.ac.uk"), person("Rodrigo", "Rubio", role = c("aut"), email = "rerubio@mat.uc.cl"), person("Vianey", "Palacios", role = c("aut"), email = "s1875879@ed.ac.uk"))
# Author: Miguel de Carvalho [aut, cre], Rodrigo Rubio [aut], Vianey Palacios [aut]
# Depends: R (>= 3.0.1)
# Maintainer: Miguel de Carvalho <miguel.decarvalho@ed.ac.uk>
# License: GPL (>= 3)
# Repository: CRAN
# Imports: emplik,methods,MASS,evd
# NeedsCompilation: yes
# LazyData: true
# Packaged: 2022-12-08 19:32:09 UTC; kramirez
# Date/Publication: 2022-12-09 14:10:02 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
