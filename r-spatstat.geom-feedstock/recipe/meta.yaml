{% set version = '2.4-0' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-spatstat.geom
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/spatstat.geom_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/spatstat.geom/spatstat.geom_{{ version }}.tar.gz
  sha256: 32b89a409ce87ffe901e4c8720a26cac9629f9816e163c4ad68b7aa012d69e67

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: spatstat.core, spatstat.random, spatstat.linnet, maptools (>= 0.9-9), spatial, fftwtools (>= 0.9-8), spatstat (>= 2.0-0)
requirements:
  build:
    - {{ compiler('c') }}              # [not win]
    - {{ compiler('m2w64_c') }}        # [win]
    - {{ posix }}filesystem        # [win]
    - {{ posix }}make
    - {{ posix }}sed               # [win]
    - {{ posix }}coreutils         # [win]
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-deldir >=1.0_2
    - r-polyclip >=1.10_0
    - r-spatstat.data >=2.0_0
    - r-spatstat.utils >=2.2_0

  run:
    - r-base
    - {{native}}gcc-libs         # [win]
    - r-deldir >=1.0_2
    - r-polyclip >=1.10_0
    - r-spatstat.data >=2.0_0
    - r-spatstat.utils >=2.2_0

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('spatstat.geom')"           # [not win]
    - "\"%R%\" -e \"library('spatstat.geom')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: http://spatstat.org/
  license: GPL-2
  summary: Defines spatial data types and supports geometrical operations on them. Data types
    include point patterns, windows (domains), pixel images, line segment patterns,
    tessellations and hyperframes. Capabilities include creation and manipulation of
    data (using command line or graphical interaction), plotting, geometrical operations
    (rotation, shift, rescale, affine transformation), convex hull, discretisation and
    pixellation, Dirichlet tessellation, Delaunay triangulation, pairwise distances,
    nearest-neighbour distances, distance transform, morphological operations (erosion,
    dilation, closing, opening), quadrat counting, geometrical measurement, geometrical
    covariance, colour maps, calculus on spatial domains, Gaussian blur, level sets
    of images, transects of images, intersections between objects, minimum distance
    matching. (Excludes spatial data on a network, which are supported by the package
    'spatstat.linnet'.)
  license_family: GPL2
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-2'

# The original CRAN metadata for this package was:

# Package: spatstat.geom
# Version: 2.4-0
# Date: 2022-03-28
# Title: Geometrical Functionality of the 'spatstat' Family
# Authors@R: c(person("Adrian", "Baddeley", role = c("aut", "cre"), email = "Adrian.Baddeley@curtin.edu.au"), person("Rolf", "Turner", role = "aut", email="r.turner@auckland.ac.nz"), person("Ege",   "Rubak", role = "aut", email = "rubak@math.aau.dk"), person("Tilman",    "Davies",        role = "ctb"), person("Ute",       "Hahn",          role = "ctb"), person("Abdollah",  "Jalilian",      role = "ctb"), person("Sebastian", "Meyer",         role = "ctb"), person("Suman",     "Rakshit",       role = "ctb"), person("Dominic",   "Schuhmacher",   role = "ctb"), person("Rasmus",    "Waagepetersen", role = "ctb"))
# Maintainer: Adrian Baddeley <Adrian.Baddeley@curtin.edu.au>
# Depends: R (>= 3.5.0), spatstat.data (>= 2.0-0), stats, graphics, grDevices, utils, methods
# Imports: spatstat.utils (>= 2.2-0), deldir (>= 1.0-2), polyclip (>= 1.10-0)
# Suggests: spatstat.core, spatstat.random, spatstat.linnet, maptools (>= 0.9-9), spatial, fftwtools (>= 0.9-8), spatstat (>= 2.0-0)
# Description: Defines spatial data types and supports geometrical operations on them. Data types include point patterns, windows (domains), pixel images, line segment patterns, tessellations and hyperframes. Capabilities include creation and manipulation of data (using command line or graphical interaction), plotting, geometrical operations (rotation, shift, rescale, affine transformation), convex hull, discretisation and pixellation, Dirichlet tessellation, Delaunay triangulation, pairwise distances, nearest-neighbour distances, distance transform, morphological operations (erosion, dilation, closing, opening), quadrat counting, geometrical measurement, geometrical covariance, colour maps, calculus on spatial domains, Gaussian blur, level sets of images, transects of images, intersections between objects, minimum distance matching. (Excludes spatial data on a network, which are supported by the package 'spatstat.linnet'.)
# License: GPL (>= 2)
# URL: http://spatstat.org/
# NeedsCompilation: yes
# ByteCompile: true
# BugReports: https://github.com/spatstat/spatstat.geom/issues
# Packaged: 2022-03-28 09:10:01 UTC; adrian
# Author: Adrian Baddeley [aut, cre], Rolf Turner [aut], Ege Rubak [aut], Tilman Davies [ctb], Ute Hahn [ctb], Abdollah Jalilian [ctb], Sebastian Meyer [ctb], Suman Rakshit [ctb], Dominic Schuhmacher [ctb], Rasmus Waagepetersen [ctb]
# Repository: CRAN
# Date/Publication: 2022-03-29 07:20:08 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
