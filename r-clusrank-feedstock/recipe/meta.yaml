{% set version = '1.0-3' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-clusrank
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/clusrank_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/clusrank/clusrank_{{ version }}.tar.gz
  sha256: 8c0526a7bafe977bf7d3debfc2c8ed52344bd1249ed7695182728e5bd77bbe54

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

requirements:
  build:
    - {{ compiler('c') }}              # [not win]
    - {{ compiler('m2w64_c') }}        # [win]
    - {{ compiler('cxx') }}            # [not win]
    - {{ compiler('m2w64_cxx') }}      # [win]
    - {{ posix }}filesystem        # [win]
    - {{ posix }}make
    - {{ posix }}sed               # [win]
    - {{ posix }}coreutils         # [win]
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-mass
    - r-rcpp >=1.0.5

  run:
    - r-base
    - {{native}}gcc-libs         # [win]
    - r-mass
    - r-rcpp >=1.0.5

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('clusrank')"           # [not win]
    - "\"%R%\" -e \"library('clusrank')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://github.com/wenjie2wang/clusrank
  license: GPL-3
  summary: Non-parametric tests (Wilcoxon rank sum test and Wilcoxon signed rank test) for clustered
    data documented in Jiang et. al (2020) <doi:10.18637/jss.v096.i06>.
  license_family: GPL3
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-3'

extra:
  recipe-maintainers:
    - katietz

# The original CRAN metadata for this package was:

# Package: clusrank
# Version: 1.0-3
# Title: Wilcoxon Rank Tests for Clustered Data
# Description: Non-parametric tests (Wilcoxon rank sum test and Wilcoxon signed rank test) for clustered data documented in Jiang et. al (2020) <doi:10.18637/jss.v096.i06>.
# Authors@R: c(person(given = "Wenjie", family = "Wang", role = c("cre", "ctb"), email = "wang@wwenjie.org", comment = c(ORCID = "0000-0003-0363-3180")), person(given = "Yujing", family = "Jiang", email = "yujing.jiang@uconn.edu", role = "aut"), person(given = "Mei-Ling Ting", family = "Lee", role = "ctb"), person(given = "Jun", family = "Yan", role = "ctb", email = "jun.yan@uconn.edu"))
# License: GPL (>= 3)
# Depends: R (>= 4.0)
# Imports: stats, MASS, Rcpp (>= 1.0.5)
# LinkingTo: Rcpp
# LazyData: true
# URL: https://github.com/wenjie2wang/clusrank
# BugReports: https://github.com/wenjie2wang/clusrank/issues
# Encoding: UTF-8
# RoxygenNote: 7.2.1
# NeedsCompilation: yes
# Packaged: 2022-08-12 02:46:53 UTC; wenjie
# Author: Wenjie Wang [cre, ctb] (<https://orcid.org/0000-0003-0363-3180>), Yujing Jiang [aut], Mei-Ling Ting Lee [ctb], Jun Yan [ctb]
# Maintainer: Wenjie Wang <wang@wwenjie.org>
# Repository: CRAN
# Date/Publication: 2022-08-12 06:10:01 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
