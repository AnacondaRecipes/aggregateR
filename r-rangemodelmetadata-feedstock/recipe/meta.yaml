{% set version = '0.1.5' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-rangemodelmetadata
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/rangeModelMetadata_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/rangeModelMetadata/rangeModelMetadata_{{ version }}.tar.gz
  sha256: 289620500522d489aafbb03c85f68182ef0a6701fed5f9d09b55fae337e2647b

build:
  merge_build_host: True  # [win]
  # If this is a new build for the same version, increment the build number.
  number: 0
  # no skip
  noarch: generic

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: dismo, googlesheets4, knitr, rmarkdown, rgbif, dplyr, sf
requirements:
  build:
    - {{ posix }}zip               # [win]

  host:
    - r-base
    - r-bien
    - r-shiny
    - r-spocc
    - r-terra

  run:
    - r-base
    - r-bien
    - r-shiny
    - r-spocc
    - r-terra

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('rangeModelMetadata')"           # [not win]
    - "\"%R%\" -e \"library('rangeModelMetadata')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://CRAN.R-project.org/package=rangeModelMetadata
  license: GPL-3
  summary: 'Range Modeling Metadata Standards (RMMS) address three challenges: they (i) are designed
    for convenience to encourage use, (ii) accommodate a wide variety of applications,
    and (iii) are extensible to allow the community of range modelers to steer it as
    needed. RMMS are based on a data dictionary that specifies a hierarchical structure
    to catalog different aspects of the range modeling process. The dictionary balances
    a constrained, minimalist vocabulary to improve standardization with flexibility
    for users to provide their own values. Merow et al. (2019) <DOI:10.1111/geb.12993>
    describe the standards in more detail. Note that users who prefer to use the R package
    ''ecospat'' can obtain it from <https://github.com/ecospat/ecospat>.'
  license_family: GPL3
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-3'

# The original CRAN metadata for this package was:

# Package: rangeModelMetadata
# Type: Package
# Title: Provides Templates for Metadata Files Associated with Species Range Models
# Version: 0.1.5
# Authors@R: c(person('Cory','Merow',email='cory.merow@gmail.com',role=c('aut','cre')), person('Brian', 'Maitner',role=c('aut')), person('Hannah', 'Owens',role=c('aut')), person('Jamie', 'Kass',role=c('aut')), person('Brian', 'Enquist',role=c('aut')), person('Rob', 'Guralnik',role=c('aut')), person('Damaris', 'Zurrell',role=c('aut')), person('Christian', 'Koenig',role=c('aut')))
# Maintainer: Cory Merow <cory.merow@gmail.com>
# Description: Range Modeling Metadata Standards (RMMS) address three challenges: they (i) are designed for convenience to encourage use, (ii) accommodate a wide variety of applications, and (iii) are extensible to allow the community of range modelers to steer it as needed. RMMS are based on a data dictionary that specifies a hierarchical structure to catalog different aspects of the range modeling process. The dictionary balances a constrained, minimalist vocabulary to improve standardization with flexibility for users to provide their own values. Merow et al. (2019) <DOI:10.1111/geb.12993> describe the standards in more detail. Note that users who prefer to use the R package 'ecospat' can obtain it from <https://github.com/ecospat/ecospat>.
# License: GPL (>= 3)
# Encoding: UTF-8
# RoxygenNote: 7.2.3
# Depends: R (>= 3.5.0)
# Suggests: dismo, googlesheets4, knitr, rmarkdown, rgbif, dplyr, sf
# Imports: shiny, spocc, utils, BIEN, terra
# VignetteBuilder: knitr
# NeedsCompilation: no
# Packaged: 2023-10-16 20:04:44 UTC; ctg
# Author: Cory Merow [aut, cre], Brian Maitner [aut], Hannah Owens [aut], Jamie Kass [aut], Brian Enquist [aut], Rob Guralnik [aut], Damaris Zurrell [aut], Christian Koenig [aut]
# Repository: CRAN
# Date/Publication: 2023-10-16 22:50:02 UTC

# See
# https://docs.conda.io/projects/conda-build for
# more information about meta.yaml
